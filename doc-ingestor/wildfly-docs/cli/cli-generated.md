## syntax of the operation to get the `add-component-via-interceptor` attribute of the `iiop-openjdk` subsystem
* Retrieve the `add-component-via-interceptor` attribute value of the `iiop-openjdk` subsystem
* `add-component-via-interceptor` attribute description: indicates whether `ssl` components should be added by an `ior` interceptor (on) or not (off).
* operation: `/subsystem=iiop-openjdk:read-attribute(name=add-component-via-interceptor)`

## syntax of the operation to get the `auth-method` attribute of the `iiop-openjdk` subsystem
* Retrieve the `auth-method` attribute value of the `iiop-openjdk` subsystem
* `auth-method` attribute description: the authentication method. valid values are 'none' and '`username_password`'.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=auth-method)`

## syntax of the operation to get the `authentication-context` attribute of the `iiop-openjdk` subsystem
* Retrieve the `authentication-context` attribute value of the `iiop-openjdk` subsystem
* `authentication-context` attribute description: the name of the authentication context used when the security initializer is set to 'elytron'.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=authentication-context)`

## syntax of the operation to get the `caller-propagation` attribute of the `iiop-openjdk` subsystem
* Retrieve the `caller-propagation` attribute value of the `iiop-openjdk` subsystem
* `caller-propagation` attribute description: indicates whether the caller identity should be propagated in the `sas` context or not. valid values are 'none' and 'supported'.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=caller-propagation)`

## syntax of the operation to get the `client-requires` attribute of the `iiop-openjdk` subsystem
* Retrieve the `client-requires` attribute value of the `iiop-openjdk` subsystem
* `client-requires` attribute description: value that indicates the client `ssl` required parameters (none, `serverauth`, `clientauth`, `mutualauth`).
* operation: `/subsystem=iiop-openjdk:read-attribute(name=client-requires)`

## syntax of the operation to get the `client-requires-ssl` attribute of the `iiop-openjdk` subsystem
* Retrieve the `client-requires-ssl` attribute value of the `iiop-openjdk` subsystem
* `client-requires-ssl` attribute description: indicates whether `iiop` connections from the server require `ssl`.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=client-requires-ssl)`

## syntax of the operation to get the `client-ssl-context` attribute of the `iiop-openjdk` subsystem
* Retrieve the `client-ssl-context` attribute value of the `iiop-openjdk` subsystem
* `client-ssl-context` attribute description: the name of the `ssl` context used to create client side `ssl` sockets.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=client-ssl-context)`

## syntax of the operation to get the `client-supports` attribute of the `iiop-openjdk` subsystem
* Retrieve the `client-supports` attribute value of the `iiop-openjdk` subsystem
* `client-supports` attribute description: value that indicates the client `ssl` supported parameters (none, `serverauth`, `clientauth`, `mutualauth`).
* operation: `/subsystem=iiop-openjdk:read-attribute(name=client-supports)`

## syntax of the operation to get the `confidentiality` attribute of the `iiop-openjdk` subsystem
* Retrieve the `confidentiality` attribute value of the `iiop-openjdk` subsystem
* `confidentiality` attribute description: indicates whether the transport must require confidentiality protection or not. valid values are 'none', 'supported' and 'required'.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=confidentiality)`

## syntax of the operation to get the `detect-misordering` attribute of the `iiop-openjdk` subsystem
* Retrieve the `detect-misordering` attribute value of the `iiop-openjdk` subsystem
* `detect-misordering` attribute description: indicates whether the transport must require `misordering` detection or not. valid values are 'none', 'supported' and 'required'.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=detect-misordering)`

## syntax of the operation to get the `detect-replay` attribute of the `iiop-openjdk` subsystem
* Retrieve the `detect-replay` attribute value of the `iiop-openjdk` subsystem
* `detect-replay` attribute description: indicates whether the transport must require replay detection or not. valid values are 'none', 'supported' and 'required'.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=detect-replay)`

## syntax of the operation to get the `export-corbaloc` attribute of the `iiop-openjdk` subsystem
* Retrieve the `export-corbaloc` attribute value of the `iiop-openjdk` subsystem
* `export-corbaloc` attribute description: indicates whether the root context should be exported as `corbaloc::address:port/nameservice` (on) or not (off).
* operation: `/subsystem=iiop-openjdk:read-attribute(name=export-corbaloc)`

## syntax of the operation to get the `giop-version` attribute of the `iiop-openjdk` subsystem
* Retrieve the `giop-version` attribute value of the `iiop-openjdk` subsystem
* `giop-version` attribute description: the `giop` version to be used.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=giop-version)`

## syntax of the operation to get the `high-water-mark` attribute of the `iiop-openjdk` subsystem
* Retrieve the `high-water-mark` attribute value of the `iiop-openjdk` subsystem
* `high-water-mark` attribute description: `tcp` connection cache parameter. each time the number of connections exceeds this value orb tries to reclaim connections. number of reclaimed connections is specified by `tcp-number-to-reclaim` property. if this property is not set then it is configured as `openjdk` orb default.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=high-water-mark)`

## syntax of the operation to get the `integrity` attribute of the `iiop-openjdk` subsystem
* Retrieve the `integrity` attribute value of the `iiop-openjdk` subsystem
* `integrity` attribute description: indicates whether the transport must require integrity protection or not. valid values are 'none', 'supported' and 'required'.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=integrity)`

## syntax of the operation to get the `iona` attribute of the `iiop-openjdk` subsystem
* Retrieve the `iona` attribute value of the `iiop-openjdk` subsystem
* `iona` attribute description: indicates whether `interoperability` with `iona's` asp is enabled.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=iona)`

## syntax of the operation to get the `number-to-reclaim` attribute of the `iiop-openjdk` subsystem
* Retrieve the `number-to-reclaim` attribute value of the `iiop-openjdk` subsystem
* `number-to-reclaim` attribute description: `tcp` connection cache parameter. each time number of connections exceeds `tcp-high-water-mark` property then orb tries to reclaim connections. number of reclaimed connections is specified by this property. if it is not set then it is configured as `openjdk` orb default.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=number-to-reclaim)`

## syntax of the operation to get the `persistent-server-id` attribute of the `iiop-openjdk` subsystem
* Retrieve the `persistent-server-id` attribute value of the `iiop-openjdk` subsystem
* `persistent-server-id` attribute description: persistent id of the server. persistent object references are valid across many `activactions` of the server and they identify it using this property. as a result of that: many activations of the same server should have this property set to the same value, different server instances running on the same host should have different server ids.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=persistent-server-id)`

## syntax of the operation to get the `properties` attribute of the `iiop-openjdk` subsystem
* Retrieve the `properties` attribute value of the `iiop-openjdk` subsystem
* `properties` attribute description: a list of generic `key/value` properties.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=properties)`

## syntax of the operation to get the `realm` attribute of the `iiop-openjdk` subsystem
* Retrieve the `realm` attribute value of the `iiop-openjdk` subsystem
* `realm` attribute description: the authentication service (as) realm name.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=realm)`

## syntax of the operation to get the `required` attribute of the `iiop-openjdk` subsystem
* Retrieve the `required` attribute value of the `iiop-openjdk` subsystem
* `required` attribute description: indicates if authentication is required (true) or not (false).
* operation: `/subsystem=iiop-openjdk:read-attribute(name=required)`

## syntax of the operation to get the `root-context` attribute of the `iiop-openjdk` subsystem
* Retrieve the `root-context` attribute value of the `iiop-openjdk` subsystem
* `root-context` attribute description: the naming service root context.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=root-context)`

## syntax of the operation to get the `security` attribute of the `iiop-openjdk` subsystem
* Retrieve the `security` attribute value of the `iiop-openjdk` subsystem
* `security` attribute description: `iiop-openjdk.security=indicates` whether the security interceptors are to be installed (elytron) or not (none). identity and client are legacy security configurations which are valid for compatibility reasons but are no longer supported.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=security)`

## syntax of the operation to get the `security-domain` attribute of the `iiop-openjdk` subsystem
* Retrieve the `security-domain` attribute value of the `iiop-openjdk` subsystem
* `security-domain` attribute description: the name of the security domain that holds the key and trust stores that will be used to establish `ssl` connections.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=security-domain)`

## syntax of the operation to get the `server-requires` attribute of the `iiop-openjdk` subsystem
* Retrieve the `server-requires` attribute value of the `iiop-openjdk` subsystem
* `server-requires` attribute description: value that indicates the server `ssl` required parameters (none, `serverauth`, `clientauth`, `mutualauth`).
* operation: `/subsystem=iiop-openjdk:read-attribute(name=server-requires)`

## syntax of the operation to get the `server-requires-ssl` attribute of the `iiop-openjdk` subsystem
* Retrieve the `server-requires-ssl` attribute value of the `iiop-openjdk` subsystem
* `server-requires-ssl` attribute description: indicates whether `iiop` connections to the server require `ssl`.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=server-requires-ssl)`

## syntax of the operation to get the `server-ssl-context` attribute of the `iiop-openjdk` subsystem
* Retrieve the `server-ssl-context` attribute value of the `iiop-openjdk` subsystem
* `server-ssl-context` attribute description: the name of the `ssl` context used to create server side `ssl` sockets.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=server-ssl-context)`

## syntax of the operation to get the `server-supports` attribute of the `iiop-openjdk` subsystem
* Retrieve the `server-supports` attribute value of the `iiop-openjdk` subsystem
* `server-supports` attribute description: value that indicates the server `ssl` supported parameters (none, `serverauth`, `clientauth`, `mutualauth`).
* operation: `/subsystem=iiop-openjdk:read-attribute(name=server-supports)`

## syntax of the operation to get the `socket-binding` attribute of the `iiop-openjdk` subsystem
* Retrieve the `socket-binding` attribute value of the `iiop-openjdk` subsystem
* `socket-binding` attribute description: the name of the socket binding configuration that specifies the orb port. at least one of `socket-binding`, `ssl-socket-binding` parameters have to be configured.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=socket-binding)`

## syntax of the operation to get the `ssl-socket-binding` attribute of the `iiop-openjdk` subsystem
* Retrieve the `ssl-socket-binding` attribute value of the `iiop-openjdk` subsystem
* `ssl-socket-binding` attribute description: the name of the socket binding configuration that specifies the orb `ssl` port. at least one of `socket-binding`, `ssl-socket-binding` parameters have to be configured.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=ssl-socket-binding)`

## syntax of the operation to get the `support-ssl` attribute of the `iiop-openjdk` subsystem
* Retrieve the `support-ssl` attribute value of the `iiop-openjdk` subsystem
* `support-ssl` attribute description: indicates whether `ssl` is to be supported (on) or not (off).
* operation: `/subsystem=iiop-openjdk:read-attribute(name=support-ssl)`

## syntax of the operation to get the `transactions` attribute of the `iiop-openjdk` subsystem
* Retrieve the `transactions` attribute value of the `iiop-openjdk` subsystem
* `transactions` attribute description: indicates whether the transactions interceptors are to be installed (full or spec) or not (none). the value 'full' enabled `jts` while 'spec' enables a spec compliant mode (non `jts`) that rejects incoming transaction contexts.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=transactions)`

## syntax of the operation to get the `trust-in-client` attribute of the `iiop-openjdk` subsystem
* Retrieve the `trust-in-client` attribute value of the `iiop-openjdk` subsystem
* `trust-in-client` attribute description: indicates if the transport must require trust in client to be established. valid values are 'none', 'supported' and 'required'.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=trust-in-client)`

## syntax of the operation to get the `trust-in-target` attribute of the `iiop-openjdk` subsystem
* Retrieve the `trust-in-target` attribute value of the `iiop-openjdk` subsystem
* `trust-in-target` attribute description: indicates if the transport must require trust in target to be established. valid values are 'none' and 'supported'.
* operation: `/subsystem=iiop-openjdk:read-attribute(name=trust-in-target)`

## syntax of the operation to get the `batch-delay` attribute of the `opentelemetry` subsystem
* Retrieve the `batch-delay` attribute value of the `opentelemetry` subsystem
* `batch-delay` attribute description: the interval, in milliseconds, between two consecutive exports. default is `5000`.
* operation: `/subsystem=opentelemetry:read-attribute(name=batch-delay)`

## syntax of the operation to get the `endpoint` attribute of the `opentelemetry` subsystem
* Retrieve the `endpoint` attribute value of the `opentelemetry` subsystem
* `endpoint` attribute description: the `url` for the exporter endpoint.
* operation: `/subsystem=opentelemetry:read-attribute(name=endpoint)`

## syntax of the operation to get the `export-timeout` attribute of the `opentelemetry` subsystem
* Retrieve the `export-timeout` attribute value of the `opentelemetry` subsystem
* `export-timeout` attribute description: the maximum allowed time, in milliseconds, to export data.
* operation: `/subsystem=opentelemetry:read-attribute(name=export-timeout)`

## syntax of the operation to get the `exporter-type` attribute of the `opentelemetry` subsystem
* Retrieve the `exporter-type` attribute value of the `opentelemetry` subsystem
* `exporter-type` attribute description: the name of the exporter to use
* operation: `/subsystem=opentelemetry:read-attribute(name=exporter-type)`

## syntax of the operation to get the `max-export-batch-size` attribute of the `opentelemetry` subsystem
* Retrieve the `max-export-batch-size` attribute value of the `opentelemetry` subsystem
* `max-export-batch-size` attribute description: the maximum number of traces to be exported in a given batch.
* operation: `/subsystem=opentelemetry:read-attribute(name=max-export-batch-size)`

## syntax of the operation to get the `max-queue-size` attribute of the `opentelemetry` subsystem
* Retrieve the `max-queue-size` attribute value of the `opentelemetry` subsystem
* `max-queue-size` attribute description: the maximum number of traces in the queue before they are exported.
* operation: `/subsystem=opentelemetry:read-attribute(name=max-queue-size)`

## syntax of the operation to get the `ratio` attribute of the `opentelemetry` subsystem
* Retrieve the `ratio` attribute value of the `opentelemetry` subsystem
* `ratio` attribute description: the ratio amount to use for the ratio sampler
* operation: `/subsystem=opentelemetry:read-attribute(name=ratio)`

## syntax of the operation to get the `sampler-type` attribute of the `opentelemetry` subsystem
* Retrieve the `sampler-type` attribute value of the `opentelemetry` subsystem
* `sampler-type` attribute description: the sampling strategy to use
* operation: `/subsystem=opentelemetry:read-attribute(name=sampler-type)`

## syntax of the operation to get the `service-name` attribute of the `opentelemetry` subsystem
* Retrieve the `service-name` attribute value of the `opentelemetry` subsystem
* `service-name` attribute description: service name reported to the trace collector
* operation: `/subsystem=opentelemetry:read-attribute(name=service-name)`

## syntax of the operation to get the `span-processor-type` attribute of the `opentelemetry` subsystem
* Retrieve the `span-processor-type` attribute value of the `opentelemetry` subsystem
* `span-processor-type` attribute description: the type of processor to use. valid values are 'simple' and 'batch'.
* operation: `/subsystem=opentelemetry:read-attribute(name=span-processor-type)`

## syntax of the operation to get the `default-datasource` attribute of the `jpa` subsystem
* Retrieve the `default-datasource` attribute value of the `jpa` subsystem
* `default-datasource` attribute description: the name of the default global `datasource`.
* operation: `/subsystem=jpa:read-attribute(name=default-datasource)`

## syntax of the operation to get the `default-extended-persistence-inheritance` attribute of the `jpa` subsystem
* Retrieve the `default-extended-persistence-inheritance` attribute value of the `jpa` subsystem
* `default-extended-persistence-inheritance` attribute description: controls how jakarta persistence extended persistence context (`xpc`) inheritance is performed. 'deep' shares the extended persistence context at top bean level.  'shallow' the extended `persistece` context is only shared with the parent bean (never with sibling beans).
* operation: `/subsystem=jpa:read-attribute(name=default-extended-persistence-inheritance)`

## syntax of the operation to get the `global-client-scheduled-thread-pool-active-count` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-scheduled-thread-pool-active-count` attribute value of the `messaging-activemq` subsystem
* `global-client-scheduled-thread-pool-active-count` attribute description: the approximate number of threads used by all `activemq` clients that are actively executing tasks.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-scheduled-thread-pool-active-count)`

## syntax of the operation to get the `global-client-scheduled-thread-pool-completed-task-count` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-scheduled-thread-pool-completed-task-count` attribute value of the `messaging-activemq` subsystem
* `global-client-scheduled-thread-pool-completed-task-count` attribute description: the approximate number of tasks that have been executed by all `activemq` clients.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-scheduled-thread-pool-completed-task-count)`

## syntax of the operation to get the `global-client-scheduled-thread-pool-current-thread-count` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-scheduled-thread-pool-current-thread-count` attribute value of the `messaging-activemq` subsystem
* `global-client-scheduled-thread-pool-current-thread-count` attribute description: the current number of threads in the pool used by all `activemq` clients.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-scheduled-thread-pool-current-thread-count)`

## syntax of the operation to get the `global-client-scheduled-thread-pool-keepalive-time` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-scheduled-thread-pool-keepalive-time` attribute value of the `messaging-activemq` subsystem
* `global-client-scheduled-thread-pool-keepalive-time` attribute description: the amount of time that pool threads should be kept running when idle.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-scheduled-thread-pool-keepalive-time)`

## syntax of the operation to get the `global-client-scheduled-thread-pool-largest-thread-count` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-scheduled-thread-pool-largest-thread-count` attribute value of the `messaging-activemq` subsystem
* `global-client-scheduled-thread-pool-largest-thread-count` attribute description: the largest number of threads that have ever simultaneously been in the pool used by all `activemq` clients.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-scheduled-thread-pool-largest-thread-count)`

## syntax of the operation to get the `global-client-scheduled-thread-pool-max-size` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-scheduled-thread-pool-max-size` attribute value of the `messaging-activemq` subsystem
* `global-client-scheduled-thread-pool-max-size` attribute description: maximum size of the pool of threads used by all `activemq` clients running inside this server. if the attribute is undefined (by default), `activemq` will configure it to be `8` x the number of available processors.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-scheduled-thread-pool-max-size)`

## syntax of the operation to get the `global-client-scheduled-thread-pool-task-count` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-scheduled-thread-pool-task-count` attribute value of the `messaging-activemq` subsystem
* `global-client-scheduled-thread-pool-task-count` attribute description: the approximate total number of tasks that have ever been scheduled by all `activemq` clients.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-scheduled-thread-pool-task-count)`

## syntax of the operation to get the `global-client-thread-pool-active-count` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-thread-pool-active-count` attribute value of the `messaging-activemq` subsystem
* `global-client-thread-pool-active-count` attribute description: the approximate number of threads used by all `activemq` clients that are actively executing tasks.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-thread-pool-active-count)`

## syntax of the operation to get the `global-client-thread-pool-completed-task-count` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-thread-pool-completed-task-count` attribute value of the `messaging-activemq` subsystem
* `global-client-thread-pool-completed-task-count` attribute description: the approximate number of tasks that have been executed by all `activemq` clients.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-thread-pool-completed-task-count)`

## syntax of the operation to get the `global-client-thread-pool-current-thread-count` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-thread-pool-current-thread-count` attribute value of the `messaging-activemq` subsystem
* `global-client-thread-pool-current-thread-count` attribute description: the current number of threads in the pool used by all `activemq` clients.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-thread-pool-current-thread-count)`

## syntax of the operation to get the `global-client-thread-pool-keepalive-time` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-thread-pool-keepalive-time` attribute value of the `messaging-activemq` subsystem
* `global-client-thread-pool-keepalive-time` attribute description: the amount of time that pool threads should be kept running when idle.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-thread-pool-keepalive-time)`

## syntax of the operation to get the `global-client-thread-pool-largest-thread-count` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-thread-pool-largest-thread-count` attribute value of the `messaging-activemq` subsystem
* `global-client-thread-pool-largest-thread-count` attribute description: the largest number of threads that have ever simultaneously been in the pool used by all `activemq` clients.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-thread-pool-largest-thread-count)`

## syntax of the operation to get the `global-client-thread-pool-max-size` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-thread-pool-max-size` attribute value of the `messaging-activemq` subsystem
* `global-client-thread-pool-max-size` attribute description: maximum size of the pool of scheduled threads used by all `activemq` clients running inside this server.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-thread-pool-max-size)`

## syntax of the operation to get the `global-client-thread-pool-task-count` attribute of the `messaging-activemq` subsystem
* Retrieve the `global-client-thread-pool-task-count` attribute value of the `messaging-activemq` subsystem
* `global-client-thread-pool-task-count` attribute description: the approximate total number of tasks that have ever been scheduled by all `activemq` clients.
* operation: `/subsystem=messaging-activemq:read-attribute(name=global-client-thread-pool-task-count)`

## syntax of the operation to get a `discovery-group` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/discovery-group=<discovery-group name>:read-resource()`
* To get the list of all the `discovery-group` resource of the `messaging-activemq` subsystem use '*' for `<discovery-group name>`.

## syntax of the operation to get the `initial-wait-timeout` attribute of the `discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-wait-timeout` attribute value of the `discovery-group` resource of the `messaging-activemq` subsystem
* `initial-wait-timeout` attribute description: period, in ms, to wait for an initial broadcast to give us at least one node in the cluster.
* operation: `/subsystem=messaging-activemq/discovery-group=<discovery-group name>:read-attribute(name=initial-wait-timeout)`

## syntax of the operation to get the `jgroups-channel` attribute of the `discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-channel` attribute value of the `discovery-group` resource of the `messaging-activemq` subsystem
* `jgroups-channel` attribute description: references the name of a `jgroups` channel.  if undefined, the default channel will be used.
* operation: `/subsystem=messaging-activemq/discovery-group=<discovery-group name>:read-attribute(name=jgroups-channel)`

## syntax of the operation to get the `jgroups-cluster` attribute of the `discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-cluster` attribute value of the `discovery-group` resource of the `messaging-activemq` subsystem
* `jgroups-cluster` attribute description: the logical cluster name.
* operation: `/subsystem=messaging-activemq/discovery-group=<discovery-group name>:read-attribute(name=jgroups-cluster)`

## syntax of the operation to get the `jgroups-stack` attribute of the `discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-stack` attribute value of the `discovery-group` resource of the `messaging-activemq` subsystem
* `jgroups-stack` attribute description: references the name of a `jgroups` channel factory.
* operation: `/subsystem=messaging-activemq/discovery-group=<discovery-group name>:read-attribute(name=jgroups-stack)`

## syntax of the operation to get the `refresh-timeout` attribute of the `discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `refresh-timeout` attribute value of the `discovery-group` resource of the `messaging-activemq` subsystem
* `refresh-timeout` attribute description: period the discovery group waits after receiving the last broadcast from a particular server before removing that `server's` connector pair entry from its list.
* operation: `/subsystem=messaging-activemq/discovery-group=<discovery-group name>:read-attribute(name=refresh-timeout)`

## syntax of the operation to get the `socket-binding` attribute of the `discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `discovery-group` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the discovery group socket binding.
* operation: `/subsystem=messaging-activemq/discovery-group=<discovery-group name>:read-attribute(name=socket-binding)`

## syntax of the operation to get a `jgroups-discovery-group` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/jgroups-discovery-group=<jgroups-discovery-group name>:read-resource()`
* To get the list of all the `jgroups-discovery-group` resource of the `messaging-activemq` subsystem use '*' for `<jgroups-discovery-group name>`.

## syntax of the operation to get the `initial-wait-timeout` attribute of the `jgroups-discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-wait-timeout` attribute value of the `jgroups-discovery-group` resource of the `messaging-activemq` subsystem
* `initial-wait-timeout` attribute description: period, in ms, to wait for an initial broadcast to give us at least one node in the cluster.
* operation: `/subsystem=messaging-activemq/jgroups-discovery-group=<jgroups-discovery-group name>:read-attribute(name=initial-wait-timeout)`

## syntax of the operation to get the `jgroups-channel` attribute of the `jgroups-discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-channel` attribute value of the `jgroups-discovery-group` resource of the `messaging-activemq` subsystem
* `jgroups-channel` attribute description: references the name of a `jgroups` channel.  if undefined, the default channel will be used.
* operation: `/subsystem=messaging-activemq/jgroups-discovery-group=<jgroups-discovery-group name>:read-attribute(name=jgroups-channel)`

## syntax of the operation to get the `jgroups-cluster` attribute of the `jgroups-discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-cluster` attribute value of the `jgroups-discovery-group` resource of the `messaging-activemq` subsystem
* `jgroups-cluster` attribute description: the logical cluster name.
* operation: `/subsystem=messaging-activemq/jgroups-discovery-group=<jgroups-discovery-group name>:read-attribute(name=jgroups-cluster)`

## syntax of the operation to get the `jgroups-stack` attribute of the `jgroups-discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-stack` attribute value of the `jgroups-discovery-group` resource of the `messaging-activemq` subsystem
* `jgroups-stack` attribute description: references the name of a `jgroups` channel factory.
* operation: `/subsystem=messaging-activemq/jgroups-discovery-group=<jgroups-discovery-group name>:read-attribute(name=jgroups-stack)`

## syntax of the operation to get the `refresh-timeout` attribute of the `jgroups-discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `refresh-timeout` attribute value of the `jgroups-discovery-group` resource of the `messaging-activemq` subsystem
* `refresh-timeout` attribute description: period the discovery group waits after receiving the last broadcast from a particular server before removing that `server's` connector pair entry from its list.
* operation: `/subsystem=messaging-activemq/jgroups-discovery-group=<jgroups-discovery-group name>:read-attribute(name=refresh-timeout)`

## syntax of the operation to get a `connection-factory` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-resource()`
* To get the list of all the `connection-factory` resource of the `messaging-activemq` subsystem use '*' for `<connection-factory name>`.

## syntax of the operation to get the `auto-group` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `auto-group` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `auto-group` attribute description: whether or not message grouping is automatically used
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=auto-group)`

## syntax of the operation to get the `block-on-acknowledge` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-acknowledge` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `block-on-acknowledge` attribute description: true to set block on acknowledge.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=block-on-acknowledge)`

## syntax of the operation to get the `block-on-durable-send` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-durable-send` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `block-on-durable-send` attribute description: true to set block on durable send.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=block-on-durable-send)`

## syntax of the operation to get the `block-on-non-durable-send` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-non-durable-send` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `block-on-non-durable-send` attribute description: true to set block on non durable send.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=block-on-non-durable-send)`

## syntax of the operation to get the `cache-large-message-client` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `cache-large-message-client` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `cache-large-message-client` attribute description: true to cache large messages.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=cache-large-message-client)`

## syntax of the operation to get the `call-failover-timeout` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `call-failover-timeout` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `call-failover-timeout` attribute description: the timeout to use when fail over is in process (in ms).
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=call-failover-timeout)`

## syntax of the operation to get the `call-timeout` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `call-timeout` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `call-timeout` attribute description: the call time out.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=call-timeout)`

## syntax of the operation to get the `client-failure-check-period` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `client-failure-check-period` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `client-failure-check-period` attribute description: the client failure check period.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=client-failure-check-period)`

## syntax of the operation to get the `client-id` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `client-id` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `client-id` attribute description: the client id.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=client-id)`

## syntax of the operation to get the `compress-large-messages` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `compress-large-messages` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `compress-large-messages` attribute description: whether large messages should be compressed.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=compress-large-messages)`

## syntax of the operation to get the `confirmation-window-size` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `confirmation-window-size` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `confirmation-window-size` attribute description: the confirmation window size.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=confirmation-window-size)`

## syntax of the operation to get the `connection-load-balancing-policy-class-name` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `connection-load-balancing-policy-class-name` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `connection-load-balancing-policy-class-name` attribute description: name of a class implementing a `client-side` load balancing policy that a client can use to load balance sessions across different nodes in a cluster.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=connection-load-balancing-policy-class-name)`

## syntax of the operation to get the `connection-ttl` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `connection-ttl` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `connection-ttl` attribute description: the connection `ttl`.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=connection-ttl)`

## syntax of the operation to get the `connectors` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `connectors` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `connectors` attribute description: defines the connectors. these are stored in a map by connector name (with an undefined value). it is possible to pass a list of connector names when writing this attribute.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=connectors)`

## syntax of the operation to get the `consumer-max-rate` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `consumer-max-rate` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `consumer-max-rate` attribute description: the consumer max rate.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=consumer-max-rate)`

## syntax of the operation to get the `consumer-window-size` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `consumer-window-size` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `consumer-window-size` attribute description: the consumer window size.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=consumer-window-size)`

## syntax of the operation to get the `deserialization-allow-list` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-allow-list` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `deserialization-allow-list` attribute description: a list of class names (separated by `whitespaces`) that are allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=deserialization-allow-list)`

## syntax of the operation to get the `deserialization-black-list` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-black-list` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `deserialization-black-list` attribute description: a list of class names (separated by `whitespaces`) that are not allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=deserialization-black-list)`

## syntax of the operation to get the `deserialization-block-list` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-block-list` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `deserialization-block-list` attribute description: a list of class names (separated by `whitespaces`) that are not allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=deserialization-block-list)`

## syntax of the operation to get the `deserialization-white-list` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-white-list` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `deserialization-white-list` attribute description: a list of class names (separated by `whitespaces`) that are allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=deserialization-white-list)`

## syntax of the operation to get the `discovery-group` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `discovery-group` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `discovery-group` attribute description: the discovery group name.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=discovery-group)`

## syntax of the operation to get the `dups-ok-batch-size` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `dups-ok-batch-size` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `dups-ok-batch-size` attribute description: the dups ok batch size.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=dups-ok-batch-size)`

## syntax of the operation to get the `enable-amq1-prefix` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `enable-amq1-prefix` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `enable-amq1-prefix` attribute description: enable the use of `activemq` `1.5.x` prefixes in the addresses.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=enable-amq1-prefix)`

## syntax of the operation to get the `entries` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `entries` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `entries` attribute description: the `jndi` names the connection factory should be bound to.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=entries)`

## syntax of the operation to get the `factory-type` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `factory-type` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `factory-type` attribute description: the type of connection factory.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=factory-type)`

## syntax of the operation to get the `failover-on-initial-connection` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `failover-on-initial-connection` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `failover-on-initial-connection` attribute description: true to fail over on initial connection.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=failover-on-initial-connection)`

## syntax of the operation to get the `group-id` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `group-id` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `group-id` attribute description: the group id.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=group-id)`

## syntax of the operation to get the `ha` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `ha` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `ha` attribute description: whether the connection factory supports high availability.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=ha)`

## syntax of the operation to get the `initial-message-packet-size` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-message-packet-size` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `initial-message-packet-size` attribute description: the initial size of messages created through this factory.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=initial-message-packet-size)`

## syntax of the operation to get the `max-retry-interval` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `max-retry-interval` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `max-retry-interval` attribute description: the max retry interval.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=max-retry-interval)`

## syntax of the operation to get the `min-large-message-size` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `min-large-message-size` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `min-large-message-size` attribute description: the min large message size.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=min-large-message-size)`

## syntax of the operation to get the `pre-acknowledge` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `pre-acknowledge` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `pre-acknowledge` attribute description: true to `pre-acknowledge`.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=pre-acknowledge)`

## syntax of the operation to get the `producer-max-rate` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `producer-max-rate` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `producer-max-rate` attribute description: the producer max rate.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=producer-max-rate)`

## syntax of the operation to get the `producer-window-size` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `producer-window-size` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `producer-window-size` attribute description: the producer window size.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=producer-window-size)`

## syntax of the operation to get the `protocol-manager-factory` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `protocol-manager-factory` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `protocol-manager-factory` attribute description: the protocol manager factory used by this connection factory (it must implement `org.apache.activemq.artemis.spi.core.remoting.clientprotocolmanagerfactory`).
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=protocol-manager-factory)`

## syntax of the operation to get the `reconnect-attempts` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `reconnect-attempts` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `reconnect-attempts` attribute description: the reconnect attempts.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=reconnect-attempts)`

## syntax of the operation to get the `retry-interval` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `retry-interval` attribute description: the retry interval.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=retry-interval)`

## syntax of the operation to get the `retry-interval-multiplier` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval-multiplier` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `retry-interval-multiplier` attribute description: the retry interval multiplier.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=retry-interval-multiplier)`

## syntax of the operation to get the `scheduled-thread-pool-max-size` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `scheduled-thread-pool-max-size` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `scheduled-thread-pool-max-size` attribute description: the scheduled thread pool max size.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=scheduled-thread-pool-max-size)`

## syntax of the operation to get the `thread-pool-max-size` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `thread-pool-max-size` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `thread-pool-max-size` attribute description: the thread pool max size.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=thread-pool-max-size)`

## syntax of the operation to get the `transaction-batch-size` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `transaction-batch-size` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `transaction-batch-size` attribute description: the transaction batch size.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=transaction-batch-size)`

## syntax of the operation to get the `use-global-pools` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `use-global-pools` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `use-global-pools` attribute description: true to use global pools.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=use-global-pools)`

## syntax of the operation to get the `use-topology-for-load-balancing` attribute of the `connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `use-topology-for-load-balancing` attribute value of the `connection-factory` resource of the `messaging-activemq` subsystem
* `use-topology-for-load-balancing` attribute description: whether to use topology information from the cluster to connect to it. if set to 'false', only the initial connector will be used.
* operation: `/subsystem=messaging-activemq/connection-factory=<connection-factory name>:read-attribute(name=use-topology-for-load-balancing)`

## syntax of the operation to get a `jms-bridge` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-resource()`
* To get the list of all the `jms-bridge` resource of the `messaging-activemq` subsystem use '*' for `<jms-bridge name>`.

## syntax of the operation to get the `aborted-message-count` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `aborted-message-count` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `aborted-message-count` attribute description: the number of messages `aborted/rolled` back.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=aborted-message-count)`

## syntax of the operation to get the `add-messageID-in-header` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `add-messageID-in-header` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `add-messageID-in-header` attribute description: if true, then the original `message's` message id will be appended in the message sent to the destination in the header `amq_bridge_msg_id_list`. if the message is bridged more than once, each message id will be appended.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=add-messageID-in-header)`

## syntax of the operation to get the `client-id` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `client-id` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `client-id` attribute description: the jakarta messaging client id to use when `creating/looking` up the subscription if it is durable and the source destination is a topic.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=client-id)`

## syntax of the operation to get the `failure-retry-interval` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `failure-retry-interval` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `failure-retry-interval` attribute description: the amount of time in milliseconds to wait between trying to recreate connections to the source or target servers when the bridge has detected they have failed.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=failure-retry-interval)`

## syntax of the operation to get the `max-batch-size` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `max-batch-size` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `max-batch-size` attribute description: the maximum number of messages to consume from the source destination before sending them in a batch to the target destination. its value must >= `1`.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `max-batch-time` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `max-batch-time` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `max-batch-time` attribute description: the maximum number of milliseconds to wait before sending a batch to target, even if the number of messages consumed has not reached `max-batch-size`. its value must be -`1` to represent 'wait forever', or >= `1` to specify an actual time.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=max-batch-time)`

## syntax of the operation to get the `max-retries` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `max-retries` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `max-retries` attribute description: the number of times to attempt to recreate connections to the source or target servers when the bridge has detected they have failed. the bridge will give up after trying this number of times. -`1` represents 'try forever'.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=max-retries)`

## syntax of the operation to get the `message-count` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `message-count` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `message-count` attribute description: the number of messages successfully committed.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=message-count)`

## syntax of the operation to get the `module` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `module` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `module` attribute description: the name of `as7` module containing the resources required to lookup source and target jakarta messaging resources.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=module)`

## syntax of the operation to get the `paused` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `paused` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `paused` attribute description: whether the jakarta messaging bridge is paused.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=paused)`

## syntax of the operation to get the `quality-of-service` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `quality-of-service` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `quality-of-service` attribute description: the desired quality of service mode (`at_most_once`, `duplicates_ok` or `once_and_only_once`).
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=quality-of-service)`

## syntax of the operation to get the `selector` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `selector` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `selector` attribute description: a jakarta messaging selector expression used for consuming messages from the source destination. only messages that match the selector expression will be bridged from the source to the target destination.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=selector)`

## syntax of the operation to get the `source-connection-factory` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `source-connection-factory` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `source-connection-factory` attribute description: the name of the source connection factory to lookup on the source messaging server.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=source-connection-factory)`

## syntax of the operation to get the `source-context` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `source-context` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `source-context` attribute description: the properties used to configure the source `jndi` initial context.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=source-context)`

## syntax of the operation to get the `source-credential-reference` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `source-credential-reference` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `source-credential-reference` attribute description: credential (from credential store) to authenticate source connection
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=source-credential-reference)`

## syntax of the operation to get the `source-destination` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `source-destination` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `source-destination` attribute description: the name of the source destination to lookup on the source messaging server.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=source-destination)`

## syntax of the operation to get the `source-password` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `source-password` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `source-password` attribute description: the password for creating the source connection.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=source-password)`

## syntax of the operation to get the `source-user` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `source-user` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `source-user` attribute description: the name of the user for creating the source connection.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=source-user)`

## syntax of the operation to get the `started` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `started` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `started` attribute description: whether the jakarta messaging bridge is started.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=started)`

## syntax of the operation to get the `subscription-name` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `subscription-name` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `subscription-name` attribute description: the name of the subscription if it is durable and the source destination is a topic.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=subscription-name)`

## syntax of the operation to get the `target-connection-factory` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `target-connection-factory` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `target-connection-factory` attribute description: the name of the target connection factory to lookup on the target messaging server.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=target-connection-factory)`

## syntax of the operation to get the `target-context` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `target-context` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `target-context` attribute description: the properties used to configure the target `jndi` initial context.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=target-context)`

## syntax of the operation to get the `target-credential-reference` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `target-credential-reference` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `target-credential-reference` attribute description: credential (from credential store) to authenticate target connection
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=target-credential-reference)`

## syntax of the operation to get the `target-destination` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `target-destination` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `target-destination` attribute description: the name of the target destination to lookup on the target messaging server.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=target-destination)`

## syntax of the operation to get the `target-password` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `target-password` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `target-password` attribute description: the password for creating the target connection.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=target-password)`

## syntax of the operation to get the `target-user` attribute of the `jms-bridge` resource of the `messaging-activemq` subsystem
* Retrieve the `target-user` attribute value of the `jms-bridge` resource of the `messaging-activemq` subsystem
* `target-user` attribute description: the name of the user for creating the target connection.
* operation: `/subsystem=messaging-activemq/jms-bridge=<jms-bridge name>:read-attribute(name=target-user)`

## syntax of the operation to get a `http-connector` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/http-connector=<http-connector name>:read-resource()`
* To get the list of all the `http-connector` resource of the `messaging-activemq` subsystem use '*' for `<http-connector name>`.

## syntax of the operation to get the `endpoint` attribute of the `http-connector` resource of the `messaging-activemq` subsystem
* Retrieve the `endpoint` attribute value of the `http-connector` resource of the `messaging-activemq` subsystem
* `endpoint` attribute description: the `http-acceptor` that serves as the endpoint of this `http-connector`.
* operation: `/subsystem=messaging-activemq/http-connector=<http-connector name>:read-attribute(name=endpoint)`

## syntax of the operation to get the `params` attribute of the `http-connector` resource of the `messaging-activemq` subsystem
* Retrieve the `params` attribute value of the `http-connector` resource of the `messaging-activemq` subsystem
* `params` attribute description: a `key-value` pair understood by the connector `factory-class` and used to configure it.
* operation: `/subsystem=messaging-activemq/http-connector=<http-connector name>:read-attribute(name=params)`

## syntax of the operation to get the `server-name` attribute of the `http-connector` resource of the `messaging-activemq` subsystem
* Retrieve the `server-name` attribute value of the `http-connector` resource of the `messaging-activemq` subsystem
* `server-name` attribute description: the name of the `activemq` artemis server that will be connected to on the remote server. if undefined, the name of the parent `activemq` artemis server will be used (suitable if the `http-connector` is used to connect to the parent server)
* operation: `/subsystem=messaging-activemq/http-connector=<http-connector name>:read-attribute(name=server-name)`

## syntax of the operation to get the `socket-binding` attribute of the `http-connector` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `http-connector` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the socket binding that the connector will use to create connections
* operation: `/subsystem=messaging-activemq/http-connector=<http-connector name>:read-attribute(name=socket-binding)`

## syntax of the operation to get the `ssl-context` attribute of the `http-connector` resource of the `messaging-activemq` subsystem
* Retrieve the `ssl-context` attribute value of the `http-connector` resource of the `messaging-activemq` subsystem
* `ssl-context` attribute description: the `ssl` context that the connector will use to connect
* operation: `/subsystem=messaging-activemq/http-connector=<http-connector name>:read-attribute(name=ssl-context)`

## syntax of the operation to get a `in-vm-connector` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/in-vm-connector=<in-vm-connector name>:read-resource()`
* To get the list of all the `in-vm-connector` resource of the `messaging-activemq` subsystem use '*' for `<in-vm-connector name>`.

## syntax of the operation to get the `params` attribute of the `in-vm-connector` resource of the `messaging-activemq` subsystem
* Retrieve the `params` attribute value of the `in-vm-connector` resource of the `messaging-activemq` subsystem
* `params` attribute description: a `key-value` pair understood by the connector `factory-class` and used to configure it.
* operation: `/subsystem=messaging-activemq/in-vm-connector=<in-vm-connector name>:read-attribute(name=params)`

## syntax of the operation to get the `server-id` attribute of the `in-vm-connector` resource of the `messaging-activemq` subsystem
* Retrieve the `server-id` attribute value of the `in-vm-connector` resource of the `messaging-activemq` subsystem
* `server-id` attribute description: the server id.
* operation: `/subsystem=messaging-activemq/in-vm-connector=<in-vm-connector name>:read-attribute(name=server-id)`

## syntax of the operation to get a `external-jms-topic` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/external-jms-topic=<external-jms-topic name>:read-resource()`
* To get the list of all the `external-jms-topic` resource of the `messaging-activemq` subsystem use '*' for `<external-jms-topic name>`.

## syntax of the operation to get the `enable-amq1-prefix` attribute of the `external-jms-topic` resource of the `messaging-activemq` subsystem
* Retrieve the `enable-amq1-prefix` attribute value of the `external-jms-topic` resource of the `messaging-activemq` subsystem
* `enable-amq1-prefix` attribute description: enable the use of `activemq` `1.x` prefixes in the addresses.
* operation: `/subsystem=messaging-activemq/external-jms-topic=<external-jms-topic name>:read-attribute(name=enable-amq1-prefix)`

## syntax of the operation to get the `entries` attribute of the `external-jms-topic` resource of the `messaging-activemq` subsystem
* Retrieve the `entries` attribute value of the `external-jms-topic` resource of the `messaging-activemq` subsystem
* `entries` attribute description: the `jndi` names the topic will be bound to.
* operation: `/subsystem=messaging-activemq/external-jms-topic=<external-jms-topic name>:read-attribute(name=entries)`

## syntax of the operation to get a `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>:read-resource()`
* To get the list of all the `server` resource of the `messaging-activemq` subsystem use '*' for `<server name>`.

## syntax of the operation to get the `active` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `active` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `active` attribute description: whether the server is active (and accepting connections) or passive (in backup mode, waiting for `failover`).
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=active)`

## syntax of the operation to get the `address-queue-scan-period` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `address-queue-scan-period` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `address-queue-scan-period` attribute description: how often to scan for addresses and queues which should be deleted.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=address-queue-scan-period)`

## syntax of the operation to get the `async-connection-execution-enabled` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `async-connection-execution-enabled` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `async-connection-execution-enabled` attribute description: whether incoming packets on the server should be handed off to a thread from the thread pool for processing. false if they should be handled on the `remoting` thread.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=async-connection-execution-enabled)`

## syntax of the operation to get the `cluster-credential-reference` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `cluster-credential-reference` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `cluster-credential-reference` attribute description: credential (from credential store) to authenticate to cluster
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=cluster-credential-reference)`

## syntax of the operation to get the `cluster-password` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `cluster-password` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `cluster-password` attribute description: the password used by cluster connections to communicate between the clustered nodes.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=cluster-password)`

## syntax of the operation to get the `cluster-user` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `cluster-user` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `cluster-user` attribute description: the user used by cluster connections to communicate between the clustered nodes.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=cluster-user)`

## syntax of the operation to get the `connection-ttl-override` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connection-ttl-override` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `connection-ttl-override` attribute description: if set, this will override how long (in ms) to keep a connection alive without receiving a ping.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=connection-ttl-override)`

## syntax of the operation to get the `create-bindings-dir` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `create-bindings-dir` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `create-bindings-dir` attribute description: whether the server should create the bindings directory on start up.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=create-bindings-dir)`

## syntax of the operation to get the `create-journal-dir` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `create-journal-dir` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `create-journal-dir` attribute description: whether the server should create the journal directory on start up.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=create-journal-dir)`

## syntax of the operation to get the `critical-analyzer-check-period` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `critical-analyzer-check-period` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `critical-analyzer-check-period` attribute description: time used to check the response times (defaults to half of `critical-analyzer-timeout`).
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=critical-analyzer-check-period)`

## syntax of the operation to get the `critical-analyzer-enabled` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `critical-analyzer-enabled` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `critical-analyzer-enabled` attribute description: to enable or disable the critical analyzer.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=critical-analyzer-enabled)`

## syntax of the operation to get the `critical-analyzer-policy` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `critical-analyzer-policy` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `critical-analyzer-policy` attribute description: should the messaging server log, be halted or shutdown upon failures.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=critical-analyzer-policy)`

## syntax of the operation to get the `critical-analyzer-timeout` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `critical-analyzer-timeout` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `critical-analyzer-timeout` attribute description: timeout used to do the critical analysis.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=critical-analyzer-timeout)`

## syntax of the operation to get the `disk-scan-period` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `disk-scan-period` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `disk-scan-period` attribute description: the interval where the disk is scanned for percentual usage.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=disk-scan-period)`

## syntax of the operation to get the `elytron-domain` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `elytron-domain` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `elytron-domain` attribute description: the name of the elytron security domain used to verify user and role information.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=elytron-domain)`

## syntax of the operation to get the `global-max-disk-usage` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `global-max-disk-usage` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `global-max-disk-usage` attribute description: the maximum percentage of data we should use from disks. the system will block while the disk is full.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=global-max-disk-usage)`

## syntax of the operation to get the `global-max-memory-size` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `global-max-memory-size` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `global-max-memory-size` attribute description: maximum amount of memory which message data may consume.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=global-max-memory-size)`

## syntax of the operation to get the `id-cache-size` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `id-cache-size` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `id-cache-size` attribute description: the size of the cache for `pre-creating` message ids.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=id-cache-size)`

## syntax of the operation to get the `incoming-interceptors` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `incoming-interceptors` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `incoming-interceptors` attribute description: the list of incoming interceptor classes used by this server.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=incoming-interceptors)`

## syntax of the operation to get the `jmx-domain` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jmx-domain` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `jmx-domain` attribute description: the `jmx` domain used to register internal `activemq` `mbeans` in the `mbeanserver`.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=jmx-domain)`

## syntax of the operation to get the `jmx-management-enabled` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jmx-management-enabled` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `jmx-management-enabled` attribute description: whether `activemq` should expose its internal management `api` via `jmx`. this is not recommended, as accessing these `mbeans` can lead to inconsistent configuration.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=jmx-management-enabled)`

## syntax of the operation to get the `journal-bindings-table` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-bindings-table` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-bindings-table` attribute description: name of the `jdbc` table to store the bindings.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-bindings-table)`

## syntax of the operation to get the `journal-buffer-size` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-buffer-size` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-buffer-size` attribute description: the size of the internal buffer on the journal.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-buffer-size)`

## syntax of the operation to get the `journal-buffer-timeout` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-buffer-timeout` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-buffer-timeout` attribute description: the timeout (in nanoseconds) used to flush internal buffers on the journal.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-buffer-timeout)`

## syntax of the operation to get the `journal-compact-min-files` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-compact-min-files` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-compact-min-files` attribute description: the minimal number of journal data files before we can start compacting.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-compact-min-files)`

## syntax of the operation to get the `journal-compact-percentage` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-compact-percentage` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-compact-percentage` attribute description: the percentage of live data on which we consider compacting the journal.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-compact-percentage)`

## syntax of the operation to get the `journal-database` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-database` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-database` attribute description: type of the database (can be used to customize `sql` statements). if this attribute is not specified, the type of the database will be determined based on the `datasource` `metadata`.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-database)`

## syntax of the operation to get the `journal-datasource` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-datasource` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-datasource` attribute description: name of the `datasource` for the `jdbc` store.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-datasource)`

## syntax of the operation to get the `journal-file-open-timeout` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-file-open-timeout` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-file-open-timeout` attribute description: the timeout (in seconds) for opening journal files. values <= `0` mean fail immediately.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-file-open-timeout)`

## syntax of the operation to get the `journal-file-size` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-file-size` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-file-size` attribute description: the size (in bytes) of each journal file.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-file-size)`

## syntax of the operation to get the `journal-jdbc-lock-expiration` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-jdbc-lock-expiration` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-jdbc-lock-expiration` attribute description: the time the ha lock is considered valid without keeping it alive.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-jdbc-lock-expiration)`

## syntax of the operation to get the `journal-jdbc-lock-renew-period` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-jdbc-lock-renew-period` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-jdbc-lock-renew-period` attribute description: the renewal period for the ha lock to keep it alive.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-jdbc-lock-renew-period)`

## syntax of the operation to get the `journal-jdbc-network-timeout` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-jdbc-network-timeout` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-jdbc-network-timeout` attribute description: the timeout used by the `jdbc` connection to detect network issues.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-jdbc-network-timeout)`

## syntax of the operation to get the `journal-jms-bindings-table` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-jms-bindings-table` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-jms-bindings-table` attribute description: name of the `jdbc` table to store the jakarta messaging bindings.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-jms-bindings-table)`

## syntax of the operation to get the `journal-large-messages-table` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-large-messages-table` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-large-messages-table` attribute description: name of the `jdbc` table to store the large messages.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-large-messages-table)`

## syntax of the operation to get the `journal-max-attic-files` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-max-attic-files` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-max-attic-files` attribute description: the maximum number of journal files stored in the attic in case of corrupted journal files.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-max-attic-files)`

## syntax of the operation to get the `journal-max-io` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-max-io` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-max-io` attribute description: the maximum number of write requests that can be in the `aio` queue at any one time.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-max-io)`

## syntax of the operation to get the `journal-messages-table` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-messages-table` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-messages-table` attribute description: name of the `jdbc` table to store the messages.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-messages-table)`

## syntax of the operation to get the `journal-min-files` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-min-files` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-min-files` attribute description: how many journal files to `pre-create`.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-min-files)`

## syntax of the operation to get the `journal-node-manager-store-table` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-node-manager-store-table` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-node-manager-store-table` attribute description: name of the `jdbc` table to store the node manager.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-node-manager-store-table)`

## syntax of the operation to get the `journal-page-store-table` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-page-store-table` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-page-store-table` attribute description: name of the `jdbc` table to store pages.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-page-store-table)`

## syntax of the operation to get the `journal-pool-files` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-pool-files` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-pool-files` attribute description: the number of journal files that can be reused. `activemq` will create as many files as needed however when reclaiming files it will shrink back to the value (-`1` means no limit).
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-pool-files)`

## syntax of the operation to get the `journal-sync-non-transactional` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-sync-non-transactional` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-sync-non-transactional` attribute description: whether to wait for non transaction data to be synced to the journal before returning a response to the client.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-sync-non-transactional)`

## syntax of the operation to get the `journal-sync-transactional` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-sync-transactional` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-sync-transactional` attribute description: whether to wait for transaction data to be synchronized to the journal before returning a response to the client.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-sync-transactional)`

## syntax of the operation to get the `journal-type` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `journal-type` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `journal-type` attribute description: the type of journal to use.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=journal-type)`

## syntax of the operation to get the `log-journal-write-rate` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `log-journal-write-rate` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `log-journal-write-rate` attribute description: whether to periodically log the `journal's` write rate and flush rate.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=log-journal-write-rate)`

## syntax of the operation to get the `management-address` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `management-address` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `management-address` attribute description: address to send management messages to.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=management-address)`

## syntax of the operation to get the `management-notification-address` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `management-notification-address` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `management-notification-address` attribute description: the name of the address that consumers bind to  to receive management notifications.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=management-notification-address)`

## syntax of the operation to get the `memory-measure-interval` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `memory-measure-interval` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `memory-measure-interval` attribute description: frequency to sample `jvm` memory in ms (or -`1` to disable memory sampling)
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=memory-measure-interval)`

## syntax of the operation to get the `memory-warning-threshold` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `memory-warning-threshold` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `memory-warning-threshold` attribute description: percentage of available memory which if exceeded results in a warning log
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=memory-warning-threshold)`

## syntax of the operation to get the `message-counter-max-day-history` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `message-counter-max-day-history` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `message-counter-max-day-history` attribute description: how many days to keep message counter history.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=message-counter-max-day-history)`

## syntax of the operation to get the `message-counter-sample-period` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `message-counter-sample-period` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `message-counter-sample-period` attribute description: the sample period (in ms) to use for message counters.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=message-counter-sample-period)`

## syntax of the operation to get the `message-expiry-scan-period` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `message-expiry-scan-period` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `message-expiry-scan-period` attribute description: how often (in ms) to scan for expired messages.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=message-expiry-scan-period)`

## syntax of the operation to get the `message-expiry-thread-priority` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `message-expiry-thread-priority` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `message-expiry-thread-priority` attribute description: the priority of the thread expiring messages.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=message-expiry-thread-priority)`

## syntax of the operation to get the `network-check-list` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `network-check-list` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `network-check-list` attribute description: this is a comma separated list, no spaces, of `dns` or ips (it should accept `ipv6`) to be used to validate the network.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=network-check-list)`

## syntax of the operation to get the `network-check-nic` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `network-check-nic` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `network-check-nic` attribute description: the `nic` (network interface controller) to be used to validate the network.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=network-check-nic)`

## syntax of the operation to get the `network-check-period` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `network-check-period` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `network-check-period` attribute description: the frequency of how often we should check if the network is still up.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=network-check-period)`

## syntax of the operation to get the `network-check-ping-command` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `network-check-ping-command` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `network-check-ping-command` attribute description: the command used to ping `ipv4` addresses.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=network-check-ping-command)`

## syntax of the operation to get the `network-check-ping6-command` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `network-check-ping6-command` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `network-check-ping6-command` attribute description: the command used to ping `ipv6` addresses.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=network-check-ping6-command)`

## syntax of the operation to get the `network-check-timeout` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `network-check-timeout` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `network-check-timeout` attribute description: the timeout used on the ping.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=network-check-timeout)`

## syntax of the operation to get the `network-check-url-list` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `network-check-url-list` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `network-check-url-list` attribute description: the list of `http` `uris` to be used to validate the network.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=network-check-url-list)`

## syntax of the operation to get the `outgoing-interceptors` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `outgoing-interceptors` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `outgoing-interceptors` attribute description: the list of outgoing interceptor classes used by this server.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=outgoing-interceptors)`

## syntax of the operation to get the `override-in-vm-security` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `override-in-vm-security` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `override-in-vm-security` attribute description: whether the `activemq` server will override security credentials for `in-vm` connections.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=override-in-vm-security)`

## syntax of the operation to get the `page-max-concurrent-io` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `page-max-concurrent-io` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `page-max-concurrent-io` attribute description: the maximum number of concurrent reads allowed on paging
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=page-max-concurrent-io)`

## syntax of the operation to get the `perf-blast-pages` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `perf-blast-pages` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `perf-blast-pages` attribute description: number of pages to add to check the journal performance (only meant to be used to test performance of pages).
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=perf-blast-pages)`

## syntax of the operation to get the `persist-delivery-count-before-delivery` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `persist-delivery-count-before-delivery` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `persist-delivery-count-before-delivery` attribute description: whether the delivery count is persisted before delivery. false means that this only happens after a message has been cancelled.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=persist-delivery-count-before-delivery)`

## syntax of the operation to get the `persist-id-cache` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `persist-id-cache` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `persist-id-cache` attribute description: whether ids are persisted to the journal.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=persist-id-cache)`

## syntax of the operation to get the `persistence-enabled` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `persistence-enabled` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `persistence-enabled` attribute description: whether the server will use the file based journal for persistence.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=persistence-enabled)`

## syntax of the operation to get the `run-sync-speed-test` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `run-sync-speed-test` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `run-sync-speed-test` attribute description: whether on startup to perform a diagnostic test on how fast your disk can sync. useful when determining performance issues.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=run-sync-speed-test)`

## syntax of the operation to get the `runtime-journal-type` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `runtime-journal-type` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `runtime-journal-type` attribute description: the effectively used journal type (it may differs from the configured one if the configured one is not supported).
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=runtime-journal-type)`

## syntax of the operation to get the `scheduled-thread-pool-max-size` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scheduled-thread-pool-max-size` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `scheduled-thread-pool-max-size` attribute description: the number of threads that the main scheduled thread pool has.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=scheduled-thread-pool-max-size)`

## syntax of the operation to get the `security-domain` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `security-domain` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `security-domain` attribute description: the security domain to use to verify user and role information.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=security-domain)`

## syntax of the operation to get the `security-enabled` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `security-enabled` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `security-enabled` attribute description: whether security is enabled.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=security-enabled)`

## syntax of the operation to get the `security-invalidation-interval` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `security-invalidation-interval` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `security-invalidation-interval` attribute description: how long (in ms) to wait before invalidating the security cache.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=security-invalidation-interval)`

## syntax of the operation to get the `server-dump-interval` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `server-dump-interval` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `server-dump-interval` attribute description: how often to dump basic runtime information to the server log. a value less than `1` disables this feature.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=server-dump-interval)`

## syntax of the operation to get the `started` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `started` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `started` attribute description: whether this server is started.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=started)`

## syntax of the operation to get the `statistics-enabled` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `statistics-enabled` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `statistics-enabled` attribute description: whether gathering of statistics such as message counters are enabled.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `thread-pool-max-size` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `thread-pool-max-size` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `thread-pool-max-size` attribute description: the number of threads that the main thread pool has. -`1` means no limit.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=thread-pool-max-size)`

## syntax of the operation to get the `transaction-timeout` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `transaction-timeout` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `transaction-timeout` attribute description: default duration before a transaction will be rolled back after create time. if a transaction manager is used, this value will be replaced by that of the tm.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=transaction-timeout)`

## syntax of the operation to get the `transaction-timeout-scan-period` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `transaction-timeout-scan-period` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `transaction-timeout-scan-period` attribute description: how often (in ms) to scan for timeout transactions.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=transaction-timeout-scan-period)`

## syntax of the operation to get the `version` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `version` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `version` attribute description: the `server's` version.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=version)`

## syntax of the operation to get the `wild-card-routing-enabled` attribute of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `wild-card-routing-enabled` attribute value of the `server` resource of the `messaging-activemq` subsystem
* `wild-card-routing-enabled` attribute description: whether the server supports wild card routing.
* operation: `/subsystem=messaging-activemq/server=<server name>:read-attribute(name=wild-card-routing-enabled)`

## syntax of the operation to get a `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/discovery-group=<discovery-group name>:read-resource()`
* To get the list of all the `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<discovery-group name>`.

## syntax of the operation to get the `initial-wait-timeout` attribute of the `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-wait-timeout` attribute value of the `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-wait-timeout` attribute description: period, in ms, to wait for an initial broadcast to give us at least one node in the cluster.
* operation: `/subsystem=messaging-activemq/server=<server name>/discovery-group=<discovery-group name>:read-attribute(name=initial-wait-timeout)`

## syntax of the operation to get the `jgroups-channel` attribute of the `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-channel` attribute value of the `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `jgroups-channel` attribute description: references the name of a `jgroups` channel.  if undefined, the default channel will be used.
* operation: `/subsystem=messaging-activemq/server=<server name>/discovery-group=<discovery-group name>:read-attribute(name=jgroups-channel)`

## syntax of the operation to get the `jgroups-cluster` attribute of the `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-cluster` attribute value of the `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `jgroups-cluster` attribute description: the logical cluster name.
* operation: `/subsystem=messaging-activemq/server=<server name>/discovery-group=<discovery-group name>:read-attribute(name=jgroups-cluster)`

## syntax of the operation to get the `jgroups-stack` attribute of the `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-stack` attribute value of the `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `jgroups-stack` attribute description: references the name of a `jgroups` channel factory.
* operation: `/subsystem=messaging-activemq/server=<server name>/discovery-group=<discovery-group name>:read-attribute(name=jgroups-stack)`

## syntax of the operation to get the `refresh-timeout` attribute of the `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `refresh-timeout` attribute value of the `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `refresh-timeout` attribute description: period the discovery group waits after receiving the last broadcast from a particular server before removing that `server's` connector pair entry from its list.
* operation: `/subsystem=messaging-activemq/server=<server name>/discovery-group=<discovery-group name>:read-attribute(name=refresh-timeout)`

## syntax of the operation to get the `socket-binding` attribute of the `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the discovery group socket binding.
* operation: `/subsystem=messaging-activemq/server=<server name>/discovery-group=<discovery-group name>:read-attribute(name=socket-binding)`

## syntax of the operation to get a `jgroups-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/jgroups-discovery-group=<jgroups-discovery-group name>:read-resource()`
* To get the list of all the `jgroups-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<jgroups-discovery-group name>`.

## syntax of the operation to get the `initial-wait-timeout` attribute of the `jgroups-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-wait-timeout` attribute value of the `jgroups-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-wait-timeout` attribute description: period, in ms, to wait for an initial broadcast to give us at least one node in the cluster.
* operation: `/subsystem=messaging-activemq/server=<server name>/jgroups-discovery-group=<jgroups-discovery-group name>:read-attribute(name=initial-wait-timeout)`

## syntax of the operation to get the `jgroups-channel` attribute of the `jgroups-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-channel` attribute value of the `jgroups-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `jgroups-channel` attribute description: references the name of a `jgroups` channel.  if undefined, the default channel will be used.
* operation: `/subsystem=messaging-activemq/server=<server name>/jgroups-discovery-group=<jgroups-discovery-group name>:read-attribute(name=jgroups-channel)`

## syntax of the operation to get the `jgroups-cluster` attribute of the `jgroups-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-cluster` attribute value of the `jgroups-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `jgroups-cluster` attribute description: the logical cluster name.
* operation: `/subsystem=messaging-activemq/server=<server name>/jgroups-discovery-group=<jgroups-discovery-group name>:read-attribute(name=jgroups-cluster)`

## syntax of the operation to get the `jgroups-stack` attribute of the `jgroups-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-stack` attribute value of the `jgroups-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `jgroups-stack` attribute description: references the name of a `jgroups` channel factory.
* operation: `/subsystem=messaging-activemq/server=<server name>/jgroups-discovery-group=<jgroups-discovery-group name>:read-attribute(name=jgroups-stack)`

## syntax of the operation to get the `refresh-timeout` attribute of the `jgroups-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `refresh-timeout` attribute value of the `jgroups-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `refresh-timeout` attribute description: period the discovery group waits after receiving the last broadcast from a particular server before removing that `server's` connector pair entry from its list.
* operation: `/subsystem=messaging-activemq/server=<server name>/jgroups-discovery-group=<jgroups-discovery-group name>:read-attribute(name=refresh-timeout)`

## syntax of the operation to get the `check-for-live-server` attribute of the `replication-primary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `check-for-live-server` attribute value of the `replication-primary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `check-for-live-server` attribute description: whether to check the cluster for another server using the same server id when starting up.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-primary:read-attribute(name=check-for-live-server)`

## syntax of the operation to get the `cluster-name` attribute of the `replication-primary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `cluster-name` attribute value of the `replication-primary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `cluster-name` attribute description: name of the cluster used for replication. if it is undefined, the name of the first cluster connection defined in the configuration will be used.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-primary:read-attribute(name=cluster-name)`

## syntax of the operation to get the `group-name` attribute of the `replication-primary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `group-name` attribute value of the `replication-primary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `group-name` attribute description: if set, backup servers will only pair with live servers with matching `group-name`.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-primary:read-attribute(name=group-name)`

## syntax of the operation to get the `initial-replication-sync-timeout` attribute of the `replication-primary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-replication-sync-timeout` attribute value of the `replication-primary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-replication-sync-timeout` attribute description: how long to wait until the initiation replication is synchronized.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-primary:read-attribute(name=initial-replication-sync-timeout)`

## syntax of the operation to get the `synchronized-with-backup` attribute of the `replication-primary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `synchronized-with-backup` attribute value of the `replication-primary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `synchronized-with-backup` attribute description: the replication synchronization process with the backup server is complete.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-primary:read-attribute(name=synchronized-with-backup)`

## syntax of the operation to get the `backup-port-offset` attribute of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `backup-port-offset` attribute value of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `backup-port-offset` attribute description: the offset to use for the connectors and acceptors when creating a new backup server.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated:read-attribute(name=backup-port-offset)`

## syntax of the operation to get the `backup-request-retries` attribute of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `backup-request-retries` attribute value of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `backup-request-retries` attribute description: how many times the live server will try to request a backup, -`1` means for ever.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated:read-attribute(name=backup-request-retries)`

## syntax of the operation to get the `backup-request-retry-interval` attribute of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `backup-request-retry-interval` attribute value of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `backup-request-retry-interval` attribute description: how long (in ms) to wait for retries between attempts to request a backup server.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated:read-attribute(name=backup-request-retry-interval)`

## syntax of the operation to get the `excluded-connectors` attribute of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `excluded-connectors` attribute value of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `excluded-connectors` attribute description: the connectors that must not have their port offset.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated:read-attribute(name=excluded-connectors)`

## syntax of the operation to get the `max-backups` attribute of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-backups` attribute value of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-backups` attribute description: whether or not this live server will accept backup requests from other live servers.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated:read-attribute(name=max-backups)`

## syntax of the operation to get the `request-backup` attribute of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `request-backup` attribute value of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `request-backup` attribute description: if true then the server will request a backup on another node.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated:read-attribute(name=request-backup)`

## syntax of the operation to get the `allow-failback` attribute of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `allow-failback` attribute value of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `allow-failback` attribute description: whether a server will automatically stop when a another places a request to take over its place. the use case is when a regular server stops and its backup takes over its duties, later the main server restarts and requests the server (the former backup) to stop operating.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=secondary:read-attribute(name=allow-failback)`

## syntax of the operation to get the `cluster-name` attribute of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `cluster-name` attribute value of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `cluster-name` attribute description: name of the cluster used for replication. if it is undefined, the name of the first cluster connection defined in the configuration will be used.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=secondary:read-attribute(name=cluster-name)`

## syntax of the operation to get the `group-name` attribute of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `group-name` attribute value of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `group-name` attribute description: if set, backup servers will only pair with live servers with matching `group-name`.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=secondary:read-attribute(name=group-name)`

## syntax of the operation to get the `initial-replication-sync-timeout` attribute of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-replication-sync-timeout` attribute value of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-replication-sync-timeout` attribute description: how long to wait until the initiation replication is synchronized.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=secondary:read-attribute(name=initial-replication-sync-timeout)`

## syntax of the operation to get the `max-saved-replicated-journal-size` attribute of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-saved-replicated-journal-size` attribute value of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-saved-replicated-journal-size` attribute description: this specifies how many times a replicated backup server can restart after moving its files on start. once there are this number of backup journal files the server will stop permanently after if fails back.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=secondary:read-attribute(name=max-saved-replicated-journal-size)`

## syntax of the operation to get the `restart-backup` attribute of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `restart-backup` attribute value of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `restart-backup` attribute description: will this server, if a backup, restart once it has been stopped because of `failback` or scaling down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=secondary:read-attribute(name=restart-backup)`

## syntax of the operation to get the `scale-down` attribute of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down` attribute value of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down` attribute description: configure whether this server send its messages to another live server in the `scale-down` cluster when it is shutdown cleanly.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=secondary:read-attribute(name=scale-down)`

## syntax of the operation to get the `scale-down-cluster-name` attribute of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-cluster-name` attribute value of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-cluster-name` attribute description: name of the cluster used to scale down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=secondary:read-attribute(name=scale-down-cluster-name)`

## syntax of the operation to get the `scale-down-connectors` attribute of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-connectors` attribute value of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-connectors` attribute description: list of connectors used to form the `scale-down` cluster.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=secondary:read-attribute(name=scale-down-connectors)`

## syntax of the operation to get the `scale-down-discovery-group` attribute of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-discovery-group` attribute value of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-discovery-group` attribute description: name of the discovery group used to build the `scale-down` cluster
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=secondary:read-attribute(name=scale-down-discovery-group)`

## syntax of the operation to get the `scale-down-group-name` attribute of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-group-name` attribute value of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-group-name` attribute description: name of the group used to scale down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=secondary:read-attribute(name=scale-down-group-name)`

## syntax of the operation to get the `synchronized-with-live` attribute of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `synchronized-with-live` attribute value of the `secondary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `synchronized-with-live` attribute description: the replication synchronization process with the live server is complete.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=secondary:read-attribute(name=synchronized-with-live)`

## syntax of the operation to get the `check-for-live-server` attribute of the `primary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `check-for-live-server` attribute value of the `primary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `check-for-live-server` attribute description: whether to check the cluster for another server using the same server id when starting up.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=primary:read-attribute(name=check-for-live-server)`

## syntax of the operation to get the `cluster-name` attribute of the `primary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `cluster-name` attribute value of the `primary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `cluster-name` attribute description: name of the cluster used for replication. if it is undefined, the name of the first cluster connection defined in the configuration will be used.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=primary:read-attribute(name=cluster-name)`

## syntax of the operation to get the `group-name` attribute of the `primary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `group-name` attribute value of the `primary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `group-name` attribute description: if set, backup servers will only pair with live servers with matching `group-name`.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=primary:read-attribute(name=group-name)`

## syntax of the operation to get the `initial-replication-sync-timeout` attribute of the `primary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-replication-sync-timeout` attribute value of the `primary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-replication-sync-timeout` attribute description: how long to wait until the initiation replication is synchronized.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=primary:read-attribute(name=initial-replication-sync-timeout)`

## syntax of the operation to get the `synchronized-with-backup` attribute of the `primary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `synchronized-with-backup` attribute value of the `primary` `configuration` resource of the `replication-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `synchronized-with-backup` attribute description: the replication synchronization process with the backup server is complete.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-colocated/configuration=primary:read-attribute(name=synchronized-with-backup)`

## syntax of the operation to get the `scale-down` attribute of the `live-only` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down` attribute value of the `live-only` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down` attribute description: configure whether this server send its messages to another live server in the `scale-down` cluster when it is shutdown cleanly.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=live-only:read-attribute(name=scale-down)`

## syntax of the operation to get the `scale-down-cluster-name` attribute of the `live-only` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-cluster-name` attribute value of the `live-only` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-cluster-name` attribute description: name of the cluster used to scale down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=live-only:read-attribute(name=scale-down-cluster-name)`

## syntax of the operation to get the `scale-down-connectors` attribute of the `live-only` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-connectors` attribute value of the `live-only` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-connectors` attribute description: list of connectors used to form the `scale-down` cluster.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=live-only:read-attribute(name=scale-down-connectors)`

## syntax of the operation to get the `scale-down-discovery-group` attribute of the `live-only` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-discovery-group` attribute value of the `live-only` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-discovery-group` attribute description: name of the discovery group used to build the `scale-down` cluster
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=live-only:read-attribute(name=scale-down-discovery-group)`

## syntax of the operation to get the `scale-down-group-name` attribute of the `live-only` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-group-name` attribute value of the `live-only` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-group-name` attribute description: name of the group used to scale down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=live-only:read-attribute(name=scale-down-group-name)`

## syntax of the operation to get the `failover-on-server-shutdown` attribute of the `shared-store-primary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `failover-on-server-shutdown` attribute value of the `shared-store-primary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `failover-on-server-shutdown` attribute description: whether the server must `failover` when it is normally shutdown.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-primary:read-attribute(name=failover-on-server-shutdown)`

## syntax of the operation to get the `allow-failback` attribute of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `allow-failback` attribute value of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `allow-failback` attribute description: whether a server will automatically stop when a another places a request to take over its place. the use case is when a regular server stops and its backup takes over its duties, later the main server restarts and requests the server (the former backup) to stop operating.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-secondary:read-attribute(name=allow-failback)`

## syntax of the operation to get the `failover-on-server-shutdown` attribute of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `failover-on-server-shutdown` attribute value of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `failover-on-server-shutdown` attribute description: whether the server must `failover` when it is normally shutdown.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-secondary:read-attribute(name=failover-on-server-shutdown)`

## syntax of the operation to get the `restart-backup` attribute of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `restart-backup` attribute value of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `restart-backup` attribute description: will this server, if a backup, restart once it has been stopped because of `failback` or scaling down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-secondary:read-attribute(name=restart-backup)`

## syntax of the operation to get the `scale-down` attribute of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down` attribute value of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down` attribute description: configure whether this server send its messages to another live server in the `scale-down` cluster when it is shutdown cleanly.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-secondary:read-attribute(name=scale-down)`

## syntax of the operation to get the `scale-down-cluster-name` attribute of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-cluster-name` attribute value of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-cluster-name` attribute description: name of the cluster used to scale down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-secondary:read-attribute(name=scale-down-cluster-name)`

## syntax of the operation to get the `scale-down-connectors` attribute of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-connectors` attribute value of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-connectors` attribute description: list of connectors used to form the `scale-down` cluster.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-secondary:read-attribute(name=scale-down-connectors)`

## syntax of the operation to get the `scale-down-discovery-group` attribute of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-discovery-group` attribute value of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-discovery-group` attribute description: name of the discovery group used to build the `scale-down` cluster
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-secondary:read-attribute(name=scale-down-discovery-group)`

## syntax of the operation to get the `scale-down-group-name` attribute of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-group-name` attribute value of the `shared-store-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-group-name` attribute description: name of the group used to scale down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-secondary:read-attribute(name=scale-down-group-name)`

## syntax of the operation to get the `allow-failback` attribute of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `allow-failback` attribute value of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `allow-failback` attribute description: whether a server will automatically stop when a another places a request to take over its place. the use case is when a regular server stops and its backup takes over its duties, later the main server restarts and requests the server (the former backup) to stop operating.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-secondary:read-attribute(name=allow-failback)`

## syntax of the operation to get the `cluster-name` attribute of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `cluster-name` attribute value of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `cluster-name` attribute description: name of the cluster used for replication. if it is undefined, the name of the first cluster connection defined in the configuration will be used.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-secondary:read-attribute(name=cluster-name)`

## syntax of the operation to get the `group-name` attribute of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `group-name` attribute value of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `group-name` attribute description: if set, backup servers will only pair with live servers with matching `group-name`.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-secondary:read-attribute(name=group-name)`

## syntax of the operation to get the `initial-replication-sync-timeout` attribute of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-replication-sync-timeout` attribute value of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-replication-sync-timeout` attribute description: how long to wait until the initiation replication is synchronized.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-secondary:read-attribute(name=initial-replication-sync-timeout)`

## syntax of the operation to get the `max-saved-replicated-journal-size` attribute of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-saved-replicated-journal-size` attribute value of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-saved-replicated-journal-size` attribute description: this specifies how many times a replicated backup server can restart after moving its files on start. once there are this number of backup journal files the server will stop permanently after if fails back.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-secondary:read-attribute(name=max-saved-replicated-journal-size)`

## syntax of the operation to get the `restart-backup` attribute of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `restart-backup` attribute value of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `restart-backup` attribute description: will this server, if a backup, restart once it has been stopped because of `failback` or scaling down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-secondary:read-attribute(name=restart-backup)`

## syntax of the operation to get the `scale-down` attribute of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down` attribute value of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down` attribute description: configure whether this server send its messages to another live server in the `scale-down` cluster when it is shutdown cleanly.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-secondary:read-attribute(name=scale-down)`

## syntax of the operation to get the `scale-down-cluster-name` attribute of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-cluster-name` attribute value of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-cluster-name` attribute description: name of the cluster used to scale down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-secondary:read-attribute(name=scale-down-cluster-name)`

## syntax of the operation to get the `scale-down-connectors` attribute of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-connectors` attribute value of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-connectors` attribute description: list of connectors used to form the `scale-down` cluster.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-secondary:read-attribute(name=scale-down-connectors)`

## syntax of the operation to get the `scale-down-discovery-group` attribute of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-discovery-group` attribute value of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-discovery-group` attribute description: name of the discovery group used to build the `scale-down` cluster
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-secondary:read-attribute(name=scale-down-discovery-group)`

## syntax of the operation to get the `scale-down-group-name` attribute of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-group-name` attribute value of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-group-name` attribute description: name of the group used to scale down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-secondary:read-attribute(name=scale-down-group-name)`

## syntax of the operation to get the `synchronized-with-live` attribute of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `synchronized-with-live` attribute value of the `replication-secondary` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `synchronized-with-live` attribute description: the replication synchronization process with the live server is complete.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=replication-secondary:read-attribute(name=synchronized-with-live)`

## syntax of the operation to get the `backup-port-offset` attribute of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `backup-port-offset` attribute value of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `backup-port-offset` attribute description: the offset to use for the connectors and acceptors when creating a new backup server.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated:read-attribute(name=backup-port-offset)`

## syntax of the operation to get the `backup-request-retries` attribute of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `backup-request-retries` attribute value of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `backup-request-retries` attribute description: how many times the live server will try to request a backup, -`1` means for ever.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated:read-attribute(name=backup-request-retries)`

## syntax of the operation to get the `backup-request-retry-interval` attribute of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `backup-request-retry-interval` attribute value of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `backup-request-retry-interval` attribute description: how long (in ms) to wait for retries between attempts to request a backup server.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated:read-attribute(name=backup-request-retry-interval)`

## syntax of the operation to get the `max-backups` attribute of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-backups` attribute value of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-backups` attribute description: whether or not this live server will accept backup requests from other live servers.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated:read-attribute(name=max-backups)`

## syntax of the operation to get the `request-backup` attribute of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `request-backup` attribute value of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `request-backup` attribute description: if true then the server will request a backup on another node.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated:read-attribute(name=request-backup)`

## syntax of the operation to get the `allow-failback` attribute of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `allow-failback` attribute value of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `allow-failback` attribute description: whether a server will automatically stop when a another places a request to take over its place. the use case is when a regular server stops and its backup takes over its duties, later the main server restarts and requests the server (the former backup) to stop operating.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated/configuration=secondary:read-attribute(name=allow-failback)`

## syntax of the operation to get the `failover-on-server-shutdown` attribute of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `failover-on-server-shutdown` attribute value of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `failover-on-server-shutdown` attribute description: whether the server must `failover` when it is normally shutdown.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated/configuration=secondary:read-attribute(name=failover-on-server-shutdown)`

## syntax of the operation to get the `restart-backup` attribute of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `restart-backup` attribute value of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `restart-backup` attribute description: will this server, if a backup, restart once it has been stopped because of `failback` or scaling down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated/configuration=secondary:read-attribute(name=restart-backup)`

## syntax of the operation to get the `scale-down` attribute of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down` attribute value of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down` attribute description: configure whether this server send its messages to another live server in the `scale-down` cluster when it is shutdown cleanly.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated/configuration=secondary:read-attribute(name=scale-down)`

## syntax of the operation to get the `scale-down-cluster-name` attribute of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-cluster-name` attribute value of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-cluster-name` attribute description: name of the cluster used to scale down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated/configuration=secondary:read-attribute(name=scale-down-cluster-name)`

## syntax of the operation to get the `scale-down-connectors` attribute of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-connectors` attribute value of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-connectors` attribute description: list of connectors used to form the `scale-down` cluster.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated/configuration=secondary:read-attribute(name=scale-down-connectors)`

## syntax of the operation to get the `scale-down-discovery-group` attribute of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-discovery-group` attribute value of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-discovery-group` attribute description: name of the discovery group used to build the `scale-down` cluster
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated/configuration=secondary:read-attribute(name=scale-down-discovery-group)`

## syntax of the operation to get the `scale-down-group-name` attribute of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scale-down-group-name` attribute value of the `secondary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `scale-down-group-name` attribute description: name of the group used to scale down.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated/configuration=secondary:read-attribute(name=scale-down-group-name)`

## syntax of the operation to get the `failover-on-server-shutdown` attribute of the `primary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `failover-on-server-shutdown` attribute value of the `primary` `configuration` resource of the `shared-store-colocated` `ha-policy` resource of the `server` resource of the `messaging-activemq` subsystem
* `failover-on-server-shutdown` attribute description: whether the server must `failover` when it is normally shutdown.
* operation: `/subsystem=messaging-activemq/server=<server name>/ha-policy=shared-store-colocated/configuration=primary:read-attribute(name=failover-on-server-shutdown)`

## syntax of the operation to get a `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-resource()`
* To get the list of all the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<connection-factory name>`.

## syntax of the operation to get the `auto-group` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `auto-group` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `auto-group` attribute description: whether or not message grouping is automatically used
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=auto-group)`

## syntax of the operation to get the `block-on-acknowledge` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-acknowledge` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `block-on-acknowledge` attribute description: true to set block on acknowledge.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=block-on-acknowledge)`

## syntax of the operation to get the `block-on-durable-send` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-durable-send` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `block-on-durable-send` attribute description: true to set block on durable send.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=block-on-durable-send)`

## syntax of the operation to get the `block-on-non-durable-send` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-non-durable-send` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `block-on-non-durable-send` attribute description: true to set block on non durable send.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=block-on-non-durable-send)`

## syntax of the operation to get the `cache-large-message-client` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `cache-large-message-client` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `cache-large-message-client` attribute description: true to cache large messages.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=cache-large-message-client)`

## syntax of the operation to get the `call-failover-timeout` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `call-failover-timeout` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `call-failover-timeout` attribute description: the timeout to use when fail over is in process (in ms).
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=call-failover-timeout)`

## syntax of the operation to get the `call-timeout` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `call-timeout` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `call-timeout` attribute description: the call time out.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=call-timeout)`

## syntax of the operation to get the `client-failure-check-period` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `client-failure-check-period` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `client-failure-check-period` attribute description: the client failure check period.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=client-failure-check-period)`

## syntax of the operation to get the `client-id` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `client-id` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `client-id` attribute description: the client id.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=client-id)`

## syntax of the operation to get the `compress-large-messages` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `compress-large-messages` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `compress-large-messages` attribute description: whether large messages should be compressed.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=compress-large-messages)`

## syntax of the operation to get the `confirmation-window-size` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `confirmation-window-size` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `confirmation-window-size` attribute description: the confirmation window size.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=confirmation-window-size)`

## syntax of the operation to get the `connection-load-balancing-policy-class-name` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connection-load-balancing-policy-class-name` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `connection-load-balancing-policy-class-name` attribute description: name of a class implementing a `client-side` load balancing policy that a client can use to load balance sessions across different nodes in a cluster.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=connection-load-balancing-policy-class-name)`

## syntax of the operation to get the `connection-ttl` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connection-ttl` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `connection-ttl` attribute description: the connection `ttl`.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=connection-ttl)`

## syntax of the operation to get the `connectors` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connectors` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `connectors` attribute description: defines the connectors. these are stored in a map by connector name (with an undefined value). it is possible to pass a list of connector names when writing this attribute.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=connectors)`

## syntax of the operation to get the `consumer-max-rate` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `consumer-max-rate` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `consumer-max-rate` attribute description: the consumer max rate.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=consumer-max-rate)`

## syntax of the operation to get the `consumer-window-size` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `consumer-window-size` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `consumer-window-size` attribute description: the consumer window size.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=consumer-window-size)`

## syntax of the operation to get the `deserialization-allow-list` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-allow-list` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `deserialization-allow-list` attribute description: a list of class names (separated by `whitespaces`) that are allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=deserialization-allow-list)`

## syntax of the operation to get the `deserialization-black-list` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-black-list` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `deserialization-black-list` attribute description: a list of class names (separated by `whitespaces`) that are not allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=deserialization-black-list)`

## syntax of the operation to get the `deserialization-block-list` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-block-list` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `deserialization-block-list` attribute description: a list of class names (separated by `whitespaces`) that are not allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=deserialization-block-list)`

## syntax of the operation to get the `deserialization-white-list` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-white-list` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `deserialization-white-list` attribute description: a list of class names (separated by `whitespaces`) that are allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=deserialization-white-list)`

## syntax of the operation to get the `discovery-group` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `discovery-group` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `discovery-group` attribute description: the discovery group name.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=discovery-group)`

## syntax of the operation to get the `dups-ok-batch-size` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `dups-ok-batch-size` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `dups-ok-batch-size` attribute description: the dups ok batch size.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=dups-ok-batch-size)`

## syntax of the operation to get the `entries` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `entries` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `entries` attribute description: the `jndi` names the connection factory should be bound to.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=entries)`

## syntax of the operation to get the `factory-type` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `factory-type` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `factory-type` attribute description: the type of connection factory.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=factory-type)`

## syntax of the operation to get the `failover-on-initial-connection` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `failover-on-initial-connection` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `failover-on-initial-connection` attribute description: true to fail over on initial connection.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=failover-on-initial-connection)`

## syntax of the operation to get the `group-id` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `group-id` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `group-id` attribute description: the group id.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=group-id)`

## syntax of the operation to get the `ha` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `ha` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `ha` attribute description: whether the connection factory supports high availability.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=ha)`

## syntax of the operation to get the `initial-message-packet-size` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-message-packet-size` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-message-packet-size` attribute description: the initial size of messages created through this factory.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=initial-message-packet-size)`

## syntax of the operation to get the `max-retry-interval` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-retry-interval` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-retry-interval` attribute description: the max retry interval.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=max-retry-interval)`

## syntax of the operation to get the `min-large-message-size` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `min-large-message-size` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `min-large-message-size` attribute description: the min large message size.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=min-large-message-size)`

## syntax of the operation to get the `pre-acknowledge` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `pre-acknowledge` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `pre-acknowledge` attribute description: true to `pre-acknowledge`.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=pre-acknowledge)`

## syntax of the operation to get the `producer-max-rate` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `producer-max-rate` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `producer-max-rate` attribute description: the producer max rate.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=producer-max-rate)`

## syntax of the operation to get the `producer-window-size` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `producer-window-size` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `producer-window-size` attribute description: the producer window size.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=producer-window-size)`

## syntax of the operation to get the `protocol-manager-factory` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `protocol-manager-factory` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `protocol-manager-factory` attribute description: the protocol manager factory used by this connection factory (it must implement `org.apache.activemq.artemis.spi.core.remoting.clientprotocolmanagerfactory`).
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=protocol-manager-factory)`

## syntax of the operation to get the `reconnect-attempts` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `reconnect-attempts` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `reconnect-attempts` attribute description: the reconnect attempts.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=reconnect-attempts)`

## syntax of the operation to get the `retry-interval` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `retry-interval` attribute description: the retry interval.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=retry-interval)`

## syntax of the operation to get the `retry-interval-multiplier` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval-multiplier` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `retry-interval-multiplier` attribute description: the retry interval multiplier.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=retry-interval-multiplier)`

## syntax of the operation to get the `scheduled-thread-pool-max-size` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scheduled-thread-pool-max-size` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `scheduled-thread-pool-max-size` attribute description: the scheduled thread pool max size.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=scheduled-thread-pool-max-size)`

## syntax of the operation to get the `thread-pool-max-size` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `thread-pool-max-size` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `thread-pool-max-size` attribute description: the thread pool max size.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=thread-pool-max-size)`

## syntax of the operation to get the `transaction-batch-size` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `transaction-batch-size` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `transaction-batch-size` attribute description: the transaction batch size.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=transaction-batch-size)`

## syntax of the operation to get the `use-global-pools` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `use-global-pools` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `use-global-pools` attribute description: true to use global pools.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=use-global-pools)`

## syntax of the operation to get the `use-topology-for-load-balancing` attribute of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `use-topology-for-load-balancing` attribute value of the `connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `use-topology-for-load-balancing` attribute description: whether to use topology information from the cluster to connect to it. if set to 'false', only the initial connector will be used.
* operation: `/subsystem=messaging-activemq/server=<server name>/connection-factory=<connection-factory name>:read-attribute(name=use-topology-for-load-balancing)`

## syntax of the operation to get a `http-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/http-acceptor=<http-acceptor name>:read-resource()`
* To get the list of all the `http-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<http-acceptor name>`.

## syntax of the operation to get the `http-listener` attribute of the `http-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `http-listener` attribute value of the `http-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `http-listener` attribute description: the `undertow's` `http-listener` that handles `http` upgrade requests.
* operation: `/subsystem=messaging-activemq/server=<server name>/http-acceptor=<http-acceptor name>:read-attribute(name=http-listener)`

## syntax of the operation to get the `params` attribute of the `http-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `params` attribute value of the `http-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `params` attribute description: a `key-value` pair understood by the acceptor `factory-class` and used to configure it.
* operation: `/subsystem=messaging-activemq/server=<server name>/http-acceptor=<http-acceptor name>:read-attribute(name=params)`

## syntax of the operation to get the `ssl-context` attribute of the `http-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `ssl-context` attribute value of the `http-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `ssl-context` attribute description: the `ssl` context that the acceptor will use to accept connections.
* operation: `/subsystem=messaging-activemq/server=<server name>/http-acceptor=<http-acceptor name>:read-attribute(name=ssl-context)`

## syntax of the operation to get the `upgrade-legacy` attribute of the `http-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `upgrade-legacy` attribute value of the `http-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `upgrade-legacy` attribute description: also accepts to upgrade `http` request from legacy (`hornetq`) clients.
* operation: `/subsystem=messaging-activemq/server=<server name>/http-acceptor=<http-acceptor name>:read-attribute(name=upgrade-legacy)`

## syntax of the operation to get a `connector-service` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/connector-service=<connector-service name>:read-resource()`
* To get the list of all the `connector-service` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<connector-service name>`.

## syntax of the operation to get the `factory-class` attribute of the `connector-service` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `factory-class` attribute value of the `connector-service` resource of the `server` resource of the `messaging-activemq` subsystem
* `factory-class` attribute description: class name of the factory class that can instantiate the connector service.
* operation: `/subsystem=messaging-activemq/server=<server name>/connector-service=<connector-service name>:read-attribute(name=factory-class)`

## syntax of the operation to get the `params` attribute of the `connector-service` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `params` attribute value of the `connector-service` resource of the `server` resource of the `messaging-activemq` subsystem
* `params` attribute description: a `key/value` pair understood by the connector `service's` `factory-class`
* operation: `/subsystem=messaging-activemq/server=<server name>/connector-service=<connector-service name>:read-attribute(name=params)`

## syntax of the operation to get a `http-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/http-connector=<http-connector name>:read-resource()`
* To get the list of all the `http-connector` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<http-connector name>`.

## syntax of the operation to get the `endpoint` attribute of the `http-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `endpoint` attribute value of the `http-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* `endpoint` attribute description: the `http-acceptor` that serves as the endpoint of this `http-connector`.
* operation: `/subsystem=messaging-activemq/server=<server name>/http-connector=<http-connector name>:read-attribute(name=endpoint)`

## syntax of the operation to get the `params` attribute of the `http-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `params` attribute value of the `http-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* `params` attribute description: a `key-value` pair understood by the connector `factory-class` and used to configure it.
* operation: `/subsystem=messaging-activemq/server=<server name>/http-connector=<http-connector name>:read-attribute(name=params)`

## syntax of the operation to get the `server-name` attribute of the `http-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `server-name` attribute value of the `http-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* `server-name` attribute description: the name of the `activemq` artemis server that will be connected to on the remote server. if undefined, the name of the parent `activemq` artemis server will be used (suitable if the `http-connector` is used to connect to the parent server)
* operation: `/subsystem=messaging-activemq/server=<server name>/http-connector=<http-connector name>:read-attribute(name=server-name)`

## syntax of the operation to get the `socket-binding` attribute of the `http-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `http-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the socket binding that the connector will use to create connections
* operation: `/subsystem=messaging-activemq/server=<server name>/http-connector=<http-connector name>:read-attribute(name=socket-binding)`

## syntax of the operation to get the `ssl-context` attribute of the `http-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `ssl-context` attribute value of the `http-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* `ssl-context` attribute description: the `ssl` context that the connector will use to connect
* operation: `/subsystem=messaging-activemq/server=<server name>/http-connector=<http-connector name>:read-attribute(name=ssl-context)`

## syntax of the operation to get a `in-vm-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/in-vm-connector=<in-vm-connector name>:read-resource()`
* To get the list of all the `in-vm-connector` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<in-vm-connector name>`.

## syntax of the operation to get the `params` attribute of the `in-vm-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `params` attribute value of the `in-vm-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* `params` attribute description: a `key-value` pair understood by the connector `factory-class` and used to configure it.
* operation: `/subsystem=messaging-activemq/server=<server name>/in-vm-connector=<in-vm-connector name>:read-attribute(name=params)`

## syntax of the operation to get the `server-id` attribute of the `in-vm-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `server-id` attribute value of the `in-vm-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* `server-id` attribute description: the server id.
* operation: `/subsystem=messaging-activemq/server=<server name>/in-vm-connector=<in-vm-connector name>:read-attribute(name=server-id)`

## syntax of the operation to get a `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/jgroups-broadcast-group=<jgroups-broadcast-group name>:read-resource()`
* To get the list of all the `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<jgroups-broadcast-group name>`.

## syntax of the operation to get the `broadcast-period` attribute of the `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `broadcast-period` attribute value of the `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `broadcast-period` attribute description: the period in milliseconds between consecutive broadcasts.
* operation: `/subsystem=messaging-activemq/server=<server name>/jgroups-broadcast-group=<jgroups-broadcast-group name>:read-attribute(name=broadcast-period)`

## syntax of the operation to get the `connectors` attribute of the `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connectors` attribute value of the `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `connectors` attribute description: specifies the names of connectors that will be broadcast.
* operation: `/subsystem=messaging-activemq/server=<server name>/jgroups-broadcast-group=<jgroups-broadcast-group name>:read-attribute(name=connectors)`

## syntax of the operation to get the `jgroups-channel` attribute of the `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-channel` attribute value of the `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `jgroups-channel` attribute description: references the name of a `jgroups` channel.  if undefined, the default channel will be used.
* operation: `/subsystem=messaging-activemq/server=<server name>/jgroups-broadcast-group=<jgroups-broadcast-group name>:read-attribute(name=jgroups-channel)`

## syntax of the operation to get the `jgroups-cluster` attribute of the `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-cluster` attribute value of the `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `jgroups-cluster` attribute description: the logical cluster name.
* operation: `/subsystem=messaging-activemq/server=<server name>/jgroups-broadcast-group=<jgroups-broadcast-group name>:read-attribute(name=jgroups-cluster)`

## syntax of the operation to get the `jgroups-stack` attribute of the `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-stack` attribute value of the `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `jgroups-stack` attribute description: references the name of a `jgroups` channel factory.
* operation: `/subsystem=messaging-activemq/server=<server name>/jgroups-broadcast-group=<jgroups-broadcast-group name>:read-attribute(name=jgroups-stack)`

## syntax of the operation to get the `started` attribute of the `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `started` attribute value of the `jgroups-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `started` attribute description: whether the broadcast group is started.
* operation: `/subsystem=messaging-activemq/server=<server name>/jgroups-broadcast-group=<jgroups-broadcast-group name>:read-attribute(name=started)`

## syntax of the operation to get a `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/security-setting=<security-setting name>:read-resource()`
* To get the list of all the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<security-setting name>`.

## syntax of the operation to get a `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/security-setting=<security-setting name>/role=<role name>:read-resource()`
* To get the list of all the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<role name>`.

## syntax of the operation to get the `consume` attribute of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `consume` attribute value of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `consume` attribute description: this permission allows the user to consume a message from a queue bound to matching addresses.
* operation: `/subsystem=messaging-activemq/server=<server name>/security-setting=<security-setting name>/role=<role name>:read-attribute(name=consume)`

## syntax of the operation to get the `create-durable-queue` attribute of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `create-durable-queue` attribute value of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `create-durable-queue` attribute description: this permission allows the user to create a durable queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/security-setting=<security-setting name>/role=<role name>:read-attribute(name=create-durable-queue)`

## syntax of the operation to get the `create-non-durable-queue` attribute of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `create-non-durable-queue` attribute value of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `create-non-durable-queue` attribute description: this permission allows the user to create a temporary queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/security-setting=<security-setting name>/role=<role name>:read-attribute(name=create-non-durable-queue)`

## syntax of the operation to get the `delete-durable-queue` attribute of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `delete-durable-queue` attribute value of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `delete-durable-queue` attribute description: this permission allows the user to delete a durable queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/security-setting=<security-setting name>/role=<role name>:read-attribute(name=delete-durable-queue)`

## syntax of the operation to get the `delete-non-durable-queue` attribute of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `delete-non-durable-queue` attribute value of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `delete-non-durable-queue` attribute description: this permission allows the user to delete a temporary queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/security-setting=<security-setting name>/role=<role name>:read-attribute(name=delete-non-durable-queue)`

## syntax of the operation to get the `manage` attribute of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `manage` attribute value of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `manage` attribute description: this permission allows the user to invoke management operations by sending management messages to the management address.
* operation: `/subsystem=messaging-activemq/server=<server name>/security-setting=<security-setting name>/role=<role name>:read-attribute(name=manage)`

## syntax of the operation to get the `send` attribute of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `send` attribute value of the `role` resource of the `security-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `send` attribute description: this permission allows the user to send a message to matching addresses.
* operation: `/subsystem=messaging-activemq/server=<server name>/security-setting=<security-setting name>/role=<role name>:read-attribute(name=send)`

## syntax of the operation to get a `divert` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/divert=<divert name>:read-resource()`
* To get the list of all the `divert` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<divert name>`.

## syntax of the operation to get the `divert-address` attribute of the `divert` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `divert-address` attribute value of the `divert` resource of the `server` resource of the `messaging-activemq` subsystem
* `divert-address` attribute description: address to divert from
* operation: `/subsystem=messaging-activemq/server=<server name>/divert=<divert name>:read-attribute(name=divert-address)`

## syntax of the operation to get the `exclusive` attribute of the `divert` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `exclusive` attribute value of the `divert` resource of the `server` resource of the `messaging-activemq` subsystem
* `exclusive` attribute description: whether the divert is exclusive, meaning that the message is diverted to the new address, and does not go to the old address at all.
* operation: `/subsystem=messaging-activemq/server=<server name>/divert=<divert name>:read-attribute(name=exclusive)`

## syntax of the operation to get the `filter` attribute of the `divert` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `filter` attribute value of the `divert` resource of the `server` resource of the `messaging-activemq` subsystem
* `filter` attribute description: an optional filter string. if specified then only messages which match the filter expression specified will be diverted. the filter string follows the `activemq` filter expression syntax described in the `activemq` documentation.
* operation: `/subsystem=messaging-activemq/server=<server name>/divert=<divert name>:read-attribute(name=filter)`

## syntax of the operation to get the `forwarding-address` attribute of the `divert` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `forwarding-address` attribute value of the `divert` resource of the `server` resource of the `messaging-activemq` subsystem
* `forwarding-address` attribute description: address to divert to
* operation: `/subsystem=messaging-activemq/server=<server name>/divert=<divert name>:read-attribute(name=forwarding-address)`

## syntax of the operation to get the `routing-name` attribute of the `divert` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `routing-name` attribute value of the `divert` resource of the `server` resource of the `messaging-activemq` subsystem
* `routing-name` attribute description: routing name of the divert
* operation: `/subsystem=messaging-activemq/server=<server name>/divert=<divert name>:read-attribute(name=routing-name)`

## syntax of the operation to get the `transformer-class-name` attribute of the `divert` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `transformer-class-name` attribute value of the `divert` resource of the `server` resource of the `messaging-activemq` subsystem
* `transformer-class-name` attribute description: the name of a class used to transform the `message's` body or properties before it is diverted.
* operation: `/subsystem=messaging-activemq/server=<server name>/divert=<divert name>:read-attribute(name=transformer-class-name)`

## syntax of the operation to get a `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-resource()`
* To get the list of all the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<pooled-connection-factory name>`.

## syntax of the operation to get the `allow-local-transactions` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `allow-local-transactions` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `allow-local-transactions` attribute description: allow local transactions for outbond jakarta messaging sessions (it does not apply to `jmscontext` that explicitly disallows it).
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=allow-local-transactions)`

## syntax of the operation to get the `auto-group` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `auto-group` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `auto-group` attribute description: the `autogroup`.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=auto-group)`

## syntax of the operation to get the `block-on-acknowledge` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-acknowledge` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `block-on-acknowledge` attribute description: true to set block on acknowledge.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=block-on-acknowledge)`

## syntax of the operation to get the `block-on-durable-send` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-durable-send` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `block-on-durable-send` attribute description: true to set block on durable send.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=block-on-durable-send)`

## syntax of the operation to get the `block-on-non-durable-send` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-non-durable-send` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `block-on-non-durable-send` attribute description: true to set block on non durable send.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=block-on-non-durable-send)`

## syntax of the operation to get the `cache-large-message-client` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `cache-large-message-client` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `cache-large-message-client` attribute description: true to cache large messages.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=cache-large-message-client)`

## syntax of the operation to get the `call-failover-timeout` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `call-failover-timeout` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `call-failover-timeout` attribute description: the timeout to use when fail over is in process (in ms).
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=call-failover-timeout)`

## syntax of the operation to get the `call-timeout` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `call-timeout` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `call-timeout` attribute description: the call time out.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=call-timeout)`

## syntax of the operation to get the `client-failure-check-period` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `client-failure-check-period` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `client-failure-check-period` attribute description: the client failure check period.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=client-failure-check-period)`

## syntax of the operation to get the `client-id` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `client-id` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `client-id` attribute description: the client id.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=client-id)`

## syntax of the operation to get the `compress-large-messages` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `compress-large-messages` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `compress-large-messages` attribute description: whether large messages should be compressed.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=compress-large-messages)`

## syntax of the operation to get the `confirmation-window-size` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `confirmation-window-size` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `confirmation-window-size` attribute description: the confirmation window size.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=confirmation-window-size)`

## syntax of the operation to get the `connection-load-balancing-policy-class-name` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connection-load-balancing-policy-class-name` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `connection-load-balancing-policy-class-name` attribute description: name of a class implementing a `client-side` load balancing policy that a client can use to load balance sessions across different nodes in a cluster.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=connection-load-balancing-policy-class-name)`

## syntax of the operation to get the `connection-ttl` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connection-ttl` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `connection-ttl` attribute description: the connection `ttl`.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=connection-ttl)`

## syntax of the operation to get the `connectors` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connectors` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `connectors` attribute description: defines the connectors. these are stored in a map by connector name (with an undefined value). it is possible to pass a list of connector names when writing this attribute.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=connectors)`

## syntax of the operation to get the `consumer-max-rate` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `consumer-max-rate` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `consumer-max-rate` attribute description: the consumer max rate.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=consumer-max-rate)`

## syntax of the operation to get the `consumer-window-size` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `consumer-window-size` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `consumer-window-size` attribute description: the consumer window size.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=consumer-window-size)`

## syntax of the operation to get the `credential-reference` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `credential-reference` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `credential-reference` attribute description: credential (from credential store) to authenticate the pooled connection factory
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=credential-reference)`

## syntax of the operation to get the `deserialization-allow-list` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-allow-list` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `deserialization-allow-list` attribute description: a list of class names (separated by `whitespaces`) that are allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=deserialization-allow-list)`

## syntax of the operation to get the `deserialization-black-list` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-black-list` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `deserialization-black-list` attribute description: a list of class names (separated by `whitespaces`) that are not allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=deserialization-black-list)`

## syntax of the operation to get the `deserialization-block-list` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-block-list` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `deserialization-block-list` attribute description: a list of class names (separated by `whitespaces`) that are not allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=deserialization-block-list)`

## syntax of the operation to get the `deserialization-white-list` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-white-list` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `deserialization-white-list` attribute description: a list of class names (separated by `whitespaces`) that are allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=deserialization-white-list)`

## syntax of the operation to get the `discovery-group` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `discovery-group` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `discovery-group` attribute description: the discovery group name.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=discovery-group)`

## syntax of the operation to get the `dups-ok-batch-size` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `dups-ok-batch-size` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `dups-ok-batch-size` attribute description: the dups ok batch size.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=dups-ok-batch-size)`

## syntax of the operation to get the `enlistment-trace` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `enlistment-trace` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `enlistment-trace` attribute description: enables `ironjacamar` to record enlistment traces for this `pooled-connection-factory`. this attribute is undefined by default and the behaviour is driven by the presence of the `ironjacamar.disable_enlistment_trace` system property.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=enlistment-trace)`

## syntax of the operation to get the `entries` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `entries` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `entries` attribute description: the `jndi` names the connection factory should be bound to.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=entries)`

## syntax of the operation to get the `failover-on-initial-connection` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `failover-on-initial-connection` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `failover-on-initial-connection` attribute description: true to fail over on initial connection.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=failover-on-initial-connection)`

## syntax of the operation to get the `group-id` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `group-id` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `group-id` attribute description: the group id.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=group-id)`

## syntax of the operation to get the `ha` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `ha` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `ha` attribute description: whether the connection factory supports high availability.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=ha)`

## syntax of the operation to get the `initial-connect-attempts` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-connect-attempts` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-connect-attempts` attribute description: the number of attempts to connect initially with this factory.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=initial-connect-attempts)`

## syntax of the operation to get the `initial-message-packet-size` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-message-packet-size` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-message-packet-size` attribute description: the initial size of messages created through this factory.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=initial-message-packet-size)`

## syntax of the operation to get the `jndi-params` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jndi-params` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `jndi-params` attribute description: the `jndi` `params` to use for locating the destination for incoming connections.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=jndi-params)`

## syntax of the operation to get the `managed-connection-pool` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `managed-connection-pool` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `managed-connection-pool` attribute description: the class name of the managed connection pool used by this `pooled-connection-factory`.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=managed-connection-pool)`

## syntax of the operation to get the `max-pool-size` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-pool-size` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-pool-size` attribute description: the maximum size for the pool
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=max-pool-size)`

## syntax of the operation to get the `max-retry-interval` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-retry-interval` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-retry-interval` attribute description: the max retry interval.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=max-retry-interval)`

## syntax of the operation to get the `min-large-message-size` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `min-large-message-size` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `min-large-message-size` attribute description: the min large message size.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=min-large-message-size)`

## syntax of the operation to get the `min-pool-size` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `min-pool-size` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `min-pool-size` attribute description: the minimum size for the pool
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=min-pool-size)`

## syntax of the operation to get the `password` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `password` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `password` attribute description: the default password to use with this connection factory. this is only needed when pointing the connection factory to a remote host.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=password)`

## syntax of the operation to get the `pre-acknowledge` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `pre-acknowledge` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `pre-acknowledge` attribute description: true to `pre-acknowledge`.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=pre-acknowledge)`

## syntax of the operation to get the `producer-max-rate` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `producer-max-rate` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `producer-max-rate` attribute description: the producer max rate.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=producer-max-rate)`

## syntax of the operation to get the `producer-window-size` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `producer-window-size` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `producer-window-size` attribute description: the producer window size.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=producer-window-size)`

## syntax of the operation to get the `protocol-manager-factory` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `protocol-manager-factory` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `protocol-manager-factory` attribute description: the protocol manager factory used by this pooled connection factory.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=protocol-manager-factory)`

## syntax of the operation to get the `rebalance-connections` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `rebalance-connections` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `rebalance-connections` attribute description: rebalance inbound connections when cluster topology changes.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=rebalance-connections)`

## syntax of the operation to get the `reconnect-attempts` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `reconnect-attempts` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `reconnect-attempts` attribute description: the reconnect attempts. by default, a pooled connection factory will try to reconnect infinitely to the messaging `server(s`).
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=reconnect-attempts)`

## syntax of the operation to get the `retry-interval` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `retry-interval` attribute description: the retry interval.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=retry-interval)`

## syntax of the operation to get the `retry-interval-multiplier` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval-multiplier` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `retry-interval-multiplier` attribute description: the retry interval multiplier.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=retry-interval-multiplier)`

## syntax of the operation to get the `scheduled-thread-pool-max-size` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scheduled-thread-pool-max-size` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `scheduled-thread-pool-max-size` attribute description: the scheduled thread pool max size.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=scheduled-thread-pool-max-size)`

## syntax of the operation to get the `setup-attempts` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `setup-attempts` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `setup-attempts` attribute description: the number of times to set up an `mdb` endpoint
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=setup-attempts)`

## syntax of the operation to get the `setup-interval` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `setup-interval` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `setup-interval` attribute description: the interval between attempts at setting up an `mdb` endpoint.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=setup-interval)`

## syntax of the operation to get the `statistics-enabled` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `statistics-enabled` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `statistics-enabled` attribute description: define whether runtime statistics are enabled.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `thread-pool-max-size` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `thread-pool-max-size` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `thread-pool-max-size` attribute description: the thread pool max size.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=thread-pool-max-size)`

## syntax of the operation to get the `transaction` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `transaction` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `transaction` attribute description: the type of transaction supported by this pooled connection factory (can be local, none or `xa`, default is `xa`).
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=transaction)`

## syntax of the operation to get the `transaction-batch-size` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `transaction-batch-size` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `transaction-batch-size` attribute description: the transaction batch size.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=transaction-batch-size)`

## syntax of the operation to get the `use-auto-recovery` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `use-auto-recovery` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `use-auto-recovery` attribute description: true to use auto recovery.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=use-auto-recovery)`

## syntax of the operation to get the `use-global-pools` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `use-global-pools` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `use-global-pools` attribute description: true to use global pools.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=use-global-pools)`

## syntax of the operation to get the `use-jndi` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `use-jndi` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `use-jndi` attribute description: use `jndi` to locate the destination for incoming connections
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=use-jndi)`

## syntax of the operation to get the `use-local-tx` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `use-local-tx` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `use-local-tx` attribute description: use a local transaction for incoming sessions
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=use-local-tx)`

## syntax of the operation to get the `use-topology-for-load-balancing` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `use-topology-for-load-balancing` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `use-topology-for-load-balancing` attribute description: whether to use topology information from the cluster to connect to it. if set to 'false', only the initial connector will be used.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=use-topology-for-load-balancing)`

## syntax of the operation to get the `user` attribute of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `user` attribute value of the `pooled-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `user` attribute description: the default `username` to use with this connection factory. this is only needed when pointing the connection factory to a remote host.
* operation: `/subsystem=messaging-activemq/server=<server name>/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=user)`

## syntax of the operation to get a `acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/acceptor=<acceptor name>:read-resource()`
* To get the list of all the `acceptor` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<acceptor name>`.

## syntax of the operation to get the `factory-class` attribute of the `acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `factory-class` attribute value of the `acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `factory-class` attribute description: class name of the factory class that can instantiate the acceptor.
* operation: `/subsystem=messaging-activemq/server=<server name>/acceptor=<acceptor name>:read-attribute(name=factory-class)`

## syntax of the operation to get the `params` attribute of the `acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `params` attribute value of the `acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `params` attribute description: a `key-value` pair understood by the acceptor `factory-class` and used to configure it.
* operation: `/subsystem=messaging-activemq/server=<server name>/acceptor=<acceptor name>:read-attribute(name=params)`

## syntax of the operation to get the `socket-binding` attribute of the `acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the socket binding that the acceptor will use to accept connections.
* operation: `/subsystem=messaging-activemq/server=<server name>/acceptor=<acceptor name>:read-attribute(name=socket-binding)`

## syntax of the operation to get the `started` attribute of the `acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `started` attribute value of the `acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `started` attribute description: whether this acceptor is started.
* operation: `/subsystem=messaging-activemq/server=<server name>/acceptor=<acceptor name>:read-attribute(name=started)`

## syntax of the operation to get a `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-resource()`
* To get the list of all the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<legacy-connection-factory name>`.

## syntax of the operation to get the `auto-group` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `auto-group` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `auto-group` attribute description: whether or not message grouping is automatically used
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=auto-group)`

## syntax of the operation to get the `block-on-acknowledge` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-acknowledge` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `block-on-acknowledge` attribute description: true to set block on acknowledge.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=block-on-acknowledge)`

## syntax of the operation to get the `block-on-durable-send` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-durable-send` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `block-on-durable-send` attribute description: true to set block on durable send.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=block-on-durable-send)`

## syntax of the operation to get the `block-on-non-durable-send` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-non-durable-send` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `block-on-non-durable-send` attribute description: true to set block on non durable send.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=block-on-non-durable-send)`

## syntax of the operation to get the `cache-large-message-client` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `cache-large-message-client` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `cache-large-message-client` attribute description: true to cache large messages.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=cache-large-message-client)`

## syntax of the operation to get the `call-failover-timeout` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `call-failover-timeout` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `call-failover-timeout` attribute description: the timeout to use when fail over is in process (in ms).
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=call-failover-timeout)`

## syntax of the operation to get the `call-timeout` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `call-timeout` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `call-timeout` attribute description: the call time out.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=call-timeout)`

## syntax of the operation to get the `client-failure-check-period` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `client-failure-check-period` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `client-failure-check-period` attribute description: the client failure check period.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=client-failure-check-period)`

## syntax of the operation to get the `client-id` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `client-id` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `client-id` attribute description: the client id.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=client-id)`

## syntax of the operation to get the `compress-large-messages` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `compress-large-messages` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `compress-large-messages` attribute description: whether large messages should be compressed.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=compress-large-messages)`

## syntax of the operation to get the `confirmation-window-size` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `confirmation-window-size` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `confirmation-window-size` attribute description: the confirmation window size.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=confirmation-window-size)`

## syntax of the operation to get the `connection-load-balancing-policy-class-name` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connection-load-balancing-policy-class-name` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `connection-load-balancing-policy-class-name` attribute description: name of a class implementing a `client-side` load balancing policy that a client can use to load balance sessions across different nodes in a cluster.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=connection-load-balancing-policy-class-name)`

## syntax of the operation to get the `connection-ttl` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connection-ttl` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `connection-ttl` attribute description: the connection `ttl`.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=connection-ttl)`

## syntax of the operation to get the `connectors` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connectors` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `connectors` attribute description: defines the connectors. these are stored in a map by connector name (with an undefined value). it is possible to pass a list of connector names when writing this attribute.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=connectors)`

## syntax of the operation to get the `consumer-max-rate` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `consumer-max-rate` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `consumer-max-rate` attribute description: the consumer max rate.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=consumer-max-rate)`

## syntax of the operation to get the `consumer-window-size` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `consumer-window-size` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `consumer-window-size` attribute description: the consumer window size.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=consumer-window-size)`

## syntax of the operation to get the `discovery-group` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `discovery-group` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `discovery-group` attribute description: the discovery group name.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=discovery-group)`

## syntax of the operation to get the `dups-ok-batch-size` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `dups-ok-batch-size` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `dups-ok-batch-size` attribute description: the dups ok batch size.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=dups-ok-batch-size)`

## syntax of the operation to get the `entries` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `entries` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `entries` attribute description: the `jndi` names the connection factory should be bound to.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=entries)`

## syntax of the operation to get the `factory-type` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `factory-type` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `factory-type` attribute description: the type of connection factory.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=factory-type)`

## syntax of the operation to get the `failover-on-initial-connection` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `failover-on-initial-connection` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `failover-on-initial-connection` attribute description: true to fail over on initial connection.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=failover-on-initial-connection)`

## syntax of the operation to get the `group-id` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `group-id` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `group-id` attribute description: the group id.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=group-id)`

## syntax of the operation to get the `ha` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `ha` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `ha` attribute description: whether the connection factory supports high availability.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=ha)`

## syntax of the operation to get the `initial-connect-attempts` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-connect-attempts` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-connect-attempts` attribute description: the number of attempts for the initial connection to the server.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=initial-connect-attempts)`

## syntax of the operation to get the `initial-message-packet-size` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-message-packet-size` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-message-packet-size` attribute description: the initial size of messages created through this factory.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=initial-message-packet-size)`

## syntax of the operation to get the `max-retry-interval` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-retry-interval` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-retry-interval` attribute description: the max retry interval.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=max-retry-interval)`

## syntax of the operation to get the `min-large-message-size` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `min-large-message-size` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `min-large-message-size` attribute description: the min large message size.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=min-large-message-size)`

## syntax of the operation to get the `pre-acknowledge` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `pre-acknowledge` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `pre-acknowledge` attribute description: true to `pre-acknowledge`.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=pre-acknowledge)`

## syntax of the operation to get the `producer-max-rate` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `producer-max-rate` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `producer-max-rate` attribute description: the producer max rate.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=producer-max-rate)`

## syntax of the operation to get the `producer-window-size` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `producer-window-size` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `producer-window-size` attribute description: the producer window size.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=producer-window-size)`

## syntax of the operation to get the `reconnect-attempts` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `reconnect-attempts` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `reconnect-attempts` attribute description: the reconnect attempts.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=reconnect-attempts)`

## syntax of the operation to get the `retry-interval` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `retry-interval` attribute description: the retry interval.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=retry-interval)`

## syntax of the operation to get the `retry-interval-multiplier` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval-multiplier` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `retry-interval-multiplier` attribute description: the retry interval multiplier.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=retry-interval-multiplier)`

## syntax of the operation to get the `scheduled-thread-pool-max-size` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scheduled-thread-pool-max-size` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `scheduled-thread-pool-max-size` attribute description: the scheduled thread pool max size.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=scheduled-thread-pool-max-size)`

## syntax of the operation to get the `thread-pool-max-size` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `thread-pool-max-size` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `thread-pool-max-size` attribute description: the thread pool max size.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=thread-pool-max-size)`

## syntax of the operation to get the `transaction-batch-size` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `transaction-batch-size` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `transaction-batch-size` attribute description: the transaction batch size.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=transaction-batch-size)`

## syntax of the operation to get the `use-global-pools` attribute of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `use-global-pools` attribute value of the `legacy-connection-factory` resource of the `server` resource of the `messaging-activemq` subsystem
* `use-global-pools` attribute description: true to use global pools.
* operation: `/subsystem=messaging-activemq/server=<server name>/legacy-connection-factory=<legacy-connection-factory name>:read-attribute(name=use-global-pools)`

## syntax of the operation to get a `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-resource()`
* To get the list of all the `queue` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<queue name>`.

## syntax of the operation to get the `consumer-count` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `consumer-count` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `consumer-count` attribute description: the number of consumers consuming messages from this queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=consumer-count)`

## syntax of the operation to get the `dead-letter-address` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `dead-letter-address` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `dead-letter-address` attribute description: the address to send the `queue's` dead messages to.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=dead-letter-address)`

## syntax of the operation to get the `delivering-count` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `delivering-count` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `delivering-count` attribute description: the number of messages that this queue is currently delivering to its consumers.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=delivering-count)`

## syntax of the operation to get the `durable` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `durable` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `durable` attribute description: defines whether the queue is durable.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=durable)`

## syntax of the operation to get the `expiry-address` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `expiry-address` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `expiry-address` attribute description: the address to send the `queue's` expired messages to.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=expiry-address)`

## syntax of the operation to get the `filter` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `filter` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `filter` attribute description: a queue message filter definition. an undefined or empty filter will match all messages.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=filter)`

## syntax of the operation to get the `id` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `id` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `id` attribute description: the id of the queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=id)`

## syntax of the operation to get the `message-count` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `message-count` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `message-count` attribute description: the number of messages currently in this queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=message-count)`

## syntax of the operation to get the `messages-added` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `messages-added` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `messages-added` attribute description: the number of messages added to this queue since it was created.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=messages-added)`

## syntax of the operation to get the `paused` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `paused` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `paused` attribute description: whether the queue is paused.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=paused)`

## syntax of the operation to get the `queue-address` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `queue-address` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `queue-address` attribute description: the queue address defines what address is used for routing messages.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=queue-address)`

## syntax of the operation to get the `routing-type` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `routing-type` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `routing-type` attribute description: routing type of the queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=routing-type)`

## syntax of the operation to get the `scheduled-count` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scheduled-count` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `scheduled-count` attribute description: the number of scheduled messages in this queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=scheduled-count)`

## syntax of the operation to get the `temporary` attribute of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `temporary` attribute value of the `queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `temporary` attribute description: whether the queue is temporary.
* operation: `/subsystem=messaging-activemq/server=<server name>/queue=<queue name>:read-attribute(name=temporary)`

## syntax of the operation to get a `in-vm-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/in-vm-acceptor=<in-vm-acceptor name>:read-resource()`
* To get the list of all the `in-vm-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<in-vm-acceptor name>`.

## syntax of the operation to get the `params` attribute of the `in-vm-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `params` attribute value of the `in-vm-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `params` attribute description: a `key-value` pair understood by the acceptor `factory-class` and used to configure it.
* operation: `/subsystem=messaging-activemq/server=<server name>/in-vm-acceptor=<in-vm-acceptor name>:read-attribute(name=params)`

## syntax of the operation to get the `server-id` attribute of the `in-vm-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `server-id` attribute value of the `in-vm-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `server-id` attribute description: the server id.
* operation: `/subsystem=messaging-activemq/server=<server name>/in-vm-acceptor=<in-vm-acceptor name>:read-attribute(name=server-id)`

## syntax of the operation to get the `started` attribute of the `in-vm-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `started` attribute value of the `in-vm-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `started` attribute description: whether this acceptor is started.
* operation: `/subsystem=messaging-activemq/server=<server name>/in-vm-acceptor=<in-vm-acceptor name>:read-attribute(name=started)`

## syntax of the operation to get a `grouping-handler` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/grouping-handler=<grouping-handler name>:read-resource()`
* To get the list of all the `grouping-handler` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<grouping-handler name>`.

## syntax of the operation to get the `group-timeout` attribute of the `grouping-handler` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `group-timeout` attribute value of the `grouping-handler` resource of the `server` resource of the `messaging-activemq` subsystem
* `group-timeout` attribute description: how long a group binding will be used, -`1` means for ever. bindings are removed after this wait elapses (valid for both local and remote handlers).
* operation: `/subsystem=messaging-activemq/server=<server name>/grouping-handler=<grouping-handler name>:read-attribute(name=group-timeout)`

## syntax of the operation to get the `grouping-handler-address` attribute of the `grouping-handler` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `grouping-handler-address` attribute value of the `grouping-handler` resource of the `server` resource of the `messaging-activemq` subsystem
* `grouping-handler-address` attribute description: a reference to a cluster connection and the address it uses.
* operation: `/subsystem=messaging-activemq/server=<server name>/grouping-handler=<grouping-handler name>:read-attribute(name=grouping-handler-address)`

## syntax of the operation to get the `reaper-period` attribute of the `grouping-handler` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `reaper-period` attribute value of the `grouping-handler` resource of the `server` resource of the `messaging-activemq` subsystem
* `reaper-period` attribute description: how often the reaper will be run to check for timed out group bindings (only valid for local handlers).
* operation: `/subsystem=messaging-activemq/server=<server name>/grouping-handler=<grouping-handler name>:read-attribute(name=reaper-period)`

## syntax of the operation to get the `timeout` attribute of the `grouping-handler` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `timeout` attribute value of the `grouping-handler` resource of the `server` resource of the `messaging-activemq` subsystem
* `timeout` attribute description: how long to wait for a handling decision to be made; an exception will be thrown during the send if this timeout is reached, ensuring that strict ordering is kept.
* operation: `/subsystem=messaging-activemq/server=<server name>/grouping-handler=<grouping-handler name>:read-attribute(name=timeout)`

## syntax of the operation to get the `type` attribute of the `grouping-handler` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `type` attribute value of the `grouping-handler` resource of the `server` resource of the `messaging-activemq` subsystem
* `type` attribute description: whether the handler is the single "local" handler for the cluster, which makes handling decisions, or a "remote" handler which converses with the local handler.
* operation: `/subsystem=messaging-activemq/server=<server name>/grouping-handler=<grouping-handler name>:read-attribute(name=type)`

## syntax of the operation to get a `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>:read-resource()`
* To get the list of all the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<core-address name>`.

## syntax of the operation to get the `binding-names` attribute of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `binding-names` attribute value of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `binding-names` attribute description: the names of all bindings (both queues and diverts) bound to this address.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>:read-attribute(name=binding-names)`

## syntax of the operation to get the `number-of-bytes-per-page` attribute of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `number-of-bytes-per-page` attribute value of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `number-of-bytes-per-page` attribute description: the number of bytes used by each page for this address.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>:read-attribute(name=number-of-bytes-per-page)`

## syntax of the operation to get the `number-of-pages` attribute of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `number-of-pages` attribute value of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `number-of-pages` attribute description: the number of pages used by this address.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>:read-attribute(name=number-of-pages)`

## syntax of the operation to get the `queue-names` attribute of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `queue-names` attribute value of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `queue-names` attribute description: the names of the queues associated with the address.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>:read-attribute(name=queue-names)`

## syntax of the operation to get a `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>/role=<role name>:read-resource()`
* To get the list of all the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<role name>`.

## syntax of the operation to get the `browse` attribute of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `browse` attribute value of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `browse` attribute description: this permission allows the user to browse a queue bound to the matching address.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>/role=<role name>:read-attribute(name=browse)`

## syntax of the operation to get the `consume` attribute of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `consume` attribute value of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `consume` attribute description: this permission allows the user to consume a message from a queue bound to matching addresses.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>/role=<role name>:read-attribute(name=consume)`

## syntax of the operation to get the `create-address` attribute of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `create-address` attribute value of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `create-address` attribute description: this permission allows the user to create an address.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>/role=<role name>:read-attribute(name=create-address)`

## syntax of the operation to get the `create-durable-queue` attribute of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `create-durable-queue` attribute value of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `create-durable-queue` attribute description: this permission allows the user to create a durable queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>/role=<role name>:read-attribute(name=create-durable-queue)`

## syntax of the operation to get the `create-non-durable-queue` attribute of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `create-non-durable-queue` attribute value of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `create-non-durable-queue` attribute description: this permission allows the user to create a temporary queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>/role=<role name>:read-attribute(name=create-non-durable-queue)`

## syntax of the operation to get the `delete-address` attribute of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `delete-address` attribute value of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `delete-address` attribute description: this permission allows the user  to delete an address.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>/role=<role name>:read-attribute(name=delete-address)`

## syntax of the operation to get the `delete-durable-queue` attribute of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `delete-durable-queue` attribute value of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `delete-durable-queue` attribute description: this permission allows the user to delete a durable queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>/role=<role name>:read-attribute(name=delete-durable-queue)`

## syntax of the operation to get the `delete-non-durable-queue` attribute of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `delete-non-durable-queue` attribute value of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `delete-non-durable-queue` attribute description: this permission allows the user to delete a temporary queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>/role=<role name>:read-attribute(name=delete-non-durable-queue)`

## syntax of the operation to get the `manage` attribute of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `manage` attribute value of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `manage` attribute description: this permission allows the user to invoke management operations by sending management messages to the management address.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>/role=<role name>:read-attribute(name=manage)`

## syntax of the operation to get the `send` attribute of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `send` attribute value of the `role` resource of the `core-address` resource of the `server` resource of the `messaging-activemq` subsystem
* `send` attribute description: this permission allows the user to send a message to matching addresses.
* operation: `/subsystem=messaging-activemq/server=<server name>/core-address=<core-address name>/role=<role name>:read-attribute(name=send)`

## syntax of the operation to get a `socket-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/socket-discovery-group=<socket-discovery-group name>:read-resource()`
* To get the list of all the `socket-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<socket-discovery-group name>`.

## syntax of the operation to get the `initial-wait-timeout` attribute of the `socket-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-wait-timeout` attribute value of the `socket-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-wait-timeout` attribute description: period, in ms, to wait for an initial broadcast to give us at least one node in the cluster.
* operation: `/subsystem=messaging-activemq/server=<server name>/socket-discovery-group=<socket-discovery-group name>:read-attribute(name=initial-wait-timeout)`

## syntax of the operation to get the `refresh-timeout` attribute of the `socket-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `refresh-timeout` attribute value of the `socket-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `refresh-timeout` attribute description: period the discovery group waits after receiving the last broadcast from a particular server before removing that `server's` connector pair entry from its list.
* operation: `/subsystem=messaging-activemq/server=<server name>/socket-discovery-group=<socket-discovery-group name>:read-attribute(name=refresh-timeout)`

## syntax of the operation to get the `socket-binding` attribute of the `socket-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `socket-discovery-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the discovery group socket binding.
* operation: `/subsystem=messaging-activemq/server=<server name>/socket-discovery-group=<socket-discovery-group name>:read-attribute(name=socket-binding)`

## syntax of the operation to get a `remote-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/remote-acceptor=<remote-acceptor name>:read-resource()`
* To get the list of all the `remote-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<remote-acceptor name>`.

## syntax of the operation to get the `params` attribute of the `remote-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `params` attribute value of the `remote-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `params` attribute description: a `key-value` pair understood by the acceptor `factory-class` and used to configure it.
* operation: `/subsystem=messaging-activemq/server=<server name>/remote-acceptor=<remote-acceptor name>:read-attribute(name=params)`

## syntax of the operation to get the `socket-binding` attribute of the `remote-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `remote-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the socket binding that the acceptor will use to accept connections.
* operation: `/subsystem=messaging-activemq/server=<server name>/remote-acceptor=<remote-acceptor name>:read-attribute(name=socket-binding)`

## syntax of the operation to get the `ssl-context` attribute of the `remote-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `ssl-context` attribute value of the `remote-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `ssl-context` attribute description: the `ssl` context that the acceptor will use to accept connections.
* operation: `/subsystem=messaging-activemq/server=<server name>/remote-acceptor=<remote-acceptor name>:read-attribute(name=ssl-context)`

## syntax of the operation to get the `started` attribute of the `remote-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `started` attribute value of the `remote-acceptor` resource of the `server` resource of the `messaging-activemq` subsystem
* `started` attribute description: whether this acceptor is started.
* operation: `/subsystem=messaging-activemq/server=<server name>/remote-acceptor=<remote-acceptor name>:read-attribute(name=started)`

## syntax of the operation to get a `socket-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/socket-broadcast-group=<socket-broadcast-group name>:read-resource()`
* To get the list of all the `socket-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<socket-broadcast-group name>`.

## syntax of the operation to get the `broadcast-period` attribute of the `socket-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `broadcast-period` attribute value of the `socket-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `broadcast-period` attribute description: the period in milliseconds between consecutive broadcasts.
* operation: `/subsystem=messaging-activemq/server=<server name>/socket-broadcast-group=<socket-broadcast-group name>:read-attribute(name=broadcast-period)`

## syntax of the operation to get the `connectors` attribute of the `socket-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connectors` attribute value of the `socket-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `connectors` attribute description: specifies the names of connectors that will be broadcast.
* operation: `/subsystem=messaging-activemq/server=<server name>/socket-broadcast-group=<socket-broadcast-group name>:read-attribute(name=connectors)`

## syntax of the operation to get the `socket-binding` attribute of the `socket-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `socket-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the broadcast group socket binding.
* operation: `/subsystem=messaging-activemq/server=<server name>/socket-broadcast-group=<socket-broadcast-group name>:read-attribute(name=socket-binding)`

## syntax of the operation to get the `started` attribute of the `socket-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `started` attribute value of the `socket-broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `started` attribute description: whether the broadcast group is started.
* operation: `/subsystem=messaging-activemq/server=<server name>/socket-broadcast-group=<socket-broadcast-group name>:read-attribute(name=started)`

## syntax of the operation to get a `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-resource()`
* To get the list of all the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<bridge name>`.

## syntax of the operation to get the `call-timeout` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `call-timeout` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `call-timeout` attribute description: timeout for blocking calls.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=call-timeout)`

## syntax of the operation to get the `check-period` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `check-period` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `check-period` attribute description: the period (in milliseconds) between client failure check.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=check-period)`

## syntax of the operation to get the `confirmation-window-size` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `confirmation-window-size` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `confirmation-window-size` attribute description: the `confirmation-window-size` to use for the connection used to forward messages to the target node.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=confirmation-window-size)`

## syntax of the operation to get the `connection-ttl` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connection-ttl` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `connection-ttl` attribute description: the maximum time (in milliseconds) for which the connections used by the bridges are considered alive (in the absence of heartbeat).
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=connection-ttl)`

## syntax of the operation to get the `credential-reference` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `credential-reference` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `credential-reference` attribute description: credential (from credential store) to authenticate the bridge
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=credential-reference)`

## syntax of the operation to get the `discovery-group` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `discovery-group` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `discovery-group` attribute description: the name of the discovery group used by this bridge. must be undefined (null) if '`static-connectors`' is defined.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=discovery-group)`

## syntax of the operation to get the `filter` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `filter` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `filter` attribute description: an optional filter string. if specified then only messages which match the filter expression specified will be forwarded. the filter string follows the `activemq` filter expression syntax described in the `activemq` documentation.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=filter)`

## syntax of the operation to get the `forwarding-address` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `forwarding-address` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `forwarding-address` attribute description: the address on the target server that the message will be forwarded to. if a forwarding address is not specified then the original destination of the message will be retained.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=forwarding-address)`

## syntax of the operation to get the `ha` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `ha` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `ha` attribute description: whether or not this bridge should support high availability. true means it will connect to any available server in a cluster and support `failover`.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=ha)`

## syntax of the operation to get the `initial-connect-attempts` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-connect-attempts` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-connect-attempts` attribute description: the number of attempts to connect initially with this bridge.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=initial-connect-attempts)`

## syntax of the operation to get the `max-retry-interval` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-retry-interval` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-retry-interval` attribute description: the maximum interval of time used to retry connections
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=max-retry-interval)`

## syntax of the operation to get the `min-large-message-size` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `min-large-message-size` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `min-large-message-size` attribute description: the minimum size (in bytes) for a message before it is considered as a large message.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=min-large-message-size)`

## syntax of the operation to get the `password` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `password` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `password` attribute description: the password to use when creating the bridge connection to the remote server. if it is not specified the default cluster password specified by the `cluster-password` attribute in the root messaging subsystem resource will be used.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=password)`

## syntax of the operation to get the `producer-window-size` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `producer-window-size` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `producer-window-size` attribute description: producer flow control size on the bridge.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=producer-window-size)`

## syntax of the operation to get the `queue-name` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `queue-name` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `queue-name` attribute description: the unique name of the local queue that the bridge consumes from.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=queue-name)`

## syntax of the operation to get the `reconnect-attempts` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `reconnect-attempts` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `reconnect-attempts` attribute description: the total number of reconnect attempts the bridge will make before giving up and shutting down. a value of -`1` signifies an unlimited number of attempts.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=reconnect-attempts)`

## syntax of the operation to get the `reconnect-attempts-on-same-node` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `reconnect-attempts-on-same-node` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `reconnect-attempts-on-same-node` attribute description: the total number of reconnect attempts on the same node the bridge will make before giving up and shutting down. a value of -`1` signifies an unlimited number of attempts.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=reconnect-attempts-on-same-node)`

## syntax of the operation to get the `retry-interval` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `retry-interval` attribute description: the period in milliseconds between subsequent reconnection attempts, if the connection to the target server has failed.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=retry-interval)`

## syntax of the operation to get the `retry-interval-multiplier` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval-multiplier` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `retry-interval-multiplier` attribute description: a multiplier to apply to the time since the last retry to compute the time to the next retry. this allows you to implement an exponential backoff between retry attempts.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=retry-interval-multiplier)`

## syntax of the operation to get the `routing-type` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `routing-type` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `routing-type` attribute description: pass or strip the routing type from the messages the bridge forwards.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=routing-type)`

## syntax of the operation to get the `started` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `started` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `started` attribute description: whether the bridge is started.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=started)`

## syntax of the operation to get the `static-connectors` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `static-connectors` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `static-connectors` attribute description: a list of names of statically defined connectors used by this bridge. must be undefined (null) if '`discovery-group-name`' is defined.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=static-connectors)`

## syntax of the operation to get the `transformer-class-name` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `transformer-class-name` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `transformer-class-name` attribute description: the name of a `user-defined` class which implements the `org.apache.activemq.artemis.core.server.cluster.transformer` interface.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=transformer-class-name)`

## syntax of the operation to get the `use-duplicate-detection` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `use-duplicate-detection` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `use-duplicate-detection` attribute description: whether the bridge will automatically insert a duplicate id property into each message that it forwards.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=use-duplicate-detection)`

## syntax of the operation to get the `user` attribute of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `user` attribute value of the `bridge` resource of the `server` resource of the `messaging-activemq` subsystem
* `user` attribute description: the user name to use when creating the bridge connection to the remote server. if it is not specified the default cluster user specified by the `cluster-user` attribute in the root messaging subsystem resource will be used.
* operation: `/subsystem=messaging-activemq/server=<server name>/bridge=<bridge name>:read-attribute(name=user)`

## syntax of the operation to get the `path` attribute of the `paging-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `path` attribute value of the `paging-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* `path` attribute description: the actual `filesystem` path. treated as an absolute path, unless the '`relative-to`' attribute is specified, in which case the value is treated as relative to that path. if treated as an absolute path, the actual runtime pathname specified by the value of this attribute will be determined as follows: if this value is already absolute, then the value is directly used.  otherwise the runtime pathname is resolved in a `system-dependent` way.  on unix systems, a relative pathname is made absolute by resolving it against the current user directory. on `microsoft` windows systems, a relative pathname is made absolute by resolving it against the current directory of the drive named by the pathname, if any; if not, it is resolved against the current user directory.
* operation: `/subsystem=messaging-activemq/server=<server name>/path=paging-directory:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `paging-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `relative-to` attribute value of the `paging-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* `relative-to` attribute description: the name of another previously named path, or of one of the standard paths provided by the system. if '`relative-to`' is provided, the value of the 'path' attribute is treated as relative to the path specified by this attribute. the standard paths provided by the system include: `jboss.home` - the root directory of the `jboss` as distribution, `user.home` - `user's` home directory, `user.dir` - `user's` current working directory, `java.home` - java installation directory, `jboss.server.base.dir` - root directory for an individual server instance, `jboss.server.data.dir` - directory the server will use for persistent data file storage, `jboss.server.log.dir` - directory the server will use for log file storage, `jboss.server.tmp.dir` - directory the server will use for temporary file storage, and `jboss.domain.servers.dir` - directory under which a host controller will create the working area for individual server instances.
* operation: `/subsystem=messaging-activemq/server=<server name>/path=paging-directory:read-attribute(name=relative-to)`

## syntax of the operation to get the `path` attribute of the `bindings-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `path` attribute value of the `bindings-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* `path` attribute description: the actual `filesystem` path. treated as an absolute path, unless the '`relative-to`' attribute is specified, in which case the value is treated as relative to that path. if treated as an absolute path, the actual runtime pathname specified by the value of this attribute will be determined as follows: if this value is already absolute, then the value is directly used.  otherwise the runtime pathname is resolved in a `system-dependent` way.  on unix systems, a relative pathname is made absolute by resolving it against the current user directory. on `microsoft` windows systems, a relative pathname is made absolute by resolving it against the current directory of the drive named by the pathname, if any; if not, it is resolved against the current user directory.
* operation: `/subsystem=messaging-activemq/server=<server name>/path=bindings-directory:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `bindings-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `relative-to` attribute value of the `bindings-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* `relative-to` attribute description: the name of another previously named path, or of one of the standard paths provided by the system. if '`relative-to`' is provided, the value of the 'path' attribute is treated as relative to the path specified by this attribute. the standard paths provided by the system include: `jboss.home` - the root directory of the `jboss` as distribution, `user.home` - `user's` home directory, `user.dir` - `user's` current working directory, `java.home` - java installation directory, `jboss.server.base.dir` - root directory for an individual server instance, `jboss.server.data.dir` - directory the server will use for persistent data file storage, `jboss.server.log.dir` - directory the server will use for log file storage, `jboss.server.tmp.dir` - directory the server will use for temporary file storage, and `jboss.domain.servers.dir` - directory under which a host controller will create the working area for individual server instances.
* operation: `/subsystem=messaging-activemq/server=<server name>/path=bindings-directory:read-attribute(name=relative-to)`

## syntax of the operation to get the `path` attribute of the `large-messages-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `path` attribute value of the `large-messages-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* `path` attribute description: the actual `filesystem` path. treated as an absolute path, unless the '`relative-to`' attribute is specified, in which case the value is treated as relative to that path. if treated as an absolute path, the actual runtime pathname specified by the value of this attribute will be determined as follows: if this value is already absolute, then the value is directly used.  otherwise the runtime pathname is resolved in a `system-dependent` way.  on unix systems, a relative pathname is made absolute by resolving it against the current user directory. on `microsoft` windows systems, a relative pathname is made absolute by resolving it against the current directory of the drive named by the pathname, if any; if not, it is resolved against the current user directory.
* operation: `/subsystem=messaging-activemq/server=<server name>/path=large-messages-directory:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `large-messages-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `relative-to` attribute value of the `large-messages-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* `relative-to` attribute description: the name of another previously named path, or of one of the standard paths provided by the system. if '`relative-to`' is provided, the value of the 'path' attribute is treated as relative to the path specified by this attribute. the standard paths provided by the system include: `jboss.home` - the root directory of the `jboss` as distribution, `user.home` - `user's` home directory, `user.dir` - `user's` current working directory, `java.home` - java installation directory, `jboss.server.base.dir` - root directory for an individual server instance, `jboss.server.data.dir` - directory the server will use for persistent data file storage, `jboss.server.log.dir` - directory the server will use for log file storage, `jboss.server.tmp.dir` - directory the server will use for temporary file storage, and `jboss.domain.servers.dir` - directory under which a host controller will create the working area for individual server instances.
* operation: `/subsystem=messaging-activemq/server=<server name>/path=large-messages-directory:read-attribute(name=relative-to)`

## syntax of the operation to get the `path` attribute of the `journal-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `path` attribute value of the `journal-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* `path` attribute description: the actual `filesystem` path. treated as an absolute path, unless the '`relative-to`' attribute is specified, in which case the value is treated as relative to that path. if treated as an absolute path, the actual runtime pathname specified by the value of this attribute will be determined as follows: if this value is already absolute, then the value is directly used.  otherwise the runtime pathname is resolved in a `system-dependent` way.  on unix systems, a relative pathname is made absolute by resolving it against the current user directory. on `microsoft` windows systems, a relative pathname is made absolute by resolving it against the current directory of the drive named by the pathname, if any; if not, it is resolved against the current user directory.
* operation: `/subsystem=messaging-activemq/server=<server name>/path=journal-directory:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `journal-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `relative-to` attribute value of the `journal-directory` `path` resource of the `server` resource of the `messaging-activemq` subsystem
* `relative-to` attribute description: the name of another previously named path, or of one of the standard paths provided by the system. if '`relative-to`' is provided, the value of the 'path' attribute is treated as relative to the path specified by this attribute. the standard paths provided by the system include: `jboss.home` - the root directory of the `jboss` as distribution, `user.home` - `user's` home directory, `user.dir` - `user's` current working directory, `java.home` - java installation directory, `jboss.server.base.dir` - root directory for an individual server instance, `jboss.server.data.dir` - directory the server will use for persistent data file storage, `jboss.server.log.dir` - directory the server will use for log file storage, `jboss.server.tmp.dir` - directory the server will use for temporary file storage, and `jboss.domain.servers.dir` - directory under which a host controller will create the working area for individual server instances.
* operation: `/subsystem=messaging-activemq/server=<server name>/path=journal-directory:read-attribute(name=relative-to)`

## syntax of the operation to get a `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-resource()`
* To get the list of all the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<cluster-connection name>`.

## syntax of the operation to get the `allow-direct-connections-only` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `allow-direct-connections-only` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `allow-direct-connections-only` attribute description: whether, if a node learns of the existence of a node that is more than `1` hop away, we do not create a bridge for direct cluster connection. only relevant if '`static-connectors`' is defined.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=allow-direct-connections-only)`

## syntax of the operation to get the `call-failover-timeout` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `call-failover-timeout` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `call-failover-timeout` attribute description: the timeout to use when fail over is in process (in ms) for remote calls made by the cluster connection.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=call-failover-timeout)`

## syntax of the operation to get the `call-timeout` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `call-timeout` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `call-timeout` attribute description: the timeout (in ms) for remote calls made by the cluster connection.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=call-timeout)`

## syntax of the operation to get the `check-period` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `check-period` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `check-period` attribute description: the period (in milliseconds) between client failure check.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=check-period)`

## syntax of the operation to get the `cluster-connection-address` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `cluster-connection-address` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `cluster-connection-address` attribute description: each cluster connection only applies to messages sent to an address that starts with this value.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=cluster-connection-address)`

## syntax of the operation to get the `confirmation-window-size` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `confirmation-window-size` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `confirmation-window-size` attribute description: the `confirmation-window-size` to use for the connection used to forward messages to a target node.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=confirmation-window-size)`

## syntax of the operation to get the `connection-ttl` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connection-ttl` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `connection-ttl` attribute description: the maximum time (in milliseconds) for which the connections used by the cluster connections are considered alive (in the absence of heartbeat).
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=connection-ttl)`

## syntax of the operation to get the `connector-name` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connector-name` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `connector-name` attribute description: the name of connector to use for live connection
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=connector-name)`

## syntax of the operation to get the `discovery-group` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `discovery-group` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `discovery-group` attribute description: the discovery group used to obtain the list of other servers in the cluster to which this cluster connection will make connections. must be undefined (null) if '`static-connectors`' is defined.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=discovery-group)`

## syntax of the operation to get the `initial-connect-attempts` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-connect-attempts` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `initial-connect-attempts` attribute description: the number of attempts to connect initially with this cluster connection.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=initial-connect-attempts)`

## syntax of the operation to get the `max-hops` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-hops` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-hops` attribute description: the maximum number of times a message can be forwarded. `activemq` can be configured to also load balance messages to nodes which might be connected to it only indirectly with other `activemq` servers as intermediates in a chain.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=max-hops)`

## syntax of the operation to get the `max-retry-interval` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-retry-interval` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-retry-interval` attribute description: the maximum interval of time used to retry connections
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=max-retry-interval)`

## syntax of the operation to get the `message-load-balancing-type` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `message-load-balancing-type` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `message-load-balancing-type` attribute description: the type of message load balancing provided by the cluster connection.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=message-load-balancing-type)`

## syntax of the operation to get the `min-large-message-size` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `min-large-message-size` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `min-large-message-size` attribute description: the minimum size (in bytes) for a message before it is considered as a large message.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=min-large-message-size)`

## syntax of the operation to get the `node-id` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `node-id` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `node-id` attribute description: the node id used by this cluster connection.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=node-id)`

## syntax of the operation to get the `notification-attempts` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `notification-attempts` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `notification-attempts` attribute description: how many times the cluster connection will broadcast itself
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=notification-attempts)`

## syntax of the operation to get the `notification-interval` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `notification-interval` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `notification-interval` attribute description: how often the cluster connection will broadcast itself
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=notification-interval)`

## syntax of the operation to get the `producer-window-size` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `producer-window-size` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `producer-window-size` attribute description: producer flow control size on the cluster connection.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=producer-window-size)`

## syntax of the operation to get the `reconnect-attempts` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `reconnect-attempts` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `reconnect-attempts` attribute description: the total number of reconnect attempts the bridge will make before giving up and shutting down. a value of -`1` signifies an unlimited number of attempts.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=reconnect-attempts)`

## syntax of the operation to get the `retry-interval` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `retry-interval` attribute description: the period in milliseconds between subsequent attempts to reconnect to a target server, if the connection to the target server has failed.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=retry-interval)`

## syntax of the operation to get the `retry-interval-multiplier` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval-multiplier` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `retry-interval-multiplier` attribute description: a multiplier to apply to the time since the last retry to compute the time to the next retry. this allows you to implement an exponential backoff between retry attempts.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=retry-interval-multiplier)`

## syntax of the operation to get the `started` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `started` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `started` attribute description: whether the cluster connection is started.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=started)`

## syntax of the operation to get the `static-connectors` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `static-connectors` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `static-connectors` attribute description: the statically defined list of connectors to which this cluster connection will make connections. must be undefined (null) if '`discovery-group-name`' is defined.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=static-connectors)`

## syntax of the operation to get the `topology` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `topology` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `topology` attribute description: the topology of the nodes that this cluster connection is aware of.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=topology)`

## syntax of the operation to get the `use-duplicate-detection` attribute of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `use-duplicate-detection` attribute value of the `cluster-connection` resource of the `server` resource of the `messaging-activemq` subsystem
* `use-duplicate-detection` attribute description: whether the bridge will automatically insert a duplicate id property into each message that it forwards.
* operation: `/subsystem=messaging-activemq/server=<server name>/cluster-connection=<cluster-connection name>:read-attribute(name=use-duplicate-detection)`

## syntax of the operation to get a `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/broadcast-group=<broadcast-group name>:read-resource()`
* To get the list of all the `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<broadcast-group name>`.

## syntax of the operation to get the `broadcast-period` attribute of the `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `broadcast-period` attribute value of the `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `broadcast-period` attribute description: the period in milliseconds between consecutive broadcasts.
* operation: `/subsystem=messaging-activemq/server=<server name>/broadcast-group=<broadcast-group name>:read-attribute(name=broadcast-period)`

## syntax of the operation to get the `connectors` attribute of the `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `connectors` attribute value of the `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `connectors` attribute description: specifies the names of connectors that will be broadcast.
* operation: `/subsystem=messaging-activemq/server=<server name>/broadcast-group=<broadcast-group name>:read-attribute(name=connectors)`

## syntax of the operation to get the `jgroups-channel` attribute of the `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-channel` attribute value of the `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `jgroups-channel` attribute description: references the name of a `jgroups` channel.  if undefined, the default channel will be used.
* operation: `/subsystem=messaging-activemq/server=<server name>/broadcast-group=<broadcast-group name>:read-attribute(name=jgroups-channel)`

## syntax of the operation to get the `jgroups-cluster` attribute of the `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-cluster` attribute value of the `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `jgroups-cluster` attribute description: the logical cluster name.
* operation: `/subsystem=messaging-activemq/server=<server name>/broadcast-group=<broadcast-group name>:read-attribute(name=jgroups-cluster)`

## syntax of the operation to get the `jgroups-stack` attribute of the `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `jgroups-stack` attribute value of the `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `jgroups-stack` attribute description: references the name of a `jgroups` channel factory.
* operation: `/subsystem=messaging-activemq/server=<server name>/broadcast-group=<broadcast-group name>:read-attribute(name=jgroups-stack)`

## syntax of the operation to get the `socket-binding` attribute of the `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `broadcast-group` resource of the `server` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the broadcast group socket binding.
* operation: `/subsystem=messaging-activemq/server=<server name>/broadcast-group=<broadcast-group name>:read-attribute(name=socket-binding)`

## syntax of the operation to get a `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-resource()`
* To get the list of all the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<runtime-queue name>`.

## syntax of the operation to get the `consumer-count` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `consumer-count` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `consumer-count` attribute description: the number of consumers consuming messages from this queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=consumer-count)`

## syntax of the operation to get the `dead-letter-address` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `dead-letter-address` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `dead-letter-address` attribute description: the address to send the `queue's` dead messages to.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=dead-letter-address)`

## syntax of the operation to get the `delivering-count` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `delivering-count` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `delivering-count` attribute description: the number of messages that this queue is currently delivering to its consumers.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=delivering-count)`

## syntax of the operation to get the `durable` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `durable` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `durable` attribute description: defines whether the queue is durable.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=durable)`

## syntax of the operation to get the `expiry-address` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `expiry-address` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `expiry-address` attribute description: the address to send the `queue's` expired messages to.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=expiry-address)`

## syntax of the operation to get the `filter` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `filter` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `filter` attribute description: a queue message filter definition. an undefined or empty filter will match all messages.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=filter)`

## syntax of the operation to get the `id` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `id` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `id` attribute description: the id of the queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=id)`

## syntax of the operation to get the `message-count` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `message-count` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `message-count` attribute description: the number of messages currently in this queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=message-count)`

## syntax of the operation to get the `messages-added` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `messages-added` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `messages-added` attribute description: the number of messages added to this queue since it was created.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=messages-added)`

## syntax of the operation to get the `paused` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `paused` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `paused` attribute description: whether the queue is paused.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=paused)`

## syntax of the operation to get the `queue-address` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `queue-address` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `queue-address` attribute description: the queue address defines what address is used for routing messages.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=queue-address)`

## syntax of the operation to get the `routing-type` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `routing-type` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `routing-type` attribute description: routing type of the queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=routing-type)`

## syntax of the operation to get the `scheduled-count` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scheduled-count` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `scheduled-count` attribute description: the number of scheduled messages in this queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=scheduled-count)`

## syntax of the operation to get the `temporary` attribute of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `temporary` attribute value of the `runtime-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `temporary` attribute description: whether the queue is temporary.
* operation: `/subsystem=messaging-activemq/server=<server name>/runtime-queue=<runtime-queue name>:read-attribute(name=temporary)`

## syntax of the operation to get a `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-resource()`
* To get the list of all the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<jms-topic name>`.

## syntax of the operation to get the `delivering-count` attribute of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `delivering-count` attribute value of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* `delivering-count` attribute description: the number of messages that this queue is currently delivering to its consumers.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-attribute(name=delivering-count)`

## syntax of the operation to get the `durable-message-count` attribute of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `durable-message-count` attribute value of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* `durable-message-count` attribute description: the number of messages for all durable subscribers for this topic.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-attribute(name=durable-message-count)`

## syntax of the operation to get the `durable-subscription-count` attribute of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `durable-subscription-count` attribute value of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* `durable-subscription-count` attribute description: the number of durable subscribers for this topic.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-attribute(name=durable-subscription-count)`

## syntax of the operation to get the `entries` attribute of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `entries` attribute value of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* `entries` attribute description: the `jndi` names the topic will be bound to.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-attribute(name=entries)`

## syntax of the operation to get the `legacy-entries` attribute of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `legacy-entries` attribute value of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* `legacy-entries` attribute description: the legacy `jndi` names the topic will be bound to.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-attribute(name=legacy-entries)`

## syntax of the operation to get the `message-count` attribute of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `message-count` attribute value of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* `message-count` attribute description: the number of messages currently in this queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-attribute(name=message-count)`

## syntax of the operation to get the `messages-added` attribute of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `messages-added` attribute value of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* `messages-added` attribute description: the number of messages added to this queue since it was created.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-attribute(name=messages-added)`

## syntax of the operation to get the `non-durable-message-count` attribute of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `non-durable-message-count` attribute value of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* `non-durable-message-count` attribute description: the number of messages for all `non-durable` subscribers for this topic.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-attribute(name=non-durable-message-count)`

## syntax of the operation to get the `non-durable-subscription-count` attribute of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `non-durable-subscription-count` attribute value of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* `non-durable-subscription-count` attribute description: the number of `non-durable` subscribers for this topic.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-attribute(name=non-durable-subscription-count)`

## syntax of the operation to get the `paused` attribute of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `paused` attribute value of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* `paused` attribute description: the current status of the topic. a paused topic `won't` send messages to its consumers.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-attribute(name=paused)`

## syntax of the operation to get the `subscription-count` attribute of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `subscription-count` attribute value of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* `subscription-count` attribute description: the number of (durable and `non-durable`) subscribers for this topic.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-attribute(name=subscription-count)`

## syntax of the operation to get the `temporary` attribute of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `temporary` attribute value of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* `temporary` attribute description: whether the topic is temporary.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-attribute(name=temporary)`

## syntax of the operation to get the `topic-address` attribute of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `topic-address` attribute value of the `jms-topic` resource of the `server` resource of the `messaging-activemq` subsystem
* `topic-address` attribute description: the address the topic points to.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-topic=<jms-topic name>:read-attribute(name=topic-address)`

## syntax of the operation to get a `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-resource()`
* To get the list of all the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<address-setting name>`.

## syntax of the operation to get the `address-full-policy` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `address-full-policy` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `address-full-policy` attribute description: determines what happens when an address where `max-size-bytes` is specified becomes full. (page, drop or block)
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=address-full-policy)`

## syntax of the operation to get the `auto-create-addresses` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `auto-create-addresses` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `auto-create-addresses` attribute description: determines whether `activemq` should automatically create an addresses corresponding to the `address-settings` match when a producer or a consumer is tries to use such a queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=auto-create-addresses)`

## syntax of the operation to get the `auto-create-jms-queues` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `auto-create-jms-queues` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `auto-create-jms-queues` attribute description: determines whether `activemq` should automatically create a jakarta messaging queue corresponding to the `address-settings` match when a jakarta messaging producer or a consumer is tries to use such a queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=auto-create-jms-queues)`

## syntax of the operation to get the `auto-create-queues` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `auto-create-queues` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `auto-create-queues` attribute description: determines whether `activemq` should automatically create a queue corresponding to the `address-settings` match when a producer or a consumer is tries to use such a queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=auto-create-queues)`

## syntax of the operation to get the `auto-delete-addresses` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `auto-delete-addresses` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `auto-delete-addresses` attribute description: determines whether `activemq` should automatically delete `auto-created` addresses when they have no consumers and no messages.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=auto-delete-addresses)`

## syntax of the operation to get the `auto-delete-created-queues` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `auto-delete-created-queues` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `auto-delete-created-queues` attribute description: determines whether `activemq` should automatically delete `auto-created` queues when they have no consumers and no messages.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=auto-delete-created-queues)`

## syntax of the operation to get the `auto-delete-jms-queues` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `auto-delete-jms-queues` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `auto-delete-jms-queues` attribute description: determines whether `activemq` should automatically delete `auto-created` jakarta messaging queues when they have no consumers and no messages.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=auto-delete-jms-queues)`

## syntax of the operation to get the `auto-delete-queues` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `auto-delete-queues` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `auto-delete-queues` attribute description: determines whether `activemq` should automatically delete `auto-created` queues when they have no consumers and no messages.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=auto-delete-queues)`

## syntax of the operation to get the `dead-letter-address` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `dead-letter-address` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `dead-letter-address` attribute description: the dead letter address
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=dead-letter-address)`

## syntax of the operation to get the `expiry-address` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `expiry-address` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `expiry-address` attribute description: defines where to send a message that has expired.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=expiry-address)`

## syntax of the operation to get the `expiry-delay` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `expiry-delay` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `expiry-delay` attribute description: defines the expiration time that will be used for messages using the default expiration time
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=expiry-delay)`

## syntax of the operation to get the `last-value-queue` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `last-value-queue` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `last-value-queue` attribute description: defines whether a queue only uses last values or not
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=last-value-queue)`

## syntax of the operation to get the `max-delivery-attempts` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-delivery-attempts` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-delivery-attempts` attribute description: defines how many time a cancelled message can be redelivered before sending to the `dead-letter-address`
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=max-delivery-attempts)`

## syntax of the operation to get the `max-read-page-bytes` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-read-page-bytes` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-read-page-bytes` attribute description: how much memory the messages read from paging can take on the queue whenever more messages are needed.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=max-read-page-bytes)`

## syntax of the operation to get the `max-redelivery-delay` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-redelivery-delay` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-redelivery-delay` attribute description: maximum value for the `redelivery-delay` (in ms).
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=max-redelivery-delay)`

## syntax of the operation to get the `max-size-bytes` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `max-size-bytes` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `max-size-bytes` attribute description: the max bytes size.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=max-size-bytes)`

## syntax of the operation to get the `message-counter-history-day-limit` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `message-counter-history-day-limit` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `message-counter-history-day-limit` attribute description: day limit for the message counter history.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=message-counter-history-day-limit)`

## syntax of the operation to get the `page-max-cache-size` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `page-max-cache-size` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `page-max-cache-size` attribute description: the number of page files to keep in memory to optimize io during paging navigation.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=page-max-cache-size)`

## syntax of the operation to get the `page-size-bytes` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `page-size-bytes` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `page-size-bytes` attribute description: the paging size.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=page-size-bytes)`

## syntax of the operation to get the `redelivery-delay` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `redelivery-delay` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `redelivery-delay` attribute description: defines how long to wait before attempting redelivery of a cancelled message
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=redelivery-delay)`

## syntax of the operation to get the `redelivery-multiplier` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `redelivery-multiplier` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `redelivery-multiplier` attribute description: multiplier to apply to the `redelivery-delay` parameter
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=redelivery-multiplier)`

## syntax of the operation to get the `redistribution-delay` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `redistribution-delay` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `redistribution-delay` attribute description: defines how long to wait when the last consumer is closed on a queue before redistributing any messages
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=redistribution-delay)`

## syntax of the operation to get the `send-to-dla-on-no-route` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `send-to-dla-on-no-route` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `send-to-dla-on-no-route` attribute description: if this parameter is set to true for that address, if the message is not routed to any queues it will instead be sent to the dead letter address (`dla`) for that address, if it exists.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=send-to-dla-on-no-route)`

## syntax of the operation to get the `slow-consumer-check-period` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `slow-consumer-check-period` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `slow-consumer-check-period` attribute description: how often to check for slow consumers on a particular queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=slow-consumer-check-period)`

## syntax of the operation to get the `slow-consumer-policy` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `slow-consumer-policy` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `slow-consumer-policy` attribute description: determine what happens when a slow consumer is identified.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=slow-consumer-policy)`

## syntax of the operation to get the `slow-consumer-threshold` attribute of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `slow-consumer-threshold` attribute value of the `address-setting` resource of the `server` resource of the `messaging-activemq` subsystem
* `slow-consumer-threshold` attribute description: the minimum rate of message consumption allowed before a consumer is considered slow.
* operation: `/subsystem=messaging-activemq/server=<server name>/address-setting=<address-setting name>:read-attribute(name=slow-consumer-threshold)`

## syntax of the operation to get a `connector` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/connector=<connector name>:read-resource()`
* To get the list of all the `connector` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<connector name>`.

## syntax of the operation to get the `factory-class` attribute of the `connector` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `factory-class` attribute value of the `connector` resource of the `server` resource of the `messaging-activemq` subsystem
* `factory-class` attribute description: class name of the factory class that can instantiate the connector.
* operation: `/subsystem=messaging-activemq/server=<server name>/connector=<connector name>:read-attribute(name=factory-class)`

## syntax of the operation to get the `params` attribute of the `connector` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `params` attribute value of the `connector` resource of the `server` resource of the `messaging-activemq` subsystem
* `params` attribute description: a `key-value` pair understood by the connector `factory-class` and used to configure it.
* operation: `/subsystem=messaging-activemq/server=<server name>/connector=<connector name>:read-attribute(name=params)`

## syntax of the operation to get the `socket-binding` attribute of the `connector` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `connector` resource of the `server` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the socket binding that the connector will use to create connections
* operation: `/subsystem=messaging-activemq/server=<server name>/connector=<connector name>:read-attribute(name=socket-binding)`

## syntax of the operation to get a `remote-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/remote-connector=<remote-connector name>:read-resource()`
* To get the list of all the `remote-connector` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<remote-connector name>`.

## syntax of the operation to get the `params` attribute of the `remote-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `params` attribute value of the `remote-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* `params` attribute description: a `key-value` pair understood by the connector `factory-class` and used to configure it.
* operation: `/subsystem=messaging-activemq/server=<server name>/remote-connector=<remote-connector name>:read-attribute(name=params)`

## syntax of the operation to get the `socket-binding` attribute of the `remote-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `remote-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the socket binding that the connector will use to create connections
* operation: `/subsystem=messaging-activemq/server=<server name>/remote-connector=<remote-connector name>:read-attribute(name=socket-binding)`

## syntax of the operation to get the `ssl-context` attribute of the `remote-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `ssl-context` attribute value of the `remote-connector` resource of the `server` resource of the `messaging-activemq` subsystem
* `ssl-context` attribute description: the `ssl` context that the connector will use to connect
* operation: `/subsystem=messaging-activemq/server=<server name>/remote-connector=<remote-connector name>:read-attribute(name=ssl-context)`

## syntax of the operation to get a `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-resource()`
* To get the list of all the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem use '*' for `<jms-queue name>`.

## syntax of the operation to get the `consumer-count` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `consumer-count` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `consumer-count` attribute description: the number of consumers consuming messages from this queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=consumer-count)`

## syntax of the operation to get the `dead-letter-address` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `dead-letter-address` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `dead-letter-address` attribute description: the address to send dead messages to.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=dead-letter-address)`

## syntax of the operation to get the `delivering-count` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `delivering-count` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `delivering-count` attribute description: the number of messages that this queue is currently delivering to its consumers.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=delivering-count)`

## syntax of the operation to get the `durable` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `durable` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `durable` attribute description: whether the queue is durable or not.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=durable)`

## syntax of the operation to get the `entries` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `entries` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `entries` attribute description: the `jndi` names the queue will be bound to.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=entries)`

## syntax of the operation to get the `expiry-address` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `expiry-address` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `expiry-address` attribute description: the address to send expired messages to.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=expiry-address)`

## syntax of the operation to get the `legacy-entries` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `legacy-entries` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `legacy-entries` attribute description: the `jndi` names the queue will be bound to.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=legacy-entries)`

## syntax of the operation to get the `message-count` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `message-count` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `message-count` attribute description: the number of messages currently in this queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=message-count)`

## syntax of the operation to get the `messages-added` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `messages-added` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `messages-added` attribute description: the number of messages added to this queue since it was created.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=messages-added)`

## syntax of the operation to get the `paused` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `paused` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `paused` attribute description: whether the queue is paused.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=paused)`

## syntax of the operation to get the `queue-address` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `queue-address` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `queue-address` attribute description: the queue address defines what address is used for routing messages.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=queue-address)`

## syntax of the operation to get the `scheduled-count` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `scheduled-count` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `scheduled-count` attribute description: the number of scheduled messages in this queue.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=scheduled-count)`

## syntax of the operation to get the `selector` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `selector` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `selector` attribute description: the queue selector.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=selector)`

## syntax of the operation to get the `temporary` attribute of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* Retrieve the `temporary` attribute value of the `jms-queue` resource of the `server` resource of the `messaging-activemq` subsystem
* `temporary` attribute description: whether the queue is temporary.
* operation: `/subsystem=messaging-activemq/server=<server name>/jms-queue=<jms-queue name>:read-attribute(name=temporary)`

## syntax of the operation to get a `external-jms-queue` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/external-jms-queue=<external-jms-queue name>:read-resource()`
* To get the list of all the `external-jms-queue` resource of the `messaging-activemq` subsystem use '*' for `<external-jms-queue name>`.

## syntax of the operation to get the `enable-amq1-prefix` attribute of the `external-jms-queue` resource of the `messaging-activemq` subsystem
* Retrieve the `enable-amq1-prefix` attribute value of the `external-jms-queue` resource of the `messaging-activemq` subsystem
* `enable-amq1-prefix` attribute description: enable the use of `activemq` `1.x` prefixes in the addresses.
* operation: `/subsystem=messaging-activemq/external-jms-queue=<external-jms-queue name>:read-attribute(name=enable-amq1-prefix)`

## syntax of the operation to get the `entries` attribute of the `external-jms-queue` resource of the `messaging-activemq` subsystem
* Retrieve the `entries` attribute value of the `external-jms-queue` resource of the `messaging-activemq` subsystem
* `entries` attribute description: the `jndi` names the queue will be bound to.
* operation: `/subsystem=messaging-activemq/external-jms-queue=<external-jms-queue name>:read-attribute(name=entries)`

## syntax of the operation to get a `connector` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/connector=<connector name>:read-resource()`
* To get the list of all the `connector` resource of the `messaging-activemq` subsystem use '*' for `<connector name>`.

## syntax of the operation to get the `factory-class` attribute of the `connector` resource of the `messaging-activemq` subsystem
* Retrieve the `factory-class` attribute value of the `connector` resource of the `messaging-activemq` subsystem
* `factory-class` attribute description: class name of the factory class that can instantiate the connector.
* operation: `/subsystem=messaging-activemq/connector=<connector name>:read-attribute(name=factory-class)`

## syntax of the operation to get the `params` attribute of the `connector` resource of the `messaging-activemq` subsystem
* Retrieve the `params` attribute value of the `connector` resource of the `messaging-activemq` subsystem
* `params` attribute description: a `key-value` pair understood by the connector `factory-class` and used to configure it.
* operation: `/subsystem=messaging-activemq/connector=<connector name>:read-attribute(name=params)`

## syntax of the operation to get the `socket-binding` attribute of the `connector` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `connector` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the socket binding that the connector will use to create connections
* operation: `/subsystem=messaging-activemq/connector=<connector name>:read-attribute(name=socket-binding)`

## syntax of the operation to get a `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-resource()`
* To get the list of all the `pooled-connection-factory` resource of the `messaging-activemq` subsystem use '*' for `<pooled-connection-factory name>`.

## syntax of the operation to get the `allow-local-transactions` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `allow-local-transactions` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `allow-local-transactions` attribute description: allow local transactions for outbond jakarta messaging sessions (it does not apply to `jmscontext` that explicitly disallows it).
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=allow-local-transactions)`

## syntax of the operation to get the `auto-group` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `auto-group` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `auto-group` attribute description: the `autogroup`.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=auto-group)`

## syntax of the operation to get the `block-on-acknowledge` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-acknowledge` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `block-on-acknowledge` attribute description: true to set block on acknowledge.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=block-on-acknowledge)`

## syntax of the operation to get the `block-on-durable-send` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-durable-send` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `block-on-durable-send` attribute description: true to set block on durable send.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=block-on-durable-send)`

## syntax of the operation to get the `block-on-non-durable-send` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `block-on-non-durable-send` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `block-on-non-durable-send` attribute description: true to set block on non durable send.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=block-on-non-durable-send)`

## syntax of the operation to get the `cache-large-message-client` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `cache-large-message-client` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `cache-large-message-client` attribute description: true to cache large messages.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=cache-large-message-client)`

## syntax of the operation to get the `call-failover-timeout` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `call-failover-timeout` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `call-failover-timeout` attribute description: the timeout to use when fail over is in process (in ms).
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=call-failover-timeout)`

## syntax of the operation to get the `call-timeout` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `call-timeout` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `call-timeout` attribute description: the call time out.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=call-timeout)`

## syntax of the operation to get the `client-failure-check-period` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `client-failure-check-period` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `client-failure-check-period` attribute description: the client failure check period.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=client-failure-check-period)`

## syntax of the operation to get the `client-id` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `client-id` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `client-id` attribute description: the client id.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=client-id)`

## syntax of the operation to get the `compress-large-messages` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `compress-large-messages` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `compress-large-messages` attribute description: whether large messages should be compressed.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=compress-large-messages)`

## syntax of the operation to get the `confirmation-window-size` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `confirmation-window-size` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `confirmation-window-size` attribute description: the confirmation window size.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=confirmation-window-size)`

## syntax of the operation to get the `connection-load-balancing-policy-class-name` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `connection-load-balancing-policy-class-name` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `connection-load-balancing-policy-class-name` attribute description: name of a class implementing a `client-side` load balancing policy that a client can use to load balance sessions across different nodes in a cluster.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=connection-load-balancing-policy-class-name)`

## syntax of the operation to get the `connection-ttl` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `connection-ttl` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `connection-ttl` attribute description: the connection `ttl`.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=connection-ttl)`

## syntax of the operation to get the `connectors` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `connectors` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `connectors` attribute description: defines the connectors. these are stored in a map by connector name (with an undefined value). it is possible to pass a list of connector names when writing this attribute.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=connectors)`

## syntax of the operation to get the `consumer-max-rate` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `consumer-max-rate` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `consumer-max-rate` attribute description: the consumer max rate.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=consumer-max-rate)`

## syntax of the operation to get the `consumer-window-size` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `consumer-window-size` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `consumer-window-size` attribute description: the consumer window size.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=consumer-window-size)`

## syntax of the operation to get the `credential-reference` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `credential-reference` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `credential-reference` attribute description: credential (from credential store) to authenticate the pooled connection factory
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=credential-reference)`

## syntax of the operation to get the `deserialization-allow-list` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-allow-list` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `deserialization-allow-list` attribute description: a list of class names (separated by `whitespaces`) that are allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=deserialization-allow-list)`

## syntax of the operation to get the `deserialization-black-list` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-black-list` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `deserialization-black-list` attribute description: a list of class names (separated by `whitespaces`) that are not allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=deserialization-black-list)`

## syntax of the operation to get the `deserialization-block-list` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-block-list` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `deserialization-block-list` attribute description: a list of class names (separated by `whitespaces`) that are not allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=deserialization-block-list)`

## syntax of the operation to get the `deserialization-white-list` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `deserialization-white-list` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `deserialization-white-list` attribute description: a list of class names (separated by `whitespaces`) that are allowed to be used in serialization of jakarta messaging `objectmessage`.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=deserialization-white-list)`

## syntax of the operation to get the `discovery-group` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `discovery-group` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `discovery-group` attribute description: the discovery group name.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=discovery-group)`

## syntax of the operation to get the `dups-ok-batch-size` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `dups-ok-batch-size` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `dups-ok-batch-size` attribute description: the dups ok batch size.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=dups-ok-batch-size)`

## syntax of the operation to get the `enable-amq1-prefix` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `enable-amq1-prefix` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `enable-amq1-prefix` attribute description: enable the use of `activemq` `1.5.x` prefixes in the addresses.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=enable-amq1-prefix)`

## syntax of the operation to get the `enlistment-trace` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `enlistment-trace` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `enlistment-trace` attribute description: enables `ironjacamar` to record enlistment traces for this `pooled-connection-factory`. this attribute is undefined by default and the behaviour is driven by the presence of the `ironjacamar.disable_enlistment_trace` system property.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=enlistment-trace)`

## syntax of the operation to get the `entries` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `entries` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `entries` attribute description: the `jndi` names the connection factory should be bound to.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=entries)`

## syntax of the operation to get the `failover-on-initial-connection` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `failover-on-initial-connection` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `failover-on-initial-connection` attribute description: true to fail over on initial connection.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=failover-on-initial-connection)`

## syntax of the operation to get the `group-id` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `group-id` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `group-id` attribute description: the group id.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=group-id)`

## syntax of the operation to get the `ha` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `ha` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `ha` attribute description: whether the connection factory supports high availability.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=ha)`

## syntax of the operation to get the `initial-connect-attempts` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-connect-attempts` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `initial-connect-attempts` attribute description: the number of attempts to connect initially with this factory.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=initial-connect-attempts)`

## syntax of the operation to get the `initial-message-packet-size` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-message-packet-size` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `initial-message-packet-size` attribute description: the initial size of messages created through this factory.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=initial-message-packet-size)`

## syntax of the operation to get the `jndi-params` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `jndi-params` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `jndi-params` attribute description: the `jndi` `params` to use for locating the destination for incoming connections.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=jndi-params)`

## syntax of the operation to get the `managed-connection-pool` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `managed-connection-pool` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `managed-connection-pool` attribute description: the class name of the managed connection pool used by this `pooled-connection-factory`.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=managed-connection-pool)`

## syntax of the operation to get the `max-pool-size` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `max-pool-size` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `max-pool-size` attribute description: the maximum size for the pool
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=max-pool-size)`

## syntax of the operation to get the `max-retry-interval` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `max-retry-interval` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `max-retry-interval` attribute description: the max retry interval.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=max-retry-interval)`

## syntax of the operation to get the `min-large-message-size` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `min-large-message-size` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `min-large-message-size` attribute description: the min large message size.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=min-large-message-size)`

## syntax of the operation to get the `min-pool-size` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `min-pool-size` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `min-pool-size` attribute description: the minimum size for the pool
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=min-pool-size)`

## syntax of the operation to get the `password` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `password` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `password` attribute description: the default password to use with this connection factory. this is only needed when pointing the connection factory to a remote host.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=password)`

## syntax of the operation to get the `pre-acknowledge` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `pre-acknowledge` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `pre-acknowledge` attribute description: true to `pre-acknowledge`.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=pre-acknowledge)`

## syntax of the operation to get the `producer-max-rate` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `producer-max-rate` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `producer-max-rate` attribute description: the producer max rate.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=producer-max-rate)`

## syntax of the operation to get the `producer-window-size` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `producer-window-size` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `producer-window-size` attribute description: the producer window size.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=producer-window-size)`

## syntax of the operation to get the `protocol-manager-factory` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `protocol-manager-factory` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `protocol-manager-factory` attribute description: the protocol manager factory used by this pooled connection factory.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=protocol-manager-factory)`

## syntax of the operation to get the `rebalance-connections` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `rebalance-connections` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `rebalance-connections` attribute description: rebalance inbound connections when cluster topology changes.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=rebalance-connections)`

## syntax of the operation to get the `reconnect-attempts` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `reconnect-attempts` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `reconnect-attempts` attribute description: the reconnect attempts. by default, a pooled connection factory will try to reconnect infinitely to the messaging `server(s`).
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=reconnect-attempts)`

## syntax of the operation to get the `retry-interval` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `retry-interval` attribute description: the retry interval.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=retry-interval)`

## syntax of the operation to get the `retry-interval-multiplier` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `retry-interval-multiplier` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `retry-interval-multiplier` attribute description: the retry interval multiplier.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=retry-interval-multiplier)`

## syntax of the operation to get the `scheduled-thread-pool-max-size` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `scheduled-thread-pool-max-size` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `scheduled-thread-pool-max-size` attribute description: the scheduled thread pool max size.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=scheduled-thread-pool-max-size)`

## syntax of the operation to get the `setup-attempts` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `setup-attempts` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `setup-attempts` attribute description: the number of times to set up an `mdb` endpoint
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=setup-attempts)`

## syntax of the operation to get the `setup-interval` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `setup-interval` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `setup-interval` attribute description: the interval between attempts at setting up an `mdb` endpoint.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=setup-interval)`

## syntax of the operation to get the `statistics-enabled` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `statistics-enabled` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `statistics-enabled` attribute description: define whether runtime statistics are enabled.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `thread-pool-max-size` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `thread-pool-max-size` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `thread-pool-max-size` attribute description: the thread pool max size.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=thread-pool-max-size)`

## syntax of the operation to get the `transaction` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `transaction` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `transaction` attribute description: the type of transaction supported by this pooled connection factory (can be local, none or `xa`, default is `xa`).
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=transaction)`

## syntax of the operation to get the `transaction-batch-size` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `transaction-batch-size` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `transaction-batch-size` attribute description: the transaction batch size.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=transaction-batch-size)`

## syntax of the operation to get the `use-auto-recovery` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `use-auto-recovery` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `use-auto-recovery` attribute description: true to use auto recovery.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=use-auto-recovery)`

## syntax of the operation to get the `use-global-pools` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `use-global-pools` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `use-global-pools` attribute description: true to use global pools.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=use-global-pools)`

## syntax of the operation to get the `use-jndi` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `use-jndi` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `use-jndi` attribute description: use `jndi` to locate the destination for incoming connections
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=use-jndi)`

## syntax of the operation to get the `use-local-tx` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `use-local-tx` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `use-local-tx` attribute description: use a local transaction for incoming sessions
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=use-local-tx)`

## syntax of the operation to get the `use-topology-for-load-balancing` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `use-topology-for-load-balancing` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `use-topology-for-load-balancing` attribute description: whether to use topology information from the cluster to connect to it. if set to 'false', only the initial connector will be used.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=use-topology-for-load-balancing)`

## syntax of the operation to get the `user` attribute of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* Retrieve the `user` attribute value of the `pooled-connection-factory` resource of the `messaging-activemq` subsystem
* `user` attribute description: the default `username` to use with this connection factory. this is only needed when pointing the connection factory to a remote host.
* operation: `/subsystem=messaging-activemq/pooled-connection-factory=<pooled-connection-factory name>:read-attribute(name=user)`

## syntax of the operation to get a `remote-connector` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/remote-connector=<remote-connector name>:read-resource()`
* To get the list of all the `remote-connector` resource of the `messaging-activemq` subsystem use '*' for `<remote-connector name>`.

## syntax of the operation to get the `params` attribute of the `remote-connector` resource of the `messaging-activemq` subsystem
* Retrieve the `params` attribute value of the `remote-connector` resource of the `messaging-activemq` subsystem
* `params` attribute description: a `key-value` pair understood by the connector `factory-class` and used to configure it.
* operation: `/subsystem=messaging-activemq/remote-connector=<remote-connector name>:read-attribute(name=params)`

## syntax of the operation to get the `socket-binding` attribute of the `remote-connector` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `remote-connector` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the socket binding that the connector will use to create connections
* operation: `/subsystem=messaging-activemq/remote-connector=<remote-connector name>:read-attribute(name=socket-binding)`

## syntax of the operation to get the `ssl-context` attribute of the `remote-connector` resource of the `messaging-activemq` subsystem
* Retrieve the `ssl-context` attribute value of the `remote-connector` resource of the `messaging-activemq` subsystem
* `ssl-context` attribute description: the `ssl` context that the connector will use to connect
* operation: `/subsystem=messaging-activemq/remote-connector=<remote-connector name>:read-attribute(name=ssl-context)`

## syntax of the operation to get a `socket-discovery-group` resource of the `messaging-activemq` subsystem
* operation: `/subsystem=messaging-activemq/socket-discovery-group=<socket-discovery-group name>:read-resource()`
* To get the list of all the `socket-discovery-group` resource of the `messaging-activemq` subsystem use '*' for `<socket-discovery-group name>`.

## syntax of the operation to get the `initial-wait-timeout` attribute of the `socket-discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `initial-wait-timeout` attribute value of the `socket-discovery-group` resource of the `messaging-activemq` subsystem
* `initial-wait-timeout` attribute description: period, in ms, to wait for an initial broadcast to give us at least one node in the cluster.
* operation: `/subsystem=messaging-activemq/socket-discovery-group=<socket-discovery-group name>:read-attribute(name=initial-wait-timeout)`

## syntax of the operation to get the `refresh-timeout` attribute of the `socket-discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `refresh-timeout` attribute value of the `socket-discovery-group` resource of the `messaging-activemq` subsystem
* `refresh-timeout` attribute description: period the discovery group waits after receiving the last broadcast from a particular server before removing that `server's` connector pair entry from its list.
* operation: `/subsystem=messaging-activemq/socket-discovery-group=<socket-discovery-group name>:read-attribute(name=refresh-timeout)`

## syntax of the operation to get the `socket-binding` attribute of the `socket-discovery-group` resource of the `messaging-activemq` subsystem
* Retrieve the `socket-binding` attribute value of the `socket-discovery-group` resource of the `messaging-activemq` subsystem
* `socket-binding` attribute description: the discovery group socket binding.
* operation: `/subsystem=messaging-activemq/socket-discovery-group=<socket-discovery-group name>:read-attribute(name=socket-binding)`

## syntax of the operation to get the `average-commit-time` attribute of the `transactions` subsystem
* Retrieve the `average-commit-time` attribute value of the `transactions` subsystem
* `average-commit-time` attribute description: the average time of transaction commit, measured from the moment the client calls commit until the transaction manager determines that the commit attempt was successful.
* operation: `/subsystem=transactions:read-attribute(name=average-commit-time)`

## syntax of the operation to get the `default-timeout` attribute of the `transactions` subsystem
* Retrieve the `default-timeout` attribute value of the `transactions` subsystem
* `default-timeout` attribute description: the default timeout, in seconds, for a new transaction started by the transaction manager (see also the '`maximum-timeout`' attribute).
* operation: `/subsystem=transactions:read-attribute(name=default-timeout)`

## syntax of the operation to get the `enable-statistics` attribute of the `transactions` subsystem
* Retrieve the `enable-statistics` attribute value of the `transactions` subsystem
* `enable-statistics` attribute description: whether transaction statistics should be gathered.
* operation: `/subsystem=transactions:read-attribute(name=enable-statistics)`

## syntax of the operation to get the `enable-tsm-status` attribute of the `transactions` subsystem
* Retrieve the `enable-tsm-status` attribute value of the `transactions` subsystem
* `enable-tsm-status` attribute description: set to 'true' to enable the transaction status manager (`tsm`) service (used for out of process recovery). when 'true' the application server binds to the socket defined by the '`status-socket-binding`' attribute.
* operation: `/subsystem=transactions:read-attribute(name=enable-tsm-status)`

## syntax of the operation to get the `hornetq-store-enable-async-io` attribute of the `transactions` subsystem
* Retrieve the `hornetq-store-enable-async-io` attribute value of the `transactions` subsystem
* `hornetq-store-enable-async-io` attribute description: whether `asyncio` should be enabled for the journal store.
* operation: `/subsystem=transactions:read-attribute(name=hornetq-store-enable-async-io)`

## syntax of the operation to get the `jdbc-action-store-drop-table` attribute of the `transactions` subsystem
* Retrieve the `jdbc-action-store-drop-table` attribute value of the `transactions` subsystem
* `jdbc-action-store-drop-table` attribute description: if set to true then the `jdbc-action-store` table will be dropped during application server startup.
* operation: `/subsystem=transactions:read-attribute(name=jdbc-action-store-drop-table)`

## syntax of the operation to get the `jdbc-action-store-table-prefix` attribute of the `transactions` subsystem
* Retrieve the `jdbc-action-store-table-prefix` attribute value of the `transactions` subsystem
* `jdbc-action-store-table-prefix` attribute description: optional prefix for the name of the database table used for writing transaction log records of the action store type.
* operation: `/subsystem=transactions:read-attribute(name=jdbc-action-store-table-prefix)`

## syntax of the operation to get the `jdbc-communication-store-drop-table` attribute of the `transactions` subsystem
* Retrieve the `jdbc-communication-store-drop-table` attribute value of the `transactions` subsystem
* `jdbc-communication-store-drop-table` attribute description: if set to true then the `jdbc-communication-store` table will be dropped during application server startup.
* operation: `/subsystem=transactions:read-attribute(name=jdbc-communication-store-drop-table)`

## syntax of the operation to get the `jdbc-communication-store-table-prefix` attribute of the `transactions` subsystem
* Retrieve the `jdbc-communication-store-table-prefix` attribute value of the `transactions` subsystem
* `jdbc-communication-store-table-prefix` attribute description: optional prefix for the name of the database table used for writing transaction log records of the communication store type.
* operation: `/subsystem=transactions:read-attribute(name=jdbc-communication-store-table-prefix)`

## syntax of the operation to get the `jdbc-state-store-drop-table` attribute of the `transactions` subsystem
* Retrieve the `jdbc-state-store-drop-table` attribute value of the `transactions` subsystem
* `jdbc-state-store-drop-table` attribute description: if set to true then the `jdbc-state-store` table will be dropped during application server startup.
* operation: `/subsystem=transactions:read-attribute(name=jdbc-state-store-drop-table)`

## syntax of the operation to get the `jdbc-state-store-table-prefix` attribute of the `transactions` subsystem
* Retrieve the `jdbc-state-store-table-prefix` attribute value of the `transactions` subsystem
* `jdbc-state-store-table-prefix` attribute description: optional prefix for the name of the database table used for writing transaction log records of the state store type.
* operation: `/subsystem=transactions:read-attribute(name=jdbc-state-store-table-prefix)`

## syntax of the operation to get the `jdbc-store-datasource` attribute of the `transactions` subsystem
* Retrieve the `jdbc-store-datasource` attribute value of the `transactions` subsystem
* `jdbc-store-datasource` attribute description: the `jndi` name of a `non-xa` `datasource` (`i.e`. one whose '`jta`' attribute is set to false) to be used for the `jdbc` store. the `datasource` must be defined in the `datasources` subsystem.
* operation: `/subsystem=transactions:read-attribute(name=jdbc-store-datasource)`

## syntax of the operation to get the `journal-store-enable-async-io` attribute of the `transactions` subsystem
* Retrieve the `journal-store-enable-async-io` attribute value of the `transactions` subsystem
* `journal-store-enable-async-io` attribute description: whether `asyncio` should be enabled for the journal store. when true, the transaction manager will use the native `aio` (`posix` asynchronous `i/o`) libraries provided by the platform, if available. applicable only when `use-journal-store` is set true.
* operation: `/subsystem=transactions:read-attribute(name=journal-store-enable-async-io)`

## syntax of the operation to get the `jts` attribute of the `transactions` subsystem
* Retrieve the `jts` attribute value of the `transactions` subsystem
* `jts` attribute description: if true, this enables the java transaction service (`jts`). if `jts` is enabled then the transactions attribute of the '`iiop-openjdk`' subsystem must be set to 'full'. this setting changes the internal mechanisms used in the transaction manager. it has no impact on the user working with `jta` `api`.
* operation: `/subsystem=transactions:read-attribute(name=jts)`

## syntax of the operation to get the `maximum-timeout` attribute of the `transactions` subsystem
* Retrieve the `maximum-timeout` attribute value of the `transactions` subsystem
* `maximum-timeout` attribute description: if the '`default-timeout`' attribute is zero then this value is used for setting the maximum timeout value (in seconds) for newly started transactions.
* operation: `/subsystem=transactions:read-attribute(name=maximum-timeout)`

## syntax of the operation to get the `node-identifier` attribute of the `transactions` subsystem
* Retrieve the `node-identifier` attribute value of the `transactions` subsystem
* `node-identifier` attribute description: used to set the node identifier. each `xid` that the transaction manager creates will have this identifier encoded within it and ensures the transaction manager will only recover branches which match the specified identifier. it is imperative that this identifier is unique between application server instances which share either an object store or access common resource managers.
* operation: `/subsystem=transactions:read-attribute(name=node-identifier)`

## syntax of the operation to get the `number-of-aborted-transactions` attribute of the `transactions` subsystem
* Retrieve the `number-of-aborted-transactions` attribute value of the `transactions` subsystem
* `number-of-aborted-transactions` attribute description: the number of aborted (`i.e`. `rolled-back`) transactions.
* operation: `/subsystem=transactions:read-attribute(name=number-of-aborted-transactions)`

## syntax of the operation to get the `number-of-application-rollbacks` attribute of the `transactions` subsystem
* Retrieve the `number-of-application-rollbacks` attribute value of the `transactions` subsystem
* `number-of-application-rollbacks` attribute description: the number of transactions that have been rolled back by application request. this includes those that timeout, since the timeout behavior is considered an attribute of the application configuration.
* operation: `/subsystem=transactions:read-attribute(name=number-of-application-rollbacks)`

## syntax of the operation to get the `number-of-committed-transactions` attribute of the `transactions` subsystem
* Retrieve the `number-of-committed-transactions` attribute value of the `transactions` subsystem
* `number-of-committed-transactions` attribute description: the number of committed transactions.
* operation: `/subsystem=transactions:read-attribute(name=number-of-committed-transactions)`

## syntax of the operation to get the `number-of-heuristics` attribute of the `transactions` subsystem
* Retrieve the `number-of-heuristics` attribute value of the `transactions` subsystem
* `number-of-heuristics` attribute description: the number of transactions which have terminated with heuristic outcomes.
* operation: `/subsystem=transactions:read-attribute(name=number-of-heuristics)`

## syntax of the operation to get the `number-of-inflight-transactions` attribute of the `transactions` subsystem
* Retrieve the `number-of-inflight-transactions` attribute value of the `transactions` subsystem
* `number-of-inflight-transactions` attribute description: the number of transactions that have begun but not yet terminated.
* operation: `/subsystem=transactions:read-attribute(name=number-of-inflight-transactions)`

## syntax of the operation to get the `number-of-nested-transactions` attribute of the `transactions` subsystem
* Retrieve the `number-of-nested-transactions` attribute value of the `transactions` subsystem
* `number-of-nested-transactions` attribute description: the total number of nested (sub) transactions created.
* operation: `/subsystem=transactions:read-attribute(name=number-of-nested-transactions)`

## syntax of the operation to get the `number-of-resource-rollbacks` attribute of the `transactions` subsystem
* Retrieve the `number-of-resource-rollbacks` attribute value of the `transactions` subsystem
* `number-of-resource-rollbacks` attribute description: the number of transactions that rolled back due to resource (participant) failure.
* operation: `/subsystem=transactions:read-attribute(name=number-of-resource-rollbacks)`

## syntax of the operation to get the `number-of-system-rollbacks` attribute of the `transactions` subsystem
* Retrieve the `number-of-system-rollbacks` attribute value of the `transactions` subsystem
* `number-of-system-rollbacks` attribute description: the number of transactions that have been rolled back due to internal system errors.
* operation: `/subsystem=transactions:read-attribute(name=number-of-system-rollbacks)`

## syntax of the operation to get the `number-of-timed-out-transactions` attribute of the `transactions` subsystem
* Retrieve the `number-of-timed-out-transactions` attribute value of the `transactions` subsystem
* `number-of-timed-out-transactions` attribute description: the number of transactions that have rolled back due to timeout.
* operation: `/subsystem=transactions:read-attribute(name=number-of-timed-out-transactions)`

## syntax of the operation to get the `number-of-transactions` attribute of the `transactions` subsystem
* Retrieve the `number-of-transactions` attribute value of the `transactions` subsystem
* `number-of-transactions` attribute description: the total number of transactions (`top-level` and nested) created.
* operation: `/subsystem=transactions:read-attribute(name=number-of-transactions)`

## syntax of the operation to get the `object-store-path` attribute of the `transactions` subsystem
* Retrieve the `object-store-path` attribute value of the `transactions` subsystem
* `object-store-path` attribute description: denotes a path where the transaction manager object store should store data. by default the value is treated as relative to the path denoted by the '`relative-to`' attribute. when the '`relative-to`' attribute is undefined the value is considered an absolute path. this setting is valid when default or journal store is used. `it's` not used when the `jdbc` journal store is used.
* operation: `/subsystem=transactions:read-attribute(name=object-store-path)`

## syntax of the operation to get the `object-store-relative-to` attribute of the `transactions` subsystem
* Retrieve the `object-store-relative-to` attribute value of the `transactions` subsystem
* `object-store-relative-to` attribute description: references a global path configuration in the domain model, defaulting to the application server data directory (`jboss.server.data.dir`). the value of the '`object-store-path`' attribute will be treated as relative to this path. undefine this attribute to disable the default behavior and force the value of the '`object-store-path`' attribute to be treated as an absolute path.
* operation: `/subsystem=transactions:read-attribute(name=object-store-relative-to)`

## syntax of the operation to get the `process-id-socket-binding` attribute of the `transactions` subsystem
* Retrieve the `process-id-socket-binding` attribute value of the `transactions` subsystem
* `process-id-socket-binding` attribute description: the name of the socket binding to use if the transaction manager should use a `socket-based` generation of transaction id. will be 'undefined' if '`process-id-uuid`' is 'true'; otherwise must be set.
* operation: `/subsystem=transactions:read-attribute(name=process-id-socket-binding)`

## syntax of the operation to get the `process-id-socket-max-ports` attribute of the `transactions` subsystem
* Retrieve the `process-id-socket-max-ports` attribute value of the `transactions` subsystem
* `process-id-socket-max-ports` attribute description: the maximum number of ports to search for an open port if the transaction manager should use a `socket-based` generation of transaction id. if the port specified by the socket binding referenced in '`process-id-socket-binding`' is occupied, the next higher port will be tried until an open port is found or the number of ports specified by this attribute have been tried. will be 'undefined' if '`process-id-uuid`' is 'true'.
* operation: `/subsystem=transactions:read-attribute(name=process-id-socket-max-ports)`

## syntax of the operation to get the `process-id-uuid` attribute of the `transactions` subsystem
* Retrieve the `process-id-uuid` attribute value of the `transactions` subsystem
* `process-id-uuid` attribute description: indicates a strategy used for obtaining a unique identifier needed for creation of transaction instances. when true then the transaction manager generates the transaction id based on process id (`pid`). when set to false then the transaction manager generates the transaction id based on the referenced socket binding, `i.e`. the attribute '`process-id-socket-binding`' is required.
* operation: `/subsystem=transactions:read-attribute(name=process-id-uuid)`

## syntax of the operation to get the `recovery-listener` attribute of the `transactions` subsystem
* Retrieve the `recovery-listener` attribute value of the `transactions` subsystem
* `recovery-listener` attribute description: used to specify if the recovery system should listen on a network socket or not. when true the application server binds to a socket at the port defined by the '`socket-binding`' attribute. when the recovery listener is activated the user can remotely control the recovery manager.
* operation: `/subsystem=transactions:read-attribute(name=recovery-listener)`

## syntax of the operation to get the `socket-binding` attribute of the `transactions` subsystem
* Retrieve the `socket-binding` attribute value of the `transactions` subsystem
* `socket-binding` attribute description: references an existing socket binding that the transaction manager will listen on for recovery requests.
* operation: `/subsystem=transactions:read-attribute(name=socket-binding)`

## syntax of the operation to get the `stale-transaction-time` attribute of the `transactions` subsystem
* Retrieve the `stale-transaction-time` attribute value of the `transactions` subsystem
* `stale-transaction-time` attribute description: the time for which transaction that contains remote enlistments is held in memory after it is being completed.
* operation: `/subsystem=transactions:read-attribute(name=stale-transaction-time)`

## syntax of the operation to get the `statistics-enabled` attribute of the `transactions` subsystem
* Retrieve the `statistics-enabled` attribute value of the `transactions` subsystem
* `statistics-enabled` attribute description: whether transaction statistics should be gathered. the statistics are available by reading subsystem attributes prefixed with 'number-' and within the '`average-commit-time`' attribute.
* operation: `/subsystem=transactions:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `status-socket-binding` attribute of the `transactions` subsystem
* Retrieve the `status-socket-binding` attribute value of the `transactions` subsystem
* `status-socket-binding` attribute description: references an existing socket binding that the transaction manager will listen on for transaction status requests.
* operation: `/subsystem=transactions:read-attribute(name=status-socket-binding)`

## syntax of the operation to get the `use-hornetq-store` attribute of the `transactions` subsystem
* Retrieve the `use-hornetq-store` attribute value of the `transactions` subsystem
* `use-hornetq-store` attribute description: use the journal store for writing transaction logs. set to true to enable and to false to use the default log store type. the default log store is normally one file system file per transaction `log.it's` alternative to `jdbc` based store.
* operation: `/subsystem=transactions:read-attribute(name=use-hornetq-store)`

## syntax of the operation to get the `use-jdbc-store` attribute of the `transactions` subsystem
* Retrieve the `use-jdbc-store` attribute value of the `transactions` subsystem
* `use-jdbc-store` attribute description: use the `jdbc` store for writing transaction logs. data is saved in the database indicated by the '`jdbc-store-datasource`' attribute. set to true to enable. note that the server will not boot if both '`use-journal-store`' and '`use-jdbc-store`' attributes are set to true. if both are set to false then the default `filesystem` based store will be used.
* operation: `/subsystem=transactions:read-attribute(name=use-jdbc-store)`

## syntax of the operation to get the `use-journal-store` attribute of the `transactions` subsystem
* Retrieve the `use-journal-store` attribute value of the `transactions` subsystem
* `use-journal-store` attribute description: use the journal store for writing transaction logs. the journal store consists of one file for all the transactions. set to true to enable. note that the server will not boot if both '`use-journal-store`' and '`use-jdbc-store`' attributes are set to true. if both are set to false then the default `filesystem` based store will be used.
* operation: `/subsystem=transactions:read-attribute(name=use-journal-store)`

## syntax of the operation to get the `expose-all-logs` attribute of the `log-store` `log-store` resource of the `transactions` subsystem
* Retrieve the `expose-all-logs` attribute value of the `log-store` `log-store` resource of the `transactions` subsystem
* `expose-all-logs` attribute description: configures the behaviour of the probe operation. when true then all transaction log records are exposed. by default only a subset of the transaction log is exposed.
* operation: `/subsystem=transactions/log-store=log-store:read-attribute(name=expose-all-logs)`

## syntax of the operation to get the `type` attribute of the `log-store` `log-store` resource of the `transactions` subsystem
* Retrieve the `type` attribute value of the `log-store` `log-store` resource of the `transactions` subsystem
* `type` attribute description: `read-only` attribute that specifies the implementation type of the logging store. the possible values are default, journal and `jdbc`.
* operation: `/subsystem=transactions/log-store=log-store:read-attribute(name=type)`

## syntax of the operation to get a `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* operation: `/subsystem=transactions/log-store=log-store/transactions=<transactions name>:read-resource()`
* To get the list of all the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem use '*' for `<transactions name>`.

## syntax of the operation to get the `age-in-seconds` attribute of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* Retrieve the `age-in-seconds` attribute value of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* `age-in-seconds` attribute description: the time since this transaction was prepared or when the recovery system last tried to recover it.
* operation: `/subsystem=transactions/log-store=log-store/transactions=<transactions name>:read-attribute(name=age-in-seconds)`

## syntax of the operation to get the `id` attribute of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* Retrieve the `id` attribute value of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* `id` attribute description: the id of this transaction.
* operation: `/subsystem=transactions/log-store=log-store/transactions=<transactions name>:read-attribute(name=id)`

## syntax of the operation to get the `jmx-name` attribute of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* Retrieve the `jmx-name` attribute value of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* `jmx-name` attribute description: the `jmx` name of this transaction.
* operation: `/subsystem=transactions/log-store=log-store/transactions=<transactions name>:read-attribute(name=jmx-name)`

## syntax of the operation to get the `type` attribute of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* Retrieve the `type` attribute value of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* `type` attribute description: the type name under which this record is stored.
* operation: `/subsystem=transactions/log-store=log-store/transactions=<transactions name>:read-attribute(name=type)`

## syntax of the operation to get a `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* operation: `/subsystem=transactions/log-store=log-store/transactions=<transactions name>/participants=<participants name>:read-resource()`
* To get the list of all the `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem use '*' for `<participants name>`.

## syntax of the operation to get the `eis-product-name` attribute of the `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* Retrieve the `eis-product-name` attribute value of the `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* `eis-product-name` attribute description: the jakarta connectors enterprise information `system's` product name.
* operation: `/subsystem=transactions/log-store=log-store/transactions=<transactions name>/participants=<participants name>:read-attribute(name=eis-product-name)`

## syntax of the operation to get the `eis-product-version` attribute of the `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* Retrieve the `eis-product-version` attribute value of the `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* `eis-product-version` attribute description: the jakarta connectors enterprise information `system's` product version.
* operation: `/subsystem=transactions/log-store=log-store/transactions=<transactions name>/participants=<participants name>:read-attribute(name=eis-product-version)`

## syntax of the operation to get the `jmx-name` attribute of the `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* Retrieve the `jmx-name` attribute value of the `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* `jmx-name` attribute description: the `jmx` name of this participant.
* operation: `/subsystem=transactions/log-store=log-store/transactions=<transactions name>/participants=<participants name>:read-attribute(name=jmx-name)`

## syntax of the operation to get the `jndi-name` attribute of the `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* Retrieve the `jndi-name` attribute value of the `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* `jndi-name` attribute description: `jndi` name of this participant.
* operation: `/subsystem=transactions/log-store=log-store/transactions=<transactions name>/participants=<participants name>:read-attribute(name=jndi-name)`

## syntax of the operation to get the `status` attribute of the `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* Retrieve the `status` attribute value of the `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* `status` attribute description: reports the commitment status of this participant (can be one of pending, prepared, failed, heuristic or `readonly`).
* operation: `/subsystem=transactions/log-store=log-store/transactions=<transactions name>/participants=<participants name>:read-attribute(name=status)`

## syntax of the operation to get the `type` attribute of the `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* Retrieve the `type` attribute value of the `participants` resource of the `transactions` resource of the `log-store` `log-store` resource of the `transactions` subsystem
* `type` attribute description: the type name under which this record is stored.
* operation: `/subsystem=transactions/log-store=log-store/transactions=<transactions name>/participants=<participants name>:read-attribute(name=type)`

## syntax of the operation to get a `commit-markable-resource` resource of the `transactions` subsystem
* operation: `/subsystem=transactions/commit-markable-resource=<commit-markable-resource name>:read-resource()`
* To get the list of all the `commit-markable-resource` resource of the `transactions` subsystem use '*' for `<commit-markable-resource name>`.

## syntax of the operation to get the `batch-size` attribute of the `commit-markable-resource` resource of the `transactions` subsystem
* Retrieve the `batch-size` attribute value of the `commit-markable-resource` resource of the `transactions` subsystem
* `batch-size` attribute description: configures number of ids placed within 'in' clause of the `sql` query 'delete from ... where `xid` in (...)' when periodic recovery processes the `cmr` database table garbage collection. when some garbage is left in the `cmr` database table after deletion another `sql` delete is run during the next recovery cycle. when '`immediate-cleanup`' is used this configuration has no big impact as the periodic recovery finds each time an empty `cmr` database table.
* operation: `/subsystem=transactions/commit-markable-resource=<commit-markable-resource name>:read-attribute(name=batch-size)`

## syntax of the operation to get the `immediate-cleanup` attribute of the `commit-markable-resource` resource of the `transactions` subsystem
* Retrieve the `immediate-cleanup` attribute value of the `commit-markable-resource` resource of the `transactions` subsystem
* `immediate-cleanup` attribute description: immediate cleanup of ids associated with this `cmr` resource after the end of the transaction. when set to false the garbage collection of the ids is processed by periodic recovery.
* operation: `/subsystem=transactions/commit-markable-resource=<commit-markable-resource name>:read-attribute(name=immediate-cleanup)`

## syntax of the operation to get the `jndi-name` attribute of the `commit-markable-resource` resource of the `transactions` subsystem
* Retrieve the `jndi-name` attribute value of the `commit-markable-resource` resource of the `transactions` subsystem
* `jndi-name` attribute description: a `jndi` name of the `non-xa` `datasource` which is made to be a `cmr` resource for the purpose of transaction manager participant handling. the `datasource` is required to be marked as '`connectable=true`'.
* operation: `/subsystem=transactions/commit-markable-resource=<commit-markable-resource name>:read-attribute(name=jndi-name)`

## syntax of the operation to get the `name` attribute of the `commit-markable-resource` resource of the `transactions` subsystem
* Retrieve the `name` attribute value of the `commit-markable-resource` resource of the `transactions` subsystem
* `name` attribute description: defines the database table name used for storing ids associated with this `cmr` resource. the default value for naming the database table is '`xids`'.
* operation: `/subsystem=transactions/commit-markable-resource=<commit-markable-resource name>:read-attribute(name=name)`

## syntax of the operation to get a `proxy` resource of the `modcluster` subsystem
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-resource()`
* To get the list of all the `proxy` resource of the `modcluster` subsystem use '*' for `<proxy name>`.

## syntax of the operation to get the `advertise` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `advertise` attribute value of the `proxy` resource of the `modcluster` subsystem
* `advertise` attribute description: whether to enable `multicast-based` advertise mechanism.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=advertise)`

## syntax of the operation to get the `advertise-security-key` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `advertise-security-key` attribute value of the `proxy` resource of the `modcluster` subsystem
* `advertise-security-key` attribute description: if specified, reverse proxy advertisements checksums will be verified using this value as a salt.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=advertise-security-key)`

## syntax of the operation to get the `advertise-socket` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `advertise-socket` attribute value of the `proxy` resource of the `modcluster` subsystem
* `advertise-socket` attribute description: name of socket binding to use for the advertise socket.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=advertise-socket)`

## syntax of the operation to get the `auto-enable-contexts` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `auto-enable-contexts` attribute value of the `proxy` resource of the `modcluster` subsystem
* `auto-enable-contexts` attribute description: if false, the contexts are registered with the reverse proxy as disabled, they need to be enabled manually by '`enable-context`' operation or via `mod_cluster_manager` console (if available).
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=auto-enable-contexts)`

## syntax of the operation to get the `balancer` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `balancer` attribute value of the `proxy` resource of the `modcluster` subsystem
* `balancer` attribute description: the name of the balancer on the reverse proxy to register with.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=balancer)`

## syntax of the operation to get the `excluded-contexts` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `excluded-contexts` attribute value of the `proxy` resource of the `modcluster` subsystem
* `excluded-contexts` attribute description: list of contexts to exclude from registration with the reverse proxies.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=excluded-contexts)`

## syntax of the operation to get the `flush-packets` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `flush-packets` attribute value of the `proxy` resource of the `modcluster` subsystem
* `flush-packets` attribute description: whether to enable packet flushing on the reverse proxy.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=flush-packets)`

## syntax of the operation to get the `flush-wait` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `flush-wait` attribute value of the `proxy` resource of the `modcluster` subsystem
* `flush-wait` attribute description: time to wait before flushing packets on the reverse proxy.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=flush-wait)`

## syntax of the operation to get the `listener` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `listener` attribute value of the `proxy` resource of the `modcluster` subsystem
* `listener` attribute description: the name of undertow listener that will be registered with the reverse proxy.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=listener)`

## syntax of the operation to get the `load-balancing-group` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `load-balancing-group` attribute value of the `proxy` resource of the `modcluster` subsystem
* `load-balancing-group` attribute description: name of the load balancing group this node belongs to.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=load-balancing-group)`

## syntax of the operation to get the `max-attempts` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `max-attempts` attribute value of the `proxy` resource of the `modcluster` subsystem
* `max-attempts` attribute description: maximum number of `failover` attempts by reverse proxy when sending the request to the `backend` server.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=max-attempts)`

## syntax of the operation to get the `node-timeout` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `node-timeout` attribute value of the `proxy` resource of the `modcluster` subsystem
* `node-timeout` attribute description: timeout (in seconds) for proxy connections to a node. that is the time `mod_cluster` will wait for the `back-end` response before returning an error.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=node-timeout)`

## syntax of the operation to get the `ping` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `ping` attribute value of the `proxy` resource of the `modcluster` subsystem
* `ping` attribute description: number of seconds for which to wait for a pong answer to a ping.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=ping)`

## syntax of the operation to get the `proxies` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `proxies` attribute value of the `proxy` resource of the `modcluster` subsystem
* `proxies` attribute description: list of reverse proxies for `mod_cluster` to register with defined by '`outbound-socket-binding`' in '`socket-binding-group`'.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=proxies)`

## syntax of the operation to get the `proxy-url` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `proxy-url` attribute value of the `proxy` resource of the `modcluster` subsystem
* `proxy-url` attribute description: base `url` for `mcmp` requests.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=proxy-url)`

## syntax of the operation to get the `session-draining-strategy` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `session-draining-strategy` attribute value of the `proxy` resource of the `modcluster` subsystem
* `session-draining-strategy` attribute description: session draining strategy used during `undeployment` of a web application.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=session-draining-strategy)`

## syntax of the operation to get the `smax` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `smax` attribute value of the `proxy` resource of the `modcluster` subsystem
* `smax` attribute description: soft maximum idle connection count for reverse proxy.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=smax)`

## syntax of the operation to get the `socket-timeout` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `socket-timeout` attribute value of the `proxy` resource of the `modcluster` subsystem
* `socket-timeout` attribute description: timeout to wait for the reverse proxy to answer a `mcmp` message.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=socket-timeout)`

## syntax of the operation to get the `ssl-context` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `ssl-context` attribute value of the `proxy` resource of the `modcluster` subsystem
* `ssl-context` attribute description: reference to the `sslcontext` to be used by `mod_cluster`.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=ssl-context)`

## syntax of the operation to get the `status-interval` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `status-interval` attribute value of the `proxy` resource of the `modcluster` subsystem
* `status-interval` attribute description: number of seconds a status message is sent from the application server to the proxy.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=status-interval)`

## syntax of the operation to get the `sticky-session` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `sticky-session` attribute value of the `proxy` resource of the `modcluster` subsystem
* `sticky-session` attribute description: indicates whether subsequent requests for a given session should be routed to the same node, if possible.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=sticky-session)`

## syntax of the operation to get the `sticky-session-force` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `sticky-session-force` attribute value of the `proxy` resource of the `modcluster` subsystem
* `sticky-session-force` attribute description: indicates whether the reverse proxy should run an error in the event that the balancer is unable to route a request to the node to which it is stuck. ignored if sticky sessions are disabled.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=sticky-session-force)`

## syntax of the operation to get the `sticky-session-remove` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `sticky-session-remove` attribute value of the `proxy` resource of the `modcluster` subsystem
* `sticky-session-remove` attribute description: indicates whether the reverse proxy should remove session stickiness in the event that the balancer is unable to route a request to the node to which it is stuck. ignored if sticky sessions are disabled.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=sticky-session-remove)`

## syntax of the operation to get the `stop-context-timeout` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `stop-context-timeout` attribute value of the `proxy` resource of the `modcluster` subsystem
* `stop-context-timeout` attribute description: maximum time to wait for context to process pending requests.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=stop-context-timeout)`

## syntax of the operation to get the `ttl` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `ttl` attribute value of the `proxy` resource of the `modcluster` subsystem
* `ttl` attribute description: time to live (in seconds) for idle connections above `smax`.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=ttl)`

## syntax of the operation to get the `worker-timeout` attribute of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `worker-timeout` attribute value of the `proxy` resource of the `modcluster` subsystem
* `worker-timeout` attribute description: number of seconds to wait for a worker to become available to handle a request.
* operation: `/subsystem=modcluster/proxy=<proxy name>:read-attribute(name=worker-timeout)`

## syntax of the operation to get the `decay` attribute of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `decay` attribute value of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* `decay` attribute description: the factor by which a historic load values should degrade in significance.
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic:read-attribute(name=decay)`

## syntax of the operation to get the `history` attribute of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `history` attribute value of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* `history` attribute description: the number of historic (previous) load values to consider in the load balance factor computation.
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic:read-attribute(name=history)`

## syntax of the operation to get the `initial-load` attribute of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `initial-load` attribute value of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* `initial-load` attribute description: initial load within the range [`0..100`] with which to `prepopulate` historical values. used to gradually drive load to the node. value of `0` `prepopulates` with full load and value of -`1` disables this behavior.
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic:read-attribute(name=initial-load)`

## syntax of the operation to get a `load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic/load-metric=<load-metric name>:read-resource()`
* To get the list of all the `load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem use '*' for `<load-metric name>`.

## syntax of the operation to get the `capacity` attribute of the `load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `capacity` attribute value of the `load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* `capacity` attribute description: maximum capacity of the metric used to normalize the load values from a metric which require explicit capacity.
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic/load-metric=<load-metric name>:read-attribute(name=capacity)`

## syntax of the operation to get the `property` attribute of the `load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `property` attribute value of the `load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* `property` attribute description: properties to apply on a loaded metric instance.
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic/load-metric=<load-metric name>:read-attribute(name=property)`

## syntax of the operation to get the `type` attribute of the `load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `type` attribute value of the `load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* `type` attribute description: type of a `built-in` load metric from the enumerated values.
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic/load-metric=<load-metric name>:read-attribute(name=type)`

## syntax of the operation to get the `weight` attribute of the `load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `weight` attribute value of the `load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* `weight` attribute description: number indicating the significance of a metric with respect to the other metrics. for example, a metric of weight `2` will have twice the impact on the overall load factor than a metric of weight `1`.
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic/load-metric=<load-metric name>:read-attribute(name=weight)`

## syntax of the operation to get a `custom-load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic/custom-load-metric=<custom-load-metric name>:read-resource()`
* To get the list of all the `custom-load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem use '*' for `<custom-load-metric name>`.

## syntax of the operation to get the `capacity` attribute of the `custom-load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `capacity` attribute value of the `custom-load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* `capacity` attribute description: maximum capacity of the metric used to normalize the load values from a metric which require explicit capacity.
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic/custom-load-metric=<custom-load-metric name>:read-attribute(name=capacity)`

## syntax of the operation to get the `class` attribute of the `custom-load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `class` attribute value of the `custom-load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* `class` attribute description: class name to use to construct a load metric from.
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic/custom-load-metric=<custom-load-metric name>:read-attribute(name=class)`

## syntax of the operation to get the `module` attribute of the `custom-load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `module` attribute value of the `custom-load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* `module` attribute description: module name from which to load the load metric class.
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic/custom-load-metric=<custom-load-metric name>:read-attribute(name=module)`

## syntax of the operation to get the `property` attribute of the `custom-load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `property` attribute value of the `custom-load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* `property` attribute description: properties to apply on a loaded metric instance.
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic/custom-load-metric=<custom-load-metric name>:read-attribute(name=property)`

## syntax of the operation to get the `weight` attribute of the `custom-load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `weight` attribute value of the `custom-load-metric` resource of the `dynamic` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* `weight` attribute description: number indicating the significance of a metric with respect to the other metrics. for example, a metric of weight `2` will have twice the impact on the overall load factor than a metric of weight `1`.
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=dynamic/custom-load-metric=<custom-load-metric name>:read-attribute(name=weight)`

## syntax of the operation to get the `factor` attribute of the `simple` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* Retrieve the `factor` attribute value of the `simple` `load-provider` resource of the `proxy` resource of the `modcluster` subsystem
* `factor` attribute description: dynamic load provider computes the load balance factor of a node from a defined set of load metrics.
* operation: `/subsystem=modcluster/proxy=<proxy name>/load-provider=simple:read-attribute(name=factor)`

## syntax of the operation to get a `static-provider` resource of the `discovery` subsystem
* operation: `/subsystem=discovery/static-provider=<static-provider name>:read-resource()`
* To get the list of all the `static-provider` resource of the `discovery` subsystem use '*' for `<static-provider name>`.

## syntax of the operation to get the `services` attribute of the `static-provider` resource of the `discovery` subsystem
* Retrieve the `services` attribute value of the `static-provider` resource of the `discovery` subsystem
* `services` attribute description: the list of services that are configured with this provider
* operation: `/subsystem=discovery/static-provider=<static-provider name>:read-attribute(name=services)`

## syntax of the operation to get a `aggregate-provider` resource of the `discovery` subsystem
* operation: `/subsystem=discovery/aggregate-provider=<aggregate-provider name>:read-resource()`
* To get the list of all the `aggregate-provider` resource of the `discovery` subsystem use '*' for `<aggregate-provider name>`.

## syntax of the operation to get the `providers` attribute of the `aggregate-provider` resource of the `discovery` subsystem
* Retrieve the `providers` attribute value of the `aggregate-provider` resource of the `discovery` subsystem
* `providers` attribute description: the list of providers that are aggregated by this provider
* operation: `/subsystem=discovery/aggregate-provider=<aggregate-provider name>:read-attribute(name=providers)`

## syntax of the operation to get a `cache-container` resource of the `infinispan` subsystem
* operation: `/subsystem=infinispan/cache-container=<cache-container name>:read-resource()`
* To get the list of all the `cache-container` resource of the `infinispan` subsystem use '*' for `<cache-container name>`.

## syntax of the operation to get the `aliases` attribute of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `aliases` attribute value of the `cache-container` resource of the `infinispan` subsystem
* `aliases` attribute description: the list of aliases for this cache container
* operation: `/subsystem=infinispan/cache-container=<cache-container name>:read-attribute(name=aliases)`

## syntax of the operation to get the `cache-manager-status` attribute of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cache-manager-status` attribute value of the `cache-container` resource of the `infinispan` subsystem
* `cache-manager-status` attribute description: the status of the cache manager component. may return null if the cache manager is not started.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>:read-attribute(name=cache-manager-status)`

## syntax of the operation to get the `cluster-name` attribute of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cluster-name` attribute value of the `cache-container` resource of the `infinispan` subsystem
* `cluster-name` attribute description: the name of the cluster this node belongs to. may return null if the cache manager is not started.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>:read-attribute(name=cluster-name)`

## syntax of the operation to get the `coordinator-address` attribute of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `coordinator-address` attribute value of the `cache-container` resource of the `infinispan` subsystem
* `coordinator-address` attribute description: the logical address of the `cluster's` coordinator. may return null if the cache manager is not started.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>:read-attribute(name=coordinator-address)`

## syntax of the operation to get the `default-cache` attribute of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `default-cache` attribute value of the `cache-container` resource of the `infinispan` subsystem
* `default-cache` attribute description: the default `infinispan` cache
* operation: `/subsystem=infinispan/cache-container=<cache-container name>:read-attribute(name=default-cache)`

## syntax of the operation to get the `is-coordinator` attribute of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `is-coordinator` attribute value of the `cache-container` resource of the `infinispan` subsystem
* `is-coordinator` attribute description: set to true if this node is the `cluster's` coordinator. may return null if the cache manager is not started.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>:read-attribute(name=is-coordinator)`

## syntax of the operation to get the `local-address` attribute of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `local-address` attribute value of the `cache-container` resource of the `infinispan` subsystem
* `local-address` attribute description: the local address of the node. may return null if the cache manager is not started.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>:read-attribute(name=local-address)`

## syntax of the operation to get the `marshaller` attribute of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `marshaller` attribute value of the `cache-container` resource of the `infinispan` subsystem
* `marshaller` attribute description: defines the marshalling implementation used to marshal cache entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>:read-attribute(name=marshaller)`

## syntax of the operation to get the `modules` attribute of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modules` attribute value of the `cache-container` resource of the `infinispan` subsystem
* `modules` attribute description: the set of modules associated with this cache `container's` configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>:read-attribute(name=modules)`

## syntax of the operation to get the `statistics-enabled` attribute of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `statistics-enabled` attribute value of the `cache-container` resource of the `infinispan` subsystem
* `statistics-enabled` attribute description: if enabled, statistics will be collected for this cache container
* operation: `/subsystem=infinispan/cache-container=<cache-container name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get a `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>:read-resource()`
* To get the list of all the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem use '*' for `<local-cache name>`.

## syntax of the operation to get the `modules` attribute of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modules` attribute value of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modules` attribute description: the set of modules associated with this `cache's` configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>:read-attribute(name=modules)`

## syntax of the operation to get the `statistics-enabled` attribute of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `statistics-enabled` attribute value of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `statistics-enabled` attribute description: if enabled, statistics will be collected for this cache
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `acquire-timeout` attribute of the `locking` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `acquire-timeout` attribute value of the `locking` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `acquire-timeout` attribute description: maximum time to attempt a particular lock acquisition.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/component=locking:read-attribute(name=acquire-timeout)`

## syntax of the operation to get the `concurrency-level` attribute of the `locking` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `concurrency-level` attribute value of the `locking` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `concurrency-level` attribute description: concurrency level for lock containers. adjust this value according to the number of concurrent threads interacting with `infinispan`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/component=locking:read-attribute(name=concurrency-level)`

## syntax of the operation to get the `isolation` attribute of the `locking` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `isolation` attribute value of the `locking` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `isolation` attribute description: sets the cache locking isolation level.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/component=locking:read-attribute(name=isolation)`

## syntax of the operation to get the `striping` attribute of the `locking` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `striping` attribute value of the `locking` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `striping` attribute description: if true, a pool of shared locks is maintained for all entries that need to be locked. otherwise, a lock is created per entry in the cache. lock striping helps control memory footprint but may reduce concurrency in the system.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/component=locking:read-attribute(name=striping)`

## syntax of the operation to get the `interval` attribute of the `expiration` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `interval` attribute value of the `expiration` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `interval` attribute description: interval (in milliseconds) between subsequent runs to purge expired entries from memory and any cache stores. if you wish to disable the periodic eviction process altogether, set `wakeupinterval` to -`1`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/component=expiration:read-attribute(name=interval)`

## syntax of the operation to get the `lifespan` attribute of the `expiration` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `lifespan` attribute value of the `expiration` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `lifespan` attribute description: maximum lifespan of a cache entry, after which the entry is expired `cluster-wide`, in milliseconds. -`1` means the entries never expire.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/component=expiration:read-attribute(name=lifespan)`

## syntax of the operation to get the `max-idle` attribute of the `expiration` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-idle` attribute value of the `expiration` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-idle` attribute description: maximum idle time a cache entry will be maintained in the cache, in milliseconds. if the idle time is exceeded, the entry will be expired `cluster-wide`. -`1` means the entries never expire.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/component=expiration:read-attribute(name=max-idle)`

## syntax of the operation to get the `complete-timeout` attribute of the `transaction` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `complete-timeout` attribute value of the `transaction` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `complete-timeout` attribute description: the duration (in ms) after which idle transactions are removed.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/component=transaction:read-attribute(name=complete-timeout)`

## syntax of the operation to get the `locking` attribute of the `transaction` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `locking` attribute value of the `transaction` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `locking` attribute description: the locking mode for this cache, one of optimistic or pessimistic.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/component=transaction:read-attribute(name=locking)`

## syntax of the operation to get the `mode` attribute of the `transaction` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `mode` attribute value of the `transaction` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `mode` attribute description: sets the cache transaction mode to one of none, `non_xa`, `non_durable_xa`, `full_xa`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/component=transaction:read-attribute(name=mode)`

## syntax of the operation to get the `stop-timeout` attribute of the `transaction` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `stop-timeout` attribute value of the `transaction` `component` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `stop-timeout` attribute description: if there are any ongoing transactions when a cache is stopped, `infinispan` waits for ongoing remote and local transactions to finish. the amount of time to wait for is defined by the cache stop timeout.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/component=transaction:read-attribute(name=stop-timeout)`

## syntax of the operation to get the `cache` attribute of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cache` attribute value of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `cache` attribute description: the name of the remote cache to use for this remote store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=remote:read-attribute(name=cache)`

## syntax of the operation to get the `fetch-state` attribute of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=remote:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=remote:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=remote:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=remote:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=remote:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=remote:read-attribute(name=purge)`

## syntax of the operation to get the `remote-servers` attribute of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-servers` attribute value of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-servers` attribute description: a list of remote servers for this cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=remote:read-attribute(name=remote-servers)`

## syntax of the operation to get the `segmented` attribute of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=remote:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=remote:read-attribute(name=shared)`

## syntax of the operation to get the `socket-timeout` attribute of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `socket-timeout` attribute value of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `socket-timeout` attribute description: a socket timeout for remote cache communication.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=remote:read-attribute(name=socket-timeout)`

## syntax of the operation to get the `tcp-no-delay` attribute of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `tcp-no-delay` attribute value of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `tcp-no-delay` attribute description: a `tcp_nodelay` value for remote cache communication.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=remote:read-attribute(name=tcp-no-delay)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `remote` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=remote/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `class` attribute of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `class` attribute value of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `class` attribute description: the custom store implementation class to use for this cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=custom:read-attribute(name=class)`

## syntax of the operation to get the `fetch-state` attribute of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=custom:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=custom:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=custom:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=custom:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=custom:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=custom:read-attribute(name=purge)`

## syntax of the operation to get the `segmented` attribute of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=custom:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=custom:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `custom` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=custom/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `cache-configuration` attribute of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cache-configuration` attribute value of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `cache-configuration` attribute description: name of the cache configuration template defined in `infinispan` server to create caches from.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=hotrod:read-attribute(name=cache-configuration)`

## syntax of the operation to get the `fetch-state` attribute of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=hotrod:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=hotrod:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=hotrod:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=hotrod:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=hotrod:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=hotrod:read-attribute(name=purge)`

## syntax of the operation to get the `remote-cache-container` attribute of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-cache-container` attribute value of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-cache-container` attribute description: reference to a `container-managed` `remote-cache-container`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=hotrod:read-attribute(name=remote-cache-container)`

## syntax of the operation to get the `segmented` attribute of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=hotrod:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=hotrod:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `hotrod` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=hotrod/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `data-source` attribute of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `data-source` attribute value of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `data-source` attribute description: references the data source used to connect to this store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc:read-attribute(name=data-source)`

## syntax of the operation to get the `dialect` attribute of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `dialect` attribute value of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `dialect` attribute description: the dialect of this `datastore`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc:read-attribute(name=dialect)`

## syntax of the operation to get the `fetch-state` attribute of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc:read-attribute(name=purge)`

## syntax of the operation to get the `segmented` attribute of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `create-on-start` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `create-on-start` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `create-on-start` attribute description: indicates whether the store should create this database table when the cache starts.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc/table=string:read-attribute(name=create-on-start)`

## syntax of the operation to get the `data-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `data-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `data-column` attribute description: a database column to hold cache entry data.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc/table=string:read-attribute(name=data-column)`

## syntax of the operation to get the `drop-on-stop` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `drop-on-stop` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `drop-on-stop` attribute description: indicates whether the store should drop this database table when the cache stops.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc/table=string:read-attribute(name=drop-on-stop)`

## syntax of the operation to get the `fetch-size` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-size` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-size` attribute description: for db queries, the fetch size will be used to set the fetch size on `resultsets`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc/table=string:read-attribute(name=fetch-size)`

## syntax of the operation to get the `id-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `id-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `id-column` attribute description: a database column to hold cache entry ids.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc/table=string:read-attribute(name=id-column)`

## syntax of the operation to get the `prefix` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `prefix` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `prefix` attribute description: the prefix for the database table name.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc/table=string:read-attribute(name=prefix)`

## syntax of the operation to get the `segment-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segment-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segment-column` attribute description: a database column to hold cache entry segment.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc/table=string:read-attribute(name=segment-column)`

## syntax of the operation to get the `timestamp-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `timestamp-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `timestamp-column` attribute description: a database column to hold cache entry timestamps.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=jdbc/table=string:read-attribute(name=timestamp-column)`

## syntax of the operation to get the `fetch-state` attribute of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=file:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=file:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=file:read-attribute(name=passivation)`

## syntax of the operation to get the `path` attribute of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `path` attribute value of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `path` attribute description: the system path under which this cache store will persist its entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=file:read-attribute(name=path)`

## syntax of the operation to get the `preload` attribute of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=file:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=file:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=file:read-attribute(name=purge)`

## syntax of the operation to get the `relative-to` attribute of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `relative-to` attribute value of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `relative-to` attribute description: the system path to which the specified path is relative.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=file:read-attribute(name=relative-to)`

## syntax of the operation to get the `segmented` attribute of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=file:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=file:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `file` `store` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/store=file/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `size` attribute of the `off-heap` `memory` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size` attribute value of the `off-heap` `memory` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size` attribute description: eviction threshold, as defined by the size unit.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/memory=off-heap:read-attribute(name=size)`

## syntax of the operation to get the `size-unit` attribute of the `off-heap` `memory` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size-unit` attribute value of the `off-heap` `memory` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size-unit` attribute description: the unit of the eviction threshold.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/memory=off-heap:read-attribute(name=size-unit)`

## syntax of the operation to get the `size` attribute of the `heap` `memory` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size` attribute value of the `heap` `memory` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size` attribute description: eviction threshold, as defined by the size unit.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/memory=heap:read-attribute(name=size)`

## syntax of the operation to get the `size-unit` attribute of the `heap` `memory` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size-unit` attribute value of the `heap` `memory` resource of the `local-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size-unit` attribute description: the unit of the eviction threshold.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/local-cache=<local-cache name>/memory=heap:read-attribute(name=size-unit)`

## syntax of the operation to get the `channel` attribute of the `jgroups` `transport` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `channel` attribute value of the `jgroups` `transport` resource of the `cache-container` resource of the `infinispan` subsystem
* `channel` attribute description: the channel of this cache `container's` transport.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/transport=jgroups:read-attribute(name=channel)`

## syntax of the operation to get the `lock-timeout` attribute of the `jgroups` `transport` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `lock-timeout` attribute value of the `jgroups` `transport` resource of the `cache-container` resource of the `infinispan` subsystem
* `lock-timeout` attribute description: the timeout for locks for the transport
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/transport=jgroups:read-attribute(name=lock-timeout)`

## syntax of the operation to get the `keepalive-time` attribute of the `blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `keepalive-time` attribute value of the `blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `keepalive-time` attribute description: used to specify the amount of milliseconds that pool threads should be kept running when idle; if not specified, threads will run until the executor is shut down.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=blocking:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `max-threads` attribute of the `blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-threads` attribute value of the `blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-threads` attribute description: the maximum thread pool size.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=blocking:read-attribute(name=max-threads)`

## syntax of the operation to get the `min-threads` attribute of the `blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `min-threads` attribute value of the `blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `min-threads` attribute description: the core thread pool size which is smaller than the maximum pool size. if undefined, the core thread pool size is the same as the maximum thread pool size.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=blocking:read-attribute(name=min-threads)`

## syntax of the operation to get the `queue-length` attribute of the `blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `queue-length` attribute value of the `blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `queue-length` attribute description: the queue length.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=blocking:read-attribute(name=queue-length)`

## syntax of the operation to get the `keepalive-time` attribute of the `expiration` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `keepalive-time` attribute value of the `expiration` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `keepalive-time` attribute description: used to specify the amount of milliseconds that pool threads should be kept running when idle; if not specified, threads will run until the executor is shut down.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=expiration:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `min-threads` attribute of the `expiration` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `min-threads` attribute value of the `expiration` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `min-threads` attribute description: the core thread pool size which is smaller than the maximum pool size. if undefined, the core thread pool size is the same as the maximum thread pool size.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=expiration:read-attribute(name=min-threads)`

## syntax of the operation to get the `keepalive-time` attribute of the `non-blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `keepalive-time` attribute value of the `non-blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `keepalive-time` attribute description: used to specify the amount of milliseconds that pool threads should be kept running when idle; if not specified, threads will run until the executor is shut down.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=non-blocking:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `max-threads` attribute of the `non-blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-threads` attribute value of the `non-blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-threads` attribute description: the maximum thread pool size.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=non-blocking:read-attribute(name=max-threads)`

## syntax of the operation to get the `min-threads` attribute of the `non-blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `min-threads` attribute value of the `non-blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `min-threads` attribute description: the core thread pool size which is smaller than the maximum pool size. if undefined, the core thread pool size is the same as the maximum thread pool size.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=non-blocking:read-attribute(name=min-threads)`

## syntax of the operation to get the `queue-length` attribute of the `non-blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `queue-length` attribute value of the `non-blocking` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `queue-length` attribute description: the queue length.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=non-blocking:read-attribute(name=queue-length)`

## syntax of the operation to get the `keepalive-time` attribute of the `listener` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `keepalive-time` attribute value of the `listener` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `keepalive-time` attribute description: used to specify the amount of milliseconds that pool threads should be kept running when idle; if not specified, threads will run until the executor is shut down.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=listener:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `max-threads` attribute of the `listener` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-threads` attribute value of the `listener` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-threads` attribute description: the maximum thread pool size.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=listener:read-attribute(name=max-threads)`

## syntax of the operation to get the `min-threads` attribute of the `listener` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `min-threads` attribute value of the `listener` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `min-threads` attribute description: the core thread pool size which is smaller than the maximum pool size. if undefined, the core thread pool size is the same as the maximum thread pool size.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=listener:read-attribute(name=min-threads)`

## syntax of the operation to get the `queue-length` attribute of the `listener` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `queue-length` attribute value of the `listener` `thread-pool` resource of the `cache-container` resource of the `infinispan` subsystem
* `queue-length` attribute description: the queue length.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/thread-pool=listener:read-attribute(name=queue-length)`

## syntax of the operation to get a `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>:read-resource()`
* To get the list of all the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem use '*' for `<distributed-cache name>`.

## syntax of the operation to get the `capacity-factor` attribute of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `capacity-factor` attribute value of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `capacity-factor` attribute description: controls the proportion of entries that will reside on the local node, compared to the other nodes in the cluster.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>:read-attribute(name=capacity-factor)`

## syntax of the operation to get the `l1-lifespan` attribute of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `l1-lifespan` attribute value of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `l1-lifespan` attribute description: maximum lifespan of an entry placed in the `l1` cache. this element configures the `l1` cache behavior in 'distributed' caches instances. in any other cache modes, this element is ignored.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>:read-attribute(name=l1-lifespan)`

## syntax of the operation to get the `modules` attribute of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modules` attribute value of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modules` attribute description: the set of modules associated with this `cache's` configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>:read-attribute(name=modules)`

## syntax of the operation to get the `owners` attribute of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `owners` attribute value of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `owners` attribute description: number of `cluster-wide` replicas for each cache entry.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>:read-attribute(name=owners)`

## syntax of the operation to get the `remote-timeout` attribute of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-timeout` attribute value of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-timeout` attribute description: in sync mode, the timeout (in ms) used to wait for an acknowledgment when making a remote call, after which the call is aborted and an exception is thrown.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>:read-attribute(name=remote-timeout)`

## syntax of the operation to get the `segments` attribute of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segments` attribute value of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segments` attribute description: controls the number of hash space segments which is the granularity for key distribution in the cluster. value must be strictly positive.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>:read-attribute(name=segments)`

## syntax of the operation to get the `statistics-enabled` attribute of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `statistics-enabled` attribute value of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `statistics-enabled` attribute description: if enabled, statistics will be collected for this cache
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `interval` attribute of the `expiration` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `interval` attribute value of the `expiration` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `interval` attribute description: interval (in milliseconds) between subsequent runs to purge expired entries from memory and any cache stores. if you wish to disable the periodic eviction process altogether, set `wakeupinterval` to -`1`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=expiration:read-attribute(name=interval)`

## syntax of the operation to get the `lifespan` attribute of the `expiration` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `lifespan` attribute value of the `expiration` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `lifespan` attribute description: maximum lifespan of a cache entry, after which the entry is expired `cluster-wide`, in milliseconds. -`1` means the entries never expire.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=expiration:read-attribute(name=lifespan)`

## syntax of the operation to get the `max-idle` attribute of the `expiration` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-idle` attribute value of the `expiration` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-idle` attribute description: maximum idle time a cache entry will be maintained in the cache, in milliseconds. if the idle time is exceeded, the entry will be expired `cluster-wide`. -`1` means the entries never expire.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=expiration:read-attribute(name=max-idle)`

## syntax of the operation to get the `chunk-size` attribute of the `state-transfer` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `chunk-size` attribute value of the `state-transfer` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `chunk-size` attribute description: the maximum number of cache entries in a batch of transferred state.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=state-transfer:read-attribute(name=chunk-size)`

## syntax of the operation to get the `timeout` attribute of the `state-transfer` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `timeout` attribute value of the `state-transfer` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `timeout` attribute description: the maximum amount of time (ms) to wait for state from neighboring caches, before throwing an exception and aborting startup. if timeout is `0`, state transfer is performed asynchronously, and the cache will be immediately available.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=state-transfer:read-attribute(name=timeout)`

## syntax of the operation to get the `enabled` attribute of the `partition-handling` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `enabled` attribute value of the `partition-handling` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `enabled` attribute description: if enabled, the cache will enter degraded mode upon detecting a network partition that threatens the integrity of the cache.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=partition-handling:read-attribute(name=enabled)`

## syntax of the operation to get the `merge-policy` attribute of the `partition-handling` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `merge-policy` attribute value of the `partition-handling` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `merge-policy` attribute description: defines the policy for resolving cache entry conflicts when merging partitions.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=partition-handling:read-attribute(name=merge-policy)`

## syntax of the operation to get the `when-split` attribute of the `partition-handling` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `when-split` attribute value of the `partition-handling` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `when-split` attribute description: defines the availability of cache entries after a network partition is detected.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=partition-handling:read-attribute(name=when-split)`

## syntax of the operation to get the `acquire-timeout` attribute of the `locking` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `acquire-timeout` attribute value of the `locking` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `acquire-timeout` attribute description: maximum time to attempt a particular lock acquisition.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=locking:read-attribute(name=acquire-timeout)`

## syntax of the operation to get the `concurrency-level` attribute of the `locking` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `concurrency-level` attribute value of the `locking` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `concurrency-level` attribute description: concurrency level for lock containers. adjust this value according to the number of concurrent threads interacting with `infinispan`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=locking:read-attribute(name=concurrency-level)`

## syntax of the operation to get the `isolation` attribute of the `locking` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `isolation` attribute value of the `locking` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `isolation` attribute description: sets the cache locking isolation level.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=locking:read-attribute(name=isolation)`

## syntax of the operation to get the `striping` attribute of the `locking` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `striping` attribute value of the `locking` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `striping` attribute description: if true, a pool of shared locks is maintained for all entries that need to be locked. otherwise, a lock is created per entry in the cache. lock striping helps control memory footprint but may reduce concurrency in the system.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=locking:read-attribute(name=striping)`

## syntax of the operation to get a `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=backups/backup=<backup name>:read-resource()`
* To get the list of all the `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem use '*' for `<backup name>`.

## syntax of the operation to get the `after-failures` attribute of the `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `after-failures` attribute value of the `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `after-failures` attribute description: indicates the number of failures after which this backup site should go offline.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=backups/backup=<backup name>:read-attribute(name=after-failures)`

## syntax of the operation to get the `enabled` attribute of the `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `enabled` attribute value of the `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `enabled` attribute description: indicates whether or not this backup site is enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=backups/backup=<backup name>:read-attribute(name=enabled)`

## syntax of the operation to get the `failure-policy` attribute of the `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `failure-policy` attribute value of the `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `failure-policy` attribute description: the policy to follow when connectivity to the backup site fails.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=backups/backup=<backup name>:read-attribute(name=failure-policy)`

## syntax of the operation to get the `min-wait` attribute of the `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `min-wait` attribute value of the `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `min-wait` attribute description: indicates the minimum time (in milliseconds) to wait after the max number of failures is reached, after which this backup site should go offline.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=backups/backup=<backup name>:read-attribute(name=min-wait)`

## syntax of the operation to get the `strategy` attribute of the `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `strategy` attribute value of the `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `strategy` attribute description: the backup strategy for this cache
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=backups/backup=<backup name>:read-attribute(name=strategy)`

## syntax of the operation to get the `timeout` attribute of the `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `timeout` attribute value of the `backup` resource of the `backups` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `timeout` attribute description: the timeout for replicating to the backup site.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=backups/backup=<backup name>:read-attribute(name=timeout)`

## syntax of the operation to get the `complete-timeout` attribute of the `transaction` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `complete-timeout` attribute value of the `transaction` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `complete-timeout` attribute description: the duration (in ms) after which idle transactions are removed.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=transaction:read-attribute(name=complete-timeout)`

## syntax of the operation to get the `locking` attribute of the `transaction` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `locking` attribute value of the `transaction` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `locking` attribute description: the locking mode for this cache, one of optimistic or pessimistic.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=transaction:read-attribute(name=locking)`

## syntax of the operation to get the `mode` attribute of the `transaction` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `mode` attribute value of the `transaction` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `mode` attribute description: sets the cache transaction mode to one of none, `non_xa`, `non_durable_xa`, `full_xa`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=transaction:read-attribute(name=mode)`

## syntax of the operation to get the `stop-timeout` attribute of the `transaction` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `stop-timeout` attribute value of the `transaction` `component` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `stop-timeout` attribute description: if there are any ongoing transactions when a cache is stopped, `infinispan` waits for ongoing remote and local transactions to finish. the amount of time to wait for is defined by the cache stop timeout.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/component=transaction:read-attribute(name=stop-timeout)`

## syntax of the operation to get the `cache` attribute of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cache` attribute value of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `cache` attribute description: the name of the remote cache to use for this remote store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=remote:read-attribute(name=cache)`

## syntax of the operation to get the `fetch-state` attribute of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=remote:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=remote:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=remote:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=remote:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=remote:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=remote:read-attribute(name=purge)`

## syntax of the operation to get the `remote-servers` attribute of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-servers` attribute value of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-servers` attribute description: a list of remote servers for this cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=remote:read-attribute(name=remote-servers)`

## syntax of the operation to get the `segmented` attribute of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=remote:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=remote:read-attribute(name=shared)`

## syntax of the operation to get the `socket-timeout` attribute of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `socket-timeout` attribute value of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `socket-timeout` attribute description: a socket timeout for remote cache communication.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=remote:read-attribute(name=socket-timeout)`

## syntax of the operation to get the `tcp-no-delay` attribute of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `tcp-no-delay` attribute value of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `tcp-no-delay` attribute description: a `tcp_nodelay` value for remote cache communication.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=remote:read-attribute(name=tcp-no-delay)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `remote` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=remote/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `class` attribute of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `class` attribute value of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `class` attribute description: the custom store implementation class to use for this cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=custom:read-attribute(name=class)`

## syntax of the operation to get the `fetch-state` attribute of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=custom:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=custom:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=custom:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=custom:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=custom:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=custom:read-attribute(name=purge)`

## syntax of the operation to get the `segmented` attribute of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=custom:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=custom:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `custom` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=custom/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `data-source` attribute of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `data-source` attribute value of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `data-source` attribute description: references the data source used to connect to this store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc:read-attribute(name=data-source)`

## syntax of the operation to get the `dialect` attribute of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `dialect` attribute value of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `dialect` attribute description: the dialect of this `datastore`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc:read-attribute(name=dialect)`

## syntax of the operation to get the `fetch-state` attribute of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc:read-attribute(name=purge)`

## syntax of the operation to get the `segmented` attribute of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `create-on-start` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `create-on-start` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `create-on-start` attribute description: indicates whether the store should create this database table when the cache starts.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc/table=string:read-attribute(name=create-on-start)`

## syntax of the operation to get the `data-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `data-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `data-column` attribute description: a database column to hold cache entry data.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc/table=string:read-attribute(name=data-column)`

## syntax of the operation to get the `drop-on-stop` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `drop-on-stop` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `drop-on-stop` attribute description: indicates whether the store should drop this database table when the cache stops.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc/table=string:read-attribute(name=drop-on-stop)`

## syntax of the operation to get the `fetch-size` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-size` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-size` attribute description: for db queries, the fetch size will be used to set the fetch size on `resultsets`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc/table=string:read-attribute(name=fetch-size)`

## syntax of the operation to get the `id-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `id-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `id-column` attribute description: a database column to hold cache entry ids.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc/table=string:read-attribute(name=id-column)`

## syntax of the operation to get the `prefix` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `prefix` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `prefix` attribute description: the prefix for the database table name.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc/table=string:read-attribute(name=prefix)`

## syntax of the operation to get the `segment-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segment-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segment-column` attribute description: a database column to hold cache entry segment.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc/table=string:read-attribute(name=segment-column)`

## syntax of the operation to get the `timestamp-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `timestamp-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `timestamp-column` attribute description: a database column to hold cache entry timestamps.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=jdbc/table=string:read-attribute(name=timestamp-column)`

## syntax of the operation to get the `fetch-state` attribute of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=file:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=file:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=file:read-attribute(name=passivation)`

## syntax of the operation to get the `path` attribute of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `path` attribute value of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `path` attribute description: the system path under which this cache store will persist its entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=file:read-attribute(name=path)`

## syntax of the operation to get the `preload` attribute of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=file:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=file:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=file:read-attribute(name=purge)`

## syntax of the operation to get the `relative-to` attribute of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `relative-to` attribute value of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `relative-to` attribute description: the system path to which the specified path is relative.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=file:read-attribute(name=relative-to)`

## syntax of the operation to get the `segmented` attribute of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=file:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=file:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `file` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=file/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `cache-configuration` attribute of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cache-configuration` attribute value of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `cache-configuration` attribute description: name of the cache configuration template defined in `infinispan` server to create caches from.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=hotrod:read-attribute(name=cache-configuration)`

## syntax of the operation to get the `fetch-state` attribute of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=hotrod:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=hotrod:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=hotrod:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=hotrod:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=hotrod:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=hotrod:read-attribute(name=purge)`

## syntax of the operation to get the `remote-cache-container` attribute of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-cache-container` attribute value of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-cache-container` attribute description: reference to a `container-managed` `remote-cache-container`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=hotrod:read-attribute(name=remote-cache-container)`

## syntax of the operation to get the `segmented` attribute of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=hotrod:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=hotrod:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `hotrod` `store` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/store=hotrod/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `size` attribute of the `off-heap` `memory` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size` attribute value of the `off-heap` `memory` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size` attribute description: eviction threshold, as defined by the size unit.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/memory=off-heap:read-attribute(name=size)`

## syntax of the operation to get the `size-unit` attribute of the `off-heap` `memory` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size-unit` attribute value of the `off-heap` `memory` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size-unit` attribute description: the unit of the eviction threshold.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/memory=off-heap:read-attribute(name=size-unit)`

## syntax of the operation to get the `size` attribute of the `heap` `memory` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size` attribute value of the `heap` `memory` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size` attribute description: eviction threshold, as defined by the size unit.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/memory=heap:read-attribute(name=size)`

## syntax of the operation to get the `size-unit` attribute of the `heap` `memory` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size-unit` attribute value of the `heap` `memory` resource of the `distributed-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size-unit` attribute description: the unit of the eviction threshold.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/distributed-cache=<distributed-cache name>/memory=heap:read-attribute(name=size-unit)`

## syntax of the operation to get a `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>:read-resource()`
* To get the list of all the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem use '*' for `<invalidation-cache name>`.

## syntax of the operation to get the `modules` attribute of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modules` attribute value of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modules` attribute description: the set of modules associated with this `cache's` configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>:read-attribute(name=modules)`

## syntax of the operation to get the `remote-timeout` attribute of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-timeout` attribute value of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-timeout` attribute description: in sync mode, the timeout (in ms) used to wait for an acknowledgment when making a remote call, after which the call is aborted and an exception is thrown.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>:read-attribute(name=remote-timeout)`

## syntax of the operation to get the `statistics-enabled` attribute of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `statistics-enabled` attribute value of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `statistics-enabled` attribute description: if enabled, statistics will be collected for this cache
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `acquire-timeout` attribute of the `locking` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `acquire-timeout` attribute value of the `locking` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `acquire-timeout` attribute description: maximum time to attempt a particular lock acquisition.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/component=locking:read-attribute(name=acquire-timeout)`

## syntax of the operation to get the `concurrency-level` attribute of the `locking` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `concurrency-level` attribute value of the `locking` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `concurrency-level` attribute description: concurrency level for lock containers. adjust this value according to the number of concurrent threads interacting with `infinispan`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/component=locking:read-attribute(name=concurrency-level)`

## syntax of the operation to get the `isolation` attribute of the `locking` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `isolation` attribute value of the `locking` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `isolation` attribute description: sets the cache locking isolation level.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/component=locking:read-attribute(name=isolation)`

## syntax of the operation to get the `striping` attribute of the `locking` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `striping` attribute value of the `locking` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `striping` attribute description: if true, a pool of shared locks is maintained for all entries that need to be locked. otherwise, a lock is created per entry in the cache. lock striping helps control memory footprint but may reduce concurrency in the system.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/component=locking:read-attribute(name=striping)`

## syntax of the operation to get the `interval` attribute of the `expiration` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `interval` attribute value of the `expiration` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `interval` attribute description: interval (in milliseconds) between subsequent runs to purge expired entries from memory and any cache stores. if you wish to disable the periodic eviction process altogether, set `wakeupinterval` to -`1`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/component=expiration:read-attribute(name=interval)`

## syntax of the operation to get the `lifespan` attribute of the `expiration` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `lifespan` attribute value of the `expiration` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `lifespan` attribute description: maximum lifespan of a cache entry, after which the entry is expired `cluster-wide`, in milliseconds. -`1` means the entries never expire.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/component=expiration:read-attribute(name=lifespan)`

## syntax of the operation to get the `max-idle` attribute of the `expiration` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-idle` attribute value of the `expiration` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-idle` attribute description: maximum idle time a cache entry will be maintained in the cache, in milliseconds. if the idle time is exceeded, the entry will be expired `cluster-wide`. -`1` means the entries never expire.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/component=expiration:read-attribute(name=max-idle)`

## syntax of the operation to get the `complete-timeout` attribute of the `transaction` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `complete-timeout` attribute value of the `transaction` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `complete-timeout` attribute description: the duration (in ms) after which idle transactions are removed.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/component=transaction:read-attribute(name=complete-timeout)`

## syntax of the operation to get the `locking` attribute of the `transaction` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `locking` attribute value of the `transaction` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `locking` attribute description: the locking mode for this cache, one of optimistic or pessimistic.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/component=transaction:read-attribute(name=locking)`

## syntax of the operation to get the `mode` attribute of the `transaction` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `mode` attribute value of the `transaction` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `mode` attribute description: sets the cache transaction mode to one of none, `non_xa`, `non_durable_xa`, `full_xa`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/component=transaction:read-attribute(name=mode)`

## syntax of the operation to get the `stop-timeout` attribute of the `transaction` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `stop-timeout` attribute value of the `transaction` `component` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `stop-timeout` attribute description: if there are any ongoing transactions when a cache is stopped, `infinispan` waits for ongoing remote and local transactions to finish. the amount of time to wait for is defined by the cache stop timeout.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/component=transaction:read-attribute(name=stop-timeout)`

## syntax of the operation to get the `cache` attribute of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cache` attribute value of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `cache` attribute description: the name of the remote cache to use for this remote store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=remote:read-attribute(name=cache)`

## syntax of the operation to get the `fetch-state` attribute of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=remote:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=remote:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=remote:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=remote:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=remote:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=remote:read-attribute(name=purge)`

## syntax of the operation to get the `remote-servers` attribute of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-servers` attribute value of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-servers` attribute description: a list of remote servers for this cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=remote:read-attribute(name=remote-servers)`

## syntax of the operation to get the `segmented` attribute of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=remote:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=remote:read-attribute(name=shared)`

## syntax of the operation to get the `socket-timeout` attribute of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `socket-timeout` attribute value of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `socket-timeout` attribute description: a socket timeout for remote cache communication.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=remote:read-attribute(name=socket-timeout)`

## syntax of the operation to get the `tcp-no-delay` attribute of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `tcp-no-delay` attribute value of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `tcp-no-delay` attribute description: a `tcp_nodelay` value for remote cache communication.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=remote:read-attribute(name=tcp-no-delay)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `remote` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=remote/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `class` attribute of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `class` attribute value of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `class` attribute description: the custom store implementation class to use for this cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=custom:read-attribute(name=class)`

## syntax of the operation to get the `fetch-state` attribute of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=custom:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=custom:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=custom:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=custom:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=custom:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=custom:read-attribute(name=purge)`

## syntax of the operation to get the `segmented` attribute of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=custom:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=custom:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `custom` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=custom/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `cache-configuration` attribute of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cache-configuration` attribute value of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `cache-configuration` attribute description: name of the cache configuration template defined in `infinispan` server to create caches from.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=hotrod:read-attribute(name=cache-configuration)`

## syntax of the operation to get the `fetch-state` attribute of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=hotrod:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=hotrod:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=hotrod:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=hotrod:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=hotrod:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=hotrod:read-attribute(name=purge)`

## syntax of the operation to get the `remote-cache-container` attribute of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-cache-container` attribute value of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-cache-container` attribute description: reference to a `container-managed` `remote-cache-container`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=hotrod:read-attribute(name=remote-cache-container)`

## syntax of the operation to get the `segmented` attribute of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=hotrod:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=hotrod:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `hotrod` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=hotrod/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `data-source` attribute of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `data-source` attribute value of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `data-source` attribute description: references the data source used to connect to this store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc:read-attribute(name=data-source)`

## syntax of the operation to get the `dialect` attribute of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `dialect` attribute value of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `dialect` attribute description: the dialect of this `datastore`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc:read-attribute(name=dialect)`

## syntax of the operation to get the `fetch-state` attribute of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc:read-attribute(name=purge)`

## syntax of the operation to get the `segmented` attribute of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `create-on-start` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `create-on-start` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `create-on-start` attribute description: indicates whether the store should create this database table when the cache starts.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc/table=string:read-attribute(name=create-on-start)`

## syntax of the operation to get the `data-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `data-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `data-column` attribute description: a database column to hold cache entry data.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc/table=string:read-attribute(name=data-column)`

## syntax of the operation to get the `drop-on-stop` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `drop-on-stop` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `drop-on-stop` attribute description: indicates whether the store should drop this database table when the cache stops.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc/table=string:read-attribute(name=drop-on-stop)`

## syntax of the operation to get the `fetch-size` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-size` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-size` attribute description: for db queries, the fetch size will be used to set the fetch size on `resultsets`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc/table=string:read-attribute(name=fetch-size)`

## syntax of the operation to get the `id-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `id-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `id-column` attribute description: a database column to hold cache entry ids.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc/table=string:read-attribute(name=id-column)`

## syntax of the operation to get the `prefix` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `prefix` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `prefix` attribute description: the prefix for the database table name.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc/table=string:read-attribute(name=prefix)`

## syntax of the operation to get the `segment-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segment-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segment-column` attribute description: a database column to hold cache entry segment.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc/table=string:read-attribute(name=segment-column)`

## syntax of the operation to get the `timestamp-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `timestamp-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `timestamp-column` attribute description: a database column to hold cache entry timestamps.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=jdbc/table=string:read-attribute(name=timestamp-column)`

## syntax of the operation to get the `fetch-state` attribute of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=file:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=file:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=file:read-attribute(name=passivation)`

## syntax of the operation to get the `path` attribute of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `path` attribute value of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `path` attribute description: the system path under which this cache store will persist its entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=file:read-attribute(name=path)`

## syntax of the operation to get the `preload` attribute of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=file:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=file:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=file:read-attribute(name=purge)`

## syntax of the operation to get the `relative-to` attribute of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `relative-to` attribute value of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `relative-to` attribute description: the system path to which the specified path is relative.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=file:read-attribute(name=relative-to)`

## syntax of the operation to get the `segmented` attribute of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=file:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=file:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `file` `store` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/store=file/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `size` attribute of the `off-heap` `memory` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size` attribute value of the `off-heap` `memory` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size` attribute description: eviction threshold, as defined by the size unit.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/memory=off-heap:read-attribute(name=size)`

## syntax of the operation to get the `size-unit` attribute of the `off-heap` `memory` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size-unit` attribute value of the `off-heap` `memory` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size-unit` attribute description: the unit of the eviction threshold.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/memory=off-heap:read-attribute(name=size-unit)`

## syntax of the operation to get the `size` attribute of the `heap` `memory` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size` attribute value of the `heap` `memory` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size` attribute description: eviction threshold, as defined by the size unit.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/memory=heap:read-attribute(name=size)`

## syntax of the operation to get the `size-unit` attribute of the `heap` `memory` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size-unit` attribute value of the `heap` `memory` resource of the `invalidation-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size-unit` attribute description: the unit of the eviction threshold.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/invalidation-cache=<invalidation-cache name>/memory=heap:read-attribute(name=size-unit)`

## syntax of the operation to get a `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>:read-resource()`
* To get the list of all the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem use '*' for `<replicated-cache name>`.

## syntax of the operation to get the `modules` attribute of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modules` attribute value of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modules` attribute description: the set of modules associated with this `cache's` configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>:read-attribute(name=modules)`

## syntax of the operation to get the `remote-timeout` attribute of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-timeout` attribute value of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-timeout` attribute description: in sync mode, the timeout (in ms) used to wait for an acknowledgment when making a remote call, after which the call is aborted and an exception is thrown.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>:read-attribute(name=remote-timeout)`

## syntax of the operation to get the `statistics-enabled` attribute of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `statistics-enabled` attribute value of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `statistics-enabled` attribute description: if enabled, statistics will be collected for this cache
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `interval` attribute of the `expiration` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `interval` attribute value of the `expiration` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `interval` attribute description: interval (in milliseconds) between subsequent runs to purge expired entries from memory and any cache stores. if you wish to disable the periodic eviction process altogether, set `wakeupinterval` to -`1`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=expiration:read-attribute(name=interval)`

## syntax of the operation to get the `lifespan` attribute of the `expiration` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `lifespan` attribute value of the `expiration` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `lifespan` attribute description: maximum lifespan of a cache entry, after which the entry is expired `cluster-wide`, in milliseconds. -`1` means the entries never expire.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=expiration:read-attribute(name=lifespan)`

## syntax of the operation to get the `max-idle` attribute of the `expiration` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-idle` attribute value of the `expiration` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-idle` attribute description: maximum idle time a cache entry will be maintained in the cache, in milliseconds. if the idle time is exceeded, the entry will be expired `cluster-wide`. -`1` means the entries never expire.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=expiration:read-attribute(name=max-idle)`

## syntax of the operation to get the `chunk-size` attribute of the `state-transfer` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `chunk-size` attribute value of the `state-transfer` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `chunk-size` attribute description: the maximum number of cache entries in a batch of transferred state.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=state-transfer:read-attribute(name=chunk-size)`

## syntax of the operation to get the `timeout` attribute of the `state-transfer` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `timeout` attribute value of the `state-transfer` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `timeout` attribute description: the maximum amount of time (ms) to wait for state from neighboring caches, before throwing an exception and aborting startup. if timeout is `0`, state transfer is performed asynchronously, and the cache will be immediately available.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=state-transfer:read-attribute(name=timeout)`

## syntax of the operation to get the `enabled` attribute of the `partition-handling` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `enabled` attribute value of the `partition-handling` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `enabled` attribute description: if enabled, the cache will enter degraded mode upon detecting a network partition that threatens the integrity of the cache.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=partition-handling:read-attribute(name=enabled)`

## syntax of the operation to get the `merge-policy` attribute of the `partition-handling` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `merge-policy` attribute value of the `partition-handling` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `merge-policy` attribute description: defines the policy for resolving cache entry conflicts when merging partitions.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=partition-handling:read-attribute(name=merge-policy)`

## syntax of the operation to get the `when-split` attribute of the `partition-handling` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `when-split` attribute value of the `partition-handling` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `when-split` attribute description: defines the availability of cache entries after a network partition is detected.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=partition-handling:read-attribute(name=when-split)`

## syntax of the operation to get the `acquire-timeout` attribute of the `locking` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `acquire-timeout` attribute value of the `locking` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `acquire-timeout` attribute description: maximum time to attempt a particular lock acquisition.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=locking:read-attribute(name=acquire-timeout)`

## syntax of the operation to get the `concurrency-level` attribute of the `locking` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `concurrency-level` attribute value of the `locking` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `concurrency-level` attribute description: concurrency level for lock containers. adjust this value according to the number of concurrent threads interacting with `infinispan`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=locking:read-attribute(name=concurrency-level)`

## syntax of the operation to get the `isolation` attribute of the `locking` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `isolation` attribute value of the `locking` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `isolation` attribute description: sets the cache locking isolation level.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=locking:read-attribute(name=isolation)`

## syntax of the operation to get the `striping` attribute of the `locking` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `striping` attribute value of the `locking` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `striping` attribute description: if true, a pool of shared locks is maintained for all entries that need to be locked. otherwise, a lock is created per entry in the cache. lock striping helps control memory footprint but may reduce concurrency in the system.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=locking:read-attribute(name=striping)`

## syntax of the operation to get a `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=backups/backup=<backup name>:read-resource()`
* To get the list of all the `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem use '*' for `<backup name>`.

## syntax of the operation to get the `after-failures` attribute of the `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `after-failures` attribute value of the `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `after-failures` attribute description: indicates the number of failures after which this backup site should go offline.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=backups/backup=<backup name>:read-attribute(name=after-failures)`

## syntax of the operation to get the `enabled` attribute of the `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `enabled` attribute value of the `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `enabled` attribute description: indicates whether or not this backup site is enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=backups/backup=<backup name>:read-attribute(name=enabled)`

## syntax of the operation to get the `failure-policy` attribute of the `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `failure-policy` attribute value of the `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `failure-policy` attribute description: the policy to follow when connectivity to the backup site fails.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=backups/backup=<backup name>:read-attribute(name=failure-policy)`

## syntax of the operation to get the `min-wait` attribute of the `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `min-wait` attribute value of the `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `min-wait` attribute description: indicates the minimum time (in milliseconds) to wait after the max number of failures is reached, after which this backup site should go offline.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=backups/backup=<backup name>:read-attribute(name=min-wait)`

## syntax of the operation to get the `strategy` attribute of the `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `strategy` attribute value of the `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `strategy` attribute description: the backup strategy for this cache
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=backups/backup=<backup name>:read-attribute(name=strategy)`

## syntax of the operation to get the `timeout` attribute of the `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `timeout` attribute value of the `backup` resource of the `backups` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `timeout` attribute description: the timeout for replicating to the backup site.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=backups/backup=<backup name>:read-attribute(name=timeout)`

## syntax of the operation to get the `complete-timeout` attribute of the `transaction` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `complete-timeout` attribute value of the `transaction` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `complete-timeout` attribute description: the duration (in ms) after which idle transactions are removed.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=transaction:read-attribute(name=complete-timeout)`

## syntax of the operation to get the `locking` attribute of the `transaction` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `locking` attribute value of the `transaction` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `locking` attribute description: the locking mode for this cache, one of optimistic or pessimistic.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=transaction:read-attribute(name=locking)`

## syntax of the operation to get the `mode` attribute of the `transaction` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `mode` attribute value of the `transaction` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `mode` attribute description: sets the cache transaction mode to one of none, `non_xa`, `non_durable_xa`, `full_xa`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=transaction:read-attribute(name=mode)`

## syntax of the operation to get the `stop-timeout` attribute of the `transaction` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `stop-timeout` attribute value of the `transaction` `component` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `stop-timeout` attribute description: if there are any ongoing transactions when a cache is stopped, `infinispan` waits for ongoing remote and local transactions to finish. the amount of time to wait for is defined by the cache stop timeout.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/component=transaction:read-attribute(name=stop-timeout)`

## syntax of the operation to get the `cache` attribute of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cache` attribute value of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `cache` attribute description: the name of the remote cache to use for this remote store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=remote:read-attribute(name=cache)`

## syntax of the operation to get the `fetch-state` attribute of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=remote:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=remote:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=remote:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=remote:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=remote:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=remote:read-attribute(name=purge)`

## syntax of the operation to get the `remote-servers` attribute of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-servers` attribute value of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-servers` attribute description: a list of remote servers for this cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=remote:read-attribute(name=remote-servers)`

## syntax of the operation to get the `segmented` attribute of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=remote:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=remote:read-attribute(name=shared)`

## syntax of the operation to get the `socket-timeout` attribute of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `socket-timeout` attribute value of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `socket-timeout` attribute description: a socket timeout for remote cache communication.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=remote:read-attribute(name=socket-timeout)`

## syntax of the operation to get the `tcp-no-delay` attribute of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `tcp-no-delay` attribute value of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `tcp-no-delay` attribute description: a `tcp_nodelay` value for remote cache communication.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=remote:read-attribute(name=tcp-no-delay)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `remote` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=remote/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `class` attribute of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `class` attribute value of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `class` attribute description: the custom store implementation class to use for this cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=custom:read-attribute(name=class)`

## syntax of the operation to get the `fetch-state` attribute of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=custom:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=custom:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=custom:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=custom:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=custom:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=custom:read-attribute(name=purge)`

## syntax of the operation to get the `segmented` attribute of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=custom:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=custom:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `custom` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=custom/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `data-source` attribute of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `data-source` attribute value of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `data-source` attribute description: references the data source used to connect to this store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc:read-attribute(name=data-source)`

## syntax of the operation to get the `dialect` attribute of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `dialect` attribute value of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `dialect` attribute description: the dialect of this `datastore`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc:read-attribute(name=dialect)`

## syntax of the operation to get the `fetch-state` attribute of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc:read-attribute(name=purge)`

## syntax of the operation to get the `segmented` attribute of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `create-on-start` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `create-on-start` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `create-on-start` attribute description: indicates whether the store should create this database table when the cache starts.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc/table=string:read-attribute(name=create-on-start)`

## syntax of the operation to get the `data-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `data-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `data-column` attribute description: a database column to hold cache entry data.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc/table=string:read-attribute(name=data-column)`

## syntax of the operation to get the `drop-on-stop` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `drop-on-stop` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `drop-on-stop` attribute description: indicates whether the store should drop this database table when the cache stops.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc/table=string:read-attribute(name=drop-on-stop)`

## syntax of the operation to get the `fetch-size` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-size` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-size` attribute description: for db queries, the fetch size will be used to set the fetch size on `resultsets`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc/table=string:read-attribute(name=fetch-size)`

## syntax of the operation to get the `id-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `id-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `id-column` attribute description: a database column to hold cache entry ids.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc/table=string:read-attribute(name=id-column)`

## syntax of the operation to get the `prefix` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `prefix` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `prefix` attribute description: the prefix for the database table name.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc/table=string:read-attribute(name=prefix)`

## syntax of the operation to get the `segment-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segment-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segment-column` attribute description: a database column to hold cache entry segment.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc/table=string:read-attribute(name=segment-column)`

## syntax of the operation to get the `timestamp-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `timestamp-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `timestamp-column` attribute description: a database column to hold cache entry timestamps.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=jdbc/table=string:read-attribute(name=timestamp-column)`

## syntax of the operation to get the `fetch-state` attribute of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=file:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=file:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=file:read-attribute(name=passivation)`

## syntax of the operation to get the `path` attribute of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `path` attribute value of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `path` attribute description: the system path under which this cache store will persist its entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=file:read-attribute(name=path)`

## syntax of the operation to get the `preload` attribute of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=file:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=file:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=file:read-attribute(name=purge)`

## syntax of the operation to get the `relative-to` attribute of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `relative-to` attribute value of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `relative-to` attribute description: the system path to which the specified path is relative.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=file:read-attribute(name=relative-to)`

## syntax of the operation to get the `segmented` attribute of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=file:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=file:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `file` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=file/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `cache-configuration` attribute of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cache-configuration` attribute value of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `cache-configuration` attribute description: name of the cache configuration template defined in `infinispan` server to create caches from.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=hotrod:read-attribute(name=cache-configuration)`

## syntax of the operation to get the `fetch-state` attribute of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=hotrod:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=hotrod:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=hotrod:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=hotrod:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=hotrod:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=hotrod:read-attribute(name=purge)`

## syntax of the operation to get the `remote-cache-container` attribute of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-cache-container` attribute value of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-cache-container` attribute description: reference to a `container-managed` `remote-cache-container`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=hotrod:read-attribute(name=remote-cache-container)`

## syntax of the operation to get the `segmented` attribute of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=hotrod:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=hotrod:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `hotrod` `store` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/store=hotrod/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `size` attribute of the `off-heap` `memory` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size` attribute value of the `off-heap` `memory` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size` attribute description: eviction threshold, as defined by the size unit.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/memory=off-heap:read-attribute(name=size)`

## syntax of the operation to get the `size-unit` attribute of the `off-heap` `memory` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size-unit` attribute value of the `off-heap` `memory` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size-unit` attribute description: the unit of the eviction threshold.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/memory=off-heap:read-attribute(name=size-unit)`

## syntax of the operation to get the `size` attribute of the `heap` `memory` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size` attribute value of the `heap` `memory` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size` attribute description: eviction threshold, as defined by the size unit.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/memory=heap:read-attribute(name=size)`

## syntax of the operation to get the `size-unit` attribute of the `heap` `memory` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size-unit` attribute value of the `heap` `memory` resource of the `replicated-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size-unit` attribute description: the unit of the eviction threshold.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/replicated-cache=<replicated-cache name>/memory=heap:read-attribute(name=size-unit)`

## syntax of the operation to get a `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>:read-resource()`
* To get the list of all the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem use '*' for `<scattered-cache name>`.

## syntax of the operation to get the `bias-lifespan` attribute of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `bias-lifespan` attribute value of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `bias-lifespan` attribute description: when greater than zero, specifies the duration (in ms) that a cache entry will be cached on a `non-owner` following a write operation.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>:read-attribute(name=bias-lifespan)`

## syntax of the operation to get the `invalidation-batch-size` attribute of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `invalidation-batch-size` attribute value of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `invalidation-batch-size` attribute description: the threshold after which batched invalidations are sent.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>:read-attribute(name=invalidation-batch-size)`

## syntax of the operation to get the `modules` attribute of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modules` attribute value of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modules` attribute description: the set of modules associated with this `cache's` configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>:read-attribute(name=modules)`

## syntax of the operation to get the `remote-timeout` attribute of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-timeout` attribute value of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-timeout` attribute description: in sync mode, the timeout (in ms) used to wait for an acknowledgment when making a remote call, after which the call is aborted and an exception is thrown.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>:read-attribute(name=remote-timeout)`

## syntax of the operation to get the `segments` attribute of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segments` attribute value of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segments` attribute description: controls the number of hash space segments which is the granularity for key distribution in the cluster. value must be strictly positive.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>:read-attribute(name=segments)`

## syntax of the operation to get the `statistics-enabled` attribute of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `statistics-enabled` attribute value of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `statistics-enabled` attribute description: if enabled, statistics will be collected for this cache
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `interval` attribute of the `expiration` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `interval` attribute value of the `expiration` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `interval` attribute description: interval (in milliseconds) between subsequent runs to purge expired entries from memory and any cache stores. if you wish to disable the periodic eviction process altogether, set `wakeupinterval` to -`1`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=expiration:read-attribute(name=interval)`

## syntax of the operation to get the `lifespan` attribute of the `expiration` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `lifespan` attribute value of the `expiration` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `lifespan` attribute description: maximum lifespan of a cache entry, after which the entry is expired `cluster-wide`, in milliseconds. -`1` means the entries never expire.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=expiration:read-attribute(name=lifespan)`

## syntax of the operation to get the `max-idle` attribute of the `expiration` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-idle` attribute value of the `expiration` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-idle` attribute description: maximum idle time a cache entry will be maintained in the cache, in milliseconds. if the idle time is exceeded, the entry will be expired `cluster-wide`. -`1` means the entries never expire.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=expiration:read-attribute(name=max-idle)`

## syntax of the operation to get the `chunk-size` attribute of the `state-transfer` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `chunk-size` attribute value of the `state-transfer` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `chunk-size` attribute description: the maximum number of cache entries in a batch of transferred state.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=state-transfer:read-attribute(name=chunk-size)`

## syntax of the operation to get the `timeout` attribute of the `state-transfer` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `timeout` attribute value of the `state-transfer` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `timeout` attribute description: the maximum amount of time (ms) to wait for state from neighboring caches, before throwing an exception and aborting startup. if timeout is `0`, state transfer is performed asynchronously, and the cache will be immediately available.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=state-transfer:read-attribute(name=timeout)`

## syntax of the operation to get the `enabled` attribute of the `partition-handling` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `enabled` attribute value of the `partition-handling` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `enabled` attribute description: if enabled, the cache will enter degraded mode upon detecting a network partition that threatens the integrity of the cache.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=partition-handling:read-attribute(name=enabled)`

## syntax of the operation to get the `merge-policy` attribute of the `partition-handling` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `merge-policy` attribute value of the `partition-handling` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `merge-policy` attribute description: defines the policy for resolving cache entry conflicts when merging partitions.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=partition-handling:read-attribute(name=merge-policy)`

## syntax of the operation to get the `when-split` attribute of the `partition-handling` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `when-split` attribute value of the `partition-handling` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `when-split` attribute description: defines the availability of cache entries after a network partition is detected.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=partition-handling:read-attribute(name=when-split)`

## syntax of the operation to get the `acquire-timeout` attribute of the `locking` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `acquire-timeout` attribute value of the `locking` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `acquire-timeout` attribute description: maximum time to attempt a particular lock acquisition.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=locking:read-attribute(name=acquire-timeout)`

## syntax of the operation to get the `concurrency-level` attribute of the `locking` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `concurrency-level` attribute value of the `locking` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `concurrency-level` attribute description: concurrency level for lock containers. adjust this value according to the number of concurrent threads interacting with `infinispan`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=locking:read-attribute(name=concurrency-level)`

## syntax of the operation to get the `isolation` attribute of the `locking` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `isolation` attribute value of the `locking` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `isolation` attribute description: sets the cache locking isolation level.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=locking:read-attribute(name=isolation)`

## syntax of the operation to get the `striping` attribute of the `locking` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `striping` attribute value of the `locking` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `striping` attribute description: if true, a pool of shared locks is maintained for all entries that need to be locked. otherwise, a lock is created per entry in the cache. lock striping helps control memory footprint but may reduce concurrency in the system.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=locking:read-attribute(name=striping)`

## syntax of the operation to get a `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=backups/backup=<backup name>:read-resource()`
* To get the list of all the `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem use '*' for `<backup name>`.

## syntax of the operation to get the `after-failures` attribute of the `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `after-failures` attribute value of the `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `after-failures` attribute description: indicates the number of failures after which this backup site should go offline.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=backups/backup=<backup name>:read-attribute(name=after-failures)`

## syntax of the operation to get the `enabled` attribute of the `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `enabled` attribute value of the `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `enabled` attribute description: indicates whether or not this backup site is enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=backups/backup=<backup name>:read-attribute(name=enabled)`

## syntax of the operation to get the `failure-policy` attribute of the `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `failure-policy` attribute value of the `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `failure-policy` attribute description: the policy to follow when connectivity to the backup site fails.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=backups/backup=<backup name>:read-attribute(name=failure-policy)`

## syntax of the operation to get the `min-wait` attribute of the `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `min-wait` attribute value of the `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `min-wait` attribute description: indicates the minimum time (in milliseconds) to wait after the max number of failures is reached, after which this backup site should go offline.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=backups/backup=<backup name>:read-attribute(name=min-wait)`

## syntax of the operation to get the `strategy` attribute of the `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `strategy` attribute value of the `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `strategy` attribute description: the backup strategy for this cache
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=backups/backup=<backup name>:read-attribute(name=strategy)`

## syntax of the operation to get the `timeout` attribute of the `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `timeout` attribute value of the `backup` resource of the `backups` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `timeout` attribute description: the timeout for replicating to the backup site.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=backups/backup=<backup name>:read-attribute(name=timeout)`

## syntax of the operation to get the `complete-timeout` attribute of the `transaction` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `complete-timeout` attribute value of the `transaction` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `complete-timeout` attribute description: the duration (in ms) after which idle transactions are removed.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=transaction:read-attribute(name=complete-timeout)`

## syntax of the operation to get the `locking` attribute of the `transaction` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `locking` attribute value of the `transaction` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `locking` attribute description: the locking mode for this cache, one of optimistic or pessimistic.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=transaction:read-attribute(name=locking)`

## syntax of the operation to get the `mode` attribute of the `transaction` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `mode` attribute value of the `transaction` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `mode` attribute description: sets the cache transaction mode to one of none, `non_xa`, `non_durable_xa`, `full_xa`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=transaction:read-attribute(name=mode)`

## syntax of the operation to get the `stop-timeout` attribute of the `transaction` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `stop-timeout` attribute value of the `transaction` `component` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `stop-timeout` attribute description: if there are any ongoing transactions when a cache is stopped, `infinispan` waits for ongoing remote and local transactions to finish. the amount of time to wait for is defined by the cache stop timeout.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/component=transaction:read-attribute(name=stop-timeout)`

## syntax of the operation to get the `cache` attribute of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cache` attribute value of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `cache` attribute description: the name of the remote cache to use for this remote store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=remote:read-attribute(name=cache)`

## syntax of the operation to get the `fetch-state` attribute of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=remote:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=remote:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=remote:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=remote:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=remote:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=remote:read-attribute(name=purge)`

## syntax of the operation to get the `remote-servers` attribute of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-servers` attribute value of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-servers` attribute description: a list of remote servers for this cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=remote:read-attribute(name=remote-servers)`

## syntax of the operation to get the `segmented` attribute of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=remote:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=remote:read-attribute(name=shared)`

## syntax of the operation to get the `socket-timeout` attribute of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `socket-timeout` attribute value of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `socket-timeout` attribute description: a socket timeout for remote cache communication.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=remote:read-attribute(name=socket-timeout)`

## syntax of the operation to get the `tcp-no-delay` attribute of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `tcp-no-delay` attribute value of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `tcp-no-delay` attribute description: a `tcp_nodelay` value for remote cache communication.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=remote:read-attribute(name=tcp-no-delay)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `remote` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=remote/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `class` attribute of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `class` attribute value of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `class` attribute description: the custom store implementation class to use for this cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=custom:read-attribute(name=class)`

## syntax of the operation to get the `fetch-state` attribute of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=custom:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=custom:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=custom:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=custom:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=custom:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=custom:read-attribute(name=purge)`

## syntax of the operation to get the `segmented` attribute of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=custom:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=custom:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `custom` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=custom/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `data-source` attribute of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `data-source` attribute value of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `data-source` attribute description: references the data source used to connect to this store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc:read-attribute(name=data-source)`

## syntax of the operation to get the `dialect` attribute of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `dialect` attribute value of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `dialect` attribute description: the dialect of this `datastore`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc:read-attribute(name=dialect)`

## syntax of the operation to get the `fetch-state` attribute of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc:read-attribute(name=purge)`

## syntax of the operation to get the `segmented` attribute of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `create-on-start` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `create-on-start` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `create-on-start` attribute description: indicates whether the store should create this database table when the cache starts.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc/table=string:read-attribute(name=create-on-start)`

## syntax of the operation to get the `data-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `data-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `data-column` attribute description: a database column to hold cache entry data.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc/table=string:read-attribute(name=data-column)`

## syntax of the operation to get the `drop-on-stop` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `drop-on-stop` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `drop-on-stop` attribute description: indicates whether the store should drop this database table when the cache stops.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc/table=string:read-attribute(name=drop-on-stop)`

## syntax of the operation to get the `fetch-size` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-size` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-size` attribute description: for db queries, the fetch size will be used to set the fetch size on `resultsets`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc/table=string:read-attribute(name=fetch-size)`

## syntax of the operation to get the `id-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `id-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `id-column` attribute description: a database column to hold cache entry ids.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc/table=string:read-attribute(name=id-column)`

## syntax of the operation to get the `prefix` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `prefix` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `prefix` attribute description: the prefix for the database table name.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc/table=string:read-attribute(name=prefix)`

## syntax of the operation to get the `segment-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segment-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segment-column` attribute description: a database column to hold cache entry segment.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc/table=string:read-attribute(name=segment-column)`

## syntax of the operation to get the `timestamp-column` attribute of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `timestamp-column` attribute value of the `string` `table` resource of the `jdbc` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `timestamp-column` attribute description: a database column to hold cache entry timestamps.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=jdbc/table=string:read-attribute(name=timestamp-column)`

## syntax of the operation to get the `fetch-state` attribute of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=file:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=file:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=file:read-attribute(name=passivation)`

## syntax of the operation to get the `path` attribute of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `path` attribute value of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `path` attribute description: the system path under which this cache store will persist its entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=file:read-attribute(name=path)`

## syntax of the operation to get the `preload` attribute of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=file:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=file:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=file:read-attribute(name=purge)`

## syntax of the operation to get the `relative-to` attribute of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `relative-to` attribute value of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `relative-to` attribute description: the system path to which the specified path is relative.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=file:read-attribute(name=relative-to)`

## syntax of the operation to get the `segmented` attribute of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=file:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=file:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `file` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=file/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `cache-configuration` attribute of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cache-configuration` attribute value of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `cache-configuration` attribute description: name of the cache configuration template defined in `infinispan` server to create caches from.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=hotrod:read-attribute(name=cache-configuration)`

## syntax of the operation to get the `fetch-state` attribute of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `fetch-state` attribute value of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `fetch-state` attribute description: if true, fetch persistent state when joining a cluster. if multiple cache stores are chained, only one of them can have this property enabled.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=hotrod:read-attribute(name=fetch-state)`

## syntax of the operation to get the `max-batch-size` attribute of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-batch-size` attribute value of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `max-batch-size` attribute description: the maximum size of a batch to be `inserted/deleted` from the store. if the value is less than one, then no upper limit is placed on the number of operations in a batch.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=hotrod:read-attribute(name=max-batch-size)`

## syntax of the operation to get the `passivation` attribute of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivation` attribute value of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivation` attribute description: if true, data is only written to the cache store when it is evicted from memory, a phenomenon known as 'passivation'. next time the data is requested, it will be 'activated' which means that data will be brought back to memory and removed from the persistent store. if false, the cache store contains a copy of the contents in memory, so writes to cache result in cache store writes. this essentially gives you a '`write-through`' configuration.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=hotrod:read-attribute(name=passivation)`

## syntax of the operation to get the `preload` attribute of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `preload` attribute value of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `preload` attribute description: if true, when the cache starts, data stored in the cache store will be `pre-loaded` into memory. this is particularly useful when data in the cache store will be needed immediately after startup and you want to avoid cache operations being delayed as a result of loading this data lazily. can be used to provide a '`warm-cache`' on startup, however there is a performance penalty as startup time is affected by this process.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=hotrod:read-attribute(name=preload)`

## syntax of the operation to get the `properties` attribute of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of cache store properties.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=hotrod:read-attribute(name=properties)`

## syntax of the operation to get the `purge` attribute of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `purge` attribute value of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `purge` attribute description: if true, purges this cache store when it starts up.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=hotrod:read-attribute(name=purge)`

## syntax of the operation to get the `remote-cache-container` attribute of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remote-cache-container` attribute value of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remote-cache-container` attribute description: reference to a `container-managed` `remote-cache-container`.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=hotrod:read-attribute(name=remote-cache-container)`

## syntax of the operation to get the `segmented` attribute of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `segmented` attribute value of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `segmented` attribute description: indicates whether or not this cache store should be segment aware.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=hotrod:read-attribute(name=segmented)`

## syntax of the operation to get the `shared` attribute of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `shared` attribute value of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `shared` attribute description: this setting should be set to true when multiple cache instances share the same cache store (`e.g`., multiple nodes in a cluster using a `jdbc-based` `cachestore` pointing to the same, shared database.) setting this to true avoids multiple cache instances writing the same modification multiple times. if enabled, only the node where the modification originated will write to the cache store. if disabled, each individual cache reacts to a potential remote update by storing the data to the cache store.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=hotrod:read-attribute(name=shared)`

## syntax of the operation to get the `modification-queue-size` attribute of the `behind` `write` resource of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `modification-queue-size` attribute value of the `behind` `write` resource of the `hotrod` `store` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `modification-queue-size` attribute description: maximum number of entries in the asynchronous queue. when the queue is full, the store becomes `write-through` until it can accept new entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/store=hotrod/write=behind:read-attribute(name=modification-queue-size)`

## syntax of the operation to get the `size` attribute of the `off-heap` `memory` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size` attribute value of the `off-heap` `memory` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size` attribute description: eviction threshold, as defined by the size unit.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/memory=off-heap:read-attribute(name=size)`

## syntax of the operation to get the `size-unit` attribute of the `off-heap` `memory` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size-unit` attribute value of the `off-heap` `memory` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size-unit` attribute description: the unit of the eviction threshold.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/memory=off-heap:read-attribute(name=size-unit)`

## syntax of the operation to get the `size` attribute of the `heap` `memory` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size` attribute value of the `heap` `memory` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size` attribute description: eviction threshold, as defined by the size unit.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/memory=heap:read-attribute(name=size)`

## syntax of the operation to get the `size-unit` attribute of the `heap` `memory` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `size-unit` attribute value of the `heap` `memory` resource of the `scattered-cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `size-unit` attribute description: the unit of the eviction threshold.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/scattered-cache=<scattered-cache name>/memory=heap:read-attribute(name=size-unit)`

## syntax of the operation to get a `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-resource()`
* To get the list of all the `cache` resource of the `cache-container` resource of the `infinispan` subsystem use '*' for `<cache name>`.

## syntax of the operation to get the `activations` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `activations` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `activations` attribute description: the number of cache node activations (bringing a node into memory from a cache store).
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=activations)`

## syntax of the operation to get the `average-read-time` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `average-read-time` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `average-read-time` attribute description: average time (in ms) for cache reads. includes hits and misses.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=average-read-time)`

## syntax of the operation to get the `average-remove-time` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `average-remove-time` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `average-remove-time` attribute description: average time (in ms) for cache removes.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=average-remove-time)`

## syntax of the operation to get the `average-replication-time` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `average-replication-time` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `average-replication-time` attribute description: the average time taken to replicate data around the cluster.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=average-replication-time)`

## syntax of the operation to get the `average-write-time` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `average-write-time` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `average-write-time` attribute description: average time (in ms) for cache writes.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=average-write-time)`

## syntax of the operation to get the `evictions` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `evictions` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `evictions` attribute description: the number of cache eviction operations.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=evictions)`

## syntax of the operation to get the `hit-ratio` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `hit-ratio` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `hit-ratio` attribute description: the `hit/miss` ratio for the cache (`hits/hits+misses`).
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=hit-ratio)`

## syntax of the operation to get the `hits` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `hits` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `hits` attribute description: the number of cache attribute hits.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=hits)`

## syntax of the operation to get the `invalidations` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `invalidations` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `invalidations` attribute description: the number of cache invalidations.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=invalidations)`

## syntax of the operation to get the `misses` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `misses` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `misses` attribute description: the number of cache attribute misses.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=misses)`

## syntax of the operation to get the `number-of-entries` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `number-of-entries` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `number-of-entries` attribute description: the number of entries in the cache including `passivated` entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=number-of-entries)`

## syntax of the operation to get the `number-of-entries-in-memory` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `number-of-entries-in-memory` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `number-of-entries-in-memory` attribute description: the number of entries in the cache excluding `passivated` entries.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=number-of-entries-in-memory)`

## syntax of the operation to get the `passivations` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `passivations` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `passivations` attribute description: the number of cache node `passivations` (`passivating` a node from memory to a cache store).
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=passivations)`

## syntax of the operation to get the `read-write-ratio` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `read-write-ratio` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `read-write-ratio` attribute description: the `read/write` ratio of the cache ((`hits+misses)/stores`).
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=read-write-ratio)`

## syntax of the operation to get the `remove-hits` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remove-hits` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remove-hits` attribute description: the number of cache attribute remove hits.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=remove-hits)`

## syntax of the operation to get the `remove-misses` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `remove-misses` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `remove-misses` attribute description: the number of cache attribute remove misses.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=remove-misses)`

## syntax of the operation to get the `replication-count` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `replication-count` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `replication-count` attribute description: the number of times data was replicated around the cluster.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=replication-count)`

## syntax of the operation to get the `replication-failures` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `replication-failures` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `replication-failures` attribute description: the number of data replication failures.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=replication-failures)`

## syntax of the operation to get the `success-ratio` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `success-ratio` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `success-ratio` attribute description: the data replication success ratio (`successes/successes+failures`).
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=success-ratio)`

## syntax of the operation to get the `time-since-reset` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `time-since-reset` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `time-since-reset` attribute description: time (in secs) since cache statistics were reset.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=time-since-reset)`

## syntax of the operation to get the `time-since-start` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `time-since-start` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `time-since-start` attribute description: time (in secs) since cache was started.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=time-since-start)`

## syntax of the operation to get the `writes` attribute of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `writes` attribute value of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `writes` attribute description: the number of cache attribute put operations.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>:read-attribute(name=writes)`

## syntax of the operation to get the `current-concurrency-level` attribute of the `locking` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `current-concurrency-level` attribute value of the `locking` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `current-concurrency-level` attribute description: the estimated number of concurrently updating threads which this cache can support.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>/component=locking:read-attribute(name=current-concurrency-level)`

## syntax of the operation to get the `number-of-locks-available` attribute of the `locking` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `number-of-locks-available` attribute value of the `locking` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `number-of-locks-available` attribute description: the number of locks available to this cache.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>/component=locking:read-attribute(name=number-of-locks-available)`

## syntax of the operation to get the `number-of-locks-held` attribute of the `locking` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `number-of-locks-held` attribute value of the `locking` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `number-of-locks-held` attribute description: the number of locks currently in use by this cache.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>/component=locking:read-attribute(name=number-of-locks-held)`

## syntax of the operation to get the `cache-loader-loads` attribute of the `persistence` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cache-loader-loads` attribute value of the `persistence` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `cache-loader-loads` attribute description: the number of cache loader node loads.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>/component=persistence:read-attribute(name=cache-loader-loads)`

## syntax of the operation to get the `cache-loader-misses` attribute of the `persistence` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `cache-loader-misses` attribute value of the `persistence` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `cache-loader-misses` attribute description: the number of cache loader node misses.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>/component=persistence:read-attribute(name=cache-loader-misses)`

## syntax of the operation to get the `commits` attribute of the `transaction` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `commits` attribute value of the `transaction` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `commits` attribute description: the number of transaction commits.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>/component=transaction:read-attribute(name=commits)`

## syntax of the operation to get the `prepares` attribute of the `transaction` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `prepares` attribute value of the `transaction` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `prepares` attribute description: the number of transaction prepares.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>/component=transaction:read-attribute(name=prepares)`

## syntax of the operation to get the `rollbacks` attribute of the `transaction` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `rollbacks` attribute value of the `transaction` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `rollbacks` attribute description: the number of transaction rollbacks.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>/component=transaction:read-attribute(name=rollbacks)`

## syntax of the operation to get the `availability` attribute of the `partition-handling` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* Retrieve the `availability` attribute value of the `partition-handling` `component` resource of the `cache` resource of the `cache-container` resource of the `infinispan` subsystem
* `availability` attribute description: indicates the current availability of the cache.
* operation: `/subsystem=infinispan/cache-container=<cache-container name>/cache=<cache name>/component=partition-handling:read-attribute(name=availability)`

## syntax of the operation to get a `remote-cache-container` resource of the `infinispan` subsystem
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-resource()`
* To get the list of all the `remote-cache-container` resource of the `infinispan` subsystem use '*' for `<remote-cache-container name>`.

## syntax of the operation to get the `active-connections` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `active-connections` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `active-connections` attribute description: the number of active connections to the `infinispan` server.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=active-connections)`

## syntax of the operation to get the `connection-timeout` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `connection-timeout` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `connection-timeout` attribute description: defines the maximum socket connect timeout before giving up connecting to the server.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=connection-timeout)`

## syntax of the operation to get the `connections` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `connections` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `connections` attribute description: the total number of connections to the `infinispan` server.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=connections)`

## syntax of the operation to get the `default-remote-cluster` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `default-remote-cluster` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `default-remote-cluster` attribute description: required default remote server cluster.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=default-remote-cluster)`

## syntax of the operation to get the `idle-connections` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `idle-connections` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `idle-connections` attribute description: the number of idle connections to the `infinispan` server.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=idle-connections)`

## syntax of the operation to get the `key-size-estimate` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `key-size-estimate` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `key-size-estimate` attribute description: this hint allows sizing of byte buffers when serializing and `deserializing` keys, to minimize array resizing.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=key-size-estimate)`

## syntax of the operation to get the `marshaller` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `marshaller` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `marshaller` attribute description: defines the marshalling implementation used to marshal cache entries.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=marshaller)`

## syntax of the operation to get the `max-retries` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-retries` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `max-retries` attribute description: sets the maximum number of retries for each request. a valid value should be greater or equals than `0`. zero means no retry will made in case of a network failure.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=max-retries)`

## syntax of the operation to get the `modules` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `modules` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `modules` attribute description: the set of modules associated with this remote cache `container's` configuration.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=modules)`

## syntax of the operation to get the `properties` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `properties` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `properties` attribute description: a list of remote cache container properties.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=properties)`

## syntax of the operation to get the `protocol-version` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `protocol-version` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `protocol-version` attribute description: this property defines the protocol version that this client should use.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=protocol-version)`

## syntax of the operation to get the `socket-timeout` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `socket-timeout` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `socket-timeout` attribute description: enable or disable `so_timeout` on socket connections to remote hot rod servers with the specified timeout, in milliseconds. a timeout of `0` is interpreted as an infinite timeout.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=socket-timeout)`

## syntax of the operation to get the `statistics-enabled` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `statistics-enabled` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `statistics-enabled` attribute description: enables statistics gathering for this remote cache.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `tcp-keep-alive` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `tcp-keep-alive` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `tcp-keep-alive` attribute description: configures `tcp` `keepalive` on the `tcp` stack.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=tcp-keep-alive)`

## syntax of the operation to get the `tcp-no-delay` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `tcp-no-delay` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `tcp-no-delay` attribute description: enable or disable `tcp_nodelay` on socket connections to remote hot rod servers.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=tcp-no-delay)`

## syntax of the operation to get the `transaction-timeout` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `transaction-timeout` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `transaction-timeout` attribute description: the duration (in ms) after which idle transactions are rolled back.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=transaction-timeout)`

## syntax of the operation to get the `value-size-estimate` attribute of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `value-size-estimate` attribute value of the `remote-cache-container` resource of the `infinispan` subsystem
* `value-size-estimate` attribute description: this hint allows sizing of byte buffers when serializing and `deserializing` values, to minimize array resizing.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>:read-attribute(name=value-size-estimate)`

## syntax of the operation to get a `remote-cluster` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cluster=<remote-cluster name>:read-resource()`
* To get the list of all the `remote-cluster` resource of the `remote-cache-container` resource of the `infinispan` subsystem use '*' for `<remote-cluster name>`.

## syntax of the operation to get the `socket-bindings` attribute of the `remote-cluster` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `socket-bindings` attribute value of the `remote-cluster` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `socket-bindings` attribute description: list of `outbound-socket-bindings` of hot rod servers to connect to.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cluster=<remote-cluster name>:read-attribute(name=socket-bindings)`

## syntax of the operation to get a `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cache=<remote-cache name>:read-resource()`
* To get the list of all the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem use '*' for `<remote-cache name>`.

## syntax of the operation to get the `average-read-time` attribute of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `average-read-time` attribute value of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `average-read-time` attribute description: the average read time, in milliseconds, for this remote cache.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cache=<remote-cache name>:read-attribute(name=average-read-time)`

## syntax of the operation to get the `average-remove-time` attribute of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `average-remove-time` attribute value of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `average-remove-time` attribute description: the average remove time, in milliseconds, for this remote cache.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cache=<remote-cache name>:read-attribute(name=average-remove-time)`

## syntax of the operation to get the `average-write-time` attribute of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `average-write-time` attribute value of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `average-write-time` attribute description: the average write time, in milliseconds, to this remote cache.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cache=<remote-cache name>:read-attribute(name=average-write-time)`

## syntax of the operation to get the `hits` attribute of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `hits` attribute value of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `hits` attribute description: the number of hits to this remote cache, excluding hits from the `near-cache`.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cache=<remote-cache name>:read-attribute(name=hits)`

## syntax of the operation to get the `misses` attribute of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `misses` attribute value of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `misses` attribute description: the number of misses to this remote cache.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cache=<remote-cache name>:read-attribute(name=misses)`

## syntax of the operation to get the `near-cache-hits` attribute of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `near-cache-hits` attribute value of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `near-cache-hits` attribute description: the number of `near-cache` hits for this remote cache.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cache=<remote-cache name>:read-attribute(name=near-cache-hits)`

## syntax of the operation to get the `near-cache-invalidations` attribute of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `near-cache-invalidations` attribute value of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `near-cache-invalidations` attribute description: the number of `near-cache` invalidations for this remote cache.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cache=<remote-cache name>:read-attribute(name=near-cache-invalidations)`

## syntax of the operation to get the `near-cache-misses` attribute of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `near-cache-misses` attribute value of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `near-cache-misses` attribute description: the number of `near-cache` misses for this remote cache.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cache=<remote-cache name>:read-attribute(name=near-cache-misses)`

## syntax of the operation to get the `near-cache-size` attribute of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `near-cache-size` attribute value of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `near-cache-size` attribute description: the number of entries in the `near-cache` for this remote cache.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cache=<remote-cache name>:read-attribute(name=near-cache-size)`

## syntax of the operation to get the `removes` attribute of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `removes` attribute value of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `removes` attribute description: the number of removes to this remote cache.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cache=<remote-cache name>:read-attribute(name=removes)`

## syntax of the operation to get the `time-since-reset` attribute of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `time-since-reset` attribute value of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `time-since-reset` attribute description: the number of seconds since statistics were reset on this remote cache.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cache=<remote-cache name>:read-attribute(name=time-since-reset)`

## syntax of the operation to get the `writes` attribute of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `writes` attribute value of the `remote-cache` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `writes` attribute description: the number of writes to this remote cache.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/remote-cache=<remote-cache name>:read-attribute(name=writes)`

## syntax of the operation to get the `keepalive-time` attribute of the `async` `thread-pool` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `keepalive-time` attribute value of the `async` `thread-pool` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `keepalive-time` attribute description: used to specify the amount of milliseconds that pool threads should be kept running when idle; if not specified, threads will run until the executor is shut down.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/thread-pool=async:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `max-threads` attribute of the `async` `thread-pool` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-threads` attribute value of the `async` `thread-pool` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `max-threads` attribute description: the maximum thread pool size.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/thread-pool=async:read-attribute(name=max-threads)`

## syntax of the operation to get the `min-threads` attribute of the `async` `thread-pool` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `min-threads` attribute value of the `async` `thread-pool` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `min-threads` attribute description: the core thread pool size which is smaller than the maximum pool size. if undefined, the core thread pool size is the same as the maximum thread pool size.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/thread-pool=async:read-attribute(name=min-threads)`

## syntax of the operation to get the `queue-length` attribute of the `async` `thread-pool` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `queue-length` attribute value of the `async` `thread-pool` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `queue-length` attribute description: the queue length.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/thread-pool=async:read-attribute(name=queue-length)`

## syntax of the operation to get the `ssl-context` attribute of the `security` `component` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `ssl-context` attribute value of the `security` `component` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `ssl-context` attribute description: reference to the `elytron-managed` `sslcontext` to be used for connecting to the remote cluster.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/component=security:read-attribute(name=ssl-context)`

## syntax of the operation to get the `exhausted-action` attribute of the `connection-pool` `component` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `exhausted-action` attribute value of the `connection-pool` `component` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `exhausted-action` attribute description: specifies what happens when asking for a connection from a `server's` pool, and that pool is exhausted.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/component=connection-pool:read-attribute(name=exhausted-action)`

## syntax of the operation to get the `max-active` attribute of the `connection-pool` `component` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-active` attribute value of the `connection-pool` `component` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `max-active` attribute description: controls the maximum number of connections per server that are allocated (checked out to client threads, or idle in the pool) at one time. when `non-positive`, there is no limit to the number of connections per server. when `maxactive` is reached, the connection pool for that server is said to be exhausted. value -`1` means no limit.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/component=connection-pool:read-attribute(name=max-active)`

## syntax of the operation to get the `max-wait` attribute of the `connection-pool` `component` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `max-wait` attribute value of the `connection-pool` `component` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `max-wait` attribute description: the amount of time in milliseconds to wait for a connection to become available when the exhausted action is `exhaustedaction.wait`, after which a `java.util.nosuchelementexception` will be thrown. if a negative value is supplied, the pool will block indefinitely.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/component=connection-pool:read-attribute(name=max-wait)`

## syntax of the operation to get the `min-evictable-idle-time` attribute of the `connection-pool` `component` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `min-evictable-idle-time` attribute value of the `connection-pool` `component` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `min-evictable-idle-time` attribute description: specifies the minimum amount of time that an connection may sit idle in the pool before it is eligible for eviction due to idle time. when `non-positive`, no connection will be dropped from the pool due to idle time alone. this setting has no effect unless `timebetweenevictionrunsmillis` > `0`.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/component=connection-pool:read-attribute(name=min-evictable-idle-time)`

## syntax of the operation to get the `min-idle` attribute of the `connection-pool` `component` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* Retrieve the `min-idle` attribute value of the `connection-pool` `component` resource of the `remote-cache-container` resource of the `infinispan` subsystem
* `min-idle` attribute description: sets a target value for the minimum number of idle connections (per server) that should always be available. if this parameter is set to a positive number and `timebetweenevictionrunsmillis` > `0`, each time the idle connection eviction thread runs, it will try to create enough idle instances so that there will be `minidle` idle instances available for each server.
* operation: `/subsystem=infinispan/remote-cache-container=<remote-cache-container name>/component=connection-pool:read-attribute(name=min-idle)`

## syntax of the operation to get the `exposed-subsystems` attribute of the `metrics` subsystem
* Retrieve the `exposed-subsystems` attribute value of the `metrics` subsystem
* `exposed-subsystems` attribute description: the names of the `wildfly` subsystems that exposes their metrics (or '*' to expose any subsystem metrics).
* operation: `/subsystem=metrics:read-attribute(name=exposed-subsystems)`

## syntax of the operation to get the `prefix` attribute of the `metrics` subsystem
* Retrieve the `prefix` attribute value of the `metrics` subsystem
* `prefix` attribute description: prefix prepended to the name of the `wildfly` metrics exposed by the `http` endpoints.
* operation: `/subsystem=metrics:read-attribute(name=prefix)`

## syntax of the operation to get the `security-enabled` attribute of the `metrics` subsystem
* Retrieve the `security-enabled` attribute value of the `metrics` subsystem
* `security-enabled` attribute description: true if authentication is required to access the `http` endpoint on the `http` management interface.
* operation: `/subsystem=metrics:read-attribute(name=security-enabled)`

## syntax of the operation to get the `max-history` attribute of the `configuration-changes` `service` resource of the `core-management` subsystem
* Retrieve the `max-history` attribute value of the `configuration-changes` `service` resource of the `core-management` subsystem
* `max-history` attribute description: the maximum number of configuration changes stored in history.
* operation: `/subsystem=core-management/service=configuration-changes:read-attribute(name=max-history)`

## syntax of the operation to get a `process-state-listener` resource of the `core-management` subsystem
* operation: `/subsystem=core-management/process-state-listener=<process-state-listener name>:read-resource()`
* To get the list of all the `process-state-listener` resource of the `core-management` subsystem use '*' for `<process-state-listener name>`.

## syntax of the operation to get the `class` attribute of the `process-state-listener` resource of the `core-management` subsystem
* Retrieve the `class` attribute value of the `process-state-listener` resource of the `core-management` subsystem
* `class` attribute description: class name of the listener.
* operation: `/subsystem=core-management/process-state-listener=<process-state-listener name>:read-attribute(name=class)`

## syntax of the operation to get the `module` attribute of the `process-state-listener` resource of the `core-management` subsystem
* Retrieve the `module` attribute value of the `process-state-listener` resource of the `core-management` subsystem
* `module` attribute description: module identifier of the listener.
* operation: `/subsystem=core-management/process-state-listener=<process-state-listener name>:read-attribute(name=module)`

## syntax of the operation to get the `properties` attribute of the `process-state-listener` resource of the `core-management` subsystem
* Retrieve the `properties` attribute value of the `process-state-listener` resource of the `core-management` subsystem
* `properties` attribute description: properties to initiate the process state listener.
* operation: `/subsystem=core-management/process-state-listener=<process-state-listener name>:read-attribute(name=properties)`

## syntax of the operation to get the `timeout` attribute of the `process-state-listener` resource of the `core-management` subsystem
* Retrieve the `timeout` attribute value of the `process-state-listener` resource of the `core-management` subsystem
* `timeout` attribute description: duration value before cancelling the execution of a listener.
* operation: `/subsystem=core-management/process-state-listener=<process-state-listener name>:read-attribute(name=timeout)`

## syntax of the operation to get the `enabled` attribute of the `bean-validation` `bean-validation` resource of the `jca` subsystem
* Retrieve the `enabled` attribute value of the `bean-validation` `bean-validation` resource of the `jca` subsystem
* `enabled` attribute description: specify whether jakarta bean validation is enabled
* operation: `/subsystem=jca/bean-validation=bean-validation:read-attribute(name=enabled)`

## syntax of the operation to get the `debug` attribute of the `cached-connection-manager` `cached-connection-manager` resource of the `jca` subsystem
* Retrieve the `debug` attribute value of the `cached-connection-manager` `cached-connection-manager` resource of the `jca` subsystem
* `debug` attribute description: `enable/disable` debug information logging
* operation: `/subsystem=jca/cached-connection-manager=cached-connection-manager:read-attribute(name=debug)`

## syntax of the operation to get the `error` attribute of the `cached-connection-manager` `cached-connection-manager` resource of the `jca` subsystem
* Retrieve the `error` attribute value of the `cached-connection-manager` `cached-connection-manager` resource of the `jca` subsystem
* `error` attribute description: `enable/disable` error information logging
* operation: `/subsystem=jca/cached-connection-manager=cached-connection-manager:read-attribute(name=error)`

## syntax of the operation to get the `ignore-unknown-connections` attribute of the `cached-connection-manager` `cached-connection-manager` resource of the `jca` subsystem
* Retrieve the `ignore-unknown-connections` attribute value of the `cached-connection-manager` `cached-connection-manager` resource of the `jca` subsystem
* `ignore-unknown-connections` attribute description: do not cache unknown connections
* operation: `/subsystem=jca/cached-connection-manager=cached-connection-manager:read-attribute(name=ignore-unknown-connections)`

## syntax of the operation to get the `install` attribute of the `cached-connection-manager` `cached-connection-manager` resource of the `jca` subsystem
* Retrieve the `install` attribute value of the `cached-connection-manager` `cached-connection-manager` resource of the `jca` subsystem
* `install` attribute description: `enable/disable` the cached connection manager valve and interceptor
* operation: `/subsystem=jca/cached-connection-manager=cached-connection-manager:read-attribute(name=install)`

## syntax of the operation to get a `workmanager` resource of the `jca` subsystem
* operation: `/subsystem=jca/workmanager=<workmanager name>:read-resource()`
* To get the list of all the `workmanager` resource of the `jca` subsystem use '*' for `<workmanager name>`.

## syntax of the operation to get the `elytron-enabled` attribute of the `workmanager` resource of the `jca` subsystem
* Retrieve the `elytron-enabled` attribute value of the `workmanager` resource of the `jca` subsystem
* `elytron-enabled` attribute description: enables elytron security for this `workmanager`.
* operation: `/subsystem=jca/workmanager=<workmanager name>:read-attribute(name=elytron-enabled)`

## syntax of the operation to get the `name` attribute of the `workmanager` resource of the `jca` subsystem
* Retrieve the `name` attribute value of the `workmanager` resource of the `jca` subsystem
* `name` attribute description: the name of the `workmanager`
* operation: `/subsystem=jca/workmanager=<workmanager name>:read-attribute(name=name)`

## syntax of the operation to get a `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* operation: `/subsystem=jca/workmanager=<workmanager name>/short-running-threads=<short-running-threads name>:read-resource()`
* To get the list of all the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem use '*' for `<short-running-threads name>`.

## syntax of the operation to get the `allow-core-timeout` attribute of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `allow-core-timeout` attribute value of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `allow-core-timeout` attribute description: whether core threads may time out.
* operation: `/subsystem=jca/workmanager=<workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=allow-core-timeout)`

## syntax of the operation to get the `core-threads` attribute of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `core-threads` attribute value of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `core-threads` attribute description: the core thread pool size which is smaller than the maximum pool size. if undefined, the core thread pool size is the same as the maximum thread pool size.
* operation: `/subsystem=jca/workmanager=<workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=core-threads)`

## syntax of the operation to get the `current-thread-count` attribute of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `current-thread-count` attribute value of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `current-thread-count` attribute description: the current number of threads in the pool.
* operation: `/subsystem=jca/workmanager=<workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=current-thread-count)`

## syntax of the operation to get the `keepalive-time` attribute of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `keepalive-time` attribute value of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `keepalive-time` attribute description: used to specify the amount of time that pool threads should be kept running when idle; if not specified, threads will run until the executor is shut down.
* operation: `/subsystem=jca/workmanager=<workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `largest-thread-count` attribute of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `largest-thread-count` attribute value of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `largest-thread-count` attribute description: the largest number of threads that have ever simultaneously been in the pool.
* operation: `/subsystem=jca/workmanager=<workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=largest-thread-count)`

## syntax of the operation to get the `max-threads` attribute of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `max-threads` attribute value of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `max-threads` attribute description: the maximum thread pool size.
* operation: `/subsystem=jca/workmanager=<workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=max-threads)`

## syntax of the operation to get the `name` attribute of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `name` attribute value of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `name` attribute description: the name of the thread pool.
* operation: `/subsystem=jca/workmanager=<workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=name)`

## syntax of the operation to get the `queue-length` attribute of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `queue-length` attribute value of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `queue-length` attribute description: the queue length.
* operation: `/subsystem=jca/workmanager=<workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=queue-length)`

## syntax of the operation to get the `queue-size` attribute of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `queue-size` attribute value of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `queue-size` attribute description: the queue size.
* operation: `/subsystem=jca/workmanager=<workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=queue-size)`

## syntax of the operation to get the `rejected-count` attribute of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `rejected-count` attribute value of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `rejected-count` attribute description: the number of tasks that have been passed to the `handoff-executor` (if one is specified) or discarded.
* operation: `/subsystem=jca/workmanager=<workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=rejected-count)`

## syntax of the operation to get the `thread-factory` attribute of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `thread-factory` attribute value of the `short-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `thread-factory` attribute description: specifies the name of a specific thread factory to use to create worker threads. if not defined an appropriate default thread factory will be used.
* operation: `/subsystem=jca/workmanager=<workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=thread-factory)`

## syntax of the operation to get a `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* operation: `/subsystem=jca/workmanager=<workmanager name>/long-running-threads=<long-running-threads name>:read-resource()`
* To get the list of all the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem use '*' for `<long-running-threads name>`.

## syntax of the operation to get the `allow-core-timeout` attribute of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `allow-core-timeout` attribute value of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `allow-core-timeout` attribute description: whether core threads may time out.
* operation: `/subsystem=jca/workmanager=<workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=allow-core-timeout)`

## syntax of the operation to get the `core-threads` attribute of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `core-threads` attribute value of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `core-threads` attribute description: the core thread pool size which is smaller than the maximum pool size. if undefined, the core thread pool size is the same as the maximum thread pool size.
* operation: `/subsystem=jca/workmanager=<workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=core-threads)`

## syntax of the operation to get the `current-thread-count` attribute of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `current-thread-count` attribute value of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `current-thread-count` attribute description: the current number of threads in the pool.
* operation: `/subsystem=jca/workmanager=<workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=current-thread-count)`

## syntax of the operation to get the `keepalive-time` attribute of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `keepalive-time` attribute value of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `keepalive-time` attribute description: used to specify the amount of time that pool threads should be kept running when idle; if not specified, threads will run until the executor is shut down.
* operation: `/subsystem=jca/workmanager=<workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `largest-thread-count` attribute of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `largest-thread-count` attribute value of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `largest-thread-count` attribute description: the largest number of threads that have ever simultaneously been in the pool.
* operation: `/subsystem=jca/workmanager=<workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=largest-thread-count)`

## syntax of the operation to get the `max-threads` attribute of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `max-threads` attribute value of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `max-threads` attribute description: the maximum thread pool size.
* operation: `/subsystem=jca/workmanager=<workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=max-threads)`

## syntax of the operation to get the `name` attribute of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `name` attribute value of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `name` attribute description: the name of the thread pool.
* operation: `/subsystem=jca/workmanager=<workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=name)`

## syntax of the operation to get the `queue-length` attribute of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `queue-length` attribute value of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `queue-length` attribute description: the queue length.
* operation: `/subsystem=jca/workmanager=<workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=queue-length)`

## syntax of the operation to get the `queue-size` attribute of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `queue-size` attribute value of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `queue-size` attribute description: the queue size.
* operation: `/subsystem=jca/workmanager=<workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=queue-size)`

## syntax of the operation to get the `rejected-count` attribute of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `rejected-count` attribute value of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `rejected-count` attribute description: the number of tasks that have been passed to the `handoff-executor` (if one is specified) or discarded.
* operation: `/subsystem=jca/workmanager=<workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=rejected-count)`

## syntax of the operation to get the `thread-factory` attribute of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* Retrieve the `thread-factory` attribute value of the `long-running-threads` resource of the `workmanager` resource of the `jca` subsystem
* `thread-factory` attribute description: specifies the name of a specific thread factory to use to create worker threads. if not defined an appropriate default thread factory will be used.
* operation: `/subsystem=jca/workmanager=<workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=thread-factory)`

## syntax of the operation to get the `enabled` attribute of the `archive-validation` `archive-validation` resource of the `jca` subsystem
* Retrieve the `enabled` attribute value of the `archive-validation` `archive-validation` resource of the `jca` subsystem
* `enabled` attribute description: specify whether archive validation is enabled
* operation: `/subsystem=jca/archive-validation=archive-validation:read-attribute(name=enabled)`

## syntax of the operation to get the `fail-on-error` attribute of the `archive-validation` `archive-validation` resource of the `jca` subsystem
* Retrieve the `fail-on-error` attribute value of the `archive-validation` `archive-validation` resource of the `jca` subsystem
* `fail-on-error` attribute description: should an archive validation error report fail the deployment
* operation: `/subsystem=jca/archive-validation=archive-validation:read-attribute(name=fail-on-error)`

## syntax of the operation to get the `fail-on-warn` attribute of the `archive-validation` `archive-validation` resource of the `jca` subsystem
* Retrieve the `fail-on-warn` attribute value of the `archive-validation` `archive-validation` resource of the `jca` subsystem
* `fail-on-warn` attribute description: should an archive validation warning report fail the deployment
* operation: `/subsystem=jca/archive-validation=archive-validation:read-attribute(name=fail-on-warn)`

## syntax of the operation to get a `bootstrap-context` resource of the `jca` subsystem
* operation: `/subsystem=jca/bootstrap-context=<bootstrap-context name>:read-resource()`
* To get the list of all the `bootstrap-context` resource of the `jca` subsystem use '*' for `<bootstrap-context name>`.

## syntax of the operation to get the `name` attribute of the `bootstrap-context` resource of the `jca` subsystem
* Retrieve the `name` attribute value of the `bootstrap-context` resource of the `jca` subsystem
* `name` attribute description: the name of the `bootstrapcontext`
* operation: `/subsystem=jca/bootstrap-context=<bootstrap-context name>:read-attribute(name=name)`

## syntax of the operation to get the `workmanager` attribute of the `bootstrap-context` resource of the `jca` subsystem
* Retrieve the `workmanager` attribute value of the `bootstrap-context` resource of the `jca` subsystem
* `workmanager` attribute description: the `workmanager` instance for the `bootstrapcontext`
* operation: `/subsystem=jca/bootstrap-context=<bootstrap-context name>:read-attribute(name=workmanager)`

## syntax of the operation to get the `enabled` attribute of the `tracer` `tracer` resource of the `jca` subsystem
* Retrieve the `enabled` attribute value of the `tracer` `tracer` resource of the `jca` subsystem
* `enabled` attribute description: specify whether tracer is enabled
* operation: `/subsystem=jca/tracer=tracer:read-attribute(name=enabled)`

## syntax of the operation to get a `distributed-workmanager` resource of the `jca` subsystem
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>:read-resource()`
* To get the list of all the `distributed-workmanager` resource of the `jca` subsystem use '*' for `<distributed-workmanager name>`.

## syntax of the operation to get the `elytron-enabled` attribute of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `elytron-enabled` attribute value of the `distributed-workmanager` resource of the `jca` subsystem
* `elytron-enabled` attribute description: enables elytron security for this `workmanager`.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>:read-attribute(name=elytron-enabled)`

## syntax of the operation to get the `name` attribute of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `name` attribute value of the `distributed-workmanager` resource of the `jca` subsystem
* `name` attribute description: the name of the `distributedworkmanager`
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>:read-attribute(name=name)`

## syntax of the operation to get the `policy` attribute of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `policy` attribute value of the `distributed-workmanager` resource of the `jca` subsystem
* `policy` attribute description: the policy decides when to redistribute a work instance
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>:read-attribute(name=policy)`

## syntax of the operation to get the `policy-options` attribute of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `policy-options` attribute value of the `distributed-workmanager` resource of the `jca` subsystem
* `policy-options` attribute description: list of `policy's` options `key/value` pairs
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>:read-attribute(name=policy-options)`

## syntax of the operation to get the `selector` attribute of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `selector` attribute value of the `distributed-workmanager` resource of the `jca` subsystem
* `selector` attribute description: the selector decides to which nodes in the network to redistribute the work instance to
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>:read-attribute(name=selector)`

## syntax of the operation to get the `selector-options` attribute of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `selector-options` attribute value of the `distributed-workmanager` resource of the `jca` subsystem
* `selector-options` attribute description: list of `selector's` options `key/value` pairs
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>:read-attribute(name=selector-options)`

## syntax of the operation to get a `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/short-running-threads=<short-running-threads name>:read-resource()`
* To get the list of all the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem use '*' for `<short-running-threads name>`.

## syntax of the operation to get the `allow-core-timeout` attribute of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `allow-core-timeout` attribute value of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `allow-core-timeout` attribute description: whether core threads may time out.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=allow-core-timeout)`

## syntax of the operation to get the `core-threads` attribute of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `core-threads` attribute value of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `core-threads` attribute description: the core thread pool size which is smaller than the maximum pool size. if undefined, the core thread pool size is the same as the maximum thread pool size.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=core-threads)`

## syntax of the operation to get the `current-thread-count` attribute of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `current-thread-count` attribute value of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `current-thread-count` attribute description: the current number of threads in the pool.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=current-thread-count)`

## syntax of the operation to get the `keepalive-time` attribute of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `keepalive-time` attribute value of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `keepalive-time` attribute description: used to specify the amount of time that pool threads should be kept running when idle; if not specified, threads will run until the executor is shut down.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `largest-thread-count` attribute of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `largest-thread-count` attribute value of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `largest-thread-count` attribute description: the largest number of threads that have ever simultaneously been in the pool.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=largest-thread-count)`

## syntax of the operation to get the `max-threads` attribute of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `max-threads` attribute value of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `max-threads` attribute description: the maximum thread pool size.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=max-threads)`

## syntax of the operation to get the `name` attribute of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `name` attribute value of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `name` attribute description: the name of the thread pool.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=name)`

## syntax of the operation to get the `queue-length` attribute of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `queue-length` attribute value of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `queue-length` attribute description: the queue length.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=queue-length)`

## syntax of the operation to get the `queue-size` attribute of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `queue-size` attribute value of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `queue-size` attribute description: the queue size.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=queue-size)`

## syntax of the operation to get the `rejected-count` attribute of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `rejected-count` attribute value of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `rejected-count` attribute description: the number of tasks that have been passed to the `handoff-executor` (if one is specified) or discarded.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=rejected-count)`

## syntax of the operation to get the `thread-factory` attribute of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `thread-factory` attribute value of the `short-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `thread-factory` attribute description: specifies the name of a specific thread factory to use to create worker threads. if not defined an appropriate default thread factory will be used.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/short-running-threads=<short-running-threads name>:read-attribute(name=thread-factory)`

## syntax of the operation to get a `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/long-running-threads=<long-running-threads name>:read-resource()`
* To get the list of all the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem use '*' for `<long-running-threads name>`.

## syntax of the operation to get the `allow-core-timeout` attribute of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `allow-core-timeout` attribute value of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `allow-core-timeout` attribute description: whether core threads may time out.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=allow-core-timeout)`

## syntax of the operation to get the `core-threads` attribute of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `core-threads` attribute value of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `core-threads` attribute description: the core thread pool size which is smaller than the maximum pool size. if undefined, the core thread pool size is the same as the maximum thread pool size.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=core-threads)`

## syntax of the operation to get the `current-thread-count` attribute of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `current-thread-count` attribute value of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `current-thread-count` attribute description: the current number of threads in the pool.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=current-thread-count)`

## syntax of the operation to get the `keepalive-time` attribute of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `keepalive-time` attribute value of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `keepalive-time` attribute description: used to specify the amount of time that pool threads should be kept running when idle; if not specified, threads will run until the executor is shut down.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `largest-thread-count` attribute of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `largest-thread-count` attribute value of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `largest-thread-count` attribute description: the largest number of threads that have ever simultaneously been in the pool.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=largest-thread-count)`

## syntax of the operation to get the `max-threads` attribute of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `max-threads` attribute value of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `max-threads` attribute description: the maximum thread pool size.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=max-threads)`

## syntax of the operation to get the `name` attribute of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `name` attribute value of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `name` attribute description: the name of the thread pool.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=name)`

## syntax of the operation to get the `queue-length` attribute of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `queue-length` attribute value of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `queue-length` attribute description: the queue length.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=queue-length)`

## syntax of the operation to get the `queue-size` attribute of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `queue-size` attribute value of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `queue-size` attribute description: the queue size.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=queue-size)`

## syntax of the operation to get the `rejected-count` attribute of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `rejected-count` attribute value of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `rejected-count` attribute description: the number of tasks that have been passed to the `handoff-executor` (if one is specified) or discarded.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=rejected-count)`

## syntax of the operation to get the `thread-factory` attribute of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* Retrieve the `thread-factory` attribute value of the `long-running-threads` resource of the `distributed-workmanager` resource of the `jca` subsystem
* `thread-factory` attribute description: specifies the name of a specific thread factory to use to create worker threads. if not defined an appropriate default thread factory will be used.
* operation: `/subsystem=jca/distributed-workmanager=<distributed-workmanager name>/long-running-threads=<long-running-threads name>:read-attribute(name=thread-factory)`

## syntax of the operation to get the `active-requests` attribute of the `request-controller` subsystem
* Retrieve the `active-requests` attribute value of the `request-controller` subsystem
* `active-requests` attribute description: the number of requests that are currently running on the server.
* operation: `/subsystem=request-controller:read-attribute(name=active-requests)`

## syntax of the operation to get the `max-requests` attribute of the `request-controller` subsystem
* Retrieve the `max-requests` attribute value of the `request-controller` subsystem
* `max-requests` attribute description: the maximum number of all types of requests that can be running on a server at a time. once this limit is hit, any new requests will be rejected.
* operation: `/subsystem=request-controller:read-attribute(name=max-requests)`

## syntax of the operation to get the `track-individual-endpoints` attribute of the `request-controller` subsystem
* Retrieve the `track-individual-endpoints` attribute value of the `request-controller` subsystem
* `track-individual-endpoints` attribute description: if this is true, requests are tracked at an endpoint level, which will allow individual deployments to be suspended.
* operation: `/subsystem=request-controller:read-attribute(name=track-individual-endpoints)`

## syntax of the operation to get a `config-source-provider` resource of the `microprofile-config-smallrye` subsystem
* operation: `/subsystem=microprofile-config-smallrye/config-source-provider=<config-source-provider name>:read-resource()`
* To get the list of all the `config-source-provider` resource of the `microprofile-config-smallrye` subsystem use '*' for `<config-source-provider name>`.

## syntax of the operation to get the `class` attribute of the `config-source-provider` resource of the `microprofile-config-smallrye` subsystem
* Retrieve the `class` attribute value of the `config-source-provider` resource of the `microprofile-config-smallrye` subsystem
* `class` attribute description: class of the `configsourceprovider` to load
* operation: `/subsystem=microprofile-config-smallrye/config-source-provider=<config-source-provider name>:read-attribute(name=class)`

## syntax of the operation to get a `config-source` resource of the `microprofile-config-smallrye` subsystem
* operation: `/subsystem=microprofile-config-smallrye/config-source=<config-source name>:read-resource()`
* To get the list of all the `config-source` resource of the `microprofile-config-smallrye` subsystem use '*' for `<config-source name>`.

## syntax of the operation to get the `class` attribute of the `config-source` resource of the `microprofile-config-smallrye` subsystem
* Retrieve the `class` attribute value of the `config-source` resource of the `microprofile-config-smallrye` subsystem
* `class` attribute description: class of the `config` source to load
* operation: `/subsystem=microprofile-config-smallrye/config-source=<config-source name>:read-attribute(name=class)`

## syntax of the operation to get the `dir` attribute of the `config-source` resource of the `microprofile-config-smallrye` subsystem
* Retrieve the `dir` attribute value of the `config-source` resource of the `microprofile-config-smallrye` subsystem
* `dir` attribute description: directory that is scanned to `config` properties for this `config` source (file names are key, file content are value)
* operation: `/subsystem=microprofile-config-smallrye/config-source=<config-source name>:read-attribute(name=dir)`

## syntax of the operation to get the `ordinal` attribute of the `config-source` resource of the `microprofile-config-smallrye` subsystem
* Retrieve the `ordinal` attribute value of the `config-source` resource of the `microprofile-config-smallrye` subsystem
* `ordinal` attribute description: ordinal value for the `config` source
* operation: `/subsystem=microprofile-config-smallrye/config-source=<config-source name>:read-attribute(name=ordinal)`

## syntax of the operation to get the `properties` attribute of the `config-source` resource of the `microprofile-config-smallrye` subsystem
* Retrieve the `properties` attribute value of the `config-source` resource of the `microprofile-config-smallrye` subsystem
* `properties` attribute description: properties configured for this `config` source and stored directly in `wildfly` management model.
* operation: `/subsystem=microprofile-config-smallrye/config-source=<config-source name>:read-attribute(name=properties)`

## syntax of the operation to get the `default-security-domain` attribute of the `undertow` subsystem
* Retrieve the `default-security-domain` attribute value of the `undertow` subsystem
* `default-security-domain` attribute description: the default security domain used by web deployments
* operation: `/subsystem=undertow:read-attribute(name=default-security-domain)`

## syntax of the operation to get the `default-server` attribute of the `undertow` subsystem
* Retrieve the `default-server` attribute value of the `undertow` subsystem
* `default-server` attribute description: the default server to use for deployments
* operation: `/subsystem=undertow:read-attribute(name=default-server)`

## syntax of the operation to get the `default-servlet-container` attribute of the `undertow` subsystem
* Retrieve the `default-servlet-container` attribute value of the `undertow` subsystem
* `default-servlet-container` attribute description: the default `servlet` container to use for deployments
* operation: `/subsystem=undertow:read-attribute(name=default-servlet-container)`

## syntax of the operation to get the `default-virtual-host` attribute of the `undertow` subsystem
* Retrieve the `default-virtual-host` attribute value of the `undertow` subsystem
* `default-virtual-host` attribute description: the default virtual host to use for deployments
* operation: `/subsystem=undertow:read-attribute(name=default-virtual-host)`

## syntax of the operation to get the `instance-id` attribute of the `undertow` subsystem
* Retrieve the `instance-id` attribute value of the `undertow` subsystem
* `instance-id` attribute description: the cluster instance id (defaults to {$`jboss.node.name`} if undefined)
* operation: `/subsystem=undertow:read-attribute(name=instance-id)`

## syntax of the operation to get the `obfuscate-session-route` attribute of the `undertow` subsystem
* Retrieve the `obfuscate-session-route` attribute value of the `undertow` subsystem
* `obfuscate-session-route` attribute description: obfuscate the `instance-id` when routing, thus preventing that data from being sent across connections when serving `http` requests with the `http` invoker
* operation: `/subsystem=undertow:read-attribute(name=obfuscate-session-route)`

## syntax of the operation to get the `statistics-enabled` attribute of the `undertow` subsystem
* Retrieve the `statistics-enabled` attribute value of the `undertow` subsystem
* `statistics-enabled` attribute description: configures if statistics are enabled. changes take effect on the connector level statistics immediately, deployment level statistics will only be affected after the deployment is redeployed (or the container is reloaded).
* operation: `/subsystem=undertow:read-attribute(name=statistics-enabled)`

## syntax of the operation to get a `byte-buffer-pool` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/byte-buffer-pool=<byte-buffer-pool name>:read-resource()`
* To get the list of all the `byte-buffer-pool` resource of the `undertow` subsystem use '*' for `<byte-buffer-pool name>`.

## syntax of the operation to get the `buffer-size` attribute of the `byte-buffer-pool` resource of the `undertow` subsystem
* Retrieve the `buffer-size` attribute value of the `byte-buffer-pool` resource of the `undertow` subsystem
* `buffer-size` attribute description: the size of the buffer
* operation: `/subsystem=undertow/byte-buffer-pool=<byte-buffer-pool name>:read-attribute(name=buffer-size)`

## syntax of the operation to get the `direct` attribute of the `byte-buffer-pool` resource of the `undertow` subsystem
* Retrieve the `direct` attribute value of the `byte-buffer-pool` resource of the `undertow` subsystem
* `direct` attribute description: if this is true the buffer pool will use direct buffers, this is recommended for best performance
* operation: `/subsystem=undertow/byte-buffer-pool=<byte-buffer-pool name>:read-attribute(name=direct)`

## syntax of the operation to get the `leak-detection-percent` attribute of the `byte-buffer-pool` resource of the `undertow` subsystem
* Retrieve the `leak-detection-percent` attribute value of the `byte-buffer-pool` resource of the `undertow` subsystem
* `leak-detection-percent` attribute description: the percentage of buffers that will be allocated with a leak detector. this should only be larger than zero if you are experiencing issues with buffers leaking.
* operation: `/subsystem=undertow/byte-buffer-pool=<byte-buffer-pool name>:read-attribute(name=leak-detection-percent)`

## syntax of the operation to get the `max-pool-size` attribute of the `byte-buffer-pool` resource of the `undertow` subsystem
* Retrieve the `max-pool-size` attribute value of the `byte-buffer-pool` resource of the `undertow` subsystem
* `max-pool-size` attribute description: the maximum amount of buffers to keep in the pool. if more buffers are required at runtime they will be allocated dynamically. setting this to zero effectively disables pooling.
* operation: `/subsystem=undertow/byte-buffer-pool=<byte-buffer-pool name>:read-attribute(name=max-pool-size)`

## syntax of the operation to get the `thread-local-cache-size` attribute of the `byte-buffer-pool` resource of the `undertow` subsystem
* Retrieve the `thread-local-cache-size` attribute value of the `byte-buffer-pool` resource of the `undertow` subsystem
* `thread-local-cache-size` attribute description: the maximum number of buffers to cache on each thread. the actual number may be lower depending on the calculated usage pattern.
* operation: `/subsystem=undertow/byte-buffer-pool=<byte-buffer-pool name>:read-attribute(name=thread-local-cache-size)`

## syntax of the operation to get a `servlet-container` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-resource()`
* To get the list of all the `servlet-container` resource of the `undertow` subsystem use '*' for `<servlet-container name>`.

## syntax of the operation to get the `allow-non-standard-wrappers` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `allow-non-standard-wrappers` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `allow-non-standard-wrappers` attribute description: if true then request and response wrappers that do not extend the standard wrapper classes can be used
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=allow-non-standard-wrappers)`

## syntax of the operation to get the `allow-orphan-session` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `allow-orphan-session` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `allow-orphan-session` attribute description: indicates whether session creation should be permitted after a `response-closing` operation, `e.g`. `httpservletresponse.sendredirect`(...). enabling this behavior is generally discouraged, as the created session will be `unreferenceable`.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=allow-orphan-session)`

## syntax of the operation to get the `default-buffer-cache` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `default-buffer-cache` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `default-buffer-cache` attribute description: the buffer cache to use for caching static resources
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=default-buffer-cache)`

## syntax of the operation to get the `default-cookie-version` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `default-cookie-version` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `default-cookie-version` attribute description: the default cookie version `servlet` applications will send
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=default-cookie-version)`

## syntax of the operation to get the `default-encoding` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `default-encoding` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `default-encoding` attribute description: default encoding to use for all deployed applications
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=default-encoding)`

## syntax of the operation to get the `default-session-timeout` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `default-session-timeout` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `default-session-timeout` attribute description: the default session timeout (in minutes) for all applications deployed in the container.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=default-session-timeout)`

## syntax of the operation to get the `directory-listing` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `directory-listing` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `directory-listing` attribute description: if directory listing should be enabled for default `servlets`.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=directory-listing)`

## syntax of the operation to get the `disable-caching-for-secured-pages` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `disable-caching-for-secured-pages` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `disable-caching-for-secured-pages` attribute description: if undertow should set headers to disable caching for secured paged. disabling this can cause security problems, as sensitive pages may be cached by an intermediary.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=disable-caching-for-secured-pages)`

## syntax of the operation to get the `disable-file-watch-service` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `disable-file-watch-service` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `disable-file-watch-service` attribute description: if this is true then the file watch service will not be used to monitor exploded deployments for changes
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=disable-file-watch-service)`

## syntax of the operation to get the `disable-session-id-reuse` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `disable-session-id-reuse` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `disable-session-id-reuse` attribute description: if this is true then an unknown session id will never be reused, and a new session id will be generated. if this is false then it will be `re-used` if and only if it is present in the session manager of another deployment, to allow the same session id to be shared between applications on the same server.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=disable-session-id-reuse)`

## syntax of the operation to get the `eager-filter-initialization` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `eager-filter-initialization` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `eager-filter-initialization` attribute description: if true undertow calls filter init() on deployment start rather than when first requested.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=eager-filter-initialization)`

## syntax of the operation to get the `file-cache-max-file-size` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `file-cache-max-file-size` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `file-cache-max-file-size` attribute description: the maximum size of a file that will be cached in the file cache
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=file-cache-max-file-size)`

## syntax of the operation to get the `file-cache-metadata-size` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `file-cache-metadata-size` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `file-cache-metadata-size` attribute description: the maximum number of files that will have their `metadata` cached
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=file-cache-metadata-size)`

## syntax of the operation to get the `file-cache-time-to-live` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `file-cache-time-to-live` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `file-cache-time-to-live` attribute description: the length of time in ms an item will stay cached. by default this is `2000` for exploded deployments, and -`1` (infinite) for archive deployments
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=file-cache-time-to-live)`

## syntax of the operation to get the `ignore-flush` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `ignore-flush` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `ignore-flush` attribute description: ignore flushes on the `servlet` output stream. in most cases these just hurt performance for no good reason.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=ignore-flush)`

## syntax of the operation to get the `max-sessions` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `max-sessions` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `max-sessions` attribute description: the maximum number of sessions that can be active at one time
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=max-sessions)`

## syntax of the operation to get the `preserve-path-on-forward` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `preserve-path-on-forward` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `preserve-path-on-forward` attribute description: if this is true undertow will reset request path, `url` and uri information to original values after forward.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=preserve-path-on-forward)`

## syntax of the operation to get the `proactive-authentication` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `proactive-authentication` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `proactive-authentication` attribute description: if proactive authentication should be used. if this is true a user will always be authenticated if credentials are present.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=proactive-authentication)`

## syntax of the operation to get the `session-id-length` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `session-id-length` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `session-id-length` attribute description: the length of the generated session id. longer session `id's` are more secure. this number refers to the number of bytes of randomness that are used to generate the session id, the actual id that is sent to the client will be `base64` encoded so will be approximately `33`% larger (`e.g`. a session id length of `30` will result in a cookie value of length `40`).
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=session-id-length)`

## syntax of the operation to get the `stack-trace-on-error` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `stack-trace-on-error` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `stack-trace-on-error` attribute description: if an error page with the stack trace should be generated on error. values are all, none and `local-only`
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=stack-trace-on-error)`

## syntax of the operation to get the `use-listener-encoding` attribute of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `use-listener-encoding` attribute value of the `servlet-container` resource of the `undertow` subsystem
* `use-listener-encoding` attribute description: use encoding defined on listener
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>:read-attribute(name=use-listener-encoding)`

## syntax of the operation to get a `welcome-file` resource of the `servlet-container` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/welcome-file=<welcome-file name>:read-resource()`
* To get the list of all the `welcome-file` resource of the `servlet-container` resource of the `undertow` subsystem use '*' for `<welcome-file name>`.

## syntax of the operation to get the `session-timeout` attribute of the `crawler-session-management` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `session-timeout` attribute value of the `crawler-session-management` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `session-timeout` attribute description: the session timeout for sessions that are owned by crawlers
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=crawler-session-management:read-attribute(name=session-timeout)`

## syntax of the operation to get the `user-agents` attribute of the `crawler-session-management` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `user-agents` attribute value of the `crawler-session-management` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `user-agents` attribute description: regular expression that is used to match the user agent of a crawler
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=crawler-session-management:read-attribute(name=user-agents)`

## syntax of the operation to get the `domain` attribute of the `affinity-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `domain` attribute value of the `affinity-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `domain` attribute description: affinity cookie domain
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=affinity-cookie:read-attribute(name=domain)`

## syntax of the operation to get the `http-only` attribute of the `affinity-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `http-only` attribute value of the `affinity-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `http-only` attribute description: is affinity cookie `http-only`
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=affinity-cookie:read-attribute(name=http-only)`

## syntax of the operation to get the `max-age` attribute of the `affinity-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `max-age` attribute value of the `affinity-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `max-age` attribute description: max age of affinity cookie
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=affinity-cookie:read-attribute(name=max-age)`

## syntax of the operation to get the `name` attribute of the `affinity-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `name` attribute value of the `affinity-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `name` attribute description: name of the affinity cookie
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=affinity-cookie:read-attribute(name=name)`

## syntax of the operation to get the `secure` attribute of the `affinity-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `secure` attribute value of the `affinity-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `secure` attribute description: is affinity cookie secure?
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=affinity-cookie:read-attribute(name=secure)`

## syntax of the operation to get the `check-interval` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `check-interval` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `check-interval` attribute description: check interval for jakarta server pages updates using a background thread. this has no effect for most deployments where jakarta server pages change notifications are handled using the file system notification `api`. this only takes effect if the file watch service is disabled.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=check-interval)`

## syntax of the operation to get the `development` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `development` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `development` attribute description: enable development mode which enables reloading jakarta server pages `on-the-fly`
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=development)`

## syntax of the operation to get the `disabled` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `disabled` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `disabled` attribute description: disable the jakarta server pages container.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=disabled)`

## syntax of the operation to get the `display-source-fragment` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `display-source-fragment` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `display-source-fragment` attribute description: when a runtime error occurs, attempts to display corresponding jakarta server pages source fragment
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=display-source-fragment)`

## syntax of the operation to get the `dump-smap` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `dump-smap` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `dump-smap` attribute description: write `smap` data to a file.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=dump-smap)`

## syntax of the operation to get the `error-on-use-bean-invalid-class-attribute` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `error-on-use-bean-invalid-class-attribute` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `error-on-use-bean-invalid-class-attribute` attribute description: enable errors when using a bad class in `usebean`.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=error-on-use-bean-invalid-class-attribute)`

## syntax of the operation to get the `generate-strings-as-char-arrays` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `generate-strings-as-char-arrays` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `generate-strings-as-char-arrays` attribute description: generate string constants as char arrays.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=generate-strings-as-char-arrays)`

## syntax of the operation to get the `java-encoding` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `java-encoding` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `java-encoding` attribute description: specify the encoding used for java sources.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=java-encoding)`

## syntax of the operation to get the `keep-generated` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `keep-generated` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `keep-generated` attribute description: keep the generated `servlets`.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=keep-generated)`

## syntax of the operation to get the `mapped-file` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `mapped-file` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `mapped-file` attribute description: map to the jakarta server pages source.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=mapped-file)`

## syntax of the operation to get the `modification-test-interval` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `modification-test-interval` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `modification-test-interval` attribute description: minimum amount of time between two tests for updates, in seconds.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=modification-test-interval)`

## syntax of the operation to get the `optimize-scriptlets` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `optimize-scriptlets` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `optimize-scriptlets` attribute description: if jakarta server pages `scriptlets` should be optimised to remove string concatenation
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=optimize-scriptlets)`

## syntax of the operation to get the `recompile-on-fail` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `recompile-on-fail` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `recompile-on-fail` attribute description: retry failed jakarta server pages compilations on each request.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=recompile-on-fail)`

## syntax of the operation to get the `scratch-dir` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `scratch-dir` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `scratch-dir` attribute description: specify a different work directory.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=scratch-dir)`

## syntax of the operation to get the `smap` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `smap` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `smap` attribute description: enable `smap`.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=smap)`

## syntax of the operation to get the `source-vm` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `source-vm` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `source-vm` attribute description: source `vm` level for compilation.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=source-vm)`

## syntax of the operation to get the `tag-pooling` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `tag-pooling` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `tag-pooling` attribute description: enable tag pooling.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=tag-pooling)`

## syntax of the operation to get the `target-vm` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `target-vm` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `target-vm` attribute description: target `vm` level for compilation.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=target-vm)`

## syntax of the operation to get the `trim-spaces` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `trim-spaces` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `trim-spaces` attribute description: trim some spaces from the generated `servlet`.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=trim-spaces)`

## syntax of the operation to get the `x-powered-by` attribute of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `x-powered-by` attribute value of the `jsp` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `x-powered-by` attribute description: enable advertising the jakarta server pages engine in `x-powered-by`.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=jsp:read-attribute(name=x-powered-by)`

## syntax of the operation to get the `path` attribute of the `persistent-sessions` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `path` attribute value of the `persistent-sessions` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `path` attribute description: the path to the persistent session data directory. if this is null sessions will be stored in memory
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=persistent-sessions:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `persistent-sessions` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `relative-to` attribute value of the `persistent-sessions` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `relative-to` attribute description: the directory the path is relative to
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=persistent-sessions:read-attribute(name=relative-to)`

## syntax of the operation to get the `comment` attribute of the `session-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `comment` attribute value of the `session-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `comment` attribute description: cookie comment
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=session-cookie:read-attribute(name=comment)`

## syntax of the operation to get the `domain` attribute of the `session-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `domain` attribute value of the `session-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `domain` attribute description: cookie domain
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=session-cookie:read-attribute(name=domain)`

## syntax of the operation to get the `http-only` attribute of the `session-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `http-only` attribute value of the `session-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `http-only` attribute description: is cookie `http-only`
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=session-cookie:read-attribute(name=http-only)`

## syntax of the operation to get the `max-age` attribute of the `session-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `max-age` attribute value of the `session-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `max-age` attribute description: max age of cookie
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=session-cookie:read-attribute(name=max-age)`

## syntax of the operation to get the `name` attribute of the `session-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `name` attribute value of the `session-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `name` attribute description: name of the cookie
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=session-cookie:read-attribute(name=name)`

## syntax of the operation to get the `secure` attribute of the `session-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `secure` attribute value of the `session-cookie` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `secure` attribute description: is cookie secure?
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=session-cookie:read-attribute(name=secure)`

## syntax of the operation to get the `buffer-pool` attribute of the `websockets` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `buffer-pool` attribute value of the `websockets` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `buffer-pool` attribute description: the buffer pool to use for `websocket` deployments
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=websockets:read-attribute(name=buffer-pool)`

## syntax of the operation to get the `deflater-level` attribute of the `websockets` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `deflater-level` attribute value of the `websockets` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `deflater-level` attribute description: configures the level of compression of the deflate algorithm
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=websockets:read-attribute(name=deflater-level)`

## syntax of the operation to get the `dispatch-to-worker` attribute of the `websockets` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `dispatch-to-worker` attribute value of the `websockets` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `dispatch-to-worker` attribute description: if callbacks should be dispatched to a worker thread. if this is false then they will be run in the io thread, which is faster however care must be taken not to perform blocking operations.
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=websockets:read-attribute(name=dispatch-to-worker)`

## syntax of the operation to get the `per-message-deflate` attribute of the `websockets` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `per-message-deflate` attribute value of the `websockets` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `per-message-deflate` attribute description: enables `websocket's` `per-message` compression extension, `rfc-7692`
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=websockets:read-attribute(name=per-message-deflate)`

## syntax of the operation to get the `worker` attribute of the `websockets` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `worker` attribute value of the `websockets` `setting` resource of the `servlet-container` resource of the `undertow` subsystem
* `worker` attribute description: the worker to use for `websocket` deployments
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/setting=websockets:read-attribute(name=worker)`

## syntax of the operation to get a `mime-mapping` resource of the `servlet-container` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/mime-mapping=<mime-mapping name>:read-resource()`
* To get the list of all the `mime-mapping` resource of the `servlet-container` resource of the `undertow` subsystem use '*' for `<mime-mapping name>`.

## syntax of the operation to get the `value` attribute of the `mime-mapping` resource of the `servlet-container` resource of the `undertow` subsystem
* Retrieve the `value` attribute value of the `mime-mapping` resource of the `servlet-container` resource of the `undertow` subsystem
* `value` attribute description: the mime type for this mapping
* operation: `/subsystem=undertow/servlet-container=<servlet-container name>/mime-mapping=<mime-mapping name>:read-attribute(name=value)`

## syntax of the operation to get a `error-page` resource of the `filter` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=filter/error-page=<error-page name>:read-resource()`
* To get the list of all the `error-page` resource of the `filter` `configuration` resource of the `undertow` subsystem use '*' for `<error-page name>`.

## syntax of the operation to get the `code` attribute of the `error-page` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `code` attribute value of the `error-page` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `code` attribute description: error page code
* operation: `/subsystem=undertow/configuration=filter/error-page=<error-page name>:read-attribute(name=code)`

## syntax of the operation to get the `path` attribute of the `error-page` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `path` attribute value of the `error-page` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `path` attribute description: error page path
* operation: `/subsystem=undertow/configuration=filter/error-page=<error-page name>:read-attribute(name=path)`

## syntax of the operation to get a `expression-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=filter/expression-filter=<expression-filter name>:read-resource()`
* To get the list of all the `expression-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem use '*' for `<expression-filter name>`.

## syntax of the operation to get the `expression` attribute of the `expression-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `expression` attribute value of the `expression-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `expression` attribute description: the expression that defines the filter
* operation: `/subsystem=undertow/configuration=filter/expression-filter=<expression-filter name>:read-attribute(name=expression)`

## syntax of the operation to get the `module` attribute of the `expression-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `module` attribute value of the `expression-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `module` attribute description: module to use to load the filter definitions
* operation: `/subsystem=undertow/configuration=filter/expression-filter=<expression-filter name>:read-attribute(name=module)`

## syntax of the operation to get a `gzip` resource of the `filter` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=filter/gzip=<gzip name>:read-resource()`
* To get the list of all the `gzip` resource of the `filter` `configuration` resource of the `undertow` subsystem use '*' for `<gzip name>`.

## syntax of the operation to get a `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-resource()`
* To get the list of all the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem use '*' for `<mod-cluster name>`.

## syntax of the operation to get the `advertise-frequency` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `advertise-frequency` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `advertise-frequency` attribute description: the frequency (in milliseconds) that `mod-cluster` advertises itself on the network
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=advertise-frequency)`

## syntax of the operation to get the `advertise-path` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `advertise-path` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `advertise-path` attribute description: the path that `mod-cluster` is registered under.
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=advertise-path)`

## syntax of the operation to get the `advertise-protocol` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `advertise-protocol` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `advertise-protocol` attribute description: the protocol that is in use.
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=advertise-protocol)`

## syntax of the operation to get the `advertise-socket-binding` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `advertise-socket-binding` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `advertise-socket-binding` attribute description: the multicast group and port that is used to advertise.
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=advertise-socket-binding)`

## syntax of the operation to get the `broken-node-timeout` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `broken-node-timeout` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `broken-node-timeout` attribute description: the amount of time that must elapse before a broken node is removed from the table
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=broken-node-timeout)`

## syntax of the operation to get the `cached-connections-per-thread` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `cached-connections-per-thread` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `cached-connections-per-thread` attribute description: the number of connections that will be kept alive indefinitely
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=cached-connections-per-thread)`

## syntax of the operation to get the `connection-idle-timeout` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `connection-idle-timeout` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `connection-idle-timeout` attribute description: the amount of time a connection can be idle before it will be closed. connections will not time out once the pool size is down to the configured minimum (as configured by `cached-connections-per-thread`)
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=connection-idle-timeout)`

## syntax of the operation to get the `connections-per-thread` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `connections-per-thread` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `connections-per-thread` attribute description: the number of connections that will be maintained to `backend` servers, per io thread.
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=connections-per-thread)`

## syntax of the operation to get the `enable-http2` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `enable-http2` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `enable-http2` attribute description: if the load balancer should attempt to upgrade back end connections to `http2`. if `http2` is not supported `http` or `https` will be used as normal
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=enable-http2)`

## syntax of the operation to get the `failover-strategy` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `failover-strategy` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `failover-strategy` attribute description: determines how a `failover` node is chosen, in the event that the node to which a session has affinity is not available.
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=failover-strategy)`

## syntax of the operation to get the `health-check-interval` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `health-check-interval` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `health-check-interval` attribute description: the frequency of health check pings to `backend` nodes
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=health-check-interval)`

## syntax of the operation to get the `http2-enable-push` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `http2-enable-push` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `http2-enable-push` attribute description: if push should be enabled for `http/2` connections
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=http2-enable-push)`

## syntax of the operation to get the `http2-header-table-size` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `http2-header-table-size` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `http2-header-table-size` attribute description: the size of the header table used for `hpack` compression, in bytes. this amount of memory will be allocated per connection for compression. larger values use more memory but may give better compression.
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=http2-header-table-size)`

## syntax of the operation to get the `http2-initial-window-size` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `http2-initial-window-size` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `http2-initial-window-size` attribute description: the flow control window size that controls how quickly the client can send data to the server
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=http2-initial-window-size)`

## syntax of the operation to get the `http2-max-concurrent-streams` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `http2-max-concurrent-streams` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `http2-max-concurrent-streams` attribute description: the maximum number of `http/2` streams that can be active at any time on a single connection
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=http2-max-concurrent-streams)`

## syntax of the operation to get the `http2-max-frame-size` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `http2-max-frame-size` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `http2-max-frame-size` attribute description: the max `http/2` frame size
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=http2-max-frame-size)`

## syntax of the operation to get the `http2-max-header-list-size` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `http2-max-header-list-size` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `http2-max-header-list-size` attribute description: the maximum size of request headers the server is prepared to accept
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=http2-max-header-list-size)`

## syntax of the operation to get the `management-access-predicate` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `management-access-predicate` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `management-access-predicate` attribute description: a predicate that is applied to incoming requests to determine if they can perform mod cluster management commands. provides additional security on top of what is provided by limiting management to requests that originate from the `management-socket-binding`
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=management-access-predicate)`

## syntax of the operation to get the `management-socket-binding` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `management-socket-binding` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `management-socket-binding` attribute description: the socket binding of the `mod_cluster` management address and port. when using `mod_cluster` two `http` listeners should be defined, a public one to handle requests, and one bound to the internal network to handle mod cluster commands. this socket binding should correspond to the internal listener, and should not be publicly accessible.
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=management-socket-binding)`

## syntax of the operation to get the `max-ajp-packet-size` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `max-ajp-packet-size` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `max-ajp-packet-size` attribute description: the maximum size for `ajp` packets. increasing this will allow `ajp` to work for `requests/responses` that have a large amount of headers. this is an advanced option, and must be the same between load balancers and `backend` servers.
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=max-ajp-packet-size)`

## syntax of the operation to get the `max-request-time` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `max-request-time` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `max-request-time` attribute description: the max amount of time that a request to a `backend` node can take before it is killed
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=max-request-time)`

## syntax of the operation to get the `max-retries` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `max-retries` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `max-retries` attribute description: the number of times to attempt to retry a request if it fails. note that if a request is not considered idempotent then it will only be retried if the proxy can be sure it was not sent to the `backend` server).
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=max-retries)`

## syntax of the operation to get the `request-queue-size` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `request-queue-size` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `request-queue-size` attribute description: the number of requests that can be queued if the connection pool is full before requests are rejected with a `503`
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=request-queue-size)`

## syntax of the operation to get the `security-key` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `security-key` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `security-key` attribute description: the security key that is used for the `mod-cluster` group. all members must use the same security key.
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=security-key)`

## syntax of the operation to get the `security-realm` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `security-realm` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `security-realm` attribute description: the security realm that provides the `ssl` configuration
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=security-realm)`

## syntax of the operation to get the `ssl-context` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `ssl-context` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `ssl-context` attribute description: reference to the `sslcontext` to be used by this filter.
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=ssl-context)`

## syntax of the operation to get the `use-alias` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `use-alias` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `use-alias` attribute description: if an alias check is performed
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=use-alias)`

## syntax of the operation to get the `worker` attribute of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `worker` attribute value of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `worker` attribute description: the `xnio` worker that is used to send the advertise notifications
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>:read-attribute(name=worker)`

## syntax of the operation to get the `delimiter` attribute of the `ranked` `affinity` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `delimiter` attribute value of the `ranked` `affinity` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `delimiter` attribute description: the delimiter used to separate ranked routes within the session id.
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/affinity=ranked:read-attribute(name=delimiter)`

## syntax of the operation to get a `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>:read-resource()`
* To get the list of all the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem use '*' for `<balancer name>`.

## syntax of the operation to get the `max-attempts` attribute of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `max-attempts` attribute value of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `max-attempts` attribute description: maximum number of `failover` attempts by reverse proxy when sending the request to the `backend` server.
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>:read-attribute(name=max-attempts)`

## syntax of the operation to get the `sticky-session` attribute of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `sticky-session` attribute value of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `sticky-session` attribute description: if sticky sessions are enabled
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>:read-attribute(name=sticky-session)`

## syntax of the operation to get the `sticky-session-cookie` attribute of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `sticky-session-cookie` attribute value of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `sticky-session-cookie` attribute description: the session cookie name
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>:read-attribute(name=sticky-session-cookie)`

## syntax of the operation to get the `sticky-session-force` attribute of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `sticky-session-force` attribute value of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `sticky-session-force` attribute description: if this is true then an error will be returned if the request cannot be routed to the sticky node, otherwise it will be routed to another node
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>:read-attribute(name=sticky-session-force)`

## syntax of the operation to get the `sticky-session-path` attribute of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `sticky-session-path` attribute value of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `sticky-session-path` attribute description: the path of the sticky session cookie
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>:read-attribute(name=sticky-session-path)`

## syntax of the operation to get the `sticky-session-remove` attribute of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `sticky-session-remove` attribute value of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `sticky-session-remove` attribute description: remove the session cookie if the request cannot be routed to the correct host
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>:read-attribute(name=sticky-session-remove)`

## syntax of the operation to get the `wait-worker` attribute of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `wait-worker` attribute value of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `wait-worker` attribute description: the number of seconds to wait for an available worker
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>:read-attribute(name=wait-worker)`

## syntax of the operation to get a `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-resource()`
* To get the list of all the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem use '*' for `<node name>`.

## syntax of the operation to get the `aliases` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `aliases` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `aliases` attribute description: the nodes aliases
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=aliases)`

## syntax of the operation to get the `cache-connections` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `cache-connections` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `cache-connections` attribute description: the number of connections to keep alive indefinitely
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=cache-connections)`

## syntax of the operation to get the `elected` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `elected` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `elected` attribute description: the elected count
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=elected)`

## syntax of the operation to get the `flush-packets` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `flush-packets` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `flush-packets` attribute description: if received data should be immediately flushed
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=flush-packets)`

## syntax of the operation to get the `load` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `load` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `load` attribute description: the current load of this node
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=load)`

## syntax of the operation to get the `load-balancing-group` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `load-balancing-group` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `load-balancing-group` attribute description: the load balancing group this node belongs to
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=load-balancing-group)`

## syntax of the operation to get the `max-connections` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `max-connections` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `max-connections` attribute description: the maximum number of connections per io thread
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=max-connections)`

## syntax of the operation to get the `open-connections` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `open-connections` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `open-connections` attribute description: the current number of open connections
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=open-connections)`

## syntax of the operation to get the `ping` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `ping` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `ping` attribute description: the nodes ping
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=ping)`

## syntax of the operation to get the `queue-new-requests` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `queue-new-requests` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `queue-new-requests` attribute description: if a request is received and there is no worker immediately available should it be queued
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=queue-new-requests)`

## syntax of the operation to get the `read` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `read` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `read` attribute description: the number of bytes read from the node
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=read)`

## syntax of the operation to get the `request-queue-size` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `request-queue-size` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `request-queue-size` attribute description: the size of the request queue
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=request-queue-size)`

## syntax of the operation to get the `status` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `status` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `status` attribute description: the current status of this node
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=status)`

## syntax of the operation to get the `timeout` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `timeout` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `timeout` attribute description: the request timeout
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=timeout)`

## syntax of the operation to get the `ttl` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `ttl` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `ttl` attribute description: the time connections will stay alive with no requests before being closed, if the number of connections is larger than `cache-connections`
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=ttl)`

## syntax of the operation to get the `uri` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `uri` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `uri` attribute description: the uri that the load balancer uses to connect to the node
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=uri)`

## syntax of the operation to get the `written` attribute of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `written` attribute value of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `written` attribute description: the number of bytes transferred to the node
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>:read-attribute(name=written)`

## syntax of the operation to get a `context` resource of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>/context=<context name>:read-resource()`
* To get the list of all the `context` resource of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem use '*' for `<context name>`.

## syntax of the operation to get the `requests` attribute of the `context` resource of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `requests` attribute value of the `context` resource of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `requests` attribute description: the number of requests against this context
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>/context=<context name>:read-attribute(name=requests)`

## syntax of the operation to get the `status` attribute of the `context` resource of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `status` attribute value of the `context` resource of the `node` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `status` attribute description: the status of this context
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/node=<node name>/context=<context name>:read-attribute(name=status)`

## syntax of the operation to get a `load-balancing-group` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=filter/mod-cluster=<mod-cluster name>/balancer=<balancer name>/load-balancing-group=<load-balancing-group name>:read-resource()`
* To get the list of all the `load-balancing-group` resource of the `balancer` resource of the `mod-cluster` resource of the `filter` `configuration` resource of the `undertow` subsystem use '*' for `<load-balancing-group name>`.

## syntax of the operation to get a `rewrite` resource of the `filter` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=filter/rewrite=<rewrite name>:read-resource()`
* To get the list of all the `rewrite` resource of the `filter` `configuration` resource of the `undertow` subsystem use '*' for `<rewrite name>`.

## syntax of the operation to get the `redirect` attribute of the `rewrite` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `redirect` attribute value of the `rewrite` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `redirect` attribute description: if this is true then a redirect will be done instead of a rewrite
* operation: `/subsystem=undertow/configuration=filter/rewrite=<rewrite name>:read-attribute(name=redirect)`

## syntax of the operation to get the `target` attribute of the `rewrite` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `target` attribute value of the `rewrite` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `target` attribute description: the expression that defines the target. if you are redirecting to a constant target put single quotes around the value
* operation: `/subsystem=undertow/configuration=filter/rewrite=<rewrite name>:read-attribute(name=target)`

## syntax of the operation to get a `response-header` resource of the `filter` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=filter/response-header=<response-header name>:read-resource()`
* To get the list of all the `response-header` resource of the `filter` `configuration` resource of the `undertow` subsystem use '*' for `<response-header name>`.

## syntax of the operation to get the `header-name` attribute of the `response-header` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `header-name` attribute value of the `response-header` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `header-name` attribute description: header name
* operation: `/subsystem=undertow/configuration=filter/response-header=<response-header name>:read-attribute(name=header-name)`

## syntax of the operation to get the `header-value` attribute of the `response-header` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `header-value` attribute value of the `response-header` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `header-value` attribute description: value for header
* operation: `/subsystem=undertow/configuration=filter/response-header=<response-header name>:read-attribute(name=header-value)`

## syntax of the operation to get a `request-limit` resource of the `filter` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=filter/request-limit=<request-limit name>:read-resource()`
* To get the list of all the `request-limit` resource of the `filter` `configuration` resource of the `undertow` subsystem use '*' for `<request-limit name>`.

## syntax of the operation to get the `max-concurrent-requests` attribute of the `request-limit` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `max-concurrent-requests` attribute value of the `request-limit` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `max-concurrent-requests` attribute description: maximum number of concurrent requests
* operation: `/subsystem=undertow/configuration=filter/request-limit=<request-limit name>:read-attribute(name=max-concurrent-requests)`

## syntax of the operation to get the `queue-size` attribute of the `request-limit` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `queue-size` attribute value of the `request-limit` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `queue-size` attribute description: number of requests to queue before they start being rejected
* operation: `/subsystem=undertow/configuration=filter/request-limit=<request-limit name>:read-attribute(name=queue-size)`

## syntax of the operation to get a `custom-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=filter/custom-filter=<custom-filter name>:read-resource()`
* To get the list of all the `custom-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem use '*' for `<custom-filter name>`.

## syntax of the operation to get the `class-name` attribute of the `custom-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `class-name` attribute value of the `custom-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `class-name` attribute description: class name of `httphandler`
* operation: `/subsystem=undertow/configuration=filter/custom-filter=<custom-filter name>:read-attribute(name=class-name)`

## syntax of the operation to get the `module` attribute of the `custom-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `module` attribute value of the `custom-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `module` attribute description: module name where class can be loaded from
* operation: `/subsystem=undertow/configuration=filter/custom-filter=<custom-filter name>:read-attribute(name=module)`

## syntax of the operation to get the `parameters` attribute of the `custom-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem
* Retrieve the `parameters` attribute value of the `custom-filter` resource of the `filter` `configuration` resource of the `undertow` subsystem
* `parameters` attribute description: filter parameters
* operation: `/subsystem=undertow/configuration=filter/custom-filter=<custom-filter name>:read-attribute(name=parameters)`

## syntax of the operation to get a `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=handler/file=<file name>:read-resource()`
* To get the list of all the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem use '*' for `<file name>`.

## syntax of the operation to get the `cache-buffer-size` attribute of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `cache-buffer-size` attribute value of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `cache-buffer-size` attribute description: size of the buffers, in bytes.
* operation: `/subsystem=undertow/configuration=handler/file=<file name>:read-attribute(name=cache-buffer-size)`

## syntax of the operation to get the `cache-buffers` attribute of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `cache-buffers` attribute value of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `cache-buffers` attribute description: number of buffers
* operation: `/subsystem=undertow/configuration=handler/file=<file name>:read-attribute(name=cache-buffers)`

## syntax of the operation to get the `case-sensitive` attribute of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `case-sensitive` attribute value of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `case-sensitive` attribute description: use case sensitive file handling
* operation: `/subsystem=undertow/configuration=handler/file=<file name>:read-attribute(name=case-sensitive)`

## syntax of the operation to get the `directory-listing` attribute of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `directory-listing` attribute value of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `directory-listing` attribute description: enable directory listing?
* operation: `/subsystem=undertow/configuration=handler/file=<file name>:read-attribute(name=directory-listing)`

## syntax of the operation to get the `follow-symlink` attribute of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `follow-symlink` attribute value of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `follow-symlink` attribute description: enable following symbolic links
* operation: `/subsystem=undertow/configuration=handler/file=<file name>:read-attribute(name=follow-symlink)`

## syntax of the operation to get the `path` attribute of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `path` attribute value of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `path` attribute description: path on `filesystem` from where file handler will serve resources
* operation: `/subsystem=undertow/configuration=handler/file=<file name>:read-attribute(name=path)`

## syntax of the operation to get the `safe-symlink-paths` attribute of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `safe-symlink-paths` attribute value of the `file` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `safe-symlink-paths` attribute description: paths that are safe to be targets of symbolic links
* operation: `/subsystem=undertow/configuration=handler/file=<file name>:read-attribute(name=safe-symlink-paths)`

## syntax of the operation to get a `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>:read-resource()`
* To get the list of all the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem use '*' for `<reverse-proxy name>`.

## syntax of the operation to get the `cached-connections-per-thread` attribute of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `cached-connections-per-thread` attribute value of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `cached-connections-per-thread` attribute description: the number of connections that will be kept alive indefinitely
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>:read-attribute(name=cached-connections-per-thread)`

## syntax of the operation to get the `connection-idle-timeout` attribute of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `connection-idle-timeout` attribute value of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `connection-idle-timeout` attribute description: the amount of time a connection can be idle before it will be closed. connections will not time out once the pool size is down to the configured minimum (as configured by `cached-connections-per-thread`)
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>:read-attribute(name=connection-idle-timeout)`

## syntax of the operation to get the `connections-per-thread` attribute of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `connections-per-thread` attribute value of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `connections-per-thread` attribute description: the number of connections that will be maintained to `backend` servers, per io thread.
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>:read-attribute(name=connections-per-thread)`

## syntax of the operation to get the `max-request-time` attribute of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `max-request-time` attribute value of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `max-request-time` attribute description: the maximum time that a proxy request can be active for, before being killed
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>:read-attribute(name=max-request-time)`

## syntax of the operation to get the `max-retries` attribute of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `max-retries` attribute value of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `max-retries` attribute description: the number of times to attempt to retry a request if it fails. note that if a request is not considered idempotent then it will only be retried if the proxy can be sure it was not sent to the `backend` server).
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>:read-attribute(name=max-retries)`

## syntax of the operation to get the `problem-server-retry` attribute of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `problem-server-retry` attribute value of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `problem-server-retry` attribute description: time in seconds to wait before attempting to reconnect to a server that is down
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>:read-attribute(name=problem-server-retry)`

## syntax of the operation to get the `request-queue-size` attribute of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `request-queue-size` attribute value of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `request-queue-size` attribute description: the number of requests that can be queued if the connection pool is full before requests are rejected with a `503`
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>:read-attribute(name=request-queue-size)`

## syntax of the operation to get the `reuse-x-forwarded-header` attribute of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `reuse-x-forwarded-header` attribute value of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `reuse-x-forwarded-header` attribute description: `append(reuse` existing) remote hop to `x-forwarded`-* headers, if not, previous value is discarded and remote hop becomes the value.
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>:read-attribute(name=reuse-x-forwarded-header)`

## syntax of the operation to get the `rewrite-host-header` attribute of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `rewrite-host-header` attribute value of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `rewrite-host-header` attribute description: set host header to connection remote end and create `x-forwarded-host` header.
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>:read-attribute(name=rewrite-host-header)`

## syntax of the operation to get the `session-cookie-names` attribute of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `session-cookie-names` attribute value of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `session-cookie-names` attribute description: comma separated list of session cookie names. generally this will just be `jsessionid`.
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>:read-attribute(name=session-cookie-names)`

## syntax of the operation to get a `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>/host=<host name>:read-resource()`
* To get the list of all the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem use '*' for `<host name>`.

## syntax of the operation to get the `enable-http2` attribute of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `enable-http2` attribute value of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `enable-http2` attribute description: if this is true then the proxy will attempt to use `http/2` to connect to the `backend`. if it is not supported it will fall back to `http/1.1`.
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>/host=<host name>:read-attribute(name=enable-http2)`

## syntax of the operation to get the `instance-id` attribute of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `instance-id` attribute value of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `instance-id` attribute description: the instance id (aka `jvm` route) that will be used to enable sticky sessions
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>/host=<host name>:read-attribute(name=instance-id)`

## syntax of the operation to get the `outbound-socket-binding` attribute of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `outbound-socket-binding` attribute value of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `outbound-socket-binding` attribute description: outbound socket binding for this host
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>/host=<host name>:read-attribute(name=outbound-socket-binding)`

## syntax of the operation to get the `path` attribute of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `path` attribute value of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `path` attribute description: optional path if host is using non root resource
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>/host=<host name>:read-attribute(name=path)`

## syntax of the operation to get the `scheme` attribute of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `scheme` attribute value of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `scheme` attribute description: what kind of scheme is used
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>/host=<host name>:read-attribute(name=scheme)`

## syntax of the operation to get the `security-realm` attribute of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `security-realm` attribute value of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `security-realm` attribute description: the security realm that provides the `ssl` configuration for the connection to the host
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>/host=<host name>:read-attribute(name=security-realm)`

## syntax of the operation to get the `ssl-context` attribute of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* Retrieve the `ssl-context` attribute value of the `host` resource of the `reverse-proxy` resource of the `handler` `configuration` resource of the `undertow` subsystem
* `ssl-context` attribute description: reference to the `sslcontext` to be used by this handler.
* operation: `/subsystem=undertow/configuration=handler/reverse-proxy=<reverse-proxy name>/host=<host name>:read-attribute(name=ssl-context)`

## syntax of the operation to get a `application-security-domain` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>:read-resource()`
* To get the list of all the `application-security-domain` resource of the `undertow` subsystem use '*' for `<application-security-domain name>`.

## syntax of the operation to get the `enable-jacc` attribute of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `enable-jacc` attribute value of the `application-security-domain` resource of the `undertow` subsystem
* `enable-jacc` attribute description: enable authorization using jakarta authorization
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>:read-attribute(name=enable-jacc)`

## syntax of the operation to get the `enable-jaspi` attribute of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `enable-jaspi` attribute value of the `application-security-domain` resource of the `undertow` subsystem
* `enable-jaspi` attribute description: enable `jaspi` authentication for the associated deployments.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>:read-attribute(name=enable-jaspi)`

## syntax of the operation to get the `http-authentication-factory` attribute of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `http-authentication-factory` attribute value of the `application-security-domain` resource of the `undertow` subsystem
* `http-authentication-factory` attribute description: the `http` authentication factory to be used by deployments that reference the mapped security domain.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>:read-attribute(name=http-authentication-factory)`

## syntax of the operation to get the `integrated-jaspi` attribute of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `integrated-jaspi` attribute value of the `application-security-domain` resource of the `undertow` subsystem
* `integrated-jaspi` attribute description: whether integrated `jaspi` should be used. if 'false', an ad hoc identity will be created instead.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>:read-attribute(name=integrated-jaspi)`

## syntax of the operation to get the `override-deployment-config` attribute of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `override-deployment-config` attribute value of the `application-security-domain` resource of the `undertow` subsystem
* `override-deployment-config` attribute description: should the authentication configuration in the deployment be overridden by the factory.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>:read-attribute(name=override-deployment-config)`

## syntax of the operation to get the `referencing-deployments` attribute of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `referencing-deployments` attribute value of the `application-security-domain` resource of the `undertow` subsystem
* `referencing-deployments` attribute description: the deployments currently referencing this mapping.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>:read-attribute(name=referencing-deployments)`

## syntax of the operation to get the `security-domain` attribute of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `security-domain` attribute value of the `application-security-domain` resource of the `undertow` subsystem
* `security-domain` attribute description: the `securitydomain` to be used by deployments that reference the mapped security domain.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>:read-attribute(name=security-domain)`

## syntax of the operation to get the `client-ssl-context` attribute of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `client-ssl-context` attribute value of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* `client-ssl-context` attribute description: reference to the `ssl` context used to secure `back-channel` logout connection.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>/setting=single-sign-on:read-attribute(name=client-ssl-context)`

## syntax of the operation to get the `cookie-name` attribute of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `cookie-name` attribute value of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* `cookie-name` attribute description: name of the cookie
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>/setting=single-sign-on:read-attribute(name=cookie-name)`

## syntax of the operation to get the `credential-reference` attribute of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `credential-reference` attribute value of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* `credential-reference` attribute description: the credential reference to decrypt the private key entry.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>/setting=single-sign-on:read-attribute(name=credential-reference)`

## syntax of the operation to get the `domain` attribute of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `domain` attribute value of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* `domain` attribute description: the cookie domain that will be used.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>/setting=single-sign-on:read-attribute(name=domain)`

## syntax of the operation to get the `http-only` attribute of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `http-only` attribute value of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* `http-only` attribute description: set cookie `httponly` attribute.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>/setting=single-sign-on:read-attribute(name=http-only)`

## syntax of the operation to get the `key-alias` attribute of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `key-alias` attribute value of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* `key-alias` attribute description: alias of the private key entry used for signing and verifying `back-channel` logout connection.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>/setting=single-sign-on:read-attribute(name=key-alias)`

## syntax of the operation to get the `key-store` attribute of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `key-store` attribute value of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* `key-store` attribute description: reference to key store containing a private key entry.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>/setting=single-sign-on:read-attribute(name=key-store)`

## syntax of the operation to get the `path` attribute of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `path` attribute value of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* `path` attribute description: cookie path.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>/setting=single-sign-on:read-attribute(name=path)`

## syntax of the operation to get the `secure` attribute of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* Retrieve the `secure` attribute value of the `single-sign-on` `setting` resource of the `application-security-domain` resource of the `undertow` subsystem
* `secure` attribute description: set cookie secure attribute.
* operation: `/subsystem=undertow/application-security-domain=<application-security-domain name>/setting=single-sign-on:read-attribute(name=secure)`

## syntax of the operation to get a `server` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/server=<server name>:read-resource()`
* To get the list of all the `server` resource of the `undertow` subsystem use '*' for `<server name>`.

## syntax of the operation to get the `default-host` attribute of the `server` resource of the `undertow` subsystem
* Retrieve the `default-host` attribute value of the `server` resource of the `undertow` subsystem
* `default-host` attribute description: the servers default virtual host
* operation: `/subsystem=undertow/server=<server name>:read-attribute(name=default-host)`

## syntax of the operation to get the `servlet-container` attribute of the `server` resource of the `undertow` subsystem
* Retrieve the `servlet-container` attribute value of the `server` resource of the `undertow` subsystem
* `servlet-container` attribute description: the servers default `servlet` container
* operation: `/subsystem=undertow/server=<server name>:read-attribute(name=servlet-container)`

## syntax of the operation to get a `host` resource of the `server` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/server=<server name>/host=<host name>:read-resource()`
* To get the list of all the `host` resource of the `server` resource of the `undertow` subsystem use '*' for `<host name>`.

## syntax of the operation to get the `alias` attribute of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `alias` attribute value of the `host` resource of the `server` resource of the `undertow` subsystem
* `alias` attribute description: aliases for the host
* operation: `/subsystem=undertow/server=<server name>/host=<host name>:read-attribute(name=alias)`

## syntax of the operation to get the `default-response-code` attribute of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `default-response-code` attribute value of the `host` resource of the `server` resource of the `undertow` subsystem
* `default-response-code` attribute description: if set, this will be response code sent back in case requested context does not exist on server.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>:read-attribute(name=default-response-code)`

## syntax of the operation to get the `default-web-module` attribute of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `default-web-module` attribute value of the `host` resource of the `server` resource of the `undertow` subsystem
* `default-web-module` attribute description: default web module
* operation: `/subsystem=undertow/server=<server name>/host=<host name>:read-attribute(name=default-web-module)`

## syntax of the operation to get the `disable-console-redirect` attribute of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `disable-console-redirect` attribute value of the `host` resource of the `server` resource of the `undertow` subsystem
* `disable-console-redirect` attribute description: if set to true, /console redirect wont be enabled for this host, default is false
* operation: `/subsystem=undertow/server=<server name>/host=<host name>:read-attribute(name=disable-console-redirect)`

## syntax of the operation to get the `queue-requests-on-start` attribute of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `queue-requests-on-start` attribute value of the `host` resource of the `server` resource of the `undertow` subsystem
* `queue-requests-on-start` attribute description: if requests should be queued on start for this host. if this is set to false the default response code will be returned instead.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>:read-attribute(name=queue-requests-on-start)`

## syntax of the operation to get a `location` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/location=<location name>:read-resource()`
* To get the list of all the `location` resource of the `host` resource of the `server` resource of the `undertow` subsystem use '*' for `<location name>`.

## syntax of the operation to get the `handler` attribute of the `location` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `handler` attribute value of the `location` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `handler` attribute description: default handler for this location
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/location=<location name>:read-attribute(name=handler)`

## syntax of the operation to get a `filter-ref` resource of the `location` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/location=<location name>/filter-ref=<filter-ref name>:read-resource()`
* To get the list of all the `filter-ref` resource of the `location` resource of the `host` resource of the `server` resource of the `undertow` subsystem use '*' for `<filter-ref name>`.

## syntax of the operation to get the `predicate` attribute of the `filter-ref` resource of the `location` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `predicate` attribute value of the `filter-ref` resource of the `location` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `predicate` attribute description: predicates provide a simple way of making a `true/false` decision  based on an exchange. many handlers have a requirement that they be applied conditionally, and predicates provide a general way to specify a condition.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/location=<location name>/filter-ref=<filter-ref name>:read-attribute(name=predicate)`

## syntax of the operation to get the `priority` attribute of the `filter-ref` resource of the `location` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `priority` attribute value of the `filter-ref` resource of the `location` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `priority` attribute description: defines filter order. a lower number instructs the server to be included earlier in the handler chain than others with higher numbers. values range from `1`, indicating the filter will be handled first, to `2147483647`, resulting in the filter being handled last.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/location=<location name>/filter-ref=<filter-ref name>:read-attribute(name=priority)`

## syntax of the operation to get the `directory` attribute of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `directory` attribute value of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `directory` attribute description: directory in which to save logs
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=access-log:read-attribute(name=directory)`

## syntax of the operation to get the `extended` attribute of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `extended` attribute value of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `extended` attribute description: if the log uses the extended log file format
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=access-log:read-attribute(name=extended)`

## syntax of the operation to get the `pattern` attribute of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `pattern` attribute value of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `pattern` attribute description: the access log pattern.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=access-log:read-attribute(name=pattern)`

## syntax of the operation to get the `predicate` attribute of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `predicate` attribute value of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `predicate` attribute description: predicate that determines if the request should be logged
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=access-log:read-attribute(name=predicate)`

## syntax of the operation to get the `prefix` attribute of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `prefix` attribute value of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `prefix` attribute description: prefix for the log file name.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=access-log:read-attribute(name=prefix)`

## syntax of the operation to get the `relative-to` attribute of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `relative-to` attribute value of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `relative-to` attribute description: the directory the path is relative to
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=access-log:read-attribute(name=relative-to)`

## syntax of the operation to get the `rotate` attribute of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `rotate` attribute value of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `rotate` attribute description: rotate the access log every day.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=access-log:read-attribute(name=rotate)`

## syntax of the operation to get the `suffix` attribute of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `suffix` attribute value of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `suffix` attribute description: suffix for the log file name.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=access-log:read-attribute(name=suffix)`

## syntax of the operation to get the `use-server-log` attribute of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `use-server-log` attribute value of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `use-server-log` attribute description: if the log should be written to the server log, rather than a separate file.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=access-log:read-attribute(name=use-server-log)`

## syntax of the operation to get the `worker` attribute of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `worker` attribute value of the `access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `worker` attribute description: name of the worker to use for logging
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=access-log:read-attribute(name=worker)`

## syntax of the operation to get the `attributes` attribute of the `console-access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `attributes` attribute value of the `console-access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `attributes` attribute description: the attributes to be included in the structured output.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=console-access-log:read-attribute(name=attributes)`

## syntax of the operation to get the `include-host-name` attribute of the `console-access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `include-host-name` attribute value of the `console-access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `include-host-name` attribute description: indicates whether or not the host name should included in the `json` structured output. if set to true the key will be `hostname` in the structured data and the value will be the host this `console-access-log` belongs to.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=console-access-log:read-attribute(name=include-host-name)`

## syntax of the operation to get the `metadata` attribute of the `console-access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `metadata` attribute value of the `console-access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `metadata` attribute description: any additional `metadata` to add to the `json` structured output.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=console-access-log:read-attribute(name=metadata)`

## syntax of the operation to get the `predicate` attribute of the `console-access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `predicate` attribute value of the `console-access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `predicate` attribute description: predicate that determines if the request should be logged.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=console-access-log:read-attribute(name=predicate)`

## syntax of the operation to get the `worker` attribute of the `console-access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `worker` attribute value of the `console-access-log` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `worker` attribute description: name of the worker to use for logging.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=console-access-log:read-attribute(name=worker)`

## syntax of the operation to get the `cookie-name` attribute of the `single-sign-on` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `cookie-name` attribute value of the `single-sign-on` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `cookie-name` attribute description: name of the cookie
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=single-sign-on:read-attribute(name=cookie-name)`

## syntax of the operation to get the `domain` attribute of the `single-sign-on` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `domain` attribute value of the `single-sign-on` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `domain` attribute description: the cookie domain that will be used.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=single-sign-on:read-attribute(name=domain)`

## syntax of the operation to get the `http-only` attribute of the `single-sign-on` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http-only` attribute value of the `single-sign-on` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `http-only` attribute description: set cookie `httponly` attribute.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=single-sign-on:read-attribute(name=http-only)`

## syntax of the operation to get the `path` attribute of the `single-sign-on` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `path` attribute value of the `single-sign-on` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `path` attribute description: cookie path.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=single-sign-on:read-attribute(name=path)`

## syntax of the operation to get the `secure` attribute of the `single-sign-on` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `secure` attribute value of the `single-sign-on` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `secure` attribute description: set cookie secure attribute.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=single-sign-on:read-attribute(name=secure)`

## syntax of the operation to get the `http-authentication-factory` attribute of the `http-invoker` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http-authentication-factory` attribute value of the `http-invoker` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `http-authentication-factory` attribute description: the `http` authentication factory to use for authentication
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=http-invoker:read-attribute(name=http-authentication-factory)`

## syntax of the operation to get the `path` attribute of the `http-invoker` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `path` attribute value of the `http-invoker` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `path` attribute description: the path that the services are installed under
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=http-invoker:read-attribute(name=path)`

## syntax of the operation to get the `security-realm` attribute of the `http-invoker` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `security-realm` attribute value of the `http-invoker` `setting` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `security-realm` attribute description: the legacy security realm to use for authentication
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/setting=http-invoker:read-attribute(name=security-realm)`

## syntax of the operation to get a `filter-ref` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/filter-ref=<filter-ref name>:read-resource()`
* To get the list of all the `filter-ref` resource of the `host` resource of the `server` resource of the `undertow` subsystem use '*' for `<filter-ref name>`.

## syntax of the operation to get the `predicate` attribute of the `filter-ref` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `predicate` attribute value of the `filter-ref` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `predicate` attribute description: predicates provide a simple way of making a `true/false` decision  based on an exchange. many handlers have a requirement that they be applied conditionally, and predicates provide a general way to specify a condition.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/filter-ref=<filter-ref name>:read-attribute(name=predicate)`

## syntax of the operation to get the `priority` attribute of the `filter-ref` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `priority` attribute value of the `filter-ref` resource of the `host` resource of the `server` resource of the `undertow` subsystem
* `priority` attribute description: defines filter order. a lower number instructs the server to be included earlier in the handler chain than others with higher numbers. values range from `1`, indicating the filter will be handled first, to `2147483647`, resulting in the filter being handled last.
* operation: `/subsystem=undertow/server=<server name>/host=<host name>/filter-ref=<filter-ref name>:read-attribute(name=priority)`

## syntax of the operation to get a `http-listener` resource of the `server` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-resource()`
* To get the list of all the `http-listener` resource of the `server` resource of the `undertow` subsystem use '*' for `<http-listener name>`.

## syntax of the operation to get the `allow-encoded-slash` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `allow-encoded-slash` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `allow-encoded-slash` attribute description: if a request comes in with encoded / characters (`i.e`. %`2f`), will these be decoded.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=allow-encoded-slash)`

## syntax of the operation to get the `allow-equals-in-cookie-value` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `allow-equals-in-cookie-value` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `allow-equals-in-cookie-value` attribute description: if this is true then undertow will allow `non-escaped` equals characters in unquoted cookie values. unquoted cookie values may not contain equals characters. if present the value ends before the equals sign. the remainder of the cookie value will be dropped.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=allow-equals-in-cookie-value)`

## syntax of the operation to get the `allow-unescaped-characters-in-url` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `allow-unescaped-characters-in-url` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `allow-unescaped-characters-in-url` attribute description: if this is true undertow will accept `non-encoded` characters that are disallowed by the uri specification. this defaults to false, and in general should not be needed as most clients correctly encode characters. note that setting this to true can be considered a security risk, as allowing `non-standard` characters can allow request smuggling attacks in some circumstances.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=allow-unescaped-characters-in-url)`

## syntax of the operation to get the `always-set-keep-alive` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `always-set-keep-alive` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `always-set-keep-alive` attribute description: if this is true then a connection: `keep-alive` header will be added to responses, even when it is not strictly required by the specification.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=always-set-keep-alive)`

## syntax of the operation to get the `buffer-pipelined-data` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `buffer-pipelined-data` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `buffer-pipelined-data` attribute description: if we should buffer pipelined requests.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=buffer-pipelined-data)`

## syntax of the operation to get the `buffer-pool` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `buffer-pool` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `buffer-pool` attribute description: the listeners buffer pool
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=buffer-pool)`

## syntax of the operation to get the `bytes-received` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `bytes-received` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `bytes-received` attribute description: the number of bytes that have been received by this listener
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=bytes-received)`

## syntax of the operation to get the `bytes-sent` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `bytes-sent` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `bytes-sent` attribute description: the number of bytes that have been sent out on this listener
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=bytes-sent)`

## syntax of the operation to get the `certificate-forwarding` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `certificate-forwarding` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `certificate-forwarding` attribute description: if certificate forwarding should be enabled. if this is enabled then the listener will take the certificate from the `ssl_client_cert` attribute. this should only be enabled if behind a proxy, and the proxy is configured to always set these headers.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=certificate-forwarding)`

## syntax of the operation to get the `decode-url` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `decode-url` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `decode-url` attribute description: if this is true then the parser will decode the `url` and query parameters using the selected character encoding (`utf-8` by default). if this is false they will not be decoded. this will allow a later handler to decode them into whatever `charset` is desired.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=decode-url)`

## syntax of the operation to get the `disallowed-methods` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `disallowed-methods` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `disallowed-methods` attribute description: a comma separated list of `http` methods that are not allowed
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=disallowed-methods)`

## syntax of the operation to get the `enable-http2` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `enable-http2` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `enable-http2` attribute description: enables `http2` support for this listener
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=enable-http2)`

## syntax of the operation to get the `enabled` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `enabled` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `enabled` attribute description: if the listener is enabled
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=enabled)`

## syntax of the operation to get the `error-count` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `error-count` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `error-count` attribute description: the number of `500` responses that have been sent by this listener
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=error-count)`

## syntax of the operation to get the `http2-enable-push` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http2-enable-push` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `http2-enable-push` attribute description: if server push is enabled for this connection
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=http2-enable-push)`

## syntax of the operation to get the `http2-header-table-size` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http2-header-table-size` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `http2-header-table-size` attribute description: the size of the header table used for `hpack` compression, in bytes. this amount of memory will be allocated per connection for compression. larger values use more memory but may give better compression.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=http2-header-table-size)`

## syntax of the operation to get the `http2-initial-window-size` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http2-initial-window-size` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `http2-initial-window-size` attribute description: the flow control window size that controls how quickly the client can send data to the server
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=http2-initial-window-size)`

## syntax of the operation to get the `http2-max-concurrent-streams` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http2-max-concurrent-streams` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `http2-max-concurrent-streams` attribute description: the maximum number of `http/2` streams that can be active at any time on a single connection
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=http2-max-concurrent-streams)`

## syntax of the operation to get the `http2-max-frame-size` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http2-max-frame-size` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `http2-max-frame-size` attribute description: the max `http/2` frame size
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=http2-max-frame-size)`

## syntax of the operation to get the `http2-max-header-list-size` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http2-max-header-list-size` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `http2-max-header-list-size` attribute description: the maximum size of request headers the server is prepared to accept
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=http2-max-header-list-size)`

## syntax of the operation to get the `max-buffered-request-size` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-buffered-request-size` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `max-buffered-request-size` attribute description: maximum size of a buffered request, in bytes. requests are not usually buffered, the most common case is when performing `ssl` renegotiation for a post request, and the post data must be fully buffered in order to perform the renegotiation.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=max-buffered-request-size)`

## syntax of the operation to get the `max-connections` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-connections` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `max-connections` attribute description: the maximum number of concurrent connections. only values greater than `0` are allowed. for unlimited connections simply undefine this attribute value.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=max-connections)`

## syntax of the operation to get the `max-cookies` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-cookies` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `max-cookies` attribute description: the maximum number of cookies that will be parsed. this is used to protect against hash vulnerabilities.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=max-cookies)`

## syntax of the operation to get the `max-header-size` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-header-size` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `max-header-size` attribute description: the maximum size of a `http` request header, in bytes.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=max-header-size)`

## syntax of the operation to get the `max-headers` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-headers` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `max-headers` attribute description: the maximum number of headers that will be parsed. this is used to protect against hash vulnerabilities.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=max-headers)`

## syntax of the operation to get the `max-parameters` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-parameters` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `max-parameters` attribute description: the maximum number of parameters that will be parsed. this is used to protect against hash vulnerabilities. this applies to both query parameters, and to post data, but is not cumulative (`i.e`. you can potentially have max parameters * `2` total parameters).
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=max-parameters)`

## syntax of the operation to get the `max-post-size` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-post-size` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `max-post-size` attribute description: the maximum size of a post that will be accepted, in bytes.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=max-post-size)`

## syntax of the operation to get the `max-processing-time` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-processing-time` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `max-processing-time` attribute description: the maximum processing time taken by a request on this listener
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=max-processing-time)`

## syntax of the operation to get the `no-request-timeout` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `no-request-timeout` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `no-request-timeout` attribute description: the length of time in milliseconds that the connection can be idle before it is closed by the container.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=no-request-timeout)`

## syntax of the operation to get the `processing-time` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `processing-time` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `processing-time` attribute description: the total processing time of all requests handed by this listener
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=processing-time)`

## syntax of the operation to get the `proxy-address-forwarding` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `proxy-address-forwarding` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `proxy-address-forwarding` attribute description: enables  handling of `x-forwarded-host` header (and other `x-forwarded`-* headers) and use this header information to set the remote address. this should only be used behind a trusted proxy that sets these headers otherwise a remote user can spoof their `ip` address.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=proxy-address-forwarding)`

## syntax of the operation to get the `proxy-protocol` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `proxy-protocol` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `proxy-protocol` attribute description: if this is true then the listener will use the proxy protocol `v1`, as defined by `https://www.haproxy.org/download/1.8/doc/proxy-protocol.txt`. this option must only be enabled for listeners that are behind a load balancer that supports the same protocol.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=proxy-protocol)`

## syntax of the operation to get the `read-timeout` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `read-timeout` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `read-timeout` attribute description: configure a read timeout for a socket, in milliseconds.  if the given amount of time elapses without a successful read taking place, the `socket's` next read will throw a {@link `readtimeoutexception`}.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=read-timeout)`

## syntax of the operation to get the `receive-buffer` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `receive-buffer` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `receive-buffer` attribute description: the receive buffer size, in bytes.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=receive-buffer)`

## syntax of the operation to get the `record-request-start-time` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `record-request-start-time` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `record-request-start-time` attribute description: if this is true then undertow will record the request start time, to allow for request time to be logged. this has a small but measurable performance impact
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=record-request-start-time)`

## syntax of the operation to get the `redirect-socket` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `redirect-socket` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `redirect-socket` attribute description: if this listener is supporting `non-ssl` requests, and a request is received for which a matching <`security-constraint`> requires `ssl` transport, undertow will automatically redirect the request to the socket binding port specified here.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=redirect-socket)`

## syntax of the operation to get the `request-count` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `request-count` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `request-count` attribute description: the number of requests this listener has served
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=request-count)`

## syntax of the operation to get the `request-parse-timeout` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `request-parse-timeout` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `request-parse-timeout` attribute description: the maximum amount of time (in milliseconds) that can be spent parsing the request
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=request-parse-timeout)`

## syntax of the operation to get the `require-host-http11` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `require-host-http11` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `require-host-http11` attribute description: require that all `http/1.1` requests have a 'host' header, as per the `rfc`. if the request does not include this header it will be rejected with a `403`.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=require-host-http11)`

## syntax of the operation to get the `resolve-peer-address` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `resolve-peer-address` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `resolve-peer-address` attribute description: enables host `dns` lookup
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=resolve-peer-address)`

## syntax of the operation to get the `rfc6265-cookie-validation` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `rfc6265-cookie-validation` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `rfc6265-cookie-validation` attribute description: if cookies should be validated to ensure they comply with `rfc6265`.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=rfc6265-cookie-validation)`

## syntax of the operation to get the `secure` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `secure` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `secure` attribute description: if this is true then requests that originate from this listener are marked as secure, even if the request is not using `https`.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=secure)`

## syntax of the operation to get the `send-buffer` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `send-buffer` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `send-buffer` attribute description: the send buffer size, in bytes.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=send-buffer)`

## syntax of the operation to get the `socket-binding` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `socket-binding` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `socket-binding` attribute description: the listener socket binding
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=socket-binding)`

## syntax of the operation to get the `tcp-backlog` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `tcp-backlog` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `tcp-backlog` attribute description: configure a server with the specified backlog.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=tcp-backlog)`

## syntax of the operation to get the `tcp-keep-alive` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `tcp-keep-alive` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `tcp-keep-alive` attribute description: configure a channel to send `tcp` `keep-alive` messages in an `implementation-dependent` manner.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=tcp-keep-alive)`

## syntax of the operation to get the `url-charset` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `url-charset` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `url-charset` attribute description: `url` `charset`
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=url-charset)`

## syntax of the operation to get the `worker` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `worker` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `worker` attribute description: the listeners `xnio` worker
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=worker)`

## syntax of the operation to get the `write-timeout` attribute of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `write-timeout` attribute value of the `http-listener` resource of the `server` resource of the `undertow` subsystem
* `write-timeout` attribute description: configure a write timeout for a socket, in milliseconds.  if the given amount of time elapses without a successful write taking place, the `socket's` next write will throw a {@link `writetimeoutexception`}.
* operation: `/subsystem=undertow/server=<server name>/http-listener=<http-listener name>:read-attribute(name=write-timeout)`

## syntax of the operation to get a `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-resource()`
* To get the list of all the `ajp-listener` resource of the `server` resource of the `undertow` subsystem use '*' for `<ajp-listener name>`.

## syntax of the operation to get the `allow-encoded-slash` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `allow-encoded-slash` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `allow-encoded-slash` attribute description: if a request comes in with encoded / characters (`i.e`. %`2f`), will these be decoded.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=allow-encoded-slash)`

## syntax of the operation to get the `allow-equals-in-cookie-value` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `allow-equals-in-cookie-value` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `allow-equals-in-cookie-value` attribute description: if this is true then undertow will allow `non-escaped` equals characters in unquoted cookie values. unquoted cookie values may not contain equals characters. if present the value ends before the equals sign. the remainder of the cookie value will be dropped.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=allow-equals-in-cookie-value)`

## syntax of the operation to get the `allow-unescaped-characters-in-url` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `allow-unescaped-characters-in-url` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `allow-unescaped-characters-in-url` attribute description: if this is true undertow will accept `non-encoded` characters that are disallowed by the uri specification. this defaults to false, and in general should not be needed as most clients correctly encode characters. note that setting this to true can be considered a security risk, as allowing `non-standard` characters can allow request smuggling attacks in some circumstances.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=allow-unescaped-characters-in-url)`

## syntax of the operation to get the `allowed-request-attributes-pattern` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `allowed-request-attributes-pattern` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `allowed-request-attributes-pattern` attribute description: `pattern(regex`) which specifies allowed custom `ajp` request attributes.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=allowed-request-attributes-pattern)`

## syntax of the operation to get the `always-set-keep-alive` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `always-set-keep-alive` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `always-set-keep-alive` attribute description: if this is true then a connection: `keep-alive` header will be added to responses, even when it is not strictly required by the specification.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=always-set-keep-alive)`

## syntax of the operation to get the `buffer-pipelined-data` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `buffer-pipelined-data` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `buffer-pipelined-data` attribute description: if we should buffer pipelined requests.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=buffer-pipelined-data)`

## syntax of the operation to get the `buffer-pool` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `buffer-pool` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `buffer-pool` attribute description: the listeners buffer pool
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=buffer-pool)`

## syntax of the operation to get the `bytes-received` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `bytes-received` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `bytes-received` attribute description: the number of bytes that have been received by this listener
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=bytes-received)`

## syntax of the operation to get the `bytes-sent` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `bytes-sent` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `bytes-sent` attribute description: the number of bytes that have been sent out on this listener
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=bytes-sent)`

## syntax of the operation to get the `decode-url` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `decode-url` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `decode-url` attribute description: if this is true then the parser will decode the `url` and query parameters using the selected character encoding (`utf-8` by default). if this is false they will not be decoded. this will allow a later handler to decode them into whatever `charset` is desired.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=decode-url)`

## syntax of the operation to get the `disallowed-methods` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `disallowed-methods` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `disallowed-methods` attribute description: a comma separated list of `http` methods that are not allowed
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=disallowed-methods)`

## syntax of the operation to get the `enabled` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `enabled` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `enabled` attribute description: if the listener is enabled
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=enabled)`

## syntax of the operation to get the `error-count` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `error-count` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `error-count` attribute description: the number of `500` responses that have been sent by this listener
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=error-count)`

## syntax of the operation to get the `max-ajp-packet-size` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-ajp-packet-size` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `max-ajp-packet-size` attribute description: the maximum supported size of `ajp` packets. if this is modified it has to be increased on the load balancer and the `backend` server.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=max-ajp-packet-size)`

## syntax of the operation to get the `max-buffered-request-size` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-buffered-request-size` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `max-buffered-request-size` attribute description: maximum size of a buffered request, in bytes. requests are not usually buffered, the most common case is when performing `ssl` renegotiation for a post request, and the post data must be fully buffered in order to perform the renegotiation.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=max-buffered-request-size)`

## syntax of the operation to get the `max-connections` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-connections` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `max-connections` attribute description: the maximum number of concurrent connections. only values greater than `0` are allowed. for unlimited connections simply undefine this attribute value.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=max-connections)`

## syntax of the operation to get the `max-cookies` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-cookies` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `max-cookies` attribute description: the maximum number of cookies that will be parsed. this is used to protect against hash vulnerabilities.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=max-cookies)`

## syntax of the operation to get the `max-header-size` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-header-size` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `max-header-size` attribute description: the maximum size of a `http` request header, in bytes.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=max-header-size)`

## syntax of the operation to get the `max-headers` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-headers` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `max-headers` attribute description: the maximum number of headers that will be parsed. this is used to protect against hash vulnerabilities.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=max-headers)`

## syntax of the operation to get the `max-parameters` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-parameters` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `max-parameters` attribute description: the maximum number of parameters that will be parsed. this is used to protect against hash vulnerabilities. this applies to both query parameters, and to post data, but is not cumulative (`i.e`. you can potentially have max parameters * `2` total parameters).
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=max-parameters)`

## syntax of the operation to get the `max-post-size` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-post-size` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `max-post-size` attribute description: the maximum size of a post that will be accepted, in bytes.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=max-post-size)`

## syntax of the operation to get the `max-processing-time` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-processing-time` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `max-processing-time` attribute description: the maximum processing time taken by a request on this listener
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=max-processing-time)`

## syntax of the operation to get the `no-request-timeout` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `no-request-timeout` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `no-request-timeout` attribute description: the length of time in milliseconds that the connection can be idle before it is closed by the container.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=no-request-timeout)`

## syntax of the operation to get the `processing-time` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `processing-time` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `processing-time` attribute description: the total processing time of all requests handed by this listener
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=processing-time)`

## syntax of the operation to get the `read-timeout` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `read-timeout` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `read-timeout` attribute description: configure a read timeout for a socket, in milliseconds.  if the given amount of time elapses without a successful read taking place, the `socket's` next read will throw a {@link `readtimeoutexception`}.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=read-timeout)`

## syntax of the operation to get the `receive-buffer` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `receive-buffer` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `receive-buffer` attribute description: the receive buffer size, in bytes.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=receive-buffer)`

## syntax of the operation to get the `record-request-start-time` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `record-request-start-time` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `record-request-start-time` attribute description: if this is true then undertow will record the request start time, to allow for request time to be logged. this has a small but measurable performance impact
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=record-request-start-time)`

## syntax of the operation to get the `redirect-socket` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `redirect-socket` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `redirect-socket` attribute description: if this listener is supporting `non-ssl` requests, and a request is received for which a matching <`security-constraint`> requires `ssl` transport, undertow will automatically redirect the request to the socket binding port specified here.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=redirect-socket)`

## syntax of the operation to get the `request-count` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `request-count` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `request-count` attribute description: the number of requests this listener has served
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=request-count)`

## syntax of the operation to get the `request-parse-timeout` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `request-parse-timeout` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `request-parse-timeout` attribute description: the maximum amount of time (in milliseconds) that can be spent parsing the request
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=request-parse-timeout)`

## syntax of the operation to get the `resolve-peer-address` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `resolve-peer-address` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `resolve-peer-address` attribute description: enables host `dns` lookup
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=resolve-peer-address)`

## syntax of the operation to get the `rfc6265-cookie-validation` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `rfc6265-cookie-validation` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `rfc6265-cookie-validation` attribute description: if cookies should be validated to ensure they comply with `rfc6265`.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=rfc6265-cookie-validation)`

## syntax of the operation to get the `scheme` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `scheme` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `scheme` attribute description: the listener scheme, can be `http` or `https`. by default the scheme will be taken from the incoming `ajp` request.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=scheme)`

## syntax of the operation to get the `secure` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `secure` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `secure` attribute description: if this is true then requests that originate from this listener are marked as secure, even if the request is not using `https`.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=secure)`

## syntax of the operation to get the `send-buffer` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `send-buffer` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `send-buffer` attribute description: the send buffer size, in bytes.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=send-buffer)`

## syntax of the operation to get the `socket-binding` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `socket-binding` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `socket-binding` attribute description: the listener socket binding
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=socket-binding)`

## syntax of the operation to get the `tcp-backlog` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `tcp-backlog` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `tcp-backlog` attribute description: configure a server with the specified backlog.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=tcp-backlog)`

## syntax of the operation to get the `tcp-keep-alive` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `tcp-keep-alive` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `tcp-keep-alive` attribute description: configure a channel to send `tcp` `keep-alive` messages in an `implementation-dependent` manner.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=tcp-keep-alive)`

## syntax of the operation to get the `url-charset` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `url-charset` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `url-charset` attribute description: `url` `charset`
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=url-charset)`

## syntax of the operation to get the `worker` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `worker` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `worker` attribute description: the listeners `xnio` worker
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=worker)`

## syntax of the operation to get the `write-timeout` attribute of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `write-timeout` attribute value of the `ajp-listener` resource of the `server` resource of the `undertow` subsystem
* `write-timeout` attribute description: configure a write timeout for a socket, in milliseconds.  if the given amount of time elapses without a successful write taking place, the `socket's` next write will throw a {@link `writetimeoutexception`}.
* operation: `/subsystem=undertow/server=<server name>/ajp-listener=<ajp-listener name>:read-attribute(name=write-timeout)`

## syntax of the operation to get a `https-listener` resource of the `server` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-resource()`
* To get the list of all the `https-listener` resource of the `server` resource of the `undertow` subsystem use '*' for `<https-listener name>`.

## syntax of the operation to get the `allow-encoded-slash` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `allow-encoded-slash` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `allow-encoded-slash` attribute description: if a request comes in with encoded / characters (`i.e`. %`2f`), will these be decoded.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=allow-encoded-slash)`

## syntax of the operation to get the `allow-equals-in-cookie-value` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `allow-equals-in-cookie-value` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `allow-equals-in-cookie-value` attribute description: if this is true then undertow will allow `non-escaped` equals characters in unquoted cookie values. unquoted cookie values may not contain equals characters. if present the value ends before the equals sign. the remainder of the cookie value will be dropped.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=allow-equals-in-cookie-value)`

## syntax of the operation to get the `allow-unescaped-characters-in-url` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `allow-unescaped-characters-in-url` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `allow-unescaped-characters-in-url` attribute description: if this is true undertow will accept `non-encoded` characters that are disallowed by the uri specification. this defaults to false, and in general should not be needed as most clients correctly encode characters. note that setting this to true can be considered a security risk, as allowing `non-standard` characters can allow request smuggling attacks in some circumstances.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=allow-unescaped-characters-in-url)`

## syntax of the operation to get the `always-set-keep-alive` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `always-set-keep-alive` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `always-set-keep-alive` attribute description: if this is true then a connection: `keep-alive` header will be added to responses, even when it is not strictly required by the specification.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=always-set-keep-alive)`

## syntax of the operation to get the `buffer-pipelined-data` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `buffer-pipelined-data` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `buffer-pipelined-data` attribute description: if we should buffer pipelined requests.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=buffer-pipelined-data)`

## syntax of the operation to get the `buffer-pool` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `buffer-pool` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `buffer-pool` attribute description: the listeners buffer pool
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=buffer-pool)`

## syntax of the operation to get the `bytes-received` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `bytes-received` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `bytes-received` attribute description: the number of bytes that have been received by this listener
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=bytes-received)`

## syntax of the operation to get the `bytes-sent` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `bytes-sent` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `bytes-sent` attribute description: the number of bytes that have been sent out on this listener
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=bytes-sent)`

## syntax of the operation to get the `certificate-forwarding` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `certificate-forwarding` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `certificate-forwarding` attribute description: if certificate forwarding should be enabled. if this is enabled then the listener will take the certificate from the `ssl_client_cert` attribute. this should only be enabled if behind a proxy, and the proxy is configured to always set these headers.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=certificate-forwarding)`

## syntax of the operation to get the `decode-url` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `decode-url` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `decode-url` attribute description: if this is true then the parser will decode the `url` and query parameters using the selected character encoding (`utf-8` by default). if this is false they will not be decoded. this will allow a later handler to decode them into whatever `charset` is desired.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=decode-url)`

## syntax of the operation to get the `disallowed-methods` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `disallowed-methods` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `disallowed-methods` attribute description: a comma separated list of `http` methods that are not allowed
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=disallowed-methods)`

## syntax of the operation to get the `enable-http2` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `enable-http2` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `enable-http2` attribute description: enables `http2` support for this listener
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=enable-http2)`

## syntax of the operation to get the `enable-spdy` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `enable-spdy` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `enable-spdy` attribute description: enables `spdy` support for this listener. this has been deprecated and has no effect, `http/2` should be used instead
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=enable-spdy)`

## syntax of the operation to get the `enabled` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `enabled` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `enabled` attribute description: if the listener is enabled
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=enabled)`

## syntax of the operation to get the `enabled-cipher-suites` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `enabled-cipher-suites` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `enabled-cipher-suites` attribute description: configures enabled `ssl` ciphers
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=enabled-cipher-suites)`

## syntax of the operation to get the `enabled-protocols` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `enabled-protocols` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `enabled-protocols` attribute description: configures `ssl` protocols
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=enabled-protocols)`

## syntax of the operation to get the `error-count` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `error-count` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `error-count` attribute description: the number of `500` responses that have been sent by this listener
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=error-count)`

## syntax of the operation to get the `http2-enable-push` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http2-enable-push` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `http2-enable-push` attribute description: if server push is enabled for this connection
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=http2-enable-push)`

## syntax of the operation to get the `http2-header-table-size` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http2-header-table-size` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `http2-header-table-size` attribute description: the size of the header table used for `hpack` compression, in bytes. this amount of memory will be allocated per connection for compression. larger values use more memory but may give better compression.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=http2-header-table-size)`

## syntax of the operation to get the `http2-initial-window-size` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http2-initial-window-size` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `http2-initial-window-size` attribute description: the flow control window size that controls how quickly the client can send data to the server
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=http2-initial-window-size)`

## syntax of the operation to get the `http2-max-concurrent-streams` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http2-max-concurrent-streams` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `http2-max-concurrent-streams` attribute description: the maximum number of `http/2` streams that can be active at any time on a single connection
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=http2-max-concurrent-streams)`

## syntax of the operation to get the `http2-max-frame-size` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http2-max-frame-size` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `http2-max-frame-size` attribute description: the max `http/2` frame size
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=http2-max-frame-size)`

## syntax of the operation to get the `http2-max-header-list-size` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `http2-max-header-list-size` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `http2-max-header-list-size` attribute description: the maximum size of request headers the server is prepared to accept
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=http2-max-header-list-size)`

## syntax of the operation to get the `max-buffered-request-size` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-buffered-request-size` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `max-buffered-request-size` attribute description: maximum size of a buffered request, in bytes. requests are not usually buffered, the most common case is when performing `ssl` renegotiation for a post request, and the post data must be fully buffered in order to perform the renegotiation.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=max-buffered-request-size)`

## syntax of the operation to get the `max-connections` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-connections` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `max-connections` attribute description: the maximum number of concurrent connections. only values greater than `0` are allowed. for unlimited connections simply undefine this attribute value.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=max-connections)`

## syntax of the operation to get the `max-cookies` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-cookies` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `max-cookies` attribute description: the maximum number of cookies that will be parsed. this is used to protect against hash vulnerabilities.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=max-cookies)`

## syntax of the operation to get the `max-header-size` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-header-size` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `max-header-size` attribute description: the maximum size of a `http` request header, in bytes.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=max-header-size)`

## syntax of the operation to get the `max-headers` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-headers` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `max-headers` attribute description: the maximum number of headers that will be parsed. this is used to protect against hash vulnerabilities.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=max-headers)`

## syntax of the operation to get the `max-parameters` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-parameters` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `max-parameters` attribute description: the maximum number of parameters that will be parsed. this is used to protect against hash vulnerabilities. this applies to both query parameters, and to post data, but is not cumulative (`i.e`. you can potentially have max parameters * `2` total parameters).
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=max-parameters)`

## syntax of the operation to get the `max-post-size` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-post-size` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `max-post-size` attribute description: the maximum size of a post that will be accepted, in bytes.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=max-post-size)`

## syntax of the operation to get the `max-processing-time` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `max-processing-time` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `max-processing-time` attribute description: the maximum processing time taken by a request on this listener
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=max-processing-time)`

## syntax of the operation to get the `no-request-timeout` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `no-request-timeout` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `no-request-timeout` attribute description: the length of time in milliseconds that the connection can be idle before it is closed by the container.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=no-request-timeout)`

## syntax of the operation to get the `processing-time` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `processing-time` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `processing-time` attribute description: the total processing time of all requests handed by this listener
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=processing-time)`

## syntax of the operation to get the `proxy-address-forwarding` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `proxy-address-forwarding` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `proxy-address-forwarding` attribute description: enables  handling of `x-forwarded-host` header (and other `x-forwarded`-* headers) and use this header information to set the remote address. this should only be used behind a trusted proxy that sets these headers otherwise a remote user can spoof their `ip` address.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=proxy-address-forwarding)`

## syntax of the operation to get the `proxy-protocol` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `proxy-protocol` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `proxy-protocol` attribute description: if this is true then the listener will use the proxy protocol `v1`, as defined by `https://www.haproxy.org/download/1.8/doc/proxy-protocol.txt`. this option must only be enabled for listeners that are behind a load balancer that supports the same protocol.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=proxy-protocol)`

## syntax of the operation to get the `read-timeout` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `read-timeout` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `read-timeout` attribute description: configure a read timeout for a socket, in milliseconds.  if the given amount of time elapses without a successful read taking place, the `socket's` next read will throw a {@link `readtimeoutexception`}.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=read-timeout)`

## syntax of the operation to get the `receive-buffer` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `receive-buffer` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `receive-buffer` attribute description: the receive buffer size, in bytes.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=receive-buffer)`

## syntax of the operation to get the `record-request-start-time` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `record-request-start-time` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `record-request-start-time` attribute description: if this is true then undertow will record the request start time, to allow for request time to be logged. this has a small but measurable performance impact
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=record-request-start-time)`

## syntax of the operation to get the `request-count` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `request-count` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `request-count` attribute description: the number of requests this listener has served
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=request-count)`

## syntax of the operation to get the `request-parse-timeout` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `request-parse-timeout` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `request-parse-timeout` attribute description: the maximum amount of time (in milliseconds) that can be spent parsing the request
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=request-parse-timeout)`

## syntax of the operation to get the `require-host-http11` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `require-host-http11` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `require-host-http11` attribute description: require that all `http/1.1` requests have a 'host' header, as per the `rfc`. if the request does not include this header it will be rejected with a `403`.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=require-host-http11)`

## syntax of the operation to get the `resolve-peer-address` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `resolve-peer-address` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `resolve-peer-address` attribute description: enables host `dns` lookup
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=resolve-peer-address)`

## syntax of the operation to get the `rfc6265-cookie-validation` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `rfc6265-cookie-validation` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `rfc6265-cookie-validation` attribute description: if cookies should be validated to ensure they comply with `rfc6265`.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=rfc6265-cookie-validation)`

## syntax of the operation to get the `secure` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `secure` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `secure` attribute description: if this is true then requests that originate from this listener are marked as secure, even if the request is not using `https`.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=secure)`

## syntax of the operation to get the `security-realm` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `security-realm` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `security-realm` attribute description: the listeners security realm
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=security-realm)`

## syntax of the operation to get the `send-buffer` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `send-buffer` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `send-buffer` attribute description: the send buffer size, in bytes.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=send-buffer)`

## syntax of the operation to get the `socket-binding` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `socket-binding` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `socket-binding` attribute description: the listener socket binding
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=socket-binding)`

## syntax of the operation to get the `ssl-context` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `ssl-context` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `ssl-context` attribute description: reference to the `sslcontext` to be used by this listener.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=ssl-context)`

## syntax of the operation to get the `ssl-session-cache-size` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `ssl-session-cache-size` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `ssl-session-cache-size` attribute description: the maximum number of active `ssl` sessions
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=ssl-session-cache-size)`

## syntax of the operation to get the `ssl-session-timeout` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `ssl-session-timeout` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `ssl-session-timeout` attribute description: the timeout for `ssl` sessions, in seconds
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=ssl-session-timeout)`

## syntax of the operation to get the `tcp-backlog` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `tcp-backlog` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `tcp-backlog` attribute description: configure a server with the specified backlog.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=tcp-backlog)`

## syntax of the operation to get the `tcp-keep-alive` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `tcp-keep-alive` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `tcp-keep-alive` attribute description: configure a channel to send `tcp` `keep-alive` messages in an `implementation-dependent` manner.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=tcp-keep-alive)`

## syntax of the operation to get the `url-charset` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `url-charset` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `url-charset` attribute description: `url` `charset`
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=url-charset)`

## syntax of the operation to get the `verify-client` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `verify-client` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `verify-client` attribute description: the desired `ssl` client authentication mode for `ssl` channels
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=verify-client)`

## syntax of the operation to get the `worker` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `worker` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `worker` attribute description: the listeners `xnio` worker
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=worker)`

## syntax of the operation to get the `write-timeout` attribute of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* Retrieve the `write-timeout` attribute value of the `https-listener` resource of the `server` resource of the `undertow` subsystem
* `write-timeout` attribute description: configure a write timeout for a socket, in milliseconds.  if the given amount of time elapses without a successful write taking place, the `socket's` next write will throw a {@link `writetimeoutexception`}.
* operation: `/subsystem=undertow/server=<server name>/https-listener=<https-listener name>:read-attribute(name=write-timeout)`

## syntax of the operation to get a `buffer-cache` resource of the `undertow` subsystem
* operation: `/subsystem=undertow/buffer-cache=<buffer-cache name>:read-resource()`
* To get the list of all the `buffer-cache` resource of the `undertow` subsystem use '*' for `<buffer-cache name>`.

## syntax of the operation to get the `buffer-size` attribute of the `buffer-cache` resource of the `undertow` subsystem
* Retrieve the `buffer-size` attribute value of the `buffer-cache` resource of the `undertow` subsystem
* `buffer-size` attribute description: the size of an individual buffer, in bytes.
* operation: `/subsystem=undertow/buffer-cache=<buffer-cache name>:read-attribute(name=buffer-size)`

## syntax of the operation to get the `buffers-per-region` attribute of the `buffer-cache` resource of the `undertow` subsystem
* Retrieve the `buffers-per-region` attribute value of the `buffer-cache` resource of the `undertow` subsystem
* `buffers-per-region` attribute description: the numbers of buffers in a region
* operation: `/subsystem=undertow/buffer-cache=<buffer-cache name>:read-attribute(name=buffers-per-region)`

## syntax of the operation to get the `max-regions` attribute of the `buffer-cache` resource of the `undertow` subsystem
* Retrieve the `max-regions` attribute value of the `buffer-cache` resource of the `undertow` subsystem
* `max-regions` attribute description: the maximum number of regions
* operation: `/subsystem=undertow/buffer-cache=<buffer-cache name>:read-attribute(name=max-regions)`

## syntax of the operation to get the `maximum-permissions` attribute of the `default` `deployment-permissions` resource of the `security-manager` subsystem
* Retrieve the `maximum-permissions` attribute value of the `default` `deployment-permissions` resource of the `security-manager` subsystem
* `maximum-permissions` attribute description: the maximum set of permissions that can be granted to a deployment or jars.
* operation: `/subsystem=security-manager/deployment-permissions=default:read-attribute(name=maximum-permissions)`

## syntax of the operation to get the `minimum-permissions` attribute of the `default` `deployment-permissions` resource of the `security-manager` subsystem
* Retrieve the `minimum-permissions` attribute value of the `default` `deployment-permissions` resource of the `security-manager` subsystem
* `minimum-permissions` attribute description: the minimum set of permissions to be granted to a deployment or jars.
* operation: `/subsystem=security-manager/deployment-permissions=default:read-attribute(name=minimum-permissions)`

## syntax of the operation to get the `installed-drivers` attribute of the `datasources` subsystem
* Retrieve the `installed-drivers` attribute value of the `datasources` subsystem
* `installed-drivers` attribute description: list of `jdbc` drivers that have been installed in the runtime
* operation: `/subsystem=datasources:read-attribute(name=installed-drivers)`

## syntax of the operation to get a `jdbc-driver` resource of the `datasources` subsystem
* operation: `/subsystem=datasources/jdbc-driver=<jdbc-driver name>:read-resource()`
* To get the list of all the `jdbc-driver` resource of the `datasources` subsystem use '*' for `<jdbc-driver name>`.

## syntax of the operation to get the `datasource-class-info` attribute of the `jdbc-driver` resource of the `datasources` subsystem
* Retrieve the `datasource-class-info` attribute value of the `jdbc-driver` resource of the `datasources` subsystem
* `datasource-class-info` attribute description: the available properties for the `datasource-class`, and `xa-datasource-class` for the `jdbc-driver`
* operation: `/subsystem=datasources/jdbc-driver=<jdbc-driver name>:read-attribute(name=datasource-class-info)`

## syntax of the operation to get the `deployment-name` attribute of the `jdbc-driver` resource of the `datasources` subsystem
* Retrieve the `deployment-name` attribute value of the `jdbc-driver` resource of the `datasources` subsystem
* `deployment-name` attribute description: the name of the deployment unit from which the driver was loaded
* operation: `/subsystem=datasources/jdbc-driver=<jdbc-driver name>:read-attribute(name=deployment-name)`

## syntax of the operation to get the `driver-class-name` attribute of the `jdbc-driver` resource of the `datasources` subsystem
* Retrieve the `driver-class-name` attribute value of the `jdbc-driver` resource of the `datasources` subsystem
* `driver-class-name` attribute description: the fully qualified class name of the `java.sql.driver` implementation
* operation: `/subsystem=datasources/jdbc-driver=<jdbc-driver name>:read-attribute(name=driver-class-name)`

## syntax of the operation to get the `driver-datasource-class-name` attribute of the `jdbc-driver` resource of the `datasources` subsystem
* Retrieve the `driver-datasource-class-name` attribute value of the `jdbc-driver` resource of the `datasources` subsystem
* `driver-datasource-class-name` attribute description: the fully qualified class name of the `javax.sql.datasource` implementation
* operation: `/subsystem=datasources/jdbc-driver=<jdbc-driver name>:read-attribute(name=driver-datasource-class-name)`

## syntax of the operation to get the `driver-major-version` attribute of the `jdbc-driver` resource of the `datasources` subsystem
* Retrieve the `driver-major-version` attribute value of the `jdbc-driver` resource of the `datasources` subsystem
* `driver-major-version` attribute description: the `driver's` major version number
* operation: `/subsystem=datasources/jdbc-driver=<jdbc-driver name>:read-attribute(name=driver-major-version)`

## syntax of the operation to get the `driver-minor-version` attribute of the `jdbc-driver` resource of the `datasources` subsystem
* Retrieve the `driver-minor-version` attribute value of the `jdbc-driver` resource of the `datasources` subsystem
* `driver-minor-version` attribute description: the `driver's` minor version number
* operation: `/subsystem=datasources/jdbc-driver=<jdbc-driver name>:read-attribute(name=driver-minor-version)`

## syntax of the operation to get the `driver-module-name` attribute of the `jdbc-driver` resource of the `datasources` subsystem
* Retrieve the `driver-module-name` attribute value of the `jdbc-driver` resource of the `datasources` subsystem
* `driver-module-name` attribute description: the name of the module from which the driver was loaded, if it was loaded from the module path
* operation: `/subsystem=datasources/jdbc-driver=<jdbc-driver name>:read-attribute(name=driver-module-name)`

## syntax of the operation to get the `driver-name` attribute of the `jdbc-driver` resource of the `datasources` subsystem
* Retrieve the `driver-name` attribute value of the `jdbc-driver` resource of the `datasources` subsystem
* `driver-name` attribute description: defines the `jdbc` driver the `datasource` should use. it is a symbolic name matching the the name of installed driver. in case the driver is deployed as a jar, the name is the name of the deployment unit
* operation: `/subsystem=datasources/jdbc-driver=<jdbc-driver name>:read-attribute(name=driver-name)`

## syntax of the operation to get the `driver-xa-datasource-class-name` attribute of the `jdbc-driver` resource of the `datasources` subsystem
* Retrieve the `driver-xa-datasource-class-name` attribute value of the `jdbc-driver` resource of the `datasources` subsystem
* `driver-xa-datasource-class-name` attribute description: the fully qualified class name of the `javax.sql.xadatasource` implementation
* operation: `/subsystem=datasources/jdbc-driver=<jdbc-driver name>:read-attribute(name=driver-xa-datasource-class-name)`

## syntax of the operation to get the `jdbc-compliant` attribute of the `jdbc-driver` resource of the `datasources` subsystem
* Retrieve the `jdbc-compliant` attribute value of the `jdbc-driver` resource of the `datasources` subsystem
* `jdbc-compliant` attribute description: whether or not the driver is `jdbc` compliant
* operation: `/subsystem=datasources/jdbc-driver=<jdbc-driver name>:read-attribute(name=jdbc-compliant)`

## syntax of the operation to get the `module-slot` attribute of the `jdbc-driver` resource of the `datasources` subsystem
* Retrieve the `module-slot` attribute value of the `jdbc-driver` resource of the `datasources` subsystem
* `module-slot` attribute description: the slot of the module from which the driver was loaded, if it was loaded from the module path
* operation: `/subsystem=datasources/jdbc-driver=<jdbc-driver name>:read-attribute(name=module-slot)`

## syntax of the operation to get the `profile` attribute of the `jdbc-driver` resource of the `datasources` subsystem
* Retrieve the `profile` attribute value of the `jdbc-driver` resource of the `datasources` subsystem
* `profile` attribute description: domain profile in which driver is defined. null in case of `standalone` server
* operation: `/subsystem=datasources/jdbc-driver=<jdbc-driver name>:read-attribute(name=profile)`

## syntax of the operation to get a `data-source` resource of the `datasources` subsystem
* operation: `/subsystem=datasources/data-source=<data-source name>:read-resource()`
* To get the list of all the `data-source` resource of the `datasources` subsystem use '*' for `<data-source name>`.

## syntax of the operation to get the `allocation-retry` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `allocation-retry` attribute value of the `data-source` resource of the `datasources` subsystem
* `allocation-retry` attribute description: the allocation retry element indicates the number of times that allocating a connection should be tried before throwing an exception
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=allocation-retry)`

## syntax of the operation to get the `allocation-retry-wait-millis` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `allocation-retry-wait-millis` attribute value of the `data-source` resource of the `datasources` subsystem
* `allocation-retry-wait-millis` attribute description: the allocation retry wait `millis` element specifies the amount of time, in milliseconds, to wait between retrying to allocate a connection
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=allocation-retry-wait-millis)`

## syntax of the operation to get the `allow-multiple-users` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `allow-multiple-users` attribute value of the `data-source` resource of the `datasources` subsystem
* `allow-multiple-users` attribute description: specifies if multiple users will access the `datasource` through the `getconnection(user`, password) method and hence if the internal pool type should account for that
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=allow-multiple-users)`

## syntax of the operation to get the `authentication-context` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `authentication-context` attribute value of the `data-source` resource of the `datasources` subsystem
* `authentication-context` attribute description: the elytron authentication context which defines the `javax.security.auth.subject` that is used to distinguish connections in the pool.
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=authentication-context)`

## syntax of the operation to get the `background-validation` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `background-validation` attribute value of the `data-source` resource of the `datasources` subsystem
* `background-validation` attribute description: an element to specify that connections should be validated on a background thread versus being validated prior to use. changing this value can be done only on disabled `datasource`,  requires a server restart otherwise.
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=background-validation)`

## syntax of the operation to get the `background-validation-millis` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `background-validation-millis` attribute value of the `data-source` resource of the `datasources` subsystem
* `background-validation-millis` attribute description: the `background-validation-millis` element specifies the amount of time, in milliseconds, that background validation will run. changing this value can be done only on disabled `datasource`,  requires a server restart otherwise
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=background-validation-millis)`

## syntax of the operation to get the `blocking-timeout-wait-millis` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `blocking-timeout-wait-millis` attribute value of the `data-source` resource of the `datasources` subsystem
* `blocking-timeout-wait-millis` attribute description: the `blocking-timeout-millis` element specifies the maximum time, in milliseconds, to block while waiting for a connection before throwing an exception. note that this blocks only while waiting for locking a connection, and will never throw an exception if creating a new connection takes an inordinately long time
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=blocking-timeout-wait-millis)`

## syntax of the operation to get the `capacity-decrementer-class` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `capacity-decrementer-class` attribute value of the `data-source` resource of the `datasources` subsystem
* `capacity-decrementer-class` attribute description: class defining the policy for decrementing connections in the pool
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=capacity-decrementer-class)`

## syntax of the operation to get the `capacity-decrementer-properties` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `capacity-decrementer-properties` attribute value of the `data-source` resource of the `datasources` subsystem
* `capacity-decrementer-properties` attribute description: properties to be injected in class defining the policy for decrementing connections in the pool
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=capacity-decrementer-properties)`

## syntax of the operation to get the `capacity-incrementer-class` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `capacity-incrementer-class` attribute value of the `data-source` resource of the `datasources` subsystem
* `capacity-incrementer-class` attribute description: class defining the policy for incrementing connections in the pool
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=capacity-incrementer-class)`

## syntax of the operation to get the `capacity-incrementer-properties` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `capacity-incrementer-properties` attribute value of the `data-source` resource of the `datasources` subsystem
* `capacity-incrementer-properties` attribute description: properties to be injected in class defining the policy for incrementing connections in the pool
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=capacity-incrementer-properties)`

## syntax of the operation to get the `check-valid-connection-sql` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `check-valid-connection-sql` attribute value of the `data-source` resource of the `datasources` subsystem
* `check-valid-connection-sql` attribute description: specify an `sql` statement to check validity of a pool connection. this may be called when managed connection is obtained from the pool
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=check-valid-connection-sql)`

## syntax of the operation to get the `connectable` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `connectable` attribute value of the `data-source` resource of the `datasources` subsystem
* `connectable` attribute description: enable the use of `cmr`. this feature means that a local resource can reliably participate in an `xa` transaction.
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=connectable)`

## syntax of the operation to get the `connection-listener-class` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `connection-listener-class` attribute value of the `data-source` resource of the `datasources` subsystem
* `connection-listener-class` attribute description: `speciefies` class name extending `org.jboss.jca.adapters.jdbc.spi.listener.connectionlistener` that provides a possible to listen for connection activation and passivation in order to perform actions before the connection is returned to the application or returned to the pool.
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=connection-listener-class)`

## syntax of the operation to get the `connection-listener-property` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `connection-listener-property` attribute value of the `data-source` resource of the `datasources` subsystem
* `connection-listener-property` attribute description: properties to be injected in class `specidied` in `connection-listener-class`
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=connection-listener-property)`

## syntax of the operation to get the `connection-url` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `connection-url` attribute value of the `data-source` resource of the `datasources` subsystem
* `connection-url` attribute description: the `jdbc` driver connection `url`
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=connection-url)`

## syntax of the operation to get the `credential-reference` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `credential-reference` attribute value of the `data-source` resource of the `datasources` subsystem
* `credential-reference` attribute description: credential (from credential store) to authenticate on data source
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=credential-reference)`

## syntax of the operation to get the `datasource-class` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `datasource-class` attribute value of the `data-source` resource of the `datasources` subsystem
* `datasource-class` attribute description: the fully qualified name of the `jdbc` `datasource` class
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=datasource-class)`

## syntax of the operation to get the `driver-class` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `driver-class` attribute value of the `data-source` resource of the `datasources` subsystem
* `driver-class` attribute description: the fully qualified name of the `jdbc` driver class
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=driver-class)`

## syntax of the operation to get the `driver-name` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `driver-name` attribute value of the `data-source` resource of the `datasources` subsystem
* `driver-name` attribute description: defines the `jdbc` driver the `datasource` should use. it is a symbolic name matching the the name of installed driver. in case the driver is deployed as a jar, the name is the name of the deployment unit
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=driver-name)`

## syntax of the operation to get the `elytron-enabled` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `elytron-enabled` attribute value of the `data-source` resource of the `datasources` subsystem
* `elytron-enabled` attribute description: enables elytron security for handling authentication of connections. the elytron `authentication-context` to be used will be current context if no context is specified (see `authentication-context`).
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=elytron-enabled)`

## syntax of the operation to get the `enabled` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `enabled` attribute value of the `data-source` resource of the `datasources` subsystem
* `enabled` attribute description: specifies if the `datasource` should be enabled. note this attribute will not be supported runtime in next versions.
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=enabled)`

## syntax of the operation to get the `enlistment-trace` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `enlistment-trace` attribute value of the `data-source` resource of the `datasources` subsystem
* `enlistment-trace` attribute description: defines if `wildfly/ironjacamar` should record enlistment traces
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=enlistment-trace)`

## syntax of the operation to get the `exception-sorter-class-name` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `exception-sorter-class-name` attribute value of the `data-source` resource of the `datasources` subsystem
* `exception-sorter-class-name` attribute description: an `org.jboss.jca.adapters.jdbc.exceptionsorter` that provides an `isexceptionfatal(sqlexception`) method to validate if an exception should broadcast an error
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=exception-sorter-class-name)`

## syntax of the operation to get the `exception-sorter-module` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `exception-sorter-module` attribute value of the `data-source` resource of the `datasources` subsystem
* `exception-sorter-module` attribute description: the name of the module which makes the implementation of `org.jboss.jca.adapters.jdbc.exceptionsorter` available
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=exception-sorter-module)`

## syntax of the operation to get the `exception-sorter-properties` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `exception-sorter-properties` attribute value of the `data-source` resource of the `datasources` subsystem
* `exception-sorter-properties` attribute description: the exception sorter properties
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=exception-sorter-properties)`

## syntax of the operation to get the `flush-strategy` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `flush-strategy` attribute value of the `data-source` resource of the `datasources` subsystem
* `flush-strategy` attribute description: specifies how the pool should be flush in case of an error.
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=flush-strategy)`

## syntax of the operation to get the `idle-timeout-minutes` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `idle-timeout-minutes` attribute value of the `data-source` resource of the `datasources` subsystem
* `idle-timeout-minutes` attribute description: the `idle-timeout-minutes` elements specifies the maximum time, in minutes, a connection may be idle before being closed. the actual maximum time depends also on the `idleremover` scan time, which is half of the smallest `idle-timeout-minutes` value of any pool. changing this value can be done only on disabled `datasource`, requires a server restart otherwise.
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=idle-timeout-minutes)`

## syntax of the operation to get the `initial-pool-size` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `initial-pool-size` attribute value of the `data-source` resource of the `datasources` subsystem
* `initial-pool-size` attribute description: the `initial-pool-size` element indicates the initial number of connections a pool should hold.
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=initial-pool-size)`

## syntax of the operation to get the `jndi-name` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `jndi-name` attribute value of the `data-source` resource of the `datasources` subsystem
* `jndi-name` attribute description: specifies the `jndi` name for the `datasource`
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=jndi-name)`

## syntax of the operation to get the `jta` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `jta` attribute value of the `data-source` resource of the `datasources` subsystem
* `jta` attribute description: enable jakarta transactions integration
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=jta)`

## syntax of the operation to get the `max-pool-size` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `max-pool-size` attribute value of the `data-source` resource of the `datasources` subsystem
* `max-pool-size` attribute description: the `max-pool-size` element specifies the maximum number of connections for a pool. no more connections will be created in each `sub-pool`
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=max-pool-size)`

## syntax of the operation to get the `mcp` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `mcp` attribute value of the `data-source` resource of the `datasources` subsystem
* `mcp` attribute description: defines the `managedconnectionpool` implementation, `f.ex`. `org.jboss.jca.core.connectionmanager.pool.mcp.semaphorearraylistmanagedconnectionpool`
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=mcp)`

## syntax of the operation to get the `min-pool-size` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `min-pool-size` attribute value of the `data-source` resource of the `datasources` subsystem
* `min-pool-size` attribute description: the `min-pool-size` element specifies the minimum number of connections for a pool
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=min-pool-size)`

## syntax of the operation to get the `new-connection-sql` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `new-connection-sql` attribute value of the `data-source` resource of the `datasources` subsystem
* `new-connection-sql` attribute description: specifies an `sql` statement to execute whenever a connection is added to the connection pool
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=new-connection-sql)`

## syntax of the operation to get the `password` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `password` attribute value of the `data-source` resource of the `datasources` subsystem
* `password` attribute description: specifies the password used when creating a new connection
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=password)`

## syntax of the operation to get the `pool-fair` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `pool-fair` attribute value of the `data-source` resource of the `datasources` subsystem
* `pool-fair` attribute description: defines if pool use should be fair
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=pool-fair)`

## syntax of the operation to get the `pool-prefill` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `pool-prefill` attribute value of the `data-source` resource of the `datasources` subsystem
* `pool-prefill` attribute description: should the pool be `prefilled`. changing this value can be done only on disabled `datasource`, requires a server restart otherwise.
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=pool-prefill)`

## syntax of the operation to get the `pool-use-strict-min` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `pool-use-strict-min` attribute value of the `data-source` resource of the `datasources` subsystem
* `pool-use-strict-min` attribute description: specifies if the `min-pool-size` should be considered strictly
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=pool-use-strict-min)`

## syntax of the operation to get the `prepared-statements-cache-size` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `prepared-statements-cache-size` attribute value of the `data-source` resource of the `datasources` subsystem
* `prepared-statements-cache-size` attribute description: the number of prepared statements per connection in an `lru` cache
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=prepared-statements-cache-size)`

## syntax of the operation to get the `query-timeout` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `query-timeout` attribute value of the `data-source` resource of the `datasources` subsystem
* `query-timeout` attribute description: any configured query timeout in seconds. if not provided no timeout will be set
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=query-timeout)`

## syntax of the operation to get the `reauth-plugin-class-name` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `reauth-plugin-class-name` attribute value of the `data-source` resource of the `datasources` subsystem
* `reauth-plugin-class-name` attribute description: the fully qualified class name of the reauthentication `plugin` implementation
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=reauth-plugin-class-name)`

## syntax of the operation to get the `reauth-plugin-properties` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `reauth-plugin-properties` attribute value of the `data-source` resource of the `datasources` subsystem
* `reauth-plugin-properties` attribute description: the properties for the reauthentication `plugin`
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=reauth-plugin-properties)`

## syntax of the operation to get the `security-domain` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `security-domain` attribute value of the `data-source` resource of the `datasources` subsystem
* `security-domain` attribute description: specifies the `picketbox` security domain which defines the `picketbox` `javax.security.auth.subject` that are used to distinguish connections in the pool
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=security-domain)`

## syntax of the operation to get the `set-tx-query-timeout` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `set-tx-query-timeout` attribute value of the `data-source` resource of the `datasources` subsystem
* `set-tx-query-timeout` attribute description: whether to set the query timeout based on the time remaining until transaction timeout. any configured query timeout will be used if there is no transaction
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=set-tx-query-timeout)`

## syntax of the operation to get the `share-prepared-statements` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `share-prepared-statements` attribute value of the `data-source` resource of the `datasources` subsystem
* `share-prepared-statements` attribute description: whether to share prepared statements, `i.e`. whether asking for same statement twice without closing uses the same underlying prepared statement
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=share-prepared-statements)`

## syntax of the operation to get the `spy` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `spy` attribute value of the `data-source` resource of the `datasources` subsystem
* `spy` attribute description: enable spying of `sql` statements
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=spy)`

## syntax of the operation to get the `stale-connection-checker-class-name` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `stale-connection-checker-class-name` attribute value of the `data-source` resource of the `datasources` subsystem
* `stale-connection-checker-class-name` attribute description: an `org.jboss.jca.adapters.jdbc.staleconnectionchecker` that provides an `isstaleconnection(sqlexception`) method which if it returns true will wrap the exception in an `org.jboss.jca.adapters.jdbc.staleconnectionexception`
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=stale-connection-checker-class-name)`

## syntax of the operation to get the `stale-connection-checker-module` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `stale-connection-checker-module` attribute value of the `data-source` resource of the `datasources` subsystem
* `stale-connection-checker-module` attribute description: the name of the module which makes the implementation of `org.jboss.jca.adapters.jdbc.staleconnectionchecker` available
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=stale-connection-checker-module)`

## syntax of the operation to get the `stale-connection-checker-properties` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `stale-connection-checker-properties` attribute value of the `data-source` resource of the `datasources` subsystem
* `stale-connection-checker-properties` attribute description: the stale connection checker properties
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=stale-connection-checker-properties)`

## syntax of the operation to get the `statistics-enabled` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `statistics-enabled` attribute value of the `data-source` resource of the `datasources` subsystem
* `statistics-enabled` attribute description: define whether runtime statistics are enabled or not.
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `track-statements` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `track-statements` attribute value of the `data-source` resource of the `datasources` subsystem
* `track-statements` attribute description: whether to check for unclosed statements when a connection is returned to the pool, result sets are closed, a statement is closed or return to the prepared statement cache. valid values are: "false" - do not track statements, "true" - track statements and result sets and warn when they are not closed, "`nowarn`" - track statements but do not warn about them being unclosed
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=track-statements)`

## syntax of the operation to get the `tracking` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `tracking` attribute value of the `data-source` resource of the `datasources` subsystem
* `tracking` attribute description: defines if `ironjacamar` should track connection handles across transaction boundaries
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=tracking)`

## syntax of the operation to get the `transaction-isolation` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `transaction-isolation` attribute value of the `data-source` resource of the `datasources` subsystem
* `transaction-isolation` attribute description: set the `java.sql.connection` transaction isolation level. valid values are: `transaction_read_uncommitted`, `transaction_read_committed`, `transaction_repeatable_read`, `transaction_serializable` and `transaction_none`. different values are used to set `customlevel` using `transactionisolation#customlevel`
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=transaction-isolation)`

## syntax of the operation to get the `url-delimiter` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `url-delimiter` attribute value of the `data-source` resource of the `datasources` subsystem
* `url-delimiter` attribute description: specifies the delimiter for `urls` in `connection-url` for ha `datasources`
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=url-delimiter)`

## syntax of the operation to get the `url-selector-strategy-class-name` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `url-selector-strategy-class-name` attribute value of the `data-source` resource of the `datasources` subsystem
* `url-selector-strategy-class-name` attribute description: a class that implements `org.jboss.jca.adapters.jdbc.urlselectorstrategy`
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=url-selector-strategy-class-name)`

## syntax of the operation to get the `use-ccm` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `use-ccm` attribute value of the `data-source` resource of the `datasources` subsystem
* `use-ccm` attribute description: enable the use of a cached connection manager
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=use-ccm)`

## syntax of the operation to get the `use-fast-fail` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `use-fast-fail` attribute value of the `data-source` resource of the `datasources` subsystem
* `use-fast-fail` attribute description: whether to fail a connection allocation on the first try if it is invalid (true) or keep trying until the pool is exhausted of all potential connections (false)
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=use-fast-fail)`

## syntax of the operation to get the `use-java-context` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `use-java-context` attribute value of the `data-source` resource of the `datasources` subsystem
* `use-java-context` attribute description: setting this to false will bind the `datasource` into global `jndi`
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=use-java-context)`

## syntax of the operation to get the `use-try-lock` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `use-try-lock` attribute value of the `data-source` resource of the `datasources` subsystem
* `use-try-lock` attribute description: any configured timeout for internal locks on the resource adapter objects in seconds
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=use-try-lock)`

## syntax of the operation to get the `user-name` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `user-name` attribute value of the `data-source` resource of the `datasources` subsystem
* `user-name` attribute description: specify the user name used when creating a new connection
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=user-name)`

## syntax of the operation to get the `valid-connection-checker-class-name` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `valid-connection-checker-class-name` attribute value of the `data-source` resource of the `datasources` subsystem
* `valid-connection-checker-class-name` attribute description: an `org.jboss.jca.adapters.jdbc.validconnectionchecker` that provides an `isvalidconnection(connection`) method to validate a connection. if an exception is returned that means the connection is invalid. this overrides the `check-valid-connection-sql` element
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=valid-connection-checker-class-name)`

## syntax of the operation to get the `valid-connection-checker-module` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `valid-connection-checker-module` attribute value of the `data-source` resource of the `datasources` subsystem
* `valid-connection-checker-module` attribute description: the name of the module which makes the implementation of `org.jboss.jca.adapters.jdbc.validconnectionchecker` available
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=valid-connection-checker-module)`

## syntax of the operation to get the `valid-connection-checker-properties` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `valid-connection-checker-properties` attribute value of the `data-source` resource of the `datasources` subsystem
* `valid-connection-checker-properties` attribute description: the valid connection checker properties
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=valid-connection-checker-properties)`

## syntax of the operation to get the `validate-on-match` attribute of the `data-source` resource of the `datasources` subsystem
* Retrieve the `validate-on-match` attribute value of the `data-source` resource of the `datasources` subsystem
* `validate-on-match` attribute description: the `validate-on-match` element specifies if connection validation should be done when a connection factory attempts to match a managed connection. this is typically exclusive to the use of background validation
* operation: `/subsystem=datasources/data-source=<data-source name>:read-attribute(name=validate-on-match)`

## syntax of the operation to get a `connection-properties` resource of the `data-source` resource of the `datasources` subsystem
* operation: `/subsystem=datasources/data-source=<data-source name>/connection-properties=<connection-properties name>:read-resource()`
* To get the list of all the `connection-properties` resource of the `data-source` resource of the `datasources` subsystem use '*' for `<connection-properties name>`.

## syntax of the operation to get the `value` attribute of the `connection-properties` resource of the `data-source` resource of the `datasources` subsystem
* Retrieve the `value` attribute value of the `connection-properties` resource of the `data-source` resource of the `datasources` subsystem
* `value` attribute description: each `connection-property` specifies a string `name/value` pair with the property name coming from the name attribute and the value coming from the element content
* operation: `/subsystem=datasources/data-source=<data-source name>/connection-properties=<connection-properties name>:read-attribute(name=value)`

## syntax of the operation to get a `xa-data-source` resource of the `datasources` subsystem
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-resource()`
* To get the list of all the `xa-data-source` resource of the `datasources` subsystem use '*' for `<xa-data-source name>`.

## syntax of the operation to get the `allocation-retry` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `allocation-retry` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `allocation-retry` attribute description: the allocation retry element indicates the number of times that allocating a connection should be tried before throwing an exception
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=allocation-retry)`

## syntax of the operation to get the `allocation-retry-wait-millis` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `allocation-retry-wait-millis` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `allocation-retry-wait-millis` attribute description: the allocation retry wait `millis` element specifies the amount of time, in milliseconds, to wait between retrying to allocate a connection
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=allocation-retry-wait-millis)`

## syntax of the operation to get the `allow-multiple-users` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `allow-multiple-users` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `allow-multiple-users` attribute description: specifies if multiple users will access the `datasource` through the `getconnection(user`, password) method and hence if the internal pool type should account for that
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=allow-multiple-users)`

## syntax of the operation to get the `authentication-context` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `authentication-context` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `authentication-context` attribute description: the elytron authentication context which defines the `javax.security.auth.subject` that is used to distinguish connections in the pool.
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=authentication-context)`

## syntax of the operation to get the `background-validation` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `background-validation` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `background-validation` attribute description: an element to specify that connections should be validated on a background thread versus being validated prior to use.
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=background-validation)`

## syntax of the operation to get the `background-validation-millis` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `background-validation-millis` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `background-validation-millis` attribute description: the `background-validation-millis` element specifies the amount of time, in milliseconds, that background validation will run.
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=background-validation-millis)`

## syntax of the operation to get the `blocking-timeout-wait-millis` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `blocking-timeout-wait-millis` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `blocking-timeout-wait-millis` attribute description: the `blocking-timeout-millis` element specifies the maximum time, in milliseconds, to block while waiting for a connection before throwing an exception. note that this blocks only while waiting for locking a connection, and will never throw an exception if creating a new connection takes an inordinately long time
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=blocking-timeout-wait-millis)`

## syntax of the operation to get the `capacity-decrementer-class` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `capacity-decrementer-class` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `capacity-decrementer-class` attribute description: class defining the policy for decrementing connections in the pool
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=capacity-decrementer-class)`

## syntax of the operation to get the `capacity-decrementer-properties` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `capacity-decrementer-properties` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `capacity-decrementer-properties` attribute description: properties to inject in class defining the policy for decrementing connections in the pool
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=capacity-decrementer-properties)`

## syntax of the operation to get the `capacity-incrementer-class` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `capacity-incrementer-class` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `capacity-incrementer-class` attribute description: class defining the policy for incrementing connections in the pool
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=capacity-incrementer-class)`

## syntax of the operation to get the `capacity-incrementer-properties` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `capacity-incrementer-properties` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `capacity-incrementer-properties` attribute description: properties to inject in class defining the policy for incrementing connections in the pool
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=capacity-incrementer-properties)`

## syntax of the operation to get the `check-valid-connection-sql` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `check-valid-connection-sql` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `check-valid-connection-sql` attribute description: specify an `sql` statement to check validity of a pool connection. this may be called when managed connection is obtained from the pool
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=check-valid-connection-sql)`

## syntax of the operation to get the `connectable` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `connectable` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `connectable` attribute description: enable the use of `cmr` for this `datasource`. this feature means that a local resource can reliably participate in an `xa` transaction.
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=connectable)`

## syntax of the operation to get the `connection-listener-class` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `connection-listener-class` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `connection-listener-class` attribute description: `speciefies` class name extending `org.jboss.jca.adapters.jdbc.spi.listener.connectionlistener` that provides a possible to listen for connection activation and passivation in order to perform actions before the connection is returned to the application or returned to the pool.
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=connection-listener-class)`

## syntax of the operation to get the `connection-listener-property` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `connection-listener-property` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `connection-listener-property` attribute description: properties to be injected in class specified in `connection-listener-class`
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=connection-listener-property)`

## syntax of the operation to get the `credential-reference` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `credential-reference` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `credential-reference` attribute description: credential (from credential store) to authenticate on data source
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=credential-reference)`

## syntax of the operation to get the `driver-name` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `driver-name` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `driver-name` attribute description: defines the `jdbc` driver the `datasource` should use. it is a symbolic name matching the the name of installed driver. in case the driver is deployed as a jar, the name is the name of the deployment unit
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=driver-name)`

## syntax of the operation to get the `elytron-enabled` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `elytron-enabled` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `elytron-enabled` attribute description: enables elytron security for handling authentication of connections for recovery. the elytron `authentication-context` to be used will be current context if no context is specified (see `authentication-context`).
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=elytron-enabled)`

## syntax of the operation to get the `enabled` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `enabled` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `enabled` attribute description: specifies if the `datasource` should be enabled. note this attribute will not be supported runtime in next versions.
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=enabled)`

## syntax of the operation to get the `enlistment-trace` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `enlistment-trace` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `enlistment-trace` attribute description: defines if `wildfly/ironjacamar` should record enlistment traces
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=enlistment-trace)`

## syntax of the operation to get the `exception-sorter-class-name` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `exception-sorter-class-name` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `exception-sorter-class-name` attribute description: an `org.jboss.jca.adapters.jdbc.exceptionsorter` that provides an `isexceptionfatal(sqlexception`) method to validate if an exception should broadcast an error
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=exception-sorter-class-name)`

## syntax of the operation to get the `exception-sorter-module` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `exception-sorter-module` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `exception-sorter-module` attribute description: the name of the module which makes the implementation of `org.jboss.jca.adapters.jdbc.exceptionsorter` available
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=exception-sorter-module)`

## syntax of the operation to get the `exception-sorter-properties` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `exception-sorter-properties` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `exception-sorter-properties` attribute description: the exception sorter properties
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=exception-sorter-properties)`

## syntax of the operation to get the `flush-strategy` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `flush-strategy` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `flush-strategy` attribute description: specifies how the pool should be flush in case of an error.
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=flush-strategy)`

## syntax of the operation to get the `idle-timeout-minutes` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `idle-timeout-minutes` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `idle-timeout-minutes` attribute description: the `idle-timeout-minutes` elements specifies the maximum time, in minutes, a connection may be idle before being closed. the actual maximum time depends also on the `idleremover` scan time, which is half of the smallest `idle-timeout-minutes` value of any pool. changing this value can be done only on disabled `datasource`, requires a server restart otherwise.
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=idle-timeout-minutes)`

## syntax of the operation to get the `initial-pool-size` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `initial-pool-size` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `initial-pool-size` attribute description: the `initial-pool-size` element indicates the initial number of connections a pool should hold.
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=initial-pool-size)`

## syntax of the operation to get the `interleaving` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `interleaving` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `interleaving` attribute description: an element to enable interleaving for `xa` connections
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=interleaving)`

## syntax of the operation to get the `jndi-name` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `jndi-name` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `jndi-name` attribute description: specifies the `jndi` name for the `datasource`
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=jndi-name)`

## syntax of the operation to get the `max-pool-size` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `max-pool-size` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `max-pool-size` attribute description: the `max-pool-size` element specifies the maximum number of connections for a pool. no more connections will be created in each `sub-pool`
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=max-pool-size)`

## syntax of the operation to get the `mcp` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `mcp` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `mcp` attribute description: defines the `managedconnectionpool` implementation, `f.ex`. `org.jboss.jca.core.connectionmanager.pool.mcp.semaphorearraylistmanagedconnectionpool`
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=mcp)`

## syntax of the operation to get the `min-pool-size` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `min-pool-size` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `min-pool-size` attribute description: the `min-pool-size` element specifies the minimum number of connections for a pool
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=min-pool-size)`

## syntax of the operation to get the `new-connection-sql` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `new-connection-sql` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `new-connection-sql` attribute description: specifies an `sql` statement to execute whenever a connection is added to the connection pool
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=new-connection-sql)`

## syntax of the operation to get the `no-recovery` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `no-recovery` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `no-recovery` attribute description: specifies if the connection pool should be excluded from recovery
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=no-recovery)`

## syntax of the operation to get the `no-tx-separate-pool` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `no-tx-separate-pool` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `no-tx-separate-pool` attribute description: oracle does not like `xa` connections getting used both inside and outside a jakarta transactions transaction. to `workaround` the problem you can create separate `sub-pools` for the different contexts
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=no-tx-separate-pool)`

## syntax of the operation to get the `pad-xid` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `pad-xid` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `pad-xid` attribute description: should the `xid` be padded
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=pad-xid)`

## syntax of the operation to get the `password` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `password` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `password` attribute description: specifies the password used when creating a new connection
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=password)`

## syntax of the operation to get the `pool-fair` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `pool-fair` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `pool-fair` attribute description: defines if pool use should be fair
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=pool-fair)`

## syntax of the operation to get the `pool-prefill` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `pool-prefill` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `pool-prefill` attribute description: should the pool be `prefilled`. changing this value can be done only on disabled `datasource`, requires a server restart otherwise.
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=pool-prefill)`

## syntax of the operation to get the `pool-use-strict-min` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `pool-use-strict-min` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `pool-use-strict-min` attribute description: specifies if the `min-pool-size` should be considered strictly
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=pool-use-strict-min)`

## syntax of the operation to get the `prepared-statements-cache-size` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `prepared-statements-cache-size` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `prepared-statements-cache-size` attribute description: the number of prepared statements per connection in an `lru` cache
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=prepared-statements-cache-size)`

## syntax of the operation to get the `query-timeout` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `query-timeout` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `query-timeout` attribute description: any configured query timeout in seconds. if not provided no timeout will be set
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=query-timeout)`

## syntax of the operation to get the `reauth-plugin-class-name` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `reauth-plugin-class-name` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `reauth-plugin-class-name` attribute description: the fully qualified class name of the reauthentication `plugin` implementation
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=reauth-plugin-class-name)`

## syntax of the operation to get the `reauth-plugin-properties` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `reauth-plugin-properties` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `reauth-plugin-properties` attribute description: the properties for the reauthentication `plugin`
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=reauth-plugin-properties)`

## syntax of the operation to get the `recovery-authentication-context` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `recovery-authentication-context` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `recovery-authentication-context` attribute description: the elytron authentication context which defines the `javax.security.auth.subject` that is used to distinguish connections in the pool.
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=recovery-authentication-context)`

## syntax of the operation to get the `recovery-credential-reference` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `recovery-credential-reference` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `recovery-credential-reference` attribute description: credential (from credential store) to authenticate on data source
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=recovery-credential-reference)`

## syntax of the operation to get the `recovery-elytron-enabled` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `recovery-elytron-enabled` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `recovery-elytron-enabled` attribute description: enables elytron security for handling authentication of connections for recovery. the elytron `authentication-context` to be used will be current context if no context is specified (see `authentication-context`).
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=recovery-elytron-enabled)`

## syntax of the operation to get the `recovery-password` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `recovery-password` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `recovery-password` attribute description: the password used for recovery
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=recovery-password)`

## syntax of the operation to get the `recovery-plugin-class-name` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `recovery-plugin-class-name` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `recovery-plugin-class-name` attribute description: the fully qualified class name of the recovery `plugin` implementation
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=recovery-plugin-class-name)`

## syntax of the operation to get the `recovery-plugin-properties` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `recovery-plugin-properties` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `recovery-plugin-properties` attribute description: the properties for the recovery `plugin`
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=recovery-plugin-properties)`

## syntax of the operation to get the `recovery-security-domain` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `recovery-security-domain` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `recovery-security-domain` attribute description: the security domain used for recovery
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=recovery-security-domain)`

## syntax of the operation to get the `recovery-username` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `recovery-username` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `recovery-username` attribute description: the user name used for recovery
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=recovery-username)`

## syntax of the operation to get the `same-rm-override` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `same-rm-override` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `same-rm-override` attribute description: the `is-same-rm-override` element allows one to unconditionally set whether the `javax.transaction.xa.xaresource.issamerm(xaresource`) returns true or false
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=same-rm-override)`

## syntax of the operation to get the `security-domain` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `security-domain` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `security-domain` attribute description: specifies the `picketbox` security domain which defines the `javax.security.auth.subject` that are used to distinguish connections in the pool
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=security-domain)`

## syntax of the operation to get the `set-tx-query-timeout` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `set-tx-query-timeout` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `set-tx-query-timeout` attribute description: whether to set the query timeout based on the time remaining until transaction timeout. any configured query timeout will be used if there is no transaction
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=set-tx-query-timeout)`

## syntax of the operation to get the `share-prepared-statements` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `share-prepared-statements` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `share-prepared-statements` attribute description: whether to share prepared statements, `i.e`. whether asking for same statement twice without closing uses the same underlying prepared statement
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=share-prepared-statements)`

## syntax of the operation to get the `spy` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `spy` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `spy` attribute description: enable spying of `sql` statements
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=spy)`

## syntax of the operation to get the `stale-connection-checker-class-name` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `stale-connection-checker-class-name` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `stale-connection-checker-class-name` attribute description: an `org.jboss.jca.adapters.jdbc.staleconnectionchecker` that provides an `isstaleconnection(sqlexception`) method which if it returns true will wrap the exception in an `org.jboss.jca.adapters.jdbc.staleconnectionexception`
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=stale-connection-checker-class-name)`

## syntax of the operation to get the `stale-connection-checker-module` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `stale-connection-checker-module` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `stale-connection-checker-module` attribute description: the name of the module which makes the implementation of `org.jboss.jca.adapters.jdbc.staleconnectionchecker` available
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=stale-connection-checker-module)`

## syntax of the operation to get the `stale-connection-checker-properties` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `stale-connection-checker-properties` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `stale-connection-checker-properties` attribute description: the stale connection checker properties
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=stale-connection-checker-properties)`

## syntax of the operation to get the `statistics-enabled` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `statistics-enabled` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `statistics-enabled` attribute description: define whether runtime statistics are enabled or not.
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `track-statements` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `track-statements` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `track-statements` attribute description: whether to check for unclosed statements when a connection is returned to the pool, result sets are closed, a statement is closed or return to the prepared statement cache. valid values are: "false" - do not track statements, "true" - track statements and result sets and warn when they are not closed, "`nowarn`" - track statements but do not warn about them being unclosed
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=track-statements)`

## syntax of the operation to get the `tracking` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `tracking` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `tracking` attribute description: defines if `ironjacamar` should track connection handles across transaction boundaries
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=tracking)`

## syntax of the operation to get the `transaction-isolation` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `transaction-isolation` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `transaction-isolation` attribute description: set the `java.sql.connection` transaction isolation level. valid values are: `transaction_read_uncommitted`, `transaction_read_committed`, `transaction_repeatable_read`, `transaction_serializable` and `transaction_none`. different values are used to set `customlevel` using `transactionisolation#customlevel`.
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=transaction-isolation)`

## syntax of the operation to get the `url-delimiter` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `url-delimiter` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `url-delimiter` attribute description: specifies the delimiter for `urls` in `connection-url` for ha `datasources`
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=url-delimiter)`

## syntax of the operation to get the `url-property` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `url-property` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `url-property` attribute description: specifies the property for the `url` property in the `xa-datasource-property` values
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=url-property)`

## syntax of the operation to get the `url-selector-strategy-class-name` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `url-selector-strategy-class-name` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `url-selector-strategy-class-name` attribute description: a class that implements `org.jboss.jca.adapters.jdbc.urlselectorstrategy`
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=url-selector-strategy-class-name)`

## syntax of the operation to get the `use-ccm` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `use-ccm` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `use-ccm` attribute description: enable the use of a cached connection manager
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=use-ccm)`

## syntax of the operation to get the `use-fast-fail` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `use-fast-fail` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `use-fast-fail` attribute description: whether to fail a connection allocation on the first try if it is invalid (true) or keep trying until the pool is exhausted of all potential connections (false)
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=use-fast-fail)`

## syntax of the operation to get the `use-java-context` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `use-java-context` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `use-java-context` attribute description: setting this to false will bind the `datasource` into global `jndi`
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=use-java-context)`

## syntax of the operation to get the `use-try-lock` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `use-try-lock` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `use-try-lock` attribute description: any configured timeout for internal locks on the resource adapter objects in seconds
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=use-try-lock)`

## syntax of the operation to get the `user-name` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `user-name` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `user-name` attribute description: specify the user name used when creating a new connection
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=user-name)`

## syntax of the operation to get the `valid-connection-checker-class-name` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `valid-connection-checker-class-name` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `valid-connection-checker-class-name` attribute description: an `org.jboss.jca.adapters.jdbc.validconnectionchecker` that provides an `isvalidconnection(connection`) method to validate a connection. if an exception is returned that means the connection is invalid. this overrides the `check-valid-connection-sql` element
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=valid-connection-checker-class-name)`

## syntax of the operation to get the `valid-connection-checker-module` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `valid-connection-checker-module` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `valid-connection-checker-module` attribute description: the name of the module which makes the implementation of `org.jboss.jca.adapters.jdbc.validconnectionchecker` available
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=valid-connection-checker-module)`

## syntax of the operation to get the `valid-connection-checker-properties` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `valid-connection-checker-properties` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `valid-connection-checker-properties` attribute description: the valid connection checker properties
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=valid-connection-checker-properties)`

## syntax of the operation to get the `validate-on-match` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `validate-on-match` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `validate-on-match` attribute description: the `validate-on-match` element specifies if connection validation should be done when a connection factory attempts to match a managed connection. this is typically exclusive to the use of background validation
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=validate-on-match)`

## syntax of the operation to get the `wrap-xa-resource` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `wrap-xa-resource` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `wrap-xa-resource` attribute description: should the `xaresource` instances be wrapped in an `org.jboss.tm.xaresourcewrapper` instance
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=wrap-xa-resource)`

## syntax of the operation to get the `xa-datasource-class` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `xa-datasource-class` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `xa-datasource-class` attribute description: the fully qualified name of the `javax.sql.xadatasource` implementation
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=xa-datasource-class)`

## syntax of the operation to get the `xa-resource-timeout` attribute of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `xa-resource-timeout` attribute value of the `xa-data-source` resource of the `datasources` subsystem
* `xa-resource-timeout` attribute description: the value is passed to `xaresource.settransactiontimeout`(), in seconds. default is zero
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>:read-attribute(name=xa-resource-timeout)`

## syntax of the operation to get a `xa-datasource-properties` resource of the `xa-data-source` resource of the `datasources` subsystem
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>/xa-datasource-properties=<xa-datasource-properties name>:read-resource()`
* To get the list of all the `xa-datasource-properties` resource of the `xa-data-source` resource of the `datasources` subsystem use '*' for `<xa-datasource-properties name>`.

## syntax of the operation to get the `value` attribute of the `xa-datasource-properties` resource of the `xa-data-source` resource of the `datasources` subsystem
* Retrieve the `value` attribute value of the `xa-datasource-properties` resource of the `xa-data-source` resource of the `datasources` subsystem
* `value` attribute description: specifies a property value to assign to the `xadatasource` implementation class. each property is identified by the name attribute and the property value is given by the `xa-datasource-property` element content. the property is mapped onto the `xadatasource` implementation by looking for a `javabeans` style getter method for the property name. if found, the value of the property is set using the `javabeans` setter with the element text translated to the true property type using the `java.beans.propertyeditor`
* operation: `/subsystem=datasources/xa-data-source=<xa-data-source name>/xa-datasource-properties=<xa-datasource-properties name>:read-attribute(name=value)`

## syntax of the operation to get the `add-logging-api-dependencies` attribute of the `logging` subsystem
* Retrieve the `add-logging-api-dependencies` attribute value of the `logging` subsystem
* `add-logging-api-dependencies` attribute description: indicates whether or not logging `api` dependencies should be added to deployments during the deployment process. a value of true will add the dependencies to the deployment. a value of false will skip the deployment from being processed for logging `api` dependencies.
* operation: `/subsystem=logging:read-attribute(name=add-logging-api-dependencies)`

## syntax of the operation to get the `use-deployment-logging-config` attribute of the `logging` subsystem
* Retrieve the `use-deployment-logging-config` attribute value of the `logging` subsystem
* `use-deployment-logging-config` attribute description: indicates whether or not deployments should use a logging configuration file found in the deployment to configure the log manager. if set to true and a logging configuration file is found in the `deployment's` `meta-inf` or `web-inf/classes` directory, then a log manager will be configured with those settings. if set to false, the `server's` logging configuration will be used regardless of any logging configuration files supplied in the deployment.
* operation: `/subsystem=logging:read-attribute(name=use-deployment-logging-config)`

## syntax of the operation to get a `periodic-rotating-file-handler` resource of the `logging` subsystem
* operation: `/subsystem=logging/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-resource()`
* To get the list of all the `periodic-rotating-file-handler` resource of the `logging` subsystem use '*' for `<periodic-rotating-file-handler name>`.

## syntax of the operation to get the `append` attribute of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `append` attribute value of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* `append` attribute description: specify whether to append to the target file.
* operation: `/subsystem=logging/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=append)`

## syntax of the operation to get the `autoflush` attribute of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `autoflush` attribute value of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* `autoflush` attribute description: automatically flush after each write.
* operation: `/subsystem=logging/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `enabled` attribute of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `file` attribute of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `file` attribute value of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* `file` attribute description: the file description consisting of the path and optional relative to path.
* operation: `/subsystem=logging/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=file)`

## syntax of the operation to get the `filter` attribute of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `filter` attribute value of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* `filter` attribute description: defines a simple filter type.
* operation: `/subsystem=logging/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=filter)`

## syntax of the operation to get the `filter-spec` attribute of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `formatter` attribute of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `formatter` attribute value of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* `formatter` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=formatter)`

## syntax of the operation to get the `level` attribute of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `name` attribute of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `named-formatter` attribute of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `suffix` attribute of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `suffix` attribute value of the `periodic-rotating-file-handler` resource of the `logging` subsystem
* `suffix` attribute description: set the suffix string.  the string is in a format which can be understood by `java.text.simpledateformat`. the period of the rotation is automatically calculated based on the suffix.
* operation: `/subsystem=logging/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=suffix)`

## syntax of the operation to get a `json-formatter` resource of the `logging` subsystem
* operation: `/subsystem=logging/json-formatter=<json-formatter name>:read-resource()`
* To get the list of all the `json-formatter` resource of the `logging` subsystem use '*' for `<json-formatter name>`.

## syntax of the operation to get the `date-format` attribute of the `json-formatter` resource of the `logging` subsystem
* Retrieve the `date-format` attribute value of the `json-formatter` resource of the `logging` subsystem
* `date-format` attribute description: the `date/time` format pattern. the pattern must be a valid `java.time.format.datetimeformatter.ofpattern`() pattern. the default pattern is an `iso-8601` extended offset `date-time` format.
* operation: `/subsystem=logging/json-formatter=<json-formatter name>:read-attribute(name=date-format)`

## syntax of the operation to get the `exception-output-type` attribute of the `json-formatter` resource of the `logging` subsystem
* Retrieve the `exception-output-type` attribute value of the `json-formatter` resource of the `logging` subsystem
* `exception-output-type` attribute description: indicates how the cause of the logged message, if one is available, will be added to the `json` output.
* operation: `/subsystem=logging/json-formatter=<json-formatter name>:read-attribute(name=exception-output-type)`

## syntax of the operation to get the `key-overrides` attribute of the `json-formatter` resource of the `logging` subsystem
* Retrieve the `key-overrides` attribute value of the `json-formatter` resource of the `logging` subsystem
* `key-overrides` attribute description: allows the names of the keys for the `json` properties to be overridden.
* operation: `/subsystem=logging/json-formatter=<json-formatter name>:read-attribute(name=key-overrides)`

## syntax of the operation to get the `meta-data` attribute of the `json-formatter` resource of the `logging` subsystem
* Retrieve the `meta-data` attribute value of the `json-formatter` resource of the `logging` subsystem
* `meta-data` attribute description: sets the meta data to use in the `json` format. properties will be added to each log message.
* operation: `/subsystem=logging/json-formatter=<json-formatter name>:read-attribute(name=meta-data)`

## syntax of the operation to get the `pretty-print` attribute of the `json-formatter` resource of the `logging` subsystem
* Retrieve the `pretty-print` attribute value of the `json-formatter` resource of the `logging` subsystem
* `pretty-print` attribute description: indicates whether or not pretty printing should be used when formatting.
* operation: `/subsystem=logging/json-formatter=<json-formatter name>:read-attribute(name=pretty-print)`

## syntax of the operation to get the `print-details` attribute of the `json-formatter` resource of the `logging` subsystem
* Retrieve the `print-details` attribute value of the `json-formatter` resource of the `logging` subsystem
* `print-details` attribute description: sets whether or not details should be printed. printing the details can be expensive as the values are retrieved from the caller. the details include the source class name, source file name, source method name, source module name, source module version and source line number.
* operation: `/subsystem=logging/json-formatter=<json-formatter name>:read-attribute(name=print-details)`

## syntax of the operation to get the `record-delimiter` attribute of the `json-formatter` resource of the `logging` subsystem
* Retrieve the `record-delimiter` attribute value of the `json-formatter` resource of the `logging` subsystem
* `record-delimiter` attribute description: the value to be used to indicate the end of a record. if set to null no delimiter will be used at the end of the record. the default value is a line feed.
* operation: `/subsystem=logging/json-formatter=<json-formatter name>:read-attribute(name=record-delimiter)`

## syntax of the operation to get the `zone-id` attribute of the `json-formatter` resource of the `logging` subsystem
* Retrieve the `zone-id` attribute value of the `json-formatter` resource of the `logging` subsystem
* `zone-id` attribute description: the zone id for formatting the date and time. the system default is used if left undefined.
* operation: `/subsystem=logging/json-formatter=<json-formatter name>:read-attribute(name=zone-id)`

## syntax of the operation to get a `socket-handler` resource of the `logging` subsystem
* operation: `/subsystem=logging/socket-handler=<socket-handler name>:read-resource()`
* To get the list of all the `socket-handler` resource of the `logging` subsystem use '*' for `<socket-handler name>`.

## syntax of the operation to get the `autoflush` attribute of the `socket-handler` resource of the `logging` subsystem
* Retrieve the `autoflush` attribute value of the `socket-handler` resource of the `logging` subsystem
* `autoflush` attribute description: automatically flush after each write.
* operation: `/subsystem=logging/socket-handler=<socket-handler name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `block-on-reconnect` attribute of the `socket-handler` resource of the `logging` subsystem
* Retrieve the `block-on-reconnect` attribute value of the `socket-handler` resource of the `logging` subsystem
* `block-on-reconnect` attribute description: if set to true the write methods will block when attempting to reconnect. this is only advisable to be set to true if using an asynchronous handler.
* operation: `/subsystem=logging/socket-handler=<socket-handler name>:read-attribute(name=block-on-reconnect)`

## syntax of the operation to get the `enabled` attribute of the `socket-handler` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `socket-handler` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/socket-handler=<socket-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `socket-handler` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `socket-handler` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/socket-handler=<socket-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `filter-spec` attribute of the `socket-handler` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `socket-handler` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/socket-handler=<socket-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `level` attribute of the `socket-handler` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `socket-handler` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/socket-handler=<socket-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `named-formatter` attribute of the `socket-handler` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `socket-handler` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/socket-handler=<socket-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `outbound-socket-binding-ref` attribute of the `socket-handler` resource of the `logging` subsystem
* Retrieve the `outbound-socket-binding-ref` attribute value of the `socket-handler` resource of the `logging` subsystem
* `outbound-socket-binding-ref` attribute description: outbound socket reference for the socket connection.
* operation: `/subsystem=logging/socket-handler=<socket-handler name>:read-attribute(name=outbound-socket-binding-ref)`

## syntax of the operation to get the `protocol` attribute of the `socket-handler` resource of the `logging` subsystem
* Retrieve the `protocol` attribute value of the `socket-handler` resource of the `logging` subsystem
* `protocol` attribute description: the protocol the socket should communicate over.
* operation: `/subsystem=logging/socket-handler=<socket-handler name>:read-attribute(name=protocol)`

## syntax of the operation to get the `ssl-context` attribute of the `socket-handler` resource of the `logging` subsystem
* Retrieve the `ssl-context` attribute value of the `socket-handler` resource of the `logging` subsystem
* `ssl-context` attribute description: the reference to the defined `ssl` context. this is only used if the protocol is set to `ssl_tcp`.
* operation: `/subsystem=logging/socket-handler=<socket-handler name>:read-attribute(name=ssl-context)`

## syntax of the operation to get a `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-resource()`
* To get the list of all the `periodic-size-rotating-file-handler` resource of the `logging` subsystem use '*' for `<periodic-size-rotating-file-handler name>`.

## syntax of the operation to get the `append` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `append` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `append` attribute description: specify whether to append to the target file.
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=append)`

## syntax of the operation to get the `autoflush` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `autoflush` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `autoflush` attribute description: automatically flush after each write.
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `enabled` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `file` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `file` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `file` attribute description: the file description consisting of the path and optional relative to path.
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=file)`

## syntax of the operation to get the `filter-spec` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `formatter` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `formatter` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `formatter` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=formatter)`

## syntax of the operation to get the `level` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `max-backup-index` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `max-backup-index` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `max-backup-index` attribute description: the maximum number of backups to keep.
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=max-backup-index)`

## syntax of the operation to get the `name` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `named-formatter` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `rotate-on-boot` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `rotate-on-boot` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `rotate-on-boot` attribute description: indicates the file should be rotated each time the file attribute is changed. this always happens when at initialization time.
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=rotate-on-boot)`

## syntax of the operation to get the `rotate-size` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `rotate-size` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `rotate-size` attribute description: the size at which to rotate the log file.
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=rotate-size)`

## syntax of the operation to get the `suffix` attribute of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `suffix` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging` subsystem
* `suffix` attribute description: set the suffix string.  the string is in a format which can be understood by `java.text.simpledateformat`. the period of the rotation is automatically calculated based on the suffix.
* operation: `/subsystem=logging/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=suffix)`

## syntax of the operation to get a `async-handler` resource of the `logging` subsystem
* operation: `/subsystem=logging/async-handler=<async-handler name>:read-resource()`
* To get the list of all the `async-handler` resource of the `logging` subsystem use '*' for `<async-handler name>`.

## syntax of the operation to get the `enabled` attribute of the `async-handler` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `async-handler` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/async-handler=<async-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `filter` attribute of the `async-handler` resource of the `logging` subsystem
* Retrieve the `filter` attribute value of the `async-handler` resource of the `logging` subsystem
* `filter` attribute description: defines a simple filter type.
* operation: `/subsystem=logging/async-handler=<async-handler name>:read-attribute(name=filter)`

## syntax of the operation to get the `filter-spec` attribute of the `async-handler` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `async-handler` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/async-handler=<async-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `level` attribute of the `async-handler` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `async-handler` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this handler. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/async-handler=<async-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `name` attribute of the `async-handler` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `async-handler` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/async-handler=<async-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `overflow-action` attribute of the `async-handler` resource of the `logging` subsystem
* Retrieve the `overflow-action` attribute value of the `async-handler` resource of the `logging` subsystem
* `overflow-action` attribute description: specify what action to take when the overflowing.  the valid options are 'block' and 'discard'
* operation: `/subsystem=logging/async-handler=<async-handler name>:read-attribute(name=overflow-action)`

## syntax of the operation to get the `queue-length` attribute of the `async-handler` resource of the `logging` subsystem
* Retrieve the `queue-length` attribute value of the `async-handler` resource of the `logging` subsystem
* `queue-length` attribute description: the queue length to use before flushing writing
* operation: `/subsystem=logging/async-handler=<async-handler name>:read-attribute(name=queue-length)`

## syntax of the operation to get the `subhandlers` attribute of the `async-handler` resource of the `logging` subsystem
* Retrieve the `subhandlers` attribute value of the `async-handler` resource of the `logging` subsystem
* `subhandlers` attribute description: the handlers associated with this async handler.
* operation: `/subsystem=logging/async-handler=<async-handler name>:read-attribute(name=subhandlers)`

## syntax of the operation to get a `syslog-handler` resource of the `logging` subsystem
* operation: `/subsystem=logging/syslog-handler=<syslog-handler name>:read-resource()`
* To get the list of all the `syslog-handler` resource of the `logging` subsystem use '*' for `<syslog-handler name>`.

## syntax of the operation to get the `app-name` attribute of the `syslog-handler` resource of the `logging` subsystem
* Retrieve the `app-name` attribute value of the `syslog-handler` resource of the `logging` subsystem
* `app-name` attribute description: the app name used when formatting the message in `rfc5424` format. by default the app name is "java".
* operation: `/subsystem=logging/syslog-handler=<syslog-handler name>:read-attribute(name=app-name)`

## syntax of the operation to get the `enabled` attribute of the `syslog-handler` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `syslog-handler` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/syslog-handler=<syslog-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `facility` attribute of the `syslog-handler` resource of the `logging` subsystem
* Retrieve the `facility` attribute value of the `syslog-handler` resource of the `logging` subsystem
* `facility` attribute description: facility as defined by `rfc-5424` (`http://tools.ietf.org/html/rfc5424)and` `rfc-3164` (`http://tools.ietf.org/html/rfc3164`).
* operation: `/subsystem=logging/syslog-handler=<syslog-handler name>:read-attribute(name=facility)`

## syntax of the operation to get the `hostname` attribute of the `syslog-handler` resource of the `logging` subsystem
* Retrieve the `hostname` attribute value of the `syslog-handler` resource of the `logging` subsystem
* `hostname` attribute description: the name of the host the messages are being sent from. for example the name of the host the application server is running on.
* operation: `/subsystem=logging/syslog-handler=<syslog-handler name>:read-attribute(name=hostname)`

## syntax of the operation to get the `level` attribute of the `syslog-handler` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `syslog-handler` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/syslog-handler=<syslog-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `named-formatter` attribute of the `syslog-handler` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `syslog-handler` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/syslog-handler=<syslog-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `port` attribute of the `syslog-handler` resource of the `logging` subsystem
* Retrieve the `port` attribute value of the `syslog-handler` resource of the `logging` subsystem
* `port` attribute description: the port the `syslog` server is listening on.
* operation: `/subsystem=logging/syslog-handler=<syslog-handler name>:read-attribute(name=port)`

## syntax of the operation to get the `server-address` attribute of the `syslog-handler` resource of the `logging` subsystem
* Retrieve the `server-address` attribute value of the `syslog-handler` resource of the `logging` subsystem
* `server-address` attribute description: the address of the `syslog` server.
* operation: `/subsystem=logging/syslog-handler=<syslog-handler name>:read-attribute(name=server-address)`

## syntax of the operation to get the `syslog-format` attribute of the `syslog-handler` resource of the `logging` subsystem
* Retrieve the `syslog-format` attribute value of the `syslog-handler` resource of the `logging` subsystem
* `syslog-format` attribute description: formats the log message according to the `rfc` specification.
* operation: `/subsystem=logging/syslog-handler=<syslog-handler name>:read-attribute(name=syslog-format)`

## syntax of the operation to get a `file-handler` resource of the `logging` subsystem
* operation: `/subsystem=logging/file-handler=<file-handler name>:read-resource()`
* To get the list of all the `file-handler` resource of the `logging` subsystem use '*' for `<file-handler name>`.

## syntax of the operation to get the `append` attribute of the `file-handler` resource of the `logging` subsystem
* Retrieve the `append` attribute value of the `file-handler` resource of the `logging` subsystem
* `append` attribute description: specify whether to append to the target file.
* operation: `/subsystem=logging/file-handler=<file-handler name>:read-attribute(name=append)`

## syntax of the operation to get the `autoflush` attribute of the `file-handler` resource of the `logging` subsystem
* Retrieve the `autoflush` attribute value of the `file-handler` resource of the `logging` subsystem
* `autoflush` attribute description: automatically flush after each write.
* operation: `/subsystem=logging/file-handler=<file-handler name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `enabled` attribute of the `file-handler` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `file-handler` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/file-handler=<file-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `file-handler` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `file-handler` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/file-handler=<file-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `file` attribute of the `file-handler` resource of the `logging` subsystem
* Retrieve the `file` attribute value of the `file-handler` resource of the `logging` subsystem
* `file` attribute description: the file description consisting of the path and optional relative to path.
* operation: `/subsystem=logging/file-handler=<file-handler name>:read-attribute(name=file)`

## syntax of the operation to get the `filter` attribute of the `file-handler` resource of the `logging` subsystem
* Retrieve the `filter` attribute value of the `file-handler` resource of the `logging` subsystem
* `filter` attribute description: defines a simple filter type.
* operation: `/subsystem=logging/file-handler=<file-handler name>:read-attribute(name=filter)`

## syntax of the operation to get the `filter-spec` attribute of the `file-handler` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `file-handler` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/file-handler=<file-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `formatter` attribute of the `file-handler` resource of the `logging` subsystem
* Retrieve the `formatter` attribute value of the `file-handler` resource of the `logging` subsystem
* `formatter` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/file-handler=<file-handler name>:read-attribute(name=formatter)`

## syntax of the operation to get the `level` attribute of the `file-handler` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `file-handler` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/file-handler=<file-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `name` attribute of the `file-handler` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `file-handler` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/file-handler=<file-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `named-formatter` attribute of the `file-handler` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `file-handler` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/file-handler=<file-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `filter` attribute of the `ROOT` `root-logger` resource of the `logging` subsystem
* Retrieve the `filter` attribute value of the `ROOT` `root-logger` resource of the `logging` subsystem
* `filter` attribute description: defines a simple filter type.
* operation: `/subsystem=logging/root-logger=ROOT:read-attribute(name=filter)`

## syntax of the operation to get the `filter-spec` attribute of the `ROOT` `root-logger` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `ROOT` `root-logger` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/root-logger=ROOT:read-attribute(name=filter-spec)`

## syntax of the operation to get the `handlers` attribute of the `ROOT` `root-logger` resource of the `logging` subsystem
* Retrieve the `handlers` attribute value of the `ROOT` `root-logger` resource of the `logging` subsystem
* `handlers` attribute description: the handlers associated with the root logger.
* operation: `/subsystem=logging/root-logger=ROOT:read-attribute(name=handlers)`

## syntax of the operation to get the `level` attribute of the `ROOT` `root-logger` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `ROOT` `root-logger` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by the root logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/root-logger=ROOT:read-attribute(name=level)`

## syntax of the operation to get a `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>:read-resource()`
* To get the list of all the `logging-profile` resource of the `logging` subsystem use '*' for `<logging-profile name>`.

## syntax of the operation to get a `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-resource()`
* To get the list of all the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<periodic-rotating-file-handler name>`.

## syntax of the operation to get the `append` attribute of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `append` attribute value of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `append` attribute description: specify whether to append to the target file.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=append)`

## syntax of the operation to get the `autoflush` attribute of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `autoflush` attribute value of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `autoflush` attribute description: automatically flush after each write.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `enabled` attribute of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `file` attribute of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `file` attribute value of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `file` attribute description: the file description consisting of the path and optional relative to path.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=file)`

## syntax of the operation to get the `filter-spec` attribute of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `formatter` attribute of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `formatter` attribute value of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `formatter` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=formatter)`

## syntax of the operation to get the `level` attribute of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `name` attribute of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `named-formatter` attribute of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `suffix` attribute of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `suffix` attribute value of the `periodic-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `suffix` attribute description: set the suffix string.  the string is in a format which can be understood by `java.text.simpledateformat`. the period of the rotation is automatically calculated based on the suffix.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-rotating-file-handler=<periodic-rotating-file-handler name>:read-attribute(name=suffix)`

## syntax of the operation to get a `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/json-formatter=<json-formatter name>:read-resource()`
* To get the list of all the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<json-formatter name>`.

## syntax of the operation to get the `date-format` attribute of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `date-format` attribute value of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `date-format` attribute description: the `date/time` format pattern. the pattern must be a valid `java.time.format.datetimeformatter.ofpattern`() pattern. the default pattern is an `iso-8601` extended offset `date-time` format.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/json-formatter=<json-formatter name>:read-attribute(name=date-format)`

## syntax of the operation to get the `exception-output-type` attribute of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `exception-output-type` attribute value of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `exception-output-type` attribute description: indicates how the cause of the logged message, if one is available, will be added to the `json` output.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/json-formatter=<json-formatter name>:read-attribute(name=exception-output-type)`

## syntax of the operation to get the `key-overrides` attribute of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `key-overrides` attribute value of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `key-overrides` attribute description: allows the names of the keys for the `json` properties to be overridden.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/json-formatter=<json-formatter name>:read-attribute(name=key-overrides)`

## syntax of the operation to get the `meta-data` attribute of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `meta-data` attribute value of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `meta-data` attribute description: sets the meta data to use in the `json` format. properties will be added to each log message.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/json-formatter=<json-formatter name>:read-attribute(name=meta-data)`

## syntax of the operation to get the `pretty-print` attribute of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `pretty-print` attribute value of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `pretty-print` attribute description: indicates whether or not pretty printing should be used when formatting.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/json-formatter=<json-formatter name>:read-attribute(name=pretty-print)`

## syntax of the operation to get the `print-details` attribute of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `print-details` attribute value of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `print-details` attribute description: sets whether or not details should be printed. printing the details can be expensive as the values are retrieved from the caller. the details include the source class name, source file name, source method name, source module name, source module version and source line number.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/json-formatter=<json-formatter name>:read-attribute(name=print-details)`

## syntax of the operation to get the `record-delimiter` attribute of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `record-delimiter` attribute value of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `record-delimiter` attribute description: the value to be used to indicate the end of a record. if set to null no delimiter will be used at the end of the record. the default value is a line feed.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/json-formatter=<json-formatter name>:read-attribute(name=record-delimiter)`

## syntax of the operation to get the `zone-id` attribute of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `zone-id` attribute value of the `json-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `zone-id` attribute description: the zone id for formatting the date and time. the system default is used if left undefined.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/json-formatter=<json-formatter name>:read-attribute(name=zone-id)`

## syntax of the operation to get a `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/socket-handler=<socket-handler name>:read-resource()`
* To get the list of all the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<socket-handler name>`.

## syntax of the operation to get the `autoflush` attribute of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `autoflush` attribute value of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `autoflush` attribute description: automatically flush after each write.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/socket-handler=<socket-handler name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `block-on-reconnect` attribute of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `block-on-reconnect` attribute value of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `block-on-reconnect` attribute description: if set to true the write methods will block when attempting to reconnect. this is only advisable to be set to true if using an asynchronous handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/socket-handler=<socket-handler name>:read-attribute(name=block-on-reconnect)`

## syntax of the operation to get the `enabled` attribute of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/socket-handler=<socket-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/socket-handler=<socket-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `filter-spec` attribute of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/socket-handler=<socket-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `level` attribute of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/socket-handler=<socket-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `named-formatter` attribute of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/socket-handler=<socket-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `outbound-socket-binding-ref` attribute of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `outbound-socket-binding-ref` attribute value of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `outbound-socket-binding-ref` attribute description: outbound socket reference for the socket connection.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/socket-handler=<socket-handler name>:read-attribute(name=outbound-socket-binding-ref)`

## syntax of the operation to get the `protocol` attribute of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `protocol` attribute value of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `protocol` attribute description: the protocol the socket should communicate over.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/socket-handler=<socket-handler name>:read-attribute(name=protocol)`

## syntax of the operation to get the `ssl-context` attribute of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `ssl-context` attribute value of the `socket-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `ssl-context` attribute description: the reference to the defined `ssl` context. this is only used if the protocol is set to `ssl_tcp`.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/socket-handler=<socket-handler name>:read-attribute(name=ssl-context)`

## syntax of the operation to get a `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-resource()`
* To get the list of all the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<periodic-size-rotating-file-handler name>`.

## syntax of the operation to get the `append` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `append` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `append` attribute description: specify whether to append to the target file.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=append)`

## syntax of the operation to get the `autoflush` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `autoflush` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `autoflush` attribute description: automatically flush after each write.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `enabled` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `file` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `file` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `file` attribute description: the file description consisting of the path and optional relative to path.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=file)`

## syntax of the operation to get the `filter-spec` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `formatter` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `formatter` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `formatter` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=formatter)`

## syntax of the operation to get the `level` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `max-backup-index` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `max-backup-index` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `max-backup-index` attribute description: the maximum number of backups to keep.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=max-backup-index)`

## syntax of the operation to get the `name` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `named-formatter` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `rotate-on-boot` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `rotate-on-boot` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `rotate-on-boot` attribute description: indicates the file should be rotated each time the file attribute is changed. this always happens when at initialization time.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=rotate-on-boot)`

## syntax of the operation to get the `rotate-size` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `rotate-size` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `rotate-size` attribute description: the size at which to rotate the log file.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=rotate-size)`

## syntax of the operation to get the `suffix` attribute of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `suffix` attribute value of the `periodic-size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `suffix` attribute description: set the suffix string.  the string is in a format which can be understood by `java.text.simpledateformat`. the period of the rotation is automatically calculated based on the suffix.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/periodic-size-rotating-file-handler=<periodic-size-rotating-file-handler name>:read-attribute(name=suffix)`

## syntax of the operation to get a `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/async-handler=<async-handler name>:read-resource()`
* To get the list of all the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<async-handler name>`.

## syntax of the operation to get the `enabled` attribute of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/async-handler=<async-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `filter-spec` attribute of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/async-handler=<async-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `level` attribute of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this handler. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/async-handler=<async-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `name` attribute of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/async-handler=<async-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `overflow-action` attribute of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `overflow-action` attribute value of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `overflow-action` attribute description: specify what action to take when the overflowing.  the valid options are 'block' and 'discard'
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/async-handler=<async-handler name>:read-attribute(name=overflow-action)`

## syntax of the operation to get the `queue-length` attribute of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `queue-length` attribute value of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `queue-length` attribute description: the queue length to use before flushing writing
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/async-handler=<async-handler name>:read-attribute(name=queue-length)`

## syntax of the operation to get the `subhandlers` attribute of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `subhandlers` attribute value of the `async-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `subhandlers` attribute description: the handlers associated with this async handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/async-handler=<async-handler name>:read-attribute(name=subhandlers)`

## syntax of the operation to get a `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/syslog-handler=<syslog-handler name>:read-resource()`
* To get the list of all the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<syslog-handler name>`.

## syntax of the operation to get the `app-name` attribute of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `app-name` attribute value of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `app-name` attribute description: the app name used when formatting the message in `rfc5424` format. by default the app name is "java".
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/syslog-handler=<syslog-handler name>:read-attribute(name=app-name)`

## syntax of the operation to get the `enabled` attribute of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/syslog-handler=<syslog-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `facility` attribute of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `facility` attribute value of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `facility` attribute description: facility as defined by `rfc-5424` (`http://tools.ietf.org/html/rfc5424)and` `rfc-3164` (`http://tools.ietf.org/html/rfc3164`).
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/syslog-handler=<syslog-handler name>:read-attribute(name=facility)`

## syntax of the operation to get the `hostname` attribute of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `hostname` attribute value of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `hostname` attribute description: the name of the host the messages are being sent from. for example the name of the host the application server is running on.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/syslog-handler=<syslog-handler name>:read-attribute(name=hostname)`

## syntax of the operation to get the `level` attribute of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/syslog-handler=<syslog-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `named-formatter` attribute of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/syslog-handler=<syslog-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `port` attribute of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `port` attribute value of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `port` attribute description: the port the `syslog` server is listening on.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/syslog-handler=<syslog-handler name>:read-attribute(name=port)`

## syntax of the operation to get the `server-address` attribute of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `server-address` attribute value of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `server-address` attribute description: the address of the `syslog` server.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/syslog-handler=<syslog-handler name>:read-attribute(name=server-address)`

## syntax of the operation to get the `syslog-format` attribute of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `syslog-format` attribute value of the `syslog-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `syslog-format` attribute description: formats the log message according to the `rfc` specification.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/syslog-handler=<syslog-handler name>:read-attribute(name=syslog-format)`

## syntax of the operation to get a `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/file-handler=<file-handler name>:read-resource()`
* To get the list of all the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<file-handler name>`.

## syntax of the operation to get the `append` attribute of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `append` attribute value of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `append` attribute description: specify whether to append to the target file.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/file-handler=<file-handler name>:read-attribute(name=append)`

## syntax of the operation to get the `autoflush` attribute of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `autoflush` attribute value of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `autoflush` attribute description: automatically flush after each write.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/file-handler=<file-handler name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `enabled` attribute of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/file-handler=<file-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/file-handler=<file-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `file` attribute of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `file` attribute value of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `file` attribute description: the file description consisting of the path and optional relative to path.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/file-handler=<file-handler name>:read-attribute(name=file)`

## syntax of the operation to get the `filter-spec` attribute of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/file-handler=<file-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `formatter` attribute of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `formatter` attribute value of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `formatter` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/file-handler=<file-handler name>:read-attribute(name=formatter)`

## syntax of the operation to get the `level` attribute of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/file-handler=<file-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `name` attribute of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/file-handler=<file-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `named-formatter` attribute of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/file-handler=<file-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `filter-spec` attribute of the `ROOT` `root-logger` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `ROOT` `root-logger` resource of the `logging-profile` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/root-logger=ROOT:read-attribute(name=filter-spec)`

## syntax of the operation to get the `handlers` attribute of the `ROOT` `root-logger` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `handlers` attribute value of the `ROOT` `root-logger` resource of the `logging-profile` resource of the `logging` subsystem
* `handlers` attribute description: the handlers associated with the root logger.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/root-logger=ROOT:read-attribute(name=handlers)`

## syntax of the operation to get the `level` attribute of the `ROOT` `root-logger` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `ROOT` `root-logger` resource of the `logging-profile` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by the root logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/root-logger=ROOT:read-attribute(name=level)`

## syntax of the operation to get a `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-handler=<custom-handler name>:read-resource()`
* To get the list of all the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<custom-handler name>`.

## syntax of the operation to get the `class` attribute of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `class` attribute value of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `class` attribute description: the logging handler class to be used.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-handler=<custom-handler name>:read-attribute(name=class)`

## syntax of the operation to get the `enabled` attribute of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-handler=<custom-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-handler=<custom-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `filter-spec` attribute of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-handler=<custom-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `formatter` attribute of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `formatter` attribute value of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `formatter` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-handler=<custom-handler name>:read-attribute(name=formatter)`

## syntax of the operation to get the `level` attribute of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-handler=<custom-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `module` attribute of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `module` attribute value of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `module` attribute description: the module that the logging handler depends on.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-handler=<custom-handler name>:read-attribute(name=module)`

## syntax of the operation to get the `name` attribute of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-handler=<custom-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `named-formatter` attribute of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-handler=<custom-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `properties` attribute of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `properties` attribute value of the `custom-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `properties` attribute description: defines the properties used for the logging handler. all properties must be accessible via a setter method.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-handler=<custom-handler name>:read-attribute(name=properties)`

## syntax of the operation to get a `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-resource()`
* To get the list of all the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<size-rotating-file-handler name>`.

## syntax of the operation to get the `append` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `append` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `append` attribute description: specify whether to append to the target file.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=append)`

## syntax of the operation to get the `autoflush` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `autoflush` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `autoflush` attribute description: automatically flush after each write.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `enabled` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `file` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `file` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `file` attribute description: the file description consisting of the path and optional relative to path.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=file)`

## syntax of the operation to get the `filter-spec` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `formatter` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `formatter` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `formatter` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=formatter)`

## syntax of the operation to get the `level` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `max-backup-index` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `max-backup-index` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `max-backup-index` attribute description: the maximum number of backups to keep.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=max-backup-index)`

## syntax of the operation to get the `name` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `named-formatter` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `rotate-on-boot` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `rotate-on-boot` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `rotate-on-boot` attribute description: indicates the file should be rotated each time the file attribute is changed. this always happens when at initialization time.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=rotate-on-boot)`

## syntax of the operation to get the `rotate-size` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `rotate-size` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `rotate-size` attribute description: the size at which to rotate the log file.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=rotate-size)`

## syntax of the operation to get the `suffix` attribute of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `suffix` attribute value of the `size-rotating-file-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `suffix` attribute description: set the suffix string. the string is in a format which can be understood by `java.text.simpledateformat`. the suffix does not determine when the file should be rotated.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=suffix)`

## syntax of the operation to get a `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/console-handler=<console-handler name>:read-resource()`
* To get the list of all the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<console-handler name>`.

## syntax of the operation to get the `autoflush` attribute of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `autoflush` attribute value of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `autoflush` attribute description: automatically flush after each write.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/console-handler=<console-handler name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `enabled` attribute of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/console-handler=<console-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/console-handler=<console-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `filter-spec` attribute of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/console-handler=<console-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `formatter` attribute of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `formatter` attribute value of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `formatter` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/console-handler=<console-handler name>:read-attribute(name=formatter)`

## syntax of the operation to get the `level` attribute of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/console-handler=<console-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `name` attribute of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/console-handler=<console-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `named-formatter` attribute of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/console-handler=<console-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `target` attribute of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `target` attribute value of the `console-handler` resource of the `logging-profile` resource of the `logging` subsystem
* `target` attribute description: defines the target of the console handler. the value can be `system.out`, `system.err` or console.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/console-handler=<console-handler name>:read-attribute(name=target)`

## syntax of the operation to get a `custom-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-formatter=<custom-formatter name>:read-resource()`
* To get the list of all the `custom-formatter` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<custom-formatter name>`.

## syntax of the operation to get the `class` attribute of the `custom-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `class` attribute value of the `custom-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `class` attribute description: the logging formatter class to be used.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-formatter=<custom-formatter name>:read-attribute(name=class)`

## syntax of the operation to get the `module` attribute of the `custom-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `module` attribute value of the `custom-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `module` attribute description: the module that the logging formatter depends on.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-formatter=<custom-formatter name>:read-attribute(name=module)`

## syntax of the operation to get the `properties` attribute of the `custom-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `properties` attribute value of the `custom-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `properties` attribute description: defines the properties used for the logging formatter. all properties must be accessible via a setter method.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/custom-formatter=<custom-formatter name>:read-attribute(name=properties)`

## syntax of the operation to get a `log-file` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/log-file=<log-file name>:read-resource()`
* To get the list of all the `log-file` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<log-file name>`.

## syntax of the operation to get the `file-size` attribute of the `log-file` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `file-size` attribute value of the `log-file` resource of the `logging-profile` resource of the `logging` subsystem
* `file-size` attribute description: the size of the log file in bytes.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/log-file=<log-file name>:read-attribute(name=file-size)`

## syntax of the operation to get the `last-modified-time` attribute of the `log-file` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `last-modified-time` attribute value of the `log-file` resource of the `logging-profile` resource of the `logging` subsystem
* `last-modified-time` attribute description: the date, in milliseconds, the file was last modified.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/log-file=<log-file name>:read-attribute(name=last-modified-time)`

## syntax of the operation to get the `last-modified-timestamp` attribute of the `log-file` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `last-modified-timestamp` attribute value of the `log-file` resource of the `logging-profile` resource of the `logging` subsystem
* `last-modified-timestamp` attribute description: the date, in iso `8601` format, the file was last modified.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/log-file=<log-file name>:read-attribute(name=last-modified-timestamp)`

## syntax of the operation to get the `stream` attribute of the `log-file` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `stream` attribute value of the `log-file` resource of the `logging-profile` resource of the `logging` subsystem
* `stream` attribute description: provides the server log as a response attachment. the response result value is the unique id of the attachment.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/log-file=<log-file name>:read-attribute(name=stream)`

## syntax of the operation to get a `filter` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/filter=<filter name>:read-resource()`
* To get the list of all the `filter` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<filter name>`.

## syntax of the operation to get the `class` attribute of the `filter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `class` attribute value of the `filter` resource of the `logging-profile` resource of the `logging` subsystem
* `class` attribute description: the logging filter class to be used.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/filter=<filter name>:read-attribute(name=class)`

## syntax of the operation to get the `constructor-properties` attribute of the `filter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `constructor-properties` attribute value of the `filter` resource of the `logging-profile` resource of the `logging` subsystem
* `constructor-properties` attribute description: defines the constructor properties used for the logging filter.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/filter=<filter name>:read-attribute(name=constructor-properties)`

## syntax of the operation to get the `module` attribute of the `filter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `module` attribute value of the `filter` resource of the `logging-profile` resource of the `logging` subsystem
* `module` attribute description: the module that the logging filter depends on.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/filter=<filter name>:read-attribute(name=module)`

## syntax of the operation to get the `properties` attribute of the `filter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `properties` attribute value of the `filter` resource of the `logging-profile` resource of the `logging` subsystem
* `properties` attribute description: defines the properties used for the logging filter. all properties must be accessible via a setter method.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/filter=<filter name>:read-attribute(name=properties)`

## syntax of the operation to get a `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/xml-formatter=<xml-formatter name>:read-resource()`
* To get the list of all the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<xml-formatter name>`.

## syntax of the operation to get the `date-format` attribute of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `date-format` attribute value of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `date-format` attribute description: the `date/time` format pattern. the pattern must be a valid `java.time.format.datetimeformatter.ofpattern`() pattern. the default pattern is an `iso-8601` extended offset `date-time` format.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/xml-formatter=<xml-formatter name>:read-attribute(name=date-format)`

## syntax of the operation to get the `exception-output-type` attribute of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `exception-output-type` attribute value of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `exception-output-type` attribute description: indicates how the cause of the logged message, if one is available, will be added to the `xml` output.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/xml-formatter=<xml-formatter name>:read-attribute(name=exception-output-type)`

## syntax of the operation to get the `key-overrides` attribute of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `key-overrides` attribute value of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `key-overrides` attribute description: allows the names of the keys for the `xml` properties to be overridden.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/xml-formatter=<xml-formatter name>:read-attribute(name=key-overrides)`

## syntax of the operation to get the `meta-data` attribute of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `meta-data` attribute value of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `meta-data` attribute description: sets the meta data to use in the `xml` format. properties will be added to each log message.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/xml-formatter=<xml-formatter name>:read-attribute(name=meta-data)`

## syntax of the operation to get the `namespace-uri` attribute of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `namespace-uri` attribute value of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `namespace-uri` attribute description: sets the `namespace` uri used for each record if `print-namespace` attribute is true. note that if no `namespace-uri` is defined and there are overridden keys no `namespace` will be written regardless if the `print-namespace` attribute is set to true.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/xml-formatter=<xml-formatter name>:read-attribute(name=namespace-uri)`

## syntax of the operation to get the `pretty-print` attribute of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `pretty-print` attribute value of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `pretty-print` attribute description: indicates whether or not pretty printing should be used when formatting.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/xml-formatter=<xml-formatter name>:read-attribute(name=pretty-print)`

## syntax of the operation to get the `print-details` attribute of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `print-details` attribute value of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `print-details` attribute description: sets whether or not details should be printed. printing the details can be expensive as the values are retrieved from the caller. the details include the source class name, source file name, source method name, source module name, source module version and source line number.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/xml-formatter=<xml-formatter name>:read-attribute(name=print-details)`

## syntax of the operation to get the `print-namespace` attribute of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `print-namespace` attribute value of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `print-namespace` attribute description: turns on or off the printing of the `namespace` for each <record/>. this is set to false by default.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/xml-formatter=<xml-formatter name>:read-attribute(name=print-namespace)`

## syntax of the operation to get the `record-delimiter` attribute of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `record-delimiter` attribute value of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `record-delimiter` attribute description: the value to be used to indicate the end of a record. if set to null no delimiter will be used at the end of the record. the default value is a line feed.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/xml-formatter=<xml-formatter name>:read-attribute(name=record-delimiter)`

## syntax of the operation to get the `zone-id` attribute of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `zone-id` attribute value of the `xml-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `zone-id` attribute description: the zone id for formatting the date and time. the system default is used if left undefined.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/xml-formatter=<xml-formatter name>:read-attribute(name=zone-id)`

## syntax of the operation to get a `pattern-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/pattern-formatter=<pattern-formatter name>:read-resource()`
* To get the list of all the `pattern-formatter` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<pattern-formatter name>`.

## syntax of the operation to get the `color-map` attribute of the `pattern-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `color-map` attribute value of the `pattern-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `color-map` attribute description: the `color-map` attribute allows for a comma delimited list of colors to be used for different levels with a pattern formatter. the format for the color mapping pattern is `level-name:color-name.valid` levels; severe, fatal, error, warn, warning, info, debug, trace, `config`, fine, finer, finest valid colors; black, green, red, yellow, blue, magenta, cyan, white, `brightblack`, `brightred`, `brightgreen`, `brightblue`, `brightyellow`, `brightmagenta`, `brightcyan`, `brightwhite`
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/pattern-formatter=<pattern-formatter name>:read-attribute(name=color-map)`

## syntax of the operation to get the `pattern` attribute of the `pattern-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `pattern` attribute value of the `pattern-formatter` resource of the `logging-profile` resource of the `logging` subsystem
* `pattern` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/pattern-formatter=<pattern-formatter name>:read-attribute(name=pattern)`

## syntax of the operation to get a `logger` resource of the `logging-profile` resource of the `logging` subsystem
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/logger=<logger name>:read-resource()`
* To get the list of all the `logger` resource of the `logging-profile` resource of the `logging` subsystem use '*' for `<logger name>`.

## syntax of the operation to get the `category` attribute of the `logger` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `category` attribute value of the `logger` resource of the `logging-profile` resource of the `logging` subsystem
* `category` attribute description: specifies the category for the logger.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/logger=<logger name>:read-attribute(name=category)`

## syntax of the operation to get the `filter-spec` attribute of the `logger` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `logger` resource of the `logging-profile` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/logger=<logger name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `handlers` attribute of the `logger` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `handlers` attribute value of the `logger` resource of the `logging-profile` resource of the `logging` subsystem
* `handlers` attribute description: the handlers associated with the logger.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/logger=<logger name>:read-attribute(name=handlers)`

## syntax of the operation to get the `level` attribute of the `logger` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `logger` resource of the `logging-profile` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by the logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/logger=<logger name>:read-attribute(name=level)`

## syntax of the operation to get the `use-parent-handlers` attribute of the `logger` resource of the `logging-profile` resource of the `logging` subsystem
* Retrieve the `use-parent-handlers` attribute value of the `logger` resource of the `logging-profile` resource of the `logging` subsystem
* `use-parent-handlers` attribute description: specifies whether or not this logger should send its output to its parent logger.
* operation: `/subsystem=logging/logging-profile=<logging-profile name>/logger=<logger name>:read-attribute(name=use-parent-handlers)`

## syntax of the operation to get a `custom-handler` resource of the `logging` subsystem
* operation: `/subsystem=logging/custom-handler=<custom-handler name>:read-resource()`
* To get the list of all the `custom-handler` resource of the `logging` subsystem use '*' for `<custom-handler name>`.

## syntax of the operation to get the `class` attribute of the `custom-handler` resource of the `logging` subsystem
* Retrieve the `class` attribute value of the `custom-handler` resource of the `logging` subsystem
* `class` attribute description: the logging handler class to be used.
* operation: `/subsystem=logging/custom-handler=<custom-handler name>:read-attribute(name=class)`

## syntax of the operation to get the `enabled` attribute of the `custom-handler` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `custom-handler` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/custom-handler=<custom-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `custom-handler` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `custom-handler` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/custom-handler=<custom-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `filter` attribute of the `custom-handler` resource of the `logging` subsystem
* Retrieve the `filter` attribute value of the `custom-handler` resource of the `logging` subsystem
* `filter` attribute description: defines a simple filter type.
* operation: `/subsystem=logging/custom-handler=<custom-handler name>:read-attribute(name=filter)`

## syntax of the operation to get the `filter-spec` attribute of the `custom-handler` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `custom-handler` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/custom-handler=<custom-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `formatter` attribute of the `custom-handler` resource of the `logging` subsystem
* Retrieve the `formatter` attribute value of the `custom-handler` resource of the `logging` subsystem
* `formatter` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/custom-handler=<custom-handler name>:read-attribute(name=formatter)`

## syntax of the operation to get the `level` attribute of the `custom-handler` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `custom-handler` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/custom-handler=<custom-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `module` attribute of the `custom-handler` resource of the `logging` subsystem
* Retrieve the `module` attribute value of the `custom-handler` resource of the `logging` subsystem
* `module` attribute description: the module that the logging handler depends on.
* operation: `/subsystem=logging/custom-handler=<custom-handler name>:read-attribute(name=module)`

## syntax of the operation to get the `name` attribute of the `custom-handler` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `custom-handler` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/custom-handler=<custom-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `named-formatter` attribute of the `custom-handler` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `custom-handler` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/custom-handler=<custom-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `properties` attribute of the `custom-handler` resource of the `logging` subsystem
* Retrieve the `properties` attribute value of the `custom-handler` resource of the `logging` subsystem
* `properties` attribute description: defines the properties used for the logging handler. all properties must be accessible via a setter method.
* operation: `/subsystem=logging/custom-handler=<custom-handler name>:read-attribute(name=properties)`

## syntax of the operation to get a `size-rotating-file-handler` resource of the `logging` subsystem
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-resource()`
* To get the list of all the `size-rotating-file-handler` resource of the `logging` subsystem use '*' for `<size-rotating-file-handler name>`.

## syntax of the operation to get the `append` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `append` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `append` attribute description: specify whether to append to the target file.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=append)`

## syntax of the operation to get the `autoflush` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `autoflush` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `autoflush` attribute description: automatically flush after each write.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `enabled` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `file` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `file` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `file` attribute description: the file description consisting of the path and optional relative to path.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=file)`

## syntax of the operation to get the `filter` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `filter` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `filter` attribute description: defines a simple filter type.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=filter)`

## syntax of the operation to get the `filter-spec` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `formatter` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `formatter` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `formatter` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=formatter)`

## syntax of the operation to get the `level` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `max-backup-index` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `max-backup-index` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `max-backup-index` attribute description: the maximum number of backups to keep.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=max-backup-index)`

## syntax of the operation to get the `name` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `named-formatter` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `rotate-on-boot` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `rotate-on-boot` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `rotate-on-boot` attribute description: indicates the file should be rotated each time the file attribute is changed. this always happens when at initialization time.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=rotate-on-boot)`

## syntax of the operation to get the `rotate-size` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `rotate-size` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `rotate-size` attribute description: the size at which to rotate the log file.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=rotate-size)`

## syntax of the operation to get the `suffix` attribute of the `size-rotating-file-handler` resource of the `logging` subsystem
* Retrieve the `suffix` attribute value of the `size-rotating-file-handler` resource of the `logging` subsystem
* `suffix` attribute description: set the suffix string. the string is in a format which can be understood by `java.text.simpledateformat`. the suffix does not determine when the file should be rotated.
* operation: `/subsystem=logging/size-rotating-file-handler=<size-rotating-file-handler name>:read-attribute(name=suffix)`

## syntax of the operation to get a `console-handler` resource of the `logging` subsystem
* operation: `/subsystem=logging/console-handler=<console-handler name>:read-resource()`
* To get the list of all the `console-handler` resource of the `logging` subsystem use '*' for `<console-handler name>`.

## syntax of the operation to get the `autoflush` attribute of the `console-handler` resource of the `logging` subsystem
* Retrieve the `autoflush` attribute value of the `console-handler` resource of the `logging` subsystem
* `autoflush` attribute description: automatically flush after each write.
* operation: `/subsystem=logging/console-handler=<console-handler name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `enabled` attribute of the `console-handler` resource of the `logging` subsystem
* Retrieve the `enabled` attribute value of the `console-handler` resource of the `logging` subsystem
* `enabled` attribute description: if set to true the handler is enabled and functioning as normal, if set to false the handler is ignored when processing log messages.
* operation: `/subsystem=logging/console-handler=<console-handler name>:read-attribute(name=enabled)`

## syntax of the operation to get the `encoding` attribute of the `console-handler` resource of the `logging` subsystem
* Retrieve the `encoding` attribute value of the `console-handler` resource of the `logging` subsystem
* `encoding` attribute description: the character encoding used by this handler.
* operation: `/subsystem=logging/console-handler=<console-handler name>:read-attribute(name=encoding)`

## syntax of the operation to get the `filter` attribute of the `console-handler` resource of the `logging` subsystem
* Retrieve the `filter` attribute value of the `console-handler` resource of the `logging` subsystem
* `filter` attribute description: defines a simple filter type.
* operation: `/subsystem=logging/console-handler=<console-handler name>:read-attribute(name=filter)`

## syntax of the operation to get the `filter-spec` attribute of the `console-handler` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `console-handler` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/console-handler=<console-handler name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `formatter` attribute of the `console-handler` resource of the `logging` subsystem
* Retrieve the `formatter` attribute value of the `console-handler` resource of the `logging` subsystem
* `formatter` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/console-handler=<console-handler name>:read-attribute(name=formatter)`

## syntax of the operation to get the `level` attribute of the `console-handler` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `console-handler` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by this logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/console-handler=<console-handler name>:read-attribute(name=level)`

## syntax of the operation to get the `name` attribute of the `console-handler` resource of the `logging` subsystem
* Retrieve the `name` attribute value of the `console-handler` resource of the `logging` subsystem
* `name` attribute description: the name of the handler.
* operation: `/subsystem=logging/console-handler=<console-handler name>:read-attribute(name=name)`

## syntax of the operation to get the `named-formatter` attribute of the `console-handler` resource of the `logging` subsystem
* Retrieve the `named-formatter` attribute value of the `console-handler` resource of the `logging` subsystem
* `named-formatter` attribute description: the name of the defined formatter to be used on the handler.
* operation: `/subsystem=logging/console-handler=<console-handler name>:read-attribute(name=named-formatter)`

## syntax of the operation to get the `target` attribute of the `console-handler` resource of the `logging` subsystem
* Retrieve the `target` attribute value of the `console-handler` resource of the `logging` subsystem
* `target` attribute description: defines the target of the console handler. the value can be `system.out`, `system.err` or console.
* operation: `/subsystem=logging/console-handler=<console-handler name>:read-attribute(name=target)`

## syntax of the operation to get a `custom-formatter` resource of the `logging` subsystem
* operation: `/subsystem=logging/custom-formatter=<custom-formatter name>:read-resource()`
* To get the list of all the `custom-formatter` resource of the `logging` subsystem use '*' for `<custom-formatter name>`.

## syntax of the operation to get the `class` attribute of the `custom-formatter` resource of the `logging` subsystem
* Retrieve the `class` attribute value of the `custom-formatter` resource of the `logging` subsystem
* `class` attribute description: the logging formatter class to be used.
* operation: `/subsystem=logging/custom-formatter=<custom-formatter name>:read-attribute(name=class)`

## syntax of the operation to get the `module` attribute of the `custom-formatter` resource of the `logging` subsystem
* Retrieve the `module` attribute value of the `custom-formatter` resource of the `logging` subsystem
* `module` attribute description: the module that the logging formatter depends on.
* operation: `/subsystem=logging/custom-formatter=<custom-formatter name>:read-attribute(name=module)`

## syntax of the operation to get the `properties` attribute of the `custom-formatter` resource of the `logging` subsystem
* Retrieve the `properties` attribute value of the `custom-formatter` resource of the `logging` subsystem
* `properties` attribute description: defines the properties used for the logging formatter. all properties must be accessible via a setter method.
* operation: `/subsystem=logging/custom-formatter=<custom-formatter name>:read-attribute(name=properties)`

## syntax of the operation to get a `log-file` resource of the `logging` subsystem
* operation: `/subsystem=logging/log-file=<log-file name>:read-resource()`
* To get the list of all the `log-file` resource of the `logging` subsystem use '*' for `<log-file name>`.

## syntax of the operation to get the `file-size` attribute of the `log-file` resource of the `logging` subsystem
* Retrieve the `file-size` attribute value of the `log-file` resource of the `logging` subsystem
* `file-size` attribute description: the size of the log file in bytes.
* operation: `/subsystem=logging/log-file=<log-file name>:read-attribute(name=file-size)`

## syntax of the operation to get the `last-modified-time` attribute of the `log-file` resource of the `logging` subsystem
* Retrieve the `last-modified-time` attribute value of the `log-file` resource of the `logging` subsystem
* `last-modified-time` attribute description: the date, in milliseconds, the file was last modified.
* operation: `/subsystem=logging/log-file=<log-file name>:read-attribute(name=last-modified-time)`

## syntax of the operation to get the `last-modified-timestamp` attribute of the `log-file` resource of the `logging` subsystem
* Retrieve the `last-modified-timestamp` attribute value of the `log-file` resource of the `logging` subsystem
* `last-modified-timestamp` attribute description: the date, in iso `8601` format, the file was last modified.
* operation: `/subsystem=logging/log-file=<log-file name>:read-attribute(name=last-modified-timestamp)`

## syntax of the operation to get the `stream` attribute of the `log-file` resource of the `logging` subsystem
* Retrieve the `stream` attribute value of the `log-file` resource of the `logging` subsystem
* `stream` attribute description: provides the server log as a response attachment. the response result value is the unique id of the attachment.
* operation: `/subsystem=logging/log-file=<log-file name>:read-attribute(name=stream)`

## syntax of the operation to get a `filter` resource of the `logging` subsystem
* operation: `/subsystem=logging/filter=<filter name>:read-resource()`
* To get the list of all the `filter` resource of the `logging` subsystem use '*' for `<filter name>`.

## syntax of the operation to get the `class` attribute of the `filter` resource of the `logging` subsystem
* Retrieve the `class` attribute value of the `filter` resource of the `logging` subsystem
* `class` attribute description: the logging filter class to be used.
* operation: `/subsystem=logging/filter=<filter name>:read-attribute(name=class)`

## syntax of the operation to get the `constructor-properties` attribute of the `filter` resource of the `logging` subsystem
* Retrieve the `constructor-properties` attribute value of the `filter` resource of the `logging` subsystem
* `constructor-properties` attribute description: defines the constructor properties used for the logging filter.
* operation: `/subsystem=logging/filter=<filter name>:read-attribute(name=constructor-properties)`

## syntax of the operation to get the `module` attribute of the `filter` resource of the `logging` subsystem
* Retrieve the `module` attribute value of the `filter` resource of the `logging` subsystem
* `module` attribute description: the module that the logging filter depends on.
* operation: `/subsystem=logging/filter=<filter name>:read-attribute(name=module)`

## syntax of the operation to get the `properties` attribute of the `filter` resource of the `logging` subsystem
* Retrieve the `properties` attribute value of the `filter` resource of the `logging` subsystem
* `properties` attribute description: defines the properties used for the logging filter. all properties must be accessible via a setter method.
* operation: `/subsystem=logging/filter=<filter name>:read-attribute(name=properties)`

## syntax of the operation to get a `xml-formatter` resource of the `logging` subsystem
* operation: `/subsystem=logging/xml-formatter=<xml-formatter name>:read-resource()`
* To get the list of all the `xml-formatter` resource of the `logging` subsystem use '*' for `<xml-formatter name>`.

## syntax of the operation to get the `date-format` attribute of the `xml-formatter` resource of the `logging` subsystem
* Retrieve the `date-format` attribute value of the `xml-formatter` resource of the `logging` subsystem
* `date-format` attribute description: the `date/time` format pattern. the pattern must be a valid `java.time.format.datetimeformatter.ofpattern`() pattern. the default pattern is an `iso-8601` extended offset `date-time` format.
* operation: `/subsystem=logging/xml-formatter=<xml-formatter name>:read-attribute(name=date-format)`

## syntax of the operation to get the `exception-output-type` attribute of the `xml-formatter` resource of the `logging` subsystem
* Retrieve the `exception-output-type` attribute value of the `xml-formatter` resource of the `logging` subsystem
* `exception-output-type` attribute description: indicates how the cause of the logged message, if one is available, will be added to the `xml` output.
* operation: `/subsystem=logging/xml-formatter=<xml-formatter name>:read-attribute(name=exception-output-type)`

## syntax of the operation to get the `key-overrides` attribute of the `xml-formatter` resource of the `logging` subsystem
* Retrieve the `key-overrides` attribute value of the `xml-formatter` resource of the `logging` subsystem
* `key-overrides` attribute description: allows the names of the keys for the `xml` properties to be overridden.
* operation: `/subsystem=logging/xml-formatter=<xml-formatter name>:read-attribute(name=key-overrides)`

## syntax of the operation to get the `meta-data` attribute of the `xml-formatter` resource of the `logging` subsystem
* Retrieve the `meta-data` attribute value of the `xml-formatter` resource of the `logging` subsystem
* `meta-data` attribute description: sets the meta data to use in the `xml` format. properties will be added to each log message.
* operation: `/subsystem=logging/xml-formatter=<xml-formatter name>:read-attribute(name=meta-data)`

## syntax of the operation to get the `namespace-uri` attribute of the `xml-formatter` resource of the `logging` subsystem
* Retrieve the `namespace-uri` attribute value of the `xml-formatter` resource of the `logging` subsystem
* `namespace-uri` attribute description: sets the `namespace` uri used for each record if `print-namespace` attribute is true. note that if no `namespace-uri` is defined and there are overridden keys no `namespace` will be written regardless if the `print-namespace` attribute is set to true.
* operation: `/subsystem=logging/xml-formatter=<xml-formatter name>:read-attribute(name=namespace-uri)`

## syntax of the operation to get the `pretty-print` attribute of the `xml-formatter` resource of the `logging` subsystem
* Retrieve the `pretty-print` attribute value of the `xml-formatter` resource of the `logging` subsystem
* `pretty-print` attribute description: indicates whether or not pretty printing should be used when formatting.
* operation: `/subsystem=logging/xml-formatter=<xml-formatter name>:read-attribute(name=pretty-print)`

## syntax of the operation to get the `print-details` attribute of the `xml-formatter` resource of the `logging` subsystem
* Retrieve the `print-details` attribute value of the `xml-formatter` resource of the `logging` subsystem
* `print-details` attribute description: sets whether or not details should be printed. printing the details can be expensive as the values are retrieved from the caller. the details include the source class name, source file name, source method name, source module name, source module version and source line number.
* operation: `/subsystem=logging/xml-formatter=<xml-formatter name>:read-attribute(name=print-details)`

## syntax of the operation to get the `print-namespace` attribute of the `xml-formatter` resource of the `logging` subsystem
* Retrieve the `print-namespace` attribute value of the `xml-formatter` resource of the `logging` subsystem
* `print-namespace` attribute description: turns on or off the printing of the `namespace` for each <record/>. this is set to false by default.
* operation: `/subsystem=logging/xml-formatter=<xml-formatter name>:read-attribute(name=print-namespace)`

## syntax of the operation to get the `record-delimiter` attribute of the `xml-formatter` resource of the `logging` subsystem
* Retrieve the `record-delimiter` attribute value of the `xml-formatter` resource of the `logging` subsystem
* `record-delimiter` attribute description: the value to be used to indicate the end of a record. if set to null no delimiter will be used at the end of the record. the default value is a line feed.
* operation: `/subsystem=logging/xml-formatter=<xml-formatter name>:read-attribute(name=record-delimiter)`

## syntax of the operation to get the `zone-id` attribute of the `xml-formatter` resource of the `logging` subsystem
* Retrieve the `zone-id` attribute value of the `xml-formatter` resource of the `logging` subsystem
* `zone-id` attribute description: the zone id for formatting the date and time. the system default is used if left undefined.
* operation: `/subsystem=logging/xml-formatter=<xml-formatter name>:read-attribute(name=zone-id)`

## syntax of the operation to get a `pattern-formatter` resource of the `logging` subsystem
* operation: `/subsystem=logging/pattern-formatter=<pattern-formatter name>:read-resource()`
* To get the list of all the `pattern-formatter` resource of the `logging` subsystem use '*' for `<pattern-formatter name>`.

## syntax of the operation to get the `color-map` attribute of the `pattern-formatter` resource of the `logging` subsystem
* Retrieve the `color-map` attribute value of the `pattern-formatter` resource of the `logging` subsystem
* `color-map` attribute description: the `color-map` attribute allows for a comma delimited list of colors to be used for different levels with a pattern formatter. the format for the color mapping pattern is `level-name:color-name.valid` levels; severe, fatal, error, warn, warning, info, debug, trace, `config`, fine, finer, finest valid colors; black, green, red, yellow, blue, magenta, cyan, white, `brightblack`, `brightred`, `brightgreen`, `brightblue`, `brightyellow`, `brightmagenta`, `brightcyan`, `brightwhite`
* operation: `/subsystem=logging/pattern-formatter=<pattern-formatter name>:read-attribute(name=color-map)`

## syntax of the operation to get the `pattern` attribute of the `pattern-formatter` resource of the `logging` subsystem
* Retrieve the `pattern` attribute value of the `pattern-formatter` resource of the `logging` subsystem
* `pattern` attribute description: defines a pattern for the formatter.
* operation: `/subsystem=logging/pattern-formatter=<pattern-formatter name>:read-attribute(name=pattern)`

## syntax of the operation to get a `logger` resource of the `logging` subsystem
* operation: `/subsystem=logging/logger=<logger name>:read-resource()`
* To get the list of all the `logger` resource of the `logging` subsystem use '*' for `<logger name>`.

## syntax of the operation to get the `category` attribute of the `logger` resource of the `logging` subsystem
* Retrieve the `category` attribute value of the `logger` resource of the `logging` subsystem
* `category` attribute description: specifies the category for the logger.
* operation: `/subsystem=logging/logger=<logger name>:read-attribute(name=category)`

## syntax of the operation to get the `filter` attribute of the `logger` resource of the `logging` subsystem
* Retrieve the `filter` attribute value of the `logger` resource of the `logging` subsystem
* `filter` attribute description: defines a simple filter type.
* operation: `/subsystem=logging/logger=<logger name>:read-attribute(name=filter)`

## syntax of the operation to get the `filter-spec` attribute of the `logger` resource of the `logging` subsystem
* Retrieve the `filter-spec` attribute value of the `logger` resource of the `logging` subsystem
* `filter-spec` attribute description: a filter expression value to define a filter. example for a filter that does not match a pattern: `not(match("jbas`.*"))
* operation: `/subsystem=logging/logger=<logger name>:read-attribute(name=filter-spec)`

## syntax of the operation to get the `handlers` attribute of the `logger` resource of the `logging` subsystem
* Retrieve the `handlers` attribute value of the `logger` resource of the `logging` subsystem
* `handlers` attribute description: the handlers associated with the logger.
* operation: `/subsystem=logging/logger=<logger name>:read-attribute(name=handlers)`

## syntax of the operation to get the `level` attribute of the `logger` resource of the `logging` subsystem
* Retrieve the `level` attribute value of the `logger` resource of the `logging` subsystem
* `level` attribute description: the log level specifying which message levels will be logged by the logger. message levels lower than this value will be discarded.
* operation: `/subsystem=logging/logger=<logger name>:read-attribute(name=level)`

## syntax of the operation to get the `use-parent-handlers` attribute of the `logger` resource of the `logging` subsystem
* Retrieve the `use-parent-handlers` attribute value of the `logger` resource of the `logging` subsystem
* `use-parent-handlers` attribute description: specifies whether or not this logger should send its output to its parent logger.
* operation: `/subsystem=logging/logger=<logger name>:read-attribute(name=use-parent-handlers)`

## syntax of the operation to get a `binding` resource of the `naming` subsystem
* operation: `/subsystem=naming/binding=<binding name>:read-resource()`
* To get the list of all the `binding` resource of the `naming` subsystem use '*' for `<binding name>`.

## syntax of the operation to get the `binding-type` attribute of the `binding` resource of the `naming` subsystem
* Retrieve the `binding-type` attribute value of the `binding` resource of the `naming` subsystem
* `binding-type` attribute description: the type of binding to create, may be simple, lookup, `external-context` or `object-factory`
* operation: `/subsystem=naming/binding=<binding name>:read-attribute(name=binding-type)`

## syntax of the operation to get the `cache` attribute of the `binding` resource of the `naming` subsystem
* Retrieve the `cache` attribute value of the `binding` resource of the `naming` subsystem
* `cache` attribute description: if the external context should be cached
* operation: `/subsystem=naming/binding=<binding name>:read-attribute(name=cache)`

## syntax of the operation to get the `class` attribute of the `binding` resource of the `naming` subsystem
* Retrieve the `class` attribute value of the `binding` resource of the `naming` subsystem
* `class` attribute description: the object factory class name for object factory bindings
* operation: `/subsystem=naming/binding=<binding name>:read-attribute(name=class)`

## syntax of the operation to get the `environment` attribute of the `binding` resource of the `naming` subsystem
* Retrieve the `environment` attribute value of the `binding` resource of the `naming` subsystem
* `environment` attribute description: the environment to use on object factory instance retrieval
* operation: `/subsystem=naming/binding=<binding name>:read-attribute(name=environment)`

## syntax of the operation to get the `lookup` attribute of the `binding` resource of the `naming` subsystem
* Retrieve the `lookup` attribute value of the `binding` resource of the `naming` subsystem
* `lookup` attribute description: the entry to lookup in `jndi` for lookup bindings
* operation: `/subsystem=naming/binding=<binding name>:read-attribute(name=lookup)`

## syntax of the operation to get the `module` attribute of the `binding` resource of the `naming` subsystem
* Retrieve the `module` attribute value of the `binding` resource of the `naming` subsystem
* `module` attribute description: the module to load the object factory from for object factory bindings
* operation: `/subsystem=naming/binding=<binding name>:read-attribute(name=module)`

## syntax of the operation to get the `type` attribute of the `binding` resource of the `naming` subsystem
* Retrieve the `type` attribute value of the `binding` resource of the `naming` subsystem
* `type` attribute description: the type of the value to bind for simple bindings, this must be a primitive type
* operation: `/subsystem=naming/binding=<binding name>:read-attribute(name=type)`

## syntax of the operation to get the `value` attribute of the `binding` resource of the `naming` subsystem
* Retrieve the `value` attribute value of the `binding` resource of the `naming` subsystem
* `value` attribute description: the value to bind for simple bindings
* operation: `/subsystem=naming/binding=<binding name>:read-attribute(name=value)`

## syntax of the operation to get the `modify-wsdl-address` attribute of the `webservices` subsystem
* Retrieve the `modify-wsdl-address` attribute value of the `webservices` subsystem
* `modify-wsdl-address` attribute description: whether the soap address can be modified.
* operation: `/subsystem=webservices:read-attribute(name=modify-wsdl-address)`

## syntax of the operation to get the `statistics-enabled` attribute of the `webservices` subsystem
* Retrieve the `statistics-enabled` attribute value of the `webservices` subsystem
* `statistics-enabled` attribute description: whether statistics are to be gathered for endpoints, default value is 'false'.
* operation: `/subsystem=webservices:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `wsdl-host` attribute of the `webservices` subsystem
* Retrieve the `wsdl-host` attribute value of the `webservices` subsystem
* `wsdl-host` attribute description: the `wsdl`, that is a required deployment artifact for an endpoint, has a <`soap:address`> element which points to the location of the endpoint. `jboss` supports rewriting of that soap address. if the content of <`soap:address`> is a valid `url`, `jbossws` will not rewrite it unless '`modify-wsdl-address`' is true. if the content of <`soap:address`> is not a valid `url`, `jbossws` will rewrite it using the attribute values given below. if '`wsdl-host`' is set to '`jbossws.undefined.host`', `jbossws` uses requesters host when rewriting the <`soap:address`>
* operation: `/subsystem=webservices:read-attribute(name=wsdl-host)`

## syntax of the operation to get the `wsdl-path-rewrite-rule` attribute of the `webservices` subsystem
* Retrieve the `wsdl-path-rewrite-rule` attribute value of the `webservices` subsystem
* `wsdl-path-rewrite-rule` attribute description: this string defines a sed substitution command (`e.g`., '`s/regexp/replacement/g`') that `jbossws` executes against the path component of each <`soap:address`> `url` published from the server. when `wsdl-path-rewrite-rule` is not defined, `jbossws` retains the original path component of each <`soap:address`> `url`. when '`modify-wsdl-address`' is set to "false" this element is ignored.
* operation: `/subsystem=webservices:read-attribute(name=wsdl-path-rewrite-rule)`

## syntax of the operation to get the `wsdl-port` attribute of the `webservices` subsystem
* Retrieve the `wsdl-port` attribute value of the `webservices` subsystem
* `wsdl-port` attribute description: the `non-secure` port that will be used for rewriting the soap address. if absent the port will be identified by querying the list of installed connectors.
* operation: `/subsystem=webservices:read-attribute(name=wsdl-port)`

## syntax of the operation to get the `wsdl-secure-port` attribute of the `webservices` subsystem
* Retrieve the `wsdl-secure-port` attribute value of the `webservices` subsystem
* `wsdl-secure-port` attribute description: the secure port that will be used for rewriting the soap address. if absent the port will be identified by querying the list of installed connectors.
* operation: `/subsystem=webservices:read-attribute(name=wsdl-secure-port)`

## syntax of the operation to get the `wsdl-uri-scheme` attribute of the `webservices` subsystem
* Retrieve the `wsdl-uri-scheme` attribute value of the `webservices` subsystem
* `wsdl-uri-scheme` attribute description: the uri scheme to use for rewriting <`soap:address`> . valid values are '`http`' and '`https`'. this configuration overrides scheme computed by processing the endpoint (even if a transport guarantee is specified). the provided values for '`wsdl-port`' and '`wsdl-secure-port`' (or their default values) are used depending on specified scheme.
* operation: `/subsystem=webservices:read-attribute(name=wsdl-uri-scheme)`

## syntax of the operation to get a `client-config` resource of the `webservices` subsystem
* operation: `/subsystem=webservices/client-config=<client-config name>:read-resource()`
* To get the list of all the `client-config` resource of the `webservices` subsystem use '*' for `<client-config name>`.

## syntax of the operation to get a `pre-handler-chain` resource of the `client-config` resource of the `webservices` subsystem
* operation: `/subsystem=webservices/client-config=<client-config name>/pre-handler-chain=<pre-handler-chain name>:read-resource()`
* To get the list of all the `pre-handler-chain` resource of the `client-config` resource of the `webservices` subsystem use '*' for `<pre-handler-chain name>`.

## syntax of the operation to get the `protocol-bindings` attribute of the `pre-handler-chain` resource of the `client-config` resource of the `webservices` subsystem
* Retrieve the `protocol-bindings` attribute value of the `pre-handler-chain` resource of the `client-config` resource of the `webservices` subsystem
* `protocol-bindings` attribute description: protocol binding
* operation: `/subsystem=webservices/client-config=<client-config name>/pre-handler-chain=<pre-handler-chain name>:read-attribute(name=protocol-bindings)`

## syntax of the operation to get a `handler` resource of the `pre-handler-chain` resource of the `client-config` resource of the `webservices` subsystem
* operation: `/subsystem=webservices/client-config=<client-config name>/pre-handler-chain=<pre-handler-chain name>/handler=<handler name>:read-resource()`
* To get the list of all the `handler` resource of the `pre-handler-chain` resource of the `client-config` resource of the `webservices` subsystem use '*' for `<handler name>`.

## syntax of the operation to get the `class` attribute of the `handler` resource of the `pre-handler-chain` resource of the `client-config` resource of the `webservices` subsystem
* Retrieve the `class` attribute value of the `handler` resource of the `pre-handler-chain` resource of the `client-config` resource of the `webservices` subsystem
* `class` attribute description: handler class
* operation: `/subsystem=webservices/client-config=<client-config name>/pre-handler-chain=<pre-handler-chain name>/handler=<handler name>:read-attribute(name=class)`

## syntax of the operation to get a `post-handler-chain` resource of the `client-config` resource of the `webservices` subsystem
* operation: `/subsystem=webservices/client-config=<client-config name>/post-handler-chain=<post-handler-chain name>:read-resource()`
* To get the list of all the `post-handler-chain` resource of the `client-config` resource of the `webservices` subsystem use '*' for `<post-handler-chain name>`.

## syntax of the operation to get the `protocol-bindings` attribute of the `post-handler-chain` resource of the `client-config` resource of the `webservices` subsystem
* Retrieve the `protocol-bindings` attribute value of the `post-handler-chain` resource of the `client-config` resource of the `webservices` subsystem
* `protocol-bindings` attribute description: protocol binding
* operation: `/subsystem=webservices/client-config=<client-config name>/post-handler-chain=<post-handler-chain name>:read-attribute(name=protocol-bindings)`

## syntax of the operation to get a `handler` resource of the `post-handler-chain` resource of the `client-config` resource of the `webservices` subsystem
* operation: `/subsystem=webservices/client-config=<client-config name>/post-handler-chain=<post-handler-chain name>/handler=<handler name>:read-resource()`
* To get the list of all the `handler` resource of the `post-handler-chain` resource of the `client-config` resource of the `webservices` subsystem use '*' for `<handler name>`.

## syntax of the operation to get the `class` attribute of the `handler` resource of the `post-handler-chain` resource of the `client-config` resource of the `webservices` subsystem
* Retrieve the `class` attribute value of the `handler` resource of the `post-handler-chain` resource of the `client-config` resource of the `webservices` subsystem
* `class` attribute description: handler class
* operation: `/subsystem=webservices/client-config=<client-config name>/post-handler-chain=<post-handler-chain name>/handler=<handler name>:read-attribute(name=class)`

## syntax of the operation to get a `property` resource of the `client-config` resource of the `webservices` subsystem
* operation: `/subsystem=webservices/client-config=<client-config name>/property=<property name>:read-resource()`
* To get the list of all the `property` resource of the `client-config` resource of the `webservices` subsystem use '*' for `<property name>`.

## syntax of the operation to get the `value` attribute of the `property` resource of the `client-config` resource of the `webservices` subsystem
* Retrieve the `value` attribute value of the `property` resource of the `client-config` resource of the `webservices` subsystem
* `value` attribute description: endpoint configuration property value
* operation: `/subsystem=webservices/client-config=<client-config name>/property=<property name>:read-attribute(name=value)`

## syntax of the operation to get a `endpoint-config` resource of the `webservices` subsystem
* operation: `/subsystem=webservices/endpoint-config=<endpoint-config name>:read-resource()`
* To get the list of all the `endpoint-config` resource of the `webservices` subsystem use '*' for `<endpoint-config name>`.

## syntax of the operation to get a `pre-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem
* operation: `/subsystem=webservices/endpoint-config=<endpoint-config name>/pre-handler-chain=<pre-handler-chain name>:read-resource()`
* To get the list of all the `pre-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem use '*' for `<pre-handler-chain name>`.

## syntax of the operation to get the `protocol-bindings` attribute of the `pre-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem
* Retrieve the `protocol-bindings` attribute value of the `pre-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem
* `protocol-bindings` attribute description: protocol binding
* operation: `/subsystem=webservices/endpoint-config=<endpoint-config name>/pre-handler-chain=<pre-handler-chain name>:read-attribute(name=protocol-bindings)`

## syntax of the operation to get a `handler` resource of the `pre-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem
* operation: `/subsystem=webservices/endpoint-config=<endpoint-config name>/pre-handler-chain=<pre-handler-chain name>/handler=<handler name>:read-resource()`
* To get the list of all the `handler` resource of the `pre-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem use '*' for `<handler name>`.

## syntax of the operation to get the `class` attribute of the `handler` resource of the `pre-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem
* Retrieve the `class` attribute value of the `handler` resource of the `pre-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem
* `class` attribute description: handler class
* operation: `/subsystem=webservices/endpoint-config=<endpoint-config name>/pre-handler-chain=<pre-handler-chain name>/handler=<handler name>:read-attribute(name=class)`

## syntax of the operation to get a `post-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem
* operation: `/subsystem=webservices/endpoint-config=<endpoint-config name>/post-handler-chain=<post-handler-chain name>:read-resource()`
* To get the list of all the `post-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem use '*' for `<post-handler-chain name>`.

## syntax of the operation to get the `protocol-bindings` attribute of the `post-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem
* Retrieve the `protocol-bindings` attribute value of the `post-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem
* `protocol-bindings` attribute description: protocol binding
* operation: `/subsystem=webservices/endpoint-config=<endpoint-config name>/post-handler-chain=<post-handler-chain name>:read-attribute(name=protocol-bindings)`

## syntax of the operation to get a `handler` resource of the `post-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem
* operation: `/subsystem=webservices/endpoint-config=<endpoint-config name>/post-handler-chain=<post-handler-chain name>/handler=<handler name>:read-resource()`
* To get the list of all the `handler` resource of the `post-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem use '*' for `<handler name>`.

## syntax of the operation to get the `class` attribute of the `handler` resource of the `post-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem
* Retrieve the `class` attribute value of the `handler` resource of the `post-handler-chain` resource of the `endpoint-config` resource of the `webservices` subsystem
* `class` attribute description: handler class
* operation: `/subsystem=webservices/endpoint-config=<endpoint-config name>/post-handler-chain=<post-handler-chain name>/handler=<handler name>:read-attribute(name=class)`

## syntax of the operation to get a `property` resource of the `endpoint-config` resource of the `webservices` subsystem
* operation: `/subsystem=webservices/endpoint-config=<endpoint-config name>/property=<property name>:read-resource()`
* To get the list of all the `property` resource of the `endpoint-config` resource of the `webservices` subsystem use '*' for `<property name>`.

## syntax of the operation to get the `value` attribute of the `property` resource of the `endpoint-config` resource of the `webservices` subsystem
* Retrieve the `value` attribute value of the `property` resource of the `endpoint-config` resource of the `webservices` subsystem
* `value` attribute description: endpoint configuration property value
* operation: `/subsystem=webservices/endpoint-config=<endpoint-config name>/property=<property name>:read-attribute(name=value)`

## syntax of the operation to get the `endpoint` attribute of the `micrometer` subsystem
* Retrieve the `endpoint` attribute value of the `micrometer` subsystem
* `endpoint` attribute description: the `url` to which metrics will be published.
* operation: `/subsystem=micrometer:read-attribute(name=endpoint)`

## syntax of the operation to get the `exposed-subsystems` attribute of the `micrometer` subsystem
* Retrieve the `exposed-subsystems` attribute value of the `micrometer` subsystem
* `exposed-subsystems` attribute description: the names of the `wildfly` subsystems that exposes their metrics (or '*' to expose any subsystem metrics).
* operation: `/subsystem=micrometer:read-attribute(name=exposed-subsystems)`

## syntax of the operation to get the `step` attribute of the `micrometer` subsystem
* Retrieve the `step` attribute value of the `micrometer` subsystem
* `step` attribute description: the step size (reporting frequency) to use in seconds.
* operation: `/subsystem=micrometer:read-attribute(name=step)`

## syntax of the operation to get the `context` attribute of the `prometheus` `registry` resource of the `micrometer` subsystem
* Retrieve the `context` attribute value of the `prometheus` `registry` resource of the `micrometer` subsystem
* `context` attribute description: the context root from which to serve prometheus metrics (`e.g`., "/metrics" or "/prometheus"). note: prometheus is not supported on host controllers.
* operation: `/subsystem=micrometer/registry=prometheus:read-attribute(name=context)`

## syntax of the operation to get the `security-enabled` attribute of the `prometheus` `registry` resource of the `micrometer` subsystem
* Retrieve the `security-enabled` attribute value of the `prometheus` `registry` resource of the `micrometer` subsystem
* `security-enabled` attribute description: whether the context should be secured
* operation: `/subsystem=micrometer/registry=prometheus:read-attribute(name=security-enabled)`

## syntax of the operation to get the `endpoint` attribute of the `otlp` `registry` resource of the `micrometer` subsystem
* Retrieve the `endpoint` attribute value of the `otlp` `registry` resource of the `micrometer` subsystem
* `endpoint` attribute description: the `url` to which metrics will be published.
* operation: `/subsystem=micrometer/registry=otlp:read-attribute(name=endpoint)`

## syntax of the operation to get the `step` attribute of the `otlp` `registry` resource of the `micrometer` subsystem
* Retrieve the `step` attribute value of the `otlp` `registry` resource of the `micrometer` subsystem
* `step` attribute description: the step size (reporting frequency) to use in seconds.
* operation: `/subsystem=micrometer/registry=otlp:read-attribute(name=step)`

## syntax of the operation to get the `async-registration` attribute of the `xts` subsystem
* Retrieve the `async-registration` attribute value of the `xts` subsystem
* `async-registration` attribute description: initialize endpoints for asynchronous registration needed for `ws-at` .net integration.
* operation: `/subsystem=xts:read-attribute(name=async-registration)`

## syntax of the operation to get the `default-context-propagation` attribute of the `xts` subsystem
* Retrieve the `default-context-propagation` attribute value of the `xts` subsystem
* `default-context-propagation` attribute description: automatically sets up client side handlers.
* operation: `/subsystem=xts:read-attribute(name=default-context-propagation)`

## syntax of the operation to get the `host` attribute of the `xts` subsystem
* Retrieve the `host` attribute value of the `xts` subsystem
* `host` attribute description: configuration of the host used by the `xts` subsystem.
* operation: `/subsystem=xts:read-attribute(name=host)`

## syntax of the operation to get the `url` attribute of the `xts` subsystem
* Retrieve the `url` attribute value of the `xts` subsystem
* `url` attribute description: if set configures a remote coordinator service to be used when an `xts` client start a transaction.
* operation: `/subsystem=xts:read-attribute(name=url)`

## syntax of the operation to get the `xts-environment` attribute of the `xts` subsystem
* Retrieve the `xts-environment` attribute value of the `xts` subsystem
* `xts-environment` attribute description: the `xts` environment configuration.
* operation: `/subsystem=xts:read-attribute(name=xts-environment)`

## syntax of the operation to get a `scanner` resource of the `deployment-scanner` subsystem
* operation: `/subsystem=deployment-scanner/scanner=<scanner name>:read-resource()`
* To get the list of all the `scanner` resource of the `deployment-scanner` subsystem use '*' for `<scanner name>`.

## syntax of the operation to get the `auto-deploy-exploded` attribute of the `scanner` resource of the `deployment-scanner` subsystem
* Retrieve the `auto-deploy-exploded` attribute value of the `scanner` resource of the `deployment-scanner` subsystem
* `auto-deploy-exploded` attribute description: allows the automatic deployment of exploded content without requiring a .`dodeploy` marker file. recommended for only basic development scenarios to prevent exploded application deployment from occurring during changes by the developer or operating system.
* operation: `/subsystem=deployment-scanner/scanner=<scanner name>:read-attribute(name=auto-deploy-exploded)`

## syntax of the operation to get the `auto-deploy-xml` attribute of the `scanner` resource of the `deployment-scanner` subsystem
* Retrieve the `auto-deploy-xml` attribute value of the `scanner` resource of the `deployment-scanner` subsystem
* `auto-deploy-xml` attribute description: allows automatic deployment of `xml` content without requiring a .`dodeploy` marker file.
* operation: `/subsystem=deployment-scanner/scanner=<scanner name>:read-attribute(name=auto-deploy-xml)`

## syntax of the operation to get the `auto-deploy-zipped` attribute of the `scanner` resource of the `deployment-scanner` subsystem
* Retrieve the `auto-deploy-zipped` attribute value of the `scanner` resource of the `deployment-scanner` subsystem
* `auto-deploy-zipped` attribute description: allows automatic deployment of zipped content without requiring a .`dodeploy` marker file.
* operation: `/subsystem=deployment-scanner/scanner=<scanner name>:read-attribute(name=auto-deploy-zipped)`

## syntax of the operation to get the `deployment-timeout` attribute of the `scanner` resource of the `deployment-scanner` subsystem
* Retrieve the `deployment-timeout` attribute value of the `scanner` resource of the `deployment-scanner` subsystem
* `deployment-timeout` attribute description: the time value in seconds for the deployment scanner to allow a deployment attempt before being cancelled.
* operation: `/subsystem=deployment-scanner/scanner=<scanner name>:read-attribute(name=deployment-timeout)`

## syntax of the operation to get the `path` attribute of the `scanner` resource of the `deployment-scanner` subsystem
* Retrieve the `path` attribute value of the `scanner` resource of the `deployment-scanner` subsystem
* `path` attribute description: the actual `filesystem` path to be scanned. treated as an absolute path, unless the '`relative-to`' attribute is specified, in which case the value is treated as relative to that path.
* operation: `/subsystem=deployment-scanner/scanner=<scanner name>:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `scanner` resource of the `deployment-scanner` subsystem
* Retrieve the `relative-to` attribute value of the `scanner` resource of the `deployment-scanner` subsystem
* `relative-to` attribute description: reference to a `filesystem` path defined in the "paths" section of the server configuration.
* operation: `/subsystem=deployment-scanner/scanner=<scanner name>:read-attribute(name=relative-to)`

## syntax of the operation to get the `runtime-failure-causes-rollback` attribute of the `scanner` resource of the `deployment-scanner` subsystem
* Retrieve the `runtime-failure-causes-rollback` attribute value of the `scanner` resource of the `deployment-scanner` subsystem
* `runtime-failure-causes-rollback` attribute description: flag indicating whether a runtime failure of a deployment causes a rollback of the deployment as well as all other (maybe unrelated) deployments as part of the scan operation.
* operation: `/subsystem=deployment-scanner/scanner=<scanner name>:read-attribute(name=runtime-failure-causes-rollback)`

## syntax of the operation to get the `scan-enabled` attribute of the `scanner` resource of the `deployment-scanner` subsystem
* Retrieve the `scan-enabled` attribute value of the `scanner` resource of the `deployment-scanner` subsystem
* `scan-enabled` attribute description: flag indicating if all scanning (including initial scanning at startup) is enabled.
* operation: `/subsystem=deployment-scanner/scanner=<scanner name>:read-attribute(name=scan-enabled)`

## syntax of the operation to get the `scan-interval` attribute of the `scanner` resource of the `deployment-scanner` subsystem
* Retrieve the `scan-interval` attribute value of the `scanner` resource of the `deployment-scanner` subsystem
* `scan-interval` attribute description: periodic interval, in milliseconds, at which the repository should be scanned for changes. a value of less than `1` indicates the repository should only be scanned at initial startup.
* operation: `/subsystem=deployment-scanner/scanner=<scanner name>:read-attribute(name=scan-interval)`

## syntax of the operation to get the `host` attribute of the `rts` subsystem
* Retrieve the `host` attribute value of the `rts` subsystem
* `host` attribute description: name of the host which should be used to deploy `servlets`
* operation: `/subsystem=rts:read-attribute(name=host)`

## syntax of the operation to get the `server` attribute of the `rts` subsystem
* Retrieve the `server` attribute value of the `rts` subsystem
* `server` attribute description: name of the server which should be used to deploy `servlets`
* operation: `/subsystem=rts:read-attribute(name=server)`

## syntax of the operation to get the `socket-binding` attribute of the `rts` subsystem
* Retrieve the `socket-binding` attribute value of the `rts` subsystem
* `socket-binding` attribute description: name of the socket binding which should be used
* operation: `/subsystem=rts:read-attribute(name=socket-binding)`

## syntax of the operation to get the `default-session-management` attribute of the `distributable-web` subsystem
* Retrieve the `default-session-management` attribute value of the `distributable-web` subsystem
* `default-session-management` attribute description: the default distributable session management provider of the server
* operation: `/subsystem=distributable-web:read-attribute(name=default-session-management)`

## syntax of the operation to get the `default-single-sign-on-management` attribute of the `distributable-web` subsystem
* Retrieve the `default-single-sign-on-management` attribute value of the `distributable-web` subsystem
* `default-single-sign-on-management` attribute description: the default distributable single `sign-on` management provider of the server
* operation: `/subsystem=distributable-web:read-attribute(name=default-single-sign-on-management)`

## syntax of the operation to get the `cache` attribute of the `infinispan` `routing` resource of the `distributable-web` subsystem
* Retrieve the `cache` attribute value of the `infinispan` `routing` resource of the `distributable-web` subsystem
* `cache` attribute description: the name of the cache associated this provider
* operation: `/subsystem=distributable-web/routing=infinispan:read-attribute(name=cache)`

## syntax of the operation to get the `cache-container` attribute of the `infinispan` `routing` resource of the `distributable-web` subsystem
* Retrieve the `cache-container` attribute value of the `infinispan` `routing` resource of the `distributable-web` subsystem
* `cache-container` attribute description: the name of the cache container associated this provider
* operation: `/subsystem=distributable-web/routing=infinispan:read-attribute(name=cache-container)`

## syntax of the operation to get a `infinispan-single-sign-on-management` resource of the `distributable-web` subsystem
* operation: `/subsystem=distributable-web/infinispan-single-sign-on-management=<infinispan-single-sign-on-management name>:read-resource()`
* To get the list of all the `infinispan-single-sign-on-management` resource of the `distributable-web` subsystem use '*' for `<infinispan-single-sign-on-management name>`.

## syntax of the operation to get the `cache` attribute of the `infinispan-single-sign-on-management` resource of the `distributable-web` subsystem
* Retrieve the `cache` attribute value of the `infinispan-single-sign-on-management` resource of the `distributable-web` subsystem
* `cache` attribute description: the name of the cache associated with this provider
* operation: `/subsystem=distributable-web/infinispan-single-sign-on-management=<infinispan-single-sign-on-management name>:read-attribute(name=cache)`

## syntax of the operation to get the `cache-container` attribute of the `infinispan-single-sign-on-management` resource of the `distributable-web` subsystem
* Retrieve the `cache-container` attribute value of the `infinispan-single-sign-on-management` resource of the `distributable-web` subsystem
* `cache-container` attribute description: the name of the cache container associated with this provider
* operation: `/subsystem=distributable-web/infinispan-single-sign-on-management=<infinispan-single-sign-on-management name>:read-attribute(name=cache-container)`

## syntax of the operation to get a `hotrod-session-management` resource of the `distributable-web` subsystem
* operation: `/subsystem=distributable-web/hotrod-session-management=<hotrod-session-management name>:read-resource()`
* To get the list of all the `hotrod-session-management` resource of the `distributable-web` subsystem use '*' for `<hotrod-session-management name>`.

## syntax of the operation to get the `cache-configuration` attribute of the `hotrod-session-management` resource of the `distributable-web` subsystem
* Retrieve the `cache-configuration` attribute value of the `hotrod-session-management` resource of the `distributable-web` subsystem
* `cache-configuration` attribute description: the name of cache configuration on the remote cache container.
* operation: `/subsystem=distributable-web/hotrod-session-management=<hotrod-session-management name>:read-attribute(name=cache-configuration)`

## syntax of the operation to get the `expiration-thread-pool-size` attribute of the `hotrod-session-management` resource of the `distributable-web` subsystem
* Retrieve the `expiration-thread-pool-size` attribute value of the `hotrod-session-management` resource of the `distributable-web` subsystem
* `expiration-thread-pool-size` attribute description: configures the number of threads dedicated to processing expiration events from the remote `infinispan` cluster.
* operation: `/subsystem=distributable-web/hotrod-session-management=<hotrod-session-management name>:read-attribute(name=expiration-thread-pool-size)`

## syntax of the operation to get the `granularity` attribute of the `hotrod-session-management` resource of the `distributable-web` subsystem
* Retrieve the `granularity` attribute value of the `hotrod-session-management` resource of the `distributable-web` subsystem
* `granularity` attribute description: defines the strategy for persisting the attributes of a session
* operation: `/subsystem=distributable-web/hotrod-session-management=<hotrod-session-management name>:read-attribute(name=granularity)`

## syntax of the operation to get the `marshaller` attribute of the `hotrod-session-management` resource of the `distributable-web` subsystem
* Retrieve the `marshaller` attribute value of the `hotrod-session-management` resource of the `distributable-web` subsystem
* `marshaller` attribute description: indicates the marshalling implementation used for serializing attributes of a session
* operation: `/subsystem=distributable-web/hotrod-session-management=<hotrod-session-management name>:read-attribute(name=marshaller)`

## syntax of the operation to get the `remote-cache-container` attribute of the `hotrod-session-management` resource of the `distributable-web` subsystem
* Retrieve the `remote-cache-container` attribute value of the `hotrod-session-management` resource of the `distributable-web` subsystem
* `remote-cache-container` attribute description: the name of the remote cache container associated with this provider
* operation: `/subsystem=distributable-web/hotrod-session-management=<hotrod-session-management name>:read-attribute(name=remote-cache-container)`

## syntax of the operation to get a `hotrod-single-sign-on-management` resource of the `distributable-web` subsystem
* operation: `/subsystem=distributable-web/hotrod-single-sign-on-management=<hotrod-single-sign-on-management name>:read-resource()`
* To get the list of all the `hotrod-single-sign-on-management` resource of the `distributable-web` subsystem use '*' for `<hotrod-single-sign-on-management name>`.

## syntax of the operation to get the `cache-configuration` attribute of the `hotrod-single-sign-on-management` resource of the `distributable-web` subsystem
* Retrieve the `cache-configuration` attribute value of the `hotrod-single-sign-on-management` resource of the `distributable-web` subsystem
* `cache-configuration` attribute description: the name of cache configuration on the remote cache container.
* operation: `/subsystem=distributable-web/hotrod-single-sign-on-management=<hotrod-single-sign-on-management name>:read-attribute(name=cache-configuration)`

## syntax of the operation to get the `remote-cache-container` attribute of the `hotrod-single-sign-on-management` resource of the `distributable-web` subsystem
* Retrieve the `remote-cache-container` attribute value of the `hotrod-single-sign-on-management` resource of the `distributable-web` subsystem
* `remote-cache-container` attribute description: the name of the remote cache container associated with this provider
* operation: `/subsystem=distributable-web/hotrod-single-sign-on-management=<hotrod-single-sign-on-management name>:read-attribute(name=remote-cache-container)`

## syntax of the operation to get a `infinispan-session-management` resource of the `distributable-web` subsystem
* operation: `/subsystem=distributable-web/infinispan-session-management=<infinispan-session-management name>:read-resource()`
* To get the list of all the `infinispan-session-management` resource of the `distributable-web` subsystem use '*' for `<infinispan-session-management name>`.

## syntax of the operation to get the `cache` attribute of the `infinispan-session-management` resource of the `distributable-web` subsystem
* Retrieve the `cache` attribute value of the `infinispan-session-management` resource of the `distributable-web` subsystem
* `cache` attribute description: the name of the cache associated with this provider
* operation: `/subsystem=distributable-web/infinispan-session-management=<infinispan-session-management name>:read-attribute(name=cache)`

## syntax of the operation to get the `cache-container` attribute of the `infinispan-session-management` resource of the `distributable-web` subsystem
* Retrieve the `cache-container` attribute value of the `infinispan-session-management` resource of the `distributable-web` subsystem
* `cache-container` attribute description: the name of the cache container associated with this provider
* operation: `/subsystem=distributable-web/infinispan-session-management=<infinispan-session-management name>:read-attribute(name=cache-container)`

## syntax of the operation to get the `granularity` attribute of the `infinispan-session-management` resource of the `distributable-web` subsystem
* Retrieve the `granularity` attribute value of the `infinispan-session-management` resource of the `distributable-web` subsystem
* `granularity` attribute description: defines the strategy for persisting the attributes of a session
* operation: `/subsystem=distributable-web/infinispan-session-management=<infinispan-session-management name>:read-attribute(name=granularity)`

## syntax of the operation to get the `marshaller` attribute of the `infinispan-session-management` resource of the `distributable-web` subsystem
* Retrieve the `marshaller` attribute value of the `infinispan-session-management` resource of the `distributable-web` subsystem
* `marshaller` attribute description: indicates the marshalling implementation used for serializing attributes of a session
* operation: `/subsystem=distributable-web/infinispan-session-management=<infinispan-session-management name>:read-attribute(name=marshaller)`

## syntax of the operation to get the `delimiter` attribute of the `ranked` `affinity` resource of the `infinispan-session-management` resource of the `distributable-web` subsystem
* Retrieve the `delimiter` attribute value of the `ranked` `affinity` resource of the `infinispan-session-management` resource of the `distributable-web` subsystem
* `delimiter` attribute description: the delimiter used to separate the individual routes within the encoded session identifier
* operation: `/subsystem=distributable-web/infinispan-session-management=<infinispan-session-management name>/affinity=ranked:read-attribute(name=delimiter)`

## syntax of the operation to get the `max-routes` attribute of the `ranked` `affinity` resource of the `infinispan-session-management` resource of the `distributable-web` subsystem
* Retrieve the `max-routes` attribute value of the `ranked` `affinity` resource of the `infinispan-session-management` resource of the `distributable-web` subsystem
* `max-routes` attribute description: the maximum number of routes to encode into the session identifier
* operation: `/subsystem=distributable-web/infinispan-session-management=<infinispan-session-management name>/affinity=ranked:read-attribute(name=max-routes)`

## syntax of the operation to get a `mail-session` resource of the `mail` subsystem
* operation: `/subsystem=mail/mail-session=<mail-session name>:read-resource()`
* To get the list of all the `mail-session` resource of the `mail` subsystem use '*' for `<mail-session name>`.

## syntax of the operation to get the `debug` attribute of the `mail-session` resource of the `mail` subsystem
* Retrieve the `debug` attribute value of the `mail-session` resource of the `mail` subsystem
* `debug` attribute description: enables jakarta mail debugging
* operation: `/subsystem=mail/mail-session=<mail-session name>:read-attribute(name=debug)`

## syntax of the operation to get the `from` attribute of the `mail-session` resource of the `mail` subsystem
* Retrieve the `from` attribute value of the `mail-session` resource of the `mail` subsystem
* `from` attribute description: from address that is used as default from, if not set when sending
* operation: `/subsystem=mail/mail-session=<mail-session name>:read-attribute(name=from)`

## syntax of the operation to get the `jndi-name` attribute of the `mail-session` resource of the `mail` subsystem
* Retrieve the `jndi-name` attribute value of the `mail-session` resource of the `mail` subsystem
* `jndi-name` attribute description: `jndi` name to where mail session should be bound
* operation: `/subsystem=mail/mail-session=<mail-session name>:read-attribute(name=jndi-name)`

## syntax of the operation to get the `credential-reference` attribute of the `smtp` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `credential-reference` attribute value of the `smtp` `server` resource of the `mail-session` resource of the `mail` subsystem
* `credential-reference` attribute description: credential (from credential store) to authenticate on server
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=smtp:read-attribute(name=credential-reference)`

## syntax of the operation to get the `outbound-socket-binding-ref` attribute of the `smtp` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `outbound-socket-binding-ref` attribute value of the `smtp` `server` resource of the `mail-session` resource of the `mail` subsystem
* `outbound-socket-binding-ref` attribute description: outbound socket binding to mail server
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=smtp:read-attribute(name=outbound-socket-binding-ref)`

## syntax of the operation to get the `password` attribute of the `smtp` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `password` attribute value of the `smtp` `server` resource of the `mail-session` resource of the `mail` subsystem
* `password` attribute description: password to authenticate on server
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=smtp:read-attribute(name=password)`

## syntax of the operation to get the `ssl` attribute of the `smtp` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `ssl` attribute value of the `smtp` `server` resource of the `mail-session` resource of the `mail` subsystem
* `ssl` attribute description: does server require `ssl`?
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=smtp:read-attribute(name=ssl)`

## syntax of the operation to get the `tls` attribute of the `smtp` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `tls` attribute value of the `smtp` `server` resource of the `mail-session` resource of the `mail` subsystem
* `tls` attribute description: does server require `tls`?
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=smtp:read-attribute(name=tls)`

## syntax of the operation to get the `username` attribute of the `smtp` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `username` attribute value of the `smtp` `server` resource of the `mail-session` resource of the `mail` subsystem
* `username` attribute description: `username` to authenticate on server
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=smtp:read-attribute(name=username)`

## syntax of the operation to get the `credential-reference` attribute of the `pop3` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `credential-reference` attribute value of the `pop3` `server` resource of the `mail-session` resource of the `mail` subsystem
* `credential-reference` attribute description: credential (from credential store) to authenticate on server
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=pop3:read-attribute(name=credential-reference)`

## syntax of the operation to get the `outbound-socket-binding-ref` attribute of the `pop3` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `outbound-socket-binding-ref` attribute value of the `pop3` `server` resource of the `mail-session` resource of the `mail` subsystem
* `outbound-socket-binding-ref` attribute description: outbound socket binding to mail server
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=pop3:read-attribute(name=outbound-socket-binding-ref)`

## syntax of the operation to get the `password` attribute of the `pop3` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `password` attribute value of the `pop3` `server` resource of the `mail-session` resource of the `mail` subsystem
* `password` attribute description: password to authenticate on server
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=pop3:read-attribute(name=password)`

## syntax of the operation to get the `ssl` attribute of the `pop3` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `ssl` attribute value of the `pop3` `server` resource of the `mail-session` resource of the `mail` subsystem
* `ssl` attribute description: does server require `ssl`?
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=pop3:read-attribute(name=ssl)`

## syntax of the operation to get the `tls` attribute of the `pop3` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `tls` attribute value of the `pop3` `server` resource of the `mail-session` resource of the `mail` subsystem
* `tls` attribute description: does server require `tls`?
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=pop3:read-attribute(name=tls)`

## syntax of the operation to get the `username` attribute of the `pop3` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `username` attribute value of the `pop3` `server` resource of the `mail-session` resource of the `mail` subsystem
* `username` attribute description: `username` to authenticate on server
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=pop3:read-attribute(name=username)`

## syntax of the operation to get the `credential-reference` attribute of the `imap` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `credential-reference` attribute value of the `imap` `server` resource of the `mail-session` resource of the `mail` subsystem
* `credential-reference` attribute description: credential (from credential store) to authenticate on server
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=imap:read-attribute(name=credential-reference)`

## syntax of the operation to get the `outbound-socket-binding-ref` attribute of the `imap` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `outbound-socket-binding-ref` attribute value of the `imap` `server` resource of the `mail-session` resource of the `mail` subsystem
* `outbound-socket-binding-ref` attribute description: outbound socket binding to mail server
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=imap:read-attribute(name=outbound-socket-binding-ref)`

## syntax of the operation to get the `password` attribute of the `imap` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `password` attribute value of the `imap` `server` resource of the `mail-session` resource of the `mail` subsystem
* `password` attribute description: password to authenticate on server
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=imap:read-attribute(name=password)`

## syntax of the operation to get the `ssl` attribute of the `imap` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `ssl` attribute value of the `imap` `server` resource of the `mail-session` resource of the `mail` subsystem
* `ssl` attribute description: does server require `ssl`?
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=imap:read-attribute(name=ssl)`

## syntax of the operation to get the `tls` attribute of the `imap` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `tls` attribute value of the `imap` `server` resource of the `mail-session` resource of the `mail` subsystem
* `tls` attribute description: does server require `tls`?
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=imap:read-attribute(name=tls)`

## syntax of the operation to get the `username` attribute of the `imap` `server` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `username` attribute value of the `imap` `server` resource of the `mail-session` resource of the `mail` subsystem
* `username` attribute description: `username` to authenticate on server
* operation: `/subsystem=mail/mail-session=<mail-session name>/server=imap:read-attribute(name=username)`

## syntax of the operation to get a `custom` resource of the `mail-session` resource of the `mail` subsystem
* operation: `/subsystem=mail/mail-session=<mail-session name>/custom=<custom name>:read-resource()`
* To get the list of all the `custom` resource of the `mail-session` resource of the `mail` subsystem use '*' for `<custom name>`.

## syntax of the operation to get the `credential-reference` attribute of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `credential-reference` attribute value of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* `credential-reference` attribute description: credential (from credential store) to authenticate on server
* operation: `/subsystem=mail/mail-session=<mail-session name>/custom=<custom name>:read-attribute(name=credential-reference)`

## syntax of the operation to get the `outbound-socket-binding-ref` attribute of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `outbound-socket-binding-ref` attribute value of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* `outbound-socket-binding-ref` attribute description: outbound socket binding to mail server
* operation: `/subsystem=mail/mail-session=<mail-session name>/custom=<custom name>:read-attribute(name=outbound-socket-binding-ref)`

## syntax of the operation to get the `password` attribute of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `password` attribute value of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* `password` attribute description: password to authenticate on server
* operation: `/subsystem=mail/mail-session=<mail-session name>/custom=<custom name>:read-attribute(name=password)`

## syntax of the operation to get the `properties` attribute of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `properties` attribute value of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* `properties` attribute description: jakarta mail properties
* operation: `/subsystem=mail/mail-session=<mail-session name>/custom=<custom name>:read-attribute(name=properties)`

## syntax of the operation to get the `ssl` attribute of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `ssl` attribute value of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* `ssl` attribute description: does server require `ssl`?
* operation: `/subsystem=mail/mail-session=<mail-session name>/custom=<custom name>:read-attribute(name=ssl)`

## syntax of the operation to get the `tls` attribute of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `tls` attribute value of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* `tls` attribute description: does server require `tls`?
* operation: `/subsystem=mail/mail-session=<mail-session name>/custom=<custom name>:read-attribute(name=tls)`

## syntax of the operation to get the `username` attribute of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* Retrieve the `username` attribute value of the `custom` resource of the `mail-session` resource of the `mail` subsystem
* `username` attribute description: `username` to authenticate on server
* operation: `/subsystem=mail/mail-session=<mail-session name>/custom=<custom name>:read-attribute(name=username)`

## syntax of the operation to get the `default-bean-management` attribute of the `distributable-ejb` subsystem
* Retrieve the `default-bean-management` attribute value of the `distributable-ejb` subsystem
* `default-bean-management` attribute description: the default distributable bean management provider of the server
* operation: `/subsystem=distributable-ejb:read-attribute(name=default-bean-management)`

## syntax of the operation to get the `cache` attribute of the `infinispan` `client-mappings-registry` resource of the `distributable-ejb` subsystem
* Retrieve the `cache` attribute value of the `infinispan` `client-mappings-registry` resource of the `distributable-ejb` subsystem
* `cache` attribute description: the name of the cache associated this provider
* operation: `/subsystem=distributable-ejb/client-mappings-registry=infinispan:read-attribute(name=cache)`

## syntax of the operation to get the `cache-container` attribute of the `infinispan` `client-mappings-registry` resource of the `distributable-ejb` subsystem
* Retrieve the `cache-container` attribute value of the `infinispan` `client-mappings-registry` resource of the `distributable-ejb` subsystem
* `cache-container` attribute description: the name of the cache container associated this provider
* operation: `/subsystem=distributable-ejb/client-mappings-registry=infinispan:read-attribute(name=cache-container)`

## syntax of the operation to get a `infinispan-timer-management` resource of the `distributable-ejb` subsystem
* operation: `/subsystem=distributable-ejb/infinispan-timer-management=<infinispan-timer-management name>:read-resource()`
* To get the list of all the `infinispan-timer-management` resource of the `distributable-ejb` subsystem use '*' for `<infinispan-timer-management name>`.

## syntax of the operation to get the `cache` attribute of the `infinispan-timer-management` resource of the `distributable-ejb` subsystem
* Retrieve the `cache` attribute value of the `infinispan-timer-management` resource of the `distributable-ejb` subsystem
* `cache` attribute description: the name of the cache associated with this provider
* operation: `/subsystem=distributable-ejb/infinispan-timer-management=<infinispan-timer-management name>:read-attribute(name=cache)`

## syntax of the operation to get the `cache-container` attribute of the `infinispan-timer-management` resource of the `distributable-ejb` subsystem
* Retrieve the `cache-container` attribute value of the `infinispan-timer-management` resource of the `distributable-ejb` subsystem
* `cache-container` attribute description: the name of the cache container associated with this provider
* operation: `/subsystem=distributable-ejb/infinispan-timer-management=<infinispan-timer-management name>:read-attribute(name=cache-container)`

## syntax of the operation to get the `marshaller` attribute of the `infinispan-timer-management` resource of the `distributable-ejb` subsystem
* Retrieve the `marshaller` attribute value of the `infinispan-timer-management` resource of the `distributable-ejb` subsystem
* `marshaller` attribute description: indicates the marshalling implementation used for serializing the timeout context of a timer.
* operation: `/subsystem=distributable-ejb/infinispan-timer-management=<infinispan-timer-management name>:read-attribute(name=marshaller)`

## syntax of the operation to get the `max-active-timers` attribute of the `infinispan-timer-management` resource of the `distributable-ejb` subsystem
* Retrieve the `max-active-timers` attribute value of the `infinispan-timer-management` resource of the `distributable-ejb` subsystem
* `max-active-timers` attribute description: the maximum number of active timers to retain in memory before triggering passivation.
* operation: `/subsystem=distributable-ejb/infinispan-timer-management=<infinispan-timer-management name>:read-attribute(name=max-active-timers)`

## syntax of the operation to get a `infinispan-bean-management` resource of the `distributable-ejb` subsystem
* operation: `/subsystem=distributable-ejb/infinispan-bean-management=<infinispan-bean-management name>:read-resource()`
* To get the list of all the `infinispan-bean-management` resource of the `distributable-ejb` subsystem use '*' for `<infinispan-bean-management name>`.

## syntax of the operation to get the `cache` attribute of the `infinispan-bean-management` resource of the `distributable-ejb` subsystem
* Retrieve the `cache` attribute value of the `infinispan-bean-management` resource of the `distributable-ejb` subsystem
* `cache` attribute description: the name of the cache associated with this provider
* operation: `/subsystem=distributable-ejb/infinispan-bean-management=<infinispan-bean-management name>:read-attribute(name=cache)`

## syntax of the operation to get the `cache-container` attribute of the `infinispan-bean-management` resource of the `distributable-ejb` subsystem
* Retrieve the `cache-container` attribute value of the `infinispan-bean-management` resource of the `distributable-ejb` subsystem
* `cache-container` attribute description: the name of the cache container associated with this provider
* operation: `/subsystem=distributable-ejb/infinispan-bean-management=<infinispan-bean-management name>:read-attribute(name=cache-container)`

## syntax of the operation to get the `max-active-beans` attribute of the `infinispan-bean-management` resource of the `distributable-ejb` subsystem
* Retrieve the `max-active-beans` attribute value of the `infinispan-bean-management` resource of the `distributable-ejb` subsystem
* `max-active-beans` attribute description: the maximum number active beans to retain in memory at a time, after which the least recently used will passivate
* operation: `/subsystem=distributable-ejb/infinispan-bean-management=<infinispan-bean-management name>:read-attribute(name=max-active-beans)`

## syntax of the operation to get the `report-directory` attribute of the `resource-adapters` subsystem
* Retrieve the `report-directory` attribute value of the `resource-adapters` subsystem
* `report-directory` attribute description: the directory to which validation logs would be written
* operation: `/subsystem=resource-adapters:read-attribute(name=report-directory)`

## syntax of the operation to get a `resource-adapter` resource of the `resource-adapters` subsystem
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-resource()`
* To get the list of all the `resource-adapter` resource of the `resource-adapters` subsystem use '*' for `<resource-adapter name>`.

## syntax of the operation to get the `archive` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `archive` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `archive` attribute description: specifies the resource adapter archive.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=archive)`

## syntax of the operation to get the `beanvalidationgroups` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `beanvalidationgroups` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `beanvalidationgroups` attribute description: specifies the jakarta bean validation groups that should be used.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=beanvalidationgroups)`

## syntax of the operation to get the `bootstrap-context` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `bootstrap-context` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `bootstrap-context` attribute description: specifies the unique name of the bootstrap context that should be used.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=bootstrap-context)`

## syntax of the operation to get the `module` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `module` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `module` attribute description: specifies the module from which resource adapter will be loaded
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=module)`

## syntax of the operation to get the `statistics-enabled` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `statistics-enabled` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `statistics-enabled` attribute description: define whether runtime statistics are enabled or not.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `transaction-support` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `transaction-support` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `transaction-support` attribute description: specifies the transaction support level of the resource adapter. this attribute allows expressions, but only simple expressions resolved from environment variables or system properties are supported. nested expressions and resolution from vaults or credential stores are not supported.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=transaction-support)`

## syntax of the operation to get the `wm-elytron-security-domain` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `wm-elytron-security-domain` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `wm-elytron-security-domain` attribute description: defines the name of the elytron security domain that should be used.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=wm-elytron-security-domain)`

## syntax of the operation to get the `wm-security` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `wm-security` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `wm-security` attribute description: toggle `on/off` `wm.security` for this resource adapter. in case of false all `wm-security`-* parameters are ignored, even the defaults.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=wm-security)`

## syntax of the operation to get the `wm-security-default-groups` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `wm-security-default-groups` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `wm-security-default-groups` attribute description: defines a default groups list that should be added to the used subject instance.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=wm-security-default-groups)`

## syntax of the operation to get the `wm-security-default-principal` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `wm-security-default-principal` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `wm-security-default-principal` attribute description: defines a default principal name that should be added to the used subject instance.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=wm-security-default-principal)`

## syntax of the operation to get the `wm-security-domain` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `wm-security-domain` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `wm-security-domain` attribute description: defines the name of the `picketbox` security domain that should be used.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=wm-security-domain)`

## syntax of the operation to get the `wm-security-mapping-groups` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `wm-security-mapping-groups` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `wm-security-mapping-groups` attribute description: list of groups mappings.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=wm-security-mapping-groups)`

## syntax of the operation to get the `wm-security-mapping-required` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `wm-security-mapping-required` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `wm-security-mapping-required` attribute description: defines if a mapping is required for security credentials.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=wm-security-mapping-required)`

## syntax of the operation to get the `wm-security-mapping-users` attribute of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `wm-security-mapping-users` attribute value of the `resource-adapter` resource of the `resource-adapters` subsystem
* `wm-security-mapping-users` attribute description: list of user mappings.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>:read-attribute(name=wm-security-mapping-users)`

## syntax of the operation to get a `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/admin-objects=<admin-objects name>:read-resource()`
* To get the list of all the `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem use '*' for `<admin-objects name>`.

## syntax of the operation to get the `class-name` attribute of the `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `class-name` attribute value of the `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `class-name` attribute description: specifies the fully qualified class name of an administration object.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/admin-objects=<admin-objects name>:read-attribute(name=class-name)`

## syntax of the operation to get the `enabled` attribute of the `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `enabled` attribute value of the `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `enabled` attribute description: specifies if the administration object should be enabled.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/admin-objects=<admin-objects name>:read-attribute(name=enabled)`

## syntax of the operation to get the `jndi-name` attribute of the `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `jndi-name` attribute value of the `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `jndi-name` attribute description: specifies the `jndi` name for the administration object.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/admin-objects=<admin-objects name>:read-attribute(name=jndi-name)`

## syntax of the operation to get the `use-java-context` attribute of the `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `use-java-context` attribute value of the `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `use-java-context` attribute description: setting this to false will bind the object into global `jndi`.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/admin-objects=<admin-objects name>:read-attribute(name=use-java-context)`

## syntax of the operation to get a `config-properties` resource of the `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/admin-objects=<admin-objects name>/config-properties=<config-properties name>:read-resource()`
* To get the list of all the `config-properties` resource of the `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem use '*' for `<config-properties name>`.

## syntax of the operation to get the `value` attribute of the `config-properties` resource of the `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `value` attribute value of the `config-properties` resource of the `admin-objects` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `value` attribute description: custom defined `config` property value.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/admin-objects=<admin-objects name>/config-properties=<config-properties name>:read-attribute(name=value)`

## syntax of the operation to get a `config-properties` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/config-properties=<config-properties name>:read-resource()`
* To get the list of all the `config-properties` resource of the `resource-adapter` resource of the `resource-adapters` subsystem use '*' for `<config-properties name>`.

## syntax of the operation to get the `value` attribute of the `config-properties` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `value` attribute value of the `config-properties` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `value` attribute description: custom defined `config` property value.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/config-properties=<config-properties name>:read-attribute(name=value)`

## syntax of the operation to get a `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-resource()`
* To get the list of all the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem use '*' for `<connection-definitions name>`.

## syntax of the operation to get the `allocation-retry` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `allocation-retry` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `allocation-retry` attribute description: the allocation retry element indicates the number of times that allocating a connection should be tried before throwing an exception.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=allocation-retry)`

## syntax of the operation to get the `allocation-retry-wait-millis` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `allocation-retry-wait-millis` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `allocation-retry-wait-millis` attribute description: the allocation retry wait `millis` element specifies the amount of time, in milliseconds, to wait between retrying to allocate a connection.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=allocation-retry-wait-millis)`

## syntax of the operation to get the `authentication-context` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `authentication-context` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `authentication-context` attribute description: the elytron authentication context which defines the `javax.security.auth.subject` that is used to distinguish connections in the pool.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=authentication-context)`

## syntax of the operation to get the `authentication-context-and-application` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `authentication-context-and-application` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `authentication-context-and-application` attribute description: indicates that either `application-supplied` parameters, such as from `getconnection(user`, `pw`), or subject (provided by elytron after authenticating using configured `authentication-context`), are used to distinguish connections in the pool.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=authentication-context-and-application)`

## syntax of the operation to get the `background-validation` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `background-validation` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `background-validation` attribute description: an element to specify that connections should be validated on a background thread versus being validated prior to use. changing this value requires a server restart.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=background-validation)`

## syntax of the operation to get the `background-validation-millis` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `background-validation-millis` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `background-validation-millis` attribute description: the `background-validation-millis` element specifies the amount of time, in milliseconds, that background validation will run. changing this value requires a server restart.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=background-validation-millis)`

## syntax of the operation to get the `blocking-timeout-wait-millis` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `blocking-timeout-wait-millis` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `blocking-timeout-wait-millis` attribute description: the `blocking-timeout-millis` element specifies the maximum time, in milliseconds, to block while waiting for a connection before throwing an exception. note that this blocks only while waiting for locking a connection, and will never throw an exception if creating a new connection takes an inordinately long time.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=blocking-timeout-wait-millis)`

## syntax of the operation to get the `capacity-decrementer-class` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `capacity-decrementer-class` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `capacity-decrementer-class` attribute description: class defining the policy for decrementing connections in the pool.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=capacity-decrementer-class)`

## syntax of the operation to get the `capacity-decrementer-properties` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `capacity-decrementer-properties` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `capacity-decrementer-properties` attribute description: properties to inject in class defining the policy for decrementing connections in the pool.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=capacity-decrementer-properties)`

## syntax of the operation to get the `capacity-incrementer-class` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `capacity-incrementer-class` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `capacity-incrementer-class` attribute description: class defining the policy for incrementing connections in the pool.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=capacity-incrementer-class)`

## syntax of the operation to get the `capacity-incrementer-properties` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `capacity-incrementer-properties` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `capacity-incrementer-properties` attribute description: properties to inject in class defining the policy for incrementing connections in the pool.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=capacity-incrementer-properties)`

## syntax of the operation to get the `class-name` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `class-name` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `class-name` attribute description: specifies the fully qualified class name of a managed connection factory or admin object.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=class-name)`

## syntax of the operation to get the `connectable` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `connectable` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `connectable` attribute description: enable the use of `cmr`. this feature means that a local resource can reliably participate in an `xa` transaction.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=connectable)`

## syntax of the operation to get the `elytron-enabled` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `elytron-enabled` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `elytron-enabled` attribute description: enables elytron security for handling authentication of connections. the elytron `authentication-context` to be used will be current context if no context is specified (see `authentication-context`).
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=elytron-enabled)`

## syntax of the operation to get the `enabled` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `enabled` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `enabled` attribute description: specifies if the resource adapter should be enabled.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=enabled)`

## syntax of the operation to get the `enlistment` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `enlistment` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `enlistment` attribute description: defines if lazy enlistment should be used if supported by the resource adapter.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=enlistment)`

## syntax of the operation to get the `enlistment-trace` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `enlistment-trace` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `enlistment-trace` attribute description: defines if `wildfly/ironjacamar` should record enlistment traces.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=enlistment-trace)`

## syntax of the operation to get the `flush-strategy` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `flush-strategy` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `flush-strategy` attribute description: specifies how the pool should be flushed in case of an error.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=flush-strategy)`

## syntax of the operation to get the `idle-timeout-minutes` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `idle-timeout-minutes` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `idle-timeout-minutes` attribute description: specifies the maximum time, in minutes, a connection may be idle before being closed. the actual maximum time depends also on the `idleremover` scan time, which is half of the smallest `idle-timeout-minutes` value of any pool. changing this value requires a server restart.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=idle-timeout-minutes)`

## syntax of the operation to get the `initial-pool-size` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `initial-pool-size` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `initial-pool-size` attribute description: specifies the initial number of connections a pool should hold.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=initial-pool-size)`

## syntax of the operation to get the `interleaving` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `interleaving` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `interleaving` attribute description: an element to enable interleaving for `xa` connections.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=interleaving)`

## syntax of the operation to get the `jndi-name` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `jndi-name` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `jndi-name` attribute description: specifies the `jndi` name for the connection factory.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=jndi-name)`

## syntax of the operation to get the `max-pool-size` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `max-pool-size` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `max-pool-size` attribute description: specifies the maximum number of connections for a pool. no more connections will be created in each `sub-pool`.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=max-pool-size)`

## syntax of the operation to get the `mcp` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `mcp` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `mcp` attribute description: defines the `managedconnectionpool` implementation. for example: `org.jboss.jca.core.connectionmanager.pool.mcp.semaphorearraylistmanagedconnectionpool`.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=mcp)`

## syntax of the operation to get the `min-pool-size` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `min-pool-size` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `min-pool-size` attribute description: specifies the minimum number of connections for a pool.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=min-pool-size)`

## syntax of the operation to get the `no-recovery` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `no-recovery` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `no-recovery` attribute description: specifies if the connection pool should be excluded from recovery.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=no-recovery)`

## syntax of the operation to get the `no-tx-separate-pool` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `no-tx-separate-pool` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `no-tx-separate-pool` attribute description: oracle does not like `xa` connections getting used both inside and outside a `jta` transaction. to `workaround` the problem you can create separate `sub-pools` for the different contexts.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=no-tx-separate-pool)`

## syntax of the operation to get the `pad-xid` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `pad-xid` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `pad-xid` attribute description: specifies whether the `xid` should be padded.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=pad-xid)`

## syntax of the operation to get the `pool-fair` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `pool-fair` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `pool-fair` attribute description: defines if pool use should be fair.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=pool-fair)`

## syntax of the operation to get the `pool-prefill` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `pool-prefill` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `pool-prefill` attribute description: specifies if the pool should be `prefilled`. changing this value requires a server restart.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=pool-prefill)`

## syntax of the operation to get the `pool-use-strict-min` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `pool-use-strict-min` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `pool-use-strict-min` attribute description: specifies if the `min-pool-size` should be considered strict.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=pool-use-strict-min)`

## syntax of the operation to get the `recovery-authentication-context` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `recovery-authentication-context` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `recovery-authentication-context` attribute description: the elytron authentication context used for recovery (current `authentication-context` will be used if unspecified).
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=recovery-authentication-context)`

## syntax of the operation to get the `recovery-credential-reference` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `recovery-credential-reference` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `recovery-credential-reference` attribute description: credential (from credential store) to authenticate on recovery connection
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=recovery-credential-reference)`

## syntax of the operation to get the `recovery-elytron-enabled` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `recovery-elytron-enabled` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `recovery-elytron-enabled` attribute description: indicates that an elytron authentication context will be used for recovery.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=recovery-elytron-enabled)`

## syntax of the operation to get the `recovery-password` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `recovery-password` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `recovery-password` attribute description: the password used for recovery.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=recovery-password)`

## syntax of the operation to get the `recovery-plugin-class-name` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `recovery-plugin-class-name` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `recovery-plugin-class-name` attribute description: the fully qualified class name of the recovery `plugin` implementation.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=recovery-plugin-class-name)`

## syntax of the operation to get the `recovery-plugin-properties` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `recovery-plugin-properties` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `recovery-plugin-properties` attribute description: the properties for the recovery `plugin`.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=recovery-plugin-properties)`

## syntax of the operation to get the `recovery-security-domain` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `recovery-security-domain` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `recovery-security-domain` attribute description: the `picketbox` security domain used for recovery.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=recovery-security-domain)`

## syntax of the operation to get the `recovery-username` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `recovery-username` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `recovery-username` attribute description: the user name used for recovery.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=recovery-username)`

## syntax of the operation to get the `same-rm-override` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `same-rm-override` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `same-rm-override` attribute description: using this attribute, you can unconditionally set whether `javax.transaction.xa.xaresource.issamerm(xaresource`) returns true or false.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=same-rm-override)`

## syntax of the operation to get the `security-application` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `security-application` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `security-application` attribute description: indicates that `application-supplied` parameters, such as from `getconnection(user`, `pw`), are used to distinguish connections in the pool.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=security-application)`

## syntax of the operation to get the `security-domain` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `security-domain` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `security-domain` attribute description: specifies the `picketbox` security domain which defines the `javax.security.auth.subject` that is used to distinguish connections in the pool.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=security-domain)`

## syntax of the operation to get the `security-domain-and-application` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `security-domain-and-application` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `security-domain-and-application` attribute description: indicates that either `application-supplied` parameters, such as from `getconnection(user`, `pw`) are used to distinguish connections in the pool.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=security-domain-and-application)`

## syntax of the operation to get the `sharable` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `sharable` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `sharable` attribute description: enable the use of sharable connections, which allows lazy association to be enabled if supported.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=sharable)`

## syntax of the operation to get the `tracking` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `tracking` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `tracking` attribute description: defines if `ironjacamar` should track connection handles across transaction boundaries.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=tracking)`

## syntax of the operation to get the `use-ccm` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `use-ccm` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `use-ccm` attribute description: enable the use of a cached connection manager.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=use-ccm)`

## syntax of the operation to get the `use-fast-fail` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `use-fast-fail` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `use-fast-fail` attribute description: whether to fail a connection allocation on the first try if it is invalid (true) or keep trying until the pool is exhausted of all potential connections (false).
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=use-fast-fail)`

## syntax of the operation to get the `use-java-context` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `use-java-context` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `use-java-context` attribute description: setting this to false will bind the object into global `jndi`.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=use-java-context)`

## syntax of the operation to get the `validate-on-match` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `validate-on-match` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `validate-on-match` attribute description: this specifies if connection validation should be done when a connection factory attempts to match a managed connection. this is typically exclusive to the use of background validation.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=validate-on-match)`

## syntax of the operation to get the `wrap-xa-resource` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `wrap-xa-resource` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `wrap-xa-resource` attribute description: specifies whether `xaresource` instances should be wrapped in an `org.jboss.tm.xaresourcewrapper` instance.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=wrap-xa-resource)`

## syntax of the operation to get the `xa-resource-timeout` attribute of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `xa-resource-timeout` attribute value of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `xa-resource-timeout` attribute description: the value is passed to `xaresource.settransactiontimeout`(), in seconds.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>:read-attribute(name=xa-resource-timeout)`

## syntax of the operation to get a `config-properties` resource of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>/config-properties=<config-properties name>:read-resource()`
* To get the list of all the `config-properties` resource of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem use '*' for `<config-properties name>`.

## syntax of the operation to get the `value` attribute of the `config-properties` resource of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* Retrieve the `value` attribute value of the `config-properties` resource of the `connection-definitions` resource of the `resource-adapter` resource of the `resource-adapters` subsystem
* `value` attribute description: custom defined `config` property value.
* operation: `/subsystem=resource-adapters/resource-adapter=<resource-adapter name>/connection-definitions=<connection-definitions name>/config-properties=<config-properties name>:read-attribute(name=value)`

## syntax of the operation to get the `jaxrs-2-0-request-matching` attribute of the `jaxrs` subsystem
* Retrieve the `jaxrs-2-0-request-matching` attribute value of the `jaxrs` subsystem
* `jaxrs-2-0-request-matching` attribute description: in searching for a matching resource method with which to respond to a request, consider only resource methods with the best match for the request path.
* operation: `/subsystem=jaxrs:read-attribute(name=jaxrs-2-0-request-matching)`

## syntax of the operation to get the `resteasy-add-charset` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-add-charset` attribute value of the `jaxrs` subsystem
* `resteasy-add-charset` attribute description: if a resource method returns a text/* or `application/xml`* media type without an explicit `charset`, `resteasy` will add "`charset=utf-8`" to the returned `content-type` header. note that the `charset` defaults to `utf-8` in this case, independent of the setting of this parameter.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-add-charset)`

## syntax of the operation to get the `resteasy-buffer-exception-entity` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-buffer-exception-entity` attribute value of the `jaxrs` subsystem
* `resteasy-buffer-exception-entity` attribute description: upon receiving an exception, the client side buffers any response entity before closing the connection.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-buffer-exception-entity)`

## syntax of the operation to get the `resteasy-disable-html-sanitizer` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-disable-html-sanitizer` attribute value of the `jaxrs` subsystem
* `resteasy-disable-html-sanitizer` attribute description: normally, a response with media type "`text/html`" and a status of `400` will be processed so that the characters "/", "<", ">", "&", """ (double quote), and "'" (single quote) are escaped to prevent an `xss` attack. setting this parameter to "true", escaping will not occur.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-disable-html-sanitizer)`

## syntax of the operation to get the `resteasy-disable-providers` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-disable-providers` attribute value of the `jaxrs` subsystem
* `resteasy-disable-providers` attribute description: a comma delimited list of fully qualified provider class names that will be disabled.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-disable-providers)`

## syntax of the operation to get the `resteasy-document-expand-entity-references` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-document-expand-entity-references` attribute value of the `jaxrs` subsystem
* `resteasy-document-expand-entity-references` attribute description: expand external entities in `org.w3c.dom.document` documents and jakarta `xml` binding object representations.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-document-expand-entity-references)`

## syntax of the operation to get the `resteasy-document-secure-disableDTDs` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-document-secure-disableDTDs` attribute value of the `jaxrs` subsystem
* `resteasy-document-secure-disableDTDs` attribute description: prohibit `dtds` in `org.w3c.dom.document` documents and jakarta `xml` binding object representations.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-document-secure-disableDTDs)`

## syntax of the operation to get the `resteasy-document-secure-processing-feature` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-document-secure-processing-feature` attribute value of the `jaxrs` subsystem
* `resteasy-document-secure-processing-feature` attribute description: impose security constraints in processing `org.w3c.dom.document` documents and jakarta `xml` binding object representations.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-document-secure-processing-feature)`

## syntax of the operation to get the `resteasy-gzip-max-input` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-gzip-max-input` attribute value of the `jaxrs` subsystem
* `resteasy-gzip-max-input` attribute description: imposes maximum size on decompressed `gzipped`.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-gzip-max-input)`

## syntax of the operation to get the `resteasy-jndi-resources` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-jndi-resources` attribute value of the `jaxrs` subsystem
* `resteasy-jndi-resources` attribute description: a comma delimited list of `jndi` names which reference objects to be registered as jakarta restful web services resources.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-jndi-resources)`

## syntax of the operation to get the `resteasy-language-mappings` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-language-mappings` attribute value of the `jaxrs` subsystem
* `resteasy-language-mappings` attribute description: replaces the need for an `accept-language` header by mapping file name extensions (like .en or .fr) to a language. used when the client is unable to use an `accept-language` header to choose a language (`i.e`. a browser).
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-language-mappings)`

## syntax of the operation to get the `resteasy-media-type-mappings` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-media-type-mappings` attribute value of the `jaxrs` subsystem
* `resteasy-media-type-mappings` attribute description: replaces the need for an accept header by mapping file name extensions (like .`xml` or .txt) to a media type. used when the client is unable to use an accept header to choose a representation (`i.e`. a browser).
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-media-type-mappings)`

## syntax of the operation to get the `resteasy-media-type-param-mapping` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-media-type-param-mapping` attribute value of the `jaxrs` subsystem
* `resteasy-media-type-param-mapping` attribute description: names a query parameter that can be set to an acceptable media type, enabling content negotiation without an accept header.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-media-type-param-mapping)`

## syntax of the operation to get the `resteasy-patchfilter-disabled` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-patchfilter-disabled` attribute value of the `jaxrs` subsystem
* `resteasy-patchfilter-disabled` attribute description: specifies that `json` patch and `json` merge patch functionalities are all disabled.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-patchfilter-disabled)`

## syntax of the operation to get the `resteasy-prefer-jackson-over-jsonb` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-prefer-jackson-over-jsonb` attribute value of the `jaxrs` subsystem
* `resteasy-prefer-jackson-over-jsonb` attribute description: specifies that the jackson `json` provider should be preferred over the default jakarta `json` processing provider for `json` binding.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-prefer-jackson-over-jsonb)`

## syntax of the operation to get the `resteasy-providers` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-providers` attribute value of the `jaxrs` subsystem
* `resteasy-providers` attribute description: a comma delimited list of fully qualified @provider class names you want to register.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-providers)`

## syntax of the operation to get the `resteasy-rfc7232preconditions` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-rfc7232preconditions` attribute value of the `jaxrs` subsystem
* `resteasy-rfc7232preconditions` attribute description: enables `rfc7232` compliant `http` preconditions handling.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-rfc7232preconditions)`

## syntax of the operation to get the `resteasy-role-based-security` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-role-based-security` attribute value of the `jaxrs` subsystem
* `resteasy-role-based-security` attribute description: enables role based security.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-role-based-security)`

## syntax of the operation to get the `resteasy-secure-random-max-use` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-secure-random-max-use` attribute value of the `jaxrs` subsystem
* `resteasy-secure-random-max-use` attribute description: the number of times a `securerandom` can be used before reseeding.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-secure-random-max-use)`

## syntax of the operation to get the `resteasy-use-builtin-providers` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-use-builtin-providers` attribute value of the `jaxrs` subsystem
* `resteasy-use-builtin-providers` attribute description: whether or not to register default, `built-in` @provider classes.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-use-builtin-providers)`

## syntax of the operation to get the `resteasy-use-container-form-params` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-use-container-form-params` attribute value of the `jaxrs` subsystem
* `resteasy-use-container-form-params` attribute description: `httpservletrequest.getparametermap`(). use this switch if you are calling this method within a `servlet` filter or consuming the input stream within the filter.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-use-container-form-params)`

## syntax of the operation to get the `resteasy-wider-request-matching` attribute of the `jaxrs` subsystem
* Retrieve the `resteasy-wider-request-matching` attribute value of the `jaxrs` subsystem
* `resteasy-wider-request-matching` attribute description: turns off the jakarta restful web services spec defined `class-level` expression filtering and instead tries to match every `method's` full path.
* operation: `/subsystem=jaxrs:read-attribute(name=resteasy-wider-request-matching)`

## syntax of the operation to get the `tracing-threshold` attribute of the `jaxrs` subsystem
* Retrieve the `tracing-threshold` attribute value of the `jaxrs` subsystem
* `tracing-threshold` attribute description: the "summary" threshold will emit some brief tracing information. the "trace" threshold will produce more detailed tracing information, and the "verbose" threshold will generate extremely detailed tracing information.
* operation: `/subsystem=jaxrs:read-attribute(name=tracing-threshold)`

## syntax of the operation to get the `tracing-type` attribute of the `jaxrs` subsystem
* Retrieve the `tracing-type` attribute value of the `jaxrs` subsystem
* `tracing-type` attribute description: "all" will enable the tracing feature. "`on_demand`" mode will give control to the client side: a client can send a tracing request via `http` header and get the tracing info back from response headers. "off" mode will disable the tracing feature, and this is the default mode. note that it is suggested this is set to "off" for production servers.
* operation: `/subsystem=jaxrs:read-attribute(name=tracing-type)`

## syntax of the operation to get the `annotation-property-replacement` attribute of the `ee` subsystem
* Retrieve the `annotation-property-replacement` attribute value of the `ee` subsystem
* `annotation-property-replacement` attribute description: flag indicating whether jakarta ee annotations will have property replacements applied
* operation: `/subsystem=ee:read-attribute(name=annotation-property-replacement)`

## syntax of the operation to get the `ear-subdeployments-isolated` attribute of the `ee` subsystem
* Retrieve the `ear-subdeployments-isolated` attribute value of the `ee` subsystem
* `ear-subdeployments-isolated` attribute description: flag indicating whether each of the `subdeployments` within a .ear can access classes belonging to another `subdeployment` within the same .ear. a value of false means the `subdeployments` can see classes belonging to other `subdeployments` within the .ear.
* operation: `/subsystem=ee:read-attribute(name=ear-subdeployments-isolated)`

## syntax of the operation to get the `global-modules` attribute of the `ee` subsystem
* Retrieve the `global-modules` attribute value of the `ee` subsystem
* `global-modules` attribute description: a list of modules that should be made available to all deployments.
* operation: `/subsystem=ee:read-attribute(name=global-modules)`

## syntax of the operation to get the `jboss-descriptor-property-replacement` attribute of the `ee` subsystem
* Retrieve the `jboss-descriptor-property-replacement` attribute value of the `ee` subsystem
* `jboss-descriptor-property-replacement` attribute description: flag indicating whether `jboss` specific deployment descriptors will have property replacements applied
* operation: `/subsystem=ee:read-attribute(name=jboss-descriptor-property-replacement)`

## syntax of the operation to get the `spec-descriptor-property-replacement` attribute of the `ee` subsystem
* Retrieve the `spec-descriptor-property-replacement` attribute value of the `ee` subsystem
* `spec-descriptor-property-replacement` attribute description: flag indicating whether descriptors defined by the jakarta ee specification will have property replacements applied
* operation: `/subsystem=ee:read-attribute(name=spec-descriptor-property-replacement)`

## syntax of the operation to get a `managed-scheduled-executor-service` resource of the `ee` subsystem
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-resource()`
* To get the list of all the `managed-scheduled-executor-service` resource of the `ee` subsystem use '*' for `<managed-scheduled-executor-service name>`.

## syntax of the operation to get the `active-thread-count` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `active-thread-count` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `active-thread-count` attribute description: the approximate number of threads that are actively executing tasks.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=active-thread-count)`

## syntax of the operation to get the `completed-task-count` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `completed-task-count` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `completed-task-count` attribute description: the approximate total number of tasks that have completed execution.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=completed-task-count)`

## syntax of the operation to get the `context-service` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `context-service` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `context-service` attribute description: the name of the context service to be used by the scheduled executor.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=context-service)`

## syntax of the operation to get the `core-threads` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `core-threads` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `core-threads` attribute description: the number of threads to be used by the scheduled executor, even if they are idle. if this is not defined or is set to `0`, the core pool size will be calculated based on the number of available processors.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=core-threads)`

## syntax of the operation to get the `current-queue-size` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `current-queue-size` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `current-queue-size` attribute description: the current size of the `executor's` task queue.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=current-queue-size)`

## syntax of the operation to get the `hung-task-termination-period` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `hung-task-termination-period` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `hung-task-termination-period` attribute description: the period, in milliseconds, for attempting hung tasks automatic termination, by cancelling such tasks, and interrupting their executing threads. if value is `0`, which is the default, hung tasks are never cancelled.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=hung-task-termination-period)`

## syntax of the operation to get the `hung-task-threshold` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `hung-task-threshold` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `hung-task-threshold` attribute description: the runtime, in milliseconds, for tasks to be considered hung by the scheduled executor. if `0` tasks are never considered hung.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=hung-task-threshold)`

## syntax of the operation to get the `hung-thread-count` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `hung-thread-count` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `hung-thread-count` attribute description: the number of executor threads that are hung.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=hung-thread-count)`

## syntax of the operation to get the `jndi-name` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `jndi-name` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `jndi-name` attribute description: the `jndi` name to lookup the managed scheduled executor service.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=jndi-name)`

## syntax of the operation to get the `keepalive-time` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `keepalive-time` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `keepalive-time` attribute description: when the number of threads is greater than the core, this is the maximum time, in milliseconds, that excess idle threads will wait for new tasks before terminating.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `long-running-tasks` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `long-running-tasks` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `long-running-tasks` attribute description: flag which hints the duration of tasks executed by the scheduled executor.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=long-running-tasks)`

## syntax of the operation to get the `max-thread-count` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `max-thread-count` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `max-thread-count` attribute description: the largest number of executor threads.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=max-thread-count)`

## syntax of the operation to get the `reject-policy` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `reject-policy` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `reject-policy` attribute description: the policy to be applied to aborted tasks.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=reject-policy)`

## syntax of the operation to get the `task-count` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `task-count` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `task-count` attribute description: the approximate total number of tasks that have ever been submitted for execution.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=task-count)`

## syntax of the operation to get the `thread-count` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `thread-count` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `thread-count` attribute description: the current number of executor threads.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=thread-count)`

## syntax of the operation to get the `thread-factory` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `thread-factory` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `thread-factory` attribute description: the name of the thread factory to be used by the scheduled executor.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=thread-factory)`

## syntax of the operation to get the `thread-priority` attribute of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* Retrieve the `thread-priority` attribute value of the `managed-scheduled-executor-service` resource of the `ee` subsystem
* `thread-priority` attribute description: the priority applied to threads created by the executor.
* operation: `/subsystem=ee/managed-scheduled-executor-service=<managed-scheduled-executor-service name>:read-attribute(name=thread-priority)`

## syntax of the operation to get the `context-service` attribute of the `default-bindings` `service` resource of the `ee` subsystem
* Retrieve the `context-service` attribute value of the `default-bindings` `service` resource of the `ee` subsystem
* `context-service` attribute description: the `jndi` name where the default ee context service can be found
* operation: `/subsystem=ee/service=default-bindings:read-attribute(name=context-service)`

## syntax of the operation to get the `datasource` attribute of the `default-bindings` `service` resource of the `ee` subsystem
* Retrieve the `datasource` attribute value of the `default-bindings` `service` resource of the `ee` subsystem
* `datasource` attribute description: the `jndi` name where the default ee `datasource` can be found
* operation: `/subsystem=ee/service=default-bindings:read-attribute(name=datasource)`

## syntax of the operation to get the `jms-connection-factory` attribute of the `default-bindings` `service` resource of the `ee` subsystem
* Retrieve the `jms-connection-factory` attribute value of the `default-bindings` `service` resource of the `ee` subsystem
* `jms-connection-factory` attribute description: the `jndi` name where the default ee jms connection factory can be found
* operation: `/subsystem=ee/service=default-bindings:read-attribute(name=jms-connection-factory)`

## syntax of the operation to get the `managed-executor-service` attribute of the `default-bindings` `service` resource of the `ee` subsystem
* Retrieve the `managed-executor-service` attribute value of the `default-bindings` `service` resource of the `ee` subsystem
* `managed-executor-service` attribute description: the `jndi` name where the default ee managed executor service can be found
* operation: `/subsystem=ee/service=default-bindings:read-attribute(name=managed-executor-service)`

## syntax of the operation to get the `managed-scheduled-executor-service` attribute of the `default-bindings` `service` resource of the `ee` subsystem
* Retrieve the `managed-scheduled-executor-service` attribute value of the `default-bindings` `service` resource of the `ee` subsystem
* `managed-scheduled-executor-service` attribute description: the `jndi` name where the default ee managed scheduled executor service can be found
* operation: `/subsystem=ee/service=default-bindings:read-attribute(name=managed-scheduled-executor-service)`

## syntax of the operation to get the `managed-thread-factory` attribute of the `default-bindings` `service` resource of the `ee` subsystem
* Retrieve the `managed-thread-factory` attribute value of the `default-bindings` `service` resource of the `ee` subsystem
* `managed-thread-factory` attribute description: the `jndi` name where the default ee managed thread factory can be found
* operation: `/subsystem=ee/service=default-bindings:read-attribute(name=managed-thread-factory)`

## syntax of the operation to get a `global-directory` resource of the `ee` subsystem
* operation: `/subsystem=ee/global-directory=<global-directory name>:read-resource()`
* To get the list of all the `global-directory` resource of the `ee` subsystem use '*' for `<global-directory name>`.

## syntax of the operation to get the `path` attribute of the `global-directory` resource of the `ee` subsystem
* Retrieve the `path` attribute value of the `global-directory` resource of the `ee` subsystem
* `path` attribute description: the path of the directory to scan. it is treated as an absolute path, unless the '`relative-to`' attribute is specified, in which case the value is treated as relative to that path. if treated as an absolute path, the actual runtime pathname specified by the value of this attribute will be determined as follows: if this value is already absolute, then the value is directly used. otherwise, the runtime pathname is resolved in a `system-dependent` way.  on unix systems, a relative pathname is made absolute by resolving it against the current user directory. on `microsoft` windows systems, a relative pathname is made absolute by resolving it against the current directory of the drive named by the pathname, if any; if not, it is resolved against the current user directory.
* operation: `/subsystem=ee/global-directory=<global-directory name>:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `global-directory` resource of the `ee` subsystem
* Retrieve the `relative-to` attribute value of the `global-directory` resource of the `ee` subsystem
* `relative-to` attribute description: the name of another previously named path, or one of the standard paths provided by the system. if '`relative-to`' is provided, the value of the 'path' attribute is treated as relative to the path specified by this attribute. the standard paths provided by the system include: `jboss.home` - the root directory of the `jboss` as distribution, `user.home` - `user's` home directory, `user.dir` - `user's` current working directory, `java.home` - java installation directory, `jboss.server.base.dir` - root directory for an individual server instance, `jboss.server.data.dir` - directory the server will use for persistent data file storage, `jboss.server.log.dir` - directory the server will use for log file storage, `jboss.server.tmp.dir` - directory the server will use for temporary file storage, and `jboss.domain.servers.dir` - directory under which a host controller will create the working area for individual server instances.
* operation: `/subsystem=ee/global-directory=<global-directory name>:read-attribute(name=relative-to)`

## syntax of the operation to get a `managed-thread-factory` resource of the `ee` subsystem
* operation: `/subsystem=ee/managed-thread-factory=<managed-thread-factory name>:read-resource()`
* To get the list of all the `managed-thread-factory` resource of the `ee` subsystem use '*' for `<managed-thread-factory name>`.

## syntax of the operation to get the `context-service` attribute of the `managed-thread-factory` resource of the `ee` subsystem
* Retrieve the `context-service` attribute value of the `managed-thread-factory` resource of the `ee` subsystem
* `context-service` attribute description: the name of the context service to be used by the managed thread factory
* operation: `/subsystem=ee/managed-thread-factory=<managed-thread-factory name>:read-attribute(name=context-service)`

## syntax of the operation to get the `jndi-name` attribute of the `managed-thread-factory` resource of the `ee` subsystem
* Retrieve the `jndi-name` attribute value of the `managed-thread-factory` resource of the `ee` subsystem
* `jndi-name` attribute description: the `jndi` name to lookup the managed thread factory.
* operation: `/subsystem=ee/managed-thread-factory=<managed-thread-factory name>:read-attribute(name=jndi-name)`

## syntax of the operation to get the `priority` attribute of the `managed-thread-factory` resource of the `ee` subsystem
* Retrieve the `priority` attribute value of the `managed-thread-factory` resource of the `ee` subsystem
* `priority` attribute description: the priority applied to threads created by the factory
* operation: `/subsystem=ee/managed-thread-factory=<managed-thread-factory name>:read-attribute(name=priority)`

## syntax of the operation to get a `context-service` resource of the `ee` subsystem
* operation: `/subsystem=ee/context-service=<context-service name>:read-resource()`
* To get the list of all the `context-service` resource of the `ee` subsystem use '*' for `<context-service name>`.

## syntax of the operation to get the `jndi-name` attribute of the `context-service` resource of the `ee` subsystem
* Retrieve the `jndi-name` attribute value of the `context-service` resource of the `ee` subsystem
* `jndi-name` attribute description: the `jndi` name to lookup the context service.
* operation: `/subsystem=ee/context-service=<context-service name>:read-attribute(name=jndi-name)`

## syntax of the operation to get the `use-transaction-setup-provider` attribute of the `context-service` resource of the `ee` subsystem
* Retrieve the `use-transaction-setup-provider` attribute value of the `context-service` resource of the `ee` subsystem
* `use-transaction-setup-provider` attribute description: flag which indicates if the transaction setup provider should be used.
* operation: `/subsystem=ee/context-service=<context-service name>:read-attribute(name=use-transaction-setup-provider)`

## syntax of the operation to get a `managed-executor-service` resource of the `ee` subsystem
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-resource()`
* To get the list of all the `managed-executor-service` resource of the `ee` subsystem use '*' for `<managed-executor-service name>`.

## syntax of the operation to get the `active-thread-count` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `active-thread-count` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `active-thread-count` attribute description: the approximate number of threads that are actively executing tasks.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=active-thread-count)`

## syntax of the operation to get the `completed-task-count` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `completed-task-count` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `completed-task-count` attribute description: the approximate total number of tasks that have completed execution.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=completed-task-count)`

## syntax of the operation to get the `context-service` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `context-service` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `context-service` attribute description: the name of the context service to be used by the executor.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=context-service)`

## syntax of the operation to get the `core-threads` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `core-threads` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `core-threads` attribute description: the minimum number of threads to be used by the executor. if left undefined the default `core-size` is calculated based on the number of processors. a value of zero is not advised and in some cases invalid. see the `queue-length` attribute for details on how this value is used to determine the queuing strategy.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=core-threads)`

## syntax of the operation to get the `current-queue-size` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `current-queue-size` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `current-queue-size` attribute description: the current size of the `executor's` task queue.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=current-queue-size)`

## syntax of the operation to get the `hung-task-termination-period` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `hung-task-termination-period` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `hung-task-termination-period` attribute description: the period, in milliseconds, for attempting hung tasks automatic termination, by cancelling such tasks, and interrupting their executing threads. if value is `0`, which is the default, hung tasks are never cancelled.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=hung-task-termination-period)`

## syntax of the operation to get the `hung-task-threshold` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `hung-task-threshold` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `hung-task-threshold` attribute description: the runtime, in milliseconds, for tasks to be considered hung by the managed executor service. if value is `0` tasks are never considered hung.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=hung-task-threshold)`

## syntax of the operation to get the `hung-thread-count` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `hung-thread-count` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `hung-thread-count` attribute description: the number of executor threads that are hung.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=hung-thread-count)`

## syntax of the operation to get the `jndi-name` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `jndi-name` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `jndi-name` attribute description: the `jndi` name to lookup the managed executor service.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=jndi-name)`

## syntax of the operation to get the `keepalive-time` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `keepalive-time` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `keepalive-time` attribute description: when the number of threads is greater than the core, this is the maximum time, in milliseconds, that excess idle threads will wait for new tasks before terminating.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `long-running-tasks` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `long-running-tasks` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `long-running-tasks` attribute description: flag which hints the duration of tasks executed by the executor.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=long-running-tasks)`

## syntax of the operation to get the `max-thread-count` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `max-thread-count` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `max-thread-count` attribute description: the largest number of executor threads.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=max-thread-count)`

## syntax of the operation to get the `max-threads` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `max-threads` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `max-threads` attribute description: the maximum number of threads to be used by the executor. if left undefined the value from `core-size` will be used. this value is ignored if an unbounded queue is used (only `core-threads` will be used in that case).
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=max-threads)`

## syntax of the operation to get the `queue-length` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `queue-length` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `queue-length` attribute description: the executors task queue capacity. a length of `0` means direct `hand-off` and possible rejection will occur. an undefined length (the default), or `integer.max_value`, indicates that an unbounded queue should be used. all other values specify an exact queue size. if an unbounded queue or direct `hand-off` is used, a `core-threads` value greater than zero is required.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=queue-length)`

## syntax of the operation to get the `reject-policy` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `reject-policy` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `reject-policy` attribute description: the policy to be applied to aborted tasks.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=reject-policy)`

## syntax of the operation to get the `task-count` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `task-count` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `task-count` attribute description: the approximate total number of tasks that have ever been submitted for execution.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=task-count)`

## syntax of the operation to get the `thread-count` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `thread-count` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `thread-count` attribute description: the current number of executor threads.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=thread-count)`

## syntax of the operation to get the `thread-factory` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `thread-factory` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `thread-factory` attribute description: the name of the thread factory to be used by the executor.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=thread-factory)`

## syntax of the operation to get the `thread-priority` attribute of the `managed-executor-service` resource of the `ee` subsystem
* Retrieve the `thread-priority` attribute value of the `managed-executor-service` resource of the `ee` subsystem
* `thread-priority` attribute description: the priority applied to threads created by the executor.
* operation: `/subsystem=ee/managed-executor-service=<managed-executor-service name>:read-attribute(name=thread-priority)`

## syntax of the operation to get the `security-enabled` attribute of the `health` subsystem
* Retrieve the `security-enabled` attribute value of the `health` subsystem
* `security-enabled` attribute description: true if authentication is required to access the `http` endpoints on the `http` management interface.
* operation: `/subsystem=health:read-attribute(name=security-enabled)`

## syntax of the operation to get the `auth-realm` attribute of the `remoting` subsystem
* Retrieve the `auth-realm` attribute value of the `remoting` subsystem
* `auth-realm` attribute description: the authentication realm to use if no authentication `callbackhandler` is specified.
* operation: `/subsystem=remoting:read-attribute(name=auth-realm)`

## syntax of the operation to get the `authentication-retries` attribute of the `remoting` subsystem
* Retrieve the `authentication-retries` attribute value of the `remoting` subsystem
* `authentication-retries` attribute description: specify the number of times a client is allowed to retry authentication before closing the connection.
* operation: `/subsystem=remoting:read-attribute(name=authentication-retries)`

## syntax of the operation to get the `authorize-id` attribute of the `remoting` subsystem
* Retrieve the `authorize-id` attribute value of the `remoting` subsystem
* `authorize-id` attribute description: the `sasl` authorization id.  used as authentication user name to use if no authentication `callbackhandler` is specified and the selected `sasl` mechanism demands a user name.
* operation: `/subsystem=remoting:read-attribute(name=authorize-id)`

## syntax of the operation to get the `buffer-region-size` attribute of the `remoting` subsystem
* Retrieve the `buffer-region-size` attribute value of the `remoting` subsystem
* `buffer-region-size` attribute description: the size of allocated buffer regions.
* operation: `/subsystem=remoting:read-attribute(name=buffer-region-size)`

## syntax of the operation to get the `heartbeat-interval` attribute of the `remoting` subsystem
* Retrieve the `heartbeat-interval` attribute value of the `remoting` subsystem
* `heartbeat-interval` attribute description: the interval to use for connection heartbeat, in milliseconds.  if the connection is idle in the outbound direction for this amount of time, a ping message will be sent, which will trigger a corresponding reply message.
* operation: `/subsystem=remoting:read-attribute(name=heartbeat-interval)`

## syntax of the operation to get the `max-inbound-channels` attribute of the `remoting` subsystem
* Retrieve the `max-inbound-channels` attribute value of the `remoting` subsystem
* `max-inbound-channels` attribute description: the maximum number of inbound channels to support for a connection.
* operation: `/subsystem=remoting:read-attribute(name=max-inbound-channels)`

## syntax of the operation to get the `max-inbound-message-size` attribute of the `remoting` subsystem
* Retrieve the `max-inbound-message-size` attribute value of the `remoting` subsystem
* `max-inbound-message-size` attribute description: the maximum inbound message size to be allowed.  messages exceeding this size will cause an exception to be thrown on the reading side as well as the writing side.
* operation: `/subsystem=remoting:read-attribute(name=max-inbound-message-size)`

## syntax of the operation to get the `max-inbound-messages` attribute of the `remoting` subsystem
* Retrieve the `max-inbound-messages` attribute value of the `remoting` subsystem
* `max-inbound-messages` attribute description: the maximum number of concurrent inbound messages on a channel.
* operation: `/subsystem=remoting:read-attribute(name=max-inbound-messages)`

## syntax of the operation to get the `max-outbound-channels` attribute of the `remoting` subsystem
* Retrieve the `max-outbound-channels` attribute value of the `remoting` subsystem
* `max-outbound-channels` attribute description: the maximum number of outbound channels to support for a connection.
* operation: `/subsystem=remoting:read-attribute(name=max-outbound-channels)`

## syntax of the operation to get the `max-outbound-message-size` attribute of the `remoting` subsystem
* Retrieve the `max-outbound-message-size` attribute value of the `remoting` subsystem
* `max-outbound-message-size` attribute description: the maximum outbound message size to send.  no messages larger than this well be transmitted; attempting to do so will cause an exception on the writing side.
* operation: `/subsystem=remoting:read-attribute(name=max-outbound-message-size)`

## syntax of the operation to get the `max-outbound-messages` attribute of the `remoting` subsystem
* Retrieve the `max-outbound-messages` attribute value of the `remoting` subsystem
* `max-outbound-messages` attribute description: the maximum number of concurrent outbound messages on a channel.
* operation: `/subsystem=remoting:read-attribute(name=max-outbound-messages)`

## syntax of the operation to get the `receive-buffer-size` attribute of the `remoting` subsystem
* Retrieve the `receive-buffer-size` attribute value of the `remoting` subsystem
* `receive-buffer-size` attribute description: the size of the largest buffer that this endpoint will accept over a connection.
* operation: `/subsystem=remoting:read-attribute(name=receive-buffer-size)`

## syntax of the operation to get the `receive-window-size` attribute of the `remoting` subsystem
* Retrieve the `receive-window-size` attribute value of the `remoting` subsystem
* `receive-window-size` attribute description: the maximum window size of the receive direction for connection channels, in bytes.
* operation: `/subsystem=remoting:read-attribute(name=receive-window-size)`

## syntax of the operation to get the `sasl-protocol` attribute of the `remoting` subsystem
* Retrieve the `sasl-protocol` attribute value of the `remoting` subsystem
* `sasl-protocol` attribute description: where a `saslserver` or `saslclient` are created by default the protocol specified it '`remoting`', this can be used to override this.
* operation: `/subsystem=remoting:read-attribute(name=sasl-protocol)`

## syntax of the operation to get the `send-buffer-size` attribute of the `remoting` subsystem
* Retrieve the `send-buffer-size` attribute value of the `remoting` subsystem
* `send-buffer-size` attribute description: the size of the largest buffer that this endpoint will transmit over a connection.
* operation: `/subsystem=remoting:read-attribute(name=send-buffer-size)`

## syntax of the operation to get the `server-name` attribute of the `remoting` subsystem
* Retrieve the `server-name` attribute value of the `remoting` subsystem
* `server-name` attribute description: the server side of the connection passes `it's` name to the client in the initial greeting, by default the name is automatically discovered from the local address of the connection or it can be overridden using this.
* operation: `/subsystem=remoting:read-attribute(name=server-name)`

## syntax of the operation to get the `transmit-window-size` attribute of the `remoting` subsystem
* Retrieve the `transmit-window-size` attribute value of the `remoting` subsystem
* `transmit-window-size` attribute description: the maximum window size of the transmit direction for connection channels, in bytes.
* operation: `/subsystem=remoting:read-attribute(name=transmit-window-size)`

## syntax of the operation to get the `worker` attribute of the `remoting` subsystem
* Retrieve the `worker` attribute value of the `remoting` subsystem
* `worker` attribute description: worker to use
* operation: `/subsystem=remoting:read-attribute(name=worker)`

## syntax of the operation to get a `http-connector` resource of the `remoting` subsystem
* operation: `/subsystem=remoting/http-connector=<http-connector name>:read-resource()`
* To get the list of all the `http-connector` resource of the `remoting` subsystem use '*' for `<http-connector name>`.

## syntax of the operation to get the `authentication-provider` attribute of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `authentication-provider` attribute value of the `http-connector` resource of the `remoting` subsystem
* `authentication-provider` attribute description: the "`authentication-provider`" element contains the name of the authentication provider to use for incoming connections.
* operation: `/subsystem=remoting/http-connector=<http-connector name>:read-attribute(name=authentication-provider)`

## syntax of the operation to get the `connector-ref` attribute of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `connector-ref` attribute value of the `http-connector` resource of the `remoting` subsystem
* `connector-ref` attribute description: the name (or names) of a connector in the undertow subsystem to connect to.
* operation: `/subsystem=remoting/http-connector=<http-connector name>:read-attribute(name=connector-ref)`

## syntax of the operation to get the `sasl-authentication-factory` attribute of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `sasl-authentication-factory` attribute value of the `http-connector` resource of the `remoting` subsystem
* `sasl-authentication-factory` attribute description: reference to the `sasl` authentication factory to use for this connector.
* operation: `/subsystem=remoting/http-connector=<http-connector name>:read-attribute(name=sasl-authentication-factory)`

## syntax of the operation to get the `sasl-protocol` attribute of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `sasl-protocol` attribute value of the `http-connector` resource of the `remoting` subsystem
* `sasl-protocol` attribute description: the protocol to pass into the `sasl` mechanisms used for authentication.
* operation: `/subsystem=remoting/http-connector=<http-connector name>:read-attribute(name=sasl-protocol)`

## syntax of the operation to get the `security-realm` attribute of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `security-realm` attribute value of the `http-connector` resource of the `remoting` subsystem
* `security-realm` attribute description: the associated security realm to use for authentication for this connector.
* operation: `/subsystem=remoting/http-connector=<http-connector name>:read-attribute(name=security-realm)`

## syntax of the operation to get the `server-name` attribute of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `server-name` attribute value of the `http-connector` resource of the `remoting` subsystem
* `server-name` attribute description: the server name to send in the initial message exchange and for `sasl` based authentication.
* operation: `/subsystem=remoting/http-connector=<http-connector name>:read-attribute(name=server-name)`

## syntax of the operation to get the `include-mechanisms` attribute of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `include-mechanisms` attribute value of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* `include-mechanisms` attribute description: the optional nested "`include-mechanisms`" element contains a list of allowed `sasl` mechanism names. only mechanisms that are present in this list will be allowed.
* operation: `/subsystem=remoting/http-connector=<http-connector name>/security=sasl:read-attribute(name=include-mechanisms)`

## syntax of the operation to get the `qop` attribute of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `qop` attribute value of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* `qop` attribute description: the optional nested "`qop`" element contains a list of `quality-of-protection` values, in decreasing order of preference.
* operation: `/subsystem=remoting/http-connector=<http-connector name>/security=sasl:read-attribute(name=qop)`

## syntax of the operation to get the `reuse-session` attribute of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `reuse-session` attribute value of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* `reuse-session` attribute description: the optional nested "`reuse-session`" boolean element specifies whether or not the server should attempt to reuse previously authenticated session information.  the mechanism may or may not support such reuse, and other factors may also prevent it.
* operation: `/subsystem=remoting/http-connector=<http-connector name>/security=sasl:read-attribute(name=reuse-session)`

## syntax of the operation to get the `server-auth` attribute of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `server-auth` attribute value of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* `server-auth` attribute description: the optional nested "`server-auth`" boolean element specifies whether the server should authenticate to the client.  not all mechanisms may support this setting.
* operation: `/subsystem=remoting/http-connector=<http-connector name>/security=sasl:read-attribute(name=server-auth)`

## syntax of the operation to get the `strength` attribute of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `strength` attribute value of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* `strength` attribute description: the optional nested "strength" element contains a list of cipher strength values, in decreasing order of preference.
* operation: `/subsystem=remoting/http-connector=<http-connector name>/security=sasl:read-attribute(name=strength)`

## syntax of the operation to get the `forward-secrecy` attribute of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `forward-secrecy` attribute value of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* `forward-secrecy` attribute description: the optional nested "`forward-secrecy`" element contains a boolean value which specifies whether mechanisms that implement forward secrecy between sessions are required. forward secrecy means that breaking into one session will not automatically provide information for breaking into future sessions.
* operation: `/subsystem=remoting/http-connector=<http-connector name>/security=sasl/sasl-policy=policy:read-attribute(name=forward-secrecy)`

## syntax of the operation to get the `no-active` attribute of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `no-active` attribute value of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* `no-active` attribute description: the optional nested "`no-active`" element contains a boolean value which specifies whether mechanisms susceptible to active (`non-dictionary`) attacks are not permitted. "false" to permit, "true" to deny.
* operation: `/subsystem=remoting/http-connector=<http-connector name>/security=sasl/sasl-policy=policy:read-attribute(name=no-active)`

## syntax of the operation to get the `no-anonymous` attribute of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `no-anonymous` attribute value of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* `no-anonymous` attribute description: the optional nested "`no-anonymous`" element contains a boolean value which specifies whether mechanisms that accept anonymous login are permitted.  "false" to permit, "true" to deny.
* operation: `/subsystem=remoting/http-connector=<http-connector name>/security=sasl/sasl-policy=policy:read-attribute(name=no-anonymous)`

## syntax of the operation to get the `no-dictionary` attribute of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `no-dictionary` attribute value of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* `no-dictionary` attribute description: the optional nested "`no-dictionary`" element contains a boolean value which specifies whether mechanisms susceptible to passive dictionary attacks are permitted.  "false" to permit, "true" to deny.
* operation: `/subsystem=remoting/http-connector=<http-connector name>/security=sasl/sasl-policy=policy:read-attribute(name=no-dictionary)`

## syntax of the operation to get the `no-plain-text` attribute of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `no-plain-text` attribute value of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* `no-plain-text` attribute description: the optional nested "`no-plain-text`" element contains a boolean value which specifies whether mechanisms susceptible to simple plain passive attacks (`e.g`., "plain") are not permitted.    "false" to permit, "true" to deny.
* operation: `/subsystem=remoting/http-connector=<http-connector name>/security=sasl/sasl-policy=policy:read-attribute(name=no-plain-text)`

## syntax of the operation to get the `pass-credentials` attribute of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `pass-credentials` attribute value of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* `pass-credentials` attribute description: the optional nested "`pass-credentials`" element contains a boolean value which specifies whether mechanisms that pass client credentials are required.
* operation: `/subsystem=remoting/http-connector=<http-connector name>/security=sasl/sasl-policy=policy:read-attribute(name=pass-credentials)`

## syntax of the operation to get a `property` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* operation: `/subsystem=remoting/http-connector=<http-connector name>/security=sasl/property=<property name>:read-resource()`
* To get the list of all the `property` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem use '*' for `<property name>`.

## syntax of the operation to get the `value` attribute of the `property` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `value` attribute value of the `property` resource of the `sasl` `security` resource of the `http-connector` resource of the `remoting` subsystem
* `value` attribute description: the property value.
* operation: `/subsystem=remoting/http-connector=<http-connector name>/security=sasl/property=<property name>:read-attribute(name=value)`

## syntax of the operation to get a `property` resource of the `http-connector` resource of the `remoting` subsystem
* operation: `/subsystem=remoting/http-connector=<http-connector name>/property=<property name>:read-resource()`
* To get the list of all the `property` resource of the `http-connector` resource of the `remoting` subsystem use '*' for `<property name>`.

## syntax of the operation to get the `value` attribute of the `property` resource of the `http-connector` resource of the `remoting` subsystem
* Retrieve the `value` attribute value of the `property` resource of the `http-connector` resource of the `remoting` subsystem
* `value` attribute description: the property value.
* operation: `/subsystem=remoting/http-connector=<http-connector name>/property=<property name>:read-attribute(name=value)`

## syntax of the operation to get a `outbound-connection` resource of the `remoting` subsystem
* operation: `/subsystem=remoting/outbound-connection=<outbound-connection name>:read-resource()`
* To get the list of all the `outbound-connection` resource of the `remoting` subsystem use '*' for `<outbound-connection name>`.

## syntax of the operation to get the `uri` attribute of the `outbound-connection` resource of the `remoting` subsystem
* Retrieve the `uri` attribute value of the `outbound-connection` resource of the `remoting` subsystem
* `uri` attribute description: the connection uri for the outbound connection.
* operation: `/subsystem=remoting/outbound-connection=<outbound-connection name>:read-attribute(name=uri)`

## syntax of the operation to get a `property` resource of the `outbound-connection` resource of the `remoting` subsystem
* operation: `/subsystem=remoting/outbound-connection=<outbound-connection name>/property=<property name>:read-resource()`
* To get the list of all the `property` resource of the `outbound-connection` resource of the `remoting` subsystem use '*' for `<property name>`.

## syntax of the operation to get the `value` attribute of the `property` resource of the `outbound-connection` resource of the `remoting` subsystem
* Retrieve the `value` attribute value of the `property` resource of the `outbound-connection` resource of the `remoting` subsystem
* `value` attribute description: the property value.
* operation: `/subsystem=remoting/outbound-connection=<outbound-connection name>/property=<property name>:read-attribute(name=value)`

## syntax of the operation to get a `local-outbound-connection` resource of the `remoting` subsystem
* operation: `/subsystem=remoting/local-outbound-connection=<local-outbound-connection name>:read-resource()`
* To get the list of all the `local-outbound-connection` resource of the `remoting` subsystem use '*' for `<local-outbound-connection name>`.

## syntax of the operation to get the `outbound-socket-binding-ref` attribute of the `local-outbound-connection` resource of the `remoting` subsystem
* Retrieve the `outbound-socket-binding-ref` attribute value of the `local-outbound-connection` resource of the `remoting` subsystem
* `outbound-socket-binding-ref` attribute description: name of the `outbound-socket-binding` which will be used to determine the destination address and port for the connection.
* operation: `/subsystem=remoting/local-outbound-connection=<local-outbound-connection name>:read-attribute(name=outbound-socket-binding-ref)`

## syntax of the operation to get a `property` resource of the `local-outbound-connection` resource of the `remoting` subsystem
* operation: `/subsystem=remoting/local-outbound-connection=<local-outbound-connection name>/property=<property name>:read-resource()`
* To get the list of all the `property` resource of the `local-outbound-connection` resource of the `remoting` subsystem use '*' for `<property name>`.

## syntax of the operation to get the `value` attribute of the `property` resource of the `local-outbound-connection` resource of the `remoting` subsystem
* Retrieve the `value` attribute value of the `property` resource of the `local-outbound-connection` resource of the `remoting` subsystem
* `value` attribute description: the property value.
* operation: `/subsystem=remoting/local-outbound-connection=<local-outbound-connection name>/property=<property name>:read-attribute(name=value)`

## syntax of the operation to get a `connector` resource of the `remoting` subsystem
* operation: `/subsystem=remoting/connector=<connector name>:read-resource()`
* To get the list of all the `connector` resource of the `remoting` subsystem use '*' for `<connector name>`.

## syntax of the operation to get the `authentication-provider` attribute of the `connector` resource of the `remoting` subsystem
* Retrieve the `authentication-provider` attribute value of the `connector` resource of the `remoting` subsystem
* `authentication-provider` attribute description: the "`authentication-provider`" element contains the name of the authentication provider to use for incoming connections.
* operation: `/subsystem=remoting/connector=<connector name>:read-attribute(name=authentication-provider)`

## syntax of the operation to get the `protocol` attribute of the `connector` resource of the `remoting` subsystem
* Retrieve the `protocol` attribute value of the `connector` resource of the `remoting` subsystem
* `protocol` attribute description: protocol used in the connection.
* operation: `/subsystem=remoting/connector=<connector name>:read-attribute(name=protocol)`

## syntax of the operation to get the `sasl-authentication-factory` attribute of the `connector` resource of the `remoting` subsystem
* Retrieve the `sasl-authentication-factory` attribute value of the `connector` resource of the `remoting` subsystem
* `sasl-authentication-factory` attribute description: reference to the `sasl` authentication factory to secure this connector.
* operation: `/subsystem=remoting/connector=<connector name>:read-attribute(name=sasl-authentication-factory)`

## syntax of the operation to get the `sasl-protocol` attribute of the `connector` resource of the `remoting` subsystem
* Retrieve the `sasl-protocol` attribute value of the `connector` resource of the `remoting` subsystem
* `sasl-protocol` attribute description: the protocol to pass into the `sasl` mechanisms used for authentication.
* operation: `/subsystem=remoting/connector=<connector name>:read-attribute(name=sasl-protocol)`

## syntax of the operation to get the `security-realm` attribute of the `connector` resource of the `remoting` subsystem
* Retrieve the `security-realm` attribute value of the `connector` resource of the `remoting` subsystem
* `security-realm` attribute description: the associated security realm to use for authentication for this connector.
* operation: `/subsystem=remoting/connector=<connector name>:read-attribute(name=security-realm)`

## syntax of the operation to get the `server-name` attribute of the `connector` resource of the `remoting` subsystem
* Retrieve the `server-name` attribute value of the `connector` resource of the `remoting` subsystem
* `server-name` attribute description: the server name to send in the initial message exchange and for `sasl` based authentication.
* operation: `/subsystem=remoting/connector=<connector name>:read-attribute(name=server-name)`

## syntax of the operation to get the `socket-binding` attribute of the `connector` resource of the `remoting` subsystem
* Retrieve the `socket-binding` attribute value of the `connector` resource of the `remoting` subsystem
* `socket-binding` attribute description: the name of the socket binding to attach to.
* operation: `/subsystem=remoting/connector=<connector name>:read-attribute(name=socket-binding)`

## syntax of the operation to get the `ssl-context` attribute of the `connector` resource of the `remoting` subsystem
* Retrieve the `ssl-context` attribute value of the `connector` resource of the `remoting` subsystem
* `ssl-context` attribute description: reference to the `sslcontext` to use for this connector.
* operation: `/subsystem=remoting/connector=<connector name>:read-attribute(name=ssl-context)`

## syntax of the operation to get the `include-mechanisms` attribute of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* Retrieve the `include-mechanisms` attribute value of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* `include-mechanisms` attribute description: the optional nested "`include-mechanisms`" element contains a list of allowed `sasl` mechanism names. only mechanisms that are present in this list will be allowed.
* operation: `/subsystem=remoting/connector=<connector name>/security=sasl:read-attribute(name=include-mechanisms)`

## syntax of the operation to get the `qop` attribute of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* Retrieve the `qop` attribute value of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* `qop` attribute description: the optional nested "`qop`" element contains a list of `quality-of-protection` values, in decreasing order of preference.
* operation: `/subsystem=remoting/connector=<connector name>/security=sasl:read-attribute(name=qop)`

## syntax of the operation to get the `reuse-session` attribute of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* Retrieve the `reuse-session` attribute value of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* `reuse-session` attribute description: the optional nested "`reuse-session`" boolean element specifies whether or not the server should attempt to reuse previously authenticated session information.  the mechanism may or may not support such reuse, and other factors may also prevent it.
* operation: `/subsystem=remoting/connector=<connector name>/security=sasl:read-attribute(name=reuse-session)`

## syntax of the operation to get the `server-auth` attribute of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* Retrieve the `server-auth` attribute value of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* `server-auth` attribute description: the optional nested "`server-auth`" boolean element specifies whether the server should authenticate to the client.  not all mechanisms may support this setting.
* operation: `/subsystem=remoting/connector=<connector name>/security=sasl:read-attribute(name=server-auth)`

## syntax of the operation to get the `strength` attribute of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* Retrieve the `strength` attribute value of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* `strength` attribute description: the optional nested "strength" element contains a list of cipher strength values, in decreasing order of preference.
* operation: `/subsystem=remoting/connector=<connector name>/security=sasl:read-attribute(name=strength)`

## syntax of the operation to get the `forward-secrecy` attribute of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* Retrieve the `forward-secrecy` attribute value of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* `forward-secrecy` attribute description: the optional nested "`forward-secrecy`" element contains a boolean value which specifies whether mechanisms that implement forward secrecy between sessions are required. forward secrecy means that breaking into one session will not automatically provide information for breaking into future sessions.
* operation: `/subsystem=remoting/connector=<connector name>/security=sasl/sasl-policy=policy:read-attribute(name=forward-secrecy)`

## syntax of the operation to get the `no-active` attribute of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* Retrieve the `no-active` attribute value of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* `no-active` attribute description: the optional nested "`no-active`" element contains a boolean value which specifies whether mechanisms susceptible to active (`non-dictionary`) attacks are not permitted. "false" to permit, "true" to deny.
* operation: `/subsystem=remoting/connector=<connector name>/security=sasl/sasl-policy=policy:read-attribute(name=no-active)`

## syntax of the operation to get the `no-anonymous` attribute of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* Retrieve the `no-anonymous` attribute value of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* `no-anonymous` attribute description: the optional nested "`no-anonymous`" element contains a boolean value which specifies whether mechanisms that accept anonymous login are permitted.  "false" to permit, "true" to deny.
* operation: `/subsystem=remoting/connector=<connector name>/security=sasl/sasl-policy=policy:read-attribute(name=no-anonymous)`

## syntax of the operation to get the `no-dictionary` attribute of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* Retrieve the `no-dictionary` attribute value of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* `no-dictionary` attribute description: the optional nested "`no-dictionary`" element contains a boolean value which specifies whether mechanisms susceptible to passive dictionary attacks are permitted.  "false" to permit, "true" to deny.
* operation: `/subsystem=remoting/connector=<connector name>/security=sasl/sasl-policy=policy:read-attribute(name=no-dictionary)`

## syntax of the operation to get the `no-plain-text` attribute of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* Retrieve the `no-plain-text` attribute value of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* `no-plain-text` attribute description: the optional nested "`no-plain-text`" element contains a boolean value which specifies whether mechanisms susceptible to simple plain passive attacks (`e.g`., "plain") are not permitted.    "false" to permit, "true" to deny.
* operation: `/subsystem=remoting/connector=<connector name>/security=sasl/sasl-policy=policy:read-attribute(name=no-plain-text)`

## syntax of the operation to get the `pass-credentials` attribute of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* Retrieve the `pass-credentials` attribute value of the `policy` `sasl-policy` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* `pass-credentials` attribute description: the optional nested "`pass-credentials`" element contains a boolean value which specifies whether mechanisms that pass client credentials are required.
* operation: `/subsystem=remoting/connector=<connector name>/security=sasl/sasl-policy=policy:read-attribute(name=pass-credentials)`

## syntax of the operation to get a `property` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* operation: `/subsystem=remoting/connector=<connector name>/security=sasl/property=<property name>:read-resource()`
* To get the list of all the `property` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem use '*' for `<property name>`.

## syntax of the operation to get the `value` attribute of the `property` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* Retrieve the `value` attribute value of the `property` resource of the `sasl` `security` resource of the `connector` resource of the `remoting` subsystem
* `value` attribute description: the property value.
* operation: `/subsystem=remoting/connector=<connector name>/security=sasl/property=<property name>:read-attribute(name=value)`

## syntax of the operation to get a `property` resource of the `connector` resource of the `remoting` subsystem
* operation: `/subsystem=remoting/connector=<connector name>/property=<property name>:read-resource()`
* To get the list of all the `property` resource of the `connector` resource of the `remoting` subsystem use '*' for `<property name>`.

## syntax of the operation to get the `value` attribute of the `property` resource of the `connector` resource of the `remoting` subsystem
* Retrieve the `value` attribute value of the `property` resource of the `connector` resource of the `remoting` subsystem
* `value` attribute description: the property value.
* operation: `/subsystem=remoting/connector=<connector name>/property=<property name>:read-attribute(name=value)`

## syntax of the operation to get a `remote-outbound-connection` resource of the `remoting` subsystem
* operation: `/subsystem=remoting/remote-outbound-connection=<remote-outbound-connection name>:read-resource()`
* To get the list of all the `remote-outbound-connection` resource of the `remoting` subsystem use '*' for `<remote-outbound-connection name>`.

## syntax of the operation to get the `authentication-context` attribute of the `remote-outbound-connection` resource of the `remoting` subsystem
* Retrieve the `authentication-context` attribute value of the `remote-outbound-connection` resource of the `remoting` subsystem
* `authentication-context` attribute description: reference to the authentication context instance containing the configuration for outbound connections.
* operation: `/subsystem=remoting/remote-outbound-connection=<remote-outbound-connection name>:read-attribute(name=authentication-context)`

## syntax of the operation to get the `outbound-socket-binding-ref` attribute of the `remote-outbound-connection` resource of the `remoting` subsystem
* Retrieve the `outbound-socket-binding-ref` attribute value of the `remote-outbound-connection` resource of the `remoting` subsystem
* `outbound-socket-binding-ref` attribute description: name of the `outbound-socket-binding` which will be used to determine the destination address and port for the connection.
* operation: `/subsystem=remoting/remote-outbound-connection=<remote-outbound-connection name>:read-attribute(name=outbound-socket-binding-ref)`

## syntax of the operation to get the `protocol` attribute of the `remote-outbound-connection` resource of the `remoting` subsystem
* Retrieve the `protocol` attribute value of the `remote-outbound-connection` resource of the `remoting` subsystem
* `protocol` attribute description: the protocol to use for the remote connection.
* operation: `/subsystem=remoting/remote-outbound-connection=<remote-outbound-connection name>:read-attribute(name=protocol)`

## syntax of the operation to get the `security-realm` attribute of the `remote-outbound-connection` resource of the `remoting` subsystem
* Retrieve the `security-realm` attribute value of the `remote-outbound-connection` resource of the `remoting` subsystem
* `security-realm` attribute description: reference to the security realm to use to obtain the password and `ssl` configuration.
* operation: `/subsystem=remoting/remote-outbound-connection=<remote-outbound-connection name>:read-attribute(name=security-realm)`

## syntax of the operation to get the `username` attribute of the `remote-outbound-connection` resource of the `remoting` subsystem
* Retrieve the `username` attribute value of the `remote-outbound-connection` resource of the `remoting` subsystem
* `username` attribute description: the user name to use when authenticating against the remote server.
* operation: `/subsystem=remoting/remote-outbound-connection=<remote-outbound-connection name>:read-attribute(name=username)`

## syntax of the operation to get a `property` resource of the `remote-outbound-connection` resource of the `remoting` subsystem
* operation: `/subsystem=remoting/remote-outbound-connection=<remote-outbound-connection name>/property=<property name>:read-resource()`
* To get the list of all the `property` resource of the `remote-outbound-connection` resource of the `remoting` subsystem use '*' for `<property name>`.

## syntax of the operation to get the `value` attribute of the `property` resource of the `remote-outbound-connection` resource of the `remoting` subsystem
* Retrieve the `value` attribute value of the `property` resource of the `remote-outbound-connection` resource of the `remoting` subsystem
* `value` attribute description: the property value.
* operation: `/subsystem=remoting/remote-outbound-connection=<remote-outbound-connection name>/property=<property name>:read-attribute(name=value)`

## syntax of the operation to get the `non-core-mbean-sensitivity` attribute of the `jmx` subsystem
* Retrieve the `non-core-mbean-sensitivity` attribute value of the `jmx` subsystem
* `non-core-mbean-sensitivity` attribute description: whether or not core `mbeans`, `i.e`. `mbeans` not coming from the model controller, should be considered sensitive.
* operation: `/subsystem=jmx:read-attribute(name=non-core-mbean-sensitivity)`

## syntax of the operation to get the `show-model` attribute of the `jmx` subsystem
* Retrieve the `show-model` attribute value of the `jmx` subsystem
* `show-model` attribute description: alias for the existence of the 'resolved' model controller `jmx` facade. when writing, if set to 'true' it will add the 'resolved' model controller `jmx` facade resource with the default domain name.
* operation: `/subsystem=jmx:read-attribute(name=show-model)`

## syntax of the operation to get the `enabled` attribute of the `audit-log` `configuration` resource of the `jmx` subsystem
* Retrieve the `enabled` attribute value of the `audit-log` `configuration` resource of the `jmx` subsystem
* `enabled` attribute description: whether audit logging is enabled.
* operation: `/subsystem=jmx/configuration=audit-log:read-attribute(name=enabled)`

## syntax of the operation to get the `log-boot` attribute of the `audit-log` `configuration` resource of the `jmx` subsystem
* Retrieve the `log-boot` attribute value of the `audit-log` `configuration` resource of the `jmx` subsystem
* `log-boot` attribute description: whether operations should be logged on server boot.
* operation: `/subsystem=jmx/configuration=audit-log:read-attribute(name=log-boot)`

## syntax of the operation to get the `log-read-only` attribute of the `audit-log` `configuration` resource of the `jmx` subsystem
* Retrieve the `log-read-only` attribute value of the `audit-log` `configuration` resource of the `jmx` subsystem
* `log-read-only` attribute description: whether operations that do not modify the configuration or any runtime services should be logged.
* operation: `/subsystem=jmx/configuration=audit-log:read-attribute(name=log-read-only)`

## syntax of the operation to get a `handler` resource of the `audit-log` `configuration` resource of the `jmx` subsystem
* operation: `/subsystem=jmx/configuration=audit-log/handler=<handler name>:read-resource()`
* To get the list of all the `handler` resource of the `audit-log` `configuration` resource of the `jmx` subsystem use '*' for `<handler name>`.

## syntax of the operation to get the `use-management-endpoint` attribute of the `jmx` `remoting-connector` resource of the `jmx` subsystem
* Retrieve the `use-management-endpoint` attribute value of the `jmx` `remoting-connector` resource of the `jmx` subsystem
* `use-management-endpoint` attribute description: if true the connector will use the management endpoint, otherwise it will use the `remoting` subsystem one
* operation: `/subsystem=jmx/remoting-connector=jmx:read-attribute(name=use-management-endpoint)`

## syntax of the operation to get the `domain-name` attribute of the `resolved` `expose-model` resource of the `jmx` subsystem
* Retrieve the `domain-name` attribute value of the `resolved` `expose-model` resource of the `jmx` subsystem
* `domain-name` attribute description: the domain name to use for the 'resolved' model controller `jmx` facade in the `mbeanserver`.
* operation: `/subsystem=jmx/expose-model=resolved:read-attribute(name=domain-name)`

## syntax of the operation to get the `proper-property-format` attribute of the `resolved` `expose-model` resource of the `jmx` subsystem
* Retrieve the `proper-property-format` attribute value of the `resolved` `expose-model` resource of the `jmx` subsystem
* `proper-property-format` attribute description: if false, property type attributes are represented as a `dmr` string, this is the legacy behaviour. if true, property type attributes are represented by a composite type where the key is a string, and the value has the same type as the property in the underlying model.
* operation: `/subsystem=jmx/expose-model=resolved:read-attribute(name=proper-property-format)`

## syntax of the operation to get the `domain-name` attribute of the `expression` `expose-model` resource of the `jmx` subsystem
* Retrieve the `domain-name` attribute value of the `expression` `expose-model` resource of the `jmx` subsystem
* `domain-name` attribute description: the domain name to use for the 'expression' model controller `jmx` facade in the `mbeanserver`.
* operation: `/subsystem=jmx/expose-model=expression:read-attribute(name=domain-name)`

## syntax of the operation to get the `default` attribute of the `singleton` subsystem
* Retrieve the `default` attribute value of the `singleton` subsystem
* `default` attribute description: the default singleton policy
* operation: `/subsystem=singleton:read-attribute(name=default)`

## syntax of the operation to get a `singleton-policy` resource of the `singleton` subsystem
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>:read-resource()`
* To get the list of all the `singleton-policy` resource of the `singleton` subsystem use '*' for `<singleton-policy name>`.

## syntax of the operation to get the `cache` attribute of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `cache` attribute value of the `singleton-policy` resource of the `singleton` subsystem
* `cache` attribute description: the cache backing the singleton `policy's` singleton service. if undefined, the default cache of the specified `cache-container` is used.
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>:read-attribute(name=cache)`

## syntax of the operation to get the `cache-container` attribute of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `cache-container` attribute value of the `singleton-policy` resource of the `singleton` subsystem
* `cache-container` attribute description: the cache container backing the singleton `policy's` singleton service
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>:read-attribute(name=cache-container)`

## syntax of the operation to get the `quorum` attribute of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `quorum` attribute value of the `singleton-policy` resource of the `singleton` subsystem
* `quorum` attribute description: the minimum number of nodes required before this singleton service will start
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>:read-attribute(name=quorum)`

## syntax of the operation to get the `name-preferences` attribute of the `random` `election-policy` resource of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `name-preferences` attribute value of the `random` `election-policy` resource of the `singleton-policy` resource of the `singleton` subsystem
* `name-preferences` attribute description: the list of preferred node names
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>/election-policy=random:read-attribute(name=name-preferences)`

## syntax of the operation to get the `socket-binding-preferences` attribute of the `random` `election-policy` resource of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `socket-binding-preferences` attribute value of the `random` `election-policy` resource of the `singleton-policy` resource of the `singleton` subsystem
* `socket-binding-preferences` attribute description: the list of preferred node socket bindings
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>/election-policy=random:read-attribute(name=socket-binding-preferences)`

## syntax of the operation to get the `name-preferences` attribute of the `simple` `election-policy` resource of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `name-preferences` attribute value of the `simple` `election-policy` resource of the `singleton-policy` resource of the `singleton` subsystem
* `name-preferences` attribute description: the list of preferred node names
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>/election-policy=simple:read-attribute(name=name-preferences)`

## syntax of the operation to get the `position` attribute of the `simple` `election-policy` resource of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `position` attribute value of the `simple` `election-policy` resource of the `singleton-policy` resource of the `singleton` subsystem
* `position` attribute description: the position in the membership list from which the primary singleton provider will be chosen
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>/election-policy=simple:read-attribute(name=position)`

## syntax of the operation to get the `socket-binding-preferences` attribute of the `simple` `election-policy` resource of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `socket-binding-preferences` attribute value of the `simple` `election-policy` resource of the `singleton-policy` resource of the `singleton` subsystem
* `socket-binding-preferences` attribute description: the list of preferred node socket bindings
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>/election-policy=simple:read-attribute(name=socket-binding-preferences)`

## syntax of the operation to get a `deployment` resource of the `singleton-policy` resource of the `singleton` subsystem
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>/deployment=<deployment name>:read-resource()`
* To get the list of all the `deployment` resource of the `singleton-policy` resource of the `singleton` subsystem use '*' for `<deployment name>`.

## syntax of the operation to get the `is-primary` attribute of the `deployment` resource of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `is-primary` attribute value of the `deployment` resource of the `singleton-policy` resource of the `singleton` subsystem
* `is-primary` attribute description: indicates whether the local member is the primary provider of this singleton deployment
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>/deployment=<deployment name>:read-attribute(name=is-primary)`

## syntax of the operation to get the `primary-provider` attribute of the `deployment` resource of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `primary-provider` attribute value of the `deployment` resource of the `singleton-policy` resource of the `singleton` subsystem
* `primary-provider` attribute description: the primary provider of this singleton deployment
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>/deployment=<deployment name>:read-attribute(name=primary-provider)`

## syntax of the operation to get the `providers` attribute of the `deployment` resource of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `providers` attribute value of the `deployment` resource of the `singleton-policy` resource of the `singleton` subsystem
* `providers` attribute description: the members on which the singleton deployment is installed
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>/deployment=<deployment name>:read-attribute(name=providers)`

## syntax of the operation to get a `service` resource of the `singleton-policy` resource of the `singleton` subsystem
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>/service=<service name>:read-resource()`
* To get the list of all the `service` resource of the `singleton-policy` resource of the `singleton` subsystem use '*' for `<service name>`.

## syntax of the operation to get the `is-primary` attribute of the `service` resource of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `is-primary` attribute value of the `service` resource of the `singleton-policy` resource of the `singleton` subsystem
* `is-primary` attribute description: indicates whether the local member is the primary provider of this singleton service
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>/service=<service name>:read-attribute(name=is-primary)`

## syntax of the operation to get the `primary-provider` attribute of the `service` resource of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `primary-provider` attribute value of the `service` resource of the `singleton-policy` resource of the `singleton` subsystem
* `primary-provider` attribute description: the primary provider of this singleton service
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>/service=<service name>:read-attribute(name=primary-provider)`

## syntax of the operation to get the `providers` attribute of the `service` resource of the `singleton-policy` resource of the `singleton` subsystem
* Retrieve the `providers` attribute value of the `service` resource of the `singleton-policy` resource of the `singleton` subsystem
* `providers` attribute description: the members on which the singleton service is installed
* operation: `/subsystem=singleton/singleton-policy=<singleton-policy name>/service=<service name>:read-attribute(name=providers)`

## syntax of the operation to get the `development-mode` attribute of the `weld` subsystem
* Retrieve the `development-mode` attribute value of the `weld` subsystem
* `development-mode` attribute description: weld comes with a special mode for application development. when the development mode is enabled, certain `built-in` tools, which facilitate the development of jakarta contexts and dependency injection applications are available. setting this attribute to true activates the development mode.
* operation: `/subsystem=weld:read-attribute(name=development-mode)`

## syntax of the operation to get the `legacy-empty-beans-xml-treatment` attribute of the `weld` subsystem
* Retrieve the `legacy-empty-beans-xml-treatment` attribute value of the `weld` subsystem
* `legacy-empty-beans-xml-treatment` attribute description: if true, all bean archives with empty `beans.xml` are considered to have discovery mode all.
* operation: `/subsystem=weld:read-attribute(name=legacy-empty-beans-xml-treatment)`

## syntax of the operation to get the `non-portable-mode` attribute of the `weld` subsystem
* Retrieve the `non-portable-mode` attribute value of the `weld` subsystem
* `non-portable-mode` attribute description: if true, then the `non-portable` mode is enabled. the `non-portable` mode is suggested by the specification to overcome problems with legacy applications that do not use jakarta contexts and dependency injection `spi` properly and may be rejected by more strict validation in `cdi` `1.1`.
* operation: `/subsystem=weld:read-attribute(name=non-portable-mode)`

## syntax of the operation to get the `require-bean-descriptor` attribute of the `weld` subsystem
* Retrieve the `require-bean-descriptor` attribute value of the `weld` subsystem
* `require-bean-descriptor` attribute description: if true, then implicit bean archives without a bean descriptor file (`beans.xml`) are ignored by weld.
* operation: `/subsystem=weld:read-attribute(name=require-bean-descriptor)`

## syntax of the operation to get the `thread-pool-size` attribute of the `weld` subsystem
* Retrieve the `thread-pool-size` attribute value of the `weld` subsystem
* `thread-pool-size` attribute description: the number of threads to be used by the weld thread pool. the pool is shared across all jakarta contexts and dependency injection enabled deployments and used primarily for parallel weld bootstrapping.
* operation: `/subsystem=weld:read-attribute(name=thread-pool-size)`

## syntax of the operation to get the `host` attribute of the `microprofile-lra-coordinator` subsystem
* Retrieve the `host` attribute value of the `microprofile-lra-coordinator` subsystem
* `host` attribute description: name of the undertow subsystem 'host' resource that the `lra` coordinator will be deployed to.
* operation: `/subsystem=microprofile-lra-coordinator:read-attribute(name=host)`

## syntax of the operation to get the `server` attribute of the `microprofile-lra-coordinator` subsystem
* Retrieve the `server` attribute value of the `microprofile-lra-coordinator` subsystem
* `server` attribute description: name of the undertow subsystem 'server' resource that the `lra` coordinator will be deployed to.
* operation: `/subsystem=microprofile-lra-coordinator:read-attribute(name=server)`

## syntax of the operation to get the `default-channel` attribute of the `jgroups` subsystem
* Retrieve the `default-channel` attribute value of the `jgroups` subsystem
* `default-channel` attribute description: the default `jgroups` channel.
* operation: `/subsystem=jgroups:read-attribute(name=default-channel)`

## syntax of the operation to get a `channel` resource of the `jgroups` subsystem
* operation: `/subsystem=jgroups/channel=<channel name>:read-resource()`
* To get the list of all the `channel` resource of the `jgroups` subsystem use '*' for `<channel name>`.

## syntax of the operation to get the `address` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `address` attribute value of the `channel` resource of the `jgroups` subsystem
* `address` attribute description: the `ip` address of the channel.
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=address)`

## syntax of the operation to get the `address-as-uuid` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `address-as-uuid` attribute value of the `channel` resource of the `jgroups` subsystem
* `address-as-uuid` attribute description: the address of the channel as a `uuid`.
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=address-as-uuid)`

## syntax of the operation to get the `cluster` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `cluster` attribute value of the `channel` resource of the `jgroups` subsystem
* `cluster` attribute description: the cluster name of the `jgroups` channel. if undefined, the name of the channel will be used.
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=cluster)`

## syntax of the operation to get the `discard-own-messages` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `discard-own-messages` attribute value of the `channel` resource of the `jgroups` subsystem
* `discard-own-messages` attribute description: if true, do not receive messages sent by this node (ourself).
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=discard-own-messages)`

## syntax of the operation to get the `module` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `module` attribute value of the `channel` resource of the `jgroups` subsystem
* `module` attribute description: the module from which to load channel services
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=module)`

## syntax of the operation to get the `received-bytes` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `received-bytes` attribute value of the `channel` resource of the `jgroups` subsystem
* `received-bytes` attribute description: the number of bytes received by this channel.
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=received-bytes)`

## syntax of the operation to get the `received-messages` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `received-messages` attribute value of the `channel` resource of the `jgroups` subsystem
* `received-messages` attribute description: the number of messages received by this channel.
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=received-messages)`

## syntax of the operation to get the `sent-bytes` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `sent-bytes` attribute value of the `channel` resource of the `jgroups` subsystem
* `sent-bytes` attribute description: the number of bytes sent by this channel.
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=sent-bytes)`

## syntax of the operation to get the `sent-messages` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `sent-messages` attribute value of the `channel` resource of the `jgroups` subsystem
* `sent-messages` attribute description: the number of messages sent by this channel.
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=sent-messages)`

## syntax of the operation to get the `stack` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `stack` attribute value of the `channel` resource of the `jgroups` subsystem
* `stack` attribute description: the protocol stack of the `jgroups` channel
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=stack)`

## syntax of the operation to get the `state` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `state` attribute value of the `channel` resource of the `jgroups` subsystem
* `state` attribute description: the state of the channel (open, connecting, connected, closed).
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=state)`

## syntax of the operation to get the `statistics-enabled` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `statistics-enabled` attribute value of the `channel` resource of the `jgroups` subsystem
* `statistics-enabled` attribute description: if enabled, collect channel statistics.
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `version` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `version` attribute value of the `channel` resource of the `jgroups` subsystem
* `version` attribute description: the `jgroups` software version.
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=version)`

## syntax of the operation to get the `view` attribute of the `channel` resource of the `jgroups` subsystem
* Retrieve the `view` attribute value of the `channel` resource of the `jgroups` subsystem
* `view` attribute description: the `channel's` view of group membership.
* operation: `/subsystem=jgroups/channel=<channel name>:read-attribute(name=view)`

## syntax of the operation to get a `fork` resource of the `channel` resource of the `jgroups` subsystem
* operation: `/subsystem=jgroups/channel=<channel name>/fork=<fork name>:read-resource()`
* To get the list of all the `fork` resource of the `channel` resource of the `jgroups` subsystem use '*' for `<fork name>`.

## syntax of the operation to get a `protocol` resource of the `fork` resource of the `channel` resource of the `jgroups` subsystem
* operation: `/subsystem=jgroups/channel=<channel name>/fork=<fork name>/protocol=<protocol name>:read-resource()`
* To get the list of all the `protocol` resource of the `fork` resource of the `channel` resource of the `jgroups` subsystem use '*' for `<protocol name>`.

## syntax of the operation to get the `module` attribute of the `protocol` resource of the `fork` resource of the `channel` resource of the `jgroups` subsystem
* Retrieve the `module` attribute value of the `protocol` resource of the `fork` resource of the `channel` resource of the `jgroups` subsystem
* `module` attribute description: the module with which to resolve the protocol type.
* operation: `/subsystem=jgroups/channel=<channel name>/fork=<fork name>/protocol=<protocol name>:read-attribute(name=module)`

## syntax of the operation to get the `properties` attribute of the `protocol` resource of the `fork` resource of the `channel` resource of the `jgroups` subsystem
* Retrieve the `properties` attribute value of the `protocol` resource of the `fork` resource of the `channel` resource of the `jgroups` subsystem
* `properties` attribute description: the properties of this protocol.
* operation: `/subsystem=jgroups/channel=<channel name>/fork=<fork name>/protocol=<protocol name>:read-attribute(name=properties)`

## syntax of the operation to get the `statistics-enabled` attribute of the `protocol` resource of the `fork` resource of the `channel` resource of the `jgroups` subsystem
* Retrieve the `statistics-enabled` attribute value of the `protocol` resource of the `fork` resource of the `channel` resource of the `jgroups` subsystem
* `statistics-enabled` attribute description: indicates whether or not this protocol will collect statistics overriding stack configuration.
* operation: `/subsystem=jgroups/channel=<channel name>/fork=<fork name>/protocol=<protocol name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get a `stack` resource of the `jgroups` subsystem
* operation: `/subsystem=jgroups/stack=<stack name>:read-resource()`
* To get the list of all the `stack` resource of the `jgroups` subsystem use '*' for `<stack name>`.

## syntax of the operation to get the `statistics-enabled` attribute of the `stack` resource of the `jgroups` subsystem
* Retrieve the `statistics-enabled` attribute value of the `stack` resource of the `jgroups` subsystem
* `statistics-enabled` attribute description: indicates whether or not all protocols in the stack will collect statistics by default.
* operation: `/subsystem=jgroups/stack=<stack name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get a `protocol` resource of the `stack` resource of the `jgroups` subsystem
* operation: `/subsystem=jgroups/stack=<stack name>/protocol=<protocol name>:read-resource()`
* To get the list of all the `protocol` resource of the `stack` resource of the `jgroups` subsystem use '*' for `<protocol name>`.

## syntax of the operation to get the `module` attribute of the `protocol` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `module` attribute value of the `protocol` resource of the `stack` resource of the `jgroups` subsystem
* `module` attribute description: the module with which to resolve the protocol type.
* operation: `/subsystem=jgroups/stack=<stack name>/protocol=<protocol name>:read-attribute(name=module)`

## syntax of the operation to get the `properties` attribute of the `protocol` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `properties` attribute value of the `protocol` resource of the `stack` resource of the `jgroups` subsystem
* `properties` attribute description: the properties of this protocol.
* operation: `/subsystem=jgroups/stack=<stack name>/protocol=<protocol name>:read-attribute(name=properties)`

## syntax of the operation to get the `statistics-enabled` attribute of the `protocol` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `statistics-enabled` attribute value of the `protocol` resource of the `stack` resource of the `jgroups` subsystem
* `statistics-enabled` attribute description: indicates whether or not this protocol will collect statistics overriding stack configuration.
* operation: `/subsystem=jgroups/stack=<stack name>/protocol=<protocol name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get a `transport` resource of the `stack` resource of the `jgroups` subsystem
* operation: `/subsystem=jgroups/stack=<stack name>/transport=<transport name>:read-resource()`
* To get the list of all the `transport` resource of the `stack` resource of the `jgroups` subsystem use '*' for `<transport name>`.

## syntax of the operation to get the `diagnostics-socket-binding` attribute of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `diagnostics-socket-binding` attribute value of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* `diagnostics-socket-binding` attribute description: the diagnostics socket binding specification for this protocol layer, used to specify `ip` interfaces and ports for communication.
* operation: `/subsystem=jgroups/stack=<stack name>/transport=<transport name>:read-attribute(name=diagnostics-socket-binding)`

## syntax of the operation to get the `machine` attribute of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `machine` attribute value of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* `machine` attribute description: the machine (`i.e`. host) identifier for this node. used by `infinispan` `topology-aware` consistent hash.
* operation: `/subsystem=jgroups/stack=<stack name>/transport=<transport name>:read-attribute(name=machine)`

## syntax of the operation to get the `module` attribute of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `module` attribute value of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* `module` attribute description: the module with which to resolve the protocol type.
* operation: `/subsystem=jgroups/stack=<stack name>/transport=<transport name>:read-attribute(name=module)`

## syntax of the operation to get the `properties` attribute of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `properties` attribute value of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* `properties` attribute description: the properties of this protocol.
* operation: `/subsystem=jgroups/stack=<stack name>/transport=<transport name>:read-attribute(name=properties)`

## syntax of the operation to get the `rack` attribute of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `rack` attribute value of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* `rack` attribute description: the rack (`i.e`. server rack) identifier for this node. used by `infinispan` `topology-aware` consistent hash.
* operation: `/subsystem=jgroups/stack=<stack name>/transport=<transport name>:read-attribute(name=rack)`

## syntax of the operation to get the `site` attribute of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `site` attribute value of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* `site` attribute description: the site (`i.e`. data centre) identifier for this node. used by `infinispan` `topology-aware` consistent hash.
* operation: `/subsystem=jgroups/stack=<stack name>/transport=<transport name>:read-attribute(name=site)`

## syntax of the operation to get the `socket-binding` attribute of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `socket-binding` attribute value of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* `socket-binding` attribute description: defines the bind `address/port` used of the server socket used to receive messages from other cluster members.
* operation: `/subsystem=jgroups/stack=<stack name>/transport=<transport name>:read-attribute(name=socket-binding)`

## syntax of the operation to get the `statistics-enabled` attribute of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `statistics-enabled` attribute value of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* `statistics-enabled` attribute description: indicates whether or not this protocol will collect statistics overriding stack configuration.
* operation: `/subsystem=jgroups/stack=<stack name>/transport=<transport name>:read-attribute(name=statistics-enabled)`

## syntax of the operation to get the `keepalive-time` attribute of the `default` `thread-pool` resource of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `keepalive-time` attribute value of the `default` `thread-pool` resource of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* `keepalive-time` attribute description: used to specify the amount of milliseconds that pool threads should be kept running when idle; if not specified, threads will run until the executor is shut down.
* operation: `/subsystem=jgroups/stack=<stack name>/transport=<transport name>/thread-pool=default:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `max-threads` attribute of the `default` `thread-pool` resource of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `max-threads` attribute value of the `default` `thread-pool` resource of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* `max-threads` attribute description: the maximum thread pool size.
* operation: `/subsystem=jgroups/stack=<stack name>/transport=<transport name>/thread-pool=default:read-attribute(name=max-threads)`

## syntax of the operation to get the `min-threads` attribute of the `default` `thread-pool` resource of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `min-threads` attribute value of the `default` `thread-pool` resource of the `transport` resource of the `stack` resource of the `jgroups` subsystem
* `min-threads` attribute description: the core thread pool size which is smaller than the maximum pool size. if undefined, the core thread pool size is the same as the maximum thread pool size.
* operation: `/subsystem=jgroups/stack=<stack name>/transport=<transport name>/thread-pool=default:read-attribute(name=min-threads)`

## syntax of the operation to get the `module` attribute of the `relay.RELAY2` `relay` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `module` attribute value of the `relay.RELAY2` `relay` resource of the `stack` resource of the `jgroups` subsystem
* `module` attribute description: the module with which to resolve the protocol type.
* operation: `/subsystem=jgroups/stack=<stack name>/relay=relay.RELAY2:read-attribute(name=module)`

## syntax of the operation to get the `properties` attribute of the `relay.RELAY2` `relay` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `properties` attribute value of the `relay.RELAY2` `relay` resource of the `stack` resource of the `jgroups` subsystem
* `properties` attribute description: the properties of this protocol.
* operation: `/subsystem=jgroups/stack=<stack name>/relay=relay.RELAY2:read-attribute(name=properties)`

## syntax of the operation to get the `site` attribute of the `relay.RELAY2` `relay` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `site` attribute value of the `relay.RELAY2` `relay` resource of the `stack` resource of the `jgroups` subsystem
* `site` attribute description: the name of the local site.
* operation: `/subsystem=jgroups/stack=<stack name>/relay=relay.RELAY2:read-attribute(name=site)`

## syntax of the operation to get the `statistics-enabled` attribute of the `relay.RELAY2` `relay` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `statistics-enabled` attribute value of the `relay.RELAY2` `relay` resource of the `stack` resource of the `jgroups` subsystem
* `statistics-enabled` attribute description: indicates whether or not this protocol will collect statistics overriding stack configuration.
* operation: `/subsystem=jgroups/stack=<stack name>/relay=relay.RELAY2:read-attribute(name=statistics-enabled)`

## syntax of the operation to get a `remote-site` resource of the `relay.RELAY2` `relay` resource of the `stack` resource of the `jgroups` subsystem
* operation: `/subsystem=jgroups/stack=<stack name>/relay=relay.RELAY2/remote-site=<remote-site name>:read-resource()`
* To get the list of all the `remote-site` resource of the `relay.RELAY2` `relay` resource of the `stack` resource of the `jgroups` subsystem use '*' for `<remote-site name>`.

## syntax of the operation to get the `channel` attribute of the `remote-site` resource of the `relay.RELAY2` `relay` resource of the `stack` resource of the `jgroups` subsystem
* Retrieve the `channel` attribute value of the `remote-site` resource of the `relay.RELAY2` `relay` resource of the `stack` resource of the `jgroups` subsystem
* `channel` attribute description: the name of the bridge channel used to communicate with this remote site.
* operation: `/subsystem=jgroups/stack=<stack name>/relay=relay.RELAY2/remote-site=<remote-site name>:read-attribute(name=channel)`

## syntax of the operation to get the `default-authentication-context` attribute of the `elytron` subsystem
* Retrieve the `default-authentication-context` attribute value of the `elytron` subsystem
* `default-authentication-context` attribute description: the default authentication context to be associated with all deployments.
* operation: `/subsystem=elytron:read-attribute(name=default-authentication-context)`

## syntax of the operation to get the `default-ssl-context` attribute of the `elytron` subsystem
* Retrieve the `default-ssl-context` attribute value of the `elytron` subsystem
* `default-ssl-context` attribute description: reference to the `sslcontext` which should be globally registered as the default.
* operation: `/subsystem=elytron:read-attribute(name=default-ssl-context)`

## syntax of the operation to get the `disallowed-providers` attribute of the `elytron` subsystem
* Retrieve the `disallowed-providers` attribute value of the `elytron` subsystem
* `disallowed-providers` attribute description: a list of providers that are not allowed, and will be removed from the providers list.
* operation: `/subsystem=elytron:read-attribute(name=disallowed-providers)`

## syntax of the operation to get the `final-providers` attribute of the `elytron` subsystem
* Retrieve the `final-providers` attribute value of the `elytron` subsystem
* `final-providers` attribute description: reference to the providers that should be registered after all existing providers.
* operation: `/subsystem=elytron:read-attribute(name=final-providers)`

## syntax of the operation to get the `initial-providers` attribute of the `elytron` subsystem
* Retrieve the `initial-providers` attribute value of the `elytron` subsystem
* `initial-providers` attribute description: reference to the providers that should be registered ahead of all existing providers.
* operation: `/subsystem=elytron:read-attribute(name=initial-providers)`

## syntax of the operation to get the `register-jaspi-factory` attribute of the `elytron` subsystem
* Retrieve the `register-jaspi-factory` attribute value of the `elytron` subsystem
* `register-jaspi-factory` attribute description: should the elytron `jaspi` factory be globally registered?
* operation: `/subsystem=elytron:read-attribute(name=register-jaspi-factory)`

## syntax of the operation to get the `security-properties` attribute of the `elytron` subsystem
* Retrieve the `security-properties` attribute value of the `elytron` subsystem
* `security-properties` attribute description: security properties to be set.
* operation: `/subsystem=elytron:read-attribute(name=security-properties)`

## syntax of the operation to get a `configurable-sasl-server-factory` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/configurable-sasl-server-factory=<configurable-sasl-server-factory name>:read-resource()`
* To get the list of all the `configurable-sasl-server-factory` resource of the `elytron` subsystem use '*' for `<configurable-sasl-server-factory name>`.

## syntax of the operation to get the `available-mechanisms` attribute of the `configurable-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `available-mechanisms` attribute value of the `configurable-sasl-server-factory` resource of the `elytron` subsystem
* `available-mechanisms` attribute description: the `sasl` mechanisms available from this factory after all filtering has been applied.
* operation: `/subsystem=elytron/configurable-sasl-server-factory=<configurable-sasl-server-factory name>:read-attribute(name=available-mechanisms)`

## syntax of the operation to get the `filters` attribute of the `configurable-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `filters` attribute value of the `configurable-sasl-server-factory` resource of the `elytron` subsystem
* `filters` attribute description: list of filters to be evaluated sequentially combining the results using 'or'.
* operation: `/subsystem=elytron/configurable-sasl-server-factory=<configurable-sasl-server-factory name>:read-attribute(name=filters)`

## syntax of the operation to get the `properties` attribute of the `configurable-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `properties` attribute value of the `configurable-sasl-server-factory` resource of the `elytron` subsystem
* `properties` attribute description: custom properties to be passed in to the `sasl` server factory calls.
* operation: `/subsystem=elytron/configurable-sasl-server-factory=<configurable-sasl-server-factory name>:read-attribute(name=properties)`

## syntax of the operation to get the `protocol` attribute of the `configurable-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `protocol` attribute value of the `configurable-sasl-server-factory` resource of the `elytron` subsystem
* `protocol` attribute description: the protocol that should be passed into factory when creating the mechanism.
* operation: `/subsystem=elytron/configurable-sasl-server-factory=<configurable-sasl-server-factory name>:read-attribute(name=protocol)`

## syntax of the operation to get the `sasl-server-factory` attribute of the `configurable-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `sasl-server-factory` attribute value of the `configurable-sasl-server-factory` resource of the `elytron` subsystem
* `sasl-server-factory` attribute description: the `sasl` server factory to be wrapped.
* operation: `/subsystem=elytron/configurable-sasl-server-factory=<configurable-sasl-server-factory name>:read-attribute(name=sasl-server-factory)`

## syntax of the operation to get the `server-name` attribute of the `configurable-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `server-name` attribute value of the `configurable-sasl-server-factory` resource of the `elytron` subsystem
* `server-name` attribute description: the server name that should be passed into factory when creating the mechanism.
* operation: `/subsystem=elytron/configurable-sasl-server-factory=<configurable-sasl-server-factory name>:read-attribute(name=server-name)`

## syntax of the operation to get a `custom-role-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/custom-role-mapper=<custom-role-mapper name>:read-resource()`
* To get the list of all the `custom-role-mapper` resource of the `elytron` subsystem use '*' for `<custom-role-mapper name>`.

## syntax of the operation to get the `class-name` attribute of the `custom-role-mapper` resource of the `elytron` subsystem
* Retrieve the `class-name` attribute value of the `custom-role-mapper` resource of the `elytron` subsystem
* `class-name` attribute description: fully qualified class name of the `rolemapper`
* operation: `/subsystem=elytron/custom-role-mapper=<custom-role-mapper name>:read-attribute(name=class-name)`

## syntax of the operation to get the `configuration` attribute of the `custom-role-mapper` resource of the `elytron` subsystem
* Retrieve the `configuration` attribute value of the `custom-role-mapper` resource of the `elytron` subsystem
* `configuration` attribute description: the optional `key/value` configuration for the `rolemapper`
* operation: `/subsystem=elytron/custom-role-mapper=<custom-role-mapper name>:read-attribute(name=configuration)`

## syntax of the operation to get the `module` attribute of the `custom-role-mapper` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `custom-role-mapper` resource of the `elytron` subsystem
* `module` attribute description: name of the module to use to load the `rolemapper`
* operation: `/subsystem=elytron/custom-role-mapper=<custom-role-mapper name>:read-attribute(name=module)`

## syntax of the operation to get a `sasl-authentication-factory` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/sasl-authentication-factory=<sasl-authentication-factory name>:read-resource()`
* To get the list of all the `sasl-authentication-factory` resource of the `elytron` subsystem use '*' for `<sasl-authentication-factory name>`.

## syntax of the operation to get the `available-mechanisms` attribute of the `sasl-authentication-factory` resource of the `elytron` subsystem
* Retrieve the `available-mechanisms` attribute value of the `sasl-authentication-factory` resource of the `elytron` subsystem
* `available-mechanisms` attribute description: the `sasl` mechanisms available from this configuration after all filtering has been applied.
* operation: `/subsystem=elytron/sasl-authentication-factory=<sasl-authentication-factory name>:read-attribute(name=available-mechanisms)`

## syntax of the operation to get the `mechanism-configurations` attribute of the `sasl-authentication-factory` resource of the `elytron` subsystem
* Retrieve the `mechanism-configurations` attribute value of the `sasl-authentication-factory` resource of the `elytron` subsystem
* `mechanism-configurations` attribute description: mechanism specific configuration
* operation: `/subsystem=elytron/sasl-authentication-factory=<sasl-authentication-factory name>:read-attribute(name=mechanism-configurations)`

## syntax of the operation to get the `sasl-server-factory` attribute of the `sasl-authentication-factory` resource of the `elytron` subsystem
* Retrieve the `sasl-server-factory` attribute value of the `sasl-authentication-factory` resource of the `elytron` subsystem
* `sasl-server-factory` attribute description: the `saslserverfactory` to associate with this resource
* operation: `/subsystem=elytron/sasl-authentication-factory=<sasl-authentication-factory name>:read-attribute(name=sasl-server-factory)`

## syntax of the operation to get the `security-domain` attribute of the `sasl-authentication-factory` resource of the `elytron` subsystem
* Retrieve the `security-domain` attribute value of the `sasl-authentication-factory` resource of the `elytron` subsystem
* `security-domain` attribute description: the `securitydomain` to associate with this resource
* operation: `/subsystem=elytron/sasl-authentication-factory=<sasl-authentication-factory name>:read-attribute(name=security-domain)`

## syntax of the operation to get a `size-rotating-file-audit-log` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/size-rotating-file-audit-log=<size-rotating-file-audit-log name>:read-resource()`
* To get the list of all the `size-rotating-file-audit-log` resource of the `elytron` subsystem use '*' for `<size-rotating-file-audit-log name>`.

## syntax of the operation to get the `autoflush` attribute of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `autoflush` attribute value of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* `autoflush` attribute description: whether every event should be immediately flushed to disk.
* operation: `/subsystem=elytron/size-rotating-file-audit-log=<size-rotating-file-audit-log name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `encoding` attribute of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `encoding` attribute value of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* `encoding` attribute description: the audit file encoding. default is `utf_8`.
* operation: `/subsystem=elytron/size-rotating-file-audit-log=<size-rotating-file-audit-log name>:read-attribute(name=encoding)`

## syntax of the operation to get the `format` attribute of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `format` attribute value of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* `format` attribute description: the format to use to record the audit event.
* operation: `/subsystem=elytron/size-rotating-file-audit-log=<size-rotating-file-audit-log name>:read-attribute(name=format)`

## syntax of the operation to get the `max-backup-index` attribute of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `max-backup-index` attribute value of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* `max-backup-index` attribute description: the maximum number of files to backup when rotating.
* operation: `/subsystem=elytron/size-rotating-file-audit-log=<size-rotating-file-audit-log name>:read-attribute(name=max-backup-index)`

## syntax of the operation to get the `path` attribute of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `path` attribute value of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* `path` attribute description: path of the file to be written.
* operation: `/subsystem=elytron/size-rotating-file-audit-log=<size-rotating-file-audit-log name>:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `relative-to` attribute value of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* `relative-to` attribute description: the relative path to the audit log.
* operation: `/subsystem=elytron/size-rotating-file-audit-log=<size-rotating-file-audit-log name>:read-attribute(name=relative-to)`

## syntax of the operation to get the `rotate-on-boot` attribute of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `rotate-on-boot` attribute value of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* `rotate-on-boot` attribute description: whether the file should be rotated before the a new file is set.
* operation: `/subsystem=elytron/size-rotating-file-audit-log=<size-rotating-file-audit-log name>:read-attribute(name=rotate-on-boot)`

## syntax of the operation to get the `rotate-size` attribute of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `rotate-size` attribute value of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* `rotate-size` attribute description: the log file size the file should rotate at.
* operation: `/subsystem=elytron/size-rotating-file-audit-log=<size-rotating-file-audit-log name>:read-attribute(name=rotate-size)`

## syntax of the operation to get the `suffix` attribute of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `suffix` attribute value of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* `suffix` attribute description: format of date used as suffix of log file names in `java.time.format.datetimeformatter`. the suffix does not play a role in determining when the file should be rotated.
* operation: `/subsystem=elytron/size-rotating-file-audit-log=<size-rotating-file-audit-log name>:read-attribute(name=suffix)`

## syntax of the operation to get the `synchronized` attribute of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `synchronized` attribute value of the `size-rotating-file-audit-log` resource of the `elytron` subsystem
* `synchronized` attribute description: whether every event should be immediately flushed to disk (if undefined will default to the value of synchronized).
* operation: `/subsystem=elytron/size-rotating-file-audit-log=<size-rotating-file-audit-log name>:read-attribute(name=synchronized)`

## syntax of the operation to get a `add-prefix-role-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/add-prefix-role-mapper=<add-prefix-role-mapper name>:read-resource()`
* To get the list of all the `add-prefix-role-mapper` resource of the `elytron` subsystem use '*' for `<add-prefix-role-mapper name>`.

## syntax of the operation to get the `prefix` attribute of the `add-prefix-role-mapper` resource of the `elytron` subsystem
* Retrieve the `prefix` attribute value of the `add-prefix-role-mapper` resource of the `elytron` subsystem
* `prefix` attribute description: the prefix to add to each role.
* operation: `/subsystem=elytron/add-prefix-role-mapper=<add-prefix-role-mapper name>:read-attribute(name=prefix)`

## syntax of the operation to get a `aggregate-http-server-mechanism-factory` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/aggregate-http-server-mechanism-factory=<aggregate-http-server-mechanism-factory name>:read-resource()`
* To get the list of all the `aggregate-http-server-mechanism-factory` resource of the `elytron` subsystem use '*' for `<aggregate-http-server-mechanism-factory name>`.

## syntax of the operation to get the `available-mechanisms` attribute of the `aggregate-http-server-mechanism-factory` resource of the `elytron` subsystem
* Retrieve the `available-mechanisms` attribute value of the `aggregate-http-server-mechanism-factory` resource of the `elytron` subsystem
* `available-mechanisms` attribute description: the `http` mechanisms available from this factory instance.
* operation: `/subsystem=elytron/aggregate-http-server-mechanism-factory=<aggregate-http-server-mechanism-factory name>:read-attribute(name=available-mechanisms)`

## syntax of the operation to get the `http-server-mechanism-factories` attribute of the `aggregate-http-server-mechanism-factory` resource of the `elytron` subsystem
* Retrieve the `http-server-mechanism-factories` attribute value of the `aggregate-http-server-mechanism-factory` resource of the `elytron` subsystem
* `http-server-mechanism-factories` attribute description: the referenced `http` server factories to aggregate.
* operation: `/subsystem=elytron/aggregate-http-server-mechanism-factory=<aggregate-http-server-mechanism-factory name>:read-attribute(name=http-server-mechanism-factories)`

## syntax of the operation to get a `custom-evidence-decoder` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/custom-evidence-decoder=<custom-evidence-decoder name>:read-resource()`
* To get the list of all the `custom-evidence-decoder` resource of the `elytron` subsystem use '*' for `<custom-evidence-decoder name>`.

## syntax of the operation to get the `class-name` attribute of the `custom-evidence-decoder` resource of the `elytron` subsystem
* Retrieve the `class-name` attribute value of the `custom-evidence-decoder` resource of the `elytron` subsystem
* `class-name` attribute description: fully qualified class name of the evidence decoder.
* operation: `/subsystem=elytron/custom-evidence-decoder=<custom-evidence-decoder name>:read-attribute(name=class-name)`

## syntax of the operation to get the `configuration` attribute of the `custom-evidence-decoder` resource of the `elytron` subsystem
* Retrieve the `configuration` attribute value of the `custom-evidence-decoder` resource of the `elytron` subsystem
* `configuration` attribute description: the optional `key/value` configuration for the evidence decoder.
* operation: `/subsystem=elytron/custom-evidence-decoder=<custom-evidence-decoder name>:read-attribute(name=configuration)`

## syntax of the operation to get the `module` attribute of the `custom-evidence-decoder` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `custom-evidence-decoder` resource of the `elytron` subsystem
* `module` attribute description: name of the module to use to load the evidence decoder.
* operation: `/subsystem=elytron/custom-evidence-decoder=<custom-evidence-decoder name>:read-attribute(name=module)`

## syntax of the operation to get a `aggregate-security-event-listener` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/aggregate-security-event-listener=<aggregate-security-event-listener name>:read-resource()`
* To get the list of all the `aggregate-security-event-listener` resource of the `elytron` subsystem use '*' for `<aggregate-security-event-listener name>`.

## syntax of the operation to get the `security-event-listeners` attribute of the `aggregate-security-event-listener` resource of the `elytron` subsystem
* Retrieve the `security-event-listeners` attribute value of the `aggregate-security-event-listener` resource of the `elytron` subsystem
* `security-event-listeners` attribute description: the referenced security event listener resources to aggregate.
* operation: `/subsystem=elytron/aggregate-security-event-listener=<aggregate-security-event-listener name>:read-attribute(name=security-event-listeners)`

## syntax of the operation to get a `case-principal-transformer` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/case-principal-transformer=<case-principal-transformer name>:read-resource()`
* To get the list of all the `case-principal-transformer` resource of the `elytron` subsystem use '*' for `<case-principal-transformer name>`.

## syntax of the operation to get the `upper-case` attribute of the `case-principal-transformer` resource of the `elytron` subsystem
* Retrieve the `upper-case` attribute value of the `case-principal-transformer` resource of the `elytron` subsystem
* `upper-case` attribute description: if set to true, the principal is adjusted to upper case. if set to false, the principal is adjusted to lower case.
* operation: `/subsystem=elytron/case-principal-transformer=<case-principal-transformer name>:read-attribute(name=upper-case)`

## syntax of the operation to get a `mapped-regex-realm-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/mapped-regex-realm-mapper=<mapped-regex-realm-mapper name>:read-resource()`
* To get the list of all the `mapped-regex-realm-mapper` resource of the `elytron` subsystem use '*' for `<mapped-regex-realm-mapper name>`.

## syntax of the operation to get the `delegate-realm-mapper` attribute of the `mapped-regex-realm-mapper` resource of the `elytron` subsystem
* Retrieve the `delegate-realm-mapper` attribute value of the `mapped-regex-realm-mapper` resource of the `elytron` subsystem
* `delegate-realm-mapper` attribute description: the `realmmapper` to delegate to if the pattern does not match. if no delegate is specified then the default realm on the domain will be used instead. if the `username` does not match the pattern and a delegate `realm-mapper` is present, the result of `delegate-realm-mapper` is mapped via the `realm-map`.
* operation: `/subsystem=elytron/mapped-regex-realm-mapper=<mapped-regex-realm-mapper name>:read-attribute(name=delegate-realm-mapper)`

## syntax of the operation to get the `pattern` attribute of the `mapped-regex-realm-mapper` resource of the `elytron` subsystem
* Retrieve the `pattern` attribute value of the `mapped-regex-realm-mapper` resource of the `elytron` subsystem
* `pattern` attribute description: the regular expression which must contain at least one capture group to extract the realm from the name. if the regular expression matches more than one capture group, the first capture group is used.
* operation: `/subsystem=elytron/mapped-regex-realm-mapper=<mapped-regex-realm-mapper name>:read-attribute(name=pattern)`

## syntax of the operation to get the `realm-map` attribute of the `mapped-regex-realm-mapper` resource of the `elytron` subsystem
* Retrieve the `realm-map` attribute value of the `mapped-regex-realm-mapper` resource of the `elytron` subsystem
* `realm-map` attribute description: mapping of realm name extracted using the regular expression to a defined realm name. if the value for the mapping is not in the map or the realm whose name is the result of the mapping does not exist in the given security domain, the default realm is used.
* operation: `/subsystem=elytron/mapped-regex-realm-mapper=<mapped-regex-realm-mapper name>:read-attribute(name=realm-map)`

## syntax of the operation to get a `constant-permission-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/constant-permission-mapper=<constant-permission-mapper name>:read-resource()`
* To get the list of all the `constant-permission-mapper` resource of the `elytron` subsystem use '*' for `<constant-permission-mapper name>`.

## syntax of the operation to get the `permission-sets` attribute of the `constant-permission-mapper` resource of the `elytron` subsystem
* Retrieve the `permission-sets` attribute value of the `constant-permission-mapper` resource of the `elytron` subsystem
* `permission-sets` attribute description: the permission sets to assign.
* operation: `/subsystem=elytron/constant-permission-mapper=<constant-permission-mapper name>:read-attribute(name=permission-sets)`

## syntax of the operation to get the `permissions` attribute of the `constant-permission-mapper` resource of the `elytron` subsystem
* Retrieve the `permissions` attribute value of the `constant-permission-mapper` resource of the `elytron` subsystem
* `permissions` attribute description: the permissions to assign.
* operation: `/subsystem=elytron/constant-permission-mapper=<constant-permission-mapper name>:read-attribute(name=permissions)`

## syntax of the operation to get a `custom-security-event-listener` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/custom-security-event-listener=<custom-security-event-listener name>:read-resource()`
* To get the list of all the `custom-security-event-listener` resource of the `elytron` subsystem use '*' for `<custom-security-event-listener name>`.

## syntax of the operation to get the `class-name` attribute of the `custom-security-event-listener` resource of the `elytron` subsystem
* Retrieve the `class-name` attribute value of the `custom-security-event-listener` resource of the `elytron` subsystem
* `class-name` attribute description: the class name of the implementation of the custom security event listener.
* operation: `/subsystem=elytron/custom-security-event-listener=<custom-security-event-listener name>:read-attribute(name=class-name)`

## syntax of the operation to get the `configuration` attribute of the `custom-security-event-listener` resource of the `elytron` subsystem
* Retrieve the `configuration` attribute value of the `custom-security-event-listener` resource of the `elytron` subsystem
* `configuration` attribute description: the optional `key/value` configuration for the custom security event listener.
* operation: `/subsystem=elytron/custom-security-event-listener=<custom-security-event-listener name>:read-attribute(name=configuration)`

## syntax of the operation to get the `module` attribute of the `custom-security-event-listener` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `custom-security-event-listener` resource of the `elytron` subsystem
* `module` attribute description: the module to use to load the custom security event listener.
* operation: `/subsystem=elytron/custom-security-event-listener=<custom-security-event-listener name>:read-attribute(name=module)`

## syntax of the operation to get a `key-manager` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/key-manager=<key-manager name>:read-resource()`
* To get the list of all the `key-manager` resource of the `elytron` subsystem use '*' for `<key-manager name>`.

## syntax of the operation to get the `algorithm` attribute of the `key-manager` resource of the `elytron` subsystem
* Retrieve the `algorithm` attribute value of the `key-manager` resource of the `elytron` subsystem
* `algorithm` attribute description: the name of the algorithm to use to create the underlying `keymanagerfactory`.
* operation: `/subsystem=elytron/key-manager=<key-manager name>:read-attribute(name=algorithm)`

## syntax of the operation to get the `alias-filter` attribute of the `key-manager` resource of the `elytron` subsystem
* Retrieve the `alias-filter` attribute value of the `key-manager` resource of the `elytron` subsystem
* `alias-filter` attribute description: a filter to apply to the aliases returned from the `keystore`, can either be a comma separated list of aliases to return or one of the following formats `all:-alias1:-alias2`, `none:+alias1:+alias2`
* operation: `/subsystem=elytron/key-manager=<key-manager name>:read-attribute(name=alias-filter)`

## syntax of the operation to get the `credential-reference` attribute of the `key-manager` resource of the `elytron` subsystem
* Retrieve the `credential-reference` attribute value of the `key-manager` resource of the `elytron` subsystem
* `credential-reference` attribute description: the credential reference to decrypt `keystore` item. (not a password of the `keystore`.)
* operation: `/subsystem=elytron/key-manager=<key-manager name>:read-attribute(name=credential-reference)`

## syntax of the operation to get the `generate-self-signed-certificate-host` attribute of the `key-manager` resource of the `elytron` subsystem
* Retrieve the `generate-self-signed-certificate-host` attribute value of the `key-manager` resource of the `elytron` subsystem
* `generate-self-signed-certificate-host` attribute description: if the file that backs the `keystore` does not exist and this attribute is set, then a `self-signed` certificate will be generated for the specified host name. this is not intended for production use.
* operation: `/subsystem=elytron/key-manager=<key-manager name>:read-attribute(name=generate-self-signed-certificate-host)`

## syntax of the operation to get the `key-store` attribute of the `key-manager` resource of the `elytron` subsystem
* Retrieve the `key-store` attribute value of the `key-manager` resource of the `elytron` subsystem
* `key-store` attribute description: reference to the `keystore` to use to initialise the underlying `keymanagerfactory`.
* operation: `/subsystem=elytron/key-manager=<key-manager name>:read-attribute(name=key-store)`

## syntax of the operation to get the `provider-name` attribute of the `key-manager` resource of the `elytron` subsystem
* Retrieve the `provider-name` attribute value of the `key-manager` resource of the `elytron` subsystem
* `provider-name` attribute description: the name of the provider to use to create the underlying `keymanagerfactory`.
* operation: `/subsystem=elytron/key-manager=<key-manager name>:read-attribute(name=provider-name)`

## syntax of the operation to get the `providers` attribute of the `key-manager` resource of the `elytron` subsystem
* Retrieve the `providers` attribute value of the `key-manager` resource of the `elytron` subsystem
* `providers` attribute description: reference to obtain the provider[] to use when creating the underlying `keymanagerfactory`.
* operation: `/subsystem=elytron/key-manager=<key-manager name>:read-attribute(name=providers)`

## syntax of the operation to get a `properties-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/properties-realm=<properties-realm name>:read-resource()`
* To get the list of all the `properties-realm` resource of the `elytron` subsystem use '*' for `<properties-realm name>`.

## syntax of the operation to get the `groups-attribute` attribute of the `properties-realm` resource of the `elytron` subsystem
* Retrieve the `groups-attribute` attribute value of the `properties-realm` resource of the `elytron` subsystem
* `groups-attribute` attribute description: the name of the attribute in the returned `authorizationidentity` that should contain the group membership information for the identity.
* operation: `/subsystem=elytron/properties-realm=<properties-realm name>:read-attribute(name=groups-attribute)`

## syntax of the operation to get the `groups-properties` attribute of the `properties-realm` resource of the `elytron` subsystem
* Retrieve the `groups-properties` attribute value of the `properties-realm` resource of the `elytron` subsystem
* `groups-properties` attribute description: the properties file containing the users and their groups.
* operation: `/subsystem=elytron/properties-realm=<properties-realm name>:read-attribute(name=groups-properties)`

## syntax of the operation to get the `hash-charset` attribute of the `properties-realm` resource of the `elytron` subsystem
* Retrieve the `hash-charset` attribute value of the `properties-realm` resource of the `elytron` subsystem
* `hash-charset` attribute description: the character set to use when converting the password string to a byte array.
* operation: `/subsystem=elytron/properties-realm=<properties-realm name>:read-attribute(name=hash-charset)`

## syntax of the operation to get the `hash-encoding` attribute of the `properties-realm` resource of the `elytron` subsystem
* Retrieve the `hash-encoding` attribute value of the `properties-realm` resource of the `elytron` subsystem
* `hash-encoding` attribute description: the string format for the password if it is not stored in plain text.
* operation: `/subsystem=elytron/properties-realm=<properties-realm name>:read-attribute(name=hash-encoding)`

## syntax of the operation to get the `synchronized` attribute of the `properties-realm` resource of the `elytron` subsystem
* Retrieve the `synchronized` attribute value of the `properties-realm` resource of the `elytron` subsystem
* `synchronized` attribute description: the time the properties files that back this realm were last loaded.
* operation: `/subsystem=elytron/properties-realm=<properties-realm name>:read-attribute(name=synchronized)`

## syntax of the operation to get the `users-properties` attribute of the `properties-realm` resource of the `elytron` subsystem
* Retrieve the `users-properties` attribute value of the `properties-realm` resource of the `elytron` subsystem
* `users-properties` attribute description: the properties file containing the users and their passwords.
* operation: `/subsystem=elytron/properties-realm=<properties-realm name>:read-attribute(name=users-properties)`

## syntax of the operation to get a `http-authentication-factory` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/http-authentication-factory=<http-authentication-factory name>:read-resource()`
* To get the list of all the `http-authentication-factory` resource of the `elytron` subsystem use '*' for `<http-authentication-factory name>`.

## syntax of the operation to get the `available-mechanisms` attribute of the `http-authentication-factory` resource of the `elytron` subsystem
* Retrieve the `available-mechanisms` attribute value of the `http-authentication-factory` resource of the `elytron` subsystem
* `available-mechanisms` attribute description: the `http` mechanisms available from this configuration after all filtering has been applied.
* operation: `/subsystem=elytron/http-authentication-factory=<http-authentication-factory name>:read-attribute(name=available-mechanisms)`

## syntax of the operation to get the `http-server-mechanism-factory` attribute of the `http-authentication-factory` resource of the `elytron` subsystem
* Retrieve the `http-server-mechanism-factory` attribute value of the `http-authentication-factory` resource of the `elytron` subsystem
* `http-server-mechanism-factory` attribute description: the `httpserverauthenticationmechanismfactory` to associate with this resource
* operation: `/subsystem=elytron/http-authentication-factory=<http-authentication-factory name>:read-attribute(name=http-server-mechanism-factory)`

## syntax of the operation to get the `mechanism-configurations` attribute of the `http-authentication-factory` resource of the `elytron` subsystem
* Retrieve the `mechanism-configurations` attribute value of the `http-authentication-factory` resource of the `elytron` subsystem
* `mechanism-configurations` attribute description: mechanism specific configuration
* operation: `/subsystem=elytron/http-authentication-factory=<http-authentication-factory name>:read-attribute(name=mechanism-configurations)`

## syntax of the operation to get the `security-domain` attribute of the `http-authentication-factory` resource of the `elytron` subsystem
* Retrieve the `security-domain` attribute value of the `http-authentication-factory` resource of the `elytron` subsystem
* `security-domain` attribute description: the `securitydomain` to associate with this resource
* operation: `/subsystem=elytron/http-authentication-factory=<http-authentication-factory name>:read-attribute(name=security-domain)`

## syntax of the operation to get a `permission-set` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/permission-set=<permission-set name>:read-resource()`
* To get the list of all the `permission-set` resource of the `elytron` subsystem use '*' for `<permission-set name>`.

## syntax of the operation to get the `permissions` attribute of the `permission-set` resource of the `elytron` subsystem
* Retrieve the `permissions` attribute value of the `permission-set` resource of the `elytron` subsystem
* `permissions` attribute description: the permissions in the permission set.
* operation: `/subsystem=elytron/permission-set=<permission-set name>:read-attribute(name=permissions)`

## syntax of the operation to get a `regex-principal-transformer` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/regex-principal-transformer=<regex-principal-transformer name>:read-resource()`
* To get the list of all the `regex-principal-transformer` resource of the `elytron` subsystem use '*' for `<regex-principal-transformer name>`.

## syntax of the operation to get the `pattern` attribute of the `regex-principal-transformer` resource of the `elytron` subsystem
* Retrieve the `pattern` attribute value of the `regex-principal-transformer` resource of the `elytron` subsystem
* `pattern` attribute description: the regular expression to use to locate the portion of the name to be replaced.
* operation: `/subsystem=elytron/regex-principal-transformer=<regex-principal-transformer name>:read-attribute(name=pattern)`

## syntax of the operation to get the `replace-all` attribute of the `regex-principal-transformer` resource of the `elytron` subsystem
* Retrieve the `replace-all` attribute value of the `regex-principal-transformer` resource of the `elytron` subsystem
* `replace-all` attribute description: should all occurrences of the pattern matched be replaced or only the first occurrence.
* operation: `/subsystem=elytron/regex-principal-transformer=<regex-principal-transformer name>:read-attribute(name=replace-all)`

## syntax of the operation to get the `replacement` attribute of the `regex-principal-transformer` resource of the `elytron` subsystem
* Retrieve the `replacement` attribute value of the `regex-principal-transformer` resource of the `elytron` subsystem
* `replacement` attribute description: the value to be used as the replacement.
* operation: `/subsystem=elytron/regex-principal-transformer=<regex-principal-transformer name>:read-attribute(name=replacement)`

## syntax of the operation to get a `filesystem-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/filesystem-realm=<filesystem-realm name>:read-resource()`
* To get the list of all the `filesystem-realm` resource of the `elytron` subsystem use '*' for `<filesystem-realm name>`.

## syntax of the operation to get the `credential-store` attribute of the `filesystem-realm` resource of the `elytron` subsystem
* Retrieve the `credential-store` attribute value of the `filesystem-realm` resource of the `elytron` subsystem
* `credential-store` attribute description: the reference to the credential store that contains the secret key to encrypt and decrypt the realm.
* operation: `/subsystem=elytron/filesystem-realm=<filesystem-realm name>:read-attribute(name=credential-store)`

## syntax of the operation to get the `encoded` attribute of the `filesystem-realm` resource of the `elytron` subsystem
* Retrieve the `encoded` attribute value of the `filesystem-realm` resource of the `elytron` subsystem
* `encoded` attribute description: whether the identity names should be stored encoded (`base32`) in file names. if encryption is configured, this attribute is ignored.
* operation: `/subsystem=elytron/filesystem-realm=<filesystem-realm name>:read-attribute(name=encoded)`

## syntax of the operation to get the `hash-charset` attribute of the `filesystem-realm` resource of the `elytron` subsystem
* Retrieve the `hash-charset` attribute value of the `filesystem-realm` resource of the `elytron` subsystem
* `hash-charset` attribute description: the character set to use when converting the password string to a byte array.
* operation: `/subsystem=elytron/filesystem-realm=<filesystem-realm name>:read-attribute(name=hash-charset)`

## syntax of the operation to get the `hash-encoding` attribute of the `filesystem-realm` resource of the `elytron` subsystem
* Retrieve the `hash-encoding` attribute value of the `filesystem-realm` resource of the `elytron` subsystem
* `hash-encoding` attribute description: the string format for the password if it is not stored in plain text.
* operation: `/subsystem=elytron/filesystem-realm=<filesystem-realm name>:read-attribute(name=hash-encoding)`

## syntax of the operation to get the `key-store` attribute of the `filesystem-realm` resource of the `elytron` subsystem
* Retrieve the `key-store` attribute value of the `filesystem-realm` resource of the `elytron` subsystem
* `key-store` attribute description: the reference to the key store that contains the key pair to use to verify integrity.
* operation: `/subsystem=elytron/filesystem-realm=<filesystem-realm name>:read-attribute(name=key-store)`

## syntax of the operation to get the `key-store-alias` attribute of the `filesystem-realm` resource of the `elytron` subsystem
* Retrieve the `key-store-alias` attribute value of the `filesystem-realm` resource of the `elytron` subsystem
* `key-store-alias` attribute description: the alias that identifies the `privatekeyentry` within the key store to use to verify integrity.
* operation: `/subsystem=elytron/filesystem-realm=<filesystem-realm name>:read-attribute(name=key-store-alias)`

## syntax of the operation to get the `levels` attribute of the `filesystem-realm` resource of the `elytron` subsystem
* Retrieve the `levels` attribute value of the `filesystem-realm` resource of the `elytron` subsystem
* `levels` attribute description: the number of levels of directory hashing to apply.
* operation: `/subsystem=elytron/filesystem-realm=<filesystem-realm name>:read-attribute(name=levels)`

## syntax of the operation to get the `path` attribute of the `filesystem-realm` resource of the `elytron` subsystem
* Retrieve the `path` attribute value of the `filesystem-realm` resource of the `elytron` subsystem
* `path` attribute description: the path to the file containing the realm.
* operation: `/subsystem=elytron/filesystem-realm=<filesystem-realm name>:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `filesystem-realm` resource of the `elytron` subsystem
* Retrieve the `relative-to` attribute value of the `filesystem-realm` resource of the `elytron` subsystem
* `relative-to` attribute description: the `pre-defined` path the path is relative to.
* operation: `/subsystem=elytron/filesystem-realm=<filesystem-realm name>:read-attribute(name=relative-to)`

## syntax of the operation to get the `secret-key` attribute of the `filesystem-realm` resource of the `elytron` subsystem
* Retrieve the `secret-key` attribute value of the `filesystem-realm` resource of the `elytron` subsystem
* `secret-key` attribute description: the alias of the secret key to encrypt and decrypt the realm.
* operation: `/subsystem=elytron/filesystem-realm=<filesystem-realm name>:read-attribute(name=secret-key)`

## syntax of the operation to get a `secret-key-credential-store` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/secret-key-credential-store=<secret-key-credential-store name>:read-resource()`
* To get the list of all the `secret-key-credential-store` resource of the `elytron` subsystem use '*' for `<secret-key-credential-store name>`.

## syntax of the operation to get the `create` attribute of the `secret-key-credential-store` resource of the `elytron` subsystem
* Retrieve the `create` attribute value of the `secret-key-credential-store` resource of the `elytron` subsystem
* `create` attribute description: if the credential store does not exist should it be dynamically created?
* operation: `/subsystem=elytron/secret-key-credential-store=<secret-key-credential-store name>:read-attribute(name=create)`

## syntax of the operation to get the `default-alias` attribute of the `secret-key-credential-store` resource of the `elytron` subsystem
* Retrieve the `default-alias` attribute value of the `secret-key-credential-store` resource of the `elytron` subsystem
* `default-alias` attribute description: the alias name of any key generated by default.
* operation: `/subsystem=elytron/secret-key-credential-store=<secret-key-credential-store name>:read-attribute(name=default-alias)`

## syntax of the operation to get the `key-size` attribute of the `secret-key-credential-store` resource of the `elytron` subsystem
* Retrieve the `key-size` attribute value of the `secret-key-credential-store` resource of the `elytron` subsystem
* `key-size` attribute description: the default size for any generated keys.
* operation: `/subsystem=elytron/secret-key-credential-store=<secret-key-credential-store name>:read-attribute(name=key-size)`

## syntax of the operation to get the `path` attribute of the `secret-key-credential-store` resource of the `elytron` subsystem
* Retrieve the `path` attribute value of the `secret-key-credential-store` resource of the `elytron` subsystem
* `path` attribute description: the path to the credential store.
* operation: `/subsystem=elytron/secret-key-credential-store=<secret-key-credential-store name>:read-attribute(name=path)`

## syntax of the operation to get the `populate` attribute of the `secret-key-credential-store` resource of the `elytron` subsystem
* Retrieve the `populate` attribute value of the `secret-key-credential-store` resource of the `elytron` subsystem
* `populate` attribute description: if the credential store does not contain the `default-alias` should it be dynamically added?
* operation: `/subsystem=elytron/secret-key-credential-store=<secret-key-credential-store name>:read-attribute(name=populate)`

## syntax of the operation to get the `relative-to` attribute of the `secret-key-credential-store` resource of the `elytron` subsystem
* Retrieve the `relative-to` attribute value of the `secret-key-credential-store` resource of the `elytron` subsystem
* `relative-to` attribute description: a reference to a previously defined path that the path is relative to.
* operation: `/subsystem=elytron/secret-key-credential-store=<secret-key-credential-store name>:read-attribute(name=relative-to)`

## syntax of the operation to get the `state` attribute of the `secret-key-credential-store` resource of the `elytron` subsystem
* Retrieve the `state` attribute value of the `secret-key-credential-store` resource of the `elytron` subsystem
* `state` attribute description: the state of the credential store service.
* operation: `/subsystem=elytron/secret-key-credential-store=<secret-key-credential-store name>:read-attribute(name=state)`

## syntax of the operation to get a `logical-role-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/logical-role-mapper=<logical-role-mapper name>:read-resource()`
* To get the list of all the `logical-role-mapper` resource of the `elytron` subsystem use '*' for `<logical-role-mapper name>`.

## syntax of the operation to get the `left` attribute of the `logical-role-mapper` resource of the `elytron` subsystem
* Retrieve the `left` attribute value of the `logical-role-mapper` resource of the `elytron` subsystem
* `left` attribute description: reference to a role mapper to be used on the left side of the operation.
* operation: `/subsystem=elytron/logical-role-mapper=<logical-role-mapper name>:read-attribute(name=left)`

## syntax of the operation to get the `logical-operation` attribute of the `logical-role-mapper` resource of the `elytron` subsystem
* Retrieve the `logical-operation` attribute value of the `logical-role-mapper` resource of the `elytron` subsystem
* `logical-operation` attribute description: the logical operation to be performed on the role mapper mappings.
* operation: `/subsystem=elytron/logical-role-mapper=<logical-role-mapper name>:read-attribute(name=logical-operation)`

## syntax of the operation to get the `right` attribute of the `logical-role-mapper` resource of the `elytron` subsystem
* Retrieve the `right` attribute value of the `logical-role-mapper` resource of the `elytron` subsystem
* `right` attribute description: reference to a role mapper to be used on the right side of the operation.
* operation: `/subsystem=elytron/logical-role-mapper=<logical-role-mapper name>:read-attribute(name=right)`

## syntax of the operation to get a `failover-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/failover-realm=<failover-realm name>:read-resource()`
* To get the list of all the `failover-realm` resource of the `elytron` subsystem use '*' for `<failover-realm name>`.

## syntax of the operation to get the `delegate-realm` attribute of the `failover-realm` resource of the `elytron` subsystem
* Retrieve the `delegate-realm` attribute value of the `failover-realm` resource of the `elytron` subsystem
* `delegate-realm` attribute description: reference to the security realm to use as a default.
* operation: `/subsystem=elytron/failover-realm=<failover-realm name>:read-attribute(name=delegate-realm)`

## syntax of the operation to get the `emit-events` attribute of the `failover-realm` resource of the `elytron` subsystem
* Retrieve the `emit-events` attribute value of the `failover-realm` resource of the `elytron` subsystem
* `emit-events` attribute description: whether `securityevent` signifying `delegate-realm` unavailability should be emitted.
* operation: `/subsystem=elytron/failover-realm=<failover-realm name>:read-attribute(name=emit-events)`

## syntax of the operation to get the `failover-realm` attribute of the `failover-realm` resource of the `elytron` subsystem
* Retrieve the `failover-realm` attribute value of the `failover-realm` resource of the `elytron` subsystem
* `failover-realm` attribute description: reference to the security realm to use in case the `delegate-realm` is unavailable.
* operation: `/subsystem=elytron/failover-realm=<failover-realm name>:read-attribute(name=failover-realm)`

## syntax of the operation to get a `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/periodic-rotating-file-audit-log=<periodic-rotating-file-audit-log name>:read-resource()`
* To get the list of all the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem use '*' for `<periodic-rotating-file-audit-log name>`.

## syntax of the operation to get the `autoflush` attribute of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `autoflush` attribute value of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* `autoflush` attribute description: whether every event should be immediately flushed to disk (if undefined will default to the value of synchronized).
* operation: `/subsystem=elytron/periodic-rotating-file-audit-log=<periodic-rotating-file-audit-log name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `encoding` attribute of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `encoding` attribute value of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* `encoding` attribute description: the audit file encoding. default is `utf_8`.
* operation: `/subsystem=elytron/periodic-rotating-file-audit-log=<periodic-rotating-file-audit-log name>:read-attribute(name=encoding)`

## syntax of the operation to get the `format` attribute of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `format` attribute value of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* `format` attribute description: the format to use to record the audit event.
* operation: `/subsystem=elytron/periodic-rotating-file-audit-log=<periodic-rotating-file-audit-log name>:read-attribute(name=format)`

## syntax of the operation to get the `path` attribute of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `path` attribute value of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* `path` attribute description: path of the file to be written.
* operation: `/subsystem=elytron/periodic-rotating-file-audit-log=<periodic-rotating-file-audit-log name>:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `relative-to` attribute value of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* `relative-to` attribute description: the relative path to the audit log.
* operation: `/subsystem=elytron/periodic-rotating-file-audit-log=<periodic-rotating-file-audit-log name>:read-attribute(name=relative-to)`

## syntax of the operation to get the `suffix` attribute of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `suffix` attribute value of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* `suffix` attribute description: the suffix string in a format which can be understood by `java.time.format.datetimeformatter`. the period of the rotation is automatically calculated based on the suffix.
* operation: `/subsystem=elytron/periodic-rotating-file-audit-log=<periodic-rotating-file-audit-log name>:read-attribute(name=suffix)`

## syntax of the operation to get the `synchronized` attribute of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* Retrieve the `synchronized` attribute value of the `periodic-rotating-file-audit-log` resource of the `elytron` subsystem
* `synchronized` attribute description: whether every event should be immediately synchronised to disk.
* operation: `/subsystem=elytron/periodic-rotating-file-audit-log=<periodic-rotating-file-audit-log name>:read-attribute(name=synchronized)`

## syntax of the operation to get a `ldap-key-store` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-resource()`
* To get the list of all the `ldap-key-store` resource of the `elytron` subsystem use '*' for `<ldap-key-store name>`.

## syntax of the operation to get the `alias-attribute` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `alias-attribute` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `alias-attribute` attribute description: the name of `ldap` attribute, where will be item alias stored.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=alias-attribute)`

## syntax of the operation to get the `certificate-attribute` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `certificate-attribute` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `certificate-attribute` attribute description: the name of `ldap` attribute, where will be certificate stored.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=certificate-attribute)`

## syntax of the operation to get the `certificate-chain-attribute` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `certificate-chain-attribute` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `certificate-chain-attribute` attribute description: the name of `ldap` attribute, where will be certificate chain stored.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=certificate-chain-attribute)`

## syntax of the operation to get the `certificate-chain-encoding` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `certificate-chain-encoding` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `certificate-chain-encoding` attribute description: the encoding of the certificate chain.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=certificate-chain-encoding)`

## syntax of the operation to get the `certificate-type` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `certificate-type` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `certificate-type` attribute description: the type of the certificate.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=certificate-type)`

## syntax of the operation to get the `dir-context` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `dir-context` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `dir-context` attribute description: the name of `dircontext`, which will be used to communication with `ldap` server.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=dir-context)`

## syntax of the operation to get the `filter-alias` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `filter-alias` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `filter-alias` attribute description: the `ldap` filter for obtaining an item of the `keystore` by alias. if this is not specified then the default value will be (`alias_attribute={0`}). the string '{`0`}' will be replaced by the searched alias and the '`alias_attribute`' value will be the value of the attribute '`alias-attribute`'.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=filter-alias)`

## syntax of the operation to get the `filter-certificate` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `filter-certificate` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `filter-certificate` attribute description: the `ldap` filter for obtaining an item of the `keystore` by certificate. if this is not specified then the default value will be (`certificate_attribute={0`}). the string '{`0`}' will be replaced by searched encoded certificate and the '`certificate_attribute`' will be the value of the attribute '`certificate-attribute`'.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=filter-certificate)`

## syntax of the operation to get the `filter-iterate` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `filter-iterate` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `filter-iterate` attribute description: the `ldap` filter for iterating over all items of the `keystore`. if this is not specified then the default value will be (`alias_attribute`=*). the '`alias_attribute`' will be the value of the attribute '`alias-attribute`'.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=filter-iterate)`

## syntax of the operation to get the `key-attribute` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `key-attribute` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `key-attribute` attribute description: the name of `ldap` attribute, where will be key stored.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=key-attribute)`

## syntax of the operation to get the `key-type` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `key-type` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `key-type` attribute description: the type of `keystore`, in which will be key serialized to `ldap` attribute.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=key-type)`

## syntax of the operation to get the `new-item-template` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `new-item-template` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `new-item-template` attribute description: configuration for item creation. define how will look `ldap` entry of newly created `keystore` item.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=new-item-template)`

## syntax of the operation to get the `search-path` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `search-path` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `search-path` attribute description: the path in `ldap`, where will be `keystore` items searched.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=search-path)`

## syntax of the operation to get the `search-recursive` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `search-recursive` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `search-recursive` attribute description: if the `ldap` search should be recursive.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=search-recursive)`

## syntax of the operation to get the `search-time-limit` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `search-time-limit` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `search-time-limit` attribute description: the time limit for obtaining `keystore` items from `ldap`.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=search-time-limit)`

## syntax of the operation to get the `size` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `size` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `size` attribute description: the size of `ldap` `keystore` in amount of `items/aliases`.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=size)`

## syntax of the operation to get the `state` attribute of the `ldap-key-store` resource of the `elytron` subsystem
* Retrieve the `state` attribute value of the `ldap-key-store` resource of the `elytron` subsystem
* `state` attribute description: the state of the underlying service that represents this `keystore` at runtime, if it is anything other than up runtime operations will not be available.
* operation: `/subsystem=elytron/ldap-key-store=<ldap-key-store name>:read-attribute(name=state)`

## syntax of the operation to get a `service-loader-http-server-mechanism-factory` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/service-loader-http-server-mechanism-factory=<service-loader-http-server-mechanism-factory name>:read-resource()`
* To get the list of all the `service-loader-http-server-mechanism-factory` resource of the `elytron` subsystem use '*' for `<service-loader-http-server-mechanism-factory name>`.

## syntax of the operation to get the `available-mechanisms` attribute of the `service-loader-http-server-mechanism-factory` resource of the `elytron` subsystem
* Retrieve the `available-mechanisms` attribute value of the `service-loader-http-server-mechanism-factory` resource of the `elytron` subsystem
* `available-mechanisms` attribute description: the `http` mechanisms available from this factory instance.
* operation: `/subsystem=elytron/service-loader-http-server-mechanism-factory=<service-loader-http-server-mechanism-factory name>:read-attribute(name=available-mechanisms)`

## syntax of the operation to get the `module` attribute of the `service-loader-http-server-mechanism-factory` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `service-loader-http-server-mechanism-factory` resource of the `elytron` subsystem
* `module` attribute description: the module to use to obtain the `classloader` to load the factories, if not specified the `classloader` to load the resource will be used instead.
* operation: `/subsystem=elytron/service-loader-http-server-mechanism-factory=<service-loader-http-server-mechanism-factory name>:read-attribute(name=module)`

## syntax of the operation to get a `server-ssl-context` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-resource()`
* To get the list of all the `server-ssl-context` resource of the `elytron` subsystem use '*' for `<server-ssl-context name>`.

## syntax of the operation to get the `active-session-count` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `active-session-count` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `active-session-count` attribute description: the count of current active sessions.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=active-session-count)`

## syntax of the operation to get the `authentication-optional` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `authentication-optional` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `authentication-optional` attribute description: rejecting of the client certificate by the security domain will not prevent the connection. allows a fall through to use other authentication mechanisms (like form login) when the client certificate is rejected by security domain. has an effect only when the security domain is set.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=authentication-optional)`

## syntax of the operation to get the `cipher-suite-filter` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `cipher-suite-filter` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `cipher-suite-filter` attribute description: the filter to apply to specify the enabled cipher suites for `tlsv1.2` and below.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=cipher-suite-filter)`

## syntax of the operation to get the `cipher-suite-names` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `cipher-suite-names` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `cipher-suite-names` attribute description: the filter to apply to specify the enabled cipher suites for `tlsv1.3`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=cipher-suite-names)`

## syntax of the operation to get the `final-principal-transformer` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `final-principal-transformer` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `final-principal-transformer` attribute description: a final principal transformer to apply for this mechanism realm.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=final-principal-transformer)`

## syntax of the operation to get the `key-manager` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `key-manager` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `key-manager` attribute description: reference to the key manager to use within the `sslcontext`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=key-manager)`

## syntax of the operation to get the `maximum-session-cache-size` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `maximum-session-cache-size` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `maximum-session-cache-size` attribute description: the maximum number of `ssl` sessions in the cache. the default value -`1` means use the `jvm` default value. value zero means there is no limit.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=maximum-session-cache-size)`

## syntax of the operation to get the `need-client-auth` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `need-client-auth` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `need-client-auth` attribute description: to require a client certificate on `ssl` handshake. connection without trusted client certificate (see `trust-manager`) will be rejected.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=need-client-auth)`

## syntax of the operation to get the `post-realm-principal-transformer` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `post-realm-principal-transformer` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `post-realm-principal-transformer` attribute description: a principal transformer to apply after the realm is selected.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=post-realm-principal-transformer)`

## syntax of the operation to get the `pre-realm-principal-transformer` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `pre-realm-principal-transformer` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `pre-realm-principal-transformer` attribute description: a principal transformer to apply before the realm is selected.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=pre-realm-principal-transformer)`

## syntax of the operation to get the `protocols` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `protocols` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `protocols` attribute description: the enabled protocols.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=protocols)`

## syntax of the operation to get the `provider-name` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `provider-name` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `provider-name` attribute description: the name of the provider to use. if not specified, all providers from providers will be passed to the `sslcontext`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=provider-name)`

## syntax of the operation to get the `providers` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `providers` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `providers` attribute description: the name of the providers to obtain the provider[] to use to load the `sslcontext`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=providers)`

## syntax of the operation to get the `realm-mapper` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `realm-mapper` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `realm-mapper` attribute description: the realm mapper to be used for `ssl` authentication.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=realm-mapper)`

## syntax of the operation to get the `security-domain` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `security-domain` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `security-domain` attribute description: the security domain to use for authentication during `ssl` session establishment.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=security-domain)`

## syntax of the operation to get the `session-timeout` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `session-timeout` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `session-timeout` attribute description: the timeout for `ssl` sessions, in seconds. the default value -`1` means use the `jvm` default value. value zero means there is no limit.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=session-timeout)`

## syntax of the operation to get the `trust-manager` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `trust-manager` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `trust-manager` attribute description: reference to the trust manager to use within the `sslcontext`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=trust-manager)`

## syntax of the operation to get the `use-cipher-suites-order` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `use-cipher-suites-order` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `use-cipher-suites-order` attribute description: to honor local cipher suites preference.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=use-cipher-suites-order)`

## syntax of the operation to get the `want-client-auth` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `want-client-auth` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `want-client-auth` attribute description: to request (but not to require) a client certificate on `ssl` handshake. if a security domain is referenced and supports `x509` evidence, this will be set to true automatically. ignored when `need-client-auth` is set.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=want-client-auth)`

## syntax of the operation to get the `wrap` attribute of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `wrap` attribute value of the `server-ssl-context` resource of the `elytron` subsystem
* `wrap` attribute description: should the `sslengine`, `sslsocket`, and `sslserversocket` instances returned be wrapped to protect against further modification.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>:read-attribute(name=wrap)`

## syntax of the operation to get a `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-resource()`
* To get the list of all the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem use '*' for `<ssl-session name>`.

## syntax of the operation to get the `application-buffer-size` attribute of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `application-buffer-size` attribute value of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* `application-buffer-size` attribute description: the application buffer size as reported by the `sslsession`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=application-buffer-size)`

## syntax of the operation to get the `cipher-suite` attribute of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `cipher-suite` attribute value of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* `cipher-suite` attribute description: the selected cipher suite as reported by the `sslsession`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=cipher-suite)`

## syntax of the operation to get the `creation-time` attribute of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `creation-time` attribute value of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* `creation-time` attribute description: the creation time as reported by the `sslsession`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=creation-time)`

## syntax of the operation to get the `last-accessed-time` attribute of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `last-accessed-time` attribute value of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* `last-accessed-time` attribute description: the last accessed time as reported by the `sslsession`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=last-accessed-time)`

## syntax of the operation to get the `local-certificates` attribute of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `local-certificates` attribute value of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* `local-certificates` attribute description: the local certificates from the `sslsession`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=local-certificates)`

## syntax of the operation to get the `local-principal` attribute of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `local-principal` attribute value of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* `local-principal` attribute description: the local principal as reported by the `sslsession`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=local-principal)`

## syntax of the operation to get the `packet-buffer-size` attribute of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `packet-buffer-size` attribute value of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* `packet-buffer-size` attribute description: the packet buffer size as reported by the `sslsession`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=packet-buffer-size)`

## syntax of the operation to get the `peer-certificates` attribute of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `peer-certificates` attribute value of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* `peer-certificates` attribute description: the peer certificates from the `sslsession`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=peer-certificates)`

## syntax of the operation to get the `peer-host` attribute of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `peer-host` attribute value of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* `peer-host` attribute description: the peer host as reported by the `sslsession`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=peer-host)`

## syntax of the operation to get the `peer-port` attribute of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `peer-port` attribute value of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* `peer-port` attribute description: the peer port as reported by the `sslsession`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=peer-port)`

## syntax of the operation to get the `peer-principal` attribute of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `peer-principal` attribute value of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* `peer-principal` attribute description: the peer principal as reported by the `sslsession`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=peer-principal)`

## syntax of the operation to get the `protocol` attribute of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `protocol` attribute value of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* `protocol` attribute description: the protocol as reported by the `sslsession`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=protocol)`

## syntax of the operation to get the `valid` attribute of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* Retrieve the `valid` attribute value of the `ssl-session` resource of the `server-ssl-context` resource of the `elytron` subsystem
* `valid` attribute description: the validity of the session as reported by the `sslsession`.
* operation: `/subsystem=elytron/server-ssl-context=<server-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=valid)`

## syntax of the operation to get a `chained-principal-transformer` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/chained-principal-transformer=<chained-principal-transformer name>:read-resource()`
* To get the list of all the `chained-principal-transformer` resource of the `elytron` subsystem use '*' for `<chained-principal-transformer name>`.

## syntax of the operation to get the `principal-transformers` attribute of the `chained-principal-transformer` resource of the `elytron` subsystem
* Retrieve the `principal-transformers` attribute value of the `chained-principal-transformer` resource of the `elytron` subsystem
* `principal-transformers` attribute description: the referenced principal transformers to chain.
* operation: `/subsystem=elytron/chained-principal-transformer=<chained-principal-transformer name>:read-attribute(name=principal-transformers)`

## syntax of the operation to get a `client-ssl-context` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>:read-resource()`
* To get the list of all the `client-ssl-context` resource of the `elytron` subsystem use '*' for `<client-ssl-context name>`.

## syntax of the operation to get the `active-session-count` attribute of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `active-session-count` attribute value of the `client-ssl-context` resource of the `elytron` subsystem
* `active-session-count` attribute description: the count of current active sessions.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>:read-attribute(name=active-session-count)`

## syntax of the operation to get the `cipher-suite-filter` attribute of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `cipher-suite-filter` attribute value of the `client-ssl-context` resource of the `elytron` subsystem
* `cipher-suite-filter` attribute description: the filter to apply to specify the enabled cipher suites for `tlsv1.2` and below.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>:read-attribute(name=cipher-suite-filter)`

## syntax of the operation to get the `cipher-suite-names` attribute of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `cipher-suite-names` attribute value of the `client-ssl-context` resource of the `elytron` subsystem
* `cipher-suite-names` attribute description: the filter to apply to specify the enabled cipher suites for `tlsv1.3`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>:read-attribute(name=cipher-suite-names)`

## syntax of the operation to get the `key-manager` attribute of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `key-manager` attribute value of the `client-ssl-context` resource of the `elytron` subsystem
* `key-manager` attribute description: reference to the key manager to use within the `sslcontext`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>:read-attribute(name=key-manager)`

## syntax of the operation to get the `protocols` attribute of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `protocols` attribute value of the `client-ssl-context` resource of the `elytron` subsystem
* `protocols` attribute description: the enabled protocols.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>:read-attribute(name=protocols)`

## syntax of the operation to get the `provider-name` attribute of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `provider-name` attribute value of the `client-ssl-context` resource of the `elytron` subsystem
* `provider-name` attribute description: the name of the provider to use. if not specified, all providers from providers will be passed to the `sslcontext`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>:read-attribute(name=provider-name)`

## syntax of the operation to get the `providers` attribute of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `providers` attribute value of the `client-ssl-context` resource of the `elytron` subsystem
* `providers` attribute description: the name of the providers to obtain the provider[] to use to load the `sslcontext`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>:read-attribute(name=providers)`

## syntax of the operation to get the `trust-manager` attribute of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `trust-manager` attribute value of the `client-ssl-context` resource of the `elytron` subsystem
* `trust-manager` attribute description: reference to the trust manager to use within the `sslcontext`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>:read-attribute(name=trust-manager)`

## syntax of the operation to get a `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-resource()`
* To get the list of all the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem use '*' for `<ssl-session name>`.

## syntax of the operation to get the `application-buffer-size` attribute of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `application-buffer-size` attribute value of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* `application-buffer-size` attribute description: the application buffer size as reported by the `sslsession`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=application-buffer-size)`

## syntax of the operation to get the `cipher-suite` attribute of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `cipher-suite` attribute value of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* `cipher-suite` attribute description: the selected cipher suite as reported by the `sslsession`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=cipher-suite)`

## syntax of the operation to get the `creation-time` attribute of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `creation-time` attribute value of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* `creation-time` attribute description: the creation time as reported by the `sslsession`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=creation-time)`

## syntax of the operation to get the `last-accessed-time` attribute of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `last-accessed-time` attribute value of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* `last-accessed-time` attribute description: the last accessed time as reported by the `sslsession`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=last-accessed-time)`

## syntax of the operation to get the `local-certificates` attribute of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `local-certificates` attribute value of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* `local-certificates` attribute description: the local certificates from the `sslsession`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=local-certificates)`

## syntax of the operation to get the `local-principal` attribute of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `local-principal` attribute value of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* `local-principal` attribute description: the local principal as reported by the `sslsession`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=local-principal)`

## syntax of the operation to get the `packet-buffer-size` attribute of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `packet-buffer-size` attribute value of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* `packet-buffer-size` attribute description: the packet buffer size as reported by the `sslsession`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=packet-buffer-size)`

## syntax of the operation to get the `peer-certificates` attribute of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `peer-certificates` attribute value of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* `peer-certificates` attribute description: the peer certificates from the `sslsession`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=peer-certificates)`

## syntax of the operation to get the `peer-host` attribute of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `peer-host` attribute value of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* `peer-host` attribute description: the peer host as reported by the `sslsession`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=peer-host)`

## syntax of the operation to get the `peer-port` attribute of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `peer-port` attribute value of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* `peer-port` attribute description: the peer port as reported by the `sslsession`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=peer-port)`

## syntax of the operation to get the `peer-principal` attribute of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `peer-principal` attribute value of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* `peer-principal` attribute description: the peer principal as reported by the `sslsession`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=peer-principal)`

## syntax of the operation to get the `protocol` attribute of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `protocol` attribute value of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* `protocol` attribute description: the protocol as reported by the `sslsession`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=protocol)`

## syntax of the operation to get the `valid` attribute of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `valid` attribute value of the `ssl-session` resource of the `client-ssl-context` resource of the `elytron` subsystem
* `valid` attribute description: the validity of the session as reported by the `sslsession`.
* operation: `/subsystem=elytron/client-ssl-context=<client-ssl-context name>/ssl-session=<ssl-session name>:read-attribute(name=valid)`

## syntax of the operation to get a `simple-regex-realm-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/simple-regex-realm-mapper=<simple-regex-realm-mapper name>:read-resource()`
* To get the list of all the `simple-regex-realm-mapper` resource of the `elytron` subsystem use '*' for `<simple-regex-realm-mapper name>`.

## syntax of the operation to get the `delegate-realm-mapper` attribute of the `simple-regex-realm-mapper` resource of the `elytron` subsystem
* Retrieve the `delegate-realm-mapper` attribute value of the `simple-regex-realm-mapper` resource of the `elytron` subsystem
* `delegate-realm-mapper` attribute description: the `realmmapper` to delegate to if there is no match using the pattern.
* operation: `/subsystem=elytron/simple-regex-realm-mapper=<simple-regex-realm-mapper name>:read-attribute(name=delegate-realm-mapper)`

## syntax of the operation to get the `pattern` attribute of the `simple-regex-realm-mapper` resource of the `elytron` subsystem
* Retrieve the `pattern` attribute value of the `simple-regex-realm-mapper` resource of the `elytron` subsystem
* `pattern` attribute description: the regular expression which must contain at least one capture group to extract the realm from the name. if the regular expression matches more than one capture group, the first capture group is used.
* operation: `/subsystem=elytron/simple-regex-realm-mapper=<simple-regex-realm-mapper name>:read-attribute(name=pattern)`

## syntax of the operation to get a `x509-subject-alt-name-evidence-decoder` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/x509-subject-alt-name-evidence-decoder=<x509-subject-alt-name-evidence-decoder name>:read-resource()`
* To get the list of all the `x509-subject-alt-name-evidence-decoder` resource of the `elytron` subsystem use '*' for `<x509-subject-alt-name-evidence-decoder name>`.

## syntax of the operation to get the `alt-name-type` attribute of the `x509-subject-alt-name-evidence-decoder` resource of the `elytron` subsystem
* Retrieve the `alt-name-type` attribute value of the `x509-subject-alt-name-evidence-decoder` resource of the `elytron` subsystem
* `alt-name-type` attribute description: the subject alternative name type to decode from the given evidence. allowed values: '`rfc822name`', '`dnsname`', '`directoryname`', '`uniformresourceidentifier`', '`ipaddress`', '`registeredid`'
* operation: `/subsystem=elytron/x509-subject-alt-name-evidence-decoder=<x509-subject-alt-name-evidence-decoder name>:read-attribute(name=alt-name-type)`

## syntax of the operation to get the `segment` attribute of the `x509-subject-alt-name-evidence-decoder` resource of the `elytron` subsystem
* Retrieve the `segment` attribute value of the `x509-subject-alt-name-evidence-decoder` resource of the `elytron` subsystem
* `segment` attribute description: the `0-based` occurrence of the subject alternative name to map. this attribute is optional and only used when there is more than one subject alternative name of the given `alt-name-type`. the default value is `0`.
* operation: `/subsystem=elytron/x509-subject-alt-name-evidence-decoder=<x509-subject-alt-name-evidence-decoder name>:read-attribute(name=segment)`

## syntax of the operation to get a `authentication-context` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/authentication-context=<authentication-context name>:read-resource()`
* To get the list of all the `authentication-context` resource of the `elytron` subsystem use '*' for `<authentication-context name>`.

## syntax of the operation to get the `extends` attribute of the `authentication-context` resource of the `elytron` subsystem
* Retrieve the `extends` attribute value of the `authentication-context` resource of the `elytron` subsystem
* `extends` attribute description: a previously defined authentication context to extend.
* operation: `/subsystem=elytron/authentication-context=<authentication-context name>:read-attribute(name=extends)`

## syntax of the operation to get the `match-rules` attribute of the `authentication-context` resource of the `elytron` subsystem
* Retrieve the `match-rules` attribute value of the `authentication-context` resource of the `elytron` subsystem
* `match-rules` attribute description: the `match-rules` for this authentication context.
* operation: `/subsystem=elytron/authentication-context=<authentication-context name>:read-attribute(name=match-rules)`

## syntax of the operation to get a `constant-principal-decoder` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/constant-principal-decoder=<constant-principal-decoder name>:read-resource()`
* To get the list of all the `constant-principal-decoder` resource of the `elytron` subsystem use '*' for `<constant-principal-decoder name>`.

## syntax of the operation to get the `constant` attribute of the `constant-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `constant` attribute value of the `constant-principal-decoder` resource of the `elytron` subsystem
* `constant` attribute description: the constant value the principal decoder will always return.
* operation: `/subsystem=elytron/constant-principal-decoder=<constant-principal-decoder name>:read-attribute(name=constant)`

## syntax of the operation to get a `certificate-authority-account` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/certificate-authority-account=<certificate-authority-account name>:read-resource()`
* To get the list of all the `certificate-authority-account` resource of the `elytron` subsystem use '*' for `<certificate-authority-account name>`.

## syntax of the operation to get the `alias` attribute of the `certificate-authority-account` resource of the `elytron` subsystem
* Retrieve the `alias` attribute value of the `certificate-authority-account` resource of the `elytron` subsystem
* `alias` attribute description: the alias of certificate authority account key in the `keystore`. if the alias does not already exist in the `keystore`, a certificate authority account key will be automatically generated and stored as a `privatekeyentry` under the alias.
* operation: `/subsystem=elytron/certificate-authority-account=<certificate-authority-account name>:read-attribute(name=alias)`

## syntax of the operation to get the `certificate-authority` attribute of the `certificate-authority-account` resource of the `elytron` subsystem
* Retrieve the `certificate-authority` attribute value of the `certificate-authority-account` resource of the `elytron` subsystem
* `certificate-authority` attribute description: the name of the certificate authority to use.
* operation: `/subsystem=elytron/certificate-authority-account=<certificate-authority-account name>:read-attribute(name=certificate-authority)`

## syntax of the operation to get the `contact-urls` attribute of the `certificate-authority-account` resource of the `elytron` subsystem
* Retrieve the `contact-urls` attribute value of the `certificate-authority-account` resource of the `elytron` subsystem
* `contact-urls` attribute description: a list of `urls` that the certificate authority can contact about any issues related to this account.
* operation: `/subsystem=elytron/certificate-authority-account=<certificate-authority-account name>:read-attribute(name=contact-urls)`

## syntax of the operation to get the `credential-reference` attribute of the `certificate-authority-account` resource of the `elytron` subsystem
* Retrieve the `credential-reference` attribute value of the `certificate-authority-account` resource of the `elytron` subsystem
* `credential-reference` attribute description: credential to be used when accessing the certificate authority account key.
* operation: `/subsystem=elytron/certificate-authority-account=<certificate-authority-account name>:read-attribute(name=credential-reference)`

## syntax of the operation to get the `key-store` attribute of the `certificate-authority-account` resource of the `elytron` subsystem
* Retrieve the `key-store` attribute value of the `certificate-authority-account` resource of the `elytron` subsystem
* `key-store` attribute description: the `keystore` that contains the certificate authority account key.
* operation: `/subsystem=elytron/certificate-authority-account=<certificate-authority-account name>:read-attribute(name=key-store)`

## syntax of the operation to get a `aggregate-principal-decoder` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/aggregate-principal-decoder=<aggregate-principal-decoder name>:read-resource()`
* To get the list of all the `aggregate-principal-decoder` resource of the `elytron` subsystem use '*' for `<aggregate-principal-decoder name>`.

## syntax of the operation to get the `principal-decoders` attribute of the `aggregate-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `principal-decoders` attribute value of the `aggregate-principal-decoder` resource of the `elytron` subsystem
* `principal-decoders` attribute description: the referenced principal decoders to aggregate.
* operation: `/subsystem=elytron/aggregate-principal-decoder=<aggregate-principal-decoder name>:read-attribute(name=principal-decoders)`

## syntax of the operation to get a `simple-permission-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/simple-permission-mapper=<simple-permission-mapper name>:read-resource()`
* To get the list of all the `simple-permission-mapper` resource of the `elytron` subsystem use '*' for `<simple-permission-mapper name>`.

## syntax of the operation to get the `mapping-mode` attribute of the `simple-permission-mapper` resource of the `elytron` subsystem
* Retrieve the `mapping-mode` attribute value of the `simple-permission-mapper` resource of the `elytron` subsystem
* `mapping-mode` attribute description: the mapping mode that should be used in the event of multiple matches.
* operation: `/subsystem=elytron/simple-permission-mapper=<simple-permission-mapper name>:read-attribute(name=mapping-mode)`

## syntax of the operation to get the `permission-mappings` attribute of the `simple-permission-mapper` resource of the `elytron` subsystem
* Retrieve the `permission-mappings` attribute value of the `simple-permission-mapper` resource of the `elytron` subsystem
* `permission-mappings` attribute description: the defined permission mappings.
* operation: `/subsystem=elytron/simple-permission-mapper=<simple-permission-mapper name>:read-attribute(name=permission-mappings)`

## syntax of the operation to get a `filtering-key-store` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/filtering-key-store=<filtering-key-store name>:read-resource()`
* To get the list of all the `filtering-key-store` resource of the `elytron` subsystem use '*' for `<filtering-key-store name>`.

## syntax of the operation to get the `alias-filter` attribute of the `filtering-key-store` resource of the `elytron` subsystem
* Retrieve the `alias-filter` attribute value of the `filtering-key-store` resource of the `elytron` subsystem
* `alias-filter` attribute description: a filter to apply to the aliases returned from the `keystore`, can either be a comma separated list of aliases to return or one of the following formats `all:-alias1:-alias2`, `none:+alias1:+alias2`
* operation: `/subsystem=elytron/filtering-key-store=<filtering-key-store name>:read-attribute(name=alias-filter)`

## syntax of the operation to get the `key-store` attribute of the `filtering-key-store` resource of the `elytron` subsystem
* Retrieve the `key-store` attribute value of the `filtering-key-store` resource of the `elytron` subsystem
* `key-store` attribute description: name of filtered `keystore`.
* operation: `/subsystem=elytron/filtering-key-store=<filtering-key-store name>:read-attribute(name=key-store)`

## syntax of the operation to get the `state` attribute of the `filtering-key-store` resource of the `elytron` subsystem
* Retrieve the `state` attribute value of the `filtering-key-store` resource of the `elytron` subsystem
* `state` attribute description: the state of the underlying service that represents this `keystore` at runtime, if it is anything other than up runtime operations will not be available.
* operation: `/subsystem=elytron/filtering-key-store=<filtering-key-store name>:read-attribute(name=state)`

## syntax of the operation to get a `server-ssl-sni-context` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/server-ssl-sni-context=<server-ssl-sni-context name>:read-resource()`
* To get the list of all the `server-ssl-sni-context` resource of the `elytron` subsystem use '*' for `<server-ssl-sni-context name>`.

## syntax of the operation to get the `default-ssl-context` attribute of the `server-ssl-sni-context` resource of the `elytron` subsystem
* Retrieve the `default-ssl-context` attribute value of the `server-ssl-sni-context` resource of the `elytron` subsystem
* `default-ssl-context` attribute description: the context to use if no `sni` information is present, or if it does not match any mappings
* operation: `/subsystem=elytron/server-ssl-sni-context=<server-ssl-sni-context name>:read-attribute(name=default-ssl-context)`

## syntax of the operation to get the `host-context-map` attribute of the `server-ssl-sni-context` resource of the `elytron` subsystem
* Retrieve the `host-context-map` attribute value of the `server-ssl-sni-context` resource of the `elytron` subsystem
* `host-context-map` attribute description: a mapping between a server name and an `sscontext`
* operation: `/subsystem=elytron/server-ssl-sni-context=<server-ssl-sni-context name>:read-attribute(name=host-context-map)`

## syntax of the operation to get a `syslog-audit-log` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/syslog-audit-log=<syslog-audit-log name>:read-resource()`
* To get the list of all the `syslog-audit-log` resource of the `elytron` subsystem use '*' for `<syslog-audit-log name>`.

## syntax of the operation to get the `format` attribute of the `syslog-audit-log` resource of the `elytron` subsystem
* Retrieve the `format` attribute value of the `syslog-audit-log` resource of the `elytron` subsystem
* `format` attribute description: the format to use to record the audit event.
* operation: `/subsystem=elytron/syslog-audit-log=<syslog-audit-log name>:read-attribute(name=format)`

## syntax of the operation to get the `host-name` attribute of the `syslog-audit-log` resource of the `elytron` subsystem
* Retrieve the `host-name` attribute value of the `syslog-audit-log` resource of the `elytron` subsystem
* `host-name` attribute description: the host name to embed withing all events sent to the remote `syslog` server.
* operation: `/subsystem=elytron/syslog-audit-log=<syslog-audit-log name>:read-attribute(name=host-name)`

## syntax of the operation to get the `port` attribute of the `syslog-audit-log` resource of the `elytron` subsystem
* Retrieve the `port` attribute value of the `syslog-audit-log` resource of the `elytron` subsystem
* `port` attribute description: the listening port on the `syslog` server.
* operation: `/subsystem=elytron/syslog-audit-log=<syslog-audit-log name>:read-attribute(name=port)`

## syntax of the operation to get the `reconnect-attempts` attribute of the `syslog-audit-log` resource of the `elytron` subsystem
* Retrieve the `reconnect-attempts` attribute value of the `syslog-audit-log` resource of the `elytron` subsystem
* `reconnect-attempts` attribute description: the maximum amount of failed reconnect attempts that should be made for sending messages to a `syslog` server before the endpoint is closed.
* operation: `/subsystem=elytron/syslog-audit-log=<syslog-audit-log name>:read-attribute(name=reconnect-attempts)`

## syntax of the operation to get the `server-address` attribute of the `syslog-audit-log` resource of the `elytron` subsystem
* Retrieve the `server-address` attribute value of the `syslog-audit-log` resource of the `elytron` subsystem
* `server-address` attribute description: the server address of the `syslog` server the events should be sent to.
* operation: `/subsystem=elytron/syslog-audit-log=<syslog-audit-log name>:read-attribute(name=server-address)`

## syntax of the operation to get the `ssl-context` attribute of the `syslog-audit-log` resource of the `elytron` subsystem
* Retrieve the `ssl-context` attribute value of the `syslog-audit-log` resource of the `elytron` subsystem
* `ssl-context` attribute description: the `sslcontext` to use to connect to the `syslog` server when `ssl_tcp` transport is used.
* operation: `/subsystem=elytron/syslog-audit-log=<syslog-audit-log name>:read-attribute(name=ssl-context)`

## syntax of the operation to get the `syslog-format` attribute of the `syslog-audit-log` resource of the `elytron` subsystem
* Retrieve the `syslog-format` attribute value of the `syslog-audit-log` resource of the `elytron` subsystem
* `syslog-format` attribute description: the `rfc` format to be used for describing the audit event.
* operation: `/subsystem=elytron/syslog-audit-log=<syslog-audit-log name>:read-attribute(name=syslog-format)`

## syntax of the operation to get the `transport` attribute of the `syslog-audit-log` resource of the `elytron` subsystem
* Retrieve the `transport` attribute value of the `syslog-audit-log` resource of the `elytron` subsystem
* `transport` attribute description: the transport to use to connect to the `syslog` server.
* operation: `/subsystem=elytron/syslog-audit-log=<syslog-audit-log name>:read-attribute(name=transport)`

## syntax of the operation to get a `aggregate-evidence-decoder` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/aggregate-evidence-decoder=<aggregate-evidence-decoder name>:read-resource()`
* To get the list of all the `aggregate-evidence-decoder` resource of the `elytron` subsystem use '*' for `<aggregate-evidence-decoder name>`.

## syntax of the operation to get the `evidence-decoders` attribute of the `aggregate-evidence-decoder` resource of the `elytron` subsystem
* Retrieve the `evidence-decoders` attribute value of the `aggregate-evidence-decoder` resource of the `elytron` subsystem
* `evidence-decoders` attribute description: the referenced evidence decoders to aggregate.
* operation: `/subsystem=elytron/aggregate-evidence-decoder=<aggregate-evidence-decoder name>:read-attribute(name=evidence-decoders)`

## syntax of the operation to get a `certificate-authority` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/certificate-authority=<certificate-authority name>:read-resource()`
* To get the list of all the `certificate-authority` resource of the `elytron` subsystem use '*' for `<certificate-authority name>`.

## syntax of the operation to get the `staging-url` attribute of the `certificate-authority` resource of the `elytron` subsystem
* Retrieve the `staging-url` attribute value of the `certificate-authority` resource of the `elytron` subsystem
* `staging-url` attribute description: `url` of the certificate authority to use in `pre-production`.
* operation: `/subsystem=elytron/certificate-authority=<certificate-authority name>:read-attribute(name=staging-url)`

## syntax of the operation to get the `url` attribute of the `certificate-authority` resource of the `elytron` subsystem
* Retrieve the `url` attribute value of the `certificate-authority` resource of the `elytron` subsystem
* `url` attribute description: `url` of the certificate authority.
* operation: `/subsystem=elytron/certificate-authority=<certificate-authority name>:read-attribute(name=url)`

## syntax of the operation to get a `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/x500-attribute-principal-decoder=<x500-attribute-principal-decoder name>:read-resource()`
* To get the list of all the `x500-attribute-principal-decoder` resource of the `elytron` subsystem use '*' for `<x500-attribute-principal-decoder name>`.

## syntax of the operation to get the `attribute-name` attribute of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `attribute-name` attribute value of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* `attribute-name` attribute description: the name of the `x.500` attribute to map (can be defined using `oid` instead)
* operation: `/subsystem=elytron/x500-attribute-principal-decoder=<x500-attribute-principal-decoder name>:read-attribute(name=attribute-name)`

## syntax of the operation to get the `convert` attribute of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `convert` attribute value of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* `convert` attribute description: when set to 'true', if the principal is not already an `x500principal` conversion will be attempted
* operation: `/subsystem=elytron/x500-attribute-principal-decoder=<x500-attribute-principal-decoder name>:read-attribute(name=convert)`

## syntax of the operation to get the `joiner` attribute of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `joiner` attribute value of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* `joiner` attribute description: the joining string
* operation: `/subsystem=elytron/x500-attribute-principal-decoder=<x500-attribute-principal-decoder name>:read-attribute(name=joiner)`

## syntax of the operation to get the `maximum-segments` attribute of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `maximum-segments` attribute value of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* `maximum-segments` attribute description: the maximum number of occurrences of the attribute to map
* operation: `/subsystem=elytron/x500-attribute-principal-decoder=<x500-attribute-principal-decoder name>:read-attribute(name=maximum-segments)`

## syntax of the operation to get the `oid` attribute of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `oid` attribute value of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* `oid` attribute description: the `oid` of the `x.500` attribute to map (can be defined using attribute name instead)
* operation: `/subsystem=elytron/x500-attribute-principal-decoder=<x500-attribute-principal-decoder name>:read-attribute(name=oid)`

## syntax of the operation to get the `required-attributes` attribute of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `required-attributes` attribute value of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* `required-attributes` attribute description: the attributes names of the attributes that must be present in the principal
* operation: `/subsystem=elytron/x500-attribute-principal-decoder=<x500-attribute-principal-decoder name>:read-attribute(name=required-attributes)`

## syntax of the operation to get the `required-oids` attribute of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `required-oids` attribute value of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* `required-oids` attribute description: the `oids` of the attributes that must be present in the principal
* operation: `/subsystem=elytron/x500-attribute-principal-decoder=<x500-attribute-principal-decoder name>:read-attribute(name=required-oids)`

## syntax of the operation to get the `reverse` attribute of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `reverse` attribute value of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* `reverse` attribute description: when set to 'true', the attribute values will be processed and returned in reverse order
* operation: `/subsystem=elytron/x500-attribute-principal-decoder=<x500-attribute-principal-decoder name>:read-attribute(name=reverse)`

## syntax of the operation to get the `start-segment` attribute of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `start-segment` attribute value of the `x500-attribute-principal-decoder` resource of the `elytron` subsystem
* `start-segment` attribute description: the `0-based` starting occurrence of the attribute to map
* operation: `/subsystem=elytron/x500-attribute-principal-decoder=<x500-attribute-principal-decoder name>:read-attribute(name=start-segment)`

## syntax of the operation to get a `token-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/token-realm=<token-realm name>:read-resource()`
* To get the list of all the `token-realm` resource of the `elytron` subsystem use '*' for `<token-realm name>`.

## syntax of the operation to get the `jwt` attribute of the `token-realm` resource of the `elytron` subsystem
* Retrieve the `jwt` attribute value of the `token-realm` resource of the `elytron` subsystem
* `jwt` attribute description: a token `validator` to be used in conjunction with a `token-based` realm that handles security tokens based on the `jwt/jws` standard.
* operation: `/subsystem=elytron/token-realm=<token-realm name>:read-attribute(name=jwt)`

## syntax of the operation to get the `oauth2-introspection` attribute of the `token-realm` resource of the `elytron` subsystem
* Retrieve the `oauth2-introspection` attribute value of the `token-realm` resource of the `elytron` subsystem
* `oauth2-introspection` attribute description: a token `validator` to be used in conjunction with a `token-based` realm that handles `oauth2` access tokens and validates them using an endpoint compliant with `oauth2` token introspection `specification(rfc-7662`).
* operation: `/subsystem=elytron/token-realm=<token-realm name>:read-attribute(name=oauth2-introspection)`

## syntax of the operation to get the `principal-claim` attribute of the `token-realm` resource of the `elytron` subsystem
* Retrieve the `principal-claim` attribute value of the `token-realm` resource of the `elytron` subsystem
* `principal-claim` attribute description: the name of the claim that should be used to obtain the `principal's` name.
* operation: `/subsystem=elytron/token-realm=<token-realm name>:read-attribute(name=principal-claim)`

## syntax of the operation to get a `custom-permission-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/custom-permission-mapper=<custom-permission-mapper name>:read-resource()`
* To get the list of all the `custom-permission-mapper` resource of the `elytron` subsystem use '*' for `<custom-permission-mapper name>`.

## syntax of the operation to get the `class-name` attribute of the `custom-permission-mapper` resource of the `elytron` subsystem
* Retrieve the `class-name` attribute value of the `custom-permission-mapper` resource of the `elytron` subsystem
* `class-name` attribute description: fully qualified class name of the permission mapper
* operation: `/subsystem=elytron/custom-permission-mapper=<custom-permission-mapper name>:read-attribute(name=class-name)`

## syntax of the operation to get the `configuration` attribute of the `custom-permission-mapper` resource of the `elytron` subsystem
* Retrieve the `configuration` attribute value of the `custom-permission-mapper` resource of the `elytron` subsystem
* `configuration` attribute description: the optional `kay/value` configuration for the permission mapper
* operation: `/subsystem=elytron/custom-permission-mapper=<custom-permission-mapper name>:read-attribute(name=configuration)`

## syntax of the operation to get the `module` attribute of the `custom-permission-mapper` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `custom-permission-mapper` resource of the `elytron` subsystem
* `module` attribute description: name of the module to use to load the permission mapper
* operation: `/subsystem=elytron/custom-permission-mapper=<custom-permission-mapper name>:read-attribute(name=module)`

## syntax of the operation to get a `mapped-role-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/mapped-role-mapper=<mapped-role-mapper name>:read-resource()`
* To get the list of all the `mapped-role-mapper` resource of the `elytron` subsystem use '*' for `<mapped-role-mapper name>`.

## syntax of the operation to get the `keep-mapped` attribute of the `mapped-role-mapper` resource of the `elytron` subsystem
* Retrieve the `keep-mapped` attribute value of the `mapped-role-mapper` resource of the `elytron` subsystem
* `keep-mapped` attribute description: when set to 'true' the mapped roles will retain all roles, that have defined mappings.
* operation: `/subsystem=elytron/mapped-role-mapper=<mapped-role-mapper name>:read-attribute(name=keep-mapped)`

## syntax of the operation to get the `keep-non-mapped` attribute of the `mapped-role-mapper` resource of the `elytron` subsystem
* Retrieve the `keep-non-mapped` attribute value of the `mapped-role-mapper` resource of the `elytron` subsystem
* `keep-non-mapped` attribute description: when set to 'true' the mapped roles will retain all roles, that have no defined mappings.
* operation: `/subsystem=elytron/mapped-role-mapper=<mapped-role-mapper name>:read-attribute(name=keep-non-mapped)`

## syntax of the operation to get the `role-map` attribute of the `mapped-role-mapper` resource of the `elytron` subsystem
* Retrieve the `role-map` attribute value of the `mapped-role-mapper` resource of the `elytron` subsystem
* `role-map` attribute description: a list of mappings to use for mapping roles.
* operation: `/subsystem=elytron/mapped-role-mapper=<mapped-role-mapper name>:read-attribute(name=role-map)`

## syntax of the operation to get a `regex-role-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/regex-role-mapper=<regex-role-mapper name>:read-resource()`
* To get the list of all the `regex-role-mapper` resource of the `elytron` subsystem use '*' for `<regex-role-mapper name>`.

## syntax of the operation to get the `keep-non-mapped` attribute of the `regex-role-mapper` resource of the `elytron` subsystem
* Retrieve the `keep-non-mapped` attribute value of the `regex-role-mapper` resource of the `elytron` subsystem
* `keep-non-mapped` attribute description: when set to 'true' then the roles that did not match the pattern will be kept and not removed.
* operation: `/subsystem=elytron/regex-role-mapper=<regex-role-mapper name>:read-attribute(name=keep-non-mapped)`

## syntax of the operation to get the `pattern` attribute of the `regex-role-mapper` resource of the `elytron` subsystem
* Retrieve the `pattern` attribute value of the `regex-role-mapper` resource of the `elytron` subsystem
* `pattern` attribute description: `regex` string that will be used for matching. `regex` can capture groups. role matches the pattern if given pattern can be found in any substring of given role.
* operation: `/subsystem=elytron/regex-role-mapper=<regex-role-mapper name>:read-attribute(name=pattern)`

## syntax of the operation to get the `replace-all` attribute of the `regex-role-mapper` resource of the `elytron` subsystem
* Retrieve the `replace-all` attribute value of the `regex-role-mapper` resource of the `elytron` subsystem
* `replace-all` attribute description: when set to 'false', only first occurrence of the pattern will be replaced in role. when set to 'true' then all of the occurrences will be replaced by replacement.
* operation: `/subsystem=elytron/regex-role-mapper=<regex-role-mapper name>:read-attribute(name=replace-all)`

## syntax of the operation to get the `replacement` attribute of the `regex-role-mapper` resource of the `elytron` subsystem
* Retrieve the `replacement` attribute value of the `regex-role-mapper` resource of the `elytron` subsystem
* `replacement` attribute description: replacement that will be used when mapping roles that contain the pattern. can make use of captured groups from pattern.
* operation: `/subsystem=elytron/regex-role-mapper=<regex-role-mapper name>:read-attribute(name=replacement)`

## syntax of the operation to get a `dir-context` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/dir-context=<dir-context name>:read-resource()`
* To get the list of all the `dir-context` resource of the `elytron` subsystem use '*' for `<dir-context name>`.

## syntax of the operation to get the `authentication-context` attribute of the `dir-context` resource of the `elytron` subsystem
* Retrieve the `authentication-context` attribute value of the `dir-context` resource of the `elytron` subsystem
* `authentication-context` attribute description: the authentication context to obtain login credentials to connect to the `ldap` server. can be omitted if `authentication-level` is 'none' (anonymous).
* operation: `/subsystem=elytron/dir-context=<dir-context name>:read-attribute(name=authentication-context)`

## syntax of the operation to get the `authentication-level` attribute of the `dir-context` resource of the `elytron` subsystem
* Retrieve the `authentication-level` attribute value of the `dir-context` resource of the `elytron` subsystem
* `authentication-level` attribute description: the authentication level (security `level/authentication` mechanism) to use. corresponds to `security_authentication` ('`java.naming.security.authentication`') environment property. allowed values: 'none', 'simple', `sasl_mech`, where `sasl_mech` is a `space-separated` list of `sasl` mechanism names.
* operation: `/subsystem=elytron/dir-context=<dir-context name>:read-attribute(name=authentication-level)`

## syntax of the operation to get the `connection-timeout` attribute of the `dir-context` resource of the `elytron` subsystem
* Retrieve the `connection-timeout` attribute value of the `dir-context` resource of the `elytron` subsystem
* `connection-timeout` attribute description: the timeout for connecting to the `ldap` server in milliseconds.
* operation: `/subsystem=elytron/dir-context=<dir-context name>:read-attribute(name=connection-timeout)`

## syntax of the operation to get the `credential-reference` attribute of the `dir-context` resource of the `elytron` subsystem
* Retrieve the `credential-reference` attribute value of the `dir-context` resource of the `elytron` subsystem
* `credential-reference` attribute description: the credential reference to authenticate and connect to the `ldap` server. can be omitted if `authentication-level` is 'none' (anonymous).
* operation: `/subsystem=elytron/dir-context=<dir-context name>:read-attribute(name=credential-reference)`

## syntax of the operation to get the `enable-connection-pooling` attribute of the `dir-context` resource of the `elytron` subsystem
* Retrieve the `enable-connection-pooling` attribute value of the `dir-context` resource of the `elytron` subsystem
* `enable-connection-pooling` attribute description: indicates if connection pooling is enabled.
* operation: `/subsystem=elytron/dir-context=<dir-context name>:read-attribute(name=enable-connection-pooling)`

## syntax of the operation to get the `module` attribute of the `dir-context` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `dir-context` resource of the `elytron` subsystem
* `module` attribute description: name of module that will be used as class loading base.
* operation: `/subsystem=elytron/dir-context=<dir-context name>:read-attribute(name=module)`

## syntax of the operation to get the `principal` attribute of the `dir-context` resource of the `elytron` subsystem
* Retrieve the `principal` attribute value of the `dir-context` resource of the `elytron` subsystem
* `principal` attribute description: the principal to authenticate and connect to the `ldap` server. can be omitted if `authentication-level` is 'none' (anonymous).
* operation: `/subsystem=elytron/dir-context=<dir-context name>:read-attribute(name=principal)`

## syntax of the operation to get the `properties` attribute of the `dir-context` resource of the `elytron` subsystem
* Retrieve the `properties` attribute value of the `dir-context` resource of the `elytron` subsystem
* `properties` attribute description: the additional connection properties for the `dircontext`.
* operation: `/subsystem=elytron/dir-context=<dir-context name>:read-attribute(name=properties)`

## syntax of the operation to get the `read-timeout` attribute of the `dir-context` resource of the `elytron` subsystem
* Retrieve the `read-timeout` attribute value of the `dir-context` resource of the `elytron` subsystem
* `read-timeout` attribute description: the read timeout for an `ldap` operation in milliseconds.
* operation: `/subsystem=elytron/dir-context=<dir-context name>:read-attribute(name=read-timeout)`

## syntax of the operation to get the `referral-mode` attribute of the `dir-context` resource of the `elytron` subsystem
* Retrieve the `referral-mode` attribute value of the `dir-context` resource of the `elytron` subsystem
* `referral-mode` attribute description: if referrals should be followed.
* operation: `/subsystem=elytron/dir-context=<dir-context name>:read-attribute(name=referral-mode)`

## syntax of the operation to get the `ssl-context` attribute of the `dir-context` resource of the `elytron` subsystem
* Retrieve the `ssl-context` attribute value of the `dir-context` resource of the `elytron` subsystem
* `ssl-context` attribute description: the name of `ssl-context` used to secure connection to the `ldap` server.
* operation: `/subsystem=elytron/dir-context=<dir-context name>:read-attribute(name=ssl-context)`

## syntax of the operation to get the `url` attribute of the `dir-context` resource of the `elytron` subsystem
* Retrieve the `url` attribute value of the `dir-context` resource of the `elytron` subsystem
* `url` attribute description: the connection `url`.
* operation: `/subsystem=elytron/dir-context=<dir-context name>:read-attribute(name=url)`

## syntax of the operation to get a `add-suffix-role-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/add-suffix-role-mapper=<add-suffix-role-mapper name>:read-resource()`
* To get the list of all the `add-suffix-role-mapper` resource of the `elytron` subsystem use '*' for `<add-suffix-role-mapper name>`.

## syntax of the operation to get the `suffix` attribute of the `add-suffix-role-mapper` resource of the `elytron` subsystem
* Retrieve the `suffix` attribute value of the `add-suffix-role-mapper` resource of the `elytron` subsystem
* `suffix` attribute description: the suffix to add to each role.
* operation: `/subsystem=elytron/add-suffix-role-mapper=<add-suffix-role-mapper name>:read-attribute(name=suffix)`

## syntax of the operation to get a `configurable-http-server-mechanism-factory` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/configurable-http-server-mechanism-factory=<configurable-http-server-mechanism-factory name>:read-resource()`
* To get the list of all the `configurable-http-server-mechanism-factory` resource of the `elytron` subsystem use '*' for `<configurable-http-server-mechanism-factory name>`.

## syntax of the operation to get the `available-mechanisms` attribute of the `configurable-http-server-mechanism-factory` resource of the `elytron` subsystem
* Retrieve the `available-mechanisms` attribute value of the `configurable-http-server-mechanism-factory` resource of the `elytron` subsystem
* `available-mechanisms` attribute description: the `http` mechanisms available from this factory instance.
* operation: `/subsystem=elytron/configurable-http-server-mechanism-factory=<configurable-http-server-mechanism-factory name>:read-attribute(name=available-mechanisms)`

## syntax of the operation to get the `filters` attribute of the `configurable-http-server-mechanism-factory` resource of the `elytron` subsystem
* Retrieve the `filters` attribute value of the `configurable-http-server-mechanism-factory` resource of the `elytron` subsystem
* `filters` attribute description: filtering to be applied to enable / disable mechanisms based on the name.
* operation: `/subsystem=elytron/configurable-http-server-mechanism-factory=<configurable-http-server-mechanism-factory name>:read-attribute(name=filters)`

## syntax of the operation to get the `http-server-mechanism-factory` attribute of the `configurable-http-server-mechanism-factory` resource of the `elytron` subsystem
* Retrieve the `http-server-mechanism-factory` attribute value of the `configurable-http-server-mechanism-factory` resource of the `elytron` subsystem
* `http-server-mechanism-factory` attribute description: the `http` server factory to be wrapped.
* operation: `/subsystem=elytron/configurable-http-server-mechanism-factory=<configurable-http-server-mechanism-factory name>:read-attribute(name=http-server-mechanism-factory)`

## syntax of the operation to get the `properties` attribute of the `configurable-http-server-mechanism-factory` resource of the `elytron` subsystem
* Retrieve the `properties` attribute value of the `configurable-http-server-mechanism-factory` resource of the `elytron` subsystem
* `properties` attribute description: custom properties to be passed in to the `http` server factory calls.
* operation: `/subsystem=elytron/configurable-http-server-mechanism-factory=<configurable-http-server-mechanism-factory name>:read-attribute(name=properties)`

## syntax of the operation to get a `aggregate-sasl-server-factory` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/aggregate-sasl-server-factory=<aggregate-sasl-server-factory name>:read-resource()`
* To get the list of all the `aggregate-sasl-server-factory` resource of the `elytron` subsystem use '*' for `<aggregate-sasl-server-factory name>`.

## syntax of the operation to get the `available-mechanisms` attribute of the `aggregate-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `available-mechanisms` attribute value of the `aggregate-sasl-server-factory` resource of the `elytron` subsystem
* `available-mechanisms` attribute description: the `sasl` mechanisms available from this factory after all filtering has been applied.
* operation: `/subsystem=elytron/aggregate-sasl-server-factory=<aggregate-sasl-server-factory name>:read-attribute(name=available-mechanisms)`

## syntax of the operation to get the `sasl-server-factories` attribute of the `aggregate-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `sasl-server-factories` attribute value of the `aggregate-sasl-server-factory` resource of the `elytron` subsystem
* `sasl-server-factories` attribute description: the referenced `sasl` server factories to aggregate.
* operation: `/subsystem=elytron/aggregate-sasl-server-factory=<aggregate-sasl-server-factory name>:read-attribute(name=sasl-server-factories)`

## syntax of the operation to get a `custom-principal-transformer` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/custom-principal-transformer=<custom-principal-transformer name>:read-resource()`
* To get the list of all the `custom-principal-transformer` resource of the `elytron` subsystem use '*' for `<custom-principal-transformer name>`.

## syntax of the operation to get the `class-name` attribute of the `custom-principal-transformer` resource of the `elytron` subsystem
* Retrieve the `class-name` attribute value of the `custom-principal-transformer` resource of the `elytron` subsystem
* `class-name` attribute description: the class name of the implementation of the custom principal transformer.
* operation: `/subsystem=elytron/custom-principal-transformer=<custom-principal-transformer name>:read-attribute(name=class-name)`

## syntax of the operation to get the `configuration` attribute of the `custom-principal-transformer` resource of the `elytron` subsystem
* Retrieve the `configuration` attribute value of the `custom-principal-transformer` resource of the `elytron` subsystem
* `configuration` attribute description: the optional `key/value` configuration for the custom principal transformer.
* operation: `/subsystem=elytron/custom-principal-transformer=<custom-principal-transformer name>:read-attribute(name=configuration)`

## syntax of the operation to get the `module` attribute of the `custom-principal-transformer` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `custom-principal-transformer` resource of the `elytron` subsystem
* `module` attribute description: the module to use to load the custom principal transformer.
* operation: `/subsystem=elytron/custom-principal-transformer=<custom-principal-transformer name>:read-attribute(name=module)`

## syntax of the operation to get a `jaspi-configuration` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/jaspi-configuration=<jaspi-configuration name>:read-resource()`
* To get the list of all the `jaspi-configuration` resource of the `elytron` subsystem use '*' for `<jaspi-configuration name>`.

## syntax of the operation to get the `application-context` attribute of the `jaspi-configuration` resource of the `elytron` subsystem
* Retrieve the `application-context` attribute value of the `jaspi-configuration` resource of the `elytron` subsystem
* `application-context` attribute description: the `application-context` this configuration is to be associated with or '*' for all application contexts.
* operation: `/subsystem=elytron/jaspi-configuration=<jaspi-configuration name>:read-attribute(name=application-context)`

## syntax of the operation to get the `description` attribute of the `jaspi-configuration` resource of the `elytron` subsystem
* Retrieve the `description` attribute value of the `jaspi-configuration` resource of the `elytron` subsystem
* `description` attribute description: description of this configuration.
* operation: `/subsystem=elytron/jaspi-configuration=<jaspi-configuration name>:read-attribute(name=description)`

## syntax of the operation to get the `layer` attribute of the `jaspi-configuration` resource of the `elytron` subsystem
* Retrieve the `layer` attribute value of the `jaspi-configuration` resource of the `elytron` subsystem
* `layer` attribute description: the layer this configuration is to be associated with or '*' for all layers.
* operation: `/subsystem=elytron/jaspi-configuration=<jaspi-configuration name>:read-attribute(name=layer)`

## syntax of the operation to get the `server-auth-modules` attribute of the `jaspi-configuration` resource of the `elytron` subsystem
* Retrieve the `server-auth-modules` attribute value of the `jaspi-configuration` resource of the `elytron` subsystem
* `server-auth-modules` attribute description: list of `serverauthmodule` definitions for this configuration.
* operation: `/subsystem=elytron/jaspi-configuration=<jaspi-configuration name>:read-attribute(name=server-auth-modules)`

## syntax of the operation to get a `regex-validating-principal-transformer` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/regex-validating-principal-transformer=<regex-validating-principal-transformer name>:read-resource()`
* To get the list of all the `regex-validating-principal-transformer` resource of the `elytron` subsystem use '*' for `<regex-validating-principal-transformer name>`.

## syntax of the operation to get the `match` attribute of the `regex-validating-principal-transformer` resource of the `elytron` subsystem
* Retrieve the `match` attribute value of the `regex-validating-principal-transformer` resource of the `elytron` subsystem
* `match` attribute description: if set to true, the name must match the given pattern to make validation successful. if set to false, the name must not match the given pattern to make validation successful.
* operation: `/subsystem=elytron/regex-validating-principal-transformer=<regex-validating-principal-transformer name>:read-attribute(name=match)`

## syntax of the operation to get the `pattern` attribute of the `regex-validating-principal-transformer` resource of the `elytron` subsystem
* Retrieve the `pattern` attribute value of the `regex-validating-principal-transformer` resource of the `elytron` subsystem
* `pattern` attribute description: the regular expression to use for the principal transformer.
* operation: `/subsystem=elytron/regex-validating-principal-transformer=<regex-validating-principal-transformer name>:read-attribute(name=pattern)`

## syntax of the operation to get a `distributed-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/distributed-realm=<distributed-realm name>:read-resource()`
* To get the list of all the `distributed-realm` resource of the `elytron` subsystem use '*' for `<distributed-realm name>`.

## syntax of the operation to get the `emit-events` attribute of the `distributed-realm` resource of the `elytron` subsystem
* Retrieve the `emit-events` attribute value of the `distributed-realm` resource of the `elytron` subsystem
* `emit-events` attribute description: whether a `securityevent` signifying realm unavailability should be emitted, applicable only when the `ignore-unavailable-realms` attribute is set to true. the default value is true.
* operation: `/subsystem=elytron/distributed-realm=<distributed-realm name>:read-attribute(name=emit-events)`

## syntax of the operation to get the `ignore-unavailable-realms` attribute of the `distributed-realm` resource of the `elytron` subsystem
* Retrieve the `ignore-unavailable-realms` attribute value of the `distributed-realm` resource of the `elytron` subsystem
* `ignore-unavailable-realms` attribute description: whether subsequent realms should be checked after an unavailable realm is reached. if set to false or not set, when the unavailable realm is reached "`org.wildfly.security.auth.server.realmunavailableexception`" is thrown and the search stops. the default value is false.
* operation: `/subsystem=elytron/distributed-realm=<distributed-realm name>:read-attribute(name=ignore-unavailable-realms)`

## syntax of the operation to get the `realms` attribute of the `distributed-realm` resource of the `elytron` subsystem
* Retrieve the `realms` attribute value of the `distributed-realm` resource of the `elytron` subsystem
* `realms` attribute description: references to one or more security realms for authentication and authorization.
* operation: `/subsystem=elytron/distributed-realm=<distributed-realm name>:read-attribute(name=realms)`

## syntax of the operation to get a `ldap-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/ldap-realm=<ldap-realm name>:read-resource()`
* To get the list of all the `ldap-realm` resource of the `elytron` subsystem use '*' for `<ldap-realm name>`.

## syntax of the operation to get the `allow-blank-password` attribute of the `ldap-realm` resource of the `elytron` subsystem
* Retrieve the `allow-blank-password` attribute value of the `ldap-realm` resource of the `elytron` subsystem
* `allow-blank-password` attribute description: does this realm support blank password direct verification? blank password attempt will be rejected otherwise.
* operation: `/subsystem=elytron/ldap-realm=<ldap-realm name>:read-attribute(name=allow-blank-password)`

## syntax of the operation to get the `dir-context` attribute of the `ldap-realm` resource of the `elytron` subsystem
* Retrieve the `dir-context` attribute value of the `ldap-realm` resource of the `elytron` subsystem
* `dir-context` attribute description: the configuration to connect to a `ldap` server.
* operation: `/subsystem=elytron/ldap-realm=<ldap-realm name>:read-attribute(name=dir-context)`

## syntax of the operation to get the `direct-verification` attribute of the `ldap-realm` resource of the `elytron` subsystem
* Retrieve the `direct-verification` attribute value of the `ldap-realm` resource of the `elytron` subsystem
* `direct-verification` attribute description: does this realm support verification of credentials by directly connecting to `ldap` as the account being authenticated?
* operation: `/subsystem=elytron/ldap-realm=<ldap-realm name>:read-attribute(name=direct-verification)`

## syntax of the operation to get the `hash-charset` attribute of the `ldap-realm` resource of the `elytron` subsystem
* Retrieve the `hash-charset` attribute value of the `ldap-realm` resource of the `elytron` subsystem
* `hash-charset` attribute description: the character set to use when converting the password string to a byte array.
* operation: `/subsystem=elytron/ldap-realm=<ldap-realm name>:read-attribute(name=hash-charset)`

## syntax of the operation to get the `hash-encoding` attribute of the `ldap-realm` resource of the `elytron` subsystem
* Retrieve the `hash-encoding` attribute value of the `ldap-realm` resource of the `elytron` subsystem
* `hash-encoding` attribute description: the string format for the password if it is not stored in plain text.
* operation: `/subsystem=elytron/ldap-realm=<ldap-realm name>:read-attribute(name=hash-encoding)`

## syntax of the operation to get the `identity-mapping` attribute of the `ldap-realm` resource of the `elytron` subsystem
* Retrieve the `identity-mapping` attribute value of the `ldap-realm` resource of the `elytron` subsystem
* `identity-mapping` attribute description: the configuration options that define how principals are mapped to their corresponding entries in the underlying `ldap` server.
* operation: `/subsystem=elytron/ldap-realm=<ldap-realm name>:read-attribute(name=identity-mapping)`

## syntax of the operation to get a `policy` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/policy=<policy name>:read-resource()`
* To get the list of all the `policy` resource of the `elytron` subsystem use '*' for `<policy name>`.

## syntax of the operation to get the `custom-policy` attribute of the `policy` resource of the `elytron` subsystem
* Retrieve the `custom-policy` attribute value of the `policy` resource of the `elytron` subsystem
* `custom-policy` attribute description: a custom policy provider definition.
* operation: `/subsystem=elytron/policy=<policy name>:read-attribute(name=custom-policy)`

## syntax of the operation to get the `default-policy` attribute of the `policy` resource of the `elytron` subsystem
* Retrieve the `default-policy` attribute value of the `policy` resource of the `elytron` subsystem
* `default-policy` attribute description: not used.
* operation: `/subsystem=elytron/policy=<policy name>:read-attribute(name=default-policy)`

## syntax of the operation to get the `jacc-policy` attribute of the `policy` resource of the `elytron` subsystem
* Retrieve the `jacc-policy` attribute value of the `policy` resource of the `elytron` subsystem
* `jacc-policy` attribute description: a policy provider definition that sets up jakarta authorization and related services.
* operation: `/subsystem=elytron/policy=<policy name>:read-attribute(name=jacc-policy)`

## syntax of the operation to get a `provider-loader` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/provider-loader=<provider-loader name>:read-resource()`
* To get the list of all the `provider-loader` resource of the `elytron` subsystem use '*' for `<provider-loader name>`.

## syntax of the operation to get the `argument` attribute of the `provider-loader` resource of the `elytron` subsystem
* Retrieve the `argument` attribute value of the `provider-loader` resource of the `elytron` subsystem
* `argument` attribute description: an argument to be passed into the constructor as the provider is instantiated.
* operation: `/subsystem=elytron/provider-loader=<provider-loader name>:read-attribute(name=argument)`

## syntax of the operation to get the `class-names` attribute of the `provider-loader` resource of the `elytron` subsystem
* Retrieve the `class-names` attribute value of the `provider-loader` resource of the `elytron` subsystem
* `class-names` attribute description: the fully qualified class names of the providers to load, these are loaded after the `service-loader` discovered providers and duplicates will be skipped.
* operation: `/subsystem=elytron/provider-loader=<provider-loader name>:read-attribute(name=class-names)`

## syntax of the operation to get the `configuration` attribute of the `provider-loader` resource of the `elytron` subsystem
* Retrieve the `configuration` attribute value of the `provider-loader` resource of the `elytron` subsystem
* `configuration` attribute description: the `key/value` configuration to be passed to the provider to initialise it.
* operation: `/subsystem=elytron/provider-loader=<provider-loader name>:read-attribute(name=configuration)`

## syntax of the operation to get the `loaded-providers` attribute of the `provider-loader` resource of the `elytron` subsystem
* Retrieve the `loaded-providers` attribute value of the `provider-loader` resource of the `elytron` subsystem
* `loaded-providers` attribute description: the list of providers loaded by this provider loader.
* operation: `/subsystem=elytron/provider-loader=<provider-loader name>:read-attribute(name=loaded-providers)`

## syntax of the operation to get the `module` attribute of the `provider-loader` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `provider-loader` resource of the `elytron` subsystem
* `module` attribute description: the name of the module to load the provider from.
* operation: `/subsystem=elytron/provider-loader=<provider-loader name>:read-attribute(name=module)`

## syntax of the operation to get the `path` attribute of the `provider-loader` resource of the `elytron` subsystem
* Retrieve the `path` attribute value of the `provider-loader` resource of the `elytron` subsystem
* `path` attribute description: the path of the file to use to initialise the providers.
* operation: `/subsystem=elytron/provider-loader=<provider-loader name>:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `provider-loader` resource of the `elytron` subsystem
* Retrieve the `relative-to` attribute value of the `provider-loader` resource of the `elytron` subsystem
* `relative-to` attribute description: the base path of the configuration file.
* operation: `/subsystem=elytron/provider-loader=<provider-loader name>:read-attribute(name=relative-to)`

## syntax of the operation to get a `mechanism-provider-filtering-sasl-server-factory` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/mechanism-provider-filtering-sasl-server-factory=<mechanism-provider-filtering-sasl-server-factory name>:read-resource()`
* To get the list of all the `mechanism-provider-filtering-sasl-server-factory` resource of the `elytron` subsystem use '*' for `<mechanism-provider-filtering-sasl-server-factory name>`.

## syntax of the operation to get the `available-mechanisms` attribute of the `mechanism-provider-filtering-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `available-mechanisms` attribute value of the `mechanism-provider-filtering-sasl-server-factory` resource of the `elytron` subsystem
* `available-mechanisms` attribute description: the `sasl` mechanisms available from this factory after all filtering has been applied.
* operation: `/subsystem=elytron/mechanism-provider-filtering-sasl-server-factory=<mechanism-provider-filtering-sasl-server-factory name>:read-attribute(name=available-mechanisms)`

## syntax of the operation to get the `enabling` attribute of the `mechanism-provider-filtering-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `enabling` attribute value of the `mechanism-provider-filtering-sasl-server-factory` resource of the `elytron` subsystem
* `enabling` attribute description: when set to 'true' no provider loaded mechanisms are enabled unless matched by one of the filters, setting to 'false' has the inverse effect.
* operation: `/subsystem=elytron/mechanism-provider-filtering-sasl-server-factory=<mechanism-provider-filtering-sasl-server-factory name>:read-attribute(name=enabling)`

## syntax of the operation to get the `filters` attribute of the `mechanism-provider-filtering-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `filters` attribute value of the `mechanism-provider-filtering-sasl-server-factory` resource of the `elytron` subsystem
* `filters` attribute description: the filters to apply when comparing the mechanisms from the providers, a filter matches when all of the specified values match the mechanism / provider pair.
* operation: `/subsystem=elytron/mechanism-provider-filtering-sasl-server-factory=<mechanism-provider-filtering-sasl-server-factory name>:read-attribute(name=filters)`

## syntax of the operation to get the `sasl-server-factory` attribute of the `mechanism-provider-filtering-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `sasl-server-factory` attribute value of the `mechanism-provider-filtering-sasl-server-factory` resource of the `elytron` subsystem
* `sasl-server-factory` attribute description: reference to a `sasl` server factory to be wrapped by this definition.
* operation: `/subsystem=elytron/mechanism-provider-filtering-sasl-server-factory=<mechanism-provider-filtering-sasl-server-factory name>:read-attribute(name=sasl-server-factory)`

## syntax of the operation to get a `virtual-security-domain` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/virtual-security-domain=<virtual-security-domain name>:read-resource()`
* To get the list of all the `virtual-security-domain` resource of the `elytron` subsystem use '*' for `<virtual-security-domain name>`.

## syntax of the operation to get the `auth-method` attribute of the `virtual-security-domain` resource of the `elytron` subsystem
* Retrieve the `auth-method` attribute value of the `virtual-security-domain` resource of the `elytron` subsystem
* `auth-method` attribute description: the authentication mechanism that will be used with the virtual security domain. allowed values: '`oidc`', '`mp-jwt`'. the default value is '`oidc`'.
* operation: `/subsystem=elytron/virtual-security-domain=<virtual-security-domain name>:read-attribute(name=auth-method)`

## syntax of the operation to get the `outflow-anonymous` attribute of the `virtual-security-domain` resource of the `elytron` subsystem
* Retrieve the `outflow-anonymous` attribute value of the `virtual-security-domain` resource of the `elytron` subsystem
* `outflow-anonymous` attribute description: when outflowing to a security domain, if outflow is not possible, should the anonymous identity be used? outflow to a security domain might not be possible if the domain does not trust this domain or if the identity being outflowed to a domain does not exist in that domain. outflowing anonymous has the effect of clearing any identity already established for that domain.
* operation: `/subsystem=elytron/virtual-security-domain=<virtual-security-domain name>:read-attribute(name=outflow-anonymous)`

## syntax of the operation to get the `outflow-security-domains` attribute of the `virtual-security-domain` resource of the `elytron` subsystem
* Retrieve the `outflow-security-domains` attribute value of the `virtual-security-domain` resource of the `elytron` subsystem
* `outflow-security-domains` attribute description: the list of security domains that the security identity from this virtual domain should automatically outflow to.
* operation: `/subsystem=elytron/virtual-security-domain=<virtual-security-domain name>:read-attribute(name=outflow-security-domains)`

## syntax of the operation to get a `kerberos-security-factory` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-resource()`
* To get the list of all the `kerberos-security-factory` resource of the `elytron` subsystem use '*' for `<kerberos-security-factory name>`.

## syntax of the operation to get the `debug` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `debug` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `debug` attribute description: should the `jaas` step of obtaining the credential have debug logging enabled.
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=debug)`

## syntax of the operation to get the `fail-cache` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `fail-cache` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `fail-cache` attribute description: amount of seconds before new try to obtain server credential should be done if it has failed last time.
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=fail-cache)`

## syntax of the operation to get the `mechanism-names` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `mechanism-names` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `mechanism-names` attribute description: the mechanism names the credential should be usable with. names will be converted to `oids` and used together with `oids` from `mechanism-oids` attribute.
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=mechanism-names)`

## syntax of the operation to get the `mechanism-oids` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `mechanism-oids` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `mechanism-oids` attribute description: the mechanism `oids` the credential should be usable with. will be used together with `oids` derived from names from `mechanism-names` attribute.
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=mechanism-oids)`

## syntax of the operation to get the `minimum-remaining-lifetime` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `minimum-remaining-lifetime` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `minimum-remaining-lifetime` attribute description: how much lifetime (in seconds) should a cached credential have remaining before it is recreated.
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=minimum-remaining-lifetime)`

## syntax of the operation to get the `obtain-kerberos-ticket` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `obtain-kerberos-ticket` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `obtain-kerberos-ticket` attribute description: should the `kerberosticket` also be obtained and associated with the credential. this is required to be true where credentials are delegated to the server.
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=obtain-kerberos-ticket)`

## syntax of the operation to get the `options` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `options` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `options` attribute description: the `krb5loginmodule` additional options.
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=options)`

## syntax of the operation to get the `path` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `path` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `path` attribute description: the path of the `keytab` to load to obtain the credential.
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=path)`

## syntax of the operation to get the `principal` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `principal` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `principal` attribute description: the principal represented by the `keytab`
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=principal)`

## syntax of the operation to get the `relative-to` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `relative-to` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `relative-to` attribute description: the name of another previously named path, or of one of the standard paths provided by the system. if '`relative-to`' is provided, the value of the 'path' attribute is treated as relative to the path specified by this attribute.
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=relative-to)`

## syntax of the operation to get the `request-lifetime` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `request-lifetime` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `request-lifetime` attribute description: how much lifetime (in seconds) should be requested for newly created credentials.
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=request-lifetime)`

## syntax of the operation to get the `required` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `required` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `required` attribute description: is the `keytab` file with adequate principal required to exist at the time the service starts?
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=required)`

## syntax of the operation to get the `server` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `server` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `server` attribute description: if this for use server side or client side?
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=server)`

## syntax of the operation to get the `wrap-gss-credential` attribute of the `kerberos-security-factory` resource of the `elytron` subsystem
* Retrieve the `wrap-gss-credential` attribute value of the `kerberos-security-factory` resource of the `elytron` subsystem
* `wrap-gss-credential` attribute description: should generated `gss` credentials be wrapped to prevent improper disposal or not?
* operation: `/subsystem=elytron/kerberos-security-factory=<kerberos-security-factory name>:read-attribute(name=wrap-gss-credential)`

## syntax of the operation to get a `authentication-configuration` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-resource()`
* To get the list of all the `authentication-configuration` resource of the `elytron` subsystem use '*' for `<authentication-configuration name>`.

## syntax of the operation to get the `anonymous` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `anonymous` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `anonymous` attribute description: enables anonymous authentication.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=anonymous)`

## syntax of the operation to get the `authentication-name` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `authentication-name` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `authentication-name` attribute description: the authentication name to use.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=authentication-name)`

## syntax of the operation to get the `authorization-name` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `authorization-name` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `authorization-name` attribute description: the authorization name to use.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=authorization-name)`

## syntax of the operation to get the `credential-reference` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `credential-reference` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `credential-reference` attribute description: the reference to credential stored in `credentialstore` under defined alias or clear text password.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=credential-reference)`

## syntax of the operation to get the `extends` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `extends` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `extends` attribute description: a previously defined authentication configuration to extend.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=extends)`

## syntax of the operation to get the `forwarding-mode` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `forwarding-mode` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `forwarding-mode` attribute description: the type of security identity forwarding to use. a mode of 'authentication' forwarding forwards the principal and credential. a mode of 'authorization' forwards the authorization id, allowing for a different authentication identity.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=forwarding-mode)`

## syntax of the operation to get the `host` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `host` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `host` attribute description: the host to use.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=host)`

## syntax of the operation to get the `kerberos-security-factory` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `kerberos-security-factory` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `kerberos-security-factory` attribute description: reference to a `kerberos` security factory used to obtain a `gss` `kerberos` credential
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=kerberos-security-factory)`

## syntax of the operation to get the `mechanism-properties` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `mechanism-properties` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `mechanism-properties` attribute description: configuration properties for the `sasl` authentication mechanism.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=mechanism-properties)`

## syntax of the operation to get the `port` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `port` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `port` attribute description: the port to use.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=port)`

## syntax of the operation to get the `protocol` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `protocol` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `protocol` attribute description: the protocol to use.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=protocol)`

## syntax of the operation to get the `realm` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `realm` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `realm` attribute description: the realm to use.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=realm)`

## syntax of the operation to get the `sasl-mechanism-selector` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `sasl-mechanism-selector` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `sasl-mechanism-selector` attribute description: the `sasl` mechanism selector string.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=sasl-mechanism-selector)`

## syntax of the operation to get the `security-domain` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `security-domain` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `security-domain` attribute description: reference to a security domain to obtain a forwarded identity.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=security-domain)`

## syntax of the operation to get the `webservices` attribute of the `authentication-configuration` resource of the `elytron` subsystem
* Retrieve the `webservices` attribute value of the `authentication-configuration` resource of the `elytron` subsystem
* `webservices` attribute description: web services client configuration definition.
* operation: `/subsystem=elytron/authentication-configuration=<authentication-configuration name>:read-attribute(name=webservices)`

## syntax of the operation to get a `custom-principal-decoder` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/custom-principal-decoder=<custom-principal-decoder name>:read-resource()`
* To get the list of all the `custom-principal-decoder` resource of the `elytron` subsystem use '*' for `<custom-principal-decoder name>`.

## syntax of the operation to get the `class-name` attribute of the `custom-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `class-name` attribute value of the `custom-principal-decoder` resource of the `elytron` subsystem
* `class-name` attribute description: fully qualified class name of the principal decoder
* operation: `/subsystem=elytron/custom-principal-decoder=<custom-principal-decoder name>:read-attribute(name=class-name)`

## syntax of the operation to get the `configuration` attribute of the `custom-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `configuration` attribute value of the `custom-principal-decoder` resource of the `elytron` subsystem
* `configuration` attribute description: the optional `kay/value` configuration for the principal decoder
* operation: `/subsystem=elytron/custom-principal-decoder=<custom-principal-decoder name>:read-attribute(name=configuration)`

## syntax of the operation to get the `module` attribute of the `custom-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `custom-principal-decoder` resource of the `elytron` subsystem
* `module` attribute description: name of the module to use to load the principal decoder
* operation: `/subsystem=elytron/custom-principal-decoder=<custom-principal-decoder name>:read-attribute(name=module)`

## syntax of the operation to get a `identity-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/identity-realm=<identity-realm name>:read-resource()`
* To get the list of all the `identity-realm` resource of the `elytron` subsystem use '*' for `<identity-realm name>`.

## syntax of the operation to get the `attribute-name` attribute of the `identity-realm` resource of the `elytron` subsystem
* Retrieve the `attribute-name` attribute value of the `identity-realm` resource of the `elytron` subsystem
* `attribute-name` attribute description: the name of the attribute associated with this identity.
* operation: `/subsystem=elytron/identity-realm=<identity-realm name>:read-attribute(name=attribute-name)`

## syntax of the operation to get the `attribute-values` attribute of the `identity-realm` resource of the `elytron` subsystem
* Retrieve the `attribute-values` attribute value of the `identity-realm` resource of the `elytron` subsystem
* `attribute-values` attribute description: the values associated with the identity attributes.
* operation: `/subsystem=elytron/identity-realm=<identity-realm name>:read-attribute(name=attribute-values)`

## syntax of the operation to get the `identity` attribute of the `identity-realm` resource of the `elytron` subsystem
* Retrieve the `identity` attribute value of the `identity-realm` resource of the `elytron` subsystem
* `identity` attribute description: the name of the identity available from the security realm.
* operation: `/subsystem=elytron/identity-realm=<identity-realm name>:read-attribute(name=identity)`

## syntax of the operation to get the `default-resolver` attribute of the `encryption` `expression` resource of the `elytron` subsystem
* Retrieve the `default-resolver` attribute value of the `encryption` `expression` resource of the `elytron` subsystem
* `default-resolver` attribute description: the default expression resolver to use if not specified within the expression.  if not set there is no default.
* operation: `/subsystem=elytron/expression=encryption:read-attribute(name=default-resolver)`

## syntax of the operation to get the `prefix` attribute of the `encryption` `expression` resource of the `elytron` subsystem
* Retrieve the `prefix` attribute value of the `encryption` `expression` resource of the `elytron` subsystem
* `prefix` attribute description: the prefix in use for encrypted expressions.
* operation: `/subsystem=elytron/expression=encryption:read-attribute(name=prefix)`

## syntax of the operation to get the `resolvers` attribute of the `encryption` `expression` resource of the `elytron` subsystem
* Retrieve the `resolvers` attribute value of the `encryption` `expression` resource of the `elytron` subsystem
* `resolvers` attribute description: list of expressions resolver definitions.
* operation: `/subsystem=elytron/expression=encryption:read-attribute(name=resolvers)`

## syntax of the operation to get a `jaas-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/jaas-realm=<jaas-realm name>:read-resource()`
* To get the list of all the `jaas-realm` resource of the `elytron` subsystem use '*' for `<jaas-realm name>`.

## syntax of the operation to get the `callback-handler` attribute of the `jaas-realm` resource of the `elytron` subsystem
* Retrieve the `callback-handler` attribute value of the `jaas-realm` resource of the `elytron` subsystem
* `callback-handler` attribute description: callback handler to use with the login context. security property "`auth.login.defaultcallbackhandler`" can be used instead. the default callback handler of the realm will be used if none of these are defined.
* operation: `/subsystem=elytron/jaas-realm=<jaas-realm name>:read-attribute(name=callback-handler)`

## syntax of the operation to get the `entry` attribute of the `jaas-realm` resource of the `elytron` subsystem
* Retrieve the `entry` attribute value of the `jaas-realm` resource of the `elytron` subsystem
* `entry` attribute description: `jaas` configuration file entry name.
* operation: `/subsystem=elytron/jaas-realm=<jaas-realm name>:read-attribute(name=entry)`

## syntax of the operation to get the `module` attribute of the `jaas-realm` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `jaas-realm` resource of the `elytron` subsystem
* `module` attribute description: the module to use to obtain the `classloader` to load the custom `loginmodules` and custom `callbackhandler`. the `classloader` that loads the resource will be used if none defined.
* operation: `/subsystem=elytron/jaas-realm=<jaas-realm name>:read-attribute(name=module)`

## syntax of the operation to get the `path` attribute of the `jaas-realm` resource of the `elytron` subsystem
* Retrieve the `path` attribute value of the `jaas-realm` resource of the `elytron` subsystem
* `path` attribute description: path to the `jaas` login context configuration file. you can also specify the location of the configuration with java system property "`java.security.auth.login.config`" or with java security property "`login.config.url`".
* operation: `/subsystem=elytron/jaas-realm=<jaas-realm name>:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `jaas-realm` resource of the `elytron` subsystem
* Retrieve the `relative-to` attribute value of the `jaas-realm` resource of the `elytron` subsystem
* `relative-to` attribute description: the base path of the `jaas` configuration file.
* operation: `/subsystem=elytron/jaas-realm=<jaas-realm name>:read-attribute(name=relative-to)`

## syntax of the operation to get a `file-audit-log` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/file-audit-log=<file-audit-log name>:read-resource()`
* To get the list of all the `file-audit-log` resource of the `elytron` subsystem use '*' for `<file-audit-log name>`.

## syntax of the operation to get the `autoflush` attribute of the `file-audit-log` resource of the `elytron` subsystem
* Retrieve the `autoflush` attribute value of the `file-audit-log` resource of the `elytron` subsystem
* `autoflush` attribute description: whether every event should be immediately flushed to disk (if undefined will default to the value of synchronized).
* operation: `/subsystem=elytron/file-audit-log=<file-audit-log name>:read-attribute(name=autoflush)`

## syntax of the operation to get the `encoding` attribute of the `file-audit-log` resource of the `elytron` subsystem
* Retrieve the `encoding` attribute value of the `file-audit-log` resource of the `elytron` subsystem
* `encoding` attribute description: the audit file encoding. default is `utf_8`.
* operation: `/subsystem=elytron/file-audit-log=<file-audit-log name>:read-attribute(name=encoding)`

## syntax of the operation to get the `format` attribute of the `file-audit-log` resource of the `elytron` subsystem
* Retrieve the `format` attribute value of the `file-audit-log` resource of the `elytron` subsystem
* `format` attribute description: the format to use to record the audit event.
* operation: `/subsystem=elytron/file-audit-log=<file-audit-log name>:read-attribute(name=format)`

## syntax of the operation to get the `path` attribute of the `file-audit-log` resource of the `elytron` subsystem
* Retrieve the `path` attribute value of the `file-audit-log` resource of the `elytron` subsystem
* `path` attribute description: path of the file to be written.
* operation: `/subsystem=elytron/file-audit-log=<file-audit-log name>:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `file-audit-log` resource of the `elytron` subsystem
* Retrieve the `relative-to` attribute value of the `file-audit-log` resource of the `elytron` subsystem
* `relative-to` attribute description: the relative path to the audit log.
* operation: `/subsystem=elytron/file-audit-log=<file-audit-log name>:read-attribute(name=relative-to)`

## syntax of the operation to get the `synchronized` attribute of the `file-audit-log` resource of the `elytron` subsystem
* Retrieve the `synchronized` attribute value of the `file-audit-log` resource of the `elytron` subsystem
* `synchronized` attribute description: whether every event should be immediately synchronised to disk.
* operation: `/subsystem=elytron/file-audit-log=<file-audit-log name>:read-attribute(name=synchronized)`

## syntax of the operation to get a `custom-realm-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/custom-realm-mapper=<custom-realm-mapper name>:read-resource()`
* To get the list of all the `custom-realm-mapper` resource of the `elytron` subsystem use '*' for `<custom-realm-mapper name>`.

## syntax of the operation to get the `class-name` attribute of the `custom-realm-mapper` resource of the `elytron` subsystem
* Retrieve the `class-name` attribute value of the `custom-realm-mapper` resource of the `elytron` subsystem
* `class-name` attribute description: fully qualified class name of the `realmmapper`
* operation: `/subsystem=elytron/custom-realm-mapper=<custom-realm-mapper name>:read-attribute(name=class-name)`

## syntax of the operation to get the `configuration` attribute of the `custom-realm-mapper` resource of the `elytron` subsystem
* Retrieve the `configuration` attribute value of the `custom-realm-mapper` resource of the `elytron` subsystem
* `configuration` attribute description: the optional `kay/value` configuration for the `realmmapper`
* operation: `/subsystem=elytron/custom-realm-mapper=<custom-realm-mapper name>:read-attribute(name=configuration)`

## syntax of the operation to get the `module` attribute of the `custom-realm-mapper` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `custom-realm-mapper` resource of the `elytron` subsystem
* `module` attribute description: name of the module to use to load the `realmmapper`
* operation: `/subsystem=elytron/custom-realm-mapper=<custom-realm-mapper name>:read-attribute(name=module)`

## syntax of the operation to get a `aggregate-role-decoder` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/aggregate-role-decoder=<aggregate-role-decoder name>:read-resource()`
* To get the list of all the `aggregate-role-decoder` resource of the `elytron` subsystem use '*' for `<aggregate-role-decoder name>`.

## syntax of the operation to get the `role-decoders` attribute of the `aggregate-role-decoder` resource of the `elytron` subsystem
* Retrieve the `role-decoders` attribute value of the `aggregate-role-decoder` resource of the `elytron` subsystem
* `role-decoders` attribute description: the referenced role decoders to aggregate.
* operation: `/subsystem=elytron/aggregate-role-decoder=<aggregate-role-decoder name>:read-attribute(name=role-decoders)`

## syntax of the operation to get a `trust-manager` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/trust-manager=<trust-manager name>:read-resource()`
* To get the list of all the `trust-manager` resource of the `elytron` subsystem use '*' for `<trust-manager name>`.

## syntax of the operation to get the `algorithm` attribute of the `trust-manager` resource of the `elytron` subsystem
* Retrieve the `algorithm` attribute value of the `trust-manager` resource of the `elytron` subsystem
* `algorithm` attribute description: the name of the algorithm to use to create the underlying `trustmanagerfactory`.
* operation: `/subsystem=elytron/trust-manager=<trust-manager name>:read-attribute(name=algorithm)`

## syntax of the operation to get the `alias-filter` attribute of the `trust-manager` resource of the `elytron` subsystem
* Retrieve the `alias-filter` attribute value of the `trust-manager` resource of the `elytron` subsystem
* `alias-filter` attribute description: a filter to apply to the aliases returned from the `keystore`, can either be a comma separated list of aliases to return or one of the following formats `all:-alias1:-alias2`, `none:+alias1:+alias2`
* operation: `/subsystem=elytron/trust-manager=<trust-manager name>:read-attribute(name=alias-filter)`

## syntax of the operation to get the `certificate-revocation-list` attribute of the `trust-manager` resource of the `elytron` subsystem
* Retrieve the `certificate-revocation-list` attribute value of the `trust-manager` resource of the `elytron` subsystem
* `certificate-revocation-list` attribute description: enables certificate revocation list checks to a trust manager.
* operation: `/subsystem=elytron/trust-manager=<trust-manager name>:read-attribute(name=certificate-revocation-list)`

## syntax of the operation to get the `certificate-revocation-lists` attribute of the `trust-manager` resource of the `elytron` subsystem
* Retrieve the `certificate-revocation-lists` attribute value of the `trust-manager` resource of the `elytron` subsystem
* `certificate-revocation-lists` attribute description: enables certificate revocation list checks to a trust manager using multiple certificate revocation lists.
* operation: `/subsystem=elytron/trust-manager=<trust-manager name>:read-attribute(name=certificate-revocation-lists)`

## syntax of the operation to get the `key-store` attribute of the `trust-manager` resource of the `elytron` subsystem
* Retrieve the `key-store` attribute value of the `trust-manager` resource of the `elytron` subsystem
* `key-store` attribute description: reference to the `keystore` to use to initialise the underlying `trustmanagerfactory`.
* operation: `/subsystem=elytron/trust-manager=<trust-manager name>:read-attribute(name=key-store)`

## syntax of the operation to get the `maximum-cert-path` attribute of the `trust-manager` resource of the `elytron` subsystem
* Retrieve the `maximum-cert-path` attribute value of the `trust-manager` resource of the `elytron` subsystem
* `maximum-cert-path` attribute description: the maximum number of `non-self-issued` intermediate certificates that may exist in a certification path for `ocsp` and crl checks. if neither `ocsp` and crl is configured, this attribute has no effect.
* operation: `/subsystem=elytron/trust-manager=<trust-manager name>:read-attribute(name=maximum-cert-path)`

## syntax of the operation to get the `ocsp` attribute of the `trust-manager` resource of the `elytron` subsystem
* Retrieve the `ocsp` attribute value of the `trust-manager` resource of the `elytron` subsystem
* `ocsp` attribute description: enables online certificate status protocol checks to a trust manager.
* operation: `/subsystem=elytron/trust-manager=<trust-manager name>:read-attribute(name=ocsp)`

## syntax of the operation to get the `only-leaf-cert` attribute of the `trust-manager` resource of the `elytron` subsystem
* Retrieve the `only-leaf-cert` attribute value of the `trust-manager` resource of the `elytron` subsystem
* `only-leaf-cert` attribute description: whether only leaf certificate should be checked for revocation status.
* operation: `/subsystem=elytron/trust-manager=<trust-manager name>:read-attribute(name=only-leaf-cert)`

## syntax of the operation to get the `provider-name` attribute of the `trust-manager` resource of the `elytron` subsystem
* Retrieve the `provider-name` attribute value of the `trust-manager` resource of the `elytron` subsystem
* `provider-name` attribute description: the name of the provider to use to create the underlying `trustmanagerfactory`.
* operation: `/subsystem=elytron/trust-manager=<trust-manager name>:read-attribute(name=provider-name)`

## syntax of the operation to get the `providers` attribute of the `trust-manager` resource of the `elytron` subsystem
* Retrieve the `providers` attribute value of the `trust-manager` resource of the `elytron` subsystem
* `providers` attribute description: reference to obtain the provider[] to use when creating the underlying `trustmanagerfactory`.
* operation: `/subsystem=elytron/trust-manager=<trust-manager name>:read-attribute(name=providers)`

## syntax of the operation to get the `soft-fail` attribute of the `trust-manager` resource of the `elytron` subsystem
* Retrieve the `soft-fail` attribute value of the `trust-manager` resource of the `elytron` subsystem
* `soft-fail` attribute description: whether a certificate with unknown `ocsp` response should be accepted.
* operation: `/subsystem=elytron/trust-manager=<trust-manager name>:read-attribute(name=soft-fail)`

## syntax of the operation to get a `provider-sasl-server-factory` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/provider-sasl-server-factory=<provider-sasl-server-factory name>:read-resource()`
* To get the list of all the `provider-sasl-server-factory` resource of the `elytron` subsystem use '*' for `<provider-sasl-server-factory name>`.

## syntax of the operation to get the `available-mechanisms` attribute of the `provider-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `available-mechanisms` attribute value of the `provider-sasl-server-factory` resource of the `elytron` subsystem
* `available-mechanisms` attribute description: the `sasl` mechanisms available from this factory after all filtering has been applied.
* operation: `/subsystem=elytron/provider-sasl-server-factory=<provider-sasl-server-factory name>:read-attribute(name=available-mechanisms)`

## syntax of the operation to get the `providers` attribute of the `provider-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `providers` attribute value of the `provider-sasl-server-factory` resource of the `elytron` subsystem
* `providers` attribute description: the providers to use to locate the factories, if not specified the globally registered list of providers will be used.
* operation: `/subsystem=elytron/provider-sasl-server-factory=<provider-sasl-server-factory name>:read-attribute(name=providers)`

## syntax of the operation to get a `jdbc-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/jdbc-realm=<jdbc-realm name>:read-resource()`
* To get the list of all the `jdbc-realm` resource of the `elytron` subsystem use '*' for `<jdbc-realm name>`.

## syntax of the operation to get the `hash-charset` attribute of the `jdbc-realm` resource of the `elytron` subsystem
* Retrieve the `hash-charset` attribute value of the `jdbc-realm` resource of the `elytron` subsystem
* `hash-charset` attribute description: the character set to use when converting the password string to a byte array.
* operation: `/subsystem=elytron/jdbc-realm=<jdbc-realm name>:read-attribute(name=hash-charset)`

## syntax of the operation to get the `principal-query` attribute of the `jdbc-realm` resource of the `elytron` subsystem
* Retrieve the `principal-query` attribute value of the `jdbc-realm` resource of the `elytron` subsystem
* `principal-query` attribute description: the authentication query used to authenticate users based on specific key types.
* operation: `/subsystem=elytron/jdbc-realm=<jdbc-realm name>:read-attribute(name=principal-query)`

## syntax of the operation to get a `key-store-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/key-store-realm=<key-store-realm name>:read-resource()`
* To get the list of all the `key-store-realm` resource of the `elytron` subsystem use '*' for `<key-store-realm name>`.

## syntax of the operation to get the `key-store` attribute of the `key-store-realm` resource of the `elytron` subsystem
* Retrieve the `key-store` attribute value of the `key-store-realm` resource of the `elytron` subsystem
* `key-store` attribute description: reference to the `keystore` that should be used to back this security realm.
* operation: `/subsystem=elytron/key-store-realm=<key-store-realm name>:read-attribute(name=key-store)`

## syntax of the operation to get a `security-domain` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-resource()`
* To get the list of all the `security-domain` resource of the `elytron` subsystem use '*' for `<security-domain name>`.

## syntax of the operation to get the `default-realm` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `default-realm` attribute value of the `security-domain` resource of the `elytron` subsystem
* `default-realm` attribute description: the default realm contained by this security domain.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=default-realm)`

## syntax of the operation to get the `evidence-decoder` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `evidence-decoder` attribute value of the `security-domain` resource of the `elytron` subsystem
* `evidence-decoder` attribute description: a reference to an `evidencedecoder` to be used by this domain.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=evidence-decoder)`

## syntax of the operation to get the `outflow-anonymous` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `outflow-anonymous` attribute value of the `security-domain` resource of the `elytron` subsystem
* `outflow-anonymous` attribute description: when outflowing to a security domain if outflow is not possible should the anonymous identity be used?  outflowing anonymous has the effect of clearing any identity already established for that domain.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=outflow-anonymous)`

## syntax of the operation to get the `outflow-security-domains` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `outflow-security-domains` attribute value of the `security-domain` resource of the `elytron` subsystem
* `outflow-security-domains` attribute description: the list of security domains that the security identity from this domain should automatically outflow to.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=outflow-security-domains)`

## syntax of the operation to get the `permission-mapper` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `permission-mapper` attribute value of the `security-domain` resource of the `elytron` subsystem
* `permission-mapper` attribute description: a reference to a `permissionmapper` to be used by this domain.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=permission-mapper)`

## syntax of the operation to get the `post-realm-principal-transformer` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `post-realm-principal-transformer` attribute value of the `security-domain` resource of the `elytron` subsystem
* `post-realm-principal-transformer` attribute description: a reference to a principal transformer to be applied after the realm has operated on the supplied identity name.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=post-realm-principal-transformer)`

## syntax of the operation to get the `pre-realm-principal-transformer` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `pre-realm-principal-transformer` attribute value of the `security-domain` resource of the `elytron` subsystem
* `pre-realm-principal-transformer` attribute description: a reference to a principal transformer to be applied before the realm is selected.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=pre-realm-principal-transformer)`

## syntax of the operation to get the `principal-decoder` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `principal-decoder` attribute value of the `security-domain` resource of the `elytron` subsystem
* `principal-decoder` attribute description: a reference to a `principaldecoder` to be used by this domain.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=principal-decoder)`

## syntax of the operation to get the `realm-mapper` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `realm-mapper` attribute value of the `security-domain` resource of the `elytron` subsystem
* `realm-mapper` attribute description: reference to the `realmmapper` to be used by this domain.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=realm-mapper)`

## syntax of the operation to get the `realms` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `realms` attribute value of the `security-domain` resource of the `elytron` subsystem
* `realms` attribute description: the list of realms contained by this security domain.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=realms)`

## syntax of the operation to get the `role-decoder` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `role-decoder` attribute value of the `security-domain` resource of the `elytron` subsystem
* `role-decoder` attribute description: reference to the `roledecoder` to be used by this domain.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=role-decoder)`

## syntax of the operation to get the `role-mapper` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `role-mapper` attribute value of the `security-domain` resource of the `elytron` subsystem
* `role-mapper` attribute description: reference to the `rolemapper` to be used by this domain.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=role-mapper)`

## syntax of the operation to get the `security-event-listener` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `security-event-listener` attribute value of the `security-domain` resource of the `elytron` subsystem
* `security-event-listener` attribute description: reference to a listener for security events.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=security-event-listener)`

## syntax of the operation to get the `trusted-security-domains` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `trusted-security-domains` attribute value of the `security-domain` resource of the `elytron` subsystem
* `trusted-security-domains` attribute description: the list of security domains that are trusted by this security domain.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=trusted-security-domains)`

## syntax of the operation to get the `trusted-virtual-security-domains` attribute of the `security-domain` resource of the `elytron` subsystem
* Retrieve the `trusted-virtual-security-domains` attribute value of the `security-domain` resource of the `elytron` subsystem
* `trusted-virtual-security-domains` attribute description: the list of virtual security domains that are trusted by this security domain.
* operation: `/subsystem=elytron/security-domain=<security-domain name>:read-attribute(name=trusted-virtual-security-domains)`

## syntax of the operation to get a `constant-realm-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/constant-realm-mapper=<constant-realm-mapper name>:read-resource()`
* To get the list of all the `constant-realm-mapper` resource of the `elytron` subsystem use '*' for `<constant-realm-mapper name>`.

## syntax of the operation to get the `realm-name` attribute of the `constant-realm-mapper` resource of the `elytron` subsystem
* Retrieve the `realm-name` attribute value of the `constant-realm-mapper` resource of the `elytron` subsystem
* `realm-name` attribute description: the name of the constant realm to return.
* operation: `/subsystem=elytron/constant-realm-mapper=<constant-realm-mapper name>:read-attribute(name=realm-name)`

## syntax of the operation to get a `source-address-role-decoder` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/source-address-role-decoder=<source-address-role-decoder name>:read-resource()`
* To get the list of all the `source-address-role-decoder` resource of the `elytron` subsystem use '*' for `<source-address-role-decoder name>`.

## syntax of the operation to get the `pattern` attribute of the `source-address-role-decoder` resource of the `elytron` subsystem
* Retrieve the `pattern` attribute value of the `source-address-role-decoder` resource of the `elytron` subsystem
* `pattern` attribute description: a regular expression that specifies the `ip` address to match.
* operation: `/subsystem=elytron/source-address-role-decoder=<source-address-role-decoder name>:read-attribute(name=pattern)`

## syntax of the operation to get the `roles` attribute of the `source-address-role-decoder` resource of the `elytron` subsystem
* Retrieve the `roles` attribute value of the `source-address-role-decoder` resource of the `elytron` subsystem
* `roles` attribute description: the list of roles to assign if the `ip` address of the remote client matches.
* operation: `/subsystem=elytron/source-address-role-decoder=<source-address-role-decoder name>:read-attribute(name=roles)`

## syntax of the operation to get the `source-address` attribute of the `source-address-role-decoder` resource of the `elytron` subsystem
* Retrieve the `source-address` attribute value of the `source-address-role-decoder` resource of the `elytron` subsystem
* `source-address` attribute description: the `ip` address to match.
* operation: `/subsystem=elytron/source-address-role-decoder=<source-address-role-decoder name>:read-attribute(name=source-address)`

## syntax of the operation to get a `aggregate-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/aggregate-realm=<aggregate-realm name>:read-resource()`
* To get the list of all the `aggregate-realm` resource of the `elytron` subsystem use '*' for `<aggregate-realm name>`.

## syntax of the operation to get the `authentication-realm` attribute of the `aggregate-realm` resource of the `elytron` subsystem
* Retrieve the `authentication-realm` attribute value of the `aggregate-realm` resource of the `elytron` subsystem
* `authentication-realm` attribute description: reference to the security realm to use for authentication steps (obtaining or validating credentials).
* operation: `/subsystem=elytron/aggregate-realm=<aggregate-realm name>:read-attribute(name=authentication-realm)`

## syntax of the operation to get the `authorization-realm` attribute of the `aggregate-realm` resource of the `elytron` subsystem
* Retrieve the `authorization-realm` attribute value of the `aggregate-realm` resource of the `elytron` subsystem
* `authorization-realm` attribute description: reference to the security realm to use for loading the identity for authorization steps (loading of the identity).
* operation: `/subsystem=elytron/aggregate-realm=<aggregate-realm name>:read-attribute(name=authorization-realm)`

## syntax of the operation to get the `authorization-realms` attribute of the `aggregate-realm` resource of the `elytron` subsystem
* Retrieve the `authorization-realms` attribute value of the `aggregate-realm` resource of the `elytron` subsystem
* `authorization-realms` attribute description: reference to one or more security realms to use for loading the identity for authorization steps and aggregating the attributes (loading of the identity).
* operation: `/subsystem=elytron/aggregate-realm=<aggregate-realm name>:read-attribute(name=authorization-realms)`

## syntax of the operation to get the `principal-transformer` attribute of the `aggregate-realm` resource of the `elytron` subsystem
* Retrieve the `principal-transformer` attribute value of the `aggregate-realm` resource of the `elytron` subsystem
* `principal-transformer` attribute description: reference to a principal transformer to apply between loading the identity for authentication and loading the identity for authorization
* operation: `/subsystem=elytron/aggregate-realm=<aggregate-realm name>:read-attribute(name=principal-transformer)`

## syntax of the operation to get a `aggregate-providers` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/aggregate-providers=<aggregate-providers name>:read-resource()`
* To get the list of all the `aggregate-providers` resource of the `elytron` subsystem use '*' for `<aggregate-providers name>`.

## syntax of the operation to get the `providers` attribute of the `aggregate-providers` resource of the `elytron` subsystem
* Retrieve the `providers` attribute value of the `aggregate-providers` resource of the `elytron` subsystem
* `providers` attribute description: the referenced provider[] resources to aggregate.
* operation: `/subsystem=elytron/aggregate-providers=<aggregate-providers name>:read-attribute(name=providers)`

## syntax of the operation to get a `logical-permission-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/logical-permission-mapper=<logical-permission-mapper name>:read-resource()`
* To get the list of all the `logical-permission-mapper` resource of the `elytron` subsystem use '*' for `<logical-permission-mapper name>`.

## syntax of the operation to get the `left` attribute of the `logical-permission-mapper` resource of the `elytron` subsystem
* Retrieve the `left` attribute value of the `logical-permission-mapper` resource of the `elytron` subsystem
* `left` attribute description: reference to the permission mapper to use to the left of the operation.
* operation: `/subsystem=elytron/logical-permission-mapper=<logical-permission-mapper name>:read-attribute(name=left)`

## syntax of the operation to get the `logical-operation` attribute of the `logical-permission-mapper` resource of the `elytron` subsystem
* Retrieve the `logical-operation` attribute value of the `logical-permission-mapper` resource of the `elytron` subsystem
* `logical-operation` attribute description: the logical operation to use to combine the permission mappers.
* operation: `/subsystem=elytron/logical-permission-mapper=<logical-permission-mapper name>:read-attribute(name=logical-operation)`

## syntax of the operation to get the `right` attribute of the `logical-permission-mapper` resource of the `elytron` subsystem
* Retrieve the `right` attribute value of the `logical-permission-mapper` resource of the `elytron` subsystem
* `right` attribute description: reference to the permission mapper to use to the right of the operation.
* operation: `/subsystem=elytron/logical-permission-mapper=<logical-permission-mapper name>:read-attribute(name=right)`

## syntax of the operation to get a `constant-principal-transformer` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/constant-principal-transformer=<constant-principal-transformer name>:read-resource()`
* To get the list of all the `constant-principal-transformer` resource of the `elytron` subsystem use '*' for `<constant-principal-transformer name>`.

## syntax of the operation to get the `constant` attribute of the `constant-principal-transformer` resource of the `elytron` subsystem
* Retrieve the `constant` attribute value of the `constant-principal-transformer` resource of the `elytron` subsystem
* `constant` attribute description: the constant value this `principaltransformer` will always return.
* operation: `/subsystem=elytron/constant-principal-transformer=<constant-principal-transformer name>:read-attribute(name=constant)`

## syntax of the operation to get a `dynamic-client-ssl-context` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/dynamic-client-ssl-context=<dynamic-client-ssl-context name>:read-resource()`
* To get the list of all the `dynamic-client-ssl-context` resource of the `elytron` subsystem use '*' for `<dynamic-client-ssl-context name>`.

## syntax of the operation to get the `authentication-context` attribute of the `dynamic-client-ssl-context` resource of the `elytron` subsystem
* Retrieve the `authentication-context` attribute value of the `dynamic-client-ssl-context` resource of the `elytron` subsystem
* `authentication-context` attribute description: the authentication context that will be queried for `ssl` context based on peer information.
* operation: `/subsystem=elytron/dynamic-client-ssl-context=<dynamic-client-ssl-context name>:read-attribute(name=authentication-context)`

## syntax of the operation to get a `provider-http-server-mechanism-factory` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/provider-http-server-mechanism-factory=<provider-http-server-mechanism-factory name>:read-resource()`
* To get the list of all the `provider-http-server-mechanism-factory` resource of the `elytron` subsystem use '*' for `<provider-http-server-mechanism-factory name>`.

## syntax of the operation to get the `available-mechanisms` attribute of the `provider-http-server-mechanism-factory` resource of the `elytron` subsystem
* Retrieve the `available-mechanisms` attribute value of the `provider-http-server-mechanism-factory` resource of the `elytron` subsystem
* `available-mechanisms` attribute description: the `http` mechanisms available from this factory instance.
* operation: `/subsystem=elytron/provider-http-server-mechanism-factory=<provider-http-server-mechanism-factory name>:read-attribute(name=available-mechanisms)`

## syntax of the operation to get the `providers` attribute of the `provider-http-server-mechanism-factory` resource of the `elytron` subsystem
* Retrieve the `providers` attribute value of the `provider-http-server-mechanism-factory` resource of the `elytron` subsystem
* `providers` attribute description: the providers to use to locate the factories, if not specified the globally registered list of providers will be used.
* operation: `/subsystem=elytron/provider-http-server-mechanism-factory=<provider-http-server-mechanism-factory name>:read-attribute(name=providers)`

## syntax of the operation to get a `constant-role-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/constant-role-mapper=<constant-role-mapper name>:read-resource()`
* To get the list of all the `constant-role-mapper` resource of the `elytron` subsystem use '*' for `<constant-role-mapper name>`.

## syntax of the operation to get the `roles` attribute of the `constant-role-mapper` resource of the `elytron` subsystem
* Retrieve the `roles` attribute value of the `constant-role-mapper` resource of the `elytron` subsystem
* `roles` attribute description: the constant roles to be returned by this role mapper.
* operation: `/subsystem=elytron/constant-role-mapper=<constant-role-mapper name>:read-attribute(name=roles)`

## syntax of the operation to get a `service-loader-sasl-server-factory` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/service-loader-sasl-server-factory=<service-loader-sasl-server-factory name>:read-resource()`
* To get the list of all the `service-loader-sasl-server-factory` resource of the `elytron` subsystem use '*' for `<service-loader-sasl-server-factory name>`.

## syntax of the operation to get the `available-mechanisms` attribute of the `service-loader-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `available-mechanisms` attribute value of the `service-loader-sasl-server-factory` resource of the `elytron` subsystem
* `available-mechanisms` attribute description: the `sasl` mechanisms available from this factory after all filtering has been applied.
* operation: `/subsystem=elytron/service-loader-sasl-server-factory=<service-loader-sasl-server-factory name>:read-attribute(name=available-mechanisms)`

## syntax of the operation to get the `module` attribute of the `service-loader-sasl-server-factory` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `service-loader-sasl-server-factory` resource of the `elytron` subsystem
* `module` attribute description: the module to use to obtain the `classloader` to load the factories, if not specified the `classloader` to load the resource will be used instead.
* operation: `/subsystem=elytron/service-loader-sasl-server-factory=<service-loader-sasl-server-factory name>:read-attribute(name=module)`

## syntax of the operation to get a `caching-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/caching-realm=<caching-realm name>:read-resource()`
* To get the list of all the `caching-realm` resource of the `elytron` subsystem use '*' for `<caching-realm name>`.

## syntax of the operation to get the `maximum-age` attribute of the `caching-realm` resource of the `elytron` subsystem
* Retrieve the `maximum-age` attribute value of the `caching-realm` resource of the `elytron` subsystem
* `maximum-age` attribute description: the time in milliseconds that an item can stay in the cache.
* operation: `/subsystem=elytron/caching-realm=<caching-realm name>:read-attribute(name=maximum-age)`

## syntax of the operation to get the `maximum-entries` attribute of the `caching-realm` resource of the `elytron` subsystem
* Retrieve the `maximum-entries` attribute value of the `caching-realm` resource of the `elytron` subsystem
* `maximum-entries` attribute description: the maximum number of entries to keep in the cache.
* operation: `/subsystem=elytron/caching-realm=<caching-realm name>:read-attribute(name=maximum-entries)`

## syntax of the operation to get the `realm` attribute of the `caching-realm` resource of the `elytron` subsystem
* Retrieve the `realm` attribute value of the `caching-realm` resource of the `elytron` subsystem
* `realm` attribute description: a reference to a `cacheable` security realm.
* operation: `/subsystem=elytron/caching-realm=<caching-realm name>:read-attribute(name=realm)`

## syntax of the operation to get a `concatenating-principal-decoder` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/concatenating-principal-decoder=<concatenating-principal-decoder name>:read-resource()`
* To get the list of all the `concatenating-principal-decoder` resource of the `elytron` subsystem use '*' for `<concatenating-principal-decoder name>`.

## syntax of the operation to get the `joiner` attribute of the `concatenating-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `joiner` attribute value of the `concatenating-principal-decoder` resource of the `elytron` subsystem
* `joiner` attribute description: the string to use to join the results of the referenced principal decoders.
* operation: `/subsystem=elytron/concatenating-principal-decoder=<concatenating-principal-decoder name>:read-attribute(name=joiner)`

## syntax of the operation to get the `principal-decoders` attribute of the `concatenating-principal-decoder` resource of the `elytron` subsystem
* Retrieve the `principal-decoders` attribute value of the `concatenating-principal-decoder` resource of the `elytron` subsystem
* `principal-decoders` attribute description: the referenced principal decoders to concatenate.
* operation: `/subsystem=elytron/concatenating-principal-decoder=<concatenating-principal-decoder name>:read-attribute(name=principal-decoders)`

## syntax of the operation to get a `credential-store` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/credential-store=<credential-store name>:read-resource()`
* To get the list of all the `credential-store` resource of the `elytron` subsystem use '*' for `<credential-store name>`.

## syntax of the operation to get the `create` attribute of the `credential-store` resource of the `elytron` subsystem
* Retrieve the `create` attribute value of the `credential-store` resource of the `elytron` subsystem
* `create` attribute description: specifies whether credential store should create storage when it `doesn't` exist.
* operation: `/subsystem=elytron/credential-store=<credential-store name>:read-attribute(name=create)`

## syntax of the operation to get the `credential-reference` attribute of the `credential-store` resource of the `elytron` subsystem
* Retrieve the `credential-reference` attribute value of the `credential-store` resource of the `elytron` subsystem
* `credential-reference` attribute description: credential reference to be used to create protection parameter.
* operation: `/subsystem=elytron/credential-store=<credential-store name>:read-attribute(name=credential-reference)`

## syntax of the operation to get the `implementation-properties` attribute of the `credential-store` resource of the `elytron` subsystem
* Retrieve the `implementation-properties` attribute value of the `credential-store` resource of the `elytron` subsystem
* `implementation-properties` attribute description: map of credentials store implementation specific properties.
* operation: `/subsystem=elytron/credential-store=<credential-store name>:read-attribute(name=implementation-properties)`

## syntax of the operation to get the `location` attribute of the `credential-store` resource of the `elytron` subsystem
* Retrieve the `location` attribute value of the `credential-store` resource of the `elytron` subsystem
* `location` attribute description: file name of credential store storage.
* operation: `/subsystem=elytron/credential-store=<credential-store name>:read-attribute(name=location)`

## syntax of the operation to get the `modifiable` attribute of the `credential-store` resource of the `elytron` subsystem
* Retrieve the `modifiable` attribute value of the `credential-store` resource of the `elytron` subsystem
* `modifiable` attribute description: specifies whether credential store is modifiable.
* operation: `/subsystem=elytron/credential-store=<credential-store name>:read-attribute(name=modifiable)`

## syntax of the operation to get the `other-providers` attribute of the `credential-store` resource of the `elytron` subsystem
* Retrieve the `other-providers` attribute value of the `credential-store` resource of the `elytron` subsystem
* `other-providers` attribute description: the name of the providers defined within the subsystem to obtain the providers to search for the one that can create the required jakarta connectors objects within credential store. this is valid only for `key-store` based `credentialstore`. if this is not specified then the global list of providers is used instead.
* operation: `/subsystem=elytron/credential-store=<credential-store name>:read-attribute(name=other-providers)`

## syntax of the operation to get the `path` attribute of the `credential-store` resource of the `elytron` subsystem
* Retrieve the `path` attribute value of the `credential-store` resource of the `elytron` subsystem
* `path` attribute description: file name of credential store storage.
* operation: `/subsystem=elytron/credential-store=<credential-store name>:read-attribute(name=path)`

## syntax of the operation to get the `provider-name` attribute of the `credential-store` resource of the `elytron` subsystem
* Retrieve the `provider-name` attribute value of the `credential-store` resource of the `elytron` subsystem
* `provider-name` attribute description: the name of the provider to use to instantiate the `credentialstorespi`. if the provider is not specified then the first provider found that can create an instance of the specified 'type' will be used.
* operation: `/subsystem=elytron/credential-store=<credential-store name>:read-attribute(name=provider-name)`

## syntax of the operation to get the `providers` attribute of the `credential-store` resource of the `elytron` subsystem
* Retrieve the `providers` attribute value of the `credential-store` resource of the `elytron` subsystem
* `providers` attribute description: the name of the providers defined within the subsystem to obtain the providers to search for the one that can create the required `credentialstore` type. if this is not specified then the global list of providers is used instead.
* operation: `/subsystem=elytron/credential-store=<credential-store name>:read-attribute(name=providers)`

## syntax of the operation to get the `relative-to` attribute of the `credential-store` resource of the `elytron` subsystem
* Retrieve the `relative-to` attribute value of the `credential-store` resource of the `elytron` subsystem
* `relative-to` attribute description: a reference to a previously defined path that the file name is relative to.
* operation: `/subsystem=elytron/credential-store=<credential-store name>:read-attribute(name=relative-to)`

## syntax of the operation to get the `state` attribute of the `credential-store` resource of the `elytron` subsystem
* Retrieve the `state` attribute value of the `credential-store` resource of the `elytron` subsystem
* `state` attribute description: the state of the underlying service that represents this credential store at runtime.
* operation: `/subsystem=elytron/credential-store=<credential-store name>:read-attribute(name=state)`

## syntax of the operation to get the `type` attribute of the `credential-store` resource of the `elytron` subsystem
* Retrieve the `type` attribute value of the `credential-store` resource of the `elytron` subsystem
* `type` attribute description: the credential store type, `e.g`. `keystorecredentialstore`.
* operation: `/subsystem=elytron/credential-store=<credential-store name>:read-attribute(name=type)`

## syntax of the operation to get a `custom-modifiable-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/custom-modifiable-realm=<custom-modifiable-realm name>:read-resource()`
* To get the list of all the `custom-modifiable-realm` resource of the `elytron` subsystem use '*' for `<custom-modifiable-realm name>`.

## syntax of the operation to get the `class-name` attribute of the `custom-modifiable-realm` resource of the `elytron` subsystem
* Retrieve the `class-name` attribute value of the `custom-modifiable-realm` resource of the `elytron` subsystem
* `class-name` attribute description: the class name of the implementation of the custom realm.
* operation: `/subsystem=elytron/custom-modifiable-realm=<custom-modifiable-realm name>:read-attribute(name=class-name)`

## syntax of the operation to get the `configuration` attribute of the `custom-modifiable-realm` resource of the `elytron` subsystem
* Retrieve the `configuration` attribute value of the `custom-modifiable-realm` resource of the `elytron` subsystem
* `configuration` attribute description: the optional `key/value` configuration for the custom realm.
* operation: `/subsystem=elytron/custom-modifiable-realm=<custom-modifiable-realm name>:read-attribute(name=configuration)`

## syntax of the operation to get the `module` attribute of the `custom-modifiable-realm` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `custom-modifiable-realm` resource of the `elytron` subsystem
* `module` attribute description: the module to use to load the custom realm.
* operation: `/subsystem=elytron/custom-modifiable-realm=<custom-modifiable-realm name>:read-attribute(name=module)`

## syntax of the operation to get a `custom-credential-security-factory` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/custom-credential-security-factory=<custom-credential-security-factory name>:read-resource()`
* To get the list of all the `custom-credential-security-factory` resource of the `elytron` subsystem use '*' for `<custom-credential-security-factory name>`.

## syntax of the operation to get the `class-name` attribute of the `custom-credential-security-factory` resource of the `elytron` subsystem
* Retrieve the `class-name` attribute value of the `custom-credential-security-factory` resource of the `elytron` subsystem
* `class-name` attribute description: the class name of the implementation of the custom security factory.
* operation: `/subsystem=elytron/custom-credential-security-factory=<custom-credential-security-factory name>:read-attribute(name=class-name)`

## syntax of the operation to get the `configuration` attribute of the `custom-credential-security-factory` resource of the `elytron` subsystem
* Retrieve the `configuration` attribute value of the `custom-credential-security-factory` resource of the `elytron` subsystem
* `configuration` attribute description: the optional `key/value` configuration for the custom security factory.
* operation: `/subsystem=elytron/custom-credential-security-factory=<custom-credential-security-factory name>:read-attribute(name=configuration)`

## syntax of the operation to get the `module` attribute of the `custom-credential-security-factory` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `custom-credential-security-factory` resource of the `elytron` subsystem
* `module` attribute description: the module to use to load the custom security factory.
* operation: `/subsystem=elytron/custom-credential-security-factory=<custom-credential-security-factory name>:read-attribute(name=module)`

## syntax of the operation to get a `key-store` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/key-store=<key-store name>:read-resource()`
* To get the list of all the `key-store` resource of the `elytron` subsystem use '*' for `<key-store name>`.

## syntax of the operation to get the `alias-filter` attribute of the `key-store` resource of the `elytron` subsystem
* Retrieve the `alias-filter` attribute value of the `key-store` resource of the `elytron` subsystem
* `alias-filter` attribute description: a filter to apply to the aliases returned from the `keystore`, can either be a comma separated list of aliases to return or one of the following formats `all:-alias1:-alias2`, `none:+alias1:+alias2`
* operation: `/subsystem=elytron/key-store=<key-store name>:read-attribute(name=alias-filter)`

## syntax of the operation to get the `credential-reference` attribute of the `key-store` resource of the `elytron` subsystem
* Retrieve the `credential-reference` attribute value of the `key-store` resource of the `elytron` subsystem
* `credential-reference` attribute description: the reference to credential stored in `credentialstore` under defined alias or clear text password.
* operation: `/subsystem=elytron/key-store=<key-store name>:read-attribute(name=credential-reference)`

## syntax of the operation to get the `loaded-provider` attribute of the `key-store` resource of the `elytron` subsystem
* Retrieve the `loaded-provider` attribute value of the `key-store` resource of the `elytron` subsystem
* `loaded-provider` attribute description: information about the provider that was used for this `keystore`.
* operation: `/subsystem=elytron/key-store=<key-store name>:read-attribute(name=loaded-provider)`

## syntax of the operation to get the `modified` attribute of the `key-store` resource of the `elytron` subsystem
* Retrieve the `modified` attribute value of the `key-store` resource of the `elytron` subsystem
* `modified` attribute description: indicates if the `in-memory` representation of the `keystore` has been changed since it was last loaded or stored.  note: for some providers updates may be immediate without further load or store calls.
* operation: `/subsystem=elytron/key-store=<key-store name>:read-attribute(name=modified)`

## syntax of the operation to get the `path` attribute of the `key-store` resource of the `elytron` subsystem
* Retrieve the `path` attribute value of the `key-store` resource of the `elytron` subsystem
* `path` attribute description: the path to the `keystore` file.
* operation: `/subsystem=elytron/key-store=<key-store name>:read-attribute(name=path)`

## syntax of the operation to get the `provider-name` attribute of the `key-store` resource of the `elytron` subsystem
* Retrieve the `provider-name` attribute value of the `key-store` resource of the `elytron` subsystem
* `provider-name` attribute description: the name of the provider to use to load the `keystore`, disables searching for the first provider that can create a `keystore` of the specified type.
* operation: `/subsystem=elytron/key-store=<key-store name>:read-attribute(name=provider-name)`

## syntax of the operation to get the `providers` attribute of the `key-store` resource of the `elytron` subsystem
* Retrieve the `providers` attribute value of the `key-store` resource of the `elytron` subsystem
* `providers` attribute description: a reference to the providers that should be used to obtain the list of provider instances to search, if not specified the global list of providers will be used instead.
* operation: `/subsystem=elytron/key-store=<key-store name>:read-attribute(name=providers)`

## syntax of the operation to get the `relative-to` attribute of the `key-store` resource of the `elytron` subsystem
* Retrieve the `relative-to` attribute value of the `key-store` resource of the `elytron` subsystem
* `relative-to` attribute description: the base path this store is relative to.
* operation: `/subsystem=elytron/key-store=<key-store name>:read-attribute(name=relative-to)`

## syntax of the operation to get the `required` attribute of the `key-store` resource of the `elytron` subsystem
* Retrieve the `required` attribute value of the `key-store` resource of the `elytron` subsystem
* `required` attribute description: is the file required to exist at the time the `keystore` service starts?
* operation: `/subsystem=elytron/key-store=<key-store name>:read-attribute(name=required)`

## syntax of the operation to get the `size` attribute of the `key-store` resource of the `elytron` subsystem
* Retrieve the `size` attribute value of the `key-store` resource of the `elytron` subsystem
* `size` attribute description: the number of entries in the `keystore`.
* operation: `/subsystem=elytron/key-store=<key-store name>:read-attribute(name=size)`

## syntax of the operation to get the `state` attribute of the `key-store` resource of the `elytron` subsystem
* Retrieve the `state` attribute value of the `key-store` resource of the `elytron` subsystem
* `state` attribute description: the state of the underlying service that represents this `keystore` at runtime, if it is anything other than up runtime operations will not be available.
* operation: `/subsystem=elytron/key-store=<key-store name>:read-attribute(name=state)`

## syntax of the operation to get the `synchronized` attribute of the `key-store` resource of the `elytron` subsystem
* Retrieve the `synchronized` attribute value of the `key-store` resource of the `elytron` subsystem
* `synchronized` attribute description: the time this `keystore` was last loaded or saved. note: some providers may continue to apply updates after the `keystore` was loaded within the application server.
* operation: `/subsystem=elytron/key-store=<key-store name>:read-attribute(name=synchronized)`

## syntax of the operation to get the `type` attribute of the `key-store` resource of the `elytron` subsystem
* Retrieve the `type` attribute value of the `key-store` resource of the `elytron` subsystem
* `type` attribute description: the type of the `keystore`, used when creating the new `keystore` instance.
* operation: `/subsystem=elytron/key-store=<key-store name>:read-attribute(name=type)`

## syntax of the operation to get a `custom-role-decoder` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/custom-role-decoder=<custom-role-decoder name>:read-resource()`
* To get the list of all the `custom-role-decoder` resource of the `elytron` subsystem use '*' for `<custom-role-decoder name>`.

## syntax of the operation to get the `class-name` attribute of the `custom-role-decoder` resource of the `elytron` subsystem
* Retrieve the `class-name` attribute value of the `custom-role-decoder` resource of the `elytron` subsystem
* `class-name` attribute description: fully qualified class name of the `roledecoder`
* operation: `/subsystem=elytron/custom-role-decoder=<custom-role-decoder name>:read-attribute(name=class-name)`

## syntax of the operation to get the `configuration` attribute of the `custom-role-decoder` resource of the `elytron` subsystem
* Retrieve the `configuration` attribute value of the `custom-role-decoder` resource of the `elytron` subsystem
* `configuration` attribute description: the optional `kay/value` configuration for the `roledecoder`
* operation: `/subsystem=elytron/custom-role-decoder=<custom-role-decoder name>:read-attribute(name=configuration)`

## syntax of the operation to get the `module` attribute of the `custom-role-decoder` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `custom-role-decoder` resource of the `elytron` subsystem
* `module` attribute description: name of the module to use to load the `roledecoder`
* operation: `/subsystem=elytron/custom-role-decoder=<custom-role-decoder name>:read-attribute(name=module)`

## syntax of the operation to get a `simple-role-decoder` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/simple-role-decoder=<simple-role-decoder name>:read-resource()`
* To get the list of all the `simple-role-decoder` resource of the `elytron` subsystem use '*' for `<simple-role-decoder name>`.

## syntax of the operation to get the `attribute` attribute of the `simple-role-decoder` resource of the `elytron` subsystem
* Retrieve the `attribute` attribute value of the `simple-role-decoder` resource of the `elytron` subsystem
* `attribute` attribute description: the name of the attribute from the identity to map directly to roles.
* operation: `/subsystem=elytron/simple-role-decoder=<simple-role-decoder name>:read-attribute(name=attribute)`

## syntax of the operation to get a `aggregate-principal-transformer` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/aggregate-principal-transformer=<aggregate-principal-transformer name>:read-resource()`
* To get the list of all the `aggregate-principal-transformer` resource of the `elytron` subsystem use '*' for `<aggregate-principal-transformer name>`.

## syntax of the operation to get the `principal-transformers` attribute of the `aggregate-principal-transformer` resource of the `elytron` subsystem
* Retrieve the `principal-transformers` attribute value of the `aggregate-principal-transformer` resource of the `elytron` subsystem
* `principal-transformers` attribute description: the referenced principal transformers to aggregate.
* operation: `/subsystem=elytron/aggregate-principal-transformer=<aggregate-principal-transformer name>:read-attribute(name=principal-transformers)`

## syntax of the operation to get a `x500-subject-evidence-decoder` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/x500-subject-evidence-decoder=<x500-subject-evidence-decoder name>:read-resource()`
* To get the list of all the `x500-subject-evidence-decoder` resource of the `elytron` subsystem use '*' for `<x500-subject-evidence-decoder name>`.

## syntax of the operation to get a `aggregate-role-mapper` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/aggregate-role-mapper=<aggregate-role-mapper name>:read-resource()`
* To get the list of all the `aggregate-role-mapper` resource of the `elytron` subsystem use '*' for `<aggregate-role-mapper name>`.

## syntax of the operation to get the `role-mappers` attribute of the `aggregate-role-mapper` resource of the `elytron` subsystem
* Retrieve the `role-mappers` attribute value of the `aggregate-role-mapper` resource of the `elytron` subsystem
* `role-mappers` attribute description: the referenced role mappers to aggregate.
* operation: `/subsystem=elytron/aggregate-role-mapper=<aggregate-role-mapper name>:read-attribute(name=role-mappers)`

## syntax of the operation to get a `custom-realm` resource of the `elytron` subsystem
* operation: `/subsystem=elytron/custom-realm=<custom-realm name>:read-resource()`
* To get the list of all the `custom-realm` resource of the `elytron` subsystem use '*' for `<custom-realm name>`.

## syntax of the operation to get the `class-name` attribute of the `custom-realm` resource of the `elytron` subsystem
* Retrieve the `class-name` attribute value of the `custom-realm` resource of the `elytron` subsystem
* `class-name` attribute description: the class name of the implementation of the custom realm.
* operation: `/subsystem=elytron/custom-realm=<custom-realm name>:read-attribute(name=class-name)`

## syntax of the operation to get the `configuration` attribute of the `custom-realm` resource of the `elytron` subsystem
* Retrieve the `configuration` attribute value of the `custom-realm` resource of the `elytron` subsystem
* `configuration` attribute description: the optional `key/value` configuration for the custom realm.
* operation: `/subsystem=elytron/custom-realm=<custom-realm name>:read-attribute(name=configuration)`

## syntax of the operation to get the `module` attribute of the `custom-realm` resource of the `elytron` subsystem
* Retrieve the `module` attribute value of the `custom-realm` resource of the `elytron` subsystem
* `module` attribute description: the module to use to load the custom realm.
* operation: `/subsystem=elytron/custom-realm=<custom-realm name>:read-attribute(name=module)`

## syntax of the operation to get the `default-jsf-impl-slot` attribute of the `jsf` subsystem
* Retrieve the `default-jsf-impl-slot` attribute value of the `jsf` subsystem
* `default-jsf-impl-slot` attribute description: default jakarta server faces implementation slot
* operation: `/subsystem=jsf:read-attribute(name=default-jsf-impl-slot)`

## syntax of the operation to get the `disallow-doctype-decl` attribute of the `jsf` subsystem
* Retrieve the `disallow-doctype-decl` attribute value of the `jsf` subsystem
* `disallow-doctype-decl` attribute description: specifies whether or not `doctype` declarations in jakarta server faces deployments should be disallowed. this setting can be overridden at the deployment level.
* operation: `/subsystem=jsf:read-attribute(name=disallow-doctype-decl)`

## syntax of the operation to get the `default-tracer` attribute of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `default-tracer` attribute value of the `microprofile-opentracing-smallrye` subsystem
* `default-tracer` attribute description: default tracer for the deployments if none is defined.
* operation: `/subsystem=microprofile-opentracing-smallrye:read-attribute(name=default-tracer)`

## syntax of the operation to get a `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-resource()`
* To get the list of all the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem use '*' for `<jaeger-tracer name>`.

## syntax of the operation to get the `propagation` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `propagation` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `propagation` attribute description: list of formats to use for propagating the trace context.
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=propagation)`

## syntax of the operation to get the `reporter-flush-interval` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `reporter-flush-interval` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `reporter-flush-interval` attribute description: the flush interval when reporting spans remotely.
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=reporter-flush-interval)`

## syntax of the operation to get the `reporter-log-spans` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `reporter-log-spans` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `reporter-log-spans` attribute description: whether the reporter should log the spans.
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=reporter-log-spans)`

## syntax of the operation to get the `reporter-max-queue-size` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `reporter-max-queue-size` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `reporter-max-queue-size` attribute description: the maximum queue size for use when reporting spans remotely.
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=reporter-max-queue-size)`

## syntax of the operation to get the `sampler-manager-host-port` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `sampler-manager-host-port` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `sampler-manager-host-port` attribute description: the jaeger sampler host port.
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=sampler-manager-host-port)`

## syntax of the operation to get the `sampler-param` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `sampler-param` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `sampler-param` attribute description: the jaeger sampler parameter. for a 'constant' sampler `0` means no trace and `1` means all traces. for a 'probabilistic' sample this is the probability of sampling. for a 'rate limiting' sampler this define the number of traces per second.
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=sampler-param)`

## syntax of the operation to get the `sampler-type` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `sampler-type` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `sampler-type` attribute description: the jaeger sampler type. there are `4` supported type: 'const', 'probabilistic', '`ratelimiting`' and 'remote'.
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=sampler-type)`

## syntax of the operation to get the `sender-auth-password` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `sender-auth-password` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `sender-auth-password` attribute description: the basic auth password to be added on authorization headers for requests sent to the endpoint.
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=sender-auth-password)`

## syntax of the operation to get the `sender-auth-token` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `sender-auth-token` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `sender-auth-token` attribute description: the auth token to be added as "bearer" on authorization headers for requests sent to the endpoint.
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=sender-auth-token)`

## syntax of the operation to get the `sender-auth-user` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `sender-auth-user` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `sender-auth-user` attribute description: the basic auth `username` to be added on authorization headers for requests sent to the endpoint.
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=sender-auth-user)`

## syntax of the operation to get the `sender-binding` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `sender-binding` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `sender-binding` attribute description: the outbound binding used to connect to the agent.
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=sender-binding)`

## syntax of the operation to get the `sender-endpoint` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `sender-endpoint` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `sender-endpoint` attribute description: the full `url` to the traces endpoint, like `https://jaeger-collector:14268/api/traces`
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=sender-endpoint)`

## syntax of the operation to get the `tags` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `tags` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `tags` attribute description: the tracer level tags.
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=tags)`

## syntax of the operation to get the `tracer_id_128bit` attribute of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* Retrieve the `tracer_id_128bit` attribute value of the `jaeger-tracer` resource of the `microprofile-opentracing-smallrye` subsystem
* `tracer_id_128bit` attribute description: `opt-in` to use `128` bit `traceids`. by default, uses `64` bits.
* operation: `/subsystem=microprofile-opentracing-smallrye/jaeger-tracer=<jaeger-tracer name>:read-attribute(name=tracer_id_128bit)`

## syntax of the operation to get the `default-job-repository` attribute of the `batch-jberet` subsystem
* Retrieve the `default-job-repository` attribute value of the `batch-jberet` subsystem
* `default-job-repository` attribute description: the name of the default job repository.
* operation: `/subsystem=batch-jberet:read-attribute(name=default-job-repository)`

## syntax of the operation to get the `default-thread-pool` attribute of the `batch-jberet` subsystem
* Retrieve the `default-thread-pool` attribute value of the `batch-jberet` subsystem
* `default-thread-pool` attribute description: the name of the default `thread-pool`.
* operation: `/subsystem=batch-jberet:read-attribute(name=default-thread-pool)`

## syntax of the operation to get the `restart-jobs-on-resume` attribute of the `batch-jberet` subsystem
* Retrieve the `restart-jobs-on-resume` attribute value of the `batch-jberet` subsystem
* `restart-jobs-on-resume` attribute description: if set to true when a resume operation has been invoked after a suspend operation any jobs stopped during the suspend will be restarted. a value of false will leave the jobs in a stopped state.
* operation: `/subsystem=batch-jberet:read-attribute(name=restart-jobs-on-resume)`

## syntax of the operation to get the `security-domain` attribute of the `batch-jberet` subsystem
* Retrieve the `security-domain` attribute value of the `batch-jberet` subsystem
* `security-domain` attribute description: references the security domain for batch jobs. this can only be defined if the elytron subsystem is available.
* operation: `/subsystem=batch-jberet:read-attribute(name=security-domain)`

## syntax of the operation to get a `in-memory-job-repository` resource of the `batch-jberet` subsystem
* operation: `/subsystem=batch-jberet/in-memory-job-repository=<in-memory-job-repository name>:read-resource()`
* To get the list of all the `in-memory-job-repository` resource of the `batch-jberet` subsystem use '*' for `<in-memory-job-repository name>`.

## syntax of the operation to get the `execution-records-limit` attribute of the `in-memory-job-repository` resource of the `batch-jberet` subsystem
* Retrieve the `execution-records-limit` attribute value of the `in-memory-job-repository` resource of the `batch-jberet` subsystem
* `execution-records-limit` attribute description: the maximum number of job execution records that should be returned by the job repository.
* operation: `/subsystem=batch-jberet/in-memory-job-repository=<in-memory-job-repository name>:read-attribute(name=execution-records-limit)`

## syntax of the operation to get a `thread-pool` resource of the `batch-jberet` subsystem
* operation: `/subsystem=batch-jberet/thread-pool=<thread-pool name>:read-resource()`
* To get the list of all the `thread-pool` resource of the `batch-jberet` subsystem use '*' for `<thread-pool name>`.

## syntax of the operation to get the `active-count` attribute of the `thread-pool` resource of the `batch-jberet` subsystem
* Retrieve the `active-count` attribute value of the `thread-pool` resource of the `batch-jberet` subsystem
* `active-count` attribute description: the approximate number of threads that are actively executing tasks.
* operation: `/subsystem=batch-jberet/thread-pool=<thread-pool name>:read-attribute(name=active-count)`

## syntax of the operation to get the `completed-task-count` attribute of the `thread-pool` resource of the `batch-jberet` subsystem
* Retrieve the `completed-task-count` attribute value of the `thread-pool` resource of the `batch-jberet` subsystem
* `completed-task-count` attribute description: the approximate total number of tasks that have completed execution.
* operation: `/subsystem=batch-jberet/thread-pool=<thread-pool name>:read-attribute(name=completed-task-count)`

## syntax of the operation to get the `current-thread-count` attribute of the `thread-pool` resource of the `batch-jberet` subsystem
* Retrieve the `current-thread-count` attribute value of the `thread-pool` resource of the `batch-jberet` subsystem
* `current-thread-count` attribute description: the current number of threads in the pool.
* operation: `/subsystem=batch-jberet/thread-pool=<thread-pool name>:read-attribute(name=current-thread-count)`

## syntax of the operation to get the `keepalive-time` attribute of the `thread-pool` resource of the `batch-jberet` subsystem
* Retrieve the `keepalive-time` attribute value of the `thread-pool` resource of the `batch-jberet` subsystem
* `keepalive-time` attribute description: used to specify the amount of time that pool threads should be kept running when idle; if not specified, threads will run until the executor is shut down.
* operation: `/subsystem=batch-jberet/thread-pool=<thread-pool name>:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `largest-thread-count` attribute of the `thread-pool` resource of the `batch-jberet` subsystem
* Retrieve the `largest-thread-count` attribute value of the `thread-pool` resource of the `batch-jberet` subsystem
* `largest-thread-count` attribute description: the largest number of threads that have ever simultaneously been in the pool.
* operation: `/subsystem=batch-jberet/thread-pool=<thread-pool name>:read-attribute(name=largest-thread-count)`

## syntax of the operation to get the `max-threads` attribute of the `thread-pool` resource of the `batch-jberet` subsystem
* Retrieve the `max-threads` attribute value of the `thread-pool` resource of the `batch-jberet` subsystem
* `max-threads` attribute description: the maximum thread pool size. note this should always be greater than `3`. two threads are reserved to ensure partition jobs can execute as expected.
* operation: `/subsystem=batch-jberet/thread-pool=<thread-pool name>:read-attribute(name=max-threads)`

## syntax of the operation to get the `name` attribute of the `thread-pool` resource of the `batch-jberet` subsystem
* Retrieve the `name` attribute value of the `thread-pool` resource of the `batch-jberet` subsystem
* `name` attribute description: the name of the thread pool.
* operation: `/subsystem=batch-jberet/thread-pool=<thread-pool name>:read-attribute(name=name)`

## syntax of the operation to get the `queue-size` attribute of the `thread-pool` resource of the `batch-jberet` subsystem
* Retrieve the `queue-size` attribute value of the `thread-pool` resource of the `batch-jberet` subsystem
* `queue-size` attribute description: the queue size.
* operation: `/subsystem=batch-jberet/thread-pool=<thread-pool name>:read-attribute(name=queue-size)`

## syntax of the operation to get the `rejected-count` attribute of the `thread-pool` resource of the `batch-jberet` subsystem
* Retrieve the `rejected-count` attribute value of the `thread-pool` resource of the `batch-jberet` subsystem
* `rejected-count` attribute description: the number of tasks that have been rejected.
* operation: `/subsystem=batch-jberet/thread-pool=<thread-pool name>:read-attribute(name=rejected-count)`

## syntax of the operation to get the `task-count` attribute of the `thread-pool` resource of the `batch-jberet` subsystem
* Retrieve the `task-count` attribute value of the `thread-pool` resource of the `batch-jberet` subsystem
* `task-count` attribute description: the approximate total number of tasks that have ever been scheduled for execution.
* operation: `/subsystem=batch-jberet/thread-pool=<thread-pool name>:read-attribute(name=task-count)`

## syntax of the operation to get the `thread-factory` attribute of the `thread-pool` resource of the `batch-jberet` subsystem
* Retrieve the `thread-factory` attribute value of the `thread-pool` resource of the `batch-jberet` subsystem
* `thread-factory` attribute description: specifies the name of a specific thread factory to use to create worker threads. if not defined an appropriate default thread factory will be used.
* operation: `/subsystem=batch-jberet/thread-pool=<thread-pool name>:read-attribute(name=thread-factory)`

## syntax of the operation to get a `thread-factory` resource of the `batch-jberet` subsystem
* operation: `/subsystem=batch-jberet/thread-factory=<thread-factory name>:read-resource()`
* To get the list of all the `thread-factory` resource of the `batch-jberet` subsystem use '*' for `<thread-factory name>`.

## syntax of the operation to get the `group-name` attribute of the `thread-factory` resource of the `batch-jberet` subsystem
* Retrieve the `group-name` attribute value of the `thread-factory` resource of the `batch-jberet` subsystem
* `group-name` attribute description: specifies the name of a  thread group to create for this thread factory.
* operation: `/subsystem=batch-jberet/thread-factory=<thread-factory name>:read-attribute(name=group-name)`

## syntax of the operation to get the `name` attribute of the `thread-factory` resource of the `batch-jberet` subsystem
* Retrieve the `name` attribute value of the `thread-factory` resource of the `batch-jberet` subsystem
* `name` attribute description: the name of the created thread factory.
* operation: `/subsystem=batch-jberet/thread-factory=<thread-factory name>:read-attribute(name=name)`

## syntax of the operation to get the `priority` attribute of the `thread-factory` resource of the `batch-jberet` subsystem
* Retrieve the `priority` attribute value of the `thread-factory` resource of the `batch-jberet` subsystem
* `priority` attribute description: may be used to specify the thread priority of created threads.
* operation: `/subsystem=batch-jberet/thread-factory=<thread-factory name>:read-attribute(name=priority)`

## syntax of the operation to get the `thread-name-pattern` attribute of the `thread-factory` resource of the `batch-jberet` subsystem
* Retrieve the `thread-name-pattern` attribute value of the `thread-factory` resource of the `batch-jberet` subsystem
* `thread-name-pattern` attribute description: the template used to create names for threads.  the following patterns may be used:
%% - emit a percent sign
%t - emit the `per-factory` thread sequence number
%g - emit the global thread sequence number
%f - emit the factory sequence number
%i - emit the thread id.
* operation: `/subsystem=batch-jberet/thread-factory=<thread-factory name>:read-attribute(name=thread-name-pattern)`

## syntax of the operation to get a `jdbc-job-repository` resource of the `batch-jberet` subsystem
* operation: `/subsystem=batch-jberet/jdbc-job-repository=<jdbc-job-repository name>:read-resource()`
* To get the list of all the `jdbc-job-repository` resource of the `batch-jberet` subsystem use '*' for `<jdbc-job-repository name>`.

## syntax of the operation to get the `data-source` attribute of the `jdbc-job-repository` resource of the `batch-jberet` subsystem
* Retrieve the `data-source` attribute value of the `jdbc-job-repository` resource of the `batch-jberet` subsystem
* `data-source` attribute description: the data source name used to connect to the database.
* operation: `/subsystem=batch-jberet/jdbc-job-repository=<jdbc-job-repository name>:read-attribute(name=data-source)`

## syntax of the operation to get the `execution-records-limit` attribute of the `jdbc-job-repository` resource of the `batch-jberet` subsystem
* Retrieve the `execution-records-limit` attribute value of the `jdbc-job-repository` resource of the `batch-jberet` subsystem
* `execution-records-limit` attribute description: the maximum number of job execution records that should be returned by the job repository.
* operation: `/subsystem=batch-jberet/jdbc-job-repository=<jdbc-job-repository name>:read-attribute(name=execution-records-limit)`

## syntax of the operation to get the `empty-liveness-checks-status` attribute of the `microprofile-health-smallrye` subsystem
* Retrieve the `empty-liveness-checks-status` attribute value of the `microprofile-health-smallrye` subsystem
* `empty-liveness-checks-status` attribute description: defines the global status returned by the health checks endpoints if no liveness probes have been defined in deployments.
* operation: `/subsystem=microprofile-health-smallrye:read-attribute(name=empty-liveness-checks-status)`

## syntax of the operation to get the `empty-readiness-checks-status` attribute of the `microprofile-health-smallrye` subsystem
* Retrieve the `empty-readiness-checks-status` attribute value of the `microprofile-health-smallrye` subsystem
* `empty-readiness-checks-status` attribute description: defines the global status returned by the health checks endpoints if no readiness probes have been defined in deployments.
* operation: `/subsystem=microprofile-health-smallrye:read-attribute(name=empty-readiness-checks-status)`

## syntax of the operation to get the `empty-startup-checks-status` attribute of the `microprofile-health-smallrye` subsystem
* Retrieve the `empty-startup-checks-status` attribute value of the `microprofile-health-smallrye` subsystem
* `empty-startup-checks-status` attribute description: defines the global status returned by the health checks endpoints if no startup probes have been defined in deployments.
* operation: `/subsystem=microprofile-health-smallrye:read-attribute(name=empty-startup-checks-status)`

## syntax of the operation to get the `security-enabled` attribute of the `microprofile-health-smallrye` subsystem
* Retrieve the `security-enabled` attribute value of the `microprofile-health-smallrye` subsystem
* `security-enabled` attribute description: true if authentication is required to access the `http` endpoints on the `http` management interface.
* operation: `/subsystem=microprofile-health-smallrye:read-attribute(name=security-enabled)`

## syntax of the operation to get a `provider` resource of the `elytron-oidc-client` subsystem
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-resource()`
* To get the list of all the `provider` resource of the `elytron-oidc-client` subsystem use '*' for `<provider name>`.

## syntax of the operation to get the `allow-any-hostname` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `allow-any-hostname` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `allow-any-hostname` attribute description: if set to 'true', `hostname` verification is skipped when communicating with the `openid` provider over `https`. this can be useful in testing environments. this should never be set to 'true' in production environments as it disables verification of `ssl` certificates.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=allow-any-hostname)`

## syntax of the operation to get the `always-refresh-token` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `always-refresh-token` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `always-refresh-token` attribute description: if set to 'true', the subsystem refreshes the token every time your application receives a web request, and a new request is sent to the `openid` provider to obtain a new access token. this can result in a higher load on the `openid` provider and may impact the performance of the application.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=always-refresh-token)`

## syntax of the operation to get the `auth-server-url` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `auth-server-url` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `auth-server-url` attribute description: the base `url` of the `keycloak` authorization server. this is `keycloak-specific`. it is recommended to use the '`provider-url`' instead.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=auth-server-url)`

## syntax of the operation to get the `autodetect-bearer-only` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `autodetect-bearer-only` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `autodetect-bearer-only` attribute description: whether to `auto-detect` soap or rest clients based on headers like '`x-requested-with`', '`soapaction`' or 'accept'. if set to 'true', the subsystem sends an `http` `401` status code to unauthenticated soap or rest clients instead of redirecting them to the `openid` provider login page. set the value to 'true' if your application serves both applications and web services.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=autodetect-bearer-only)`

## syntax of the operation to get the `client-key-password` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-key-password` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `client-key-password` attribute description: the password for the client key. this is required if '`client-keystore`' has been specified.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=client-key-password)`

## syntax of the operation to get the `client-keystore` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `client-keystore` attribute description: the path to the client `keystore` to use when communicating with the `openid` provider over `https`. this is optional.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=client-keystore)`

## syntax of the operation to get the `client-keystore-password` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore-password` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `client-keystore-password` attribute description: the password for the client `keystore`. this is required if '`client-keystore`' has been specified.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=client-keystore-password)`

## syntax of the operation to get the `confidential-port` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `confidential-port` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `confidential-port` attribute description: the confidential port used by the `openid` provider when communicating securely over `ssl/tls`.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=confidential-port)`

## syntax of the operation to get the `connection-pool-size` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `connection-pool-size` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `connection-pool-size` attribute description: the connection pool size to use when communicating with the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=connection-pool-size)`

## syntax of the operation to get the `connection-timeout-millis` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `connection-timeout-millis` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `connection-timeout-millis` attribute description: the timeout for establishing a connection with the remote host in milliseconds. a timeout value of zero is interpreted as an infinite timeout, and a negative value is interpreted as undefined.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=connection-timeout-millis)`

## syntax of the operation to get the `connection-ttl-millis` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `connection-ttl-millis` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `connection-ttl-millis` attribute description: the amount of time in milliseconds for the connection to be kept alive. a value less than or equal to zero is interpreted as an infinite value.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=connection-ttl-millis)`

## syntax of the operation to get the `cors-allowed-headers` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-allowed-headers` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `cors-allowed-headers` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-allow-headers`' header. this should be a `comma-separated` string. this is optional. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=cors-allowed-headers)`

## syntax of the operation to get the `cors-allowed-methods` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-allowed-methods` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `cors-allowed-methods` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-allow-methods`' header. this should be a `comma-separated` string. this is optional. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=cors-allowed-methods)`

## syntax of the operation to get the `cors-exposed-headers` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-exposed-headers` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `cors-exposed-headers` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-expose-headers`' header. this should be a `comma-separated` string. this is optional. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=cors-exposed-headers)`

## syntax of the operation to get the `cors-max-age` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-max-age` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `cors-max-age` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-max-age`' header. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=cors-max-age)`

## syntax of the operation to get the `disable-trust-manager` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `disable-trust-manager` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `disable-trust-manager` attribute description: whether or not to make use of a trust manager when communicating with the `openid` provider over `https`. this is optional. this should be set to 'true' only during development and never in production as it disables verification of `ssl` certificates.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=disable-trust-manager)`

## syntax of the operation to get the `enable-cors` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `enable-cors` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `enable-cors` attribute description: enable the `keycloak` `cross-origin` resource sharing (`cors`) support. this is optional. this is `keycloak-specific`.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=enable-cors)`

## syntax of the operation to get the `expose-token` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `expose-token` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `expose-token` attribute description: if set to 'true', an authenticated browser client can obtain the signed access token (through a `javascript` `http` invocation) via the `url` '`root/k_query_bearer_token`'. this is optional.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=expose-token)`

## syntax of the operation to get the `ignore-oauth-query-parameter` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `ignore-oauth-query-parameter` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `ignore-oauth-query-parameter` attribute description: disable query parameter parsing for the '`access_token`'. users will not be able to authenticate if they only pass in an '`access_token`'.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=ignore-oauth-query-parameter)`

## syntax of the operation to get the `principal-attribute` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `principal-attribute` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `principal-attribute` attribute description: indicates which value from the id token to use as the principal for the identity. the principal defaults to the value of the 'sub' if the token attribute is null.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=principal-attribute)`

## syntax of the operation to get the `provider-url` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `provider-url` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `provider-url` attribute description: the `openid` provider `url`.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=provider-url)`

## syntax of the operation to get the `proxy-url` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `proxy-url` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `proxy-url` attribute description: the `url` for the `http` proxy if one is used.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=proxy-url)`

## syntax of the operation to get the `realm-public-key` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `realm-public-key` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `realm-public-key` attribute description: the public key of the `openid` provider in `pem` format. this is optional. it is not recommended to set it. if the public key is not set, the subsystem downloads the public key from the `openid` provider when needed. if the public key is set, the subsystem never downloads new keys from the `openid` provider, breaking the subsystem when the `openid` provider rotates its keys.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=realm-public-key)`

## syntax of the operation to get the `register-node-at-startup` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `register-node-at-startup` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `register-node-at-startup` attribute description: if set to 'true', the subsystem sends a registration request to the `openid` provider. this attribute is useful only when your application is clustered.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=register-node-at-startup)`

## syntax of the operation to get the `register-node-period` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `register-node-period` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `register-node-period` attribute description: if '`register-node-at-startup`' is set to 'true', this specifies the frequency (in seconds) at which the node should be `re-registered`.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=register-node-period)`

## syntax of the operation to get the `socket-timeout-millis` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `socket-timeout-millis` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `socket-timeout-millis` attribute description: the timeout for the socket waiting for data after establishing the connection in milliseconds. a timeout value of zero is interpreted as an infinite timeout, and a negative value is interpreted as undefined.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=socket-timeout-millis)`

## syntax of the operation to get the `ssl-required` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `ssl-required` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `ssl-required` attribute description: whether the communication with the `openid` provider should be over `https`. valid values are: 'all' - to always require `https`, 'external' - to only require `https` for external requests, 'none' - if `https` is not required. this should be set to 'all' in production environments.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=ssl-required)`

## syntax of the operation to get the `token-signature-algorithm` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `token-signature-algorithm` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `token-signature-algorithm` attribute description: the token signature algorithm used by the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=token-signature-algorithm)`

## syntax of the operation to get the `token-store` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `token-store` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `token-store` attribute description: defines whether to store account information in an `http` session or in a cookie.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=token-store)`

## syntax of the operation to get the `truststore` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `truststore` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `truststore` attribute description: the path to the `truststore` to use when communicating with `keycloak` over `https`. prefix the path with '`classpath`:' to obtain the `truststore` from the `deployment's` `classpath`.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=truststore)`

## syntax of the operation to get the `truststore-password` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `truststore-password` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `truststore-password` attribute description: the password for the `truststore`.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=truststore-password)`

## syntax of the operation to get the `verify-token-audience` attribute of the `provider` resource of the `elytron-oidc-client` subsystem
* Retrieve the `verify-token-audience` attribute value of the `provider` resource of the `elytron-oidc-client` subsystem
* `verify-token-audience` attribute description: if set to 'true', then during `bearer-only` authentication, the subsystem verifies if the token contains the client name defined as an audience. it is recommended to set the value to 'true' for improved security.
* operation: `/subsystem=elytron-oidc-client/provider=<provider name>:read-attribute(name=verify-token-audience)`

## syntax of the operation to get a `secure-server` resource of the `elytron-oidc-client` subsystem
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-resource()`
* To get the list of all the `secure-server` resource of the `elytron-oidc-client` subsystem use '*' for `<secure-server name>`.

## syntax of the operation to get the `adapter-state-cookie-path` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `adapter-state-cookie-path` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `adapter-state-cookie-path` attribute description: if set, this defines the path used in cookies set by the subsystem. this is useful when deploying an application in the root context path.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=adapter-state-cookie-path)`

## syntax of the operation to get the `allow-any-hostname` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `allow-any-hostname` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `allow-any-hostname` attribute description: if set to 'true', `hostname` verification is skipped when communicating with the `openid` provider over `https`. this can be useful in testing environments. this should never be set to 'true' in production environments as it disables verification of `ssl` certificates.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=allow-any-hostname)`

## syntax of the operation to get the `always-refresh-token` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `always-refresh-token` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `always-refresh-token` attribute description: if set to 'true', the subsystem refreshes the token every time your application receives a web request, and a new request is sent to the `openid` provider to obtain a new access token. this can result in a higher load on the `openid` provider and may impact the performance of the application.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=always-refresh-token)`

## syntax of the operation to get the `auth-server-url` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `auth-server-url` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `auth-server-url` attribute description: the base `url` of the `keycloak` authorization server. this is `keycloak-specific`. it is recommended to use '`provider-url`' instead.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=auth-server-url)`

## syntax of the operation to get the `autodetect-bearer-only` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `autodetect-bearer-only` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `autodetect-bearer-only` attribute description: whether to `auto-detect` soap or rest clients based on headers like '`x-requested-with`', '`soapaction`' or 'accept'. if set to 'true', the subsystem sends an `http` `401` status code to unauthenticated soap or rest clients instead of redirecting them to the `openid` provider login page. set the value to 'true' if your application serves both applications and web services.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=autodetect-bearer-only)`

## syntax of the operation to get the `bearer-only` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `bearer-only` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `bearer-only` attribute description: enable `bearer-token` only authentication. set this to 'true' if your application serves only web services and does not authenticate users.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=bearer-only)`

## syntax of the operation to get the `client-id` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-id` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `client-id` attribute description: the unique identifier for a client application registered in the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=client-id)`

## syntax of the operation to get the `client-key-password` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-key-password` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `client-key-password` attribute description: the password for the client key. this is required if '`client-keystore`' has been specified.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=client-key-password)`

## syntax of the operation to get the `client-keystore` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `client-keystore` attribute description: the path to the client `keystore` to use when communicating with the `openid` provider over `https`. this is optional.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=client-keystore)`

## syntax of the operation to get the `client-keystore-password` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore-password` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `client-keystore-password` attribute description: the password for the client `keystore`. this is required if '`client-keystore`' has been specified.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=client-keystore-password)`

## syntax of the operation to get the `confidential-port` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `confidential-port` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `confidential-port` attribute description: the confidential port used by the `openid` provider when communicating securely over `ssl/tls`.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=confidential-port)`

## syntax of the operation to get the `connection-pool-size` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `connection-pool-size` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `connection-pool-size` attribute description: the connection pool size to use when communicating with the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=connection-pool-size)`

## syntax of the operation to get the `connection-timeout-millis` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `connection-timeout-millis` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `connection-timeout-millis` attribute description: the timeout for establishing a connection with the remote host in milliseconds. a timeout value of zero is interpreted as an infinite timeout, and a negative value is interpreted as undefined.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=connection-timeout-millis)`

## syntax of the operation to get the `connection-ttl-millis` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `connection-ttl-millis` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `connection-ttl-millis` attribute description: the amount of time in milliseconds for the connection to be kept alive. a value less than or equal to zero is interpreted as an infinite value.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=connection-ttl-millis)`

## syntax of the operation to get the `cors-allowed-headers` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-allowed-headers` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `cors-allowed-headers` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-allow-headers`' header. this should be a `comma-separated` string. this is optional. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=cors-allowed-headers)`

## syntax of the operation to get the `cors-allowed-methods` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-allowed-methods` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `cors-allowed-methods` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-allow-methods`' header. this should be a `comma-separated` string. this is optional. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=cors-allowed-methods)`

## syntax of the operation to get the `cors-exposed-headers` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-exposed-headers` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `cors-exposed-headers` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-expose-headers`' header. this should be a `comma-separated` string. this is optional. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=cors-exposed-headers)`

## syntax of the operation to get the `cors-max-age` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-max-age` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `cors-max-age` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-max-age`' header. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=cors-max-age)`

## syntax of the operation to get the `credential` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `credential` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `credential` attribute description: the credential used to communicate with the `openid` connect provider.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=credential)`

## syntax of the operation to get the `disable-trust-manager` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `disable-trust-manager` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `disable-trust-manager` attribute description: whether or not to make use of a trust manager when communicating with the `openid` provider over `https`. this is optional. this should be set to 'true' only during development and never in production as it disables verification of `ssl` certificates.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=disable-trust-manager)`

## syntax of the operation to get the `enable-basic-auth` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `enable-basic-auth` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `enable-basic-auth` attribute description: enable basic authentication. this is not supported in the current release.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=enable-basic-auth)`

## syntax of the operation to get the `enable-cors` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `enable-cors` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `enable-cors` attribute description: enable the `keycloak` `cross-origin` resource sharing (`cors`) support. this is optional. this is `keycloak-specific`.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=enable-cors)`

## syntax of the operation to get the `expose-token` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `expose-token` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `expose-token` attribute description: if set to 'true', an authenticated browser client can obtain the signed access token (through a `javascript` `http` invocation) via the `url` '`root/k_query_bearer_token`'. this is optional.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=expose-token)`

## syntax of the operation to get the `ignore-oauth-query-parameter` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `ignore-oauth-query-parameter` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `ignore-oauth-query-parameter` attribute description: disable query parameter parsing for the '`access_token`'. users will not be able to authenticate if they only pass in an '`access_token`'.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=ignore-oauth-query-parameter)`

## syntax of the operation to get the `min-time-between-jwks-requests` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `min-time-between-jwks-requests` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `min-time-between-jwks-requests` attribute description: if the subsystem recognizes a token signed by an unknown public key, it will try to download a new public key from the server. the subsystem `won't` try to download a public key if it already tried last in less than '`min-time-between-jwks-requests`' seconds.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=min-time-between-jwks-requests)`

## syntax of the operation to get the `principal-attribute` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `principal-attribute` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `principal-attribute` attribute description: indicates which value from the id token to use as the principal for the identity. the principal defaults to the value of the 'sub' if the token attribute is null.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=principal-attribute)`

## syntax of the operation to get the `provider` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `provider` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `provider` attribute description: the `openid` connect provider to use for authentication.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=provider)`

## syntax of the operation to get the `provider-url` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `provider-url` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `provider-url` attribute description: the `openid` provider `url`.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=provider-url)`

## syntax of the operation to get the `proxy-url` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `proxy-url` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `proxy-url` attribute description: the `url` for the `http` proxy if one is used.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=proxy-url)`

## syntax of the operation to get the `public-client` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `public-client` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `public-client` attribute description: if set to 'true', no client credentials are sent when communicating with the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=public-client)`

## syntax of the operation to get the `public-key-cache-ttl` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `public-key-cache-ttl` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `public-key-cache-ttl` attribute description: the maximum interval between two requests to retrieve new public keys in seconds. new public keys are downloaded when the subsystem recognizes a token signed by an unknown public key. even if the `token's` key is already known, new public keys are downloaded periodically as per the interval set here at least once.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=public-key-cache-ttl)`

## syntax of the operation to get the `realm` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `realm` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `realm` attribute description: the `keycloak` realm to use for authentication.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=realm)`

## syntax of the operation to get the `realm-public-key` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `realm-public-key` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `realm-public-key` attribute description: the public key of the `openid` provider in `pem` format. this is optional. it is not recommended to set it. if the public key is not set, the subsystem downloads the public key from the `openid` provider when needed. if the public key is set, the subsystem never downloads new keys from the `openid` provider, breaking the subsystem when the `openid` provider rotates its keys.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=realm-public-key)`

## syntax of the operation to get the `redirect-rewrite-rule` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `redirect-rewrite-rule` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `redirect-rewrite-rule` attribute description: the rewrite rule for the redirect uri. the rewrite rule is an object notation, where the key is a regular expression with which the redirect uri is matched and the value is the replacement string.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=redirect-rewrite-rule)`

## syntax of the operation to get the `register-node-at-startup` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `register-node-at-startup` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `register-node-at-startup` attribute description: if set to 'true', the subsystem sends a registration request to the `openid` provider. this attribute is useful only when your application is clustered.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=register-node-at-startup)`

## syntax of the operation to get the `register-node-period` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `register-node-period` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `register-node-period` attribute description: if '`register-node-at-startup`' is set to 'true', this specifies the frequency (in seconds) at which the node should be `re-registered`.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=register-node-period)`

## syntax of the operation to get the `resource` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `resource` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `resource` attribute description: the unique, legacy identifier for a client application registered in the `openid` provider. it is recommended to use the '`client-id`'.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=resource)`

## syntax of the operation to get the `socket-timeout-millis` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `socket-timeout-millis` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `socket-timeout-millis` attribute description: the timeout for the socket waiting for data after establishing the connection in milliseconds. a timeout value of zero is interpreted as an infinite timeout, and a negative value is interpreted as undefined.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=socket-timeout-millis)`

## syntax of the operation to get the `ssl-required` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `ssl-required` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `ssl-required` attribute description: whether the communication with the `openid` provider should be over `https`. valid values are: 'all' - to always require `https`, 'external' - to only require `https` for external requests, 'none' - if `https` is not required. this should be set to 'all' in production environments.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=ssl-required)`

## syntax of the operation to get the `token-minimum-time-to-live` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `token-minimum-time-to-live` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `token-minimum-time-to-live` attribute description: the subsystem will refresh the token if it will expire within the duration specified in '`token-minimum-time-to-live`' seconds. this value should never exceed the access token lifespan. if the value is set to `0` seconds, the subsystem will refresh the token only if the token has expired.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=token-minimum-time-to-live)`

## syntax of the operation to get the `token-signature-algorithm` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `token-signature-algorithm` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `token-signature-algorithm` attribute description: the token signature algorithm used by the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=token-signature-algorithm)`

## syntax of the operation to get the `token-store` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `token-store` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `token-store` attribute description: defines whether to store account information in an `http` session or in a cookie.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=token-store)`

## syntax of the operation to get the `truststore` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `truststore` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `truststore` attribute description: the path to the `truststore` to use when communicating with `keycloak` over `https`. prefix the path with '`classpath`:' to obtain the `truststore` from the `deployment's` `classpath`.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=truststore)`

## syntax of the operation to get the `truststore-password` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `truststore-password` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `truststore-password` attribute description: the password for the `truststore`.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=truststore-password)`

## syntax of the operation to get the `turn-off-change-session-id-on-login` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `turn-off-change-session-id-on-login` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `turn-off-change-session-id-on-login` attribute description: the session id is changed by default on a successful login. set this to 'true' if you want to turn this off.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=turn-off-change-session-id-on-login)`

## syntax of the operation to get the `use-resource-role-mappings` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `use-resource-role-mappings` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `use-resource-role-mappings` attribute description: if set to 'true', the subsystem will look inside the token for `application-level` role mappings for a user. if set to 'false', the subsystem will look at the `realm-level` for `user-role` mappings. this is optional.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=use-resource-role-mappings)`

## syntax of the operation to get the `verify-token-audience` attribute of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `verify-token-audience` attribute value of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `verify-token-audience` attribute description: if set to 'true', then during `bearer-only` authentication, the subsystem verifies if the token contains the client name defined as an audience. it is recommended to set the value to 'true' for improved security.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>:read-attribute(name=verify-token-audience)`

## syntax of the operation to get a `redirect-rewrite-rule` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>/redirect-rewrite-rule=<redirect-rewrite-rule name>:read-resource()`
* To get the list of all the `redirect-rewrite-rule` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem use '*' for `<redirect-rewrite-rule name>`.

## syntax of the operation to get the `replacement` attribute of the `redirect-rewrite-rule` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `replacement` attribute value of the `redirect-rewrite-rule` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `replacement` attribute description: the replacement string in the rewrite rule for the redirect uri.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>/redirect-rewrite-rule=<redirect-rewrite-rule name>:read-attribute(name=replacement)`

## syntax of the operation to get a `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>/credential=<credential name>:read-resource()`
* To get the list of all the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem use '*' for `<credential name>`.

## syntax of the operation to get the `algorithm` attribute of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `algorithm` attribute value of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `algorithm` attribute description: the credential signature algorithm used by the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>/credential=<credential name>:read-attribute(name=algorithm)`

## syntax of the operation to get the `client-key-alias` attribute of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-key-alias` attribute value of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `client-key-alias` attribute description: the client key alias when the credential provider is used.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>/credential=<credential name>:read-attribute(name=client-key-alias)`

## syntax of the operation to get the `client-key-password` attribute of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-key-password` attribute value of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `client-key-password` attribute description: the password for the client key when the credential provider is used. this is required if '`client-keystore-file`' has been specified.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>/credential=<credential name>:read-attribute(name=client-key-password)`

## syntax of the operation to get the `client-keystore-file` attribute of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore-file` attribute value of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `client-keystore-file` attribute description: the path to the client `keystore` when the credential provider is used. this is optional.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>/credential=<credential name>:read-attribute(name=client-keystore-file)`

## syntax of the operation to get the `client-keystore-password` attribute of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore-password` attribute value of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `client-keystore-password` attribute description: the password for the client `keystore` when the credential provider is used. this is required if '`client-keystore-file`' has been specified.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>/credential=<credential name>:read-attribute(name=client-keystore-password)`

## syntax of the operation to get the `client-keystore-type` attribute of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore-type` attribute value of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `client-keystore-type` attribute description: the client `keystore` type when the credential provider is used.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>/credential=<credential name>:read-attribute(name=client-keystore-type)`

## syntax of the operation to get the `secret` attribute of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `secret` attribute value of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `secret` attribute description: the client secret that was registered with the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>/credential=<credential name>:read-attribute(name=secret)`

## syntax of the operation to get the `token-timeout` attribute of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* Retrieve the `token-timeout` attribute value of the `credential` resource of the `secure-server` resource of the `elytron-oidc-client` subsystem
* `token-timeout` attribute description: the amount of time after which the token expires and can no longer be used to authenticate requests.
* operation: `/subsystem=elytron-oidc-client/secure-server=<secure-server name>/credential=<credential name>:read-attribute(name=token-timeout)`

## syntax of the operation to get a `secure-deployment` resource of the `elytron-oidc-client` subsystem
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-resource()`
* To get the list of all the `secure-deployment` resource of the `elytron-oidc-client` subsystem use '*' for `<secure-deployment name>`.

## syntax of the operation to get the `adapter-state-cookie-path` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `adapter-state-cookie-path` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `adapter-state-cookie-path` attribute description: if set, this defines the path used in cookies set by the subsystem. this is useful when deploying an application in the root context path.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=adapter-state-cookie-path)`

## syntax of the operation to get the `allow-any-hostname` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `allow-any-hostname` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `allow-any-hostname` attribute description: if set to 'true', `hostname` verification is skipped when communicating with the `openid` provider over `https`. this can be useful in testing environments. this should never be set to 'true' in production environments as it disables verification of `ssl` certificates.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=allow-any-hostname)`

## syntax of the operation to get the `always-refresh-token` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `always-refresh-token` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `always-refresh-token` attribute description: if set to 'true', the subsystem refreshes the token every time your application receives a web request, and a new request is sent to the `openid` provider to obtain a new access token. this can result in a higher load on the `openid` provider and may impact the performance of the application.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=always-refresh-token)`

## syntax of the operation to get the `auth-server-url` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `auth-server-url` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `auth-server-url` attribute description: the base `url` of the `keycloak` authorization server. this is `keycloak-specific`. it is recommended to use '`provider-url`' instead.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=auth-server-url)`

## syntax of the operation to get the `autodetect-bearer-only` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `autodetect-bearer-only` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `autodetect-bearer-only` attribute description: whether to `auto-detect` soap or rest clients based on headers like '`x-requested-with`', '`soapaction`' or 'accept'. if set to 'true', the subsystem sends an `http` `401` status code to unauthenticated soap or rest clients instead of redirecting them to the `openid` provider login page. set the value to 'true' if your application serves both applications and web services.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=autodetect-bearer-only)`

## syntax of the operation to get the `bearer-only` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `bearer-only` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `bearer-only` attribute description: enable `bearer-token` only authentication. set this to 'true' if your application serves only web services and does not authenticate users.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=bearer-only)`

## syntax of the operation to get the `client-id` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-id` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `client-id` attribute description: the unique identifier for a client application registered in the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=client-id)`

## syntax of the operation to get the `client-key-password` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-key-password` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `client-key-password` attribute description: the password for the client key. this is required if '`client-keystore`' has been specified.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=client-key-password)`

## syntax of the operation to get the `client-keystore` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `client-keystore` attribute description: the path to the client `keystore` to use when communicating with the `openid` provider over `https`. this is optional.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=client-keystore)`

## syntax of the operation to get the `client-keystore-password` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore-password` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `client-keystore-password` attribute description: the password for the client `keystore`. this is required if '`client-keystore`' has been specified.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=client-keystore-password)`

## syntax of the operation to get the `confidential-port` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `confidential-port` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `confidential-port` attribute description: the confidential port used by the `openid` provider when communicating securely over `ssl/tls`.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=confidential-port)`

## syntax of the operation to get the `connection-pool-size` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `connection-pool-size` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `connection-pool-size` attribute description: the connection pool size to use when communicating with the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=connection-pool-size)`

## syntax of the operation to get the `connection-timeout-millis` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `connection-timeout-millis` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `connection-timeout-millis` attribute description: the timeout for establishing a connection with the remote host in milliseconds. a timeout value of zero is interpreted as an infinite timeout, and a negative value is interpreted as undefined.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=connection-timeout-millis)`

## syntax of the operation to get the `connection-ttl-millis` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `connection-ttl-millis` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `connection-ttl-millis` attribute description: the amount of time in milliseconds for the connection to be kept alive. a value less than or equal to zero is interpreted as an infinite value.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=connection-ttl-millis)`

## syntax of the operation to get the `cors-allowed-headers` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-allowed-headers` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `cors-allowed-headers` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-allow-headers`' header. this should be a `comma-separated` string. this is optional. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=cors-allowed-headers)`

## syntax of the operation to get the `cors-allowed-methods` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-allowed-methods` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `cors-allowed-methods` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-allow-methods`' header. this should be a `comma-separated` string. this is optional. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=cors-allowed-methods)`

## syntax of the operation to get the `cors-exposed-headers` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-exposed-headers` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `cors-exposed-headers` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-expose-headers`' header. this should be a `comma-separated` string. this is optional. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=cors-exposed-headers)`

## syntax of the operation to get the `cors-max-age` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-max-age` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `cors-max-age` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-max-age`' header. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=cors-max-age)`

## syntax of the operation to get the `credential` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `credential` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `credential` attribute description: the credential used to communicate with the `openid` connect provider.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=credential)`

## syntax of the operation to get the `disable-trust-manager` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `disable-trust-manager` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `disable-trust-manager` attribute description: whether or not to make use of a trust manager when communicating with the `openid` provider over `https`. this is optional. this should be set to 'true' only during development and never in production as it disables verification of `ssl` certificates.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=disable-trust-manager)`

## syntax of the operation to get the `enable-basic-auth` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `enable-basic-auth` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `enable-basic-auth` attribute description: enable basic authentication. this is not supported in the current release.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=enable-basic-auth)`

## syntax of the operation to get the `enable-cors` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `enable-cors` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `enable-cors` attribute description: enable the `keycloak` `cross-origin` resource sharing (`cors`) support. this is optional. this is `keycloak-specific`.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=enable-cors)`

## syntax of the operation to get the `expose-token` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `expose-token` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `expose-token` attribute description: if set to 'true', an authenticated browser client can obtain the signed access token (through a `javascript` `http` invocation) via the `url` '`root/k_query_bearer_token`'. this is optional.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=expose-token)`

## syntax of the operation to get the `ignore-oauth-query-parameter` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `ignore-oauth-query-parameter` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `ignore-oauth-query-parameter` attribute description: disable query parameter parsing for the '`access_token`'. users will not be able to authenticate if they only pass in an '`access_token`'.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=ignore-oauth-query-parameter)`

## syntax of the operation to get the `min-time-between-jwks-requests` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `min-time-between-jwks-requests` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `min-time-between-jwks-requests` attribute description: if the subsystem recognizes a token signed by an unknown public key, it will try to download a new public key from the server. the subsystem `won't` try to download a public key if it already tried last in less than '`min-time-between-jwks-requests`' seconds.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=min-time-between-jwks-requests)`

## syntax of the operation to get the `principal-attribute` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `principal-attribute` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `principal-attribute` attribute description: indicates which value from the id token to use as the principal for the identity. the principal defaults to the value of the 'sub' if the token attribute is null.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=principal-attribute)`

## syntax of the operation to get the `provider` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `provider` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `provider` attribute description: the `openid` connect provider to use for authentication.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=provider)`

## syntax of the operation to get the `provider-url` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `provider-url` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `provider-url` attribute description: the `openid` provider `url`.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=provider-url)`

## syntax of the operation to get the `proxy-url` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `proxy-url` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `proxy-url` attribute description: the `url` for the `http` proxy if one is used.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=proxy-url)`

## syntax of the operation to get the `public-client` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `public-client` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `public-client` attribute description: if set to 'true', no client credentials are sent when communicating with the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=public-client)`

## syntax of the operation to get the `public-key-cache-ttl` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `public-key-cache-ttl` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `public-key-cache-ttl` attribute description: the maximum interval between two requests to retrieve new public keys in seconds. new public keys are downloaded when the subsystem recognizes a token signed by an unknown public key. even if the `token's` key is already known, new public keys are downloaded periodically as per the interval set here at least once.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=public-key-cache-ttl)`

## syntax of the operation to get the `realm` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `realm` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `realm` attribute description: the `keycloak` realm to use for authentication.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=realm)`

## syntax of the operation to get the `realm-public-key` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `realm-public-key` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `realm-public-key` attribute description: the public key of the `openid` provider in `pem` format. this is optional. it is not recommended to set it. if the public key is not set, the subsystem downloads the public key from the `openid` provider when needed. if the public key is set, the subsystem never downloads new keys from the `openid` provider, breaking the subsystem when the `openid` provider rotates its keys.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=realm-public-key)`

## syntax of the operation to get the `redirect-rewrite-rule` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `redirect-rewrite-rule` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `redirect-rewrite-rule` attribute description: the rewrite rule for the redirect uri. the rewrite rule is an object notation, where the key is a regular expression with which the redirect uri is matched and the value is the replacement string.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=redirect-rewrite-rule)`

## syntax of the operation to get the `register-node-at-startup` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `register-node-at-startup` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `register-node-at-startup` attribute description: if set to 'true', the subsystem sends a registration request to the `openid` provider. this attribute is useful only when your application is clustered.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=register-node-at-startup)`

## syntax of the operation to get the `register-node-period` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `register-node-period` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `register-node-period` attribute description: if '`register-node-at-startup`' is set to 'true', this specifies the frequency (in seconds) at which the node should be `re-registered`.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=register-node-period)`

## syntax of the operation to get the `resource` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `resource` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `resource` attribute description: the unique, legacy identifier for a client application registered in the `openid` provider. it is recommended to use the '`client-id`'.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=resource)`

## syntax of the operation to get the `socket-timeout-millis` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `socket-timeout-millis` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `socket-timeout-millis` attribute description: the timeout for the socket waiting for data after establishing the connection in milliseconds. a timeout value of zero is interpreted as an infinite timeout, and a negative value is interpreted as undefined.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=socket-timeout-millis)`

## syntax of the operation to get the `ssl-required` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `ssl-required` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `ssl-required` attribute description: whether the communication with the `openid` provider should be over `https`. valid values are: 'all' - to always require `https`, 'external' - to only require `https` for external requests, 'none' - if `https` is not required. this should be set to 'all' in production environments.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=ssl-required)`

## syntax of the operation to get the `token-minimum-time-to-live` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `token-minimum-time-to-live` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `token-minimum-time-to-live` attribute description: the subsystem will refresh the token if it will expire within the duration specified in '`token-minimum-time-to-live`' seconds. this value should never exceed the access token lifespan. if the value is set to `0` seconds, the subsystem will refresh the token only if the token has expired.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=token-minimum-time-to-live)`

## syntax of the operation to get the `token-signature-algorithm` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `token-signature-algorithm` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `token-signature-algorithm` attribute description: the token signature algorithm used by the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=token-signature-algorithm)`

## syntax of the operation to get the `token-store` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `token-store` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `token-store` attribute description: defines whether to store account information in an `http` session or in a cookie.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=token-store)`

## syntax of the operation to get the `truststore` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `truststore` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `truststore` attribute description: the path to the `truststore` to use when communicating with `keycloak` over `https`. prefix the path with '`classpath`:' to obtain the `truststore` from the `deployment's` `classpath`.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=truststore)`

## syntax of the operation to get the `truststore-password` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `truststore-password` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `truststore-password` attribute description: the password for the `truststore`.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=truststore-password)`

## syntax of the operation to get the `turn-off-change-session-id-on-login` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `turn-off-change-session-id-on-login` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `turn-off-change-session-id-on-login` attribute description: the session id is changed by default on a successful login. set this to 'true' if you want to turn this off.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=turn-off-change-session-id-on-login)`

## syntax of the operation to get the `use-resource-role-mappings` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `use-resource-role-mappings` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `use-resource-role-mappings` attribute description: if set to 'true', the subsystem will look inside the token for `application-level` role mappings for a user. if set to 'false', the subsystem will look at the `realm-level` for `user-role` mappings. this is optional.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=use-resource-role-mappings)`

## syntax of the operation to get the `verify-token-audience` attribute of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `verify-token-audience` attribute value of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `verify-token-audience` attribute description: if set to 'true', then during `bearer-only` authentication, the subsystem verifies if the token contains the client name defined as an audience. it is recommended to set the value to 'true' for improved security.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>:read-attribute(name=verify-token-audience)`

## syntax of the operation to get a `redirect-rewrite-rule` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>/redirect-rewrite-rule=<redirect-rewrite-rule name>:read-resource()`
* To get the list of all the `redirect-rewrite-rule` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem use '*' for `<redirect-rewrite-rule name>`.

## syntax of the operation to get the `replacement` attribute of the `redirect-rewrite-rule` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `replacement` attribute value of the `redirect-rewrite-rule` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `replacement` attribute description: the replacement string in the rewrite rule for the redirect uri.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>/redirect-rewrite-rule=<redirect-rewrite-rule name>:read-attribute(name=replacement)`

## syntax of the operation to get a `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>/credential=<credential name>:read-resource()`
* To get the list of all the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem use '*' for `<credential name>`.

## syntax of the operation to get the `algorithm` attribute of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `algorithm` attribute value of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `algorithm` attribute description: the credential signature algorithm used by the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>/credential=<credential name>:read-attribute(name=algorithm)`

## syntax of the operation to get the `client-key-alias` attribute of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-key-alias` attribute value of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `client-key-alias` attribute description: the client key alias when the credential provider is used.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>/credential=<credential name>:read-attribute(name=client-key-alias)`

## syntax of the operation to get the `client-key-password` attribute of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-key-password` attribute value of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `client-key-password` attribute description: the password for the client key when the credential provider is used. this is required if '`client-keystore-file`' has been specified.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>/credential=<credential name>:read-attribute(name=client-key-password)`

## syntax of the operation to get the `client-keystore-file` attribute of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore-file` attribute value of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `client-keystore-file` attribute description: the path to the client `keystore` when the credential provider is used. this is optional.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>/credential=<credential name>:read-attribute(name=client-keystore-file)`

## syntax of the operation to get the `client-keystore-password` attribute of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore-password` attribute value of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `client-keystore-password` attribute description: the password for the client `keystore` when the credential provider is used. this is required if '`client-keystore-file`' has been specified.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>/credential=<credential name>:read-attribute(name=client-keystore-password)`

## syntax of the operation to get the `client-keystore-type` attribute of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore-type` attribute value of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `client-keystore-type` attribute description: the client `keystore` type when the credential provider is used.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>/credential=<credential name>:read-attribute(name=client-keystore-type)`

## syntax of the operation to get the `secret` attribute of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `secret` attribute value of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `secret` attribute description: the client secret that was registered with the `openid` provider.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>/credential=<credential name>:read-attribute(name=secret)`

## syntax of the operation to get the `token-timeout` attribute of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* Retrieve the `token-timeout` attribute value of the `credential` resource of the `secure-deployment` resource of the `elytron-oidc-client` subsystem
* `token-timeout` attribute description: the amount of time after which the token expires and can no longer be used to authenticate requests.
* operation: `/subsystem=elytron-oidc-client/secure-deployment=<secure-deployment name>/credential=<credential name>:read-attribute(name=token-timeout)`

## syntax of the operation to get a `realm` resource of the `elytron-oidc-client` subsystem
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-resource()`
* To get the list of all the `realm` resource of the `elytron-oidc-client` subsystem use '*' for `<realm name>`.

## syntax of the operation to get the `allow-any-hostname` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `allow-any-hostname` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `allow-any-hostname` attribute description: if set to 'true', `hostname` verification will be skipped when communicating with `keycloak` over `https`. this can be useful in testing environments. this should never be set to 'true' in production environments as it disables verification of `ssl` certificates.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=allow-any-hostname)`

## syntax of the operation to get the `always-refresh-token` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `always-refresh-token` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `always-refresh-token` attribute description: if set to 'true', the subsystem refreshes the token every time your application receives a web request, and a new request is sent to `keycloak` to obtain a new access token. this can result in a higher load on the `keycloak` and may impact the performance of the application.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=always-refresh-token)`

## syntax of the operation to get the `auth-server-url` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `auth-server-url` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `auth-server-url` attribute description: the base `url` of the `keycloak` authorization server. it is recommended to use the '`provider-url`' instead.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=auth-server-url)`

## syntax of the operation to get the `autodetect-bearer-only` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `autodetect-bearer-only` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `autodetect-bearer-only` attribute description: whether to `auto-detect` soap or rest clients based on headers like '`x-requested-with`', '`soapaction`' or 'accept'. if set to 'true', the subsystem sends an `http` `401` status code to unauthenticated soap or rest clients instead of redirecting them to the `keycloak` login page. set the value to 'true' if your application serves both applications and web services.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=autodetect-bearer-only)`

## syntax of the operation to get the `client-key-password` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-key-password` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `client-key-password` attribute description: the password for the client key. this is required if '`client-keystore`' has been specified.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=client-key-password)`

## syntax of the operation to get the `client-keystore` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `client-keystore` attribute description: the path to the client `keystore` to use when communicating with `keycloak` over `https`. this is optional.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=client-keystore)`

## syntax of the operation to get the `client-keystore-password` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `client-keystore-password` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `client-keystore-password` attribute description: the password for the client `keystore`. this is required if '`client-keystore`' has been specified.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=client-keystore-password)`

## syntax of the operation to get the `confidential-port` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `confidential-port` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `confidential-port` attribute description: the confidential port used by `keycloak` when communicating securely over `ssl/tls`.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=confidential-port)`

## syntax of the operation to get the `connection-pool-size` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `connection-pool-size` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `connection-pool-size` attribute description: the connection pool size to use when communicating with `keycloak`.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=connection-pool-size)`

## syntax of the operation to get the `connection-timeout-millis` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `connection-timeout-millis` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `connection-timeout-millis` attribute description: the timeout for establishing a connection with the remote host in milliseconds. a timeout value of zero is interpreted as an infinite timeout, and a negative value is interpreted as undefined.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=connection-timeout-millis)`

## syntax of the operation to get the `connection-ttl-millis` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `connection-ttl-millis` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `connection-ttl-millis` attribute description: the amount of time in milliseconds for the connection to be kept alive. a value less than or equal to zero is interpreted as an infinite value.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=connection-ttl-millis)`

## syntax of the operation to get the `cors-allowed-headers` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-allowed-headers` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `cors-allowed-headers` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-allow-headers`' header. this should be a `comma-separated` string. this is optional. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=cors-allowed-headers)`

## syntax of the operation to get the `cors-allowed-methods` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-allowed-methods` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `cors-allowed-methods` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-allow-methods`' header. this should be a `comma-separated` string. this is optional. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=cors-allowed-methods)`

## syntax of the operation to get the `cors-exposed-headers` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-exposed-headers` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `cors-exposed-headers` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-expose-headers`' header. this should be a `comma-separated` string. this is optional. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=cors-exposed-headers)`

## syntax of the operation to get the `cors-max-age` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `cors-max-age` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `cors-max-age` attribute description: if `cross-origin` resource sharing (`cors`) is enabled, this sets the value of the '`access-control-max-age`' header. if not set, this header is not returned in `cors` responses.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=cors-max-age)`

## syntax of the operation to get the `disable-trust-manager` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `disable-trust-manager` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `disable-trust-manager` attribute description: whether or not to make use of a trust manager when communicating with `keycloak` over `https`. this is optional. this should be set to 'true' only during development and never in production as it disables verification of `ssl` certificates.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=disable-trust-manager)`

## syntax of the operation to get the `enable-cors` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `enable-cors` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `enable-cors` attribute description: enable the `keycloak` `cross-origin` resource sharing (`cors`) support. this is optional.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=enable-cors)`

## syntax of the operation to get the `expose-token` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `expose-token` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `expose-token` attribute description: if set to 'true', an authenticated browser client can obtain the signed access token (through a `javascript` `http` invocation) via the `url` '`root/k_query_bearer_token`'. this is optional.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=expose-token)`

## syntax of the operation to get the `ignore-oauth-query-parameter` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `ignore-oauth-query-parameter` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `ignore-oauth-query-parameter` attribute description: disable query parameter parsing for the '`access_token`'. users will not be able to authenticate if they only pass in an '`access_token`'.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=ignore-oauth-query-parameter)`

## syntax of the operation to get the `principal-attribute` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `principal-attribute` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `principal-attribute` attribute description: indicates which value from the id token to use as the principal for the identity. the principal defaults to the value of the 'sub' if the token attribute is null.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=principal-attribute)`

## syntax of the operation to get the `provider-url` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `provider-url` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `provider-url` attribute description: the `openid` provider `url`.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=provider-url)`

## syntax of the operation to get the `proxy-url` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `proxy-url` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `proxy-url` attribute description: the `url` for the `http` proxy if one is used.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=proxy-url)`

## syntax of the operation to get the `realm-public-key` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `realm-public-key` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `realm-public-key` attribute description: the public key of the `keycloak` realm in `pem` format. this is optional. it is not recommended to set it. if the public key is not set, the subsystem downloads the public key from `keycloak` when needed. if the public key is set, the subsystem never downloads new keys from `keycloak`, breaking the subsystem when `keycloak` rotates its keys.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=realm-public-key)`

## syntax of the operation to get the `register-node-at-startup` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `register-node-at-startup` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `register-node-at-startup` attribute description: if set to 'true', the subsystem sends a registration request to `keycloak`. this attribute is useful only when your application is clustered.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=register-node-at-startup)`

## syntax of the operation to get the `register-node-period` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `register-node-period` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `register-node-period` attribute description: if '`register-node-at-startup`' is set to 'true', this specifies the frequency (in seconds) at which the node should be `re-registered`.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=register-node-period)`

## syntax of the operation to get the `socket-timeout-millis` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `socket-timeout-millis` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `socket-timeout-millis` attribute description: the timeout for the socket waiting for data after establishing the connection in milliseconds. a timeout value of zero is interpreted as an infinite timeout, and a negative value is interpreted as undefined.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=socket-timeout-millis)`

## syntax of the operation to get the `ssl-required` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `ssl-required` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `ssl-required` attribute description: whether the communication with `keycloak` should be over `https`. valid values are: 'all' - to always require `https`, 'external' - to only require `https` for external requests, 'none' - if `https` is not required. this should be set to 'all' in production environments.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=ssl-required)`

## syntax of the operation to get the `token-signature-algorithm` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `token-signature-algorithm` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `token-signature-algorithm` attribute description: the token signature algorithm used by `keycloak`.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=token-signature-algorithm)`

## syntax of the operation to get the `token-store` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `token-store` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `token-store` attribute description: defines whether to store account information in an `http` session or in a cookie.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=token-store)`

## syntax of the operation to get the `truststore` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `truststore` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `truststore` attribute description: the path to the `truststore` to use when communicating with `keycloak` over `https`. prefix the path with '`classpath`:' to obtain the `truststore` from the `deployment's` `classpath`.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=truststore)`

## syntax of the operation to get the `truststore-password` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `truststore-password` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `truststore-password` attribute description: the password for the `truststore`.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=truststore-password)`

## syntax of the operation to get the `verify-token-audience` attribute of the `realm` resource of the `elytron-oidc-client` subsystem
* Retrieve the `verify-token-audience` attribute value of the `realm` resource of the `elytron-oidc-client` subsystem
* `verify-token-audience` attribute description: if set to 'true', then during `bearer-only` authentication, the subsystem verifies if the token contains the client name defined as an audience. it is recommended to set to 'true' for improved security.
* operation: `/subsystem=elytron-oidc-client/realm=<realm name>:read-attribute(name=verify-token-audience)`

## syntax of the operation to get the `default-worker` attribute of the `io` subsystem
* Retrieve the `default-worker` attribute value of the `io` subsystem
* `default-worker` attribute description: specifies the default `i/o` worker
* operation: `/subsystem=io:read-attribute(name=default-worker)`

## syntax of the operation to get a `worker` resource of the `io` subsystem
* operation: `/subsystem=io/worker=<worker name>:read-resource()`
* To get the list of all the `worker` resource of the `io` subsystem use '*' for `<worker name>`.

## syntax of the operation to get the `busy-task-thread-count` attribute of the `worker` resource of the `io` subsystem
* Retrieve the `busy-task-thread-count` attribute value of the `worker` resource of the `io` subsystem
* `busy-task-thread-count` attribute description: an estimate of busy threads in the task worker thread pool
* operation: `/subsystem=io/worker=<worker name>:read-attribute(name=busy-task-thread-count)`

## syntax of the operation to get the `core-pool-size` attribute of the `worker` resource of the `io` subsystem
* Retrieve the `core-pool-size` attribute value of the `worker` resource of the `io` subsystem
* `core-pool-size` attribute description: minimum number of threads to keep in the underlying thread pool even if they are idle. threads over this limit will be terminated over time specified by `task-keepalive` attribute.
* operation: `/subsystem=io/worker=<worker name>:read-attribute(name=core-pool-size)`

## syntax of the operation to get the `io-thread-count` attribute of the `worker` resource of the `io` subsystem
* Retrieve the `io-thread-count` attribute value of the `worker` resource of the `io` subsystem
* `io-thread-count` attribute description: `i/o` thread count
* operation: `/subsystem=io/worker=<worker name>:read-attribute(name=io-thread-count)`

## syntax of the operation to get the `io-threads` attribute of the `worker` resource of the `io` subsystem
* Retrieve the `io-threads` attribute value of the `worker` resource of the `io` subsystem
* `io-threads` attribute description: specify the number of `i/o` threads to create for the worker.  if not specified, a default will be chosen, which is calculated by `cpucount` * `2`
* operation: `/subsystem=io/worker=<worker name>:read-attribute(name=io-threads)`

## syntax of the operation to get the `max-pool-size` attribute of the `worker` resource of the `io` subsystem
* Retrieve the `max-pool-size` attribute value of the `worker` resource of the `io` subsystem
* `max-pool-size` attribute description: the maximum number of threads allowed in the worker task thread pool. depending on the pool implementation, when this limit is reached tasks which cannot be queued may be rejected. this can be configured using the '`task-max-threads`' attribute; see the description of that attribute for details on how this value is determined.
* operation: `/subsystem=io/worker=<worker name>:read-attribute(name=max-pool-size)`

## syntax of the operation to get the `queue-size` attribute of the `worker` resource of the `io` subsystem
* Retrieve the `queue-size` attribute value of the `worker` resource of the `io` subsystem
* `queue-size` attribute description: an estimate of the number of tasks in the worker queue.
* operation: `/subsystem=io/worker=<worker name>:read-attribute(name=queue-size)`

## syntax of the operation to get the `shutdown-requested` attribute of the `worker` resource of the `io` subsystem
* Retrieve the `shutdown-requested` attribute value of the `worker` resource of the `io` subsystem
* `shutdown-requested` attribute description: true is shutdown of the pool was requested
* operation: `/subsystem=io/worker=<worker name>:read-attribute(name=shutdown-requested)`

## syntax of the operation to get the `stack-size` attribute of the `worker` resource of the `io` subsystem
* Retrieve the `stack-size` attribute value of the `worker` resource of the `io` subsystem
* `stack-size` attribute description: the stack size (in bytes) to attempt to use for worker threads.
* operation: `/subsystem=io/worker=<worker name>:read-attribute(name=stack-size)`

## syntax of the operation to get the `task-core-threads` attribute of the `worker` resource of the `io` subsystem
* Retrieve the `task-core-threads` attribute value of the `worker` resource of the `io` subsystem
* `task-core-threads` attribute description: specify the starting number of threads for the worker task thread pool.
* operation: `/subsystem=io/worker=<worker name>:read-attribute(name=task-core-threads)`

## syntax of the operation to get the `task-keepalive` attribute of the `worker` resource of the `io` subsystem
* Retrieve the `task-keepalive` attribute value of the `worker` resource of the `io` subsystem
* `task-keepalive` attribute description: specify the number of milliseconds to keep `non-core` task threads alive.
* operation: `/subsystem=io/worker=<worker name>:read-attribute(name=task-keepalive)`

## syntax of the operation to get the `task-max-threads` attribute of the `worker` resource of the `io` subsystem
* Retrieve the `task-max-threads` attribute value of the `worker` resource of the `io` subsystem
* `task-max-threads` attribute description: specify the maximum number of threads for the worker task thread `pool.if` not set, a default value used which is calculated by the formula `cpucount` * `16,as` long as the `maxfiledescriptorcount` `jmx` property allows that number; otherwise the calculation takes that max into account and adjusts accordingly.
* operation: `/subsystem=io/worker=<worker name>:read-attribute(name=task-max-threads)`

## syntax of the operation to get a `outbound-bind-address` resource of the `worker` resource of the `io` subsystem
* operation: `/subsystem=io/worker=<worker name>/outbound-bind-address=<outbound-bind-address name>:read-resource()`
* To get the list of all the `outbound-bind-address` resource of the `worker` resource of the `io` subsystem use '*' for `<outbound-bind-address name>`.

## syntax of the operation to get the `bind-address` attribute of the `outbound-bind-address` resource of the `worker` resource of the `io` subsystem
* Retrieve the `bind-address` attribute value of the `outbound-bind-address` resource of the `worker` resource of the `io` subsystem
* `bind-address` attribute description: the address to bind to when the destination address matches
* operation: `/subsystem=io/worker=<worker name>/outbound-bind-address=<outbound-bind-address name>:read-attribute(name=bind-address)`

## syntax of the operation to get the `bind-port` attribute of the `outbound-bind-address` resource of the `worker` resource of the `io` subsystem
* Retrieve the `bind-port` attribute value of the `outbound-bind-address` resource of the `worker` resource of the `io` subsystem
* `bind-port` attribute description: the port number to bind to when the destination address matches
* operation: `/subsystem=io/worker=<worker name>/outbound-bind-address=<outbound-bind-address name>:read-attribute(name=bind-port)`

## syntax of the operation to get the `match` attribute of the `outbound-bind-address` resource of the `worker` resource of the `io` subsystem
* Retrieve the `match` attribute value of the `outbound-bind-address` resource of the `worker` resource of the `io` subsystem
* `match` attribute description: the destination address range to match
* operation: `/subsystem=io/worker=<worker name>/outbound-bind-address=<outbound-bind-address name>:read-attribute(name=match)`

## syntax of the operation to get a `server` resource of the `worker` resource of the `io` subsystem
* operation: `/subsystem=io/worker=<worker name>/server=<server name>:read-resource()`
* To get the list of all the `server` resource of the `worker` resource of the `io` subsystem use '*' for `<server name>`.

## syntax of the operation to get the `connection-count` attribute of the `server` resource of the `worker` resource of the `io` subsystem
* Retrieve the `connection-count` attribute value of the `server` resource of the `worker` resource of the `io` subsystem
* `connection-count` attribute description: estimate of the current connection count
* operation: `/subsystem=io/worker=<worker name>/server=<server name>:read-attribute(name=connection-count)`

## syntax of the operation to get the `connection-limit-high-water-mark` attribute of the `server` resource of the `worker` resource of the `io` subsystem
* Retrieve the `connection-limit-high-water-mark` attribute value of the `server` resource of the `worker` resource of the `io` subsystem
* `connection-limit-high-water-mark` attribute description: if the connection count hits this number, no new connections will be accepted until the count drops below the `low-water` mark.
* operation: `/subsystem=io/worker=<worker name>/server=<server name>:read-attribute(name=connection-limit-high-water-mark)`

## syntax of the operation to get the `connection-limit-low-water-mark` attribute of the `server` resource of the `worker` resource of the `io` subsystem
* Retrieve the `connection-limit-low-water-mark` attribute value of the `server` resource of the `worker` resource of the `io` subsystem
* `connection-limit-low-water-mark` attribute description: if the connection count has previously hit the high water mark, once it drops back down below this count, connections will be accepted again.
* operation: `/subsystem=io/worker=<worker name>/server=<server name>:read-attribute(name=connection-limit-low-water-mark)`

## syntax of the operation to get a `buffer-pool` resource of the `io` subsystem
* operation: `/subsystem=io/buffer-pool=<buffer-pool name>:read-resource()`
* To get the list of all the `buffer-pool` resource of the `io` subsystem use '*' for `<buffer-pool name>`.

## syntax of the operation to get the `buffer-size` attribute of the `buffer-pool` resource of the `io` subsystem
* Retrieve the `buffer-size` attribute value of the `buffer-pool` resource of the `io` subsystem
* `buffer-size` attribute description: the size of each buffer slice in bytes, if not set optimal value is calculated based on available ram resources in your system.
* operation: `/subsystem=io/buffer-pool=<buffer-pool name>:read-attribute(name=buffer-size)`

## syntax of the operation to get the `buffers-per-slice` attribute of the `buffer-pool` resource of the `io` subsystem
* Retrieve the `buffers-per-slice` attribute value of the `buffer-pool` resource of the `io` subsystem
* `buffers-per-slice` attribute description: how many buffers per slice, if not set optimal value is calculated based on available ram resources in your system.
* operation: `/subsystem=io/buffer-pool=<buffer-pool name>:read-attribute(name=buffers-per-slice)`

## syntax of the operation to get the `direct-buffers` attribute of the `buffer-pool` resource of the `io` subsystem
* Retrieve the `direct-buffers` attribute value of the `buffer-pool` resource of the `io` subsystem
* `direct-buffers` attribute description: does the buffer pool use direct buffers, some platforms `don't` support direct buffers
* operation: `/subsystem=io/buffer-pool=<buffer-pool name>:read-attribute(name=direct-buffers)`

## syntax of the operation to get the `allow-ejb-name-regex` attribute of the `ejb3` subsystem
* Retrieve the `allow-ejb-name-regex` attribute value of the `ejb3` subsystem
* `allow-ejb-name-regex` attribute description: if this is true then regular expressions can be used in interceptor bindings to allow interceptors to be mapped to all beans that match the regular expression
* operation: `/subsystem=ejb3:read-attribute(name=allow-ejb-name-regex)`

## syntax of the operation to get the `client-interceptors` attribute of the `ejb3` subsystem
* Retrieve the `client-interceptors` attribute value of the `ejb3` subsystem
* `client-interceptors` attribute description: client interceptor definitions.
* operation: `/subsystem=ejb3:read-attribute(name=client-interceptors)`

## syntax of the operation to get the `default-clustered-sfsb-cache` attribute of the `ejb3` subsystem
* Retrieve the `default-clustered-sfsb-cache` attribute value of the `ejb3` subsystem
* `default-clustered-sfsb-cache` attribute description: name of the default stateful bean cache, which will be applicable to all clustered stateful jakarta enterprise beans, unless overridden at the deployment or bean level
* operation: `/subsystem=ejb3:read-attribute(name=default-clustered-sfsb-cache)`

## syntax of the operation to get the `default-distinct-name` attribute of the `ejb3` subsystem
* Retrieve the `default-distinct-name` attribute value of the `ejb3` subsystem
* `default-distinct-name` attribute description: the default distinct name that is applied to every jakarta enterprise beans deployed on this server
* operation: `/subsystem=ejb3:read-attribute(name=default-distinct-name)`

## syntax of the operation to get the `default-entity-bean-instance-pool` attribute of the `ejb3` subsystem
* Retrieve the `default-entity-bean-instance-pool` attribute value of the `ejb3` subsystem
* `default-entity-bean-instance-pool` attribute description: name of the default entity bean instance pool, which will be applicable to all entity beans, unless overridden at the deployment or bean level
* operation: `/subsystem=ejb3:read-attribute(name=default-entity-bean-instance-pool)`

## syntax of the operation to get the `default-entity-bean-optimistic-locking` attribute of the `ejb3` subsystem
* Retrieve the `default-entity-bean-optimistic-locking` attribute value of the `ejb3` subsystem
* `default-entity-bean-optimistic-locking` attribute description: if set to true entity beans will use optimistic locking by default
* operation: `/subsystem=ejb3:read-attribute(name=default-entity-bean-optimistic-locking)`

## syntax of the operation to get the `default-mdb-instance-pool` attribute of the `ejb3` subsystem
* Retrieve the `default-mdb-instance-pool` attribute value of the `ejb3` subsystem
* `default-mdb-instance-pool` attribute description: name of the default `mdb` instance pool, which will be applicable to all `mdbs`, unless overridden at the deployment or bean level
* operation: `/subsystem=ejb3:read-attribute(name=default-mdb-instance-pool)`

## syntax of the operation to get the `default-missing-method-permissions-deny-access` attribute of the `ejb3` subsystem
* Retrieve the `default-missing-method-permissions-deny-access` attribute value of the `ejb3` subsystem
* `default-missing-method-permissions-deny-access` attribute description: if this is set to true then methods on jakarta enterprise beans with a security domain specified or with other methods with security `metadata` will have an implicit @`denyall` unless other security `metadata` is present
* operation: `/subsystem=ejb3:read-attribute(name=default-missing-method-permissions-deny-access)`

## syntax of the operation to get the `default-resource-adapter-name` attribute of the `ejb3` subsystem
* Retrieve the `default-resource-adapter-name` attribute value of the `ejb3` subsystem
* `default-resource-adapter-name` attribute description: name of the default resource adapter name that will be used by `mdbs`, unless overridden at the deployment or bean level
* operation: `/subsystem=ejb3:read-attribute(name=default-resource-adapter-name)`

## syntax of the operation to get the `default-security-domain` attribute of the `ejb3` subsystem
* Retrieve the `default-security-domain` attribute value of the `ejb3` subsystem
* `default-security-domain` attribute description: the default security domain that will be used for jakarta enterprise beans if the bean `doesn't` explicitly specify one
* operation: `/subsystem=ejb3:read-attribute(name=default-security-domain)`

## syntax of the operation to get the `default-sfsb-cache` attribute of the `ejb3` subsystem
* Retrieve the `default-sfsb-cache` attribute value of the `ejb3` subsystem
* `default-sfsb-cache` attribute description: name of the default stateful bean cache, which will be applicable to all stateful jakarta enterprise beans, unless overridden at the deployment or bean level
* operation: `/subsystem=ejb3:read-attribute(name=default-sfsb-cache)`

## syntax of the operation to get the `default-sfsb-passivation-disabled-cache` attribute of the `ejb3` subsystem
* Retrieve the `default-sfsb-passivation-disabled-cache` attribute value of the `ejb3` subsystem
* `default-sfsb-passivation-disabled-cache` attribute description: name of the default stateful bean cache, which will be applicable to all stateful jakarta enterprise beans that have passivation disabled. each deployment or jakarta enterprise beans can optionally override this cache name.
* operation: `/subsystem=ejb3:read-attribute(name=default-sfsb-passivation-disabled-cache)`

## syntax of the operation to get the `default-singleton-bean-access-timeout` attribute of the `ejb3` subsystem
* Retrieve the `default-singleton-bean-access-timeout` attribute value of the `ejb3` subsystem
* `default-singleton-bean-access-timeout` attribute description: the default access timeout for singleton beans
* operation: `/subsystem=ejb3:read-attribute(name=default-singleton-bean-access-timeout)`

## syntax of the operation to get the `default-slsb-instance-pool` attribute of the `ejb3` subsystem
* Retrieve the `default-slsb-instance-pool` attribute value of the `ejb3` subsystem
* `default-slsb-instance-pool` attribute description: name of the default stateless bean instance pool, which will be applicable to all stateless jakarta enterprise beans, unless overridden at the deployment or bean level
* operation: `/subsystem=ejb3:read-attribute(name=default-slsb-instance-pool)`

## syntax of the operation to get the `default-stateful-bean-access-timeout` attribute of the `ejb3` subsystem
* Retrieve the `default-stateful-bean-access-timeout` attribute value of the `ejb3` subsystem
* `default-stateful-bean-access-timeout` attribute description: the default access timeout for stateful beans
* operation: `/subsystem=ejb3:read-attribute(name=default-stateful-bean-access-timeout)`

## syntax of the operation to get the `default-stateful-bean-session-timeout` attribute of the `ejb3` subsystem
* Retrieve the `default-stateful-bean-session-timeout` attribute value of the `ejb3` subsystem
* `default-stateful-bean-session-timeout` attribute description: the default session timeout for stateful beans. modification to this attribute takes effect immediately for subsequent deployments; for jakarta enterprise beans already deployed, redeploying is needed to use the new value.
* operation: `/subsystem=ejb3:read-attribute(name=default-stateful-bean-session-timeout)`

## syntax of the operation to get the `disable-default-ejb-permissions` attribute of the `ejb3` subsystem
* Retrieve the `disable-default-ejb-permissions` attribute value of the `ejb3` subsystem
* `disable-default-ejb-permissions` attribute description: this deprecated attribute has no effect and will be removed in a future release; it may never be set to a "false" value
* operation: `/subsystem=ejb3:read-attribute(name=disable-default-ejb-permissions)`

## syntax of the operation to get the `enable-graceful-txn-shutdown` attribute of the `ejb3` subsystem
* Retrieve the `enable-graceful-txn-shutdown` attribute value of the `ejb3` subsystem
* `enable-graceful-txn-shutdown` attribute description: enabling `txn` graceful shutdown will make the server wait for active remote jakarta enterprise `beans-related` transactions to complete before suspending. for that reason, if the server is running on a cluster, the suspending cluster node may receive jakarta enterprise beans requests until all active remote transactions are complete. to avoid this behavior, omit this tag. this attribute has no effect on local jakarta enterprise `beans-related` transactions.
* operation: `/subsystem=ejb3:read-attribute(name=enable-graceful-txn-shutdown)`

## syntax of the operation to get the `enable-statistics` attribute of the `ejb3` subsystem
* Retrieve the `enable-statistics` attribute value of the `ejb3` subsystem
* `enable-statistics` attribute description: if set to true, enable the collection of invocation statistics. deprecated in favour of "`statistics-enabled`"
* operation: `/subsystem=ejb3:read-attribute(name=enable-statistics)`

## syntax of the operation to get the `in-vm-remote-interface-invocation-pass-by-value` attribute of the `ejb3` subsystem
* Retrieve the `in-vm-remote-interface-invocation-pass-by-value` attribute value of the `ejb3` subsystem
* `in-vm-remote-interface-invocation-pass-by-value` attribute description: if set to false, the parameters to invocations on the remote interface of a jakarta enterprise beans, will be passed by reference. else, the parameters will be passed by value.
* operation: `/subsystem=ejb3:read-attribute(name=in-vm-remote-interface-invocation-pass-by-value)`

## syntax of the operation to get the `log-system-exceptions` attribute of the `ejb3` subsystem
* Retrieve the `log-system-exceptions` attribute value of the `ejb3` subsystem
* `log-system-exceptions` attribute description: if this is true then all jakarta enterprise beans system (not application) exceptions will be logged. the jakarta enterprise beans spec mandates this behaviour, however, it is not recommended as it will often result in exceptions being logged twice (once by the jakarta enterprise beans and once by the calling code)
* operation: `/subsystem=ejb3:read-attribute(name=log-system-exceptions)`

## syntax of the operation to get the `server-interceptors` attribute of the `ejb3` subsystem
* Retrieve the `server-interceptors` attribute value of the `ejb3` subsystem
* `server-interceptors` attribute description: server interceptor definitions.
* operation: `/subsystem=ejb3:read-attribute(name=server-interceptors)`

## syntax of the operation to get the `statistics-enabled` attribute of the `ejb3` subsystem
* Retrieve the `statistics-enabled` attribute value of the `ejb3` subsystem
* `statistics-enabled` attribute description: if set to true, enable the collection of invocation statistics.
* operation: `/subsystem=ejb3:read-attribute(name=statistics-enabled)`

## syntax of the operation to get a `remoting-profile` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/remoting-profile=<remoting-profile name>:read-resource()`
* To get the list of all the `remoting-profile` resource of the `ejb3` subsystem use '*' for `<remoting-profile name>`.

## syntax of the operation to get the `exclude-local-receiver` attribute of the `remoting-profile` resource of the `ejb3` subsystem
* Retrieve the `exclude-local-receiver` attribute value of the `remoting-profile` resource of the `ejb3` subsystem
* `exclude-local-receiver` attribute description: if set no local receiver is used in this profile
* operation: `/subsystem=ejb3/remoting-profile=<remoting-profile name>:read-attribute(name=exclude-local-receiver)`

## syntax of the operation to get the `local-receiver-pass-by-value` attribute of the `remoting-profile` resource of the `ejb3` subsystem
* Retrieve the `local-receiver-pass-by-value` attribute value of the `remoting-profile` resource of the `ejb3` subsystem
* `local-receiver-pass-by-value` attribute description: if set local receiver will pass `ejb` beans by value
* operation: `/subsystem=ejb3/remoting-profile=<remoting-profile name>:read-attribute(name=local-receiver-pass-by-value)`

## syntax of the operation to get the `static-ejb-discovery` attribute of the `remoting-profile` resource of the `ejb3` subsystem
* Retrieve the `static-ejb-discovery` attribute value of the `remoting-profile` resource of the `ejb3` subsystem
* `static-ejb-discovery` attribute description: describes static discovery `config` for jakarta enterprise `beans's`
* operation: `/subsystem=ejb3/remoting-profile=<remoting-profile name>:read-attribute(name=static-ejb-discovery)`

## syntax of the operation to get a `remoting-ejb-receiver` resource of the `remoting-profile` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/remoting-profile=<remoting-profile name>/remoting-ejb-receiver=<remoting-ejb-receiver name>:read-resource()`
* To get the list of all the `remoting-ejb-receiver` resource of the `remoting-profile` resource of the `ejb3` subsystem use '*' for `<remoting-ejb-receiver name>`.

## syntax of the operation to get the `connect-timeout` attribute of the `remoting-ejb-receiver` resource of the `remoting-profile` resource of the `ejb3` subsystem
* Retrieve the `connect-timeout` attribute value of the `remoting-ejb-receiver` resource of the `remoting-profile` resource of the `ejb3` subsystem
* `connect-timeout` attribute description: `remoting` `ejb` receiver connect timeout
* operation: `/subsystem=ejb3/remoting-profile=<remoting-profile name>/remoting-ejb-receiver=<remoting-ejb-receiver name>:read-attribute(name=connect-timeout)`

## syntax of the operation to get the `outbound-connection-ref` attribute of the `remoting-ejb-receiver` resource of the `remoting-profile` resource of the `ejb3` subsystem
* Retrieve the `outbound-connection-ref` attribute value of the `remoting-ejb-receiver` resource of the `remoting-profile` resource of the `ejb3` subsystem
* `outbound-connection-ref` attribute description: name of outbound connection that will be used by the `ejb` receiver
* operation: `/subsystem=ejb3/remoting-profile=<remoting-profile name>/remoting-ejb-receiver=<remoting-ejb-receiver name>:read-attribute(name=outbound-connection-ref)`

## syntax of the operation to get a `channel-creation-options` resource of the `remoting-ejb-receiver` resource of the `remoting-profile` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/remoting-profile=<remoting-profile name>/remoting-ejb-receiver=<remoting-ejb-receiver name>/channel-creation-options=<channel-creation-options name>:read-resource()`
* To get the list of all the `channel-creation-options` resource of the `remoting-ejb-receiver` resource of the `remoting-profile` resource of the `ejb3` subsystem use '*' for `<channel-creation-options name>`.

## syntax of the operation to get the `type` attribute of the `channel-creation-options` resource of the `remoting-ejb-receiver` resource of the `remoting-profile` resource of the `ejb3` subsystem
* Retrieve the `type` attribute value of the `channel-creation-options` resource of the `remoting-ejb-receiver` resource of the `remoting-profile` resource of the `ejb3` subsystem
* `type` attribute description: the type of the channel creation option
* operation: `/subsystem=ejb3/remoting-profile=<remoting-profile name>/remoting-ejb-receiver=<remoting-ejb-receiver name>/channel-creation-options=<channel-creation-options name>:read-attribute(name=type)`

## syntax of the operation to get the `value` attribute of the `channel-creation-options` resource of the `remoting-ejb-receiver` resource of the `remoting-profile` resource of the `ejb3` subsystem
* Retrieve the `value` attribute value of the `channel-creation-options` resource of the `remoting-ejb-receiver` resource of the `remoting-profile` resource of the `ejb3` subsystem
* `value` attribute description: the value for the jakarta enterprise beans remote channel creation option
* operation: `/subsystem=ejb3/remoting-profile=<remoting-profile name>/remoting-ejb-receiver=<remoting-ejb-receiver name>/channel-creation-options=<channel-creation-options name>:read-attribute(name=value)`

## syntax of the operation to get a `remote-http-connection` resource of the `remoting-profile` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/remoting-profile=<remoting-profile name>/remote-http-connection=<remote-http-connection name>:read-resource()`
* To get the list of all the `remote-http-connection` resource of the `remoting-profile` resource of the `ejb3` subsystem use '*' for `<remote-http-connection name>`.

## syntax of the operation to get the `uri` attribute of the `remote-http-connection` resource of the `remoting-profile` resource of the `ejb3` subsystem
* Retrieve the `uri` attribute value of the `remote-http-connection` resource of the `remoting-profile` resource of the `ejb3` subsystem
* `uri` attribute description: remote `http` connection uri
* operation: `/subsystem=ejb3/remoting-profile=<remoting-profile name>/remote-http-connection=<remote-http-connection name>:read-attribute(name=uri)`

## syntax of the operation to get a `mdb-delivery-group` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/mdb-delivery-group=<mdb-delivery-group name>:read-resource()`
* To get the list of all the `mdb-delivery-group` resource of the `ejb3` subsystem use '*' for `<mdb-delivery-group name>`.

## syntax of the operation to get the `active` attribute of the `mdb-delivery-group` resource of the `ejb3` subsystem
* Retrieve the `active` attribute value of the `mdb-delivery-group` resource of the `ejb3` subsystem
* `active` attribute description: indicates if delivery for all `mdbs` belonging to this group is active
* operation: `/subsystem=ejb3/mdb-delivery-group=<mdb-delivery-group name>:read-attribute(name=active)`

## syntax of the operation to get a `application-security-domain` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/application-security-domain=<application-security-domain name>:read-resource()`
* To get the list of all the `application-security-domain` resource of the `ejb3` subsystem use '*' for `<application-security-domain name>`.

## syntax of the operation to get the `enable-jacc` attribute of the `application-security-domain` resource of the `ejb3` subsystem
* Retrieve the `enable-jacc` attribute value of the `application-security-domain` resource of the `ejb3` subsystem
* `enable-jacc` attribute description: enable authorization using jakarta authorization
* operation: `/subsystem=ejb3/application-security-domain=<application-security-domain name>:read-attribute(name=enable-jacc)`

## syntax of the operation to get the `legacy-compliant-principal-propagation` attribute of the `application-security-domain` resource of the `ejb3` subsystem
* Retrieve the `legacy-compliant-principal-propagation` attribute value of the `application-security-domain` resource of the `ejb3` subsystem
* `legacy-compliant-principal-propagation` attribute description: option to switch between legacy compliant principal propagation or elytron principal propagation
* operation: `/subsystem=ejb3/application-security-domain=<application-security-domain name>:read-attribute(name=legacy-compliant-principal-propagation)`

## syntax of the operation to get the `referencing-deployments` attribute of the `application-security-domain` resource of the `ejb3` subsystem
* Retrieve the `referencing-deployments` attribute value of the `application-security-domain` resource of the `ejb3` subsystem
* `referencing-deployments` attribute description: the deployments currently referencing this mapping
* operation: `/subsystem=ejb3/application-security-domain=<application-security-domain name>:read-attribute(name=referencing-deployments)`

## syntax of the operation to get the `security-domain` attribute of the `application-security-domain` resource of the `ejb3` subsystem
* Retrieve the `security-domain` attribute value of the `application-security-domain` resource of the `ejb3` subsystem
* `security-domain` attribute description: the elytron security domain to be used by deployments that reference the mapped security domain
* operation: `/subsystem=ejb3/application-security-domain=<application-security-domain name>:read-attribute(name=security-domain)`

## syntax of the operation to get a `thread-pool` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/thread-pool=<thread-pool name>:read-resource()`
* To get the list of all the `thread-pool` resource of the `ejb3` subsystem use '*' for `<thread-pool name>`.

## syntax of the operation to get the `active-count` attribute of the `thread-pool` resource of the `ejb3` subsystem
* Retrieve the `active-count` attribute value of the `thread-pool` resource of the `ejb3` subsystem
* `active-count` attribute description: the approximate number of threads that are actively executing tasks.
* operation: `/subsystem=ejb3/thread-pool=<thread-pool name>:read-attribute(name=active-count)`

## syntax of the operation to get the `completed-task-count` attribute of the `thread-pool` resource of the `ejb3` subsystem
* Retrieve the `completed-task-count` attribute value of the `thread-pool` resource of the `ejb3` subsystem
* `completed-task-count` attribute description: the approximate total number of tasks that have completed execution.
* operation: `/subsystem=ejb3/thread-pool=<thread-pool name>:read-attribute(name=completed-task-count)`

## syntax of the operation to get the `core-threads` attribute of the `thread-pool` resource of the `ejb3` subsystem
* Retrieve the `core-threads` attribute value of the `thread-pool` resource of the `ejb3` subsystem
* `core-threads` attribute description: the core thread pool size which is smaller than the maximum pool size. if undefined, the core thread pool size is the same as the maximum thread pool size.
* operation: `/subsystem=ejb3/thread-pool=<thread-pool name>:read-attribute(name=core-threads)`

## syntax of the operation to get the `current-thread-count` attribute of the `thread-pool` resource of the `ejb3` subsystem
* Retrieve the `current-thread-count` attribute value of the `thread-pool` resource of the `ejb3` subsystem
* `current-thread-count` attribute description: the current number of threads in the pool.
* operation: `/subsystem=ejb3/thread-pool=<thread-pool name>:read-attribute(name=current-thread-count)`

## syntax of the operation to get the `keepalive-time` attribute of the `thread-pool` resource of the `ejb3` subsystem
* Retrieve the `keepalive-time` attribute value of the `thread-pool` resource of the `ejb3` subsystem
* `keepalive-time` attribute description: used to specify the amount of time that pool threads should be kept running when idle; if not specified, threads will run until the executor is shut down.
* operation: `/subsystem=ejb3/thread-pool=<thread-pool name>:read-attribute(name=keepalive-time)`

## syntax of the operation to get the `largest-thread-count` attribute of the `thread-pool` resource of the `ejb3` subsystem
* Retrieve the `largest-thread-count` attribute value of the `thread-pool` resource of the `ejb3` subsystem
* `largest-thread-count` attribute description: the largest number of threads that have ever simultaneously been in the pool.
* operation: `/subsystem=ejb3/thread-pool=<thread-pool name>:read-attribute(name=largest-thread-count)`

## syntax of the operation to get the `max-threads` attribute of the `thread-pool` resource of the `ejb3` subsystem
* Retrieve the `max-threads` attribute value of the `thread-pool` resource of the `ejb3` subsystem
* `max-threads` attribute description: the maximum thread pool size.
* operation: `/subsystem=ejb3/thread-pool=<thread-pool name>:read-attribute(name=max-threads)`

## syntax of the operation to get the `name` attribute of the `thread-pool` resource of the `ejb3` subsystem
* Retrieve the `name` attribute value of the `thread-pool` resource of the `ejb3` subsystem
* `name` attribute description: the name of the thread pool.
* operation: `/subsystem=ejb3/thread-pool=<thread-pool name>:read-attribute(name=name)`

## syntax of the operation to get the `queue-size` attribute of the `thread-pool` resource of the `ejb3` subsystem
* Retrieve the `queue-size` attribute value of the `thread-pool` resource of the `ejb3` subsystem
* `queue-size` attribute description: the queue size.
* operation: `/subsystem=ejb3/thread-pool=<thread-pool name>:read-attribute(name=queue-size)`

## syntax of the operation to get the `rejected-count` attribute of the `thread-pool` resource of the `ejb3` subsystem
* Retrieve the `rejected-count` attribute value of the `thread-pool` resource of the `ejb3` subsystem
* `rejected-count` attribute description: the number of tasks that have been rejected.
* operation: `/subsystem=ejb3/thread-pool=<thread-pool name>:read-attribute(name=rejected-count)`

## syntax of the operation to get the `task-count` attribute of the `thread-pool` resource of the `ejb3` subsystem
* Retrieve the `task-count` attribute value of the `thread-pool` resource of the `ejb3` subsystem
* `task-count` attribute description: the approximate total number of tasks that have ever been scheduled for execution.
* operation: `/subsystem=ejb3/thread-pool=<thread-pool name>:read-attribute(name=task-count)`

## syntax of the operation to get the `thread-factory` attribute of the `thread-pool` resource of the `ejb3` subsystem
* Retrieve the `thread-factory` attribute value of the `thread-pool` resource of the `ejb3` subsystem
* `thread-factory` attribute description: specifies the name of a specific thread factory to use to create worker threads. if not defined an appropriate default thread factory will be used.
* operation: `/subsystem=ejb3/thread-pool=<thread-pool name>:read-attribute(name=thread-factory)`

## syntax of the operation to get the `cluster` attribute of the `remote` `service` resource of the `ejb3` subsystem
* Retrieve the `cluster` attribute value of the `remote` `service` resource of the `ejb3` subsystem
* `cluster` attribute description: the name of the clustered cache container which will be used to `store/access` the `client-mappings` of the jakarta enterprise beans `remoting` `connector's` `socket-binding` on each node, in the cluster
* operation: `/subsystem=ejb3/service=remote:read-attribute(name=cluster)`

## syntax of the operation to get the `connector-ref` attribute of the `remote` `service` resource of the `ejb3` subsystem
* Retrieve the `connector-ref` attribute value of the `remote` `service` resource of the `ejb3` subsystem
* `connector-ref` attribute description: the name of the connector on which the enterprise beans `3` `remoting` channel is registered.
* operation: `/subsystem=ejb3/service=remote:read-attribute(name=connector-ref)`

## syntax of the operation to get the `connectors` attribute of the `remote` `service` resource of the `ejb3` subsystem
* Retrieve the `connectors` attribute value of the `remote` `service` resource of the `ejb3` subsystem
* `connectors` attribute description: a list of names of connectors on which the enterprise beans `3` invocations are received.
* operation: `/subsystem=ejb3/service=remote:read-attribute(name=connectors)`

## syntax of the operation to get the `execute-in-worker` attribute of the `remote` `service` resource of the `ejb3` subsystem
* Retrieve the `execute-in-worker` attribute value of the `remote` `service` resource of the `ejb3` subsystem
* `execute-in-worker` attribute description: if this is true the jakarta enterprise beans request will be executed in the io subsystems worker, otherwise it will dispatch to the jakarta enterprise beans thread pool
* operation: `/subsystem=ejb3/service=remote:read-attribute(name=execute-in-worker)`

## syntax of the operation to get the `thread-pool-name` attribute of the `remote` `service` resource of the `ejb3` subsystem
* Retrieve the `thread-pool-name` attribute value of the `remote` `service` resource of the `ejb3` subsystem
* `thread-pool-name` attribute description: the name of the thread pool that handles remote invocations
* operation: `/subsystem=ejb3/service=remote:read-attribute(name=thread-pool-name)`

## syntax of the operation to get a `channel-creation-options` resource of the `remote` `service` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/service=remote/channel-creation-options=<channel-creation-options name>:read-resource()`
* To get the list of all the `channel-creation-options` resource of the `remote` `service` resource of the `ejb3` subsystem use '*' for `<channel-creation-options name>`.

## syntax of the operation to get the `type` attribute of the `channel-creation-options` resource of the `remote` `service` resource of the `ejb3` subsystem
* Retrieve the `type` attribute value of the `channel-creation-options` resource of the `remote` `service` resource of the `ejb3` subsystem
* `type` attribute description: the type of the channel creation option
* operation: `/subsystem=ejb3/service=remote/channel-creation-options=<channel-creation-options name>:read-attribute(name=type)`

## syntax of the operation to get the `value` attribute of the `channel-creation-options` resource of the `remote` `service` resource of the `ejb3` subsystem
* Retrieve the `value` attribute value of the `channel-creation-options` resource of the `remote` `service` resource of the `ejb3` subsystem
* `value` attribute description: the value for the jakarta enterprise beans remote channel creation option
* operation: `/subsystem=ejb3/service=remote/channel-creation-options=<channel-creation-options name>:read-attribute(name=value)`

## syntax of the operation to get the `enable-by-default` attribute of the `iiop` `service` resource of the `ejb3` subsystem
* Retrieve the `enable-by-default` attribute value of the `iiop` `service` resource of the `ejb3` subsystem
* `enable-by-default` attribute description: if this is true jakarta enterprise `beans's` will be exposed over `iiop` by default, otherwise it needs to be explicitly enabled in the deployment descriptor
* operation: `/subsystem=ejb3/service=iiop:read-attribute(name=enable-by-default)`

## syntax of the operation to get the `use-qualified-name` attribute of the `iiop` `service` resource of the `ejb3` subsystem
* Retrieve the `use-qualified-name` attribute value of the `iiop` `service` resource of the `ejb3` subsystem
* `use-qualified-name` attribute description: if true jakarta enterprise beans names will be bound into the naming service with the application and module name prepended to the name (`e.g`. `myapp/mymodule/myejb`)
* operation: `/subsystem=ejb3/service=iiop:read-attribute(name=use-qualified-name)`

## syntax of the operation to get the `default-data-store` attribute of the `timer-service` `service` resource of the `ejb3` subsystem
* Retrieve the `default-data-store` attribute value of the `timer-service` `service` resource of the `ejb3` subsystem
* `default-data-store` attribute description: the default data store used for persistent timers
* operation: `/subsystem=ejb3/service=timer-service:read-attribute(name=default-data-store)`

## syntax of the operation to get the `default-persistent-timer-management` attribute of the `timer-service` `service` resource of the `ejb3` subsystem
* Retrieve the `default-persistent-timer-management` attribute value of the `timer-service` `service` resource of the `ejb3` subsystem
* `default-persistent-timer-management` attribute description: the default timer management to use for persistent timers.
* operation: `/subsystem=ejb3/service=timer-service:read-attribute(name=default-persistent-timer-management)`

## syntax of the operation to get the `default-transient-timer-management` attribute of the `timer-service` `service` resource of the `ejb3` subsystem
* Retrieve the `default-transient-timer-management` attribute value of the `timer-service` `service` resource of the `ejb3` subsystem
* `default-transient-timer-management` attribute description: the default timer management to use for transient timers.
* operation: `/subsystem=ejb3/service=timer-service:read-attribute(name=default-transient-timer-management)`

## syntax of the operation to get the `thread-pool-name` attribute of the `timer-service` `service` resource of the `ejb3` subsystem
* Retrieve the `thread-pool-name` attribute value of the `timer-service` `service` resource of the `ejb3` subsystem
* `thread-pool-name` attribute description: the name of the thread pool used to run timer service invocations
* operation: `/subsystem=ejb3/service=timer-service:read-attribute(name=thread-pool-name)`

## syntax of the operation to get a `database-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/service=timer-service/database-data-store=<database-data-store name>:read-resource()`
* To get the list of all the `database-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem use '*' for `<database-data-store name>`.

## syntax of the operation to get the `allow-execution` attribute of the `database-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* Retrieve the `allow-execution` attribute value of the `database-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* `allow-execution` attribute description: if this node is allowed to execute timers. if this is false then the timers will be added to the database, and another node may execute them. note that depending on your refresh interval if you add timers with a very short delay they will not be executed until another node refreshes.
* operation: `/subsystem=ejb3/service=timer-service/database-data-store=<database-data-store name>:read-attribute(name=allow-execution)`

## syntax of the operation to get the `database` attribute of the `database-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* Retrieve the `database` attribute value of the `database-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* `database` attribute description: the type of database that is in use. `sql` can be `customised` per database type. common values are: `postgresql`, `mysql`, `mariadb`, `db2`, `hsql`, `h2`, oracle, `mssql` and `sybase`.
* operation: `/subsystem=ejb3/service=timer-service/database-data-store=<database-data-store name>:read-attribute(name=database)`

## syntax of the operation to get the `datasource-jndi-name` attribute of the `database-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* Retrieve the `datasource-jndi-name` attribute value of the `database-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* `datasource-jndi-name` attribute description: the `datasource` that is used to persist the timers
* operation: `/subsystem=ejb3/service=timer-service/database-data-store=<database-data-store name>:read-attribute(name=datasource-jndi-name)`

## syntax of the operation to get the `partition` attribute of the `database-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* Retrieve the `partition` attribute value of the `database-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* `partition` attribute description: the partition name. this should be set to a different value for every node that is sharing a database to prevent the same timer being loaded by multiple noded.
* operation: `/subsystem=ejb3/service=timer-service/database-data-store=<database-data-store name>:read-attribute(name=partition)`

## syntax of the operation to get the `refresh-interval` attribute of the `database-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* Retrieve the `refresh-interval` attribute value of the `database-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* `refresh-interval` attribute description: interval between refreshing the current timer set against the underlying database. a low value means timers get picked up more quickly, but increase load on the database.
* operation: `/subsystem=ejb3/service=timer-service/database-data-store=<database-data-store name>:read-attribute(name=refresh-interval)`

## syntax of the operation to get a `file-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/service=timer-service/file-data-store=<file-data-store name>:read-resource()`
* To get the list of all the `file-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem use '*' for `<file-data-store name>`.

## syntax of the operation to get the `path` attribute of the `file-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* Retrieve the `path` attribute value of the `file-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* `path` attribute description: the directory to store persistent timer information in
* operation: `/subsystem=ejb3/service=timer-service/file-data-store=<file-data-store name>:read-attribute(name=path)`

## syntax of the operation to get the `relative-to` attribute of the `file-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* Retrieve the `relative-to` attribute value of the `file-data-store` resource of the `timer-service` `service` resource of the `ejb3` subsystem
* `relative-to` attribute description: the relative path that is used to resolve the timer data store location
* operation: `/subsystem=ejb3/service=timer-service/file-data-store=<file-data-store name>:read-attribute(name=relative-to)`

## syntax of the operation to get the `outflow-security-domains` attribute of the `identity` `service` resource of the `ejb3` subsystem
* Retrieve the `outflow-security-domains` attribute value of the `identity` `service` resource of the `ejb3` subsystem
* `outflow-security-domains` attribute description: references to security domains to attempt to outflow any established identity to
* operation: `/subsystem=ejb3/service=identity:read-attribute(name=outflow-security-domains)`

## syntax of the operation to get the `thread-pool-name` attribute of the `async` `service` resource of the `ejb3` subsystem
* Retrieve the `thread-pool-name` attribute value of the `async` `service` resource of the `ejb3` subsystem
* `thread-pool-name` attribute description: the name of the thread pool which handles asynchronous invocations
* operation: `/subsystem=ejb3/service=async:read-attribute(name=thread-pool-name)`

## syntax of the operation to get a `cluster-passivation-store` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/cluster-passivation-store=<cluster-passivation-store name>:read-resource()`
* To get the list of all the `cluster-passivation-store` resource of the `ejb3` subsystem use '*' for `<cluster-passivation-store name>`.

## syntax of the operation to get the `bean-cache` attribute of the `cluster-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `bean-cache` attribute value of the `cluster-passivation-store` resource of the `ejb3` subsystem
* `bean-cache` attribute description: the name of the cache used to store bean instances.
* operation: `/subsystem=ejb3/cluster-passivation-store=<cluster-passivation-store name>:read-attribute(name=bean-cache)`

## syntax of the operation to get the `cache-container` attribute of the `cluster-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `cache-container` attribute value of the `cluster-passivation-store` resource of the `ejb3` subsystem
* `cache-container` attribute description: the name of the cache container used for the bean and `client-mappings` caches
* operation: `/subsystem=ejb3/cluster-passivation-store=<cluster-passivation-store name>:read-attribute(name=cache-container)`

## syntax of the operation to get the `client-mappings-cache` attribute of the `cluster-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `client-mappings-cache` attribute value of the `cluster-passivation-store` resource of the `ejb3` subsystem
* `client-mappings-cache` attribute description: the name of the cache used to store `client-mappings` of the jakarta enterprise beans `remoting` `connector's` `socket-bindings`
* operation: `/subsystem=ejb3/cluster-passivation-store=<cluster-passivation-store name>:read-attribute(name=client-mappings-cache)`

## syntax of the operation to get the `idle-timeout` attribute of the `cluster-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `idle-timeout` attribute value of the `cluster-passivation-store` resource of the `ejb3` subsystem
* `idle-timeout` attribute description: the timeout in units specified by `idle-timeout-unit`, after which a bean will passivate
* operation: `/subsystem=ejb3/cluster-passivation-store=<cluster-passivation-store name>:read-attribute(name=idle-timeout)`

## syntax of the operation to get the `idle-timeout-unit` attribute of the `cluster-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `idle-timeout-unit` attribute value of the `cluster-passivation-store` resource of the `ejb3` subsystem
* `idle-timeout-unit` attribute description: the unit of `idle-timeout`
* operation: `/subsystem=ejb3/cluster-passivation-store=<cluster-passivation-store name>:read-attribute(name=idle-timeout-unit)`

## syntax of the operation to get the `max-size` attribute of the `cluster-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `max-size` attribute value of the `cluster-passivation-store` resource of the `ejb3` subsystem
* `max-size` attribute description: the maximum number of beans this cache should store before forcing old beans to passivate
* operation: `/subsystem=ejb3/cluster-passivation-store=<cluster-passivation-store name>:read-attribute(name=max-size)`

## syntax of the operation to get the `passivate-events-on-replicate` attribute of the `cluster-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `passivate-events-on-replicate` attribute value of the `cluster-passivation-store` resource of the `ejb3` subsystem
* `passivate-events-on-replicate` attribute description: indicates whether replication should trigger passivation events on the bean
* operation: `/subsystem=ejb3/cluster-passivation-store=<cluster-passivation-store name>:read-attribute(name=passivate-events-on-replicate)`

## syntax of the operation to get a `simple-cache` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/simple-cache=<simple-cache name>:read-resource()`
* To get the list of all the `simple-cache` resource of the `ejb3` subsystem use '*' for `<simple-cache name>`.

## syntax of the operation to get a `distributable-cache` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/distributable-cache=<distributable-cache name>:read-resource()`
* To get the list of all the `distributable-cache` resource of the `ejb3` subsystem use '*' for `<distributable-cache name>`.

## syntax of the operation to get the `bean-management` attribute of the `distributable-cache` resource of the `ejb3` subsystem
* Retrieve the `bean-management` attribute value of the `distributable-cache` resource of the `ejb3` subsystem
* `bean-management` attribute description: name of the bean management provider for the distributable cache
* operation: `/subsystem=ejb3/distributable-cache=<distributable-cache name>:read-attribute(name=bean-management)`

## syntax of the operation to get a `file-passivation-store` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/file-passivation-store=<file-passivation-store name>:read-resource()`
* To get the list of all the `file-passivation-store` resource of the `ejb3` subsystem use '*' for `<file-passivation-store name>`.

## syntax of the operation to get the `groups-path` attribute of the `file-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `groups-path` attribute value of the `file-passivation-store` resource of the `ejb3` subsystem
* `groups-path` attribute description: the subdirectory within the path specified by `relative-to` in which to store `passivated` bean groups
* operation: `/subsystem=ejb3/file-passivation-store=<file-passivation-store name>:read-attribute(name=groups-path)`

## syntax of the operation to get the `idle-timeout` attribute of the `file-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `idle-timeout` attribute value of the `file-passivation-store` resource of the `ejb3` subsystem
* `idle-timeout` attribute description: the timeout in units specified by `idle-timeout-unit`, after which a bean will passivate
* operation: `/subsystem=ejb3/file-passivation-store=<file-passivation-store name>:read-attribute(name=idle-timeout)`

## syntax of the operation to get the `idle-timeout-unit` attribute of the `file-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `idle-timeout-unit` attribute value of the `file-passivation-store` resource of the `ejb3` subsystem
* `idle-timeout-unit` attribute description: the unit of `idle-timeout`
* operation: `/subsystem=ejb3/file-passivation-store=<file-passivation-store name>:read-attribute(name=idle-timeout-unit)`

## syntax of the operation to get the `max-size` attribute of the `file-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `max-size` attribute value of the `file-passivation-store` resource of the `ejb3` subsystem
* `max-size` attribute description: the maximum number of beans this cache should store before forcing old beans to passivate
* operation: `/subsystem=ejb3/file-passivation-store=<file-passivation-store name>:read-attribute(name=max-size)`

## syntax of the operation to get the `relative-to` attribute of the `file-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `relative-to` attribute value of the `file-passivation-store` resource of the `ejb3` subsystem
* `relative-to` attribute description: the root path used to store `passivated` beans
* operation: `/subsystem=ejb3/file-passivation-store=<file-passivation-store name>:read-attribute(name=relative-to)`

## syntax of the operation to get the `sessions-path` attribute of the `file-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `sessions-path` attribute value of the `file-passivation-store` resource of the `ejb3` subsystem
* `sessions-path` attribute description: the subdirectory within the path specified by `relative-to` in which to store `passivated` beans
* operation: `/subsystem=ejb3/file-passivation-store=<file-passivation-store name>:read-attribute(name=sessions-path)`

## syntax of the operation to get the `subdirectory-count` attribute of the `file-passivation-store` resource of the `ejb3` subsystem
* Retrieve the `subdirectory-count` attribute value of the `file-passivation-store` resource of the `ejb3` subsystem
* `subdirectory-count` attribute description: specifies the number of subdirectories into which stored state should be divided, used to minimize the number of files created per directory
* operation: `/subsystem=ejb3/file-passivation-store=<file-passivation-store name>:read-attribute(name=subdirectory-count)`

## syntax of the operation to get a `strict-max-bean-instance-pool` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/strict-max-bean-instance-pool=<strict-max-bean-instance-pool name>:read-resource()`
* To get the list of all the `strict-max-bean-instance-pool` resource of the `ejb3` subsystem use '*' for `<strict-max-bean-instance-pool name>`.

## syntax of the operation to get the `derive-size` attribute of the `strict-max-bean-instance-pool` resource of the `ejb3` subsystem
* Retrieve the `derive-size` attribute value of the `strict-max-bean-instance-pool` resource of the `ejb3` subsystem
* `derive-size` attribute description: specifies if and what the max pool size should be derived from. an undefined value (or the deprecated value 'none' which is converted to undefined) indicates that the explicit value of `max-pool-size` should be used. a value of '`from-worker-pools`' indicates that the max pool size should be derived from the size of the total threads for all worker pools configured on the system. a value of '`from-cpu-count`' indicates that the max pool size should be derived from the total number of processors available on the system. note that the computation `isn't` a `1:1` mapping, the values may or may not be augmented by other factors.
* operation: `/subsystem=ejb3/strict-max-bean-instance-pool=<strict-max-bean-instance-pool name>:read-attribute(name=derive-size)`

## syntax of the operation to get the `derived-size` attribute of the `strict-max-bean-instance-pool` resource of the `ejb3` subsystem
* Retrieve the `derived-size` attribute value of the `strict-max-bean-instance-pool` resource of the `ejb3` subsystem
* `derived-size` attribute description: derived maximum number of bean instances that the pool can hold at a given point in time
* operation: `/subsystem=ejb3/strict-max-bean-instance-pool=<strict-max-bean-instance-pool name>:read-attribute(name=derived-size)`

## syntax of the operation to get the `max-pool-size` attribute of the `strict-max-bean-instance-pool` resource of the `ejb3` subsystem
* Retrieve the `max-pool-size` attribute value of the `strict-max-bean-instance-pool` resource of the `ejb3` subsystem
* `max-pool-size` attribute description: configured maximum number of bean instances that the pool can hold at a given point in time
* operation: `/subsystem=ejb3/strict-max-bean-instance-pool=<strict-max-bean-instance-pool name>:read-attribute(name=max-pool-size)`

## syntax of the operation to get the `timeout` attribute of the `strict-max-bean-instance-pool` resource of the `ejb3` subsystem
* Retrieve the `timeout` attribute value of the `strict-max-bean-instance-pool` resource of the `ejb3` subsystem
* `timeout` attribute description: the maximum amount of time to wait for a bean instance to be available from the pool
* operation: `/subsystem=ejb3/strict-max-bean-instance-pool=<strict-max-bean-instance-pool name>:read-attribute(name=timeout)`

## syntax of the operation to get the `timeout-unit` attribute of the `strict-max-bean-instance-pool` resource of the `ejb3` subsystem
* Retrieve the `timeout-unit` attribute value of the `strict-max-bean-instance-pool` resource of the `ejb3` subsystem
* `timeout-unit` attribute description: the instance acquisition timeout unit
* operation: `/subsystem=ejb3/strict-max-bean-instance-pool=<strict-max-bean-instance-pool name>:read-attribute(name=timeout-unit)`

## syntax of the operation to get a `passivation-store` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/passivation-store=<passivation-store name>:read-resource()`
* To get the list of all the `passivation-store` resource of the `ejb3` subsystem use '*' for `<passivation-store name>`.

## syntax of the operation to get the `bean-cache` attribute of the `passivation-store` resource of the `ejb3` subsystem
* Retrieve the `bean-cache` attribute value of the `passivation-store` resource of the `ejb3` subsystem
* `bean-cache` attribute description: the name of the cache used to store bean instances.
* operation: `/subsystem=ejb3/passivation-store=<passivation-store name>:read-attribute(name=bean-cache)`

## syntax of the operation to get the `cache-container` attribute of the `passivation-store` resource of the `ejb3` subsystem
* Retrieve the `cache-container` attribute value of the `passivation-store` resource of the `ejb3` subsystem
* `cache-container` attribute description: the name of the cache container used for the bean and `client-mappings` caches
* operation: `/subsystem=ejb3/passivation-store=<passivation-store name>:read-attribute(name=cache-container)`

## syntax of the operation to get the `max-size` attribute of the `passivation-store` resource of the `ejb3` subsystem
* Retrieve the `max-size` attribute value of the `passivation-store` resource of the `ejb3` subsystem
* `max-size` attribute description: the maximum number of beans this cache should store before forcing old beans to passivate
* operation: `/subsystem=ejb3/passivation-store=<passivation-store name>:read-attribute(name=max-size)`

## syntax of the operation to get a `cache` resource of the `ejb3` subsystem
* operation: `/subsystem=ejb3/cache=<cache name>:read-resource()`
* To get the list of all the `cache` resource of the `ejb3` subsystem use '*' for `<cache name>`.

## syntax of the operation to get the `aliases` attribute of the `cache` resource of the `ejb3` subsystem
* Retrieve the `aliases` attribute value of the `cache` resource of the `ejb3` subsystem
* `aliases` attribute description: the aliases by which this cache may also be referenced
* operation: `/subsystem=ejb3/cache=<cache name>:read-attribute(name=aliases)`

## syntax of the operation to get the `passivation-store` attribute of the `cache` resource of the `ejb3` subsystem
* Retrieve the `passivation-store` attribute value of the `cache` resource of the `ejb3` subsystem
* `passivation-store` attribute description: the passivation store used by this cache
* operation: `/subsystem=ejb3/cache=<cache name>:read-attribute(name=passivation-store)`

## syntax of the operation to get the `lra-coordinator-url` attribute of the `microprofile-lra-participant` subsystem
* Retrieve the `lra-coordinator-url` attribute value of the `microprofile-lra-participant` subsystem
* `lra-coordinator-url` attribute description: configuration of the `lra-coordinator-url`, used by the `lra` participant subsystem.
* operation: `/subsystem=microprofile-lra-participant:read-attribute(name=lra-coordinator-url)`

## syntax of the operation to get the `proxy-host` attribute of the `microprofile-lra-participant` subsystem
* Retrieve the `proxy-host` attribute value of the `microprofile-lra-participant` subsystem
* `proxy-host` attribute description: name of the undertow subsystem 'host' resource that the `lra` participant proxy will be deployed to.
* operation: `/subsystem=microprofile-lra-participant:read-attribute(name=proxy-host)`

## syntax of the operation to get the `proxy-server` attribute of the `microprofile-lra-participant` subsystem
* Retrieve the `proxy-server` attribute value of the `microprofile-lra-participant` subsystem
* `proxy-server` attribute description: name of the undertow subsystem 'server' resource that the `lra` participant proxy will be deployed to.
* operation: `/subsystem=microprofile-lra-participant:read-attribute(name=proxy-server)`

## syntax of the operation to get the `exposed-subsystems` attribute of the `microprofile-metrics-smallrye` subsystem
* Retrieve the `exposed-subsystems` attribute value of the `microprofile-metrics-smallrye` subsystem
* `exposed-subsystems` attribute description: the names of the `wildfly` subsystems that exposes their metrics (or '*' to expose any subsystem metrics).
* operation: `/subsystem=microprofile-metrics-smallrye:read-attribute(name=exposed-subsystems)`

## syntax of the operation to get the `prefix` attribute of the `microprofile-metrics-smallrye` subsystem
* Retrieve the `prefix` attribute value of the `microprofile-metrics-smallrye` subsystem
* `prefix` attribute description: prefix prepended to the name of the `wildfly` metrics exposed by the `http` endpoints.
* operation: `/subsystem=microprofile-metrics-smallrye:read-attribute(name=prefix)`

## syntax of the operation to get the `security-enabled` attribute of the `microprofile-metrics-smallrye` subsystem
* Retrieve the `security-enabled` attribute value of the `microprofile-metrics-smallrye` subsystem
* `security-enabled` attribute description: true if authentication is required to access the `http` endpoint on the `http` management `interface.this` attribute overrides the value specified in the `security-enabled` attribute of the /`subsystem=metrics` resource.
* operation: `/subsystem=microprofile-metrics-smallrye:read-attribute(name=security-enabled)`

## syntax of the operation to get the `amqp-connector` attribute of the `config` `opentelemetry-tracing` resource of the `microprofile-reactive-messaging-smallrye` subsystem
* Retrieve the `amqp-connector` attribute value of the `config` `opentelemetry-tracing` resource of the `microprofile-reactive-messaging-smallrye` subsystem
* `amqp-connector` attribute description: configure `opentelemetry` tracing for the `ampq` connector
* operation: `/subsystem=microprofile-reactive-messaging-smallrye/opentelemetry-tracing=config:read-attribute(name=amqp-connector)`

## syntax of the operation to get the `kafka-connector` attribute of the `config` `opentelemetry-tracing` resource of the `microprofile-reactive-messaging-smallrye` subsystem
* Retrieve the `kafka-connector` attribute value of the `config` `opentelemetry-tracing` resource of the `microprofile-reactive-messaging-smallrye` subsystem
* `kafka-connector` attribute description: configure `opentelemetry` tracing for the kafka connector
* operation: `/subsystem=microprofile-reactive-messaging-smallrye/opentelemetry-tracing=config:read-attribute(name=kafka-connector)`

